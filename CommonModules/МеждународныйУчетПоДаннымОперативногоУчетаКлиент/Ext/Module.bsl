#Область ПрограммныйИнтерфейс

// Открывает форму настройки шаблонов проводок по данным оперативного учета.
// 
// Параметры:
// 	УчетнаяПолитика - СправочникСсылка.УчетныеПолитики,
// 	                  Неопределено - Учетная политика, для которой необходимо выполнить настройку.
// 	                                 Если Неопределено, то форма открывается для настройки по всем учетным политикам.
// 	ХозяйственнаяОперация - СправочникСсылка.НастройкиХозяйственныхОпераций,
// 	                        Неопределено - Операция, для которой необходимо выполнить настройку.
// 	                                       Если Неопределено, тогда форма открывается без отбора по операции.
// 	ФормаВладелец - ФормаКлиентскогоПриложения - Владелец формы настройки.
//
Процедура ОткрытьНастройкуШаблоновПроводок(УчетнаяПолитика, ХозяйственнаяОперация, ФормаВладелец) Экспорт
	
	ПараметрыФормы = Новый Структура;
	Если УчетнаяПолитика <> Неопределено Тогда
		ПараметрыФормы.Вставить("УчетнаяПолитика", УчетнаяПолитика);
	КонецЕсли;
	Если ХозяйственнаяОперация <> Неопределено Тогда
		ПараметрыФормы.Вставить("ХозяйственнаяОперация", ХозяйственнаяОперация);
	КонецЕсли;
	ОткрытьФорму("Обработка.НастройкаШаблоновПроводокДляМеждународногоУчета.Форма.НастройкаШаблоновПроводок", 
		ПараметрыФормы, ФормаВладелец, , , , ,РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

#КонецОбласти