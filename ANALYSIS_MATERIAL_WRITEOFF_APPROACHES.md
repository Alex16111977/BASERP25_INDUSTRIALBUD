# Аналіз підходів до списання матеріалів у виробництво

## Дата аналізу: 2025-12-17

## Контекст проблеми

При спробі провести документ `СписаниеЗатратНаВыпуск`, створений на основі `ПроизводствоБезЗаказа`, виникає помилка:

```
Запись не верна! Значение поля 'Распоряжение' не может быть пустым!
(Регистр накопления: РаспоряженияНаСписаниеПоНормативам; Номер строки: 1)
```

## Знайдена помилка в попередньому аналізі

Попередній аналіз (ANALYSIS_DOCUMENT_POSTING_LOGIC.md) **НЕ врахував**:
1. Регістр `РаспоряженияНаСписаниеПоНормативам`
2. Обов'язкове поле `Распоряжение` типу `DocumentRef.ВыпускПродукции`
3. Архітектурну різницю між workflow "з замовленням" та "без замовлення"

## Архітектурний аналіз

### Workflow "Виробництво ЗА замовленням"
```
ЗаказНаПроизводство → ВыпускПродукции → СписаниеЗатратНаВыпуск
                              ↓
                    (Распоряжение = ВыпускПродукции)
```

### Workflow "Виробництво БЕЗ замовлення"
```
ПроизводствоБезЗаказа → (НЕ створює ВыпускПродукции!)
         ↓
    [Списання через ТЧ МатериалыИРаботы]
```

## Структура регістра РаспоряженияНаСписаниеПоНормативам

```xml
<Dimension uuid="ab7168a5-417a-467a-a5c7-5d4c3886f5ac">
  <Properties>
    <Name>Распоряжение</Name>
    <Type>
      <v8:Type>cfg:DocumentRef.ВыпускПродукции</v8:Type>
    </Type>
    <FillChecking>ShowError</FillChecking>
    <DenyIncompleteValues>true</DenyIncompleteValues>  <!-- КРИТИЧНО! -->
  </Properties>
</Dimension>
```

## Аналіз двох підходів

### ПІДХІД A: Заповнення МатериалыИРаботы в ПроизводствоБезЗаказа

**Опис:** Замість створення СписаниеЗатратНаВыпуск, заповнювати ТЧ `МатериалыИРаботы` безпосередньо в документі `ПроизводствоБезЗаказа`.

**Як це працює:**
1. Документ `ПроизводствоБезЗаказа` має ТЧ `МатериалыИРаботы` (xml line 4961)
2. При проведенні викликається функція `ТекстЗапросаТаблицаМатериалыИРаботыВПроизводстве` (ManagerModule.bsl:4639)
3. Дані з ТЧ записуються в регістр `МатериалыИРаботыВПроизводстве`
4. Списання відбувається в цьому ж документі!

**Переваги:**
- ✅ Повністю відповідає архітектурі BAS ERP 2.5
- ✅ Не потребує документа ВыпускПродукции
- ✅ Один документ замість двох
- ✅ Простіше для користувача

**Недоліки:**
- ⚠️ Потрібно перепровести ПроизводствоБезЗаказа після заповнення

### ПІДХІД B: Виправлення обробки для заповнення Распоряжение

**Опис:** Спробувати заповнити поле `Распоряжение` в СписаниеЗатратНаВыпуск.

**Проблема:**
1. Поле `Распоряжение` має тип `DocumentRef.ВыпускПродукции`
2. Документ `ПроизводствоБезЗаказа` **НЕ СТВОРЮЄ** документ `ВыпускПродукции`
3. Немає валідного значення для заповнення!

**Висновок:** ❌ **НЕМОЖЛИВО** використовувати СписаниеЗатратНаВыпуск для workflow "без замовлення"

## РЕКОМЕНДАЦІЯ

### Використовувати ПІДХІД A

**Причини:**
1. Підхід B технічно неможливий
2. Підхід A відповідає проектній архітектурі BAS ERP 2.5
3. При проведенні ПроизводствоБезЗаказа з заповненою ТЧ МатериалыИРаботы:
   - Матеріали списуються напряму
   - Не потрібен додатковий документ СписаниеЗатратНаВыпуск

## Створена нова обробка

**Шлях:** `_Rarzrabotki/Обработки/Заполнить МатериалыИРаботы в ПроизводствоБезЗаказа/`

**Функціонал:**
1. Вибір документа ПроизводствоБезЗаказа
2. Попередній перегляд матеріалів з регістра
3. Заповнення ТЧ МатериалыИРаботы
4. Опція очищення ТЧ перед заповненням

**Джерело даних:** Регістр `МатериалыИРаботыВПроизводстве.Остатки` за організацією та підрозділом

## Файли

| Файл | Призначення |
|------|-------------|
| `Заполнить МатериалыИРаботы в ПроизводствоБезЗаказа.xml` | Метадані обробки |
| `Forms/Форма.xml` | Метадані форми |
| `Forms/Форма/Ext/Form/Module.bsl` | Бізнес-логіка |

## Коректний порядок дій для користувача

1. Створити та провести `ПроизводствоБезЗаказа` (з ВыходныеИзделия)
2. Відкрити зовнішню обробку `Заполнить МатериалыИРаботы в ПроизводствоБезЗаказа`
3. Вибрати документ ПроизводствоБезЗаказа
4. Натиснути "Перегляд матеріалів" для перевірки
5. Натиснути "Заповнити матеріали"
6. Перевірити заповнені дані в документі
7. **Перепровести** документ ПроизводствоБезЗаказа

## Стара обробка

Обробка `Создать СписаниеЗатратНаВыпуск по выпуску` залишена для довідки, але вона **НЕ ПРАЦЮЄ** для workflow "без замовлення" через архітектурні обмеження.
