
Процедура ПередЗаписью(Отказ)

	// Автозаповнення СотрудникЕРП за алгоритмом документа А_ОтражениеЗПпоКазне:
	//   ИНН → Справочник.Сотрудники.А_КодПоДРФО (прямий матч),
	//   fallback — код Казни → ФізЛицо → Сотрудник.
	// LINK-ONLY: нічого не створюється; уже заповнений (у т.ч. ручний) зв'язок не затирається.
	// Без гарда ОбменДанными.Загрузка — щоб запис самозаповнювався і при обміні з Казни.
	Если ЭтоГруппа Тогда
		Возврат;
	КонецЕсли;
	Если ЗначениеЗаполнено(СотрудникЕРП) Тогда
		Возврат;
	КонецЕсли;
	СотрудникЕРП = А_Привилегированный.НайтиСотрудникаЕРПпоДаннымКазны(ИНН, Код);

КонецПроцедуры
