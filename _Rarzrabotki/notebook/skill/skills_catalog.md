# Каталог навыков Claude Code для 1С (cc-1c-skills)

> Автоматически собрано из `.claude/skills/*/SKILL.md`
> Дата: 2026-04-12

---

## 1. Разработка и стандарты BSL

| Навык | Описание |
|-------|----------|
| `1c-bsl-review` | Code Review BSL-кода по 17-пунктному чеклисту (3 уровня критичности) |
| `1c-bsl-standards` | Стандарты и правила кода BSL (1С:Предприятие 8.3 / BAS ERP 2.5) |
| `1c-query-optimizer` | Анализ и оптимизация запросов 1С по 10 правилам |
| `1c-developer` | Карта компетенций 1С-разработчика middle-senior (BSL / 8.3) |

## 2. Конфигурация (CF)

| Навык | Описание |
|-------|----------|
| `cf-edit` | Точечное редактирование конфигурации 1С (свойства, состав, роли) |
| `cf-info` | Анализ структуры конфигурации — свойства, состав, счётчики объектов |
| `cf-init` | Создать пустую конфигурацию 1С (scaffold XML-исходников) |
| `cf-validate` | Валидация конфигурации 1С после создания или модификации |

## 3. Расширения (CFE)

| Навык | Описание |
|-------|----------|
| `cfe-borrow` | Заимствование объектов из конфигурации в расширение |
| `cfe-diff` | Анализ расширения — состав, заимствованные объекты, перехватчики |
| `cfe-init` | Создать расширение конфигурации 1С (scaffold XML-исходников) |
| `cfe-patch-method` | Генерация перехватчика метода в расширении (&Перед/&После/&Вместо) |
| `cfe-validate` | Валидация расширения конфигурации после создания или модификации |

## 4. Внешние обработки (EPF)

| Навык | Описание |
|-------|----------|
| `epf-add-form` | Добавить управляемую форму к внешней обработке |
| `epf-bsp-add-command` | Добавить команду в дополнительную обработку БСП |
| `epf-bsp-init` | Добавить функцию регистрации БСП (СведенияОВнешнейОбработке) |
| `epf-build` | Собрать EPF/ERF из XML-исходников |
| `epf-dump` | Разобрать EPF-файл в XML-исходники |
| `epf-init` | Создать пустую внешнюю обработку (scaffold) |
| `epf-validate` | Валидация внешней обработки после создания или модификации |

## 5. Внешние отчёты (ERF)

| Навык | Описание |
|-------|----------|
| `erf-build` | Собрать ERF из XML-исходников |
| `erf-dump` | Разобрать ERF-файл в XML-исходники |
| `erf-init` | Создать пустой внешний отчёт (scaffold) |
| `erf-validate` | Валидация внешнего отчёта после создания или модификации |

## 6. Управляемые формы

| Навык | Описание |
|-------|----------|
| `form-add` | Добавить управляемую форму к объекту конфигурации |
| `form-compile` | Компиляция формы из компактного JSON-определения |
| `form-edit` | Добавление элементов, реквизитов и команд в существующую форму |
| `form-info` | Анализ структуры формы (Form.xml) — элементы, реквизиты, команды |
| `form-patterns` | Справочник паттернов компоновки управляемых форм |
| `form-remove` | Удалить форму из объекта 1С |
| `form-validate` | Валидация управляемой формы после создания или модификации |

## 7. Объекты метаданных

| Навык | Описание |
|-------|----------|
| `meta-compile` | Создать объект метаданных (справочник, документ, регистр и др.) |
| `meta-edit` | Точечное редактирование — реквизиты, табличные части, измерения |
| `meta-info` | Анализ структуры объекта из XML-выгрузки |
| `meta-remove` | Удалить объект метаданных из конфигурации |
| `meta-validate` | Валидация объекта после создания или модификации |

## 8. Подсистемы и интерфейс

| Навык | Описание |
|-------|----------|
| `subsystem-compile` | Создать подсистему из JSON-определения |
| `subsystem-edit` | Точечное редактирование подсистемы (состав, дочерние) |
| `subsystem-info` | Анализ структуры подсистемы — состав, иерархия |
| `subsystem-validate` | Валидация подсистемы |
| `interface-edit` | Настройка командного интерфейса подсистемы |
| `interface-validate` | Валидация командного интерфейса |

## 9. Роли и права

| Навык | Описание |
|-------|----------|
| `role-compile` | Создание роли из описания прав |
| `role-info` | Компактная сводка прав роли — объекты, права, RLS |
| `role-validate` | Валидация роли |

## 10. Схемы компоновки данных (СКД)

| Навык | Описание |
|-------|----------|
| `skd-compile` | Компиляция СКД из компактного JSON-определения |
| `skd-edit` | Точечное редактирование СКД (поля, итоги, фильтры, запрос) |
| `skd-info` | Анализ структуры СКД — наборы, поля, параметры, варианты |
| `skd-validate` | Валидация СКД |

## 11. Макеты (MXL)

| Навык | Описание |
|-------|----------|
| `mxl-compile` | Компиляция табличного документа из JSON-определения |
| `mxl-decompile` | Декомпиляция табличного документа в JSON |
| `mxl-info` | Анализ структуры макета — области, параметры, колонки |
| `mxl-validate` | Валидация макета |

## 12. Базы данных

| Навык | Описание |
|-------|----------|
| `db-create` | Создание информационной базы 1С |
| `db-dump-cf` | Выгрузка конфигурации в CF-файл |
| `db-dump-xml` | Выгрузка конфигурации в XML-файлы |
| `db-list` | Управление реестром баз (.v8-project.json) |
| `db-load-cf` | Загрузка конфигурации из CF-файла |
| `db-load-git` | Загрузка изменений из Git в базу |
| `db-load-xml` | Загрузка конфигурации из XML-файлов |
| `db-run` | Запуск 1С:Предприятие |
| `db-update` | Обновление конфигурации базы данных |

## 13. Веб-публикация

| Навык | Описание |
|-------|----------|
| `web-info` | Статус Apache и веб-публикаций 1С |
| `web-publish` | Публикация базы через Apache |
| `web-stop` | Остановка Apache HTTP Server |
| `web-test` | Тестирование 1С через веб-клиент (автоматизация браузера) |
| `web-unpublish` | Удаление веб-публикации из Apache |

## 14. Прочие

| Навык | Описание |
|-------|----------|
| `help-add` | Добавить встроенную справку к объекту 1С |
| `img-grid` | Наложить пронумерованную сетку на изображение (пропорции колонок) |
| `template-add` | Добавить макет к объекту 1С |
| `template-remove` | Удалить макет из объекта 1С |
| `consult-notebooklm` | Консультация с NotebookLM по архитектуре BAS ERP 2.5 |

---

**Итого: 67 навыков** в 14 категориях.
