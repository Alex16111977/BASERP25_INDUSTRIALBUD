# 03. Связанные объекты, выгрузка книги, эталоны, тесты, грабли

## Связанные объекты

| Объект | Роль |
|---|---|
| `Документ.А_ВводОстатковОтчетаPL` | точка фиксации накопительных (30 док. на 30.06.2026, по-листово) |
| `РН.А_ОстаткиОтчетаPL` | хранилище остатков (обороты), читается веткой 890000/900000 |
| `Документ.А_ОтчетPL` | помесячный факт финансиста; ТЧ ДанныеОтчета (поля `СуммаФома1` — опечатка историческая, `СуммаФорма2`, Статья, Комментарий); шапка: Подразделение, ВключатьДочерние (слой) |
| `Справочник.А_Статьи_PL` / `А_ГруппаСтатей_PL` | статьи и группы листа; реквизиты Сорт, НаименованиеДляОтчетаНаРусском |
| `Справочник.СтруктураПредприятия` | подразделения; реквизит `А_НаправлениеДеятельности` |
| РН ЕРП: `ПрочиеРасходы`, `ВыручкаИСебестоимостьПродаж`, `ПрочиеДоходы` | факт последнего месяца |
| `Документ.А_БюджетМесяц` (ВидПериода=Объект) | план объекта (колонка 900100) |
| `Отчет.А_ОтчетPL_ПолныйЕРП` | отчёт-родитель БЕЗ механизма остатков (накопительные из всей истории документов); модуль отличается только запросом (v11) и отсутствием `МесяцДокументаОстатков` |
| `РС.А_СтруктураВыгрузкиОтчетаPLвЕксел` | состав листов книги Excel (лист = имя + подразделение/направление + вариант + флаги ОтчетПоНаправлению/ТолькоЭтоПодразделение/Выгружать) |
| Обработка `ВыгрузкаОтчетаPLвExcel` (.epf) | книга Excel по листам — строит ИМЕННО ЭТОТ отчёт |

## Выгрузка книги Excel (потребитель №1)

`_Rarzrabotki/Обработки/ВыгрузкаОтчетаPLвExcel/Ext/ObjectModule.bsl`:
`Отчеты.А_ОтчетPL_ПолныйЕРП_Остатки.Создать()`, настройки пользовательского
варианта листа грузятся в компоновщик, подставляется Период и отборы:

- **объектный лист** — подразделение «В группе из списка» (пара отборов из
  варианта: «В списке» + «В группе из списка»);
- **лист «Только это подразделение»** (ЦО-листы) — корень «В списке»;
- **свод направления и PL_Свод** — СЛОЙ СВОДОВ: `УстановитьСводыНаправлений`
  (параметр СводыНаправлений=Истина поверх варианта) + корень направления
  (PL_Свод — корни всех строк-сводов; ЦО отсекается условием «Не в группе ЦО»
  самого варианта). Состав листов книги на цифры сводов НЕ влияет — закрытые
  объекты сидят в свод-остатках корней.

**Варианты книги (с 01.09.2026 — «PL: …», отчёта остатков; регистр перепривязан):**
| Вариант | Отбор | Листы |
|---|---|---|
| PL: Месяц, PL накопительный | пара с ИЛИ («В списке» + «В группе из списка») | объектные |
| PL: Месяц, PL с начала года | пара с ИЛИ | объектные + ЦО-листы (ТолькоЭто: корень «Равно» ставит обработка) |
| PL: Месяц, PL с начала года (для свода) | ОДИН «Подразделение В группе из списка» | своды направлений |
| PL: (без направления ЦО): Месяц, PL с начала года | «В группе из списка» + «Не в группе ЦО» | PL_Свод |
Для сводов одного «В группе из списка» ДОСТАТОЧНО (решение финансиста
01.09.2026): на слое сводов строки лежат на самом корне, «в группе корня» их
пропускает. Обработка поддерживает ОБА стиля: пара → корень «В списке»;
без пары → значение кладётся в одиночный элемент «Подразделение»
(свод направления — корень, PL_Свод — `ЗаполнитьОдиночныйОтборСписком`
корнями строк-сводов). Устаревшего fallback по полю «НаправлениеДеятельности»
больше нет.

## Эталонные цифры (приняты 31.08.2026; строка «Операционный доход», колонка «PL с начала года», июльская книга)

PL_Свод 481 278 773,38; PL Строительство Свод 399 223 454,66;
PL_Производство_СВОД 63 083 595,36; PL_Логистика 3 349 603,38;
Техника 15 622 119,98. Формула эталона: червневые константы книги
(= свод-слой документов А_ОтчетPL, сверено копейка-в-копейку: Строительство
339 313 061,76; Производство 51 004 810,23; Логистика 2 788 550,00; Спецтехника
12 404 410,34) + ЖИВОЙ июль (обороты меняются ежедневно — жёсткие суммы-цели
быстро устаревают, при сверке июль пересчитывать независимым запросом).

## Тесты (Python COM, localhost/BaseERP; все зелёные на 01.09.2026)

| Скрипт (`_Rarzrabotki/Python/test/`) | Что проверяет |
|---|---|
| `test_otchetpl_ostatki_v12_2026-08-31.py` | v12 == v11 агрегатно по колонкам 202607/890000/900000, оба слоя; регистратор остатков в накопительных. ⚠ писался ДО удаления документа «ВНЕ ЛИСТОВ»: объектный слой подразделений без листа теперь расходится с v11 — это by design |
| `acceptance_otchetpl_ostatki_smoke_2026-08-31.py` | этот отчёт против родителя полным путём (вариант+компоновка), ячейка-в-ячейку; перед сверкой отключаются группировки раскрытия (Аналитика/Документ/Контрагент) — расшифровка накопительных различается by design |
| `acceptance_kniga_svody_celi_2026-08-31.py` | свод-листы книги = червень (константы) + живой июль (независимый запрос) |
| `test_procent_zatrat_v13_2026-09-01.py`, `acceptance_procent_zatrat_2026-09-01.py` | «% суммы затрат»: база по отбору, <=100%, регрессия одного объекта |
| `raskladka_ostatkov_po_listam_2026-08-31.py` | раскладчик остатков по документам-листам (идемпотентен по комментарию) |

## Грабли (проверены фактом)

- **Нет проведённого документа остатков** → отчёт падает с исключением — так
  задумано; создать/провести `А_ВводОстатковОтчетаPL`.
- Закрытый объект в интерактивном отборе покажет НУЛИ в накопительных
  (его остатки не зафиксированы) — принято финансистом 31.08.2026.
- Сборка внешнего .erf: при занятом конфигураторе BaseERP собирать через
  `BaseERPRazr` (те же креды). В корневом XML внешнего клона `xr:ClassId` —
  ПЛАТФОРМЕННЫЙ id класса, не регенерировать; `GeneratedType name` и
  `MainDataCompositionSchema` несут имя отчёта.
- Клонирование: UUID регенерировать ТОЛЬКО в корневых XML метаданных; в СКД
  Template.xml UUID не трогать — `dcsset:right` хранит значения отборов вариантов.
- Частичная загрузка в конфигурацию: модули прямым путём; правка СКД —
  корневым `Templates/ОсновнаяСхемаКомпоновкиДанных.xml`.
- Пользовательские варианты — живые данные (могут нести свои группировки,
  например «Аналитика»); приёмки отключают их перед ячеечной сверкой.
- Представления документов в расшифровке: РКО «…ордер N…» (ЛАТИНСКАЯ N);
  АмортизацияОС «Амортизация ОС (2.1) 000Ц-…»; ВнутреннееПотреблениеТоваров имеет
  ДВА представления по операции; ПрочиеДоходыРасходы — «Регистрация расходов».
- Запросы: алиасы В/ПО/ИЗ/КАК запрещены; ИМЕЮЩИЕ не использовать; у
  `Справочник.Организации` нет поля Код. Отбор вариантов по отчёту: поле «Отчет»
  составное — фильтровать через `ВЫРАЗИТЬ(Вар.Отчет КАК
  Справочник.ИдентификаторыОбъектовМетаданных).ПолноеИмя`; поля
  «ВариантПользователя» в справочнике нет.
