# KNOWLEDGE MAP — Отчет.А_ОтчетPL_ПолныйЕРП_Остатки

Entry-point. **Прочитать первым.** База знаний самодостаточна.

## Что это

Конфигурационный отчёт **«PL по объекту (остатки): фикс накопительных из документа
остатков»** (BaseERP, BAS ERP 2.5 INDUSTRIALBUD). Строит PL в форме Excel-листа
«!PL по компании»: строки — статьи PL в водопаде прибыли, колонки — месяцы периода
и накопительные показатели. Существует также как внешний
`_Rarzrabotki/Отчеты/А_ОтчетPL_ПолныйЕРП_Остатки.erf` (те же исходники).

Ключевое отличие от «обычного» PL-отчёта: **накопительные колонки СТРОГО зафиксированы
документами остатков** — история до точки фиксации не пересчитывается из документов,
а берётся из регистра остатков.

## 🔴 Закон источника факта (исключений нет)

| Колонка | Источник |
|---|---|
| ПОСЛЕДНИЙ месяц периода | ТОЛЬКО регистры ЕРП (ПрочиеРасходы, ВыручкаИСебестоимостьПродаж, ПрочиеДоходы) |
| Более ранний месяц | ТОЛЬКО проведённые `Документ.А_ОтчетPL`; месяц без документа = НОЛЬ |
| «PL с начала года» (890000) | РН `А_ОстаткиОтчетаPL` (ресурсы `…СНачалаГода`) + документы А_ОтчетPL ПОСЛЕ `&МесяцОстатков` (с `МесяцКлюч >= &КлючНачалаГода`) + ЕРП последнего месяца |
| «Накопительный PL» (900000) | РН `А_ОстаткиОтчетаPL` (ресурсы `…Накопительно`) + документы А_ОтчетPL ПОСЛЕ `&МесяцОстатков` + ЕРП последнего месяца |
| «План итого» (900100) | `Документ.А_БюджетМесяц` (ВидПериода = Объект) |

`&МесяцОстатков` (ГГГГ×100+ММ) ставит модуль по дате последнего проведённого
`Документ.А_ВводОстатковОтчетаPL`; **нет проведённого документа остатков —
отчёт падает с исключением** (это осознанный контроль, не бага).
Точка фиксации: 30.06.2026 23:59:59 (30 документов, по одному на лист книги выгрузки).

## 🔴 Критические инварианты — НЕ МЕНЯТЬ

1. Закон источника выше. Дыры факта закрываются документами А_ОтчетPL,
   а не кодом отчёта.
2. Слои НЕ складывать: параметр `СводыНаправлений` выбирает слой —
   Ложь = объектные документы (`ВключатьДочерние = Ложь`) и объектный слой
   регистра остатков; Истина = свод-документы корней направлений и свод-слой
   остатков. Свод корня ≠ сумма его объектов (свод-доки содержат закрытые
   объекты, у которых нет объектных строк).
3. `&МесяцОстатков` только из документа остатков (`МесяцДокументаОстатков()`),
   руками не подставлять.
4. База «% суммы затрат» — единая на колонку по отбору компоновщика (см. 02),
   формулу СКД `Максимум(БазаПроцента)` не менять.
5. Опечатка `СуммаФома1` в ТЧ `Документ.А_ОтчетPL.ДанныеОтчета` — историческая,
   НЕ чинить (ключ совместимости запроса).
6. Статья «Дивиденды» (код 000000058) исключена из вывода и из базы процента.

## Живой код — источник истины

| Что | Где |
|---|---|
| Модуль (запрос v12 + вся BSL-логика) | `Reports/А_ОтчетPL_ПолныйЕРП_Остатки/Ext/ObjectModule.bsl` (~2030 строк) |
| СКД (поля, ресурсы, вариант PL_Отчет) | `Reports/А_ОтчетPL_ПолныйЕРП_Остатки/Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml` |
| Копия запроса для Python-тестов | `_Rarzrabotki/Python/test/v12_full.sql` |
| Зеркало-внешний отчёт | `_Rarzrabotki/Отчеты/А_ОтчетPL_ПолныйЕРП_Остатки(.xml/.erf)` |

Формы своих нет: `DefaultForm = CommonForm.ФормаОтчета` (типовой интерфейс отчётов
БСП), «Использовать стандартные команды» включено.

## Файлы базы знаний

| Файл | Что внутри |
|---|---|
| [01_dannye_i_istochniki.md](01_dannye_i_istochniki.md) | Конвейер данных: документы остатков → регистр → запрос v12 → BSL-доработки → внешний набор СКД |
| [02_skd_i_variant.md](02_skd_i_variant.md) | СКД: поля/ресурсы/параметры, вариант PL_Отчет, форма листа (водопад), «% суммы затрат», русский язык |
| [03_svyazi_i_vygruzka.md](03_svyazi_i_vygruzka.md) | Связанные объекты, выгрузка книги Excel, эталонные цифры, тесты, грабли |
