# KNOWLEDGE MAP — knowledge_Бухгалтерия_РасчетКомплектаций

Entry-point для ИИ-сессий. **Прочитать первым** перед любой доработкой документа.

> `Документ.РасчетКомплектаций` (синоним «Списание по СС» / «Списання по СС»).
> База: **BuhBud** — `Srvr="localhost";Ref="bas_industrialbud";Usr="cfo";Pwd="2442"`. Дамп: `_Rarzrabotki/BASEBuh`. НЕ BaseERP.
> Источник истины — **живой код** `_Rarzrabotki/BASEBuh/Documents/РасчетКомплектаций/`, а не эта база знаний.

---

## TL;DR за 60 секунд

Документ — **сохраняемое рабочее место** «розрахунок комплектацій за структурою собівартості». Движок = порт
обработки `СозданиеКомплектацийБух` 1:1; документ добавляет сохранение, статусы, редактируемое понаднормовое
списание, семейство малоценки, дубли и Акт. **Непроводимый** (`Posting=Deny`).

1. **Источник остатков** — `РегистрБухгалтерии.Хозрасчетный.Остатки` по счетам класса 2: `СчетаОстатков`
   (материалы, default группы **20/28**) + `СчетаМалоценки` (default **22**). Субконто1=**Номенклатура**,
   Субконто3=**Склад**. Товарного накопит.регистра в BuhBud НЕТ. Сумма остатка ×1.2 (норма с НДС, счёт без).
2. **Норма** — `Справочник.СтруктураСебестоимости` (реквизит `Спецификация`), ТЧ `Комплектующие`. **Кап** розподілу —
   **Σ `Количество` по `ОбщееНазвание` (эталон)**. Мост карточка→эталон: `Номенклатура.ОбщееНазваниеНоменклатуры`.
   План по номенклатуре списывается первым. Фильтр по `Этап`.
3. **Расчёт** (`РассчитатьАнализ`): остаток → норма эталона → жадное распределение `min(Остаток, ОстатокНормы)`→`ВНорме`,
   излишек→`ПонадНорму`; `Економія = max(0, Норма − Σ Остаток)` на эталон. Результат → ТЧ `ТабличнаяЧастьОстатков`,
   затем **раскладывается в 2 сохраняемые таблицы списания** (`ЗаполнитьТаблицыСписания`). Статус → `РасчетВыполнен`.
4. **Правка** (только `СписаниеСверхНормы`): бухгалтер меняет количество/цену, добавляет строки (счёт/цена
   автоподбираются из остатков — `ДанныеОстаткаДляСтроки`), пишет `Причина`.
5. **Заполнение документов** (`ЗаполнитьДокументыПоАнализу`): читает **сохранённые** `СписаниеПоНормам`/`СписаниеСверхНормы`
   (ручные правки доходят!), роутит: `ПоНормам`→`ДокументПоНормам`, `СверхНормы`→`ДокументДодаткова`; семейство
   по счёту строки (малоценка/комплектация). Пакет пишется **в одной транзакции**. Статус → `ДокументыСозданы`.

6. **Печать М-29** (`СформироватьПечатьМ29`, [14](14_m29_pechat.md)) — единственная печать, которая читает
   **факт бухучёта**: проводки `Хозрасчетный` всех **проведённых** документов расчёта, два блока
   Матеріали/МШП. Не зависит от «Розрахувати» и от ТЧ расчёта вообще.

**Архитектура:** движок в `Ext/ObjectModule.bsl` (экспорт `РассчитатьАнализ`/`ЗаполнитьДокументыПоАнализу`/`СформироватьПечать*`/`ДанныеОстаткаДляСтроки`/`НайтиДубликатыРасчета`),
модуль формы делегирует через `РеквизитФормыВЗначение("Объект")` / `ЗначениеВРеквизитФормы`. **Суммы:**
анализ — **с НДС** (×1.2), таблицы списания и документы — **без НДС** (÷1.2), как на счетах;
**М-29 — прямо из проводок** (в учётной оценке, никаких ×1.2/÷1.2).

---

## Порядок чтения

| # | Файл | Что узнаешь |
|---|---|---|
| 1 | **этот файл** | TL;DR + структура реквизитов/ТЧ + экспорт модуля + отличия документ↔обработка |
| 2 | [01_arhitektura_dokument.md](01_arhitektura_dokument.md) | Документ vs обработка, непроводимый, порт движка, модуль объекта↔форма, 4 макета, регистрация (подсистема/роли), пайплайн создания скриптами |
| 3 | [02_istochniki_zaprosy.md](02_istochniki_zaprosy.md) | **★** Запрос остатков (Хозрасчетный, субконто, В ИЕРАРХИИ, организация, ×1.2), запрос норм (СтруктураСебестоимости), ЕдиницыЭталонов, ДанныеОстаткаДляСтроки |
| 4 | [03_algoritm.md](03_algoritm.md) | **★** Жадное распределение ВНорме/ПонадНорму/Економія, план-приоритет, норм-картки без остатка, инварианты |
| 5 | [04_filtry_scheta.md](04_filtry_scheta.md) | Фильтры: склады, счета комплектации (20/28) + малоценки (22), организация, этапы, счёт-на-строку, классификация семейства |
| 6 | [05_tablicy_spisaniya.md](05_tablicy_spisaniya.md) | **★** `СписаниеПоНормам` (RO) + `СписаниеСверхНормы` (edit), ручной ввод строк, суммы БЕЗ НДС |
| 7 | [06_zapolnenie_dokumentov.md](06_zapolnenie_dokumentov.md) | **★** `ЗаполнитьДокументыПоАнализу` из ТЧ списания, 2 семейства, малоценка-реквизиты, транзакция, структура-результат, авто-склады |
| 8 | [07_statusy_dubli.md](07_statusy_dubli.md) | **★** Перечисление статусов + переходы, `НайтиДубликатыРасчета` + `ПередЗаписью`, `ОбработкаЗаполнения` (дефолты) |
| 9 | [08_pechatnye_formy.md](08_pechatnye_formy.md) | 7 печатей: плоский Друк + 4 СКД + Акт + Розбіжності; НДС; динамический заголовок; рецепт СКД |
| 10 | [09_edinicy_rashozhdenie.md](09_edinicy_rashozhdenie.md) | **★** `РасхождениеЕдиниц` (правило по СокрЛП), подсветка формы, печать розбіжностей |
| 11 | [10_forma_dokumenta.md](10_forma_dokumenta.md) | Форма «живёт своей жизнью», канон «без прокрутки», не перезапускать генераторы, точечные патчи |
| 12 | [11_kak_dorabotat.md](11_kak_dorabotat.md) | **★** Рецепты доработки + чек-лист сборки + пайплайн скриптов |
| 13 | [12_kontrol_edinic_postuplenie.md](12_kontrol_edinic_postuplenie.md) | **★** Контроль единиц/загальних назв при оприходовании `ПоступлениеТоваровУслуг`: подписка `BeforeWrite`, **2 независимых шлюза** (`ЗастосовуватиПеревіркуОдиниць` вниз + `ЗастосовуватиПеревіркуПоЗагальнимНазвам` цепочка вверх), **4 типа нарушений** (`НетЭталона`/`ЭталонНеВСС`/`ПустаяЕдиницаЭталона`/`РазныеЕдиницы`), флаг-исключение `НеПроверятьВЗакупкахпоСС`, блок проведения. ⚠ Отчёт ретро-аудита рассинхронизирован — см. раздел в файле |
| 14 | [13_kolichestvo_k_spisaniyu.md](13_kolichestvo_k_spisaniyu.md) | **★** Списание понаднормы **частями**: `СписаниеСверхНормы.КоличествоКСписанию` + флаг автозаполнения + документы «Додаткова» на КоличествоКСписанию с пропорц.суммой + проверка ≤Количество (✅ 2026-07-21) |
| 15 | [14_m29_pechat.md](14_m29_pechat.md) | **★** Друк **М-29** «Звіт про витрати основних матеріалів»: суми **тільки з проводок** `Хозрасчетный`, два блоки Матеріали/МШП, 13 колонок, адаптивна точність ціни, 7 реквізитів шапки під підписи (✅ 2026-08-03) |
| 16 | [LESSONS.md](LESSONS.md) | **★★** Все грабли (документ-специфичные §Д1-Д17 + М-29/проводки §Д18-Д27 + наследованные от обработки) |
| 17 | [FINDINGS.md](FINDINGS.md) | Эталоны для smoke: спецификации, склады, организации, контрольные числа, тестовые данные в базе |

---

## Доработки (промты + статус реализации)

| Промт / спека | Задача | Статус |
|---|---|---|
| [PROMPT_dorabotka_KolichestvoKSpisaniyu.md](PROMPT_dorabotka_KolichestvoKSpisaniyu.md) | `СписаниеСверхНормы.КоличествоКСписанию` (списание понаднормы **частями**) + шапка-флаг `КоличествоКСписаниюАвтоматическиЗаполнять` + проверка `≤ Количество` | **✅ реализовано и проверено COM 2026-07-21** ([13](13_kolichestvo_k_spisaniyu.md)): T1 автозаполнение, T2 документы «Додаткова» на КоличествоКСписанию + пропорц.сумма, T3 отказ |
| `docs/superpowers/specs/2026-08-06-raschet-komplektacij-pechat-ceh-ekonomia-design.md` | Печать №10 **«Аналіз для цеху (з коментарем)»** (кладовщику, только количество, Норма СС/Економія на группе) + формула `ЕкономіяСума = Економія × нормовая цена` | **✅ реализовано и проверено COM 2026-08-06**: smoke 43/43, кросс-сверка с отчётом по домику, регрессия печатей/М-29/актов зелёная |
| `docs/superpowers/specs/2026-08-03-m29-print-form-design.md` | Друкована форма **М-29** по всем проведённым документам расчёта, отдельно Матеріали и МШП | **✅ реализовано и проверено COM 2026-08-03** ([14](14_m29_pechat.md)): суммы только из проводок, 34/34 smoke, регрессия 7 печатей чиста. Комміты `da221d7963` + `ffbad13d8c` |

> Контроль единиц ([12](12_kontrol_edinic_postuplenie.md)) — **реализован и проверен** (4 типа, 2 шлюза, флаг-исключение). Отчёт ретро-аудита `А_ПроверкаЕдиницИзмеренияВПоступлениях` — **рассинхронизирован с модулем** (нужны 4-й тип + два шлюза + цепочка предков, пересборка `.erf` против `Srvr="SQLSERVER";Ref="bas_industrialbud"`).

---

## Реквизиты шапки (манифест `Documents/РасчетКомплектаций.xml`)

| Реквизит | Тип | Назначение |
|---|---|---|
| `Спецификация` | `CatalogRef.СтруктураСебестоимости` | Источник норм |
| `Период` | Дата | Остатки на дату (КонецДня) |
| `Организация` | `CatalogRef.Организации` | Фильтр остатков (пусто=все) |
| `ИсключатьЭтапы` | Булево | Режим фильтра этапов (Ложь=включать список, Истина=исключать) |
| `Статус` | `EnumRef.СтатусыРасчетаКомплектаций` | Черновик / РасчетВыполнен / ДокументыСозданы |
| `Ответственный` | `CatalogRef.Пользователи` | Автор расчёта (ОбработкаЗаполнения) |
| `Комментарий` | Строка(0), неогранич. | Комментарий (в запросах — только `ВЫРАЗИТЬ(… КАК Строка(N))`) |
| `КоличествоКСписаниюАвтоматическиЗаполнять` | Булево | Заполнять `КоличествоКСписанию = Количество` при расчёте → [13](13_kolichestvo_k_spisaniyu.md) |
| `НаименованиеРаботыМ29` | Строка | Название объекта/работы для М-29 → [14](14_m29_pechat.md) |
| `Бухгалтер` / `Перевірив` / `НачальникВТВ` / `Інженер` / `Начальник` / `МВО` | `CatalogRef.ФизическиеЛица` | 6 подписей М-29; синонимы = подписи бланка 1:1 → [14](14_m29_pechat.md) |

**Стандартные:** `Номер` (String, длина 11, авто, уникальный), `Дата`. `Posting=Deny`, `RealTimePosting=Deny`.

⚠ Реквизиты М-29 и `КоличествоКСписаниюАвтоматическиЗаполнять` добавлены **заказчиком в Конфигураторе**,
не скриптами. `Перевірив` и `Інженер` содержат украинскую «і» (U+0456) — обращаться точным написанием (§Д3).

## Табличные части (ТЧ)

| ТЧ | Колонки | Роль |
|---|---|---|
| `СкладыОстатков` | `Склад` | Выбранные склады остатков |
| `СчетаОстатков` | `Счет` (`ChartOfAccountsRef.Хозрасчетный`) | Счета материалов (пусто→fallback 20/28) |
| `СчетаМалоценки` | `Счет` | Счета малоценки (пусто→fallback 22) — задают «семейство малоценка» |
| `Этапы` | `Этап` (`CatalogRef.ЭтапыРабот`) | Этапы для фильтра норм |
| `ТабличнаяЧастьОстатков` | Номенклатура/ОбщееНазвание/Склад/Счет/Остаток/Норма/ВНорме/ПонадНорму/Экономия/Единица/ЕдиницаСС/СуммаОстатка/НормаСумма/ВНормеСумма/ПонадНормуСумма/ЕкономіяСума/НормаНоменкл/НормаСуммаНоменкл/Этап/**РасхождениеЕдиниц** | Результат анализа. Норма отображается **по картке** (`НормаНоменкл`); суммы **с НДС ×1.2** |
| `СписаниеПоНормам` | Номенклатура/ОбщееНазвание/Склад/Счет/Единица/Количество/Цена/Сумма | Сохраняемое списание «за нормами» (**read-only** в форме). Суммы **без НДС** |
| `СписаниеСверхНормы` | + `Причина` + **`КоличествоКСписанию`** (Число — сколько понаднормы списать сейчас, ≤ `Количество`; см. промт-доработку) | Сохраняемое списание «понад норму» (**редактируемое**). Суммы **без НДС** |
| `ДокументиКомплектації` | Склад/ДокументПоНормам/ДокументДодаткова (`DocumentRef.КомплектацияНоменклатуры`) | Целевые документы материалов по складам |
| `ДокументиМалоценки` | Склад/ДокументПоНормам/ДокументДодаткова (`DocumentRef.ПередачаМалоценныхАктивовВЭксплуатацию`) + `ФизЛицо` + `СпособОтраженияРасходов` | Целевые документы малоценки по складам |

---

## Экспортный интерфейс `Ext/ObjectModule.bsl`

| Метод | Роль |
|---|---|
| `РассчитатьАнализ()` | Остатки→нормы→анализ→таблицы списания→статус `РасчетВыполнен`. Оркестратор расчёта |
| `ЗаполнитьДокументыПоАнализу(ЗаполнятьВНорме=Истина, ЗаполнятьПонад=Истина)` | Из сохранённых ТЧ списания заполнить целевые документы (транзакция), вернуть структуру-результат, статус `ДокументыСозданы` |
| `СформироватьПечать(ДанныеАнализа)` | Плоский «Друк» аналізу (14 колонок, программный ТабДок) |
| `СформироватьПечатьПланФакт(ДанныеАнализа)` | СКД «План-факт по матеріалах» (Эталон→Номенклатура) |
| `СформироватьПечатьПланФактЕтапи(ДанныеАнализа)` | СКД «за етапами» (Этап→Эталон→Номенклатура; по-этапная розшивка) |
| `СформироватьПечатьАнализСС(ДанныеАнализа)` | СКД «Аналіз СС (план-факт списання)» (2 одиниці) |
| `СформироватьПечатьАнализССОдна(ДанныеАнализа)` | СКД «Аналіз СС» (1 коалесцентна одиниця) |
| `СформироватьПечатьАкт()` | **Акт на списання понаднормових матеріалів** (по `СписаниеСверхНормы`, подписи комиссии) |
| `СформироватьПечатьРозбіжностіОдиниць()` | Печать розбіжностей одиниць (по `РасхождениеЕдиниц`) |
| `СформироватьПечатьАнализЦех(ДанныеАнализа)` | **Аналіз для цеху** (кладовщику, только количество): Норма СС/Економія лише на групі, «Коментар цеху» під рукопис, жирні групи + жовта понаднорма → [08](08_pechatnye_formy.md) |
| `СформироватьПечатьМ29()` | **М-29** «Звіт про витрати основних матеріалів» — 13 колонок, блоки Матеріали/МШП **из проводок** `Хозрасчетный` проведённых документов. Не зависит от «Розрахувати» → [14](14_m29_pechat.md) |
| `ДанныеОстаткаДляСтроки(Номенклатура, Склад)` | Для ручной строки понаднормы: подобрать Счет/Цена(без НДС)/Единица/ОбщееНазвание/Остаток |
| `НайтиДубликатыРасчета()` | Другие расчёты по той же спецификации/дню/складам (пересечение) |
| `ТекстЗаголовкаЗвіту(НазваЗвіту)` | Многострочный динамический заголовок СКД (подразделение/дата/склады/спец/орг) |

**Событийные обработчики (модуль объекта):** `ОбработкаЗаполнения` (статус=Черновик, Период=нач.дня, Ответственный, дефолт-счета 20/28 + 22), `ПередЗаписью` (дефолт статуса + **предупреждение о дублях, НЕ отказ**; выход при `ОбменДанными.Загрузка`).

**Модуль формы `Forms/ФформаДокумента`** делегирует движку и держит UI: `Рассчитать` (с вопросом-подтверждением, если статус уже `ДокументыСозданы`), `ЗаполнитьОбе/ТолькоВНорме/ТолькоПонад`, **8 команд печати** (popup «Друк»), отбор/пошук/итоги на «Аналіз залишків», редактирование `СписаниеСверхНормы` (автоподбор счёта/цены). Подробно — [10](10_forma_dokumenta.md).

⚠ Максимальный занятый `id` в `Form.xml` — **612** (610-612 — кнопка/команда «Аналіз для цеху»; 574-609 заняты правками заказчика после М-29). Новые элементы — с 613. Перед выбором id — ВСЕГДА `grep -o 'id="[0-9]*"' | sort -n | tail` по живой форме.

---

## Связанные объекты конфигурации (BuhBud)

| Объект | Роль |
|---|---|
| `РегистрБухгалтерии.Хозрасчетный` | **Два применения:** (1) источник остатков для расчёта (`.Остатки`); (2) источник **фактических сумм для М-29** (`.ДвиженияССубконто` по регистраторам). ⚠ `СубконтоКт1..3` есть ТОЛЬКО в `ДвиженияССубконто`, в основной таблице их нет (§Д22) |
| `ПланСчетов.Хозрасчетный` | Счета 20/28 (материалы), 22 (МШП/малоценка) — группы → субсчета (`В ИЕРАРХИИ`) |
| `Справочник.СтруктураСебестоимости` | Спецификация-эталон. ТЧ `Комплектующие` (ОбщееНазвание, Номенклатура, Количество, Сумма **с НДС**, Единица, Этап) |
| `Справочник.Номенклатура` | `ОбщееНазваниеНоменклатуры` (мост к эталону), `БазоваяЕдиницаИзмерения` |
| `Справочник.ОбщиеНазванияНоменклатуры` | Эталон (норма считается по нему); реквизит `Единица` (для розбіжностей) |
| `Справочник.ЭтапыРабот` | Фильтр норм по этапам |
| `Справочник.НазначенияИспользования` | Для малоценки: поиск по (Владелец=Номенклатура, СпособОтраженияРасходов) |
| `Справочник.НалоговыеНазначенияАктивовИЗатрат` | `НДС_Облагаемая` (предопред.) — для строк малоценки |
| `Документ.КомплектацияНоменклатуры` | Цель (материалы). ТЧ `Комплектующие` (Номенклатура, ЕдиницаИзмерения, Количество, **СчетУчетаБУ**, Сумма, Коэффициент). Сумма **без НДС** |
| `Документ.ПередачаМалоценныхАктивовВЭксплуатацию` | Цель (малоценка). ТЧ `МалоценныеАктивы` (Номенклатура, СчетУчетаБУ, Количество, ФизЛицо, НалоговоеНазначение, НазначениеИспользования — **без Сумма**) |
| `Перечисление.СтатусыРасчетаКомплектаций` | Черновик / РасчетВыполнен / ДокументыСозданы |
| `Документ.ПоступлениеТоваровУслуг` | Приход. Кастомные реквизиты шапки `Подразделение`/`СС`/`Направление`; ТЧ `Товары`. На нём висит контроль единиц — [12](12_kontrol_edinic_postuplenie.md) |
| `ОбщийМодуль.А_ПроверкаЕдиницДокументов` + `ПодпискаНаСобытие.А_ПоступлениеТоваровУслугПередЗаписьюПроверкаЕдиниц` | Контроль единиц при оприходовании (блок проведения). ⚠ У модуля обязателен флаг **«Внешнее соединение»** — [12](12_kontrol_edinic_postuplenie.md) |

---

## Smoke-тесты (`_Rarzrabotki/Python/test/`)

| Тест | Что проверяет |
|---|---|
| `smoke_doc_kompl_analiz.py` | Инварианты: построчный баланс, таблицы списания = срезы анализа, суммы ÷1.2, статус, persist |
| `smoke_doc_kompl_zapolnenie.py` | Правка количества + ручная строка ДОХОДЯТ до документа; идемпотентность |
| `smoke_doc_kompl_malocenka.py` | Роутинг 22→ПередачаМалоценныхАктивовВЭксплуатацию (склад ІБ_МШП) |
| `smoke_doc_kompl_pechat.py` | 7 печатей COM |
| `smoke_doc_kompl_dubli.py` | НайтиДубликатыРасчета |
| `smoke_doc_kompl_edinicy.py` | Фаза 2: правило розбіжностей одиниць, фильтрация печати |
| `smoke_doc_kompl_m29.py` | **М-29**: 34 проверки — шапка, нумерация 1..13, 3 итога и их согласованность, число строк блоков, блок АВР, арифметика `Ціна × Кіл = Сума` построчно, защита от удвоения МШП, 6 подписей |

**Образцы аналогичных баз знаний:** [`../knowledge_Создания комплектаций для бух учета/`](../knowledge_Создания%20комплектаций%20для%20бух%20учета/) (движок-прародитель, фазы 1–16), [`../knowledge/`](../knowledge/) (главная база: document_hidden_fields).
