# ПНМП — Roadmap: что сделано / что осталось

> Состояние на 2026-05-30. Документ **реализован, развёрнут в BaseERP, протестирован**.
> Остаток — косметика, расширение охвата и пункты по запросу пользователя.

---

## ✅ Сделано (DONE)

| # | Что | Где |
|---|---|---|
| 1 | **Метаданные документа**: шапка (6 рекв.) + 3 ТЧ (Начисления/Налоги/Удержания) + движения в ПАП и А_ВзСС + нумератор Строка(9) + автоформа | `Documents/А_ПередачаНачисленийМеждуПодразделениями.xml` |
| 2 | **Доставка в БД**: `db-load-xml -Mode Partial` + `db-update -Dynamic+` → новый объект создан в BaseERP | live |
| 3 | **`ОбработкаПроведения`**: ПАП 3 статьи (реверс на исходном / копия на целевом, Источник=ПустаяСсылка) + вызов А_ВзСС | `…/Ext/ObjectModule.bsl` |
| 4 | **`А_ВзСС.ЗаполнитьИзПередачиНачислений`**: инверсный знак, разрез ФизическоеЛицо | `AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl` |
| 5 | **Заполнение**: `СоздатьПередачуНачислений` + `_ПНМП_*` — источник=дочерний ОЗФУ, Find-Or-Create, **пропорциональное дробление** по весам РаспределениеКазна | `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` |
| 6 | **Кнопка** «Создать передачу начислений» в форме А_ОтражениеЗПпоКазне (Command id=9 + Button + обработчики) | `…/Forms/ФормаДокумента/Ext/{Form.xml, Form/Module.bsl}` |
| 7 | **4 теста PASS** (pretest G1–G4 / smoke / idempotency / balanse) | `_Rarzrabotki/Python/test/test_peredacha_*.py` |
| 8 | **Spec + plan + knowledge** (этот каталог + 16_peredacha в knowledge_А_ОтражениеЗПпоКазне) | `docs/superpowers/{specs,plans}/2026-05-29-*` |
| 9 | **Git**: 12 коммитов, merge в `main` (fast-forward, локально) | branch `claude/peredacha-nachisleniy-mezhdu-podrazdeleniyami` |

**Бизнес-результат:** обязательства ЗП/налогов/удержаний переносятся с реальных подразделений на целевые
подразделения юр.лиц-плательщиков; управ.баланс по подразделениям сходится (Σ КО=0, итог по организации
не меняется, 0 новых плугов).

---

## ⏳ Осталось доделать (TODO)

### Косметика / UX
| # | Задача | Детали |
|---|---|---|
| T1 | **Ярлык кнопки** → «Создать передачу налогов и ЗП» | Сейчас авто-ярлык «Создать передачу начислений» (form-edit сгенерировал из имени команды). Поправить `<Title>` команды `СоздатьПередачуНачислений` (id=9) в Form.xml + передоставить форму (db-load + db-update, Конфигуратор закрыт). |
| T2 | **Расположение кнопки** | Кнопка легла в тело формы (form-edit не умеет вставлять в Popup командной панели через `into`). Для единообразия — перенести в Popup `ФормаГруппаСоздать` (id=563), рядом с «Создать ведомости в кассу». |

### Функциональный охват
| # | Задача | Детали |
|---|---|---|
| T3 | **Триггер на `ОтражениеЗарплатыВФинансовомУчете`** | Out-of-scope сейчас (кнопка только на А_ОтражениеЗПпоКазне). Когда А_ОтражениеЗПпоКазне отпадёт (полный переход на ERP) — добавить кнопку/триггер на форме ОЗФУ. **Логика заполнения уже читает ОЗФУ напрямую** → метод future-ready; нужно лишь перенести кнопку и убрать поиск дочернего ОЗФУ (использовать сам ОЗФУ). `А_ДокументОснование` уже составной (поддерживает оба типа). |
| T4 | **Массовый прогон** | Протестирован только эталон (№000000005 / апрель). Прогнать на остальных месяцах/родителях; при закрытых периодах — перепроводить `А_ФинРез_Баланс` помесячно (не Удалить). |
| T5 | **Донастройка `Организации.А_ПодразделениеЗарплатаБух`** | Сейчас заполнено: ТОВ ІНДАСТРІАЛБУД/ДЖИ ТРИ→Строительство, ІНДЕПТ СТІЛ→Производство. У **ВФ АССЕТС, І.П.С ТРЕЙД** и др. — пусто → их доля не переносится (остаётся на исходном). Заполнить, если их ЗП должна переноситься. |
| T6 | **«Управленческая организация» (~36% весов)** | По решению пользователя остаётся на исходном (управучёт через кассу наличными — отдельный механизм). Если бизнес позже захочет переносить и эту долю — нужно правило целевого для управ.учёта. |

### Качество данных / опционально
| # | Задача | Детали |
|---|---|---|
| T7 | **Аудит-поле `Получатель`** в ТЧ Удержания | Сейчас пусто (источник `УдержаннаяЗарплата`, получатель не извлекается). При необходимости — обогатить из источника удержаний. ПАП «Прочие пассивы» аналитику не использует, на баланс не влияет. |
| T8 | **66 пред-существующих плугов ОТ** в Свод_ОплатаТруда | НЕ от этого документа (вне scope). Отдельная задача: полнота покрытия `А_ВзСС` регистраторами статьи «Оплата труда» (НачислениеЗарплаты, сторно и т.п. не заполняют А_ВзСС). |

### Git / выпуск
| # | Задача | Детали |
|---|---|---|
| T9 | **Push / PR** | Не пушено (по §12 задачи — push только по явному запросу). Ветка `claude/peredacha-…` сохранена. По команде «push» — оформить PR. |

---

## Принятые решения (фиксация, чтобы не пересматривать)

- **Пропорциональное дробление** (не «доминирующее юр.лицо») — решение пользователя 2026-05-29, веса из РаспределениеКазна per Сотрудник.
- **«Управленческая»/непокрытые остаются на исходном** — управучёт через кассу, отдельно.
- **Удержания (статья «Прочие пассивы») включены** — решение пользователя (3-я ТЧ).
- **Суммы — из дочернего ОЗФУ** (не ТЧ родителя) — гарантия Σ=ПАП.
- **Без бух.проводок, без новых движений ДДС/статей, без правок структуры типовых регистров.**
