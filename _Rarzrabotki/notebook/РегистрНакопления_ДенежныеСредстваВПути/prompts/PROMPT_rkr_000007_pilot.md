# Промт — ПИЛОТ доработки РасчетКурсовыхРазниц (Подразделение в РНДС.ВПути)

## TL;DR

Пилот №5 (финальный, после ПКО ✅, РКО ✅, СписаниеБезнал ✅, ПостБезнал ✅). Доработать **2 функции** в `Documents/РасчетКурсовыхРазниц/Ext/ManagerModule.bsl`:
- **`ТекстЗапросаВтТаблицаДенежныеСредстваВПути`** (стр. **1250**) — формирует движения переоценки уже существующих переоценочных записей. Использует `Получатель.Подразделение` (текущее значение реквизита). Цель — поменять на `Подразделение` (новое измерение регистра, по аналогии с РКО/ПКО)
- **`ТекстЗапросаКурсовыеРазницыДенежныеСредстваВПути`** (стр. **4710**) — формирует переоценку из валютных остатков. **НЕ передаёт Подразделение** во все ВТ. Цель — добавить колонку `Подразделение` во `ДеньгиВПутиЗаДень`, `втОстаткиВПути`, `ТаблицаДСвПути` и финальный SELECT.

**Тестовый документ:** `Расчет курсовых разниц 000Ц-000007 от 31.01.2026 23:59:59`, ХозОп **«Переоценка денежных средств»**, 2 движения USD по нулевой Σ (но с курсовыми разницами в СуммаУпр/СуммаРегл).

⚠ **ОСОБЕННОСТЬ:** в шапке РасчетКурсовых **НЕТ Подразделения** (только Организация + ХозОп). Поэтому источник Подразделения — **самые валютные остатки в регистре**, которые переоцениваются.

**Pilot-скрипты:** `pilot_rkr_01_baseline.py` (готов), `pilot_rkr_02_diagnostic.py` (готов — диагностика валютных остатков). Скрипты 03 (repost) и 04 (verify) создать аналогично pilot_post_03/04 по шаблону.

## Параметры

| Параметр | Значение |
|---|---|
| Файл | `Documents/РасчетКурсовыхРазниц/Ext/ManagerModule.bsl` |
| Функция 1 | `ТекстЗапросаВтТаблицаДенежныеСредстваВПути` (стр. **1250**) |
| Функция 2 | `ТекстЗапросаКурсовыеРазницыДенежныеСредстваВПути` (стр. **4710**) |
| Тестовый док | `000Ц-000007 от 31.01.2026 23:59:59` |
| ХозОп | Переоценка денежных средств |
| Шапка | НЕТ Подразделения |
| Источник Подр | Регистр.ДенежныеСредстваВПути.Подразделение (новое измерение) |
| Безопасность COM | ✅ безопасно (нет РСКПС/РСППС) |

## Уникальная сложность

После доработки ПКО/РКО/Спис/Пост (4 пилота, все PASS) **новые** проводки этих документов имеют корректное Подразделение в РНДС.ВПути. Но **старые** записи остаются с пустым Подр пока их источники не перепроведут.

⚠ Поэтому: даже после нашей доработки РасчетКурсовых — если он переоценивает старые остатки с пустым Подр, итоговое движение тоже будет с пустым Подр. **Это нормально**, не ошибка. Чтобы получить заполненное Подр для пилота — нужно либо:
- (A) Сначала перепровести через UI исходный валютный документ (например, ПостБезнал/СписаниеБезнал, который сформировал валютный остаток в пути)
- (B) Принять что для исторических остатков Подр=пусто и проверить только на новых после миграции

## Что уже сделано

1. ✅ ПКО / РКО / СписаниеБезнал / ПостБезнал — 4 пилота PASS
2. ✅ `pilot_rkr_01_baseline.py` создан и **доработан**: фильтр теперь по `Номер + ХозОп = ПереоценкаДенежныхСредств + ГОД(Дата) = 2026` (одинаковые номера у разных ХозОп — учтено)
3. ✅ `pilot_rkr_02_diagnostic.py` показал: на 31.01.2026 в РНДС.ВПути 2 валютных остатка USD ±505,58 с Подр=пусто

**Корректировка для будущей сессии:** в `pilot_rkr_01_baseline.py` искать документ через UUID или дату+ХозОп вместо `Номер = "000Ц-000007"` (одинаковые номера у разных ХозОп).

## Структура правок

### Функция 1 — `ТекстЗапросаВтТаблицаДенежныеСредстваВПути` (стр. 1250-1360)

**Сейчас (стр. 1258-1259):**
```sql
|	ЕСТЬNULL(ДенежныеСредства.Получатель.Подразделение,
|			ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)) КАК Подразделение,
```

**Заменить на:**
```sql
|	ЕСТЬNULL(ДенежныеСредства.Подразделение,
|			ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)) КАК Подразделение,
```

⚠ Также заменить в `СГРУППИРОВАТЬ ПО` стр. 1286-1287 (тот же текст).

**Эффект:** для записей переоценки которые УЖЕ есть в регистре с новым измерением Подр заполненным — будут корректно передаваться. Согласовано с другими доработанными документами.

### Функция 2 — `ТекстЗапросаКурсовыеРазницыДенежныеСредстваВПути` (стр. 4710)

**Сейчас не передаёт Подразделение НИГДЕ.**

Правки:

#### 2.1 ВТ `ДеньгиВПутиЗаДень` (стр. 4716-4732)
В SELECT добавить:
```sql
|	ОборотыЗаДень.Подразделение КАК Подразделение,
```

#### 2.2 ВТ `втОстаткиВПути` (стр. 4736-4807)
В обоих SELECT (положительные и отрицательные курсовые) добавить:
```sql
|	ОстаткиНаДень.Подразделение КАК Подразделение,
```
В ЛЕВОЕ СОЕДИНЕНИЕ добавить `И ОстаткиНаДень.Подразделение = ДеньгиВПутиЗаДень.Подразделение`.

#### 2.3 ВТ `ТаблицаДСвПути` (стр. 4811-4838)
В SELECT добавить:
```sql
|	Остатки.Подразделение КАК Подразделение,
```

#### 2.4 Финальный SELECT (после стр. 4838)
В итоговом SELECT который ПОПАДАЕТ в РНДС.ВПути добавить колонку Подразделение в нужном месте.

⚠ Финальный SELECT нужно найти прочитав код после стр. 4838 (Read offset=4836 limit=150).

## Алгоритм (8 шагов)

```bash
# 1. Бэкап
cp "C:/Configuration_downloads/BASERP25/Documents/РасчетКурсовыхРазниц/Ext/ManagerModule.bsl" \
   "C:/Configuration_downloads/BASERP25/_Rarzrabotki/notebook/РегистрНакопления_ДенежныеСредстваВПути/_backup/RKR_ManagerModule_BEFORE.bsl"

# 2. Baseline
cd "C:/Configuration_downloads/BASERP25/_Rarzrabotki/notebook/РегистрНакопления_ДенежныеСредстваВПути/Python/test"
python pilot_rkr_01_baseline.py
python pilot_rkr_02_diagnostic.py

# 3. Read функций
# Read offset=1250 limit=115  — функция 1
# Read offset=4710 limit=200  — функция 2 + финальный SELECT

# 4. Edit Функция 1 (2 места: SELECT стр. 1258-1259 и GROUP BY стр. 1286-1287)
#    Заменить ДенежныеСредства.Получатель.Подразделение → ДенежныеСредства.Подразделение

# 5. Edit Функция 2 (4 места: 3 ВТ + финальный SELECT)
#    Добавить колонку Подразделение

# 6-7. db-load-xml + db-update
powershell.exe -NoProfile -File "C:\Configuration_downloads\BASERP25\.claude\skills\db-load-xml\scripts\db-load-xml.ps1" `
  -V8Path "C:\Program Files\1cv8\8.3.20.1914\bin" `
  -InfoBaseServer "SQLSERVER" -InfoBaseRef "BaseERP" -UserName "Администратор" -Password "24043" `
  -ConfigDir "C:\Configuration_downloads\BASERP25" `
  -Mode Partial -Files "Documents/РасчетКурсовыхРазниц/Ext/ManagerModule.bsl"

powershell.exe -NoProfile -File "C:\Configuration_downloads\BASERP25\.claude\skills\db-update\scripts\db-update.ps1" `
  -V8Path "C:\Program Files\1cv8\8.3.20.1914\bin" `
  -InfoBaseServer "SQLSERVER" -InfoBaseRef "BaseERP" -UserName "Администратор" -Password "24043" `
  -Dynamic "+"

# 8. Создать pilot_rkr_03_repost.py (по шаблону pilot_post_03_repost.py)
#    + pilot_rkr_04_verify.py (по шаблону pilot_post_04_verify.py)
#    Запустить:
python pilot_rkr_03_repost.py
python pilot_rkr_04_verify.py
```

## Acceptance

PASS если:
1. ✅ Функции 1250 и 4710 содержат правильные ссылки на `Подразделение` (новое измерение, не реквизит)
2. ✅ `/db-update` прошёл
3. ✅ `pilot_rkr_03_repost.py` показывает `[OK] Проведено`
4. ✅ `pilot_rkr_04_verify.py` показывает PASS — Подр в РНДС.ВПути НЕ пустое (или совпадает с Подр исходного валютного остатка)
5. ✅ Σ Сумма/СуммаУпр/СуммаРегл движений НЕ изменилась
6. ✅ Если исходные остатки с пустым Подр — переоценка тоже даст пустое (это допустимый PARTIAL PASS — нужно сначала перепровести исходники)

**Полный PASS** возможен только после перепроведения исходных валютных документов (СписаниеБезнал/ПостБезнал) которые формируют валютные остатки в пути.

## Антипаттерны

- ❌ Edit `replace_all: true` — функции большие, можно задеть похожие участки
- ❌ Ожидать что переоценка получит Подразделение если исходные остатки с пустым (это by-design — переоценка наследует Подр от источника)
- ❌ Использовать подзапросы `(ВЫБРАТЬ ...)` в SELECT — НЕ работает в 1С (урок ПостБезнал)
- ❌ Пропустить функцию 4710 — без неё движения переоценки всё равно будут с пустым Подр

## Зависимости

После успеха РасчетКурсовых — **все 5 фактических документов** доработаны.

Следующий шаг (вне scope этого пилота): **массовое перепроведение** всех существующих 4 558 движений документов чтобы заполнить Подр для исторических записей. Это требует UI 1С Enterprise (для СписаниеБезнал/ПостБезнал — обязательно UI из-за РегистраторРасчётов в РСКПС/РСППС).

## Контакты

- Memory: `balans_money_knowledge_base`, `feedback_use_db_skills_for_config_load`, `feedback_balans_etalon_period_serverside`, `feedback_balans_live_code_source_of_truth`.
- Пилоты-предшественники: `PROMPT_pko_000001_pilot.md`, `PROMPT_rko_N0000053020_pilot.md`, `PROMPT_spis_beznal_00000019546_pilot.md`, `PROMPT_post_beznal_00DL-007179_pilot.md` (все ✅ PASS).
- **Урок ПостБезнал:** подзапросы в SELECT 1С не работают. Использовать ВТ-колонки или ЛЕВОЕ СОЕДИНЕНИЕ.
- **Особенность РасчетКурсовых:** один документ может появляться с разными ХозОп (`Переоценка ДС` vs `Переоценка расчётов с клиентами`) с одинаковыми номерами — искать по ХозОп или дате, не только по номеру.
- Baseline скрипт нашёл `000Ц-000007 от 31.05.2024 «Переоценка расчётов с клиентами»` (другой!), правильный — `000Ц-000007 от 31.01.2026 «Переоценка денежных средств»`. Перед запуском исправь поиск в `pilot_rkr_01_baseline.py` (например через GUID или дату).
