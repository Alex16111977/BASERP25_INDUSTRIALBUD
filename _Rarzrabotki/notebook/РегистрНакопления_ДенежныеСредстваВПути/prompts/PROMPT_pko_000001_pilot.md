# Промт для новой ИИ-сессии — ПИЛОТ доработки проведения ПКО (Подразделение в РНДС.ВПути)

## TL;DR (контекст за 30 секунд)

Пользователь УЖЕ добавил измерение `Подразделение` (`СправочникСсылка.СтруктураПредприятия`) в `РегистрНакопления.ДенежныеСредстваВПути` через Конфигуратор. Теперь нужно **доработать функцию `ТекстЗапросаДенежныеСредстваВПути`** в `Documents/ПриходныйКассовыйОрдер/Ext/ManagerModule.bsl` (начало стр. 1739) — добавить колонку `Подразделение` в каждый из ~5 блоков `ОБЪЕДИНИТЬ ВСЕ`, чтобы при проведении ПКО движение в РНДС.ВПути писало правильное Подразделение из `Касса.Подразделение`.

**Тестовый документ (пилот):** `ПриходныйКассовыйОрдер 000Ц-000001 от 10.12.2025 16:35:01`, Касса = «2 Касса Подгорцы» (Подразделение в карточке = КГ "Подгорцы"), Сумма 373 800 UAH, ХозОперация «Конвертация валюты» (USD).

**Готовые pilot-скрипты** (созданы и проверены 2026-05-24): `Python/test/pilot_01_baseline.py` → `pilot_02_sql_pretest.py` → `pilot_03_repost.py` → `pilot_04_verify.py`.

После пилота — расширить на остальные 4 фактических документа (РКО, СписаниеБезнал, ПостБезнал, РасчетКурсовых).

## Параметры задачи

| Параметр | Значение |
|---|---|
| Конфигурация | `C:\Configuration_downloads\BASERP25` |
| База | `Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"` |
| Регистр | `РегистрНакопления.ДенежныеСредстваВПути` (новое измерение `Подразделение` уже добавлено) |
| Файл доработки | `Documents/ПриходныйКассовыйОрдер/Ext/ManagerModule.bsl` |
| Функция | `ТекстЗапросаДенежныеСредстваВПути` (начало строки **1739**) |
| Тестовый ПКО | Номер **000Ц-000001**, Дата 10.12.2025 16:35:01, Сумма 373 800 UAH |
| Касса | «2 Касса Подгорцы» (Подразделение в карточке = КГ "Подгорцы") |
| ХозОп пилота | Конвертация валюты (3-й блок ОБЪЕДИНИТЬ ВСЕ, ~стр. 1815-1843) |

## Что уже сделано

1. ✅ **Пользователь добавил Подразделение** в измерения регистра через Конфигуратор (скриншот 24.05.2026)
2. ✅ **Инвентаризация выполнена** — `_Rarzrabotki/notebook/РегистрНакопления_ДенежныеСредстваВПути/` содержит README/STRUCTURE/ACTUAL_USAGE/CODE_USAGE/PROMPT_add_podrazdelenie
3. ✅ **4 pilot-скрипта созданы и протестированы**:
   - `pilot_01_baseline.py` — работает (РНДС.ВПути сейчас имеет Подр=пусто для этого ПКО)
   - `pilot_02_sql_pretest.py` — работает (SQL с `Касса.Подразделение` возвращает «КГ Подгорцы»)
   - `pilot_03_repost.py` — готов (запустить ПОСЛЕ правки BSL + /db-update)
   - `pilot_04_verify.py` — готов (сравнит pilot_01 baseline.json с текущим)
4. ✅ **Phase 7 (Безнал/Налич) ЗАКРЫТА** — в шапке этого ПКО Подразделение уже КГ "Подгорцы" (поставлено COM в Phase 7). Но в РНДС.ВПути всё равно пусто, потому что движение пишется НЕ из шапки.

## Knowledge base — обязательно прочитать

1. **`../README.md`** + **`../STRUCTURE.md`** — структура регистра до/после
2. **`../CODE_USAGE.md`** — все 17 WRITE-точек (включая нашу строку 1739)
3. **`../ACTUAL_USAGE.md`** — какие документы реально пишут в регистр
4. **`../PROMPT_add_podrazdelenie.md`** — общий промт системной доработки (этот пилот — его узкая первая итерация)
5. **`../../knowledge_Balanse_money/docs/PHASE7_REPORT_beznal_nalich.md`** — Phase 7 кейс ПКО 000Ц-000001 уже разбирался

## Структура функции (что менять в `Documents/ПриходныйКассовыйОрдер/Ext/ManagerModule.bsl`)

Функция `ТекстЗапросаДенежныеСредстваВПути(Запрос, ТекстыЗапроса, Регистры)` стр. 1739 содержит ~5 блоков `ОБЪЕДИНИТЬ ВСЕ`:

| # | Блок ОБЪЕДИНИТЬ ВСЕ | ХозОперация | Касса в SQL | Источник Подразделения |
|---|---|---|---|---|
| 1 | стр. 1751-1782 | ПоступлениеДенежныхСредствИзДругойКассы | `ДанныеДокумента.Касса` | `ДанныеДокумента.Касса.Подразделение` |
| 2 | стр. 1786-1812 | ИнкассацияДенежныхСредствИзБанка | `ДанныеДокумента.Касса` | `ДанныеДокумента.Касса.Подразделение` |
| 3 | стр. 1815-1843 | **КонвертацияВалюты** ⭐ пилот | `ДанныеДокумента.Касса` | `ДанныеДокумента.Касса.Подразделение` |
| 4 | стр. 1848+ | Положительные курсовые разницы (Конвертация) | `ТаблицаКурсовыхРазниц.Касса` | `ТаблицаКурсовыхРазниц.Касса.Подразделение` |
| 5 | (после 4) | Отрицательные курсовые разницы | `ТаблицаКурсовыхРазниц.Касса` | `ТаблицаКурсовыхРазниц.Касса.Подразделение` |

### Что добавить в каждый блок

В каждом блоке SELECT перед строкой `&ХозяйственнаяОперация` добавить:

**Для блоков 1-3 (Касса из шапки):**
```sql
|	ДанныеДокумента.Касса.Подразделение                                                  КАК Подразделение,
```

**Для блоков 4-5 (Касса из временной таблицы курсовых):**
```sql
|	ТаблицаКурсовыхРазниц.Касса.Подразделение                                            КАК Подразделение,
```

⚠️ Имя колонки в SELECT должно быть **точно `Подразделение`** — это требуется типовым механизмом `ПроведениеДокументовБП.СформироватьДвижения()` (имена должны совпадать с именами измерений регистра).

## Готовые pilot-скрипты (НЕ переписывать)

Все в `_Rarzrabotki/notebook/РегистрНакопления_ДенежныеСредстваВПути/Python/test/`:

| Скрипт | Назначение | Когда запускать |
|---|---|---|
| `pilot_01_baseline.py` | Дамп шапки + всех движений ПКО 000Ц-000001 → `_artifacts/pilot_01_baseline.json` | ⭐ **ПЕРВЫМ** (до правки BSL) |
| `pilot_02_sql_pretest.py` | Python COM тест SQL блока КонвертацияВалюты с новой колонкой Подразделение (Rule #-1) | Перед правкой BSL — убедиться что SQL парсится |
| `pilot_03_repost.py` | Отмена проведения + проведение через COM | **После** Edit BSL + /db-load-xml + /db-update |
| `pilot_04_verify.py` | Diff pilot_01_baseline.json vs текущее: проверка Подр=КГ Подгорцы, Σ не изменилась, число движений то же | **После** pilot_03 |

Образец для расширения: `_Rarzrabotki/Python/test/train_repost_single_doc.py` (дамп ДО/ПОСЛЕ + сравнение).

## Алгоритм (10 шагов с готовыми командами)

### Шаг 1: Бэкап
```bash
mkdir -p "C:/Configuration_downloads/BASERP25/_Rarzrabotki/notebook/РегистрНакопления_ДенежныеСредстваВПути/_backup"
cp "C:/Configuration_downloads/BASERP25/Documents/ПриходныйКассовыйОрдер/Ext/ManagerModule.bsl" \
   "C:/Configuration_downloads/BASERP25/_Rarzrabotki/notebook/РегистрНакопления_ДенежныеСредстваВПути/_backup/PKO_ManagerModule_BEFORE.bsl"
```

### Шаг 2: Baseline ДО доработки
```bash
cd "C:/Configuration_downloads/BASERP25/_Rarzrabotki/notebook/РегистрНакопления_ДенежныеСредстваВПути/Python/test"
python pilot_01_baseline.py
```
Сохранит `_artifacts/pilot_01_baseline.json`. Ожидаемо в РНДС.ВПути для этого ПКО: 2 строки Расход, Подр=пусто.

### Шаг 3: Python COM пре-тест SQL (Rule #-1)
```bash
python pilot_02_sql_pretest.py
```
Должно показать `★ Подразделение = КГ "Подгорцы"` для блока КонвертацияВалюты. Если ошибка — НЕ переходить к Шагу 4.

⚠️ Для **полной валидации** аналогичный pre-test нужно сделать для **остальных 4 блоков** (адаптировать `pilot_02_sql_pretest.py` — копировать SQL других блоков из ManagerModule.bsl стр. 1751/1786/1848+). Это страховка от ошибок типов / опечаток имён реквизитов.

### Шаг 4: Прочитать функцию полностью + найти точные строки
```python
Read(file_path="C:/Configuration_downloads/BASERP25/Documents/ПриходныйКассовыйОрдер/Ext/ManagerModule.bsl",
     offset=1739, limit=300)
```
Идентифицировать ВСЕ 5 блоков ОБЪЕДИНИТЬ ВСЕ (по комментариям и ХозОп в WHERE).

### Шаг 5: Применить правки через Edit

Для **каждого** из 5 блоков SELECT — найти строку с `&ХозяйственнаяОперация` (маркер «конца бизнес-полей») и добавить ПЕРЕД ней нужную строку (см. раздел «Что добавить в каждый блок» выше).

⚠️ **Edit с `replace_all: false`** + достаточно уникальный `old_string` (5-7 строк контекста), чтобы не задеть другие похожие блоки в этом же файле.

### Шаг 6: Загрузить изменения через skills

⚠️ **Использовать skills** `/db-load-xml` и `/db-update` — НЕ запускать 1cv8.exe вручную (memory `feedback_use_db_skills_for_config_load`).

```bash
/db-load-xml -Mode Partial -updateConfigDumpInfo
/db-update -Dynamic+
```

### Шаг 7: Перепровести ПКО через COM
```bash
cd "C:/Configuration_downloads/BASERP25/_Rarzrabotki/notebook/РегистрНакопления_ДенежныеСредстваВПути/Python/test"
python pilot_03_repost.py
```
Должно показать `[OK] Проведено`. Если ошибка «Поле не найдено Подразделение» → Шаг 6 не выполнен корректно.

ХозОп «Конвертация валюты» НЕ пишет в РСКПС/РСППС → COM-репост безопасен (memory `feedback_com_repost_skips_registrator_raschetov` не применима).

### Шаг 8: Verify пилота
```bash
python pilot_04_verify.py
```
Ожидаемый вывод:
```
PASS:
  ✓ ДенежныеСредстваВПути.Подразделение = 'КГ "Подгорцы"' ✅ совпадает с Касса.Подразделение
[OVERALL] PASS — пилот успешен, можно расширять на РКО / СписаниеБезнал / ПостБезнал / РасчетКурсовых
```

Если FAIL — посмотреть diff: какие регистры изменились, изменилась ли Σ Сумма, в каких блоках забыли добавить Подразделение.

### Шаг 9: Перепровести А_ФинРез_Баланс декабря + сверка плуга
```bash
cd ../../../knowledge_Balanse_klient/Python/test
python 18_repost_finrez_balans.py 2025-12

cd ../../knowledge_Balanse_money/Python/test
python 03_drill_vputi.py 2025-12              # должен показать новую раскладку с Подр
python 06_verify_money_disp.py VERIFY         # сравнение с baseline (Phase 7)
```

### Шаг 10: Решение о расширении

Если пилот PASS → перейти к расширению. План в `../PROMPT_add_podrazdelenie.md`.
Если PASS пилота → создать `pilot_05_extend_rko.py`, `pilot_05_extend_spis_beznal.py` и т.д. (по образцу pilot_01-04 — копировать и менять имя документа/реквизит источника Подр).

## Acceptance критерии (для пилота)

**PASS если:**
1. ✅ Файл `Documents/ПриходныйКассовыйОрдер/Ext/ManagerModule.bsl` содержит `Подразделение` в каждом из 5 блоков SELECT функции `ТекстЗапросаДенежныеСредстваВПути`
2. ✅ После `/db-update` динамическое обновление прошло без ошибок
3. ✅ `pilot_03_repost.py` показывает `[OK] Проведено`
4. ✅ `pilot_04_verify.py` показывает `[OVERALL] PASS` — Подразделение в РНДС.ВПути = КГ "Подгорцы"
5. ✅ Σ Сумма движений ПКО НЕ изменилась (safeguard от случайной правки соседнего блока)
6. ✅ Прочие реквизиты движения (Получатель, Отправитель, ВидПеревода, Контрагент, Валюта) НЕ изменились

**FAIL если:**
1. ❌ Ошибка компиляции BSL при `/db-update`
2. ❌ Ошибка проведения ПКО (`pilot_03` упал)
3. ❌ Σ Сумма движения изменилась (`pilot_04` показывает `Δ Σ ≠ 0`)
4. ❌ Изменились другие движения документа (не РНДС.ВПути)
5. ❌ Подразделение в РНДС.ВПути осталось пустое — правка не сработала

## Критические правила

1. **Rule #-1**: SQL пре-тест через Python COM (`pilot_02_sql_pretest.py`) перед записью в BSL
2. **`feedback_use_db_skills_for_config_load`**: загрузка изменений ТОЛЬКО через skills `/db-load-xml -Mode Partial -updateConfigDumpInfo` + `/db-update -Dynamic+`
3. **`feedback_designer_cache_invalidation`**: после правки BSL `cp` в main config + skill команды (см. п. 2)
4. **`feedback_no_typical_register_changes`** ⚠️: правка типового документа разрешена explicit consent пользователя (без неё свод не работает)
5. **`feedback_balans_etalon_period_serverside`**: даты СЕРВЕРНО (`ДАТАВРЕМЯ(2025,12,10)`)
6. **`feedback_com_repost_skips_registrator_raschetov`**: для этого ПКО (ХозОп «Конвертация валюты») COM-репост безопасен. Для будущих документов (СписаниеБезнал/ПостБезнал) — UI only.
7. **`feedback_query_pre_check`**: каждый запрос — Python COM первым
8. **Бэкап перед правкой** — обязательно (Шаг 1)
9. **`balans_klient_scripts_catalog`** + **`balans_money_knowledge_base`**: использовать готовые pilot-скрипты, не плодить новые без проверки
10. **`feedback_autonomy_no_obvious_confirm`**: не спрашивать очевидное, решать сам
11. **`feedback_russian_communication`**: общение на русском

## Антипаттерны

- ❌ Edit с `replace_all: true` — задеть похожие блоки в других ХозОп
- ❌ Использовать `ВЫРАЗИТЬ(Касса КАК Справочник.Кассы).Подразделение` если поле Касса в SELECT уже типа Кассы — не нужно ВЫРАЗИТЬ
- ❌ Забыть про блоки 4-5 (курсовые разницы) — там Касса берётся из ТаблицаКурсовыхРазниц
- ❌ Перепроводить ПКО раньше чем `/db-update` — будет ошибка «Поле не найдено»
- ❌ Брать Подр из шапки (`ДанныеДокумента.Подразделение`) — было пустым в Phase 7; правильно из `Касса.Подразделение`
- ❌ Пропустить `pilot_04_verify.py` — может незаметно сломать Σ
- ❌ Запускать 1cv8.exe вручную вместо skills `/db-load-xml` + `/db-update`
- ❌ Писать новый Python скрипт для repost/verify когда есть готовые pilot_*.py
- ❌ Тестировать только блок 3 (Конвертация) — пилот покрывает все 5 блоков (потом будет на других ПКО с другими ХозОп)

## Стартовые команды

```bash
# 1. Прочитать knowledge base (5 .md в этой папке + 1 в knowledge_Balanse_money)

# 2. Бэкап
mkdir -p "C:/Configuration_downloads/BASERP25/_Rarzrabotki/notebook/РегистрНакопления_ДенежныеСредстваВПути/_backup"
cp "C:/Configuration_downloads/BASERP25/Documents/ПриходныйКассовыйОрдер/Ext/ManagerModule.bsl" \
   "C:/Configuration_downloads/BASERP25/_Rarzrabotki/notebook/РегистрНакопления_ДенежныеСредстваВПути/_backup/PKO_ManagerModule_BEFORE.bsl"

# 3. Baseline + SQL пре-тест (готовые скрипты)
cd "C:/Configuration_downloads/BASERP25/_Rarzrabotki/notebook/РегистрНакопления_ДенежныеСредстваВПути/Python/test"
python pilot_01_baseline.py
python pilot_02_sql_pretest.py

# 4. Прочитать функцию (5 блоков):
# Read offset=1739 limit=300 → найти все блоки ОБЪЕДИНИТЬ ВСЕ

# 5. Применить Edit (5 раз с replace_all:false)
# 6. /db-load-xml -Mode Partial -updateConfigDumpInfo
# 7. /db-update -Dynamic+

# 8. Перепровести + verify (готовые скрипты)
python pilot_03_repost.py
python pilot_04_verify.py

# 9. (опционально) Перепровести А_ФинРез_Баланс + сверить плуг свода
cd ../../../knowledge_Balanse_klient/Python/test
python 18_repost_finrez_balans.py 2025-12
cd ../../knowledge_Balanse_money/Python/test
python 03_drill_vputi.py 2025-12
```

## После успеха пилота

Расширить аналогично на остальные 4 фактических документа (см. общий промт `../PROMPT_add_podrazdelenie.md` Шаг 4):

| Документ | Файл стр. | Функция | Источник Подразделения | Безопасность COM |
|---|---|---|---|---|
| **РасходныйКассовыйОрдер** | `Documents/РасходныйКассовыйОрдер/Ext/ManagerModule.bsl:2462` | `ТекстЗапросаДенежныеСредстваВПути` (3 блока) | `Касса.Подразделение` (источник) | ✅ безопасно для большинства ХозОп |
| **СписаниеБезналичныхДенежныхСредств** | `Documents/СписаниеБезналичныхДенежныхСредств/Ext/ManagerModule.bsl:2859` | `ТекстЗапросаТаблицаДенежныеСредстваВПути` (3 блока) | `БанковскийСчет.Подразделение` (источник) | ⚠️ ТОЛЬКО UI (риск потери РегистраторРасчётов в РСКПС/РСППС) |
| **ПоступлениеБезналичныхДенежныхСредств** | `Documents/ПоступлениеБезналичныхДенежныхСредств/Ext/ManagerModule.bsl:2019` | `ТекстЗапросаТаблицаДенежныеСредстваВПути` (5 блоков) | `БанковскийСчет.Подразделение` (получатель) | ⚠️ ТОЛЬКО UI |
| **РасчетКурсовыхРазниц** | `Documents/РасчетКурсовыхРазниц/Ext/ManagerModule.bsl:1250` | особый — переоценка | из самого регистра после миграции | ✅ безопасно |

Создать `pilot_05_extend_rko.py`, `pilot_05_extend_spis_beznal.py` и т.п. — копировать pilot_01-04 и менять имя документа + поле источника Подр.

## Контакты сессии-предшественника

- Memory: `balans_money_knowledge_base`, `balans_klient_scripts_catalog`, `feedback_balans_etalon_period_serverside`, `feedback_balans_live_code_source_of_truth`, `feedback_com_repost_skips_registrator_raschetov`, `feedback_no_typical_register_changes`, `feedback_designer_cache_invalidation`, `feedback_use_db_skills_for_config_load`, `feedback_query_pre_check`, `feedback_autonomy_no_obvious_confirm`, `feedback_russian_communication`.
- Phase 7 артефакт: `../knowledge_Balanse_money/docs/PHASE7_REPORT_beznal_nalich.md` — этот же ПКО разбирался (там Подр поставили в шапке через COM как временный фикс; пилот делает системно через проведение).
- Образец паттерна перепроведения: `_Rarzrabotki/Python/test/train_repost_single_doc.py` (дамп ДО → repost → дамп ПОСЛЕ → сравнение).
- Подразделение в Кассе «2 Касса Подгорцы» = `КГ "Подгорцы"` (проверено `pilot_01_baseline.py`).
- pilot_01 baseline уже создан 2026-05-24, файл: `_artifacts/pilot_01_baseline.json`.
- pilot_02 SQL пре-тест уже пройден 2026-05-24: SQL с `Касса.Подразделение` возвращает «КГ Подгорцы».
