# Промт — ПИЛОТ доработки ПостБезнал 00DL-007179 (Подразделение в РНДС.ВПути)

## TL;DR

Пилот №4 (после ПКО ✅, РКО ✅, СписаниеБезнал). Доработать `ТекстЗапросаТаблицаДенежныеСредстваВПути` в `Documents/ПоступлениеБезналичныхДенежныхСредств/Ext/ManagerModule.bsl` (стр. **2019**) — добавить `Подразделение = ДанныеДокумента.Подразделение`.

**Функция СЛОЖНАЯ — 8 блоков ОБЪЕДИНИТЬ ВСЕ:**
- Блоки 1-4, 7-8 (используют `ДанныеДокумента`) — простая правка
- **Блоки 5-6 (курсовые разницы) используют параметры (`&Организация`, `&БанковскийСчет`)** — требуют отдельной правки: либо добавить параметр `&Подразделение` в основной процедуре проведения, либо использовать подзапрос `(ВЫБРАТЬ Подразделение ИЗ Документ.ПоступлениеБезнал ГДЕ Ссылка=&Ссылка)`

**Тестовый документ:** `Поступление безналичных ДС 00DL-007179 от 10.12.2025 17:25:02`, БС получатель = «ОТП», БС отправитель = «ТАС_Виробн», Подразделение шапки = **Производство**, Сумма 6 487,20 UAH, ХозОп **«Поступление ДС с другого счета»** (межбанк, БЕЗ РСКПС/РСППС → COM безопасен).

**Pilot-скрипты:** `pilot_post_01_baseline.py` → `pilot_post_02_sql_pretest.py` → `pilot_post_03_repost.py` → `pilot_post_04_verify.py`.

## Параметры

| Параметр | Значение |
|---|---|
| Файл | `Documents/ПоступлениеБезналичныхДенежныхСредств/Ext/ManagerModule.bsl` |
| Функция | `ТекстЗапросаТаблицаДенежныеСредстваВПути` (стр. **2019**) |
| Тестовый док | `00DL-007179` от 10.12.2025 17:25:02, 6 487,20 UAH |
| Подразделение шапки | **Производство** |
| ХозОп пилота | Поступление ДС с другого счета (блок 2 — стр. **2062-2088**) |
| Безопасность COM | ✅ безопасно (нет РСКПС/РСППС; safety-check в `pilot_post_03_repost.py`) |

## Структура функции — 8 блоков

| # | Строки | ХозОперация | Источник Подразделения |
|---|---|---|---|
| 1 | 2031-2058 | ПоступлениеОплатыПоПлатежнойКарте | `ДанныеДокумента.Подразделение` |
| 2 | 2062-2088 | **ПоступлениеДенежныхСредствСДругогоСчета** ⭐ пилот | `ДанныеДокумента.Подразделение` |
| 3 | 2092-2118 | ПоступлениеДенежныхСредствИзКассыНаРасчетныйСчет | `ДанныеДокумента.Подразделение` |
| 4 | 2122-2154 | КонвертацияВалюты | `ДанныеДокумента.Подразделение` |
| 5 | 2159-2186 | **Положительные курсовые разницы** | ⚠ из `ТаблицаКурсовыхРазниц` (нет ДанныеДокумента в FROM) |
| 6 | 2190-2218 | **Отрицательные курсовые разницы** | ⚠ same |
| 7 | 2222-2259 | Комиссия (КонвертацияВалюты) | `ДанныеДокумента.Подразделение` (есть FROM) |
| 8 | 2264-2293 | Отчисления в ПФ (КонвертацияВалюты) | `ДанныеДокумента.Подразделение` (есть FROM) |

### Для блоков 1-4, 7-8

Вставить перед `&ХозяйственнаяОперация`:
```sql
|	ДанныеДокумента.Подразделение                                                       КАК Подразделение,
```

### Для блоков 5-6 (КУРСОВЫЕ — особый случай)

3 варианта:

**Вариант A (рекомендуется): добавить через подзапрос внутри SELECT**
```sql
|	(ВЫБРАТЬ ПЕРВЫЕ 1 Д.Подразделение ИЗ Документ.ПоступлениеБезналичныхДенежныхСредств КАК Д ГДЕ Д.Ссылка = &Ссылка) КАК Подразделение,
```

**Вариант B: добавить параметр `&Подразделение`**
Тогда нужно найти где формируется запрос (обычно в `ИнициализироватьДанныеДокумента` или вышестоящей процедуре) и установить параметр `Запрос.УстановитьПараметр("Подразделение", ДанныеДокумента.Подразделение);`. Это требует анализа основной процедуры.

**Вариант C: добавить Подразделение в саму ТаблицаКурсовыхРазниц**
Найти функцию `ТекстЗапросаТаблицаКурсовыхРазниц` в этом же файле (искать через Grep) и добавить `ДанныеДокумента.Подразделение КАК Подразделение` в SELECT. Потом в блоках 5-6 использовать `ТаблицаКурсовыхРазниц.Подразделение`.

**Для пилота 00DL-007179** (ХозОп = «Поступление ДС с другого счета», без курсовых) — блоки 5-6 НЕ задействуются. Их правка не критична для пилота, но обязательна для документов с конвертацией валюты в будущем.

## Что уже сделано

1. ✅ ПКО + РКО пилоты PASS
2. ✅ pilot_post_01 baseline: РНДС.ВПути 1 строка Расход 6 487,20 с Подр=(пусто)
3. ✅ pilot_post_02 sql пре-тест: SQL с `ДанныеДокумента.Подразделение` → «Производство»

## Алгоритм (8 шагов)

```bash
# 1. Бэкап
cp "C:/Configuration_downloads/BASERP25/Documents/ПоступлениеБезналичныхДенежныхСредств/Ext/ManagerModule.bsl" \
   "C:/Configuration_downloads/BASERP25/_Rarzrabotki/notebook/РегистрНакопления_ДенежныеСредстваВПути/_backup/PostBeznal_ManagerModule_BEFORE.bsl"

# 2-3. Baseline + SQL пре-тест
cd "C:/Configuration_downloads/BASERP25/_Rarzrabotki/notebook/РегистрНакопления_ДенежныеСредстваВПути/Python/test"
python pilot_post_01_baseline.py
python pilot_post_02_sql_pretest.py

# 4. Read функции (offset=2019 limit=280)

# 5. Edit 8 блоков
#    Минимально для пилота: блоки 1-4, 7-8 (6 блоков с ДанныеДокумента)
#    Курсовые блоки 5-6 — отдельно (вариант A через подзапрос для надёжности)

# 6-7. db-load-xml + db-update
powershell.exe -NoProfile -File "...db-load-xml.ps1" `
  -InfoBaseServer "SQLSERVER" -InfoBaseRef "BaseERP" -UserName "Администратор" -Password "24043" `
  -ConfigDir "C:\Configuration_downloads\BASERP25" `
  -Mode Partial -Files "Documents/ПоступлениеБезналичныхДенежныхСредств/Ext/ManagerModule.bsl"

powershell.exe -NoProfile -File "...db-update.ps1" `
  -InfoBaseServer "SQLSERVER" -InfoBaseRef "BaseERP" -UserName "Администратор" -Password "24043" `
  -Dynamic "+"

# 8. Repost + Verify
python pilot_post_03_repost.py
python pilot_post_04_verify.py
```

## Acceptance

PASS если:
1. ✅ 8 блоков SELECT содержат `Подразделение` (минимум блоки 1-4, 7-8 для пилота; 5-6 для конвертации)
2. ✅ `/db-update` прошёл
3. ✅ `pilot_post_03_repost.py` показывает `[OK] Проведено`
4. ✅ `pilot_post_04_verify.py` показывает PASS — Подр в РНДС.ВПути = «Производство»
5. ✅ Σ Сумма (6 487,20) НЕ изменилась
6. ✅ Прочие регистры без изменений

## Антипаттерны

- ❌ Edit `replace_all: true` — 8 блоков с похожей структурой, очень опасно
- ❌ Игнорировать курсовые блоки 5-6 (для документа без конвертации — OK для пилота, для других — нужна правка)
- ❌ Использовать `БанковскийСчет.Подразделение` — нужно ИЗ ШАПКИ
- ❌ Перепроводить через COM с РСКПС движениями (safety-check в pilot_post_03 защищает)
- ❌ `-- комментарии` в SQL — 1С не парсит, нужно `//` или без комментариев

## После успеха

| Документ | Источник Подразделения | Безопасность COM |
|---|---|---|
| **РасчетКурсовыхРазниц** (`ManagerModule.bsl:1250`) | особый — переоценка существующих записей | ✅ безопасно (нет РСКПС) |

5-й (и последний фактический) документ — РасчетКурсовыхРазниц. Архитектурно отличается (переоценивает существующие записи РНДС.ВПути), требует индивидуального подхода.

## Контакты

- Memory: те же что для ПКО/РКО/Спис
- Пилоты-предшественники: `PROMPT_pko_000001_pilot.md`, `PROMPT_rko_N0000053020_pilot.md`, `PROMPT_spis_beznal_00000019546_pilot.md`
- Baseline уже создан: `_artifacts/pilot_post_01_baseline.json`
- SQL пре-тест пройден: SQL с `ДанныеДокумента.Подразделение` → «Производство»
