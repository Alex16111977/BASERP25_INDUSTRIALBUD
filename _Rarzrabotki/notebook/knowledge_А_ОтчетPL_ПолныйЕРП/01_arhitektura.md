# 01. Архитектура: программная компоновка СКД

## Почему отчёт программный

Чистым СКД-запросом отчёт не собирается по трём причинам.

Во-первых, оси колонок нет в данных: месяцы периода, «PL с начала года», «Накопительный PL»
и «План итого» строит BSL (`ТаблицаКолонок`) и передаёт в запрос параметром-таблицей `&КолонкиPL`;
состав оси зависит от периода и от флажков блоков. Во-вторых, строки-показатели
водопада прибыли (Маржинальный доход, Операционная прибыль, Чистый доход, Рентабельность)
не существуют в данных — они считаются из агрегатов групп и добавляются в таблицу синтетически.
В-третьих, «полный шаблон» листа требует нулевых строк для статей, у которых нет ни факта,
ни плана: без них лист PL выглядит дырявым и не совпадает с Excel построчно.

Отчёт унаследовал этот подход от донора `А_ОтчетPL` (вариант `А_ОтчетPL_ВидКакВExcel`),
откуда взяты дословно четыре сложные CTE ЕРП-факта.

## Поток данных

`ПриКомпоновкеРезультата` (строка 21 модуля) выполняет девять шагов.

**Шаг 0 — период.** `Период` читается напрямую через
`КомпоновкаДанныхКлиентСервер.ПолучитьПараметр`; при незаполненной границе —
`ВызватьИсключение «Заполните параметр Период»`. Это ЕДИНСТВЕННАЯ обязательная настройка.
Параметра `Подразделение` в отчёте НЕТ: отбор по подразделению делает штатная СКД любым
способом — в иерархии, списком, по группе. Замер 18.08.2026: полный скан базы 0,4–1,9 с,
ограничивать выборку отбором на входе незачем.

**Шаг 0.1 — флажки.** `ЗначениеПараметра` (обёртка над `ПолучитьПараметр` с дефолтом) читает
`СводыНаправлений`, `ПоказИтогиПрибыли`, `ПолныйШаблон`, `ПоказатьСлужебныеСтроки`,
`ТолькоСтатьиСФиксами`, `НазванияНаРусском` (дефолт Ложь) и собирает структуру `Блоки` из
`ВыводитьМесяцы`, `ВыводитьСНачалаГода` (дефолт Ложь — сохранённые варианты не знают параметра
и не должны менять вид), `ВыводитьНакопительный`, `ВыводитьПлан`, `ВыводитьФиксы`.
Снятый флажок блока — это НЕ отбор:
колонки не создаётся вовсе, поэтому её ветка запроса данные и не читает.

**Шаг 1 — данные.** `ПолучитьДанные(НачалоПериода, КонецПериода, СводыНаправлений, Блоки)` —
ОДИН пакетный запрос из 11 временных таблиц (действующий текст — `v11_full.sql`), отдающий
сразу всю таблицу значений: факт ЕРП,
документный факт, план объекта и норму-фикс в единой форме строки. Отдельных запросов
«факт за период», «ЕРП-хвост», «документы», «план» больше нет. Правило выбора источника —
**регистры только за последний месяц периода, всё раньше только документы `А_ОтчетPL`,
исключений нет** — живёт внутри пакета ОДНИМ условием
`ГОД(Период) * 100 + МЕСЯЦ(Период) = &ПоследнийМесяц` в четырёх ЕРП-ветках. Таблицы
`втМесяцыДок` больше нет. См. [12_novaya_forma_ebitda_ebit.md](12_novaya_forma_ebitda_ebit.md).

**Шаг 1.1 — дивиденды.** `УдалитьСтрокиДивидендов` БЕЗУСЛОВНО убирает строки статьи
«Дивиденды» (000000058) — статья исключена из отчёта решением финансиста 21.08.2026,
в обоих режимах названий; данные в справочнике и регистрах не трогаются.

**Шаг 2 — читаемый вид колонки.** Колонка `ВидКолонки` по `КолонкаСорт`:
900100 → «План объекта», 900000 → «Накопительный PL», 890000 → «PL с начала года»,
остальное → «Месяц». Нужна, чтобы
в сохранённых настройках отбор шёл словами, а не числом 900 100.

**Шаг 3 — `Аналитика1`.** Строковое представление для группировки: у строк документа
`А_ОтчетPL` это `Комментарий` (комментарий финансиста из Excel), у остальных — `Аналитика`
(контрагент / физлицо / подотчётник, у РКО «Прочая выдача ДС», СписанияНедостач,
ВнутреннегоПотребления и ПрочихДоходовРасходов — комментарий регистратора, у амортизации ОС —
карточка `Справочник.ОбъектыЭксплуатации`).

**Шаг 4 — разметка листа.** `ДополнитьВидКакВExcel` — строки листа, полный шаблон, водопад,
контрольные строки. См. [05_razmetka_lista.md](05_razmetka_lista.md).

**Шаг 5 — подпись бюджетных строк без статьи PL.** Строки бюджета и нормы, чей ДДС не
привязан к статье PL, сворачивались бы в безымянную группу и читались как сбой отчёта.
Им проставляется `СтрокаЛиста = «Бюджет без статьи PL»` — только там, где разметка листа
не задала свою подпись.

**Шаг 6 — фильтр по фиксам.** При `ТолькоСтатьиСФиксами` процедура `ОставитьСтатьиСФиксами`
выбрасывает статьи без заведённой нормы. Строго ПОСЛЕ разметки: полный шаблон дорисовал бы
удалённые статьи обратно. См. [11_fiksy_norma_i_otklonenie.md](11_fiksy_norma_i_otklonenie.md).

**Шаг 7 — раскрытие источников.** `ЗаполнитьРаскрытиеИсточников` добавляет пять колонок
и заполняет их у ВСЕХ строк. Идёт ПОСЛЕ разметки — иначе строки-показатели и строки полного
шаблона остались бы без дат.

**Шаг 8 — русские наименования.** `ЗаполнитьРусскиеНаименования` добавляет служебные поля
`СтатьяPLРус` / `ГруппаРус` и заполняет их у всех строк в ОБОИХ режимах параметра
«Названия на русском» (числа от параметра не зависят). Строго ПОСЛЕ разметки и полного
шаблона — иначе часть строк статьи выводилась бы старым именем и лист двоился.

Дальше — стандартная компоновка: `ВнешниеНаборыДанных = Новый Структура("Данные", ТаблицаДанных)`,
перед компоновщиком `УстановитьЗаголовкиФ1Ф2` + `УстановитьЗаголовкиФ1Ф2ВСтруктуре` безусловно
ставят подписи колонок факта «Сумма по Ф1, грн» / «Сумма по Ф2, грн» (перекрывают подписи
сохранённых вариантов; «Безнал»/«Нал» — ОТМЕНЕНЫ 21.08.2026), затем
`КомпоновщикМакетаКомпоновкиДанных` → `ПроцессорКомпоновкиДанных` →
`ПроцессорВыводаРезультатаКомпоновкиДанныхВТабличныйДокумент`, а после вывода
`РусифицироватьСлужебныеТексты(ДокументРезультат)` дочищает служебную шапку от украинских
платформенных надписей (язык сеанса, 26.08.2026).

## Ключевой приём: ось колонок — параметр запроса

Оси нет в данных, поэтому её строит BSL и отдаёт в запрос как таблицу значений
`&КолонкиPL` (`КолонкаСорт`, `КолонкаПредставление`, `МесяцДата`), а запрос кладёт её
в `втКолонкиPL`. Регистровые ветки соединяются с осью условием

    ПО (КЛ.КолонкаСорт > 0 И КЛ.КолонкаСорт < 890000 И Д.МесяцКлюч = КЛ.КолонкаСорт)
        ИЛИ КЛ.КолонкаСорт = 900000
        ИЛИ (КЛ.КолонкаСорт = 890000 И Д.МесяцКлюч >= &КлючНачалаГода)

— одна и та же строка факта попадает и в свой месяц, и в «Накопительный PL», а при включённом
флажке — и в «PL с начала года» (890000, с 1 января года КОНЦА периода,
`&КлючНачалаГода = ГОД(КонецПериода)*100+1`). Отсюда
и правило «месяцы — ЧАСТЬ накопительного, а не добавка к нему». Маркер месячной колонки —
`КолонкаСорт < 890000` (НЕ `< 900000`!).

Зачем так, а не через `ВЫБОР` с нулями в чужих колонках: флажок блока просто не добавляет
строку в таблицу колонок, и ветка запроса перестаёт читать данные — снять «Месяцы» на
длинном периоде это самый дешёвый способ ускорить отчёт. Если сняты все флажки и таблица
колонок пуста, `ТаблицаКолонок` бросает исключение: выводить нечего.

Финальное объединение — шесть веток, каждая отдаёт ОДИН И ТОТ ЖЕ набор из 18 колонок
(чужие суммы = 0), поэтому склейка бесплатна и типы таблицы значений описывать не надо:

| Ветка | Источник | Колонки оси |
|---|---|---|
| Расходы ЕРП | `втЕРП_Расх_Детали` | месяцы + 890000 + 900000 |
| Доходы и CoGS ЕРП | `втЕРП_ДохCoGS_PL` | месяцы + 890000 + 900000 |
| Документы помесячно | `Документ.А_ОтчетPL.ДанныеОтчета` | месяцы (кроме `&ПоследнийМесяц`) |
| Документы накопительно | `Документ.А_ОтчетPL.ДанныеОтчета` | 900000 + 890000 (месяцы < `&ПоследнийМесяц`; в 890000 — только с `&КлючНачалаГода`) |
| План объекта | `РегистрНакопления.А_БюджетыНаМесяц`, ВидПериода = Объект | 900100 |
| Норма-фикс | `втФиксНорма` | месяцы (в 890000 фиксов и плана НЕТ) |

## Состав таблицы данных (18 колонок от запроса)

Подразделение · НаправлениеДеятельности · ПодразделениеСтрока ·
Группа · СтатьяPL · ДДС ·
КолонкаСорт · КолонкаPL ·
СуммаФ1 · СуммаФ2 · СуммаИтого ·
ФиксФ1 · ФиксФ2 · ФиксИтого ·
Документ · Комментарий · Контрагент · Аналитика

Плюс 16 колонок, добавляемых в BSL:

* `ПриКомпоновкеРезультата` — ВидКолонки, Аналитика1;
* `ЗаполнитьРусскиеНаименования` — СтатьяPLРус, ГруппаРус;
* `ДополнитьВидКакВExcel` — ВидСтроки, СтрокаЛиста, СортЛиста, ПоказыватьСтатью,
  БазаПроцента, СуммаСтатья, Знаменатель;
* `ЗаполнитьРаскрытиеИсточников` — НакопительноС, ДокументыПо, ЕРПС, ПланАктуаленНа,
  ИсточникиФакта.

Итого 34 — ровно столько полей в наборе данных СКД.

Сумм «за период» и «накопительно» больше НЕ бывает в одной строке: разрез задаёт
`КолонкаСорт`, суммы всегда лежат в одной паре `СуммаФ1 / СуммаФ2` (+ `СуммаИтого`).

`Подразделение` — это `ПодразделениеОтчета` из `втКартаУровней`: при `СводыНаправлений`
движение поднимается на ближайшего предка, у которого есть свод-документ направления,
иначе остаётся на своём подразделении. Свёртку ветки под нужный уровень делает уже отбор СКД.

## Роли файлов

| Файл | Роль |
|---|---|
| `Ext/ObjectModule.bsl` | Вся логика. Единственный источник истины |
| `Templates/…/Ext/Template.xml` | 34 поля набора, 3 вычисляемых поля, 19 итогов, 14 параметров, 3 варианта настроек |
| `Templates/ОсновнаяСхемаКомпоновкиДанных.xml` | Описание макета (имя, тип `DataCompositionSchema`) |
| `А_ОтчетPL_ПолныйЕРП.xml` | Корневой XML: имя, синоним, `DefaultForm = CommonForm.ФормаОтчета` |

Форма своя не делается: отчёт использует типовые формы БСП
(`ФормаОтчета` / `ФормаНастроекОтчета` / `ФормаВариантаОтчета`).

## Функции и процедуры модуля

Всего в модуле 21 процедура и функция (1820 строк).

| Строка | Имя | Роль |
|---|---|---|
| 21 | `ПриКомпоновкеРезультата` | Оркестратор (см. выше) |
| 148 | `ОставитьСтатьиСФиксами` | Фильтр «только статьи с заведённой нормой» |
| 173 | `УдалитьСтрокиДивидендов` | Безусловное удаление строк статьи «Дивиденды» (000000058) |
| 205 | `ЗаполнитьРусскиеНаименования` | Служебные поля СтатьяPLРус/ГруппаРус для «Названий на русском» |
| 253 | `УстановитьЗаголовкиФ1Ф2` | Подписи «Сумма по Ф1, грн» / «Сумма по Ф2, грн» в выборе настроек |
| 273 | `УстановитьЗаголовкиФ1Ф2ВСтруктуре` | То же — обход структуры настроек (группировки вариантов) |
| 295 | `РусифицироватьСлужебныеТексты` (Экспорт) | Русская служебная шапка после вывода |
| 338 | `ЗаменыУкраинскихТекстов` | Таблица замен укр→рус для служебной шапки |
| 381 | `ДобавитьЗамену` | Хелпер таблицы замен |
| 389 | `ЗначениеПараметра` | Параметр компоновки или дефолт |
| 406 | `ТаблицаКолонок` | Ось колонок: месяцы, 890000, 900000, 900100 |
| 467 | `ПолучитьДДСПриёмникCoGS` | ДДС с флагом `А_ПриёмникСебестоимостиПродажPL` |
| 491 | `ПолучитьСтатьюДоходовТовара` | СтатьяДоходов для товарной выручки (PL 000000002) |
| 521 | `КодыОсобыхСтатей` | Коды статей PL с особым поведением в водопаде — в одном месте |
| 552 | `ПолучитьДанные` | Пакет из 11 временных таблиц — весь факт, план и норма |
| 1337 | `ДополнитьВидКакВExcel` | Разметка листа, водопад, полный шаблон |
| 1631 | `ДобавитьКонтрольныеСтроки` | Контроль плана + предупреждение об отсутствии документов |
| 1669 | `РассчитатьВодопадКолонки` | Водопад одной колонки |
| 1703 | `ДобавитьСтрокуПоказателя` | Синтетическая строка водопада/контроля |
| 1726 | `ЗаполнитьРаскрытиеИсточников` | Четыре даты + текст для шапки |
| 1809 | `ПредставлениеДаты` | Пустая дата → «—» |

Функции `ПолучитьДДСПриёмникCoGS` и `ПолучитьСтатьюДоходовТовара` перенесены
из донора без изменений — они управляют веткой CoGS и расщеплением выручки по `ТипЗапасов`.
Их результат уходит в запрос параметрами `&ДДСCoGS` и `&СтатьяДоходовТовар`.
