# 13. Контур «Ввод остатков PL» и своды книги (31.08.2026)

## Зачем
Закрытые до начала месяца объекты выпадали из накопительных колонок свод-листов
книги (состав сводов строился только из строк РС `А_СтруктураВыгрузкиОтчетаPLвЕксел`
— активных листов). Решение финансиста: зафиксировать «PL с начала года» и
«Накопительный PL» на 30.06.2026 документами остатков и строить накопительные
СТРОГО от них.

## Объекты (в конфигурации BaseERP с 31.08.2026)

| Объект | Суть |
|---|---|
| РН `А_ОстаткиОтчетаPL` (обороты) | Измерения: Подразделение (СтруктураПредприятия), СтатьяPL (А_Статьи_PL), СводНаправления (Булево — слой, как ВключатьДочерние у А_ОтчетPL). Ресурсы: СуммаФ1СНачалаГода, СуммаФ2СНачалаГода, СуммаФ1Накопительно, СуммаФ2Накопительно |
| Документ `А_ВводОстатковОтчетаPL` | Шапка: Организация, Ответственный, Комментарий. ТЧ «Остатки» = измерения + 4 суммы. Прямые движения 1:1 (проектная конвенция А_*). Серверная `ЗаполнитьИзДокументовPL()` — из проведённых А_ОтчетPL по дату документа, оба слоя, нулевые строки отбрасываются |
| Отчёт `А_ОтчетPL_ПолныйЕРП_Остатки` | Клон v11 → запрос v12 (`_Rarzrabotki/Python/test/v12_full.sql`). Живёт в конфигурации И как исходники `_Rarzrabotki/Отчеты/А_ОтчетPL_ПолныйЕРП_Остатки` |

Действующий `А_ОтчетPL_ПолныйЕРП` НЕ менялся (v11).

## Остатки — ПО-ЛИСТОВО (решение финансиста 31.08.2026)
Документ на каждый лист книги, Комментарий = «Остатки PL на 30.06.2026 — лист «X»»:
- объектный лист → объектный слой его подразделений (сам + дочерние по дереву);
- лист «Только это подразделение» (ЦО-листы) → объектный слой корня;
- свод-лист (ОтчетПоНаправлению) → СВОД-слой корня направления;
- строки без листа (закрытые, Староконстантинов-свод и пр.) → НЕ фиксируются
  (решение финансиста 31.08.2026: документ «ВНЕ ЛИСТОВ» на 386 строк был создан
  и УДАЛЁН — распроведён, помечен). Следствие принято: закрытый объект в отчёте
  остатков интерактивно покажет нули в накопительных; своды книги НЕ страдают —
  они берут свод-слой корней направлений (закрытые внутри свод-остатков).
  Инвариант «v12 == v11 в объектном слое» для подразделений без листа больше
  не выполняется (тест v12 писался до удаления);
- PL_Свод — агрегат, своего документа НЕТ.
Итого 30 документов, 860 строк (637 объектный слой + 223 свод).
Раскладчик: `raskladka_ostatkov_po_listam_2026-08-31.py` (идемпотентен по комментарию).

**Контроль в ПередЗаписью:** комбинация Подразделение × СтатьяPL × СводНаправления —
ровно в ОДНОМ проведённом документе (запрос по регистру против ТЧ через
`&ТаблицаКлючей`); пересечение = отказ с именем конфликтующего документа.

## Запрос v12 (отличия от v11)
1. Документная накопительная ветка: `И ГОД(Док.Дата)*100+МЕСЯЦ(Док.Дата) > &МесяцОстатков`.
2. Новая ветка ОБЪЕДИНИТЬ ВСЕ из `РегистрНакопления.А_ОстаткиОтчетаPL` в колонки
   890000/900000: `ГДЕ Ост.СводНаправления = &СводыНаправлений`, суммы через
   `ВЫБОР КОГДА КЛ.КолонкаСорт = 890000 ТОГДА …СНачалаГода ИНАЧЕ …Накопительно КОНЕЦ`,
   Документ = Ост.Регистратор (расшифровка ведёт на документ остатков листа).
3. `&МесяцОстатков` ставит модуль: `МесяцДокументаОстатков()` — ГГГГ*100+ММ даты
   последнего проведённого документа; нет ни одного → ВызватьИсключение.

## Выгрузка книги (ВыгрузкаОтчетаPLвExcel, с 31.08.2026)
- Строится отчётом `А_ОтчетPL_ПолныйЕРП_Остатки` (не действующим!).
- Свод направления и PL_Свод — СЛОЙ СВОДОВ: `УстановитьСводыНаправлений`
  (параметр СводыНаправлений=Истина поверх варианта) + пара отборов: «В списке» =
  корень направления (PL_Свод — корни всех строк-сводов; ЦО отсекает «Не в группе ЦО»
  самого варианта). Состав листов книги на цифры сводов больше НЕ влияет.
- Функции СоставСводаНаправления/СоставОбщегоСвода УДАЛЕНЫ (мёртвый код).
- Объектные и ЦО-листы — механика прежняя (объектный слой, состав/корень).
- .epf пересобран (при занятом BaseERP — через BaseERPRazr).

## Эталоны сводов июльской книги (приняты 31.08.2026, строка ОД, «PL с начала года»)
черв. книга + живой июль: PL_Свод 481 278 773,38; Строительство 399 223 454,66;
Производство 63 083 595,36; Логистика 3 349 603,38; Техника 15 622 119,98.
Червневые константы (= свод-слой А_ОтчетPL, копейка-в-копейку с книгой):
Строительство 339 313 061,76; Производство 51 004 810,23; Логистика 2 788 550,00;
Спецтехника 12 404 410,34.
⚠ Цели PROMPT-сверки 30.08 (481 574 592,96 и др.) УСТАРЕЛИ: снимались с данных
того дня; проверено — июль по старому составу == июль по полному дереву до копейки,
расхождение целиком от изменения живых июльских оборотов.

## «% суммы затрат в общем объеме» — база по отбору (v13, 01.09.2026, ОБА отчёта)
Семантика: доля статьи в «Итого затрат» ТОЙ ЖЕ колонки (СС+ДР+ОПЗ+Аморт+МЗ+АЗ+НС+
3 финрасхода+налог на прибыль; Дивиденды исключены). Сумма затратных статей = 100%.
БЫЛО: база per-подразделение, знаменатель `Максимум(БазаПроцента)` → при отборе
по ГРУППЕ подразделений база одного объекта: СС 157%, аналитика > родителя.
СТАЛО: модуль читает отбор компоновщика (`НастройкиОтчета.Отбор` → 
`МножествоПодразделенийОтбора`: поля Подразделение/НаправлениеДеятельности,
Равно/ВСписке точно, В(Списке)ПоИерархии — группами, НеВ* — исключения; запрос по
СтруктураПредприятия с булевым &ЕстьВключающие) и пишет ВСЕМ строкам колонки ЕДИНУЮ
базу (`БазыЗатратПоОтбору`). СКД не менялась: `Максимум(БазаПроцента)` по константе
корректен на всех уровнях. Строки группы ОД базы не получают → их % пуст.
«Финансовая деятельность» в водопаде — НЕТТО (доходы − расходы), минус легален.
⚠ Грабли СКД, добытые фактом: `Вычислить(...,"ОбщийИтог")` в кросс-таблице смешивает
ОБЕ оси (знаменатель по всем колонкам); имя поля («КолонкаСорт») как группировка в
`Вычислить` не резолвится — «Выражение не может быть вычислено». Поэтому расчёт в BSL.
⚠ Клон внешнего отчёта: xr:ClassId — ПЛАТФОРМЕННЫЙ id класса, не регенерировать
(«Неверный идентификатор класса хранимого объекта»); GeneratedType name и
MainDataCompositionSchema внутри корневого XML тоже несут ИМЯ отчёта — заменить.
Тесты: test_procent_zatrat_v13_2026-09-01.py (7/7, внешний RED-erf + регрессия
1 объекта против старого) и acceptance_procent_zatrat_2026-09-01.py (10/10, оба
конфигурационных отчёта; СС 82%/86% на «в группе Строительство»).

## Тесты (все зелёные 31.08.2026)
- `test_otchetpl_ostatki_v12_2026-08-31.py` — 10/10: v12 == v11 агрегатно
  (202607/890000/900000, оба слоя), регистратор остатков в накопительных.
- `acceptance_otchetpl_ostatki_smoke_2026-08-31.py` — 8/8: новый отчёт против
  старого полным путём (вариант+компоновка), объектный лист и свод ячейка-в-ячейку
  (пользовательские группировки раскрытия отключаются перед сверкой).
- `acceptance_kniga_svody_celi_2026-08-31.py` — 5/5: свод-листы книги = червень
  (константы) + живой июль (независимый контрольный запрос).
- `create_ostatki_doc_2026-08-31.py` — get-or-create единого документа (историч.).

## Грабли
- Частичная загрузка НОВЫХ объектов: Configuration.xml + корневые XML объектов +
  модули; формы — ТОЛЬКО корневой `Forms/<Форма>.xml` (явный Ext → «Неизвестный
  объект метаданных»). Реструктуризация двух пустых таблиц — минуты (принятие
  изменений всей конфигурации), пользователи в Предприятии не мешают.
- Пакетный сеанс загрузки виден как «Конфигуратор» в списке сеансов — его
  завершение = срыв загрузки («Сеанс работы завершен администратором»).
- `-listFile` у DumpConfigToFiles 8.3.20 ждёт ИМЕНА ОБЪЕКТОВ («Configuration»),
  не имена файлов, и БЕЗ BOM; выгрузка требует ConfigDumpInfo.xml в каталоге.
- Клонирование отчёта: регенерировать UUID ТОЛЬКО в корневых XML метаданных;
  в СКД Template.xml UUID НЕ трогать — `dcsset:right` хранит ссылки-значения
  отборов вариантов (регенерация ломает отборы).
- Алиас `В` в запросе = оператор В (IN) — «Ожидается имя»; у ВариантыОтчетов
  НЕТ ПометкиУдаления; у Организаций НЕТ Кода.
- Дерево ≠ направление: закрытые объекты переносят в группу/направление
  «Закрытые обьекты» или направление пусто — отбор по А_НаправлениеДеятельности
  теряет их; свод-слой (свод-доки корня) закрытых СОДЕРЖИТ.
