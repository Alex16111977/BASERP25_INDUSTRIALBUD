# ФормаPL «остальные» (Часть A + Часть B) — Implementation Plan

> **For agentic workers:** РЕАЛИЗАЦИЯ построчно по задачам. Чекбоксы (`- [ ]`) — для трекинга.
> Источник требований: [PROMPT_ФормаPL_ПрочиеРасходы_Остальные_Форма1.md](PROMPT_ФормаPL_ПрочиеРасходы_Остальные_Форма1.md).
> Дизайн (anchors): [formapl_ostalnye_DESIGN.md](formapl_ostalnye_DESIGN.md). Прецедент-скрипты: [formapl_ozfu_done.md](formapl_ozfu_done.md) §6.

**Goal:** Заполнить измерение `ФормаPL` во всех движениях `РегистрНакопления.ПрочиеРасходы` — `РаспределениеПрочихЗатрат` наследует форму из остатков (Часть A), все прочие регистраторы по умолчанию → `Форма1` (Часть B, register-level).

**Architecture:** Часть A — протянуть `ФормаPL` (измерение, доступно в `.Остатки`) «спутником» по цепочке ВТ собственного запроса распределения в `ManagerModule.bsl`; запись движений подхватит форму через `ЗаполнитьЗначенияСвойств`. Часть B — 5-строчный цикл-дефолт в `RecordSetModule.ПередЗаписью`. Строгий порядок **A → (Rule #-1 green) → B**.

**Tech Stack:** 1С:Предприятие 8.3.20 BSL · BAS ERP 2.5 · Python 3 + `win32com` (V83.COMConnector → BaseERP) · MCP `1c-workerp` · db-skills (`/db-load-xml`, `/db-update`).

**Соединение (все Python-скрипты):** `Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"`. Скрипты класть в `_Rarzrabotki/Python/test/`, запускать `python <путь>` (НЕ inline — кириллица в `-c` портится). Период — границами серверно (`НачалоМесяца`/`КонецМесяца`/`Граница`), не naive datetime.

**Пути файлов кода (Rule #4 — править в worktree, копировать в основную конфигурацию):**
- Часть A: `Documents/РаспределениеПрочихЗатрат/Ext/ManagerModule.bsl`
- Часть B: `AccumulationRegisters/ПрочиеРасходы/Ext/RecordSetModule.bsl`
- worktree-корень: `C:\Configuration_downloads\BASERP25\.claude\worktrees\amazing-cartwright-301cba\`
- основная конфигурация (откуда грузит Designer/сервер): `C:\Configuration_downloads\BASERP25\`

---

## ЧАСТЬ A — `РаспределениеПрочихЗатрат` наследует `ФормаPL`

### Task A0: Pre-flight — метаданные, тест-документ, эталон Σ

**Files:**
- Create: `_Rarzrabotki/Python/test/discovery_raspredelenie_formapl.py`

- [ ] **Шаг 1 (Rule #0): подтвердить метаданные.** Через MCP `get_metadata_structure` (`AccumulationRegister.ПрочиеРасходы`) убедиться: `ФормаPL` — **измерение** #7, тип `ПеречислениеСсылка.А_ФормыPL`. Через `find_enum_ref` подтвердить `Перечисления.А_ФормыPL.Форма1` и `.Форма2` существуют.

- [ ] **Шаг 2: написать `discovery_raspredelenie_formapl.py`.** Скрипт должен:
  1. Найти **тест-документ**: проведённый `РаспределениеПрочихЗатрат` за месяц с **несколькими приёмниками** (чтобы была погрешность округления) — запрос движений `ПрочиеРасходы.Обороты(...) ГДЕ Регистратор ССЫЛКА Документ.РаспределениеПрочихЗатрат`, выбрать регистратор с максимумом строк-движений.
  2. Снять **эталон Σ** этого документа по `ПрочиеРасходы` (прямая таблица, `ВидДвижения=Расход`): Σ по `Сумма, СуммаБезНДС, СуммаУпр, СуммаУпрБезНДС, СуммаРегл, СуммаРеглБезНДС, НДСРегл`.
  3. Снять **эталон баланса по форме источника**: для каждого (Орг,Подр,Напр,Статья,Аналитика) распределяемой затраты — какая `ФормаPL` у её исходного `Приход` (через `ПрочиеРасходы.Остатки(&ГраницаНаНачало, ...)` ДО распределения, разрез по `ФормаPL`).
  4. Напечатать: `РегистраторТест=<ссылка>`, `Период=<...>`, эталоны Σ, распределение исходных приходов по форме.

```python
import win32com.client, sys
if sys.stdout.encoding != 'utf-8':
    sys.stdout.reconfigure(encoding='utf-8')
v8 = win32com.client.Dispatch("V83.COMConnector")
erp = v8.Connect('Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')

# 1) тест-документ: РаспределениеПрочихЗатрат с макс. числом движений ПрочиеРасходы
q = erp.NewObject("Запрос")
q.Text = """
ВЫБРАТЬ Дв.Регистратор КАК Рег, КОЛИЧЕСТВО(*) КАК К
ИЗ РегистрНакопления.ПрочиеРасходы КАК Дв
ГДЕ ТИПЗНАЧЕНИЯ(Дв.Регистратор) = ТИП(Документ.РаспределениеПрочихЗатрат)
    И Дв.Активность
СГРУППИРОВАТЬ ПО Дв.Регистратор
УПОРЯДОЧИТЬ ПО К УБЫВ
"""
sel = q.Execute().Выбрать()
sel.Следующий()
рег = sel.Рег
print("РегистраторТест =", erp.XMLСтрока(рег), "| движений =", sel.К)
print("Период =", рег.Дата)
# 2) эталон Σ (Расход)  + 3) разрез по ФормаPL — отдельными запросами по &Рег / &ГраницаНаНачало
# ... (см. tests_and_etalons.md §2; границы серверно)
```

- [ ] **Шаг 3: запустить.** `python _Rarzrabotki/Python/test/discovery_raspredelenie_formapl.py`
  Expected: печатает `РегистраторТест`, `Период`, эталон Σ (7 ресурсов, ≠0), таблицу исходных приходов по `ФормаPL` (видно и Ф1, и Ф2 — иначе взять другой документ/месяц, где есть смешанная форма). **Зафиксировать эти числа как baseline.**

- [ ] **Шаг 4: commit** (только скрипт; knowledge untracked).
```bash
git add _Rarzrabotki/Python/test/discovery_raspredelenie_formapl.py
git commit -m "test(формапл-розподіл): discovery — тест-документ + еталон Σ + розріз по формі"
```

---

### Task A1: Baseline-харнесс — реконструкция текущей цепочки == регистр (БЕЗ ФормаPL)

Доказать, что мы умеем собрать и исполнить запрос распределения из .bsl и получить **те же** движения, что в регистре. Это фундамент: только на верной реконструкции можно проверять правку (Rule #-1, как `verify_formapl_prochie_chain.py` для ОЗФУ).

**Files:**
- Create: `_Rarzrabotki/Python/test/verify_raspredelenie_formapl_chain.py`

- [ ] **Шаг 1: извлечь текст запроса** процедуры `РаспределитьРасходыНаФинансовыйРезультат` из `Documents/РаспределениеПрочихЗатрат/Ext/ManagerModule.bsl` (de-pipe строкового литерала: брать контент после `|`, заменять `""`→`"`). В реальном проведении строка 1114 делает `СтрЗаменить(Текст, "ПОМЕСТИТЬ РезультатРаспределения", "")` — воспроизвести, чтобы `РезультатРаспределения` стал финальным SELECT.

- [ ] **Шаг 2: воспроизвести служебные ВТ.** Процедура вызывает `ПодготовитьСлужебныеТаблицы(Период, Организации, Запрос)` (стр. 43) — она кладёт в МВТ таблицы `Регистраторы`, `БазаРаспределения`, `БазыРаспределенияСводно`. Вариант: вызвать саму процедуру через COM на тест-документе и перехватить МВТ, **или** воспроизвести её запросы. Проще: исполнять весь пакет в одном `Запрос` с тем же МВТ, установив параметры как в коде (стр. 32-39): `НачалоПериода/КонецПериода/ГраницаПериода/МассивОрганизаций/ПоВсемОрганизациям/ФормироватьФинансовыйРезультат/ДокументРаспределения/ПредварительныйРасчет`. Для тест-прогона `ДокументРаспределения = &РегистраторТест`, `ПредварительныйРасчет = Истина`.

- [ ] **Шаг 3: собрать финал в проверку.** Обернуть финальный SELECT (`РезультатРаспределения`) в:
```
ВЫБРАТЬ СУММА(Т.Сумма) КАК Сумма, СУММА(Т.СуммаУпр) КАК СуммаУпр, СУММА(Т.СуммаРегл) КАК СуммаРегл, ...
ИЗ (<финальный select>) КАК Т
```
и сверить с эталоном Σ из A0.

- [ ] **Шаг 4: запустить.** `python _Rarzrabotki/Python/test/verify_raspredelenie_formapl_chain.py`
  Expected: `BASELINE OK` — Σ реконструкции == эталон A0 по всем 7 ресурсам (Δ ≤ 0.01). Если ≠ — чинить реконструкцию/служебные ВТ ДО перехода к A2 (иначе проверка правки бессмысленна).

- [ ] **Шаг 5: commit.**
```bash
git add _Rarzrabotki/Python/test/verify_raspredelenie_formapl_chain.py
git commit -m "test(формапл-розподіл): baseline-харнес — реконструкція == регістр (без ФормаPL)"
```

---

### Task A2: Протянуть `ФормаPL` по основному пути (RED→GREEN, Python COM)

**Files:**
- Modify: `_Rarzrabotki/Python/test/verify_raspredelenie_formapl_chain.py`

Применить в **тексте запроса внутри скрипта** (не в .bsl!) правки основного пути из DESIGN §3.1 (#1-10):

- [ ] **Шаг 1: добавить `ФормаPL` в ВТ #1-9** по таблице DESIGN §3.1:
  - `ПредварительныеОстаткиРасходов` SELECT + ИНДЕКС: `Расходы.ФормаPL КАК ФормаPL` (anchor 52/76).
  - `ОстаткиРасходов`: обе ветки UNION SELECT (`Расходы.ФормаPL` / `ТекущиеДвижения.ФормаPL` ×2), **условие INNER JOIN** `И (ТекущиеДвижения.ФормаPL = Остатки.ФормаPL)` (anchor 250), внешний SELECT + GROUP BY (anchor 86-101/288).
  - `РасходыКРаспределению` SELECT: `ОстаткиРасходов.ФормаPL КАК ФормаPL` (anchor 312); join к `Регистраторы` НЕ трогать.
  - `РаспределенныеРасходы` SELECT: `РасходыКРаспределению.ФормаPL КАК ФормаPL` (anchor 702).
  - `РезультатРаспределения` внешний SELECT + GROUP BY + ветка #1 (`РаспределенныеРасходы.ФормаPL`) (anchor 955-982/985-1015).

- [ ] **Шаг 2: обновить проверку** — добавить разрез по форме:
```
ВЫБРАТЬ Т.ФормаPL КАК ФормаPL, СУММА(Т.Сумма) КАК Сумма, СУММА(Т.СуммаРегл) КАК СуммаРегл, ...
ИЗ (<финальный select c ФормаPL>) КАК Т СГРУППИРОВАТЬ ПО Т.ФормаPL
```

- [ ] **Шаг 3: запустить (ожидаем частичный успех).** `python ...\verify_raspredelenie_formapl_chain.py`
  Expected:
  - **Σ-инвариант:** Σ по всем ресурсам == эталон A0 (Δ ≤ 0.01). ✅ обязательно.
  - **Разрез по форме:** появились строки Ф1/Ф2; Σ(Ф1)+Σ(Ф2) == Σ(эталон).
  - **Закрытие баланса по форме:** Σ(Расход по форме Ф) ≈ Σ(исходный приход по форме Ф) — **может разойтись на копейки** из-за погрешности округления (ещё не протянута). Зафиксировать величину расхождения (ожидается ≤ несколько копеек на регистратор).

- [ ] **Шаг 4: commit.**
```bash
git add _Rarzrabotki/Python/test/verify_raspredelenie_formapl_chain.py
git commit -m "test(формапл-розподіл): основний шлях ФормаPL — Σ-інваріант OK, розріз Ф1/Ф2 зʼявився"
```

---

### Task A3: Протянуть `ФормаPL` по пути погрешности округления (закрытие баланса до копейки)

**Files:**
- Modify: `_Rarzrabotki/Python/test/verify_raspredelenie_formapl_chain.py`

Применить правки пути погрешности из DESIGN §3.2 (#11-15) — это закрывает копеечный разрыв из A2.

- [ ] **Шаг 1: `ПогрешностиОкругления`** (anchor 776-842): добавить `ФормаPL` в внешний SELECT (786-787), в обе ветки UNION (`РасходыКРаспределению.ФормаPL` 790-801 / `РаспределенныеРасходы.ФормаPL` 809-820) и в `СГРУППИРОВАТЬ ПО` (824).

- [ ] **Шаг 2: `НаправленияДеятельностиДляПогрешности`** (anchor 758-772): добавить `ФормаPL` в SELECT и `СГРУППИРОВАТЬ ПО` (приёмник погрешности выбирается per-форма).

- [ ] **Шаг 3: `РезультатРаспределения` ветка погрешности (UNION #2)** (anchor 1019-1054): добавить `ПогрешностиОкругления.ФормаPL` и `ФормаPL` в условия join (1050-1054). Дочитать и протянуть **UNION #3** (1056-1115) тем же приёмом.

- [ ] **Шаг 4: оценить `МаксимальныеКоэффициентыПоРегистраторам`** (anchor 743-754): прогнать без правки; если закрытие по форме сошлось — оставить как есть; если нет — добавить `ФормаPL` в GROUP BY и пере-прогнать.

- [ ] **Шаг 5: запустить (ожидаем полный успех).** `python ...\verify_raspredelenie_formapl_chain.py`
  Expected: **GREEN** —
  - Σ-инвариант == эталон (Δ ≤ 0.01);
  - **закрытие баланса по форме: Σ(Расход Ф) == Σ(исходный приход Ф) для каждой формы, Δ ≤ 0.01** (копеечный разрыв из A2 устранён);
  - нет «зависших» Ф2→Ф1.

- [ ] **Шаг 6: commit.**
```bash
git add _Rarzrabotki/Python/test/verify_raspredelenie_formapl_chain.py
git commit -m "test(формапл-розподіл): шлях похибки округлення — закриття балансу по формі до копійки (GREEN)"
```

---

### Task A4: Перенести валидированные правки в BSL

**Files:**
- Modify (worktree): `...\amazing-cartwright-301cba\Documents\РаспределениеПрочихЗатрат\Ext\ManagerModule.bsl`
- Copy to: `C:\Configuration_downloads\BASERP25\Documents\РаспределениеПрочихЗатрат\Ext\ManagerModule.bsl`

- [ ] **Шаг 1: бэкап.** Скопировать текущий `ManagerModule.bsl` → `_Rarzrabotki/Python/test/_raspredelenie_ManagerModule.bak_formapl`.

- [ ] **Шаг 2: применить правки** ровно те, что прошли GREEN в A3 (anchors DESIGN §3.1+§3.2). Каждую вставку — точечным `Edit` с уникальным контекстом; проверять, что не задеты ветки сумм/коэффициентов. Файл UTF-8 с BOM — сохранить кодировку.

- [ ] **Шаг 3: статический контроль** — `Греп` по новому файлу: число вхождений `ФормаPL` == ожидаемому (по числу правок); нет переменной/алиаса `Ссылка`/`Выполнить`.

- [ ] **Шаг 4: cp в основную конфигурацию (Rule #4).**
```bash
cp ".../amazing-cartwright-301cba/Documents/РаспределениеПрочихЗатрат/Ext/ManagerModule.bsl" \
   "C:/Configuration_downloads/BASERP25/Documents/РаспределениеПрочихЗатрат/Ext/ManagerModule.bsl"
```

- [ ] **Шаг 5: commit.**
```bash
git add Documents/РаспределениеПрочихЗатрат/Ext/ManagerModule.bsl
git commit -m "feat(формапл-розподіл): РаспределениеПрочихЗатрат успадковує ФормаPL з залишків (Частина A)"
```

---

### Task A5: BSL review + загрузка + smoke

**Files:** —

- [ ] **Шаг 1: BSL review.** `/1c-bsl-review Documents/РаспределениеПрочихЗатрат/Ext/ManagerModule.bsl` — устранить критичные замечания (зарезервированные имена, алиасы, читаемость вставок).

- [ ] **Шаг 2: загрузка** (Конфигуратор закрыт!).
  `/db-load-xml -Mode Partial` (только этот `ManagerModule.bsl`) → "Load completed successfully";
  `/db-update -Dynamic+` → "Обновление конфигурации успешно завершено".

- [ ] **Шаг 3: smoke — перепровести тест-документ.** Через MCP `unpost_document`+`post_document` ИЛИ Python COM `obj.Записать(РежимЗаписиДокумента.Проведение)` на `РегистраторТест` из A0 (подтвердить, что COM-перепроведение `РаспределениеПрочихЗатрат` восстанавливает движения 1:1 — это документ закрытия месяца).
  Затем `discovery_raspredelenie_formapl.py` (или его проверочная ветка):
  Expected: движения `ПрочиеРасходы` тест-документа имеют `ФормаPL` (Ф1/Ф2 по форме источника, не пусто); Σ == эталон A0; закрытие по форме сходится.

- [ ] **Шаг 4 (GATE):** только если smoke зелёный → переходить к Части B. Если COM-перепроведение ненадёжно — зафиксировать, smoke перенести в UI и согласовать с пользователем.

---

## ЧАСТЬ B — register-level дефолт `пусто → Форма1` (только после зелёной Части A)

### Task B1: Правка `RecordSetModule.ПередЗаписью`

**Files:**
- Modify (worktree): `...\amazing-cartwright-301cba\AccumulationRegisters\ПрочиеРасходы\Ext\RecordSetModule.bsl`
- Copy to: `C:\Configuration_downloads\BASERP25\AccumulationRegisters\ПрочиеРасходы\Ext\RecordSetModule.bsl`

- [ ] **Шаг 1: вставить цикл-дефолт** в начало `Процедура ПередЗаписью(Отказ, Замещение)`, ПЕРЕД `Если ОбменДанными.Загрузка`:
```bsl
	// А_ФормаPL: дефолт формы учёта. Движения, которым документ-регистратор не задал ФормаPL явно
	// (ОЗФУ/РКО/РаспределениеПрочихЗатрат ставят сами), считаем официальной затратой → Форма1.
	Для Каждого Запись Из ЭтотОбъект Цикл
		Если НЕ ЗначениеЗаполнено(Запись.ФормаPL) Тогда
			Запись.ФормаPL = Перечисления.А_ФормыPL.Форма1;
		КонецЕсли;
	КонецЦикла;

```
(Старый `Если ОбменДанными.Загрузка … Возврат` и ниже — без изменений.)

- [ ] **Шаг 2: cp в основную конфигурацию (Rule #4).**
```bash
cp ".../amazing-cartwright-301cba/AccumulationRegisters/ПрочиеРасходы/Ext/RecordSetModule.bsl" \
   "C:/Configuration_downloads/BASERP25/AccumulationRegisters/ПрочиеРасходы/Ext/RecordSetModule.bsl"
```

- [ ] **Шаг 3: commit.**
```bash
git add AccumulationRegisters/ПрочиеРасходы/Ext/RecordSetModule.bsl
git commit -m "feat(формапл-розподіл): register-level дефолт ПрочиеРасходы пусто→Форма1 (Частина B)"
```

---

### Task B2: BSL review + загрузка + smoke (3 сценария)

**Files:** —

- [ ] **Шаг 1: BSL review.** `/1c-bsl-review AccumulationRegisters/ПрочиеРасходы/Ext/RecordSetModule.bsl`.

- [ ] **Шаг 2: загрузка.** `/db-load-xml -Mode Partial` (этот `RecordSetModule.bsl`) + `/db-update -Dynamic+`.

- [ ] **Шаг 3: smoke (а) — «прочий» документ → Форма1.** Перепровести 1 `ПриобретениеТоваровУслуг` ИЛИ `СписаниеНедостачТоваров` (через MCP/COM — это не ОЗФУ). Проверить: движения `ПрочиеРасходы` = `Форма1`, Σ неизменна.

- [ ] **Шаг 4: smoke (б) — ОЗФУ/РКО не затёрты.** Проверить запросом `ПрочиеРасходы.Обороты ГДЕ Регистратор ССЫЛКА ...` уже-перепроведённый ОЗФУ (Ф1/Ф2 сохранены) и РКО (Ф2). Дефолт их не трогает (форма заполнена). ОЗФУ перепроводить только в UI (COM задваивает ТЧ) — здесь только чтение.

- [ ] **Шаг 5: smoke (в) — РаспределениеПрочихЗатрат сосуществует.** Перепровести тест-документ A0 ещё раз: формы по-прежнему унаследованы (Часть A), дефолт Части B их не перетёр; Σ == эталон.

- [ ] **Шаг 6:** все три сценария зелёные → код готов.

---

## Финализация

### Task F1: Обновить базу знаний

**Files:**
- Modify: `knowledge_ПрочиеРасходы/KNOWLEDGE_MAP.md`, `formapl_roadmap.md`, `tests_and_etalons.md`, `register_structure.md` (§6 — RecordSetModule теперь содержит дефолт)
- Create: `knowledge_ПрочиеРасходы/formapl_ostalnye_done.md` (запись о реализации: точки правки, эталоны, скрипты — по образцу `formapl_ozfu_done.md`)

- [ ] **Шаг 1:** пометить Этап «остальные» DONE в `formapl_roadmap.md` и таблице состояния `KNOWLEDGE_MAP.md`; внести эталоны тест-документа в `tests_and_etalons.md`; в `register_structure.md §6` отметить, что `ПередЗаписью` теперь ставит дефолт `Форма1`.

- [ ] **Шаг 2:** записать `formapl_ostalnye_done.md` (что сделано в Части A — список ВТ/anchors; Часть B — дефолт; верификация Σ + закрытие по форме; скрипты `discovery_raspredelenie_formapl.py`, `verify_raspredelenie_formapl_chain.py`; бэкап).

### Task F2: Завершение ветки

- [ ] **Шаг 1:** напомнить пользователю порядок перепроведения истории в UI (сначала источники, в конце `РаспределениеПрочихЗатрат`) — массово НЕ автономно (`feedback_user_reposts_history_self`).
- [ ] **Шаг 2:** `verification-before-completion` (Σ, закрытие по форме, ОЗФУ/РКО целы) → `finishing-a-development-branch` (merge в `claude/main`, при необходимости push/PR — автономно, `feedback_autonomous_finish_merge_push_pr`).

---

## Self-review (покрытие спецификации)

| Требование DESIGN/PROMPT | Задача |
|---|---|
| Часть A: основной путь ФормаPL (ВТ #1-10) | A2 (Python) → A4 (BSL) |
| Часть A: путь погрешности округления (ВТ #11-15) | A3 (Python) → A4 (BSL) |
| Часть A: Σ-инвариант + закрытие по форме (Rule #-1 ПЕРЕД BSL) | A1 baseline, A2/A3 проверки |
| Часть A: запись движений через ЗаполнитьЗначенияСвойств (без правок) | подтверждается в A5 smoke |
| Часть B: дефолт пусто→Форма1 перед Возврат-ами | B1 |
| Часть B: ОЗФУ/РКО не затёрты | B2 шаг 4 |
| Порядок A→B со шлюзом | A5 шаг 4 (GATE) |
| Перепроведение истории — пользователь в UI | F2 шаг 1 |
| Rule #4 cp + db-skills загрузка | A4/A5, B1/B2 |
| Обновление БЗ | F1 |
