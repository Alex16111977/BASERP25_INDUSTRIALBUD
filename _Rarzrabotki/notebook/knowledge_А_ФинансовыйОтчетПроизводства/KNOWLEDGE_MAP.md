# KNOWLEDGE MAP — Документ.А_ФинансовыйОтчетПроизводства

Entry-point для ИИ-сессий. **Прочитать первым** перед любой доработкой документа.

> `Документ.А_ФинансовыйОтчетПроизводства` (синоним «Фінансовий звіт виробництва»).
> База: **BuhBud** — `Srvr="SQLSERVER";Ref="bas_industrialbud";Usr="cfo";Pwd="2442"`.
> Дамп: `_Rarzrabotki/BASEBuh`. **НЕ BaseERP.**
> Источник истины — **живой код** `_Rarzrabotki/BASEBuh/Documents/А_ФинансовыйОтчетПроизводства/`,
> а не эта база знаний.

---

## TL;DR за 60 секунд

Документ заменяет ручной Excel «Финансовый_отчет Производства»: собирает из бухучёта то, что
экономист сводил руками. **Непроводимый** (`Posting=Deny`) — движений по регистрам не делает,
это калькулятор с сохранением результата, расшифровкой и печатью.

1. **Разрез** — `ДоговорКонтрагента.Подразделение` (справочник `Подразделения`, те же имена,
   что в отчёте «План факт Производство» из базы **kazna**). Фильтр по `Направление` = «Производство».
   НЕ `ПодразделенияОрганизаций` — это другое дерево, совпадений с отчётом ноль.
2. **Источник** — счёт **361** «Расчеты с отечественными покупателями», субконто
   1 = Контрагенти, 2 = Договори, 3 = Документи розрахунків.
3. **Четыре колонки** ([02](02_metodika_kolonok.md)):
   `E` — оборот Дт по документам **с** отметкой `ОтправкаПолучениеДокументов.ДокументыПолучены`;
   `F` — оборот Кт; `H` — проведённые **без** отметки + **непроведённые** документы;
   `G = E − F` (это **НЕ сальдо**: сальдо содержит ещё и H).
4. **Параметры расчёта живут в РЕГИСТРЕ** `РС.А_ПараметрыДоговоровФинотчета`
   (с 14.08.2026; измерение Договор — ведущее, 5 ресурсов). Реквизиты `А_*` из
   `Справочник.ДоговорыКонтрагентов` удалены. Ввод — список регистра, TSV-цикл ([04](04_parametry_dogovora.md)).
5. **Строка результата = ДОГОВОР** (ТЧ `Расчет`: +Контрагент, +Договор). Итог K считается
   по варианту формулы договора с его процентом; выше по иерархии (контрагент, підрозділ)
   — **суммируется**, а не пересчитывается.
6. **Валидация** ([05](05_validaciya_kontroli.md)): коды `П*` до расчёта (блокируют) и
   `К01–К10` после (в ТЧ `Контроль`). Каждое замечание привязано к строке ТЧ.
   С 14.08 нет П10/П14/П19/П22–П28 и К11 (ушли с ТЧ-параметрами и корректировками);
   добавлены П33–П35 (заполнение из kazna).
7. **Пересчёт безопасен**: очищаются только результатные ТЧ, параметры и ручные правки живут.

**Архитектура:** движок в `Ext/ObjectModule.bsl` (1868 строк, 53 процедуры/функции, 8 экспортных),
форма — тонкий диспетчер через `РеквизитФормыВЗначение("Объект")` / `ЗначениеВРеквизитФормы`.

---

## Порядок чтения

| # | Файл | Что узнаешь |
|---|---|---|
| 1 | **этот файл** | TL;DR + структура реквизитов и ТЧ + экспорт модуля |
| 2 | [01_arhitektura_dokument.md](01_arhitektura_dokument.md) | Непроводимый документ, движок↔форма, регистрация, пайплайн создания |
| 3 | [02_metodika_kolonok.md](02_metodika_kolonok.md) | **★★** Что означают E/F/G/H/J/K и доказательства на цифрах Excel |
| 4 | [03_istochniki_zaprosy.md](03_istochniki_zaprosy.md) | **★** Три запроса, фильтр направления, граница периода |
| 5 | [04_parametry_dogovora.md](04_parametry_dogovora.md) | **★★** Пять реквизитов договора, заливка из Excel, TSV-цикл |
| 6 | [05_validaciya_kontroli.md](05_validaciya_kontroli.md) | **★** Коды П* и К01–К10, привязка к строке ТЧ |
| 7 | [06_korrektirovki_isklyucheniya.md](06_korrektirovki_isklyucheniya.md) | Ручные суммы и исключения: зачем были и почему удаляются |
| 8 | [07_pechatnye_formy.md](07_pechatnye_formy.md) | Звіт, доповідь, выгрузка HTML |
| 9 | [08_forma_dokumenta.md](08_forma_dokumenta.md) | Страницы, master-detail, условное оформление кодом |
| 10 | [09_ukrainskiy_yazyk.md](09_ukrainskiy_yazyk.md) | **★** `DefaultLanguage = Украинский`, словарь + патчер |
| 11 | [10_kak_dorabotat.md](10_kak_dorabotat.md) | **★** Рецепты доработки, чек-лист, пайплайн скриптов |
| 12 | [11_pererabotka_v_rabote.md](11_pererabotka_v_rabote.md) | **★** Что переделывается прямо сейчас и что уже сделано |
| — | [LESSONS.md](LESSONS.md) | **★★** Грабли — каждая стоила отладки |
| — | [FINDINGS.md](FINDINGS.md) | Эталоны для smoke, контрольные числа, аномалии данных |

---

## Реквизиты шапки

| Реквизит | Тип | Смысл |
|---|---|---|
| `ДатаРасчета` | Дата | приводится к `КонецДня()` функцией `МоментРасчета()` |
| `НачалоПериода` | Дата | пусто = с начала учёта |
| `Направление` | `CatalogRef.Направления` | по умолчанию «Производство» |
| `ЗаполнятьТолькоСДвижениями` | Булево | иначе 45 подразделений вместо 17 |
| `ВключатьНепроведенные` | Булево | вторая ветка колонки H |
| `ЕстьОшибки` | Булево | служебный |
| `Ответственный`, `Комментарий` | | |
| `ДокументСравнения` | `DocumentRef.А_ФинансовыйОтчетПроизводства` | база «Динаміки»; пуст — автоподбор при `Рассчитать()` (последний непомеченный того же направления с меньшей ДатаРасчета) |

**Четыре реквизита удалены 16.08.2026** (решение пользователя — минимум полей на форме):
`СтавкаНДС` (ставка константой в коде — функция `СтавкаНДСРасчета()` = 20, проверка П18 снята),
`ЗаголовокОтчета` (заголовок печати всегда из даты расчёта), `Статус` (дубль надписи
«Стан розрахунку» на форме) и **`Организация`** — она НИКОГДА не участвовала в отборе:
ни один из пяти запросов по ней не фильтровал, отчёт считает по ВСЕМ организациям,
разрез задают `Направление` + `Подразделение`; поле лишь дописывало строку в шапку печати.
Проверено: после удаления все цифры приёмки и smoke не изменились.

## Табличные части (8 с 16.08.2026)

**Параметры:** `Подразделения` (2 реквизита: Подразделение, Включать), `ДоговорыДополнительно`
(с 16.08 также носитель БЕЗДВИЖНЫХ договоров: строка с назначением = собственному подразделению
даёт в отчёте пустую строку с C/D из регистра), `СчетаРасчетов`,
`СтатьиИсключенияКазны` (Статья — ССЫЛКА `СправочникСсылка.СтатьиДвиженияДенежныхСредств`:
справочник синхронизирован обменом, UUID шести статей в BuhBud и kazna совпадают 1:1;
6 статей по умолчанию в ОбработкеЗаполнения / `ЗаполнитьСтатьиИсключенияПоУмолчанию()`;
в запрос казны идут НАИМЕНОВАНИЯ ссылок).
**Результат:** `Расчет`, `РасшифровкаДокументов`, `Контроль`,
`Казна` (очищается при расчёте: Подразделение, ИмяВКазне, Поступления/Затраты/Маржа План+Факт;
план-факт производства из kazna механикой Отчеты.ПланФакт, свёртка иерархии до корня,
сопоставление по имени; затраты хранятся ОТРИЦАТЕЛЬНЫМИ, Маржа = Поступления + Затраты).

«Коригування» и «Виключені документи» удалены 14.08.2026 — расхождения чинятся в первичке
([06](06_korrektirovki_isklyucheniya.md) — историческое).

## Экспортные методы модуля объекта

| Метод | Назначение |
|---|---|
| `ПроверитьПараметры()` | Булево; заполняет `Контроль`, выводит сообщения с привязкой к строке |
| `Рассчитать()` | Структура: Выполнено, Ошибок, Предупреждений, СтрокРасчета |
| `ЗаполнитьПодразделенияПоУмолчанию(Перезаполнять)` | по направлению либо по фиксированному списку 14 |
| `ЗаполнитьПодразделенияИзКазны(Перезаполнять)` | COM к kazna, `РН.БюджетыНаМесяц`, константа `А_СтрокаПодключенияКазна` |
| `ЗаголовокПечати()` | |
| `СформироватьПечатьОтчет()` | ТабличныйДокумент: Підрозділ → Контрагент → Договор (директорский) |
| `СформироватьПечатьОтчетРасшифровка()` | то же + строки документов под договорами |
| `СформироватьПечатьДоклад()` | ТабличныйДокумент |
| `ТекстДокладаHTML()` | Строка для сохранения в файл |
| `СформироватьПечатьДинамика()` | «Динаміка» против ДокументСравнения: потоковая, уровни 1–3, клик открывает документ |
| `ТекстДинамикиHTML()` | HTML-письмо директору по регламенту knowledge_Письмо Директору; "" если нет документа сравнения |

## Перечислений НЕТ (с 16.08.2026)

Все 4 перечисления `А_*` удалены (решение пользователя: минимум объектов в типовой
конфигурации, «всё в коде документа»). Значения — СТРОКИ:
- `Статус`: «Чернетка» / «Розрахунок виконано» / «Є зауваження»;
- `Контроль.Уровень`: «Помилка» / «Попередження» / «Інформація»;
- `РасшифровкаДокументов.Вид`: «Виконані роботи» / «Оплата» / «Акт поданий» /
  «Акт поданий (непроведений)»;
- `Расчет.ВариантФормулы` и `РС.ВариантФормулыИтога` — ТЕХНИЧЕСКИЕ имена
  `ЗадолженностьПлюсАкты` / `ВыполнениеМинусОплата` / `ТолькоРучной` (ключи TSV-цикла).

Константа `А_СтрокаПодключенияКазна` тоже удалена — строка подключения kazna живёт
литералом в функции `СтрокаПодключенияКазны()` модуля объекта.

## Связанные ресурсы

- **Живой код:** `_Rarzrabotki/BASEBuh/Documents/А_ФинансовыйОтчетПроизводства/` — источник истины
- **Спецификация переработки:** `_Rarzrabotki/notebook/specs/2026-08-13-finzvit-vyrobnytstva-design.md`
- **Промт доработки:** [ПРОМТ_Доработка_печатной_формы_и_очистка_ТЧ.md](ПРОМТ_Доработка_печатной_формы_и_очистка_ТЧ.md)
- **Параметры договоров:** [Данные по Производсту для заливки в базу Бухгалтерия.txt](Данные%20по%20Производсту%20для%20заливки%20в%20базу%20Бухгалтерия.txt)
- **Эталон-образец документа:** [`../knowledge_Бухгалтерия_РасчетКомплектаций/`](../knowledge_Бухгалтерия_РасчетКомплектаций/)
- **Регламент писем и докладов:** [`../knowledge_Письмо Директору/`](../knowledge_Письмо%20Директору/)
- **Скрипты:** `_Rarzrabotki/Python/test/doc_finrep_*.py`, `smoke_doc_finrep.py`
- **Скилы:** `meta-compile`, `meta-edit`, `form-compile`, `db-load-xml`, `db-update`
