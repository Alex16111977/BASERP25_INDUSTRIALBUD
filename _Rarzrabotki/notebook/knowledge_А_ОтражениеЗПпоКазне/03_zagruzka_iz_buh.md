# 03. Блок 2 — Загрузка налогов и удержаний из zup_1

> Главная процедура: `ЗагрузитьНалогиИУдержанияИзЗарплатыБух()` (BSL 392-572)
> Запись в ТЧ: `ЗаписатьНалогиВТЧ_Бух` (BSL 576-651) + `ЗаписатьУдержанияВТЧ_Бух` (BSL 654-717)
> Поиск контрагента-получателя: `НайтиКонтрагентаПоУд_Бух` (BSL 725-753)

⚠️ **Важная поправка к PROMPT.md:** реальный источник — **`zup_1`** (BAS ЗУП регламент), а НЕ BuhBud (BAS Бухгалтерія). Connection string: `Srvr="SQLSERVER";Ref="zup_1";Usr="cfo";Pwd="2442"`.

---

## 1. Назначение

Заполнить 2 ТЧ из базы регламентированного учёта ЗУП:
- **`НалогиБухгалтерия`** ← НДФЛ + ВС (`РегНакопл.ВзаиморасчетыПоНДФЛ`) + ЕСВ (`РегРасчет.ВзносыВФонды`)
- **`УдержанияБухгалтерия`** ← алименты / исп.листы (`РегРасчет.УдержанияРаботниковОрганизаций` + `Документ.ИсполнительныйЛист`)

Период — месяц текущей Даты документа.

---

## 2. Структура zup_1 (discovery 2026-05-26)

### РегистрНакопления.ВзаиморасчетыПоНДФЛ
- **Измерения:** `Организация`, `Сотрудник`, `ПериодВзаиморасчетов`, `ДоходНДФЛ` (+ устаревшие `УдалитьВидСтавки`, `УдалитьЛьгота`, `УдалитьФизЛицо`)
- **Ресурсы:** `Налог`, `Доход`, `НеподтвержденнаяСумма`
- **Реквизиты:** `НалоговыйПериод`, `КоличествоЛьгот`, `ОбособленноеПодразделение`, `Авторасчет`, `Сторно`, `Льгота`, `ВидСтавки`, `УвеличеннаяСтавка`, `ДоходПолный`

⚠️ **Ловушка:** регистр сальдовый — `.Обороты(...)` возвращает поля с префиксами `НалогПриход`, `НалогРасход`, `ДоходПриход`, `ДоходРасход` (НЕ просто `Налог`/`Доход`). Без префикса — поле не найдено. См. CLAUDE.md «Rule #-1 — Pattern, який ВЖЕ знайшов помилки».

### РегистрРасчета.ВзносыВФонды
- **Измерения:** `Организация`, `Сотрудник` (+ устаревший `УдалитьФизЛицо`)
- **Ресурсы:** `База`, `БазаВзноса`, `Результат`
- **Реквизиты:** `Налог`, `НомерПорога`, `ДатаНачала`, `ДатаОкончания`, `Предел`, `СтатьяНалоговойДекларации`, `Ставка`, `Авторасчет`, `НомерСтрокиТЧ`, `ОтработаноДней`, `ОтработаноДнейВсего`, `ПодразделениеОрганизации`, `КодКатегорииЗЛ`, `ТипНачисления`, `БазаВычет`, `БазаОтпускные`, `КоэффициентСтавки`, `МинимальнаяБаза`, `БазаДоначисление`, `РезультатДоначисление`, `СтавкаПолная`, `РезультатПолная`

### РегистрРасчета.УдержанияРаботниковОрганизаций
- **Измерения:** `Организация`, `Сотрудник`
- **Ресурсы:** `Результат`, `ПервичныйРезультат`
- **Реквизиты:** `ДокументОснование`, `Авторасчет`, `Показатель1`..`Показатель6`

---

## 3. COM-подключение к zup_1

```bsl
СтрокаПодключенияЗУП = "Srvr=""SQLSERVER"";Ref=""zup_1"";Usr=""cfo"";Pwd=""2442""";
Попытка
    V8 = Новый COMОбъект("V83.COMConnector");
    V83 = V8.Connect(СокрЛП(СтрокаПодключенияЗУП));
Исключение
    ВызватьИсключение "Помилка підключення до ZUP_1: " + ОписаниеОшибки();
КонецПопытки;
```

---

## 4. Передача массива ЕДРПОУ как параметр в COM-запрос

В отличие от Блока 1 (где Кстамена.БДДС фильтровался по флагу организации), в zup_1 нет аналогичного флага — приходится передавать список ЕДРПОУ организаций ERP, у которых стоит `А_ВБалансе = Истина`.

⚠️ **Особенность COM-параметра типа «Массив»:** массив создаётся **в контексте V83**, а не ERP:

```bsl
// Получили массив в ERP
ЗапросОрганизаций = Новый Запрос;
ЗапросОрганизаций.Текст =
"ВЫБРАТЬ Организации.КодПоЕДРПОУ КАК КодПоЕДРПОУ
|ИЗ Справочник.Организации КАК Организации
|ГДЕ Организации.А_ВБалансе = ИСТИНА И Организации.КодПоЕДРПОУ <> """"";
масКодПоЕДРПОУ = Новый Массив;
ВыборкаОрг = ЗапросОрганизаций.Выполнить().Выбрать();
Пока ВыборкаОрг.Следующий() Цикл
    масКодПоЕДРПОУ.Добавить(ВыборкаОрг.КодПоЕДРПОУ);
КонецЦикла;

// Переносим в контекст V83
масКодПоЕДРПОУ_V83 = V83.NewObject("Массив");
Для Каждого КодЕДРПОУ Из масКодПоЕДРПОУ Цикл
    масКодПоЕДРПОУ_V83.Добавить(КодЕДРПОУ);
КонецЦикла;

// Передаём v83-массив в zup_1-запрос
ЗапросНал.УстановитьПараметр("масКодПоЕДРПОУ", масКодПоЕДРПОУ_V83);
```

Без этой конвертации параметр «массив» через COM не работает.

---

## 5. Запрос 1 — Налоги (НДФЛ + ВС + ЕСВ)

```sql
ВЫБРАТЬ
    ВзаиморасчетыПоНДФЛ.Сотрудник.Физлицо.КодПоДРФО КАК СотрудникИНН,
    ВзаиморасчетыПоНДФЛ.Сотрудник.Физлицо.Наименование КАК СотрудникНаименование,
    СУММА(ВзаиморасчетыПоНДФЛ.НалогПриход) КАК СуммаНалога,
    СУММА(ВзаиморасчетыПоНДФЛ.ДоходПриход) КАК СуммаДохода,
    ВложенныйЗапрос.КодПоЕДРПОУ КАК КодПоЕДРПОУ,
    ВзаиморасчетыПоНДФЛ.ДоходНДФЛ.Код КАК КодДохода,
    0 КАК СуммаФОТ,
    0 КАК СуммаБазы,
    "ВоенныйСбор" КАК КодНалогаСтатьяДенег,
    "ВоенныйСбор" КАК КодНалога
ИЗ
    РегистрНакопления.ВзаиморасчетыПоНДФЛ.Обороты(&НачалоПериода, &КонецПериода, , ) КАК ВзаиморасчетыПоНДФЛ
        ВНУТРЕННЕЕ СОЕДИНЕНИЕ (
            ВЫБРАТЬ
                КодыОрганизацииСрезПоследних.Организация КАК Организация,
                КодыОрганизацииСрезПоследних.КодПоЕДРПОУ КАК КодПоЕДРПОУ
            ИЗ
                РегистрСведений.КодыОрганизации.СрезПоследних КАК КодыОрганизацииСрезПоследних
        ) КАК ВложенныйЗапрос
        ПО ВзаиморасчетыПоНДФЛ.Организация = ВложенныйЗапрос.Организация
ГДЕ
    ВзаиморасчетыПоНДФЛ.НалогПриход <> 0
    И ВложенныйЗапрос.КодПоЕДРПОУ В(&масКодПоЕДРПОУ)
СГРУППИРОВАТЬ ПО
    ВзаиморасчетыПоНДФЛ.Сотрудник.Физлицо.КодПоДРФО,
    ВзаиморасчетыПоНДФЛ.Сотрудник.Физлицо.Наименование,
    ВложенныйЗапрос.КодПоЕДРПОУ,
    ВзаиморасчетыПоНДФЛ.ДоходНДФЛ.Код

ОБЪЕДИНИТЬ ВСЕ

ВЫБРАТЬ
    ВзносыВФонды.Сотрудник.Физлицо.КодПоДРФО,
    ВзносыВФонды.Сотрудник.Физлицо.Наименование,
    0, 0,
    ВложенныйЗапрос.КодПоЕДРПОУ,
    NULL,
    СУММА(ВзносыВФонды.Результат),
    СУММА(ВзносыВФонды.База),
    "ЕСВФОТРаботники",
    ВзносыВФонды.Налог.Код
ИЗ
    РегистрРасчета.ВзносыВФонды КАК ВзносыВФонды
        ВНУТРЕННЕЕ СОЕДИНЕНИЕ (
            ВЫБРАТЬ
                КодыОрганизацииСрезПоследних.Организация КАК Организация,
                КодыОрганизацииСрезПоследних.КодПоЕДРПОУ КАК КодПоЕДРПОУ
            ИЗ
                РегистрСведений.КодыОрганизации.СрезПоследних КАК КодыОрганизацииСрезПоследних
        ) КАК ВложенныйЗапрос
        ПО ВзносыВФонды.Организация = ВложенныйЗапрос.Организация
ГДЕ
    ВложенныйЗапрос.КодПоЕДРПОУ В(&масКодПоЕДРПОУ)
    И ВзносыВФонды.Результат <> 0
    И ВзносыВФонды.ПериодРегистрации МЕЖДУ &НачалоПериода И &КонецПериода
СГРУППИРОВАТЬ ПО
    ВложенныйЗапрос.КодПоЕДРПОУ,
    ВзносыВФонды.Сотрудник.Физлицо.КодПоДРФО,
    ВзносыВФонды.Сотрудник.Физлицо.Наименование,
    ВзносыВФонды.Налог.Код
```

**Особенности:**
- ЕДРПОУ организации получается через `РегистрСведений.КодыОрганизации.СрезПоследних` (типовой механизм ЗУП — ЕДРПОУ хранится в РС).
- `НалогПриход` / `ДоходПриход` — оборотные поля сальдового регистра (см. §2 — Rule #-1).
- ОБЪЕДИНИТЬ ВСЕ объединяет НДФЛ+ВС и ЕСВ в **одну** ТЗ — отличие по `СуммаНалога` (> 0 для НДФЛ/ВС) vs `СуммаФОТ` (> 0 для ЕСВ).

---

## 6. Запрос 2 — Удержания (исп.листы)

```sql
ВЫБРАТЬ
    У.Сотрудник.Физлицо.КодПоДРФО КАК СотрудникИНН,
    У.Сотрудник.Наименование КАК СотрудникНаименование,
    У.ВидРасчета.Наименование КАК ВидУдержания,
    СУММА(У.Результат) КАК Сумма,
    У.ДокументОснование.Номер КАК НомерИЛ,
    У.ДокументОснование.Получатель.Наименование КАК ИмяПолучателя,
    У.ДокументОснование.Получатель.КодПоЕДРПОУ КАК ЕДРПОУПолучателя,
    У.ДокументОснование.Получатель.ИНН КАК ИННПолучателя,
    КодыОрг.КодПоЕДРПОУ КАК КодПоЕДРПОУ
ИЗ
    РегистрРасчета.УдержанияРаботниковОрганизаций КАК У
        ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.КодыОрганизации.СрезПоследних КАК КодыОрг
        ПО У.Организация = КодыОрг.Организация
ГДЕ
    У.ПериодРегистрации МЕЖДУ &НачалоПериода И &КонецПериода
    И У.Результат <> 0
    И У.ДокументОснование <> ЗНАЧЕНИЕ(Документ.ИсполнительныйЛист.ПустаяСсылка)
    И КодыОрг.КодПоЕДРПОУ В (&масКодПоЕДРПОУ)
СГРУППИРОВАТЬ ПО ...
```

Берутся только удержания с заполненным `ДокументОснование = ИсполнительныйЛист`. Получатель ИЛ (`У.ДокументОснование.Получатель`) — это `Контрагент` (взыскатель: ВДВС, банк, частное лицо ФОП и т.д.).

---

## 7. Сериализация результатов через `ЗначениеВСтрокуВнутр`

Когда оба COM-запроса выполнены и **в ТЗ только примитивные поля** (строки, числа, без COM-references на справочники Казны/Буха), можно безопасно использовать `ЗначениеВСтрокуВнутр` в Казне → `ЗначениеИзСтрокиВнутр` в ERP:

```bsl
ТЗНалогов = ЗапросНал.Выполнить().Выгрузить();
стр_ТЗНал = V83.ЗначениеВСтрокуВнутр(ТЗНалогов);

ТЗУдержаний = ЗапросУд.Выполнить().Выгрузить();
стр_ТЗУд = V83.ЗначениеВСтрокуВнутр(ТЗУдержаний);

V83 = Неопределено;
V8 = Неопределено;

ТаблицаНалогов   = ЗначениеИзСтрокиВнутр(стр_ТЗНал);
ТаблицаУдержаний = ЗначениеИзСтрокиВнутр(стр_ТЗУд);
```

⚠️ **Этот паттерн работает ТОЛЬКО потому, что:**
- В ТЗ нет ссылок на справочники Казны/Буха (типа `Сотрудник` или `Организация` как Reference)
- Все нужные поля — `СотрудникИНН`, `СотрудникНаименование`, `КодДохода` — **строки** (примитивы)
- Результат — `ВзаиморасчетыПоНДФЛ.Сотрудник.Физлицо.КодПоДРФО`, а не сам `.Сотрудник`

**Если в выборку добавить, например, `У.Сотрудник КАК Сотрудник`** — после десериализации в ERP это будет невалидный reference типа `СправочникСсылка.Сотрудники` (из контекста ЗУП, не ERP). См. CLAUDE.md «BuhBud — ЗначениеВСтрокуВнутр across different databases».

---

## 8. Заполнение ТЧ `НалогиБухгалтерия` (BSL 576-651)

```bsl
Для Каждого СтрокаТЗ Из ТаблицаНалогов Цикл
    ИННСотрудника = СокрЛП("" + СтрокаТЗ.СотрудникИНН);

    // Матчинг сотрудника по ИНН через Справочник.Сотрудники.А_КодПоДРФО
    СотрудникERP = Справочники.Сотрудники.НайтиПоРеквизиту("А_КодПоДРФО", ИННСотрудника);

    // Матчинг организации по ЕДРПОУ
    ОрганизацияERP = Справочники.Организации.НайтиПоРеквизиту("КодПоЕДРПОУ", СокрЛП("" + СтрокаТЗ.КодПоЕДРПОУ));

    // КодДохода → Справочник.ВидыДоходовНДФЛ
    КодДоходаЗУП = СокрЛП("" + СтрокаТЗ.КодДохода);
    ВидДоходаНДФЛERP = Справочники.ВидыДоходовНДФЛ.НайтиПоКоду(КодДоходаЗУП);

    НоваяСтрока = НалогиБухгалтерия.Добавить();
    // ... заполнение полей

    // ТипНалога — парсинг кода дохода:
    //   "101", "ВЗ" — нет, но "101" — все цифры → НДФЛ
    //   "ВЗ" — есть буквы → ВоенныйСбор
    Если СтрДлина(КодДоходаЗУП) > 0 Тогда
        КодЦифровий = Истина;
        Для Счет = 1 По СтрДлина(КодДоходаЗУП) Цикл
            Симв = Сред(КодДоходаЗУП, Счет, 1);
            Если НЕ (Симв >= "0" И Симв <= "9") Тогда
                КодЦифровий = Ложь;
                Прервать;
            КонецЕсли;
        КонецЦикла;
        Если КодЦифровий Тогда
            НоваяСтрока.ТипНалога = Перечисления.ТипыНалогов.НДФЛ;
        Иначе
            НоваяСтрока.ТипНалога = Перечисления.ТипыНалогов.ВоенныйСбор;
        КонецЕсли;
    КонецЕсли;
КонецЦикла;
```

**Правило `Сумма`:**
- `Сумма = СуммаНалога` (когда НалогПриход ≠ 0, т.е. НДФЛ/ВС)
- `Сумма = СуммаФОТ` (когда ВзносыВФонды.Результат ≠ 0, т.е. ЕСВ)

---

## 9. Заполнение ТЧ `УдержанияБухгалтерия` (BSL 654-717)

Кешируются константные ссылки **вне цикла**:
```bsl
ПрочиеПассивы = ПланыВидовХарактеристик.СтатьиАктивовПассивов.НайтиПоНаименованию("Прочие пассивы");
СчетУчета6853 = ПланыСчетов.Хозрасчетный.НайтиПоКоду("6853");
ГруппаУчетаЗарплата = Справочники.ГруппыУчетаНачисленийИУдержаний.Зарплата;
ВидОперацииАлименты = Перечисления.ВидыОперацийПоЗарплате.АлиментыПрочиеИсполнительныеЛисты;
```

Для каждой строки:
1. Матчинг сотрудника по ИНН (`А_КодПоДРФО`)
2. Поиск контрагента-получателя через `НайтиКонтрагентаПоУд_Бух` (см. §10)
3. Заполнение полей: ВидОперации, СтатьяАктивовПассивов (Прочие пассивы), СчетУчета (6853), Субконто1 (Контрагент-Получатель), ГруппаУчетаНачислений (Зарплата)

⚠️ Поле `Контрагент` в ТЧ НЕ заполняется — Получатель идёт в `Субконто1`. Это соответствует стандарту `000Ц-000002`.

---

## 10. Функция `НайтиКонтрагентаПоУд_Бух` (BSL 725-753) — 3-уровневый fallback

```bsl
Функция НайтиКонтрагентаПоУд_Бух(ЄДРПОУ, ІНН, Ім_я)
    // 1. По КодПоЕДРПОУ (основной для юридических лиц: ВДВС и т.д.)
    Если НЕ ПустаяСтрока(ЄДРПОУ) Тогда
        К = Справочники.Контрагенты.НайтиПоРеквизиту("КодПоЕДРПОУ", ЄДРПОУ);
        Если ЗначениеЗаполнено(К) Тогда Возврат К; КонецЕсли;
    КонецЕсли;
    // 2. По КодПоЕДРПОУ с ИНН (для ФОП — ИНН часто записывают в КодПоЕДРПОУ)
    Если НЕ ПустаяСтрока(ІНН) Тогда
        К = Справочники.Контрагенты.НайтиПоРеквизиту("КодПоЕДРПОУ", ІНН);
        Если ЗначениеЗаполнено(К) Тогда Возврат К; КонецЕсли;
    КонецЕсли;
    // 3. Fallback — точное совпадение по Наименованию
    Если НЕ ПустаяСтрока(Ім_я) Тогда
        К = Справочники.Контрагенты.НайтиПоНаименованию(Ім_я, Истина);  // ТочноеСоответствие=Истина
        Если ЗначениеЗаполнено(К) Тогда Возврат К; КонецЕсли;
    КонецЕсли;
    Возврат Справочники.Контрагенты.ПустаяСсылка();
КонецФункции
```

---

## 11. Результаты выполнения

Сообщения пользователю:
- `"Налоги: помилок матчингу сотрудника: N"` (если есть сотрудники в ЗУП, которых нет в ERP)
- `"Удержания: не знайдено контрагента 'ИмяПолучателя' (ЄДРПОУ=..., ІНН=...)"` (если получатель не найден ни одним из 3-х способов)

Без ошибок — никаких сообщений (тихий happy path).

---

## 12. Cross-references

- [KNOWLEDGE_MAP.md](KNOWLEDGE_MAP.md)
- [01_dokument_struktura.md](01_dokument_struktura.md) §2.2 (структура ТЧ НалогиБухгалтерия), §2.3 (УдержанияБухгалтерия)
- [04_zapolnenie_nachisleniy.md](04_zapolnenie_nachisleniy.md) — как НалогиБухгалтерия используется в Op 1, Op 3
- [LESSONS.md](LESSONS.md) §3 (ЗначениеВСтрокуВнутр между разными типами), §4 (Rule #-1: Налог→НалогПриход)
- [`Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl`](../../../Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl) (строки 392-753)
- [`Python/test/discovery_zup_struktura.py`](Python/test/discovery_zup_struktura.py)
- [`Python/_artifacts/zup_schema.md`](Python/_artifacts/zup_schema.md)
- CLAUDE.md §«ЗначениеВСтрокуВнутр across different databases»
