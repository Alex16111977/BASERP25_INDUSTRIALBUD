# -*- coding: utf-8 -*-
import win32com.client, sys
sys.stdout.reconfigure(encoding='utf-8')
v8 = win32com.client.Dispatch('V83.COMConnector')
erp = v8.Connect('Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
S = erp.String

q = erp.NewObject('Запрос')
q.Text = 'ВЫБРАТЬ ПЕРВЫЕ 1 Д.Ссылка ИЗ Документ.А_ОтражениеЗПпоКазне КАК Д ГДЕ Д.Номер = "000000001"'
sel = q.Execute().Выбрать()
sel.Следующий()
obj = sel.Ссылка.ПолучитьОбъект()

print('=== РОДИТЕЛЬ УдержанияБухгалтерия.СтатьяАктивовПассивов ===')
for i in range(obj.УдержанияБухгалтерия.Количество()):
    r = obj.УдержанияБухгалтерия.Получить(i)
    стат = r.СтатьяАктивовПассивов
    if erp.ЗначениеЗаполнено(стат):
        is_group = S(стат.ЭтоГруппа)
        name = S(стат.Наименование)
        uuid = S(стат.УникальныйИдентификатор())
        print(f'  [{i+1}] Наим="{name}" ЭтоГруппа={is_group} UUID={uuid}')
    else:
        print(f'  [{i+1}] <ПУСТО>')

# Проверить предопределенный элемент ПрочиеПассивы
print()
print('=== Предопределённый ПрочиеПассивы ===')
pp = erp.ПланыВидовХарактеристик.СтатьиАктивовПассивов.ПрочиеПассивы
print(f'  Наим={S(pp.Наименование)} ЭтоГруппа={S(pp.ЭтоГруппа)} UUID={S(pp.УникальныйИдентификатор())}')
