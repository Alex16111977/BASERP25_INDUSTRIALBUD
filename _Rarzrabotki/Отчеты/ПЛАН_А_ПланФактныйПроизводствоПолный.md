# А_ПланФактныйПроизводствоПолный — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Внешний отчёт 1С, воспроизводящий «Звіт виробництва по табелях» (лист «Оріль») как программный `ТабличныйДокумент`: одна СС за прогон, 5 блоков План/Факт/%% + 2 колонки реальной ЗП.

**Architecture:** Внешний `.erf` (скаффолд — клон `А_ПланФактныйАнализРабот`). Логика-«движок» в `ObjectModule` (запросы → структура данных → построение `ТабличныйДокумент` кодом, без MXL). Тонкая кастомная форма (`Період`, `СтруктураСебестоимости`, «Сформувати», `ПолеТабличногоДокумента`) делегирует в движок через `РеквизитФормыВЗначение`.

**Tech Stack:** 1С:Предприятие 8.3 (BSL), внешний отчёт; Python `win32com` (`V83.COMConnector`, BaseERP) для проверки запросов/смоук; навыки `epf-build`, `form-compile`, `meta-edit`, `1c-bsl-review`.

**Спецификация:** `_Rarzrabotki/Отчеты/ПРОМТ_А_ПланФактныйПроизводствоПолный.md`

**Базовые правила проекта:**
- **Rule #-1:** каждый запрос — сперва Python-COM на BaseERP, потом в BSL. Соединение: `Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"`.
- **Rule #4:** после каждой правки копировать файл из worktree в `C:\Configuration_downloads\BASERP25\…` (оттуда грузит Designer).
- Контрольные числа Оріль (план): D=753000; F=1711992; G=206605; I=1927; K=868; ставки 200/350; ФОТ-план грн 385400/302400. Факт: часы 2229; реальная ЗП ≈257156,94; материалы — на ПРООН.

---

## Файловая структура

```
_Rarzrabotki/Отчеты/
  А_ПланФактныйПроизводствоПолный.xml          ← MODIFY: Name/Synonym/GeneratedType/DefaultForm/ChildObjects
  А_ПланФактныйПроизводствоПолный/
    Ext/ObjectModule.bsl                         ← REWRITE: движок (регистрация + ПолучитьДанныеАнализа + СформироватьТабличныйДокумент)
    Forms/Форма/Ext/Form.xml                     ← CREATE: кастомная форма
    Forms/Форма/Ext/Form/Module.bsl              ← CREATE: тонкий модуль формы
    Forms/Форма.xml                              ← CREATE: метаданные формы
  А_ПланФактныйПроизводствоПолный.erf            ← BUILD output
  ПЛАН_А_ПланФактныйПроизводствоПолный.md         ← этот план

_Rarzrabotki/Python/test/
  test_pfpp_plan.py        ← Task 1.x: проверка плановых запросов
  test_pfpp_fakt.py        ← Task 1.x: проверка фактических запросов
  test_pfpp_smoke.py       ← Task 4.x: смоук готового .erf (рендер ТабличныйДокумент)
```

**Ответственность модулей:**
- `ObjectModule.bsl` — весь расчёт и построение документа (экспортные `ПолучитьДанныеАнализа`, `СформироватьТабличныйДокумент`; служебные функции блоков).
- `Form/Module.bsl` — только: собрать параметры → `РеквизитФормыВЗначение(ЭтотОбъект)` → вызвать движок → вывести в `ПолеТабличногоДокумента`.

---

## Phase 0 — Скаффолд и переименование

### Task 0: Переименовать клон-скаффолд

**Files:**
- Modify: `_Rarzrabotki/Отчеты/А_ПланФактныйПроизводствоПолный.xml`

- [ ] **Step 1: Заменить идентификаторы отчёта**

В `.xml` (сейчас всё ещё `…АнализРабот`) заменить:
- `<xr:GeneratedType name="ExternalReportObject.А_ПланФактныйАнализРабот"` → `…ПроизводствоПолный`
- `<Name>А_ПланФактныйАнализРабот</Name>` → `<Name>А_ПланФактныйПроизводствоПолный</Name>`
- `<Synonym>` ru → «План-факт виробництва (повний)», uk → «План-факт виробництва (повний)»
- `<MainDataCompositionSchema>…</MainDataCompositionSchema>` — удалить строку (СКД не используется)
- `<DefaultForm>CommonForm.ФормаОтчета</DefaultForm>` → `<DefaultForm>ExternalReport.А_ПланФактныйПроизводствоПолный.Form.Форма</DefaultForm>`
- `<DefaultSettingsForm>`/`<DefaultVariantForm>` — очистить (`<…/>`)
- В `<ChildObjects>` убрать `<Template>ОсновнаяСхемаКомпоновкиДанных</Template>`, добавить `<Form>Форма</Form>`
- GUID `uuid`/`ContainedObject`/`TypeId`/`ValueId` оставить как есть (у клона уникальны).

- [ ] **Step 2: Удалить SKD-шаблон скаффолда**

Run:
```bash
rm -rf "_Rarzrabotki/Отчеты/А_ПланФактныйПроизводствоПолный/Templates"
```

- [ ] **Step 3: Копия в основную конфигурацию (Rule #4) и коммит**

```bash
cp "_Rarzrabotki/Отчеты/А_ПланФактныйПроизводствоПолный.xml" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Отчеты/"
rm -rf "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Отчеты/А_ПланФактныйПроизводствоПолный/Templates"
git add -A && git commit -m "feat(pfpp): переименован скаффолд отчёта, форма вместо СКД"
```

---

## Phase 1 — Движок данных (запросы; Python ПЕРЕД BSL — Rule #-1)

### Task 1: Плановый запрос (работы + материалы + ставки)

**Files:**
- Create: `_Rarzrabotki/Python/test/test_pfpp_plan.py`
- Modify: `_Rarzrabotki/Отчеты/А_ПланФактныйПроизводствоПолный/Ext/ObjectModule.bsl`

- [ ] **Step 1: Python-проверка планового запроса (RED→GREEN на данных)**

`test_pfpp_plan.py` — выполнить запрос и проверить контрольные суммы Оріль:
```python
import win32com.client, sys
if sys.stdout.encoding != 'utf-8': sys.stdout.reconfigure(encoding='utf-8')
erp = win32com.client.Dispatch("V83.COMConnector").Connect('Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
q = erp.NewObject("Запрос")
q.Text = """
ВЫБРАТЬ
    Р.НаВыезде КАК НаВыезде,
    СУММА(Р.Количество) КАК Часы,
    СУММА(Р.Сумма) КАК Сумма
ИЗ Справочник.А_СтруктураСебестоимости.Работы КАК Р
ГДЕ Р.Ссылка.Наименование = &Имя
СГРУППИРОВАТЬ ПО Р.НаВыезде
"""
q.SetParameter("Имя", "МД МХП ОРІЛЬ / СТІЛ МД МХП ОРІЛЬ")
t = q.Execute().Выгрузить()
res = {row.НаВыезде: (row.Часы, row.Сумма) for row in [t.Получить(i) for i in range(t.Количество())]}
assert res[False][0] == 1927 and res[True][0] == 868, res        # часы Вир/Монт
assert res[False][1] + res[True][1] == 753000, res                # Σ робіт
print("OK works", res)

# материалы: монтаж = этап содержит «Монтаж», остальное — производство
q.Text = """
ВЫБРАТЬ
    ВЫБОР КОГДА К.Этап.Наименование ПОДОБНО ""%Монтаж%"" ТОГДА ИСТИНА ИНАЧЕ ЛОЖЬ КОНЕЦ КАК Монтаж,
    СУММА(К.Сумма) КАК Сумма
ИЗ Справочник.А_СтруктураСебестоимости.Комплектующие КАК К
ГДЕ К.Ссылка.Наименование = &Имя
СГРУППИРОВАТЬ ПО ВЫБОР КОГДА К.Этап.Наименование ПОДОБНО ""%Монтаж%"" ТОГДА ИСТИНА ИНАЧЕ ЛОЖЬ КОНЕЦ
"""
q.SetParameter("Имя", "МД МХП ОРІЛЬ / СТІЛ МД МХП ОРІЛЬ")
t = q.Execute().Выгрузить()
mat = {row.Монтаж: row.Сумма for row in [t.Получить(i) for i in range(t.Количество())]}
assert round(mat[True]) == 206605, mat
assert round(mat[False]) == 1711992 or round(mat[False]) == 1711993, mat
print("OK materials", mat)

# ставка = модальная цена по часам
q.Text = """
ВЫБРАТЬ Р.НаВыезде КАК НаВыезде, Р.Цена КАК Цена, СУММА(Р.Количество) КАК Часы
ИЗ Справочник.А_СтруктураСебестоимости.Работы КАК Р
ГДЕ Р.Ссылка.Наименование = &Имя СГРУППИРОВАТЬ ПО Р.НаВыезде, Р.Цена
УПОРЯДОЧИТЬ ПО Часы УБЫВ
"""
q.SetParameter("Имя", "МД МХП ОРІЛЬ / СТІЛ МД МХП ОРІЛЬ")
t = q.Execute().Выгрузить()
print("rates rows:", [(t.Получить(i).НаВыезде, t.Получить(i).Цена, t.Получить(i).Часы) for i in range(t.Количество())])
```
Run: `python _Rarzrabotki/Python/test/test_pfpp_plan.py`
Expected: `OK works {False:(1927,429800),True:(868,323200)}`, `OK materials {True:206605, False:1711992}`, ставки 200/350 (первая строка по часам в каждой категории).

- [ ] **Step 2: Перенести в ObjectModule функцию `ЗаполнитьПлан(Данные, СС)`**

В `ObjectModule.bsl` добавить (план статичен, без периода). Соберает в структуру `Данные`: `РаботыГрн`, `МатВир`, `МатМонт`, `ЧасВир`, `ЧасМонт`, `СтавкаВир`, `СтавкаМонт`. Ставка = цена с макс. суммой часов в категории (`ПЕРВЫЕ 1 … УПОРЯДОЧИТЬ ПО Часы УБЫВ`). Список этапов-монтажа — параметр `ЭтапыМонтажа` (по умолч. условие `ПОДОБНО "%Монтаж%"`).

- [ ] **Step 3: Commit** — `git add -A && git commit -m "feat(pfpp): движок — плановый блок (работы/материалы/ставки)"` + cp в main.

---

### Task 2: Факт часов (табель, 2 среза)

**Files:**
- Modify: `_Rarzrabotki/Python/test/test_pfpp_fakt.py` (создать), `…/ObjectModule.bsl`

- [ ] **Step 1: Python-проверка факта часов**

Запрос к `РегистрНакопления.А_ДанныеТабелирования` по `Подразделение` (= `СС.Подразделение`), ресурс `ЧасыФакт`, два среза: `Период <= КонецПериода` (проект) и `Период МЕЖДУ НачалоМесяца И КонецМесяца`. Контроль Оріль (проект, без верхней границы) — Факт=2229.
```python
q.Text = """
ВЫБРАТЬ СУММА(Т.ЧасыФакт) КАК Часы
ИЗ РегистрНакопления.А_ДанныеТабелирования КАК Т
ГДЕ Т.Подразделение = &Подр И Т.Период <= &КонецПериода
"""
```
`&Подр` = ссылка `СС.Подразделение` (получить из карты: `СтруктураСебестоимости.Подразделение`). Expected Оріль: 2229 (на конец периода, покрывающего все табели).

- [ ] **Step 2: ObjectModule `ЗаполнитьФактЧасов(Данные, Подразделение, НачМес, КонМес, КонПроекта)`** — пишет `ФактЧасПроект`, `ФактЧасМесяц`. Монтаж-факт = 0 (в табеле нет признака выезда).

- [ ] **Step 3: Commit** + cp в main.

---

### Task 3: Факт ЗП (взаиморасчёты ОЗФУ, 2 среза)

**Files:** `test_pfpp_fakt.py`, `…/ObjectModule.bsl`

- [ ] **Step 1: Python-проверка реальной ЗП**

Клонировать логику `А_ПланФактныйАнализРабот` (Фаза 2): `РегистрНакопления.А_ВзаиморасчетыССотрудниками`, **только** `Регистратор ССЫЛКА Документ.ОтражениеЗарплатыВФинансовомУчете`, `ВидДвижения=Приход`, ресурс `СуммаВзаиморасчетов`; мост сотрудника `А_СотрудникиКазна.СотрудникЕРП.ФизическоеЛицо = ВЗ.ФизическоеЛицо`; ключ подразделения — объект начисления (`СтруктураПредприятия`, как план). Контроль Оріль (накоп.): ≈257156,94.
```python
# ВТ МостФЛ (дедуп МИНИМУМ(Ссылка)) + ВЗ ОЗФУ Приход; группировать по Подразделение
# фильтр: Регистратор ССЫЛКА Документ.ОтражениеЗарплатыВФинансовомУчете И Период <= &КонецПериода
```
Expected: ОРІЛЬ ≈ 257156.94 (свериться с готовым `А_ПланФактныйАнализРабот`).

- [ ] **Step 2: ObjectModule `ЗаполнитьФактЗП(Данные, Подразделение, …)`** — `ЗПнарахПроект`, `ЗПнарахМесяц`.

- [ ] **Step 3: Commit** + cp в main.

---

### Task 4: Факт списания материалов (СебестоимостьТоваров, по складам)

**Files:** `test_pfpp_fakt.py`, `…/ObjectModule.bsl`

- [ ] **Step 1: Python — подобрать ресурс/период под эталон ПРООН**

Запрос (склады берём из карты СС: `СкладПроизводство`/`СкладМонтаж`):
```python
q.Text = """
ВЫБРАТЬ
    ВЫРАЗИТЬ(СТ.Регистратор КАК Документ.СборкаТоваров).Склад КАК Склад,
    СУММА(СТ.Стоимость) КАК СтоимостьРегл,
    СУММА(СТ.СтоимостьУпр) КАК СтоимостьУпр
ИЗ РегистрНакопления.СебестоимостьТоваров КАК СТ
ГДЕ СТ.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход)
    И СТ.Регистратор ССЫЛКА Документ.СборкаТоваров
    И ВЫРАЗИТЬ(СТ.Регистратор КАК Документ.СборкаТоваров).А_СтруктураСебестоимости.Наименование = &Имя
    И СТ.Период <= &КонецПериода
СГРУППИРОВАТЬ ПО ВЫРАЗИТЬ(СТ.Регистратор КАК Документ.СборкаТоваров).Склад
"""
q.SetParameter("Имя", "МД ПРООН Черкаси  ДСНС / СТІЛ Цех МД ПРООН")
q.SetParameter("КонецПериода", "30.04.2026 23:59:59")  # эталон Excel «30.04.2026»
```
Expected: склад «виробничий» при `КонецПериода=30.04.2026` → одна из колонок (Стоимость/СтоимостьУпр) = **4 106 111,26**. Зафиксировать в плане, какой ресурс совпал → его и использовать в BSL. (Без верхней границы виробничий = 4 445 080,63 регл / 4 064 510,9 упр — это все доки, включая майские; апрельский срез даёт эталон.)

- [ ] **Step 2: ObjectModule `ЗаполнитьФактМатериалов(Данные, СС, КонПроекта)`** — `МатВирФакт` (склад = `СС.СкладПроизводство`), `МатМонтФакт` (склад = `СС.СкладМонтаж`).

- [ ] **Step 3: Commit** + cp в main.

---

### Task 5: Сборка структуры `ПолучитьДанныеАнализа`

**Files:** `…/ObjectModule.bsl`

- [ ] **Step 1: Экспортная функция-оркестратор**

```bsl
Функция ПолучитьДанныеАнализа(Период, СС) Экспорт
    Данные = Новый Структура;
    Подр = СС.Подразделение;
    НачМес = НачалоМесяца(Период); КонМес = КонецМесяца(Период);
    КонПроекта = ?(ЗначениеЗаполнено(Период), КонМес, '39991231');
    ЗаполнитьПлан(Данные, СС);
    ЗаполнитьФактЧасов(Данные, Подр, НачМес, КонМес, КонПроекта);
    ЗаполнитьФактЗП(Данные, Подр, НачМес, КонМес, КонПроекта);
    ЗаполнитьФактМатериалов(Данные, СС, КонПроекта);
    // производные: ФОТ-грн нотиональный
    Данные.Вставить("ВирГрнПлан", Данные.ЧасВир * Данные.СтавкаВир);
    Данные.Вставить("ВирГрнФакт", Данные.ФактЧасПроект * Данные.СтавкаВир);
    Данные.Вставить("МонтГрнПлан", Данные.ЧасМонт * Данные.СтавкаМонт);
    // … (месячные срезы аналогично)
    Возврат Данные;
КонецФункции
```

- [ ] **Step 2: Python-COM smoke структуры** (через `ВнешниеОтчеты.Создать(путь)` после сборки — выполняется в Task 9; здесь — ревью логики).

- [ ] **Step 3: Commit** + cp в main.

---

## Phase 2 — Построение ТабличногоДокумента

### Task 6: Каркас и шапка (3-уровневая, объединения)

**Files:** `…/ObjectModule.bsl`

- [ ] **Step 1: `СформироватьТабличныйДокумент(Данные)` — заголовок отчёта + шапка блоков**

Строить кодом (без MXL). Объединения — `ТабДок.ОбъединитьЯчейки(Область)`; границы — `Область.Граница…=Новый Линия(ТипЛинииЯчейкиТабличногоДокумента.Сплошная)` (НЕ `.Граница` напрямую — память `mxl`/печать комплектаций). Уровни:
- Стр.1 — групп-заголовки: «Згідно СС», «ВІДВАНТАЖЕННЯ», «ФОТ Проект», «ФОТ <місяць>», «Списання матеріалів».
- Стр.2 — подблоки (Виробництво БЦ чол/год, … грн, Монтаж …, Виробництво+Монтаж).
- Стр.3 — «План | Факт | %%» под каждым подблоком (чол/год — только Факт делится; грн — План/Факт/%).
Заголовок месяца — `Формат(Период,"ДФ='LLLL yyyy'")`.

- [ ] **Step 2: Commit** + cp в main.

### Task 7: Строка данных «Розрахунок в 1С» + %% + границы

**Files:** `…/ObjectModule.bsl`

- [ ] **Step 1: Data-driven вывод метрик**

Определить список колонок (массив структур `{Тип:"Тройка"/"Пара"/"Одно", План, Факт}`) и в цикле выводить значения с расчётом `%% = ?(План=0, 0, Факт/План*100)`. Блоки:
- Згідно СС: РаботыГрн, МатВир, МатМонт, ЧасВир, СтавкаВир, ЧасМонт, СтавкаМонт.
- ФОТ Проект: (ЧасВир|ФактЧасПроект), (ВирГрнПлан|ВирГрнФакт), (ЧасМонт|0), (МонтГрнПлан|0), (СумГрнПлан|СумГрнФакт), ЗПнарахПроект.
- ФОТ Місяць: аналогично с месячными фактами + ЗПнарахМесяц.
- Списання: (МатВир|МатВирФакт), (МатМонт|МатМонтФакт).
- Відвантаження: План=КолвоПроект, Факт=0 (отложено).
Формат чисел `ЧЦ=15, ЧДЦ=2`; % — `ЧДЦ=1`, суффикс «%».

- [ ] **Step 2: Commit** + cp в main.

---

## Phase 3 — Форма

### Task 8: Кастомная форма + тонкий модуль

**Files:**
- Create: `…/А_ПланФактныйПроизводствоПолный/Forms/Форма.xml`, `Forms/Форма/Ext/Form.xml`, `Forms/Форма/Ext/Form/Module.bsl`

- [ ] **Step 1: Сгенерировать форму через `form-compile`**

JSON-определение: реквизиты `Період` (Дата, состав Дата), `СтруктураСебестоимости` (`СправочникСсылка.А_СтруктураСебестоимости`), `Результат` (`ТабличныйДокумент`); элементы — поля ввода Період/СС, кнопка-команда «Сформувати», `ПолеТабличногоДокумента` (Результат). Вызвать навык `/form-compile`.

- [ ] **Step 2: Модуль формы (тонкий)**

```bsl
&НаКлиенте
Процедура Сформувати(Команда)
    СформуватиНаСервере();
КонецПроцедуры

&НаСервере
Процедура СформуватиНаСервере()
    Отчёт = РеквизитФормыВЗначение("Отчет");   // объект внешнего отчёта
    Результат.Очистить();
    Данные = Отчёт.ПолучитьДанныеАнализа(Період, СтруктураСебестоимости);
    Отчёт.СформироватьТабличныйДокумент(Данные, Результат);
КонецПроцедуры
```
(`СформироватьТабличныйДокумент` принять 2-м параметром `ТабДок` или возвращать его — согласовать сигнатуру с Task 6/7.)

- [ ] **Step 3: `form-validate`** (учесть ложные срабатывания — память `form_validate_tilde_spisok_false_positive`).

- [ ] **Step 4: Commit** + cp в main (включая всю папку Forms).

---

## Phase 4 — Сборка, смоук, ревью

### Task 9: Сборка .erf и COM-смоук рендера

**Files:** `_Rarzrabotki/Python/test/test_pfpp_smoke.py`

- [ ] **Step 1: Собрать `.erf`** через `/epf-build` против BaseERP из изолированного каталога (память `epf_build_stub_isolation_gotcha`; при «занятой» базе закрыть Конфигуратор).

- [ ] **Step 2: COM-смоук**

```python
import win32com.client
erp = win32com.client.Dispatch("V83.COMConnector").Connect('Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
rep = erp.ВнешниеОтчеты.Создать(r"...\А_ПланФактныйПроизводствоПолный.erf")
сс = erp.Справочники.А_СтруктураСебестоимости.НайтиПоНаименованию("МД МХП ОРІЛЬ / СТІЛ МД МХП ОРІЛЬ")
данные = rep.ПолучитьДанныеАнализа(erp.Дата(2026,4,30), сс.Ссылка)
assert round(данные.РаботыГрн)==753000 and round(данные.МатВир)==1711992 and round(данные.МатМонт)==206605
assert данные.ЧасВир==1927 and данные.ЧасМонт==868
assert round(данные.ВирГрнПлан)==385400 and round(данные.МонтГрнПлан)==302400
td = erp.NewObject("ТабличныйДокумент"); rep.СформироватьТабличныйДокумент(данные, td)
assert td.ВысотаТаблицы > 3
print("SMOKE OK", данные.ФактЧасПроект, данные.ЗПнарахПроект)
```
Expected: assert'ы проходят; `ВысотаТаблицы>3`; печать факта (часы 2229, ЗП ≈257156,94).

- [ ] **Step 3: Commit** результата сборки + cp `.erf` в main.

### Task 10: Ревью и финал

- [ ] **Step 1: `/1c-bsl-review`** по `ObjectModule.bsl` и `Form/Module.bsl` — исправить критические замечания.
- [ ] **Step 2: Проверить чек-лист §10 спецификации** (план Оріль, материалы ПРООН, ЗП-сверка, копии в main).
- [ ] **Step 3: Финальный commit** — `feat(pfpp): отчёт А_ПланФактныйПроизводствоПолный готов + smoke PASS`.
- [ ] **Step 4:** обновить статус в `ПРОМТ_…md` на «РЕАЛИЗОВАНО + smoke PASS», записать memory.

---

## Self-review (покрытие спецификации)

- §2 источники → Tasks 1–4 (план/часы/ЗП/материалы) ✓
- §3 правила Вир/Монт (работы НаВыезде, материалы Этап, факт склад) → Tasks 1, 4 ✓
- §4 5 блоков + 2 ЗП-колонки → Tasks 6–7 (шапка + строка) ✓
- §5 период (проект/місяць) → Task 5 (`КонПроекта`/`НачМес`/`КонМес`) ✓
- §6 архитектура (программный ТабДок, тонкая форма) → Tasks 0, 6–8 ✓
- §8 сборка/смоук → Task 9 ✓; §9 отложенное (відвантаження-факт=0, E/H/BC-BF) — не включаем, отражено в Task 7 (Відвантаження факт=0) ✓
- Типы согласованы: структура `Данные` (поля РаботыГрн/МатВир/МатМонт/ЧасВир/ЧасМонт/СтавкаВир/СтавкаМонт/ФактЧасПроект/ФактЧасМесяц/ЗПнарахПроект/ЗПнарахМесяц/МатВирФакт/МатМонтФакт + производные ВирГрн*/МонтГрн*/СумГрн*) определяется в Tasks 1–5 и потребляется в Tasks 7, 9 — имена едины.
