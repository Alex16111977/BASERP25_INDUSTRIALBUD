# ПРОМТ: Розробка обробки "Заповнити Счета учета" для BAS ERP 2.5

## 🎯 МЕТА ОБРОБКИ
Зовнішня обробка для синхронізації полів `А_СчетУчетаБух` та `А_Организация` у довіднику **Номенклатура** на основі даних з регістру бухгалтерії **Хозрасчетный** (журнал проводок).

---

## 📋 БІЗНЕС-ЛОГІКА

### Алгоритм визначення рахунку для кожної картки номенклатури:

1. **Отримати залишки** з регістру бухгалтерії Хозрасчетний де Субконто1 = Номенклатура
2. **Визначити рахунок** - за яким рахунком ведеться облік (201, 205, 281 тощо)
3. **Визначити Організацію** - з виміру Організація регістру
4. **Записати в картку** номенклатури поля:
   - `А_СчетУчетаБух` ← Рахунок з залишків
   - `А_Организация` ← Організація з залишків

### КРИТИЧНО - Правильний синтаксис запитів до РегістрБухгалтерії.Хозрасчетний:
- **НЕ існують** поля Субконто1, Субконто2, Субконто3 в основній таблиці!
- **Використовувати** віртуальну таблицю **Остатки** - вона повертає Субконто1, Субконто2, Субконто3
- **Параметри** віртуальної таблиці: `Остатки(ДатаОкончания, Счет..., МерыОстатков, Субконто)`

---

## ✅ ПЕРЕВІРЕНИЙ РОБОЧИЙ ЗАПИТ

```sql
ВЫБРАТЬ
    Остатки.Организация КАК Организация,
    Остатки.Счет КАК Счет,
    Остатки.Субконто1 КАК Номенклатура
ИЗ
    РегистрБухгалтерии.Хозрасчетный.Остатки(
        &ДатаОкончания, 
        Счет.Код ПОДОБНО "20%" ИЛИ Счет.Код ПОДОБНО "28%", 
        , 
    ) КАК Остатки
ГДЕ
    НЕ Остатки.Субконто1 = НЕОПРЕДЕЛЕНО
    И Остатки.Субконто1 ССЫЛКА Справочник.Номенклатура
    
СГРУППИРОВАТЬ ПО
    Остатки.Субконто1,
    Остатки.Счет,
    Остатки.Организация
```

### Приклад результату запиту (реальні дані з БД):
| Организация | Счет | Номенклатура |
|-------------|------|--------------|
| ТОВ "ІНДАСТРІАЛБУД" | 205 | Арматура 12 міра (12-3760 А400С/А500С) |
| ТОВ "ІНДАСТРІАЛБУД" | 205 | Швелер 16У (160 12,05- 8240 3пс) |

---

## 🏗️ АРХІТЕКТУРА ОБРОБКИ

### Структура (за зразком "Перенос остатків номенклатури")

```
Зовнішня обробка: А_ЗаполнитьСчетаУчета
│
├── Реквізити обробки:
│   ├── ДатаОкончания (Дата) - граничная дата для расчета остатков
│   ├── Организация (СправочникСсылка.Организации) - фільтр по організації
│   └── ТолькоПустые (Булево) - обробляти тільки картки з пустими А_СчетУчетаБух
│
├── Таблична частина: Товары
│   ├── Номенклатура (СправочникСсылка.Номенклатура)
│   ├── СчетУчета (ПланСчетовСсылка.Хозрасчетный)
│   ├── Организация (СправочникСсылка.Организации)
│   ├── ТекущийСчет (ПланСчетовСсылка.Хозрасчетный) - поточне значення в картці
│   ├── ТекущаяОрганизация (СправочникСсылка.Организации) - поточне значення в картці
│   └── Обработано (Булево)
│
└── Форма: Форма (керована форма)
```

---

## 📝 КОД МОДУЛЯ ОБ'ЄКТУ

### Процедура ЗаполнитьТаблицу()

```bsl
Процедура ЗаполнитьТаблицу() Экспорт
    
    Товары.Очистить();
    
    // Запит: отримати залишки з субконто Номенклатура
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ РАЗРЕШЕННЫЕ
    |    Остатки.Организация КАК Организация,
    |    Остатки.Счет КАК Счет,
    |    Остатки.Субконто1 КАК Номенклатура
    |ПОМЕСТИТЬ ВТ_Остатки
    |ИЗ
    |    РегистрБухгалтерии.Хозрасчетный.Остатки(
    |        &ДатаОкончания,
    |        Счет.Код ПОДОБНО ""20%"" 
    |            ИЛИ Счет.Код ПОДОБНО ""28%""
    |            ИЛИ Счет.Код ПОДОБНО ""22%""
    |            ИЛИ Счет.Код ПОДОБНО ""23%""
    |            ИЛИ Счет.Код ПОДОБНО ""26%"",
    |        ,
    |    ) КАК Остатки
    |ГДЕ
    |    НЕ Остатки.Субконто1 = НЕОПРЕДЕЛЕНО
    |    И Остатки.Субконто1 ССЫЛКА Справочник.Номенклатура
    |    И (&Организация = ЗНАЧЕНИЕ(Справочник.Организации.ПустаяСсылка)
    |        ИЛИ Остатки.Организация = &Организация)
    |
    |СГРУППИРОВАТЬ ПО
    |    Остатки.Субконто1,
    |    Остатки.Счет,
    |    Остатки.Организация
    |;
    |
    |////////////////////////////////////////////////////////////////////////////////
    |ВЫБРАТЬ
    |    ВТ_Остатки.Номенклатура КАК Номенклатура,
    |    ВТ_Остатки.Счет КАК СчетУчета,
    |    ВТ_Остатки.Организация КАК Организация,
    |    ВТ_Остатки.Номенклатура.А_СчетУчетаБух КАК ТекущийСчет,
    |    ВТ_Остатки.Номенклатура.А_Организация КАК ТекущаяОрганизация
    |ИЗ
    |    ВТ_Остатки КАК ВТ_Остатки
    |ГДЕ
    |    НЕ &ТолькоПустые
    |    ИЛИ (ВТ_Остатки.Номенклатура.А_СчетУчетаБух = ЗНАЧЕНИЕ(ПланСчетов.Хозрасчетный.ПустаяСсылка)
    |        ИЛИ ВТ_Остатки.Номенклатура.А_СчетУчетаБух = НЕОПРЕДЕЛЕНО)";
    
    Запрос.УстановитьПараметр("ДатаОкончания", ?(ЗначениеЗаполнено(ДатаОкончания), КонецДня(ДатаОкончания), КонецДня(ТекущаяДата())));
    Запрос.УстановитьПараметр("Организация", Организация);
    Запрос.УстановитьПараметр("ТолькоПустые", ТолькоПустые);
    
    Результат = Запрос.Выполнить();
    Выборка = Результат.Выбрать();
    
    Пока Выборка.Следующий() Цикл
        
        НоваяСтрока = Товары.Добавить();
        НоваяСтрока.Номенклатура = Выборка.Номенклатура;
        НоваяСтрока.СчетУчета = Выборка.СчетУчета;
        НоваяСтрока.Организация = Выборка.Организация;
        НоваяСтрока.ТекущийСчет = Выборка.ТекущийСчет;
        НоваяСтрока.ТекущаяОрганизация = Выборка.ТекущаяОрганизация;
        НоваяСтрока.Обработано = Ложь;
        
    КонецЦикла;
    
КонецПроцедуры
```

### Процедура ОбновитьКарточкиНоменклатуры()

```bsl
Процедура ОбновитьКарточкиНоменклатуры() Экспорт
    
    КоличествоОбновленных = 0;
    КоличествоОшибок = 0;
    
    Для Каждого СтрокаТовары Из Товары Цикл
        
        Если СтрокаТовары.Обработано Тогда
            Продолжить;
        КонецЕсли;
        
        // Пропускаємо якщо значення не змінилися
        Если СтрокаТовары.СчетУчета = СтрокаТовары.ТекущийСчет 
            И СтрокаТовары.Организация = СтрокаТовары.ТекущаяОрганизация Тогда
            СтрокаТовары.Обработано = Истина;
            Продолжить;
        КонецЕсли;
        
        Попытка
            
            СпрНоменклатура = СтрокаТовары.Номенклатура.ПолучитьОбъект();
            
            Если СпрНоменклатура = Неопределено Тогда
                Продолжить;
            КонецЕсли;
            
            СпрНоменклатура.А_СчетУчетаБух = СтрокаТовары.СчетУчета;
            СпрНоменклатура.А_Организация = СтрокаТовары.Организация;
            
            СпрНоменклатура.ОбменДанными.Загрузка = Истина;
            СпрНоменклатура.Записать();
            
            СтрокаТовары.Обработано = Истина;
            КоличествоОбновленных = КоличествоОбновленных + 1;
            
        Исключение
            КоличествоОшибок = КоличествоОшибок + 1;
            Сообщить("Ошибка при обновлении " + СтрокаТовары.Номенклатура + ": " + ОписаниеОшибки());
        КонецПопытки;
        
    КонецЦикла;
    
    Сообщить("Обновлено карточек: " + КоличествоОбновленных + ", ошибок: " + КоличествоОшибок);
    
КонецПроцедуры
```

---

## 📊 МЕТАДАНІ (перевірені через MCP)

### Довідник Номенклатура - цільові поля:
- `А_СчетУчетаБух` - ПланСчетов.Хозрасчетный ✅
- `А_Организация` - Справочник.Организации ✅

### Регістр бухгалтерії Хозрасчетный:
- **Вимір** `Организация` - Справочник.Организации ✅
- **Віртуальна таблиця Остатки** повертає:
  - `Счет` - рахунок
  - `Субконто1`, `Субконто2`, `Субконто3` - субконто
  - `Организация` - організація (вимір)
  - `СуммаОстаток`, `КоличествоОстаток` - залишки

### ⚠️ КРИТИЧНО - Синтаксис запитів:
```
❌ НЕПРАВИЛЬНО (не працює!):
   РегистрБухгалтерии.Хозрасчетный.Субконто1
   РегистрБухгалтерии.Хозрасчетный.СубконтоДт1

✅ ПРАВИЛЬНО:
   РегистрБухгалтерии.Хозрасчетный.Остатки(...).Субконто1
```

---

## ✅ ЧЕКЛІСТ РЕАЛІЗАЦІЇ

### КРОК 1: Створення XML обробки
- [ ] Створити новий файл `Заполнить Счета учета.xml`
- [ ] Скопіювати структуру з `Перенос остатков номенклатуры.xml`
- [ ] Змінити UUID на нові (генерувати нові!)
- [ ] Змінити ім'я: `А_ЗаполнитьСчетаУчета`

### КРОК 2: Налаштування реквізитів
- [ ] ДатаОкончания (Дата) - для розрахунку залишків
- [ ] Организация (СправочникСсылка.Организации) - фільтр
- [ ] ТолькоПустые (Булево) - режим обробки

### КРОК 3: Таблична частина Товары
- [ ] Номенклатура (СправочникСсылка.Номенклатура)
- [ ] СчетУчета (ПланСчетовСсылка.Хозрасчетный)
- [ ] Организация (СправочникСсылка.Організації)
- [ ] ТекущийСчет (для порівняння)
- [ ] ТекущаяОрганизация (для порівняння)
- [ ] Обработано (Булево)

### КРОК 4: Форма
- [ ] Створити керовану форму
- [ ] Кнопка "Заполнить" → ЗаполнитьТаблицу()
- [ ] Кнопка "Обновить карточки" → ОбновитьКарточкиНоменклатуры()
- [ ] Кнопка "Очистить"
- [ ] Таблиця Товари з колонками

### КРОК 5: Тестування
- [ ] Запустити заповнення - перевірити що запит виконується
- [ ] Порівняти рахунки в таблиці з реальними проводками
- [ ] Оновити 2-3 картки вручну для перевірки
- [ ] Повний запуск оновлення

---

## ⚠️ КРИТИЧНІ МОМЕНТИ

1. **Використовуй віртуальну таблицю Остатки** - прямий доступ до субконто в основній таблиці не працює!

2. **Коди рахунків товарів**: 
   - 20% - матеріали
   - 22% - МШП
   - 23% - виробництво  
   - 26% - готова продукція
   - 28% - товари

3. **ОбменДанными.Загрузка = Истина** - обов'язково для масового оновлення

4. **Перевірка на зміни** - не перезаписувати якщо значення однакові

5. **Фільтр по організації** - дозволяє обробити картки для конкретної фірми

---

## 🔗 ПОСИЛАННЯ

- Зразок обробки: `C:\Configuration_downloads\BASERP25\_Rarzrabotki\Обработки\Перенос остатков номенклатуры.xml`
- Файл результату: `C:\Configuration_downloads\BASERP25\_Rarzrabotki\Обработки\Заполнить Счета учета.xml`
