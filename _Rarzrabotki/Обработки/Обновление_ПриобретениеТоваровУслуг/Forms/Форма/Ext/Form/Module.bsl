
&НаСервере
Процедура ОбновитьПоступлениеНаСервере()
	
	//***  
	Док = Объект.ДокПриобретениеТоваровУслуг.ПолучитьОбъект();
	  Док.Товары.Очистить();
	  Для каждого СтрокаТЗ Из Док.А_Товары Цикл
	  
	  	 СтрокаТабличнойЧасти = Док.Товары.Добавить();
	     СтрокаТабличнойЧасти.Номенклатура = СтрокаТЗ.Номенклатура;
	     СтрокаТабличнойЧасти.Количество = СтрокаТЗ.Количество;

	  КонецЦикла;
	  Док.Записать(РежимЗаписиДокумента.Проведение);
	//***

КонецПроцедуры

&НаКлиенте
Процедура ОбновитьПоступление(Команда)
	ОбновитьПоступлениеНаСервере();
КонецПроцедуры
