
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	Список.Параметры.УстановитьЗначениеПараметра("ТекущийАкт",Параметры.ТекущийАкт);
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(Список,"ЛокальнаяСмета",Параметры.ТекущийАкт.ЛокальнаяСмета,ВидСравненияКомпоновкиДанных.Равно);

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура СписокВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	
	// Если Не ЗначениеЗаполнено(КомментарийКАкту) Тогда
	//	Состояние(НСтр("ru='Необходимо указать комментарий'"),100);
	//	Возврат;
	// КонецЕсли;

	Структура = Новый Структура("Акт,Комментарий",ВыбраннаяСтрока,КомментарийКАкту);
	Закрыть(Структура);
КонецПроцедуры

&НаКлиенте
Процедура СписокВыборЗначения(Элемент, Значение, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	
	// Если Не ЗначениеЗаполнено(КомментарийКАкту) Тогда
	//	Состояние(НСтр("ru='Необходимо указать комментарий'"),100);
	//	Возврат;
	// КонецЕсли;

	Структура = Новый Структура("Акт,Комментарий",Значение,КомментарийКАкту);
	Закрыть(Структура);
	Возврат;
КонецПроцедуры

#КонецОбласти