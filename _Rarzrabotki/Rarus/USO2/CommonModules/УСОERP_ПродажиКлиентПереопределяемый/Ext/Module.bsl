////////////////////////////////////////////////////////////////////////////////
// Модуль содержит процедуры и функции для обработки действий пользователя
// в процессе работы с документами продажи.
//
////////////////////////////////////////////////////////////////////////////////

#Область ПрограммныйИнтерфейс

Процедура ВвестиСчетФактуруПослеВопроса(РезультатВопроса, ДополнительныеПараметры) Экспорт
    
    Исправление = ДополнительныеПараметры.Исправление;
    Корректировочный = ДополнительныеПараметры.Корректировочный;
    Организация = ДополнительныеПараметры.Организация;
    ОткрыватьСуществующую = ДополнительныеПараметры.ОткрыватьСуществующую;
    Форма = ДополнительныеПараметры.Форма;
    
    КодОтвета = РезультатВопроса;
    Если КодОтвета = КодВозвратаДиалога.Да Тогда
        Попытка
            ВводитьСчетФактуру = Форма.Записать(Новый Структура("РежимЗаписи", РежимЗаписиДокумента.Проведение));
        Исключение
            ПоказатьПредупреждение(Новый ОписаниеОповещения("ВвестиСчетФактуруЗавершение", ЭтотОбъект, Новый Структура("Исправление, Корректировочный, Организация, ОткрыватьСуществующую, Форма", Исправление, Корректировочный, Организация, ОткрыватьСуществующую, Форма)), НСтр("ru = 'Не удалось выполнить проведение документа'"));
            Возврат;
        КонецПопытки;
    Иначе
        ВводитьСчетФактуру = Ложь;
    КонецЕсли;
    
    ВвестиСчетФактуруФрагмент(ВводитьСчетФактуру, Исправление, Корректировочный, Организация, ОткрыватьСуществующую, Форма);

КонецПроцедуры

Процедура ВвестиСчетФактуруФрагмент(Знач ВводитьСчетФактуру, Знач Исправление, Знач Корректировочный, Знач Организация, Знач ОткрыватьСуществующую, Знач Форма)
    
    Перем ДанныеСчетаФактуры, ПараметрыФормы;
    
    Если ВводитьСчетФактуру Тогда
        
        ДанныеСчетаФактуры = Новый Структура;
        ДанныеСчетаФактуры.Вставить("ДокументОснование", Форма.Объект.Ссылка);
        ДанныеСчетаФактуры.Вставить("Организация",       Организация);
        ДанныеСчетаФактуры.Вставить("Дата", 	         ?(ОткрыватьСуществующую, Форма.Объект.Дата, '00010101'));
        ДанныеСчетаФактуры.Вставить("Исправление",       Исправление);
        ДанныеСчетаФактуры.Вставить("Корректировочный",  Корректировочный);
        
        ПараметрыФормы = Новый Структура("Основание, ДокументОснование, ВозвращатьПараметрыПредставления",
        ДанныеСчетаФактуры, Форма.Объект.Ссылка, Истина);
		ФормаСчетаФактуры = ПолучитьФорму("Документ.СчетФактураВыданный.ФормаОбъекта", ПараметрыФормы, Форма.Объект.Ссылка);
		ФормаСчетаФактуры.Записать(Новый Структура("РежимЗаписи", РежимЗаписиДокумента.Проведение));
        
    КонецЕсли;

КонецПроцедуры

#КонецОбласти
