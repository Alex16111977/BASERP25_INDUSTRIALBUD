#Область ОбработчикиСобытий

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	Если Объект.Ссылка = Справочники.СМ_ИтогиПоГлавам.ПустаяСсылка() Тогда
		Объект.Код = 0;
		Объект.Идентификатор = "";
		Объект.ПорядковыйНомер = Справочники.СМ_ИтогиПоГлавам.ПолучитьМаксимальныйНомерПоПорядку() + 1;
	КонецЕсли;

КонецПроцедуры

#КонецОбласти