
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;

	РазрешитьРедактированиеВариантАктирования = Истина;
	РазрешитьРедактированиеВидГрафика  = Истина;
	РазрешитьРедактированиеКалендарь   = Истина;
	РазрешитьРедактированиеФормаОплаты = Истина;
	РазрешитьРедактированиеЭтапов      = Истина;

	Если Параметры.Свойство("ВидГрафика") Тогда
		Элементы.РазрешитьРедактированиеВариантАктирования.Видимость = (Параметры.ВидГрафика = Перечисления.УСОERP_ВидыГрафиковАктированияФинансирования.ГрафикАктирования);
		Элементы.РазрешитьРедактированиеЭтапов.Видимость = (Параметры.ВидГрафика = Перечисления.УСОERP_ВидыГрафиковАктированияФинансирования.ГрафикФинансирования);
	КонецЕсли;
		
	СобытияФорм.ПриСозданииНаСервере(ЭтаФорма, Отказ, СтандартнаяОбработка);

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура РазрешитьРедактирование(Команда)

	Результат = Новый Массив;
	           
	Если РазрешитьРедактированиеВидГрафика Тогда
		Результат.Добавить("ВидГрафика");
	КонецЕсли;
	
	Если РазрешитьРедактированиеВариантАктирования Тогда
		Результат.Добавить("ВариантАктирования");
	КонецЕсли;
	
	Если РазрешитьРедактированиеКалендарь Тогда
		Результат.Добавить("Календарь");
	КонецЕсли;
	
	Если РазрешитьРедактированиеФормаОплаты Тогда
		Результат.Добавить("ФормаОплаты");
	КонецЕсли;
	
	Если РазрешитьРедактированиеЭтапов Тогда
		Результат.Добавить("Этапы");
	КонецЕсли;
	
	Закрыть(Результат);

КонецПроцедуры

&НаКлиенте
Процедура УстановитьВсеОтметки(Команда)
	
	РазрешитьРедактированиеВариантАктирования = Истина;
	РазрешитьРедактированиеВидГрафика  = Истина;
	РазрешитьРедактированиеКалендарь   = Истина;
	РазрешитьРедактированиеФормаОплаты = Истина;
	РазрешитьРедактированиеЭтапов      = Истина;
	
КонецПроцедуры

&НаКлиенте
Процедура СнятьВсеОтметки(Команда)
	
	РазрешитьРедактированиеВариантАктирования = Ложь;
	РазрешитьРедактированиеВидГрафика  = Ложь;
	РазрешитьРедактированиеКалендарь   = Ложь;
	РазрешитьРедактированиеФормаОплаты = Ложь;
	РазрешитьРедактированиеЭтапов      = Ложь;
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ВыполнитьПереопределяемуюКоманду(Команда)
	
	СобытияФормКлиент.ВыполнитьПереопределяемуюКоманду(ЭтаФорма, Команда);
	
КонецПроцедуры

#КонецОбласти
