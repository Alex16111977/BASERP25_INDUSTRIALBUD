#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	Если Параметры.РежимВыбора Тогда
		Элементы.Список.МножественныйВыбор = Ложь;
	КонецЕсли;
	
	// *********************************************
	// ++ AVV Роли в системе
	//Если РольДоступна("СМ_Сметчик") ИЛИ РольДоступна("СМ_СметчикМатериалы") Тогда
	//	Если Параметры.Свойство("СНБ") Тогда
	//		Если ЗначениеЗаполнено(Параметры.СНБ) Тогда
	//			ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(Список,"СметноНормативнаяБаза",Параметры.СНБ,ВидСравненияКомпоновкиДанных.Равно);
	//		КонецЕсли;
	//	КонецЕсли;
	//КонецЕсли;
	// -- AVV
	//**********************************************************

	Список.Параметры.УстановитьЗначениеПараметра("ИспользоватьСтатусы",ПолучитьФункциональнуюОпцию("СМ_ИспользоватьСтатусыНормативов"));
	
КонецПроцедуры

#КонецОбласти
