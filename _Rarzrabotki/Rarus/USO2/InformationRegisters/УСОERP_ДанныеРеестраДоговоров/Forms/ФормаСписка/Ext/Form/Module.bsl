
&НаСервере
Процедура ДобавитьНовуюЗаписьНаСервере()
	//Документы.УСОERP_РеестрДоговоров.СоздатьДокумент().ПолучитьФорму("ФормаДокумента").ОткрытьМодально();
	// Вставить содержимое обработчика.
КонецПроцедуры

&НаКлиенте
Процедура ДобавитьНовуюЗапись(Команда)
	ОткрытьФорму("Документ.УСОERP_РеестрДоговоров.ФормаОбъекта");
	//ДобавитьНовуюЗаписьНаСервере();
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Параметры.Свойство("Договор") И ЗначениеЗаполнено(Параметры.Договор) Тогда
		
		Параметры.Отбор.Вставить("Договор",Параметры.Договор);
	КонецЕсли;	
КонецПроцедуры
