Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	
	Для Каждого Строка Из Счета Цикл
		Движения.СчетаНал.ВыполнитьСторноОстатка(Строка.Счет, Дата);
		Движения.СчетаПоставщиков.ВыполнитьСторноОстатка(Строка.Счет, Дата);
		Движения.ЗаявкиНаРасходованиеДС.ВыполнитьСторноОстатка(Строка.Счет, Дата);
	КонецЦикла; 
	
КонецПроцедуры

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	
	Ответственный = ПараметрыСеанса.ТекущийПользователь;
	Подразделение = Ответственный.Подразделение;
	
КонецПроцедуры