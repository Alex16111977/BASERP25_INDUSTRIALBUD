
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	
	Ответственный= Пользователи.ТекущийПользователь();

КонецПроцедуры

Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	
	Движения.Бюджеты.Записывать = Истина;
	
	Для Каждого Строка Из Показатели Цикл
		Если Строка.Сумма = 0 тогда 
			Продолжить;
		КонецЕсли;
		Движение	= Движения.Бюджеты.Добавить();
		Движение.Регистратор	= Ссылка;
		Движение.Период			= Дата;
		Движение.ВерсияБюджета	= ВерсияБюджета;
		Движение.Подразделение	= Подразделение;//Строка.Подразделение;
		Движение.Секция			= Строка.Секция;
		Движение.СДДС			= Строка.СДДС;
		Движение.Месяц			= НачалоМесяца(Строка.Месяц);
		Движение.НаправлениеДДС	= Строка.НаправлениеДДС;
		Движение.Сумма			= Строка.Сумма
	КонецЦикла;
	
КонецПроцедуры

Процедура ПроверитьЗаполнениеОбязательныхРевизитов(Отказ)
	
	МассивПроверяемыхРеквизитов = Новый Массив;
	МассивПроверяемыхРеквизитов.Добавить("Подразделение");
	
	АХ_ОбщегоНазначения.КонтрольЗаполненияОбязательныхРеквизитовШапкиОбъекта(ЭтотОбъект, МассивПроверяемыхРеквизитов, Отказ);
	
	МассивПроверяемыхРеквизитовТЧ = Новый Массив;
	МассивПроверяемыхРеквизитовТЧ.Добавить("Месяц");

	АХ_ОбщегоНазначения.КонтрольЗаполненияОбязательныхРеквизитовТачбличнойЧастиОбъекта(ЭтотОбъект, Показатели, МассивПроверяемыхРеквизитовТЧ, Отказ);

КонецПроцедуры