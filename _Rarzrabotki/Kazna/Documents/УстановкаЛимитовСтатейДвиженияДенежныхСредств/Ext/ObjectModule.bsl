Процедура ОбработкаПроведения(Отказ, Режим)
	
	Движения.ЛимитыДДС.Записывать = Истина;
	Для Каждого ТекСтрокаЛимиты Из Лимиты Цикл
		Движение = Движения.ЛимитыДДС.Добавить();
		Движение.ВидДвижения = ВидДвиженияНакопления.Приход;
		Движение.Период = Дата;
		Движение.Подразделение = ТекСтрокаЛимиты.Подразделение;
		Движение.СтатьяДвиженияДенежныхСредств = ТекСтрокаЛимиты.СтатьяДвиженияДенежныхСредств;
		Движение.ВидОплаты = ТекСтрокаЛимиты.ВидОплаты;
		Движение.Организация = Организация;
		//Движение.Направление = ?(ЗначениеЗаполнено(ТекСтрокаЛимиты.Направление),ТекСтрокаЛимиты.Направление,Направление);
		Движение.Направление = Направление;
		Движение.Сумма = ТекСтрокаЛимиты.Сумма;
	КонецЦикла;

КонецПроцедуры

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	Ответственный = Пользователи.ТекущийПользователь();
КонецПроцедуры

Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	СуммаДокумента = 0;
	Для Каждого лСтрока Из Лимиты Цикл 
		СуммаДокумента = СуммаДокумента + лСтрока.Сумма;
	КонецЦикла;
	
КонецПроцедуры
