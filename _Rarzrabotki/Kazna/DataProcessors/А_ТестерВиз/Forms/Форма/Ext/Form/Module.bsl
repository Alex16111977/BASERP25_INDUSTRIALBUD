
&НаСервере
Процедура ПроверитьНаСервере()
	
	//Схема визирования                                                      
	ПараметрыСхемыВизирования = Новый Структура();
	ПараметрыСхемыВизирования.Вставить("Подразделение",Подразделение);
	ПараметрыСхемыВизирования.Вставить("Организация",Организация);
	ПараметрыСхемыВизирования.Вставить("ВидОплаты",ВидОплаты);
	ПараметрыСхемыВизирования.Вставить("Сумма",Сумма);
	ПараметрыСхемыВизирования.Вставить("СтатьяДвиженияДенежныхСредств",СтатьяДДС);
	ПараметрыСхемыВизирования.Вставить("Направление",Направление);
	ПараметрыСхемыВизирования.Вставить("Должность",Инициатор.Должность);
	
	СхемаВизирования =РегистрыСведений.ОпределениеСхемыВизированияПоСчетам.ПолучитьСхемуВизирования(ПараметрыСхемыВизирования);
	
	Объект.СоставСхемы.Очистить();
	//РегистрыСведений.ОпределениеСхемыВизированияПоСчетам.ПолучитьМассивВизерейПоСхеме(СхемаВизирования);
	Для каждого Стр Из СхемаВизирования.Роли Цикл
		нСТр = Объект.СоставСхемы.Добавить();
		нСТр.Роль =Стр.Роль;
		
		ПараметрыСхемыВизирования.Вставить("Роль",Стр.Роль);
		МассивПользователей = РегистрыСведений.ИдентификацияВизирующихПоРолям.ПолучитьМассивПользователей(ПараметрыСхемыВизирования);
		Для Каждого Строка Из МассивПользователей Цикл 
			нСТр.Ответственные = нСТр.Ответственные+Строка+", ";
		КонецЦикла;           
		нСТр.Ответственные= Сред(нСТр.Ответственные,1,СтрДлина(нСТр.Ответственные)-2) ;
		ПараметрыСхемыВизирования.Удалить("Роль");
	КонецЦикла;
	
	
	////Визирущие 
	//ПараметрыОтбора = Новый Структура("Подразделение, СтатьяДвиженияДенежныхСредств, ВидОплаты, Организация, Сумма, ВидЗатрат,Направление");
	//ЗаполнитьЗначенияСвойств(ПараметрыОтбора, ЭтотОбъект);
	//ПараметрыОтбора.Вставить("Роль", Роль);
	//МассивПользователей = РегистрыСведений.ИдентификацияВизирующихПоРолям.ПолучитьМассивПользователей(ПараметрыОтбора);
	//ТекущиеВизирующие.Очистить();
	//Для Каждого Строка Из МассивПользователей Цикл 
	//	ТекущиеВизирующие.Добавить().Пользователь = Строка;
	//КонецЦикла;

	
КонецПроцедуры

&НаКлиенте
Процедура Проверить(Команда)
	ПроверитьНаСервере();
КонецПроцедуры
