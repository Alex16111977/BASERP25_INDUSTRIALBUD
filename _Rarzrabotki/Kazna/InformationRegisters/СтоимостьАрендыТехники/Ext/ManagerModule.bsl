Функция ПолучитьЦену(НаДату,Оборудование,ВидУчета) Экспорт 
	УстановитьПривилегированныйРежим(Истина);
	Запрос = Новый Запрос;
	Запрос.Текст=   "ВЫБРАТЬ
	                |	СтоимостьПродукцииСрезПоследних.Цена,
	                |	СтоимостьПродукцииСрезПоследних.ЦенаПерегона
	                |ИЗ
	                |	РегистрСведений.СтоимостьАрендыТехники.СрезПоследних(&НаДату, ) КАК СтоимостьПродукцииСрезПоследних
	                |ГДЕ
	                |	(СтоимостьПродукцииСрезПоследних.Оборудование = &Оборудование
	                |			ИЛИ СтоимостьПродукцииСрезПоследних.Оборудование = ЗНАЧЕНИЕ(Справочник.Подразделения.ПустаяСсылка))";
	ЗАпрос.УстановитьПараметр("НаДату",НаДату);
	ЗАпрос.УстановитьПараметр("Оборудование",Оборудование);
	Выборка = Запрос.Выполнить().Выбрать();
	
	
	Если Выборка.Следующий() Тогда
		Если ВидУчета=Перечисления.ВидыУчетаРаботыТехники.Работа Тогда
		
			Цена  = Выборка.Цена;
		Иначе
			Цена  = Выборка.ЦенаПерегона;
		
		КонецЕсли;
		
	Иначе
		Цена=  0;
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Ложь);
	
	Возврат Цена;
КонецФункции // ()


