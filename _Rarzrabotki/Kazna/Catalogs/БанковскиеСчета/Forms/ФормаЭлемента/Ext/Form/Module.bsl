
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Элементы.ГруппаНастройки.Видимость=ЭтоОрганизация();
КонецПроцедуры

&НаСервере
Функция  ЭтоОрганизация()
	Возврат ТипЗнч(объект.Владелец)=Тип("СправочникСсылка.Организации");
КонецФункции

Функция СформироватьНаименование()
	Если ЭтоОрганизация() Тогда
			Объект.Наименование= ""+СокрЛП(Объект.Владелец.Наименование)+" -  "+СокрЛП(Объект.НомерСчета)+" в "+СокрЛП(Объект.Банк)+", МФО "+Объект.Банк.Код;
	Иначе	
			Объект.Наименование= ""+СокрЛП(Объект.НомерСчета)+" в "+СокрЛП(Объект.Банк)+", МФО "+Объект.Банк.Код;
		
	КонецЕсли;

КонецФункции // ()


&НаКлиенте
Процедура БанкПриИзменении(Элемент)
	// Вставить содержимое обработчика.
	СформироватьНаименование();
КонецПроцедуры

&НаКлиенте
Процедура НомерСчетаПриИзменении(Элемент)
	// Вставить содержимое обработчика.
	СформироватьНаименование();
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ВерсионированиеОбъектов.ПриСозданииНаСервере(ЭтотОбъект);
КонецПроцедуры


