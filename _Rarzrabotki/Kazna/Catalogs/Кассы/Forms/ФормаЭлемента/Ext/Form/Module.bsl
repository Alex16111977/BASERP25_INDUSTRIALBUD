
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ВерсионированиеОбъектов.ПриСозданииНаСервере(ЭтотОбъект);
КонецПроцедуры

 //Тютюн
&НаКлиенте
Процедура _ПодотчетПриИзменении(Элемент)
	ВидимостьПодотчет();
КонецПроцедуры  

&НаКлиенте
Процедура ВидимостьПодотчет()  
	Если Объект._Подотчет Тогда 
		Элементы._Лимит.Видимость = Истина;
	Иначе 
		Элементы._Лимит.Видимость = Ложь; 
		Объект._Лимит = 0;
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ВидимостьПодотчет()  
КонецПроцедуры
//КОнецИзменений
 
