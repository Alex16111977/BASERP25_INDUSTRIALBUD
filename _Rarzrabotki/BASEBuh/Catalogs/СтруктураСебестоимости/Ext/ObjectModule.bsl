
Процедура ПередЗаписью(Отказ)
	
    Для каждого СтрокаТабличнойЧасти Из Комплектующие Цикл
	
		  Если СтрокаТабличнойЧасти.ОбщееНазвание.Единица <> СтрокаТабличнойЧасти.Единица Тогда
		      СообщенияОбОшибке = " Рядок "+СокрЛП(СтрокаТабличнойЧасти.НомерСтроки)+Символы.ПС+
			  "одиниця виміру загальна назва "  +СокрЛП(СтрокаТабличнойЧасти.ОбщееНазвание.Единица)+ "<>" + СокрЛП(СтрокаТабличнойЧасти.Единица); 
		  	 ОбщегоНазначенияКлиентСервер.СообщитьПользователю(СообщенияОбОшибке);
		  
		  КонецЕсли;
	
	КонецЦикла;
	
КонецПроцедуры
