import os
import base64

# Read the content from the user-provided exact specification
# Build it using the original Python content expression from the user
apo = chr(39)
emdash = chr(8212)

content = (
