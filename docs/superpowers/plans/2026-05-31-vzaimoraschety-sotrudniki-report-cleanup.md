# Чистка отчёта А_ВзаиморасчетыССотрудникамиДляБаланса — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** Убрать из отчёта лишнее — 4 авто-поля периодов в СКД + неверный синоним (обе копии) и 4 неприменимые процедуры в ObjectModule.bsl (config Report), сохранив поведение.

**Architecture:** Отчёт на `РегистрНакопления.А_ВзаиморасчетыССотрудниками.ОстаткиИОбороты`. Правки чисто декларативные (СКД XML) + удаление мёртвого/копипаст-кода (BSL). Все правки делает один идемпотентный Python-скрипт с ассертами (read utf-8-sig, сохранение BOM/переводов строк).

**Tech Stack:** 1С СКД (XML), BSL, Python 3 (regex), MCP `1c-workerp.execute_query`, скилы `/skd-validate`, `/1c-bsl-review`.

**Файлы:**
- Edit: `_Rarzrabotki/Отчеты/.../Templates/.../Ext/Template.xml` (внешний отчёт)
- Edit: `Reports/.../Templates/.../Ext/Template.xml` (config Report)
- Edit: `_Rarzrabotki/Отчеты/А_ВзаиморасчетыССотрудникамиДляБаланса.xml` (синоним)
- Edit: `Reports/А_ВзаиморасчетыССотрудникамиДляБаланса.xml` (синоним)
- Edit: `Reports/.../Ext/ObjectModule.bsl` (процедуры)
- Create: `_Rarzrabotki/Python/fix_vzs_report_cleanup.py` (фикс-скрипт)

---

### Task 1: Rule #-1 — проверить целевой запрос на реальной базе

- [ ] **Шаг 1.** Выполнить через `execute_query` (BaseERP) запрос с оставляемыми полями:
```
ВЫБРАТЬ Т.Организация, Т.ФормаPL, Т.Подразделение, Т.ФизическоеЛицо,
    Т.СуммаВзаиморасчетовНачальныйОстаток, Т.СуммаВзаиморасчетовПриход,
    Т.СуммаВзаиморасчетовРасход, Т.СуммаВзаиморасчетовКонечныйОстаток,
    Т.Регистратор, Т.ПериодСекунда
ИЗ РегистрНакопления.А_ВзаиморасчетыССотрудниками.ОстаткиИОбороты(, , Авто, , ) КАК Т
```
Ожидается: выполняется без ошибки (любое число строк).

### Task 2: Написать фикс-скрипт (dry-run, RED→GREEN)

- [ ] **Шаг 1.** Создать `_Rarzrabotki/Python/fix_vzs_report_cleanup.py` (transform-функции + ассерты; параметр `--apply`, по умолчанию dry-run).
- [ ] **Шаг 2.** Запустить БЕЗ `--apply`. Ожидается: все ассерты прошли, выведены before/after счётчики строк, в Template убраны 4 поля и 4 строки запроса (Секунда сохранена, запятая снята), синоним заменён, в ObjectModule отсутствуют `ПриКомпоновкеРезультата/УстановитьОбязательныеНастройки/СтруктураЗаголовковПолей/НастроитьПараметрыОтборыПоФункциональнымОпциям`, присутствуют `ОпределитьНастройкиФормы/НастроитьПараметрыОтборыПоУмолчанию`.

### Task 3: Применить правки

- [ ] **Шаг 1.** Запустить скрипт с `--apply`. Ожидается: 5 файлов перезаписаны, ассерты прошли.

### Task 4: Валидация

- [ ] **Шаг 1.** `/skd-validate` для обоих Template.xml — ошибок нет (учесть известный ложноположительный `~Список` — здесь не применим).
- [ ] **Шаг 2.** `/1c-bsl-review Reports/.../Ext/ObjectModule.bsl` — нет ошибок компиляции/висячих ссылок.
- [ ] **Шаг 3.** Грепом подтвердить отсутствие вызовов удалённых процедур во всём модуле.

### Task 5: Коммит

- [ ] **Шаг 1.** `git add` пять файлов отчёта + скрипт + обновлённую спеку + этот план; коммит на ветке `claude/bas-doc-skills`.

### Task 6: Приёмка

- [ ] **Шаг 1.** Открыть отчёт в 1С:Предприятие (или `/web-test`): формируется стандартной компоновкой, период = текущий год, данные по `ФормаPL → Подразделение → ФизическоеЛицо`.
- [ ] **Шаг 2.** Напомнить пользователю перезагрузить конфигурацию из файлов (`/db-load-xml -Mode Partial` + `/db-update -Dynamic+`) — Designer кеширует BSL.

## Self-Review
- Покрытие спеки: Часть 1 (СКД) → Task 2/3; Часть 2 (процедуры) → Task 2/3; проверки → Task 1/4/6. ✓
- Плейсхолдеров нет; запрос и имена процедур указаны явно. ✓
- `ПериодСекунда` сохраняется, запятая снимается — отражено в ассертах. ✓
