# BAS БСП «единое окно» — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Сделать так, чтобы скилы `bas-*` создавали `А_*` документы/справочники, неотличимые от типовых (командное меню + контракты БСП), и починить пример-документ `А_ПередачаНачисленийМеждуПодразделениями`.

**Architecture:** 2 новых скила (`bas-common-module` — организация кода #455/#469/#std551; `bas-bsp-form` — вербатим-контракты «единое окно» документа/справочника) + правки 5 существующих скилов + немедленный фикс формы примера. Всё грунтовано на БСП самой конфигурации (вербатим из `РеализацияТоваровУслуг`, `Контрагенты`, `Номенклатура`).

**Tech Stack:** Claude Code skills (Markdown SKILL.md + reference/), 1С BSL/Form.xml (BAS ERP 2.5), скилы `form-validate`/`meta-validate`/`db-load-xml`/`db-update`, Python COM (`V83.COMConnector`).

**Источник истины:** `docs/superpowers/specs/2026-05-30-bas-bsp-edinoe-okno-design.md` (закоммичен, `6f4231691`). Разделы §6/§7 содержат вербатим-контракты — субагенты читают их оттуда.

**Ветка/worktree:** работаем прямо в `claude/bas-doc-skills` без worktree — изменения идут в основную конфигурацию `C:\Configuration_downloads\BASERP25` (Конфигуратор грузит оттуда, Rule #4). Каждая задача = отдельный коммит.

**Что значит «тест» в этом домене:**
- Файлы скилов (Markdown) → приёмка «свежим агентом» (дать скил чистому субагенту + задачу, проверить, что вывод содержит контракт) + сверка содержимого со спекой.
- Фикс формы (BSL/XML) → `form-validate` (структура) + загрузка конфигурации + открытие формы в Enterprise (меню видно) + опц. Python COM smoke.

---

## Фаза 1 — Немедленный фикс формы `А_ПередачаНачисленийМеждуПодразделениями` (видимый proof)

Корень бага (агент-анализ): у `AutoCommandBar id="-1"` стоит `Autofill=false` **и панель пуста** → меню не рисуется; плюс лишний дубль `CommandBar id="1"`. Рабочий брат `А_ОказаниеУслугМеждуПодразделениями` имеет `<AutoCommandBar .../>` без `Autofill=false` и полное меню без кода. Минимальный надёжный фикс = вернуть автозаполнение.

### Task 1: Включить автозаполнение командной панели формы

**Files:**
- Modify: `Documents/А_ПередачаНачисленийМеждуПодразделениями/Forms/ФормаДокумента/Ext/Form.xml`

- [ ] **Step 1: Прочитать Form.xml и найти обе панели**

Run (Read tool): `Documents/А_ПередачаНачисленийМеждуПодразделениями/Forms/ФормаДокумента/Ext/Form.xml` (первые ~40 строк).
Ожидается увидеть:
```xml
<AutoCommandBar name="ФормаКоманднаяПанель" id="-1">
    <HorizontalAlign>Right</HorizontalAlign>
    <Autofill>false</Autofill>
</AutoCommandBar>
...
<ChildItems>
    <CommandBar name="ФормаКоманднаяПанель" id="1">
        <Autofill>true</Autofill>
    </CommandBar>
```

- [ ] **Step 2: Убрать `<Autofill>false</Autofill>` у `AutoCommandBar id="-1"`**

Edit: удалить строку `    <Autofill>false</Autofill>` внутри `<AutoCommandBar name="ФормаКоманднаяПанель" id="-1">` (после неё панель станет автозаполняемой по умолчанию).
Результат:
```xml
<AutoCommandBar name="ФормаКоманднаяПанель" id="-1">
    <HorizontalAlign>Right</HorizontalAlign>
</AutoCommandBar>
```

- [ ] **Step 3: Удалить дубль-панель `CommandBar id="1"` из `ChildItems`**

Edit: удалить целиком элемент
```xml
<CommandBar name="ФормаКоманднаяПанель" id="1">
    <Autofill>true</Autofill>
</CommandBar>
```
(дубликат имени `ФормаКоманднаяПанель` — артефакт конструктора; пустая полоса в теле формы). Остальные элементы `ChildItems` (ГруппаШапка и т.д.) не трогать.

- [ ] **Step 4: Структурная валидация формы**

Run: skill `form-validate` на `Documents/А_ПередачаНачисленийМеждуПодразделениями/Forms/ФормаДокумента/Ext/Form.xml`.
Expected: без ошибок (уникальность id, ссылки команд, компаньоны).

- [ ] **Step 5: Commit**

```bash
git add "Documents/А_ПередачаНачисленийМеждуПодразделениями/Forms/ФормаДокумента/Ext/Form.xml"
git commit -m "fix(А_ПередачаНачислений): автозаполнение командной панели формы — возвращает типовое меню"
```

### Task 2: Загрузить конфигурацию и подтвердить меню

**Files:** (нет правок — проверка)

- [ ] **Step 1: Частичная загрузка изменённой формы в базу**

Run: skill `db-load-xml -Mode Partial` для документа `А_ПередачаНачисленийМеждуПодразделениями` (авто `-updateConfigDumpInfo`), затем `db-update -Dynamic+`.
Expected: загрузка без ошибок.

- [ ] **Step 2: Подтверждение пользователем (чекпоинт)**

Попросить пользователя открыть документ `А_ПередачаНачисленийМеждуПодразделениями` в 1С Enterprise и подтвердить: видны «Провести и закрыть», «Записать», «Провести», «Создать на основании», «Ещё».
Expected: меню как у типового документа. Если нет — вернуться к Task 1, проверить, что `Autofill=false` точно удалён и форма перезагружена (сбросить кеш Конфигуратора через `ConfigDumpInfo.xml`).

---

## Фаза 2 — Скил `bas-bsp-form` (контракты «единое окно»)

Это ядро. Reference-файлы = вербатим-контракты из спеки §6 (документ) и §7 (справочник).

### Task 3: Создать `bas-bsp-form/reference/document-contract.md`

**Files:**
- Create: `.claude/skills/bas-bsp-form/reference/document-contract.md`

- [ ] **Step 1: Прочитать источник**

Read: `docs/superpowers/specs/2026-05-30-bas-bsp-edinoe-okno-design.md` — раздел **§6** целиком.

- [ ] **Step 2: Записать reference-файл документа**

Содержимое = §6 спеки, оформленное как самостоятельный справочник: заголовок «Контракт «единое окно» — документ (эталон РеализацияТоваровУслуг)», подразделы 6.1 `ПриСозданииНаСервере` (порядок вызовов вербатим), 6.2 таблица асимметрии жизненного цикла, 6.3 обязательные обработчики (вербатим-делегаты + примечание о полном наборе ПодключаемыеКоманды), 6.4 контракт-методы менеджера. Все блоки кода — дословно как в §6. В конце — ссылка на эталон: `Documents/РеализацияТоваровУслуг/Forms/ФормаДокумента/Ext/Form/Module.bsl`.

- [ ] **Step 3: Commit**

```bash
git add ".claude/skills/bas-bsp-form/reference/document-contract.md"
git commit -m "feat(bas-bsp-form): reference контракта документа (вербатим РеализацияТоваровУслуг)"
```

### Task 4: Создать `bas-bsp-form/reference/catalog-contract.md`

**Files:**
- Create: `.claude/skills/bas-bsp-form/reference/catalog-contract.md`

- [ ] **Step 1: Прочитать источник**

Read: спека §7 целиком.

- [ ] **Step 2: Записать reference-файл справочника**

Содержимое = §7 спеки: заголовок «Контракт «единое окно» — справочник (эталон Контрагенты/Номенклатура)», подразделы 7.1 `ПриСозданииНаСервере` (порядок: Версионирование → Свойства → КИ → ПодключаемыеКоманды → СобытияФорм, вербатим), 7.2 жизненный цикл, 7.3 обработчики (Свойства 4 + ПодключаемыеКоманды 3 + КонтактнаяИнформация 8) с примечанием о выборе одного паттерна, 7.4 менеджер (`ПриОпределенииНастроекВерсионированияОбъектов` + `ПолучитьБлокируемыеРеквизитыОбъекта`), 7.5 что предопределяется в XML (пустые `ГруппаДополнительныеРеквизиты`/`ГруппаКонтактнаяИнформация` + реквизит `ПараметрыПодключаемыхКоманд` с `<Type/>`).

- [ ] **Step 3: Commit**

```bash
git add ".claude/skills/bas-bsp-form/reference/catalog-contract.md"
git commit -m "feat(bas-bsp-form): reference контракта справочника (вербатим Контрагенты/Номенклатура)"
```

### Task 5: Создать `bas-bsp-form/SKILL.md`

**Files:**
- Create: `.claude/skills/bas-bsp-form/SKILL.md`

- [ ] **Step 1: Записать SKILL.md**

Frontmatter (вербатим):
```markdown
---
name: bas-bsp-form
description: >-
  Используй когда создаёшь или чинишь управляемую форму документа/справочника
  BAS ERP 2.5 и нужно, чтобы у объекта было типовое «единое окно» — командное
  меню (Провести и закрыть, Создать на основании, Печать, Отчёты, Заполнить, Ещё)
  и стандартные подсистемы БСП (Свойства, Версионирование, Подключаемые команды,
  Файлы, Контактная информация). Применяй при «нет меню на форме», «как у типовых
  документов», «пустая командная панель», «подключить БСП к форме».
---
```
Тело: §«Когда использовать», §«Главный принцип» (контракты БСП дают единое окно; грунт на эталоны этой конфигурации; видимость #std551 — звать только Программный интерфейс БСП), §«Командная панель» (рекомендация: autofill + явные вызовы; антипаттерн `Autofill=false`+пусто), §«Документ → [reference/document-contract.md]», §«Справочник → [reference/catalog-contract.md]», §«Обязательный минимум» (чек-лист: вызовы в ПриСозданииНаСервере + парные обработчики жизненного цикла + `Подключаемый_*` методы + менеджер-контракты + XML-контейнеры), §«Anti-patterns» (`Autofill=false`+пусто; вызов подсистемы без её парных обработчиков → ошибка; ручные кнопки вместо автозаполнения без нужды; вызов Служебного интерфейса БСП).

- [ ] **Step 2: Проверить упоминание в bas-house-style (отложено в Task 11) — пометить зависимость**

(Связь скила в house-style делается в Task 11; здесь только отметить в теле SKILL ссылку «читай ПЕРЕД: bas-house-style».)

- [ ] **Step 3: Commit**

```bash
git add ".claude/skills/bas-bsp-form/SKILL.md"
git commit -m "feat(bas-bsp-form): скил контрактов «единое окно» для форм документов/справочников"
```

### Task 6: Приёмка `bas-bsp-form` свежим агентом

**Files:** (проверка)

- [ ] **Step 1: Запустить свежего субагента**

Дать субагенту (Explore/general-purpose) БЕЗ контекста только содержимое `.claude/skills/bas-bsp-form/` и задачу: «По этому скилу опиши, какой код нужно вставить в форму нового документа `А_Тест`, чтобы у него было типовое меню и подсистемы БСП». 
Expected: субагент воспроизводит порядок вызовов §6 (Свойства→Версионирование→ПодключаемыеКоманды→СобытияФорм→Файлы) + парные обработчики, не выдумывая имён. Если буксует — дописать в SKILL недостающее, повторить.

- [ ] **Step 2: Commit (если правки)**

```bash
git add ".claude/skills/bas-bsp-form/"
git commit -m "fix(bas-bsp-form): уточнения по приёмке свежим агентом"
```

---

## Фаза 3 — Скил `bas-common-module` (организация кода)

### Task 7: Создать `bas-common-module/reference/module-structure.md`

**Files:**
- Create: `.claude/skills/bas-common-module/reference/module-structure.md`

- [ ] **Step 1: Записать reference**

Содержимое: §«Регионы #455» (общий модуль: ПрограммныйИнтерфейс/СлужебныйПрограммныйИнтерфейс/СлужебныеПроцедурыИФункции; модуль объекта/менеджера: ОписаниеПеременных/ПрограммныйИнтерфейс/ОбработчикиСобытий/СлужебныйПрограммныйИнтерфейс/СлужебныеПроцедурыИФункции/Инициализация — с примерами-болванками BSL), §«Нейминг/флаги #469» (таблица суффиксов Сервер/Клиент/КлиентСервер/ВызовСервера; запрет `#Если Клиент`-сплита), §«Видимость #std551» (наш экспорт для формы/менеджера/объекта — в ПрограммныйИнтерфейс; БСП звать только из её Программного интерфейса), §«Делегирование» (тонкий модуль объекта/менеджера → `А_<Объект>Сервер`; пример: ОбработкаПроведения делегирует в ПроведениеДокументов, ДанныеДокументаДляПроведения форвардит в `А_<Объект>Сервер`). Источники — стандарты #455/#469/#551.

- [ ] **Step 2: Commit**

```bash
git add ".claude/skills/bas-common-module/reference/module-structure.md"
git commit -m "feat(bas-common-module): reference структуры модулей (#455/#469/#std551)"
```

### Task 8: Создать `bas-common-module/SKILL.md`

**Files:**
- Create: `.claude/skills/bas-common-module/SKILL.md`

- [ ] **Step 1: Записать SKILL.md**

Frontmatter (вербатим):
```markdown
---
name: bas-common-module
description: >-
  Используй когда создаёшь документ/справочник/логику в BAS ERP 2.5 и нужно
  разложить код по БСП-стилю: на каждый объект — общий модуль А_<Объект>Сервер
  (+ Клиент/КлиентСервер по нужде), тонкие модули объекта/менеджера/формы
  делегируют в него. Регионы #455, нейминг #469, области видимости #std551.
  Применяй при «общий модуль в БСП-стиле», «куда положить логику документа»,
  «делегирование», «тонкий модуль объекта».
---
```
Тело: §«Принцип» (логика объекта живёт в `А_<Объект>Сервер`, не в модуле объекта/формы; неотличимо от типового), §«Топология» (модуль на объект; когда заводить Клиент/КлиентСервер), §«Структура → [reference/module-structure.md]», §«Делегирование документа» (ОбработкаПроведения → ПроведениеДокументов; остальное + запрос движений → `А_<Объект>Сервер`; менеджер = форвардер), §«Видимость #std551» (только Программный интерфейс БСП, модуль нужного контекста), §«Anti-patterns» (логика inline в модуле объекта/формы; `#Если Клиент`-сплит; модуль-«бог» на все объекты; вызов Служебного интерфейса БСП).

- [ ] **Step 2: Commit**

```bash
git add ".claude/skills/bas-common-module/SKILL.md"
git commit -m "feat(bas-common-module): скил организации кода в БСП-стиле (модуль на объект)"
```

### Task 9: Приёмка `bas-common-module` свежим агентом

**Files:** (проверка)

- [ ] **Step 1: Свежий субагент**

Дать субагенту только `.claude/skills/bas-common-module/` + задачу: «Как разложить логику нового документа `А_Тест` по модулям в БСП-стиле?»
Expected: предлагает `А_ТестСервер` с регионами #455, тонкий модуль объекта делегирует, проведение через ПроведениеДокументов. Буксует → дописать, повторить.

- [ ] **Step 2: Commit (если правки)**

```bash
git add ".claude/skills/bas-common-module/"
git commit -m "fix(bas-common-module): уточнения по приёмке свежим агентом"
```

---

## Фаза 4 — Правки 5 существующих скилов

### Task 10: Правка `bas-doc-new/SKILL.md`

**Files:**
- Modify: `.claude/skills/bas-doc-new/SKILL.md`

- [ ] **Step 1: Прочитать текущий SKILL.md** (Read).

- [ ] **Step 2: Внести правки**
  - В «REQUIRED BACKGROUND» добавить: `bas-common-module` (организация кода) и `bas-bsp-form` (контракты единого окна) — читать ПЕРЕД.
  - В шаг 4 (Создай формы): добавить «вшей контракт «единое окно» документа по `bas-bsp-form` (reference/document-contract.md): autofill-панель + вызовы подсистем в ПриСозданииНаСервере + парные обработчики».
  - В шаг 5 (модуль объекта): прикладную логику размещать в `А_<Объект>Сервер` (по `bas-common-module`), модуль объекта — тонкий; убрать строку-опцию `// <Документ>Локализация ... // если нужна`, заменить на обязательное делегирование в `А_<Объект>Сервер`.
  - В чек-лист и Anti-patterns добавить пункты про меню/контракты/`А_<Объект>Сервер`.

- [ ] **Step 3: Commit**

```bash
git add ".claude/skills/bas-doc-new/SKILL.md"
git commit -m "feat(bas-doc-new): обязательные А_<Объект>Сервер + контракт единого окна"
```

### Task 11: Правка `bas-catalog-new/SKILL.md`

**Files:**
- Modify: `.claude/skills/bas-catalog-new/SKILL.md`

- [ ] **Step 1: Read.**
- [ ] **Step 2: Правки**
  - REQUIRED BACKGROUND: + `bas-common-module`, `bas-bsp-form`.
  - Шаг 4 (формы): вшить контракт справочника по `bas-bsp-form/reference/catalog-contract.md` — включая `УправлениеКонтактнойИнформацией`, пустые `ГруппаДополнительныеРеквизиты`/`ГруппаКонтактнаяИнформация`, реквизит `ПараметрыПодключаемыхКоманд`.
  - Шаг 5/6 (модули): логика в `А_<Объект>Сервер`; менеджер — контракт-методы (`ПриОпределенииНастроекВерсионированияОбъектов`, `ПолучитьБлокируемыеРеквизитыОбъекта` при запрете редактирования + вызов `ЗапретРедактированияРеквизитовОбъектов.ЗаблокироватьРеквизиты` в форме).
  - Чек-лист/Anti-patterns: меню/контракты/КИ.

- [ ] **Step 3: Commit**

```bash
git add ".claude/skills/bas-catalog-new/SKILL.md"
git commit -m "feat(bas-catalog-new): контракт единого окна справочника + А_<Объект>Сервер"
```

### Task 12: Правка `bas-house-style/SKILL.md`

**Files:**
- Modify: `.claude/skills/bas-house-style/SKILL.md`

- [ ] **Step 1: Read.**
- [ ] **Step 2: Правки**
  - В таблицу/раздел reference добавить ссылки на `bas-common-module` и `bas-bsp-form` как обязательные к прочтению ПЕРЕД bas-doc-new/bas-catalog-new.
  - В «Главный принцип»/«Обязательно всегда» добавить пункт: интеграция подсистем БСП «единое окно» (контракты) + области видимости #std551.
  - Убрать опциональность `<Документ>Локализация // если нужна` (заменить на делегирование в `А_<Объект>Сервер`).

- [ ] **Step 3: Commit**

```bash
git add ".claude/skills/bas-house-style/SKILL.md"
git commit -m "feat(bas-house-style): БСП-контракты + видимость #std551 + ссылки на новые скилы"
```

### Task 13: Правка `bas-register-new/SKILL.md`

**Files:**
- Modify: `.claude/skills/bas-register-new/SKILL.md`

- [ ] **Step 1: Read.**
- [ ] **Step 2: Правки** — в разделе C (привязать движения) уточнить: запрос движений (`ДанныеДокументаДляПроведения`) живёт в `А_<Объект>Сервер`, модуль менеджера форвардит; ссылка на `bas-common-module`.
- [ ] **Step 3: Commit**

```bash
git add ".claude/skills/bas-register-new/SKILL.md"
git commit -m "feat(bas-register-new): запрос движений в А_<Объект>Сервер (форвард из менеджера)"
```

### Task 14: Правка `bas-verify/SKILL.md`

**Files:**
- Modify: `.claude/skills/bas-verify/SKILL.md`

- [ ] **Step 1: Read.**
- [ ] **Step 2: Добавить пункты гейта (из спеки §10):**
  - Командная панель не пустая (нет связки `Autofill=false`+пустой `AutoCommandBar`).
  - Вшиты контракты §6/§7 (вызовы ПриСозданииНаСервере + парные обработчики + `Подключаемый_*`).
  - Регионы #455 / нейминг #469 у `А_<Объект>Сервер` и тонких модулей.
  - Видимость #std551 (БСП только из Программного интерфейса, верный контекст).
  - Менеджер: контракт-методы версионирования/блокировки.
- [ ] **Step 3: Commit**

```bash
git add ".claude/skills/bas-verify/SKILL.md"
git commit -m "feat(bas-verify): гейт меню/контрактов БСП/регионов/видимости"
```

---

## Фаза 5 — Сквозная проверка

### Task 15: Приёмка end-to-end свежим агентом

**Files:** (проверка)

- [ ] **Step 1: Свежий субагент создаёт документ по скилам**

Дать чистому субагенту скилы `bas-house-style`, `bas-common-module`, `bas-bsp-form`, `bas-doc-new` + задачу: «Создай новый документ `А_ТестЕдиноеОкно` в типовом стиле (только опиши состав файлов и ключевой код формы/модулей, без записи в базу)».
Expected: субагент выдаёт форму с autofill-панелью + контракт §6 (вызовы + обработчики), `А_ТестЕдиноеОкноСервер` с регионами #455, тонкий модуль объекта, менеджер-форвардер. Нет пустого меню, нет inline-логики в модуле объекта.

- [ ] **Step 2: Зафиксировать пробелы**

Если субагент что-то упустил — определить, какого скила/раздела не хватило, дописать, перезапустить приёмку.

- [ ] **Step 3: Commit (если правки)**

```bash
git add ".claude/skills/"
git commit -m "fix(bas-skills): доводка по сквозной приёмке"
```

### Task 16: Финальная проверка и сводка

**Files:** (проверка)

- [ ] **Step 1: Проверить, что Фаза 1 жива** — форма `А_ПередачаНачисленийМеждуПодразделениями` всё ещё с меню (пользователь подтвердил в Task 2).
- [ ] **Step 2: `git log --oneline`** — убедиться, что все задачи закоммичены отдельными коммитами на `claude/bas-doc-skills`.
- [ ] **Step 3: Сводка пользователю** — что сделано (2 скила + 5 правок + фикс), как проверять, что вне scope (доступ/блокировки, ретрофит). Спросить про финал ветки (merge/push/PR) — по `feedback_autonomous_finish_merge_push_pr` делать автономно при завершении.

---

## Self-Review (выполнено при написании плана)

- **Покрытие спеки:** §5.1→Task 7-9; §5.2→Task 3-6; §6→Task 3; §7→Task 4; §8→Task 1; §9→Task 1-2; §10→Task 14; правки 5 скилов §5.3→Task 10-14. ✓
- **Плейсхолдеры:** контент reference-файлов берётся из закоммиченной спеки §6/§7 (источник истины), субагент читает и переносит — это не TBD, а явная ссылка на готовый вербатим. Фикс формы — точные шаги Read→Edit. ✓
- **Согласованность имён:** `А_<Объект>Сервер`, `ПодключаемыеКоманды.ПриСозданииНаСервере`, `ПриОпределенииНастроекВерсионированияОбъектов`, группы `ГруппаДополнительныеРеквизиты`/`ГруппаКонтактнаяИнформация`, реквизит `ПараметрыПодключаемыхКоманд` — единообразны между задачами и спекой. ✓
