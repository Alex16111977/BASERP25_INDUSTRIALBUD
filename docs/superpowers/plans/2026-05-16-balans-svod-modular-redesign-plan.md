# Модульная логика проведения А_ФинРез_Баланс → А_ОтчетБаланс_Свод (по регистру-источнику, со сверкой ПАП) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Заменить монолитный `СформироватьЗапросСверткиБаланса` в `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl` на модульную логику (оркестратор + per-источник функции `Свод_*` + сверка с ПАП), зеркаля штатную `Обработка.ДвиженияАктивовПассивов`, с acceptance через штатный `Отчет.УправленческийБаланс`.

**Architecture:** ПАП (`РегистрНакопления.ПрочиеАктивыПассивы`) деривирован штатной обработкой из регистров-источников per `Регистратор`; каждый источник = реквизит `ПАП.Источник` (`Перечисление.ИсточникиУправленческогоБаланса`). Документ читает ОСНОВНУЮ таблицу ПАП (не виртуальную — `Источник`/`ВидИсточника` это реквизиты), помодульно строит расшифровку по источнику, сверяет Σ с `ПАП.ОстаткиИОбороты` per (Орг,Подр,Статья): расшифровка>ПАП → пропорц.обрезка, ПАП>расшифровки → строка-добивка пустая аналитика; обе → `Расхождение=Истина`. Σ по Статья == ПАП всегда (инвариант).

**Tech Stack:** 1С:Предприятие 8.3.20 (BAS ERP 2.5), BSL, Python 3 + win32com (`V83.COMConnector`), pytest-стиль assert-скрипты, MCP `1c-workerp` (get_metadata_structure), skills `/cf-validate` `/db-load-xml` `/db-update`.

---

## Контекст и жёсткие правила (прочитать ПЕРЕД Task 0)

- **Спека (источник истины решений DR-1…DR-13):** `C:\Configuration_downloads\BASERP25\docs\superpowers\specs\2026-05-16-balans-svod-modular-redesign-design.md`. Канон-инварианты (OD-3/OD-8/Σ==ПАП): `docs/superpowers/specs/2026-05-15-balans-svod-canonical-design.md`. База знаний: `_Rarzrabotki/notebook/knowledge_Balanse/`.
- **Worktree удалён.** Прошлый worktree `peaceful-knuth-51295b` убран при закрытии прошлой ветки. Работаем **напрямую в main config** `C:\Configuration_downloads\BASERP25\`, ветка `main`. Все пути — без `.claude\worktrees`. Правки .bsl делаются сразу в main config (Rule #4 «cp в main» = тривиально, уже там); после правки — `/cf-validate` → `/db-load-xml` → `/db-update`.
- **Rule #-1 (CLAUDE.md, КРИТИЧНО):** ни одного запроса 1С в BSL без предварительного Python COM теста. Каждая `Свод_*` и `СверкаСПАП` имеет свой Python пре-тест ПЕРЕД написанием BSL.
- **Connection (Python COM):** `Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"`. Шаблон — ниже (Task 1).
- **TZ-safe:** даты в Python — опівдні (`datetime(2026,1,1,12,0,0)`); границы периода в запросах считать СЕРВЕРНО (`НАЧАЛОПЕРИОДА(&М,МЕСЯЦ)`, `КОНЕЦПЕРИОДА(КОНЕЦПЕРИОДА(&М,МЕСЯЦ),ДЕНЬ)`), не Python-наивно.
- **Организация-эталон:** ТОВ ІНДАСТРІАЛБУД, `КодПоЕДРПОУ = "40645273"`. Период валидации — **январь 2026** (закрытый).
- **DR-12: типовые объекты ТОЛЬКО чтение.** `ПрочиеАктивыПассивы`, `РасчетыС*ПоСрокам`, `СебестоимостьТоваров`, `ДенежныеСредства*`, `ВзаиморасчетыССотрудниками`, `ДвиженияАктивовПассивов` — только SELECT/.ОстаткиИОбороты. Структуру регистра `А_ОтчетБаланс_Свод`/enum правит **финансист вручную** (Task 3 — список для него); план НЕ меняет метаданные сам.
- **БД блокирует Конфигуратор** при `/db-load-xml`/`/db-update` → STOP, попросить пользователя закрыть Конфигуратор, **не обходить**.
- **Эталоны для зеркалирования (прочитать соответствующую часть в нужной задаче):**
  - `_Rarzrabotki/ERP_Pyramida/Documents/А_Баланс/Ext/ObjectModule.bsl` — `ОбработкаПроведения`(600), `ПровестиБалансУпр`(627), `Отразить_А_БалансУпр`(616), `Упр_ПрочиеАктивыПассивы`(968), `Упр_РасчетыСКлиентами`(1446), `Упр_РасчетыСПоставщиками`(1150), `Упр_СебестоимостьТоваров`(2047).
  - `DataProcessors/ДвиженияАктивовПассивов/Ext/ManagerModule.bsl` — `ОтразитьДокументыВУправленческомБалансе`(275), `БалансовыеРегистры`(314), `ДобавитьТекстЗапросаРасчетыСПартнерами`(964), `ДобавитьТекстЗапросаВременныхТаблицКонтроляБаланса`(3286), фильтр `ВидИсточника`(~162-166).
  - Текущий код для замены: `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl` (монолитный `СформироватьЗапросСверткиБаланса`).

---

## File Structure

| Файл | Ответственность | Действие |
|---|---|---|
| `_Rarzrabotki/Python/test/test_balans_m_discovery.py` | Discovery: РАЗЛИЧНЫЕ Источник + набор ВидИсточника (Σ==ПАП.ОстаткиИОбороты) | Create |
| `_Rarzrabotki/Python/test/test_balans_m_uprreport.py` | Discovery: как программно гонять Отчет.УправленческийБаланс (СКД) + извлечь по Статья | Create |
| `_Rarzrabotki/Python/test/test_balans_m_src_raschety.py` | Пре-тест запроса Свод_РасчетыСПартнерами | Create |
| `_Rarzrabotki/Python/test/test_balans_m_src_sebest.py` | Пре-тест Свод_СебестоимостьТоваров | Create |
| `_Rarzrabotki/Python/test/test_balans_m_src_dengi.py` | Пре-тест Свод_ДенежныеСредства | Create |
| `_Rarzrabotki/Python/test/test_balans_m_src_prochie.py` | Пре-тест Свод_ПрочиеРасходыДоходы | Create |
| `_Rarzrabotki/Python/test/test_balans_m_src_pap.py` | Пре-тест Свод_ПрочиеАктивыПассивы_Прямой (Источник="") | Create |
| `_Rarzrabotki/Python/test/test_balans_m_src_ot.py` | Пре-тест Свод_ОплатаТруда (разнос по ФизЛицо) | Create |
| `_Rarzrabotki/Python/test/test_balans_m_sverka.py` | Пре-тест СверкаСПАП (обрезка/добивка/Расхождение, инвариант) | Create |
| `_Rarzrabotki/Python/test/test_balans_m_post.py` | Test проведения (Проведен, idempotent) | Create |
| `_Rarzrabotki/Python/test/test_balans_vs_upr_balance_report.py` | ОСНОВНОЙ acceptance: Σ Свод по Статья == Отчет.УправленческийБаланс до копейки | Create |
| `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl` | Модульная логика: оркестратор + Свод_* + СверкаСПАП + хелперы | Rewrite |
| `_Rarzrabotki/notebook/knowledge_Balanse/balanse_provedenie_logic.md` / `balanse_mapping_actual_vs_planned.md` / `KNOWLEDGE_MAP_BALANSE.md` | Обновить под новую модульную логику | Modify (финальная задача) |

**Изменения структуры регистра/enum (Task 3) делает финансист вручную — НЕ файлы плана.**

---

## Python COM шаблон (используется во ВСЕХ test_balans_m_*.py)

```python
# -*- coding: utf-8 -*-
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime

pythoncom.CoInitialize()
V8 = win32com.client.Dispatch("V83.COMConnector")
ERP = V8.Connect('Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"')

def org_ref(conn):
    q = conn.NewObject("Запрос")
    q.Текст = ('ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Справочник.Организации '
               'ГДЕ КодПоЕДРПОУ = "40645273"')
    s = q.Выполнить().Выбрать(); s.Следующий(); return s.С

def excinfo(e):
    return e.excepinfo[2] if hasattr(e, "excepinfo") and e.excepinfo else str(e)
```

Запуск любого теста: `python "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\<имя>.py"`. Все тесты печатают `PASS <имя>` или `FAIL: <причина>` и `sys.exit(1)` при провале.

---

## Task 0: Передусловия — перечитать структуру + git baseline

**Files:** (нет правок; верификация)

- [ ] **Step 1: Перечитать структуру регистра-приёмника из БД**

Через MCP: `get_metadata_structure InformationRegisters А_ОтчетБаланс_Свод`.
Expected: 17+ измерений включая `Source`, `ДокументДвижения` (ДокументСсылка), 4 ресурса. Зафиксировать актуальный список измерений (финансист мог менять). Если `Source` уже перетипизирован / `Расхождение` уже есть — отметить (Task 3 частично выполнен).

- [ ] **Step 2: Перечитать структуру источника**

Через MCP: `get_metadata_structure AccumulationRegisters ПрочиеАктивыПассивы`.
Expected: измерения (Организация, Подразделение, НаправлениеДеятельности, Статья, Аналитика), ресурс `Сумма`, реквизиты `ВидИсточника` (Перечисление.ВидыИсточниковДвижений), `Источник` (Перечисление.ИсточникиУправленческогоБаланса), `РасчетСебестоимости`. Подтвердить что `Источник`/`ВидИсточника` — реквизиты (не измерения).

- [ ] **Step 3: Git baseline**

Run: `cd "C:/Configuration_downloads/BASERP25" && git branch --show-current && git status --porcelain "Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl"`
Expected: ветка `main`; зафиксировать текущее состояние ObjectModule.bsl (модифицирован/чист). Сделать резервную копию текущего монолита для отката:
Run: `cp "C:/Configuration_downloads/BASERP25/Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test/_backup_objectmodule_balans_pre_modular.bsl"`
Expected: файл-бэкап создан (для отката при регрессе).

---

## Task 1: Discovery — перечень Источников + набор ВидИсточника (Rule #-1, источник истины)

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_m_discovery.py`

- [ ] **Step 1: Написать discovery-скрипт**

```python
# -*- coding: utf-8 -*-
"""Task1 discovery: перечень Источник + набор ВидИсточника дающий
Σ == ПАП.ОстаткиИОбороты по статьям источника (январь/ТОВ). Источник истины
для всех Свод_* задач (как _Fld discovery канона v1.4)."""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime
pythoncom.CoInitialize()
ERP = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"')

q0 = ERP.NewObject("Запрос")
q0.Текст = ('ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Справочник.Организации '
            'ГДЕ КодПоЕДРПОУ = "40645273"')
s = q0.Выполнить().Выбрать(); s.Следующий(); ORG = s.С
qm = ERP.NewObject("Запрос")
qm.Текст = ("ВЫБРАТЬ НАЧАЛОПЕРИОДА(&М,МЕСЯЦ) КАК НМ, "
            "КОНЕЦПЕРИОДА(КОНЕЦПЕРИОДА(&М,МЕСЯЦ),ДЕНЬ) КАК КД")
qm.УстановитьПараметр("М", datetime(2026, 1, 1, 12, 0, 0))
rm = qm.Выполнить().Выбрать(); rm.Следующий(); НМ, КД = rm.НМ, rm.КД

# (a) РАЗЛИЧНЫЕ Источник + ВидИсточника (основная таблица, январь+декабрь)
q1 = ERP.NewObject("Запрос")
q1.Текст = """ВЫБРАТЬ
 ПРЕДСТАВЛЕНИЕ(Т.Источник) КАК Источник, ПРЕДСТАВЛЕНИЕ(Т.ВидИсточника) КАК ВидИст,
 КОЛИЧЕСТВО(РАЗЛИЧНЫЕ Т.Статья) КАК КолСтатей,
 СУММА(ВЫБОР КОГДА Т.ВидДвижения=ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
   ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ) КАК Оборот
ИЗ РегистрНакопления.ПрочиеАктивыПассивы КАК Т
ГДЕ Т.Организация=&Орг И Т.Период МЕЖДУ ДОБАВИТЬКДАТЕ(&НМ,МЕСЯЦ,-1) И &КД
СГРУППИРОВАТЬ ПО ПРЕДСТАВЛЕНИЕ(Т.Источник), ПРЕДСТАВЛЕНИЕ(Т.ВидИсточника)
УПОРЯДОЧИТЬ ПО Источник, ВидИст"""
q1.УстановитьПараметр("Орг", ORG); q1.УстановитьПараметр("НМ", НМ)
q1.УстановитьПараметр("КД", КД)
t1 = q1.Выполнить().Выгрузить()
print("=== (a) Источник x ВидИсточника ===")
for i in range(t1.Количество()):
    r = t1.Получить(i)
    print(f"  Источник='{r.Источник}' | ВидИст='{r.ВидИст}' | "
          f"статей={int(r.КолСтатей)} | оборот={float(r.Оборот or 0):,.2f}")

# (b) Эталон ПАП.ОстаткиИОбороты по статьям (январь)
q2 = ERP.NewObject("Запрос")
q2.Текст = """ВЫБРАТЬ ПРЕДСТАВЛЕНИЕ(Б.Статья) КАК Ст,
 СУММА(Б.СуммаКонечныйОстаток) КАК sK
ИЗ РегистрНакопления.ПрочиеАктивыПассивы.ОстаткиИОбороты(&Д1,&Д2,Авто,,
 Организация=&Орг) КАК Б
СГРУППИРОВАТЬ ПО ПРЕДСТАВЛЕНИЕ(Б.Статья)
ИМЕЮЩИЕ СУММА(Б.СуммаКонечныйОстаток)<>0"""
q2.УстановитьПараметр("Д1", НМ); q2.УстановитьПараметр("Д2", КД)
q2.УстановитьПараметр("Орг", ORG)
t2 = q2.Выполнить().Выгрузить()
etalon = {str(t2.Получить(i).Ст): float(t2.Получить(i).sK or 0)
          for i in range(t2.Количество())}

# (c) КонОст из ОСНОВНОЙ таблицы по статьям (Σ signed весь период до КД)
#     набор ВидИсточника = ВСЕ (берём все слои) — проверяем == эталон
q3 = ERP.NewObject("Запрос")
q3.Текст = """ВЫБРАТЬ ПРЕДСТАВЛЕНИЕ(Т.Статья) КАК Ст,
 СУММА(ВЫБОР КОГДА Т.ВидДвижения=ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
   ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ) КАК sK
ИЗ РегистрНакопления.ПрочиеАктивыПассивы КАК Т
ГДЕ Т.Организация=&Орг И Т.Период <= &КД
СГРУППИРОВАТЬ ПО ПРЕДСТАВЛЕНИЕ(Т.Статья)"""
q3.УстановитьПараметр("Орг", ORG); q3.УстановитьПараметр("КД", КД)
t3 = q3.Выполнить().Выгрузить()
osnov = {str(t3.Получить(i).Ст): float(t3.Получить(i).sK or 0)
         for i in range(t3.Количество())}

print("\n=== (c) sверка ОСНОВНАЯ(все ВидИсточника) КонОст vs ПАП.ОстаткиИОбороты ===")
bad = []
for k in set(etalon) | set(osnov):
    d = abs(etalon.get(k, 0) - osnov.get(k, 0))
    if d > 0.01:
        bad.append((k, etalon.get(k, 0), osnov.get(k, 0), d))
for k, a, b, dd in bad[:15]:
    print(f"  ДЕЛЬТА {k[:40]}: ОстИОб={a:,.2f} основн={b:,.2f} Δ={dd:,.4f}")
print(f"\nстатей расхождений (все слои): {len(bad)} "
      f"(0 → НачОст/КонОст из основной таблицы по всем ВидИсточника == ПАП)")
print("\nИтог Task1: список Источник (a) → перечень Свод_* процедур; "
      "если len(bad)=0 → набор ВидИсточника = ВСЕ слои (DR-7 решён); "
      "если >0 → подобрать фильтр ВидИсточника (исключить РасчетБаланса/отложенный) "
      "и повторить (c) до len(bad)=0.")
print("PASS test_balans_m_discovery (см. вывод — это вход для Task 4+)")
```

- [ ] **Step 2: Запустить discovery**

Run: `python "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_balans_m_discovery.py"`
Expected: блок (a) — перечень `Источник` (ожид.: «Расчеты с клиентами по срокам», «Расчеты с поставщиками по срокам», «Себестоимость товаров», «Денежные средства (безналичные/наличные/в пути/у подотчетных лиц)», «Прочие расходы», «Прочие доходы», «Партии прочих расходов», `Источник=''`); блок (c) — `статей расхождений (все слои): 0`. **Зафиксировать вывод (a) и решение DR-7 (набор ВидИсточника) — это вход для всех Свод_* задач.** Если (c) ≠ 0 — задокументировать набор ВидИсточника, при котором =0 (исключить `РасчетБаланса`/отложенный).

- [ ] **Step 3: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25"
git add _Rarzrabotki/Python/test/test_balans_m_discovery.py
git commit -m "test(balans): Task1 discovery — перечень Источник + набор ВидИсточника (Rule #-1)"
```

---

## Task 2: Discovery — программный запуск штатного `Отчет.УправленческийБаланс` (acceptance-механизм)

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_m_uprreport.py`

- [ ] **Step 1: Изучить отчёт**

Через MCP: `get_metadata_structure Reports УправленческийБаланс`. Прочитать `Reports/УправленческийБаланс/Ext/ObjectModule.bsl` (если есть) и схему СКД (`Templates`/`ОсновнаяСхемаКомпоновкиДанных`) — выяснить имя основной СКД-схемы, параметры (`НачалоПериода`/`КонецПериода`/`Организация`), поле статьи в результате.
Expected: зафиксировать имя схемы (обычно `ОсновнаяСхемаКомпоновкиДанных`), имена параметров периода/организации, поле `Статья` и ресурс `СуммаКонечныйОстаток` (или Актив/Пассив).

- [ ] **Step 2: Написать пре-тест запуска СКД**

```python
# -*- coding: utf-8 -*-
"""Task2 discovery: программно выполнить Отчет.УправленческийБаланс через СКД
за январь/ТОВ и извлечь итоги по Статья. Механизм для основного acceptance."""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime
pythoncom.CoInitialize()
ERP = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"')

q0 = ERP.NewObject("Запрос")
q0.Текст = ('ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Справочник.Организации '
            'ГДЕ КодПоЕДРПОУ = "40645273"')
s = q0.Выполнить().Выбрать(); s.Следующий(); ORG = s.С
qm = ERP.NewObject("Запрос")
qm.Текст = ("ВЫБРАТЬ НАЧАЛОПЕРИОДА(&М,МЕСЯЦ) КАК НМ, "
            "КОНЕЦПЕРИОДА(КОНЕЦПЕРИОДА(&М,МЕСЯЦ),ДЕНЬ) КАК КД")
qm.УстановитьПараметр("М", datetime(2026, 1, 1, 12, 0, 0))
rm = qm.Выполнить().Выбрать(); rm.Следующий(); НМ, КД = rm.НМ, rm.КД

Отчет = ERP.Отчеты.УправленческийБаланс.Создать()
Схема = Отчет.ПолучитьМакет("ОсновнаяСхемаКомпоновкиДанных")
Компоновщик = ERP.NewObject("КомпоновщикНастроекКомпоновкиДанных")
Компоновщик.Инициализировать(
    ERP.NewObject("ИсточникДоступныхНастроекКомпоновкиДанных", Схема))
Настройки = Схема.НастройкиПоУмолчанию
# параметры (имена уточнить из Step 1)
def уст_парам(имя, знач):
    п = Настройки.ПараметрыДанных.НайтиЗначениеПараметра(
        ERP.NewObject("ПараметрКомпоновкиДанных", имя))
    if п is not None:
        п.Значение = знач; п.Использование = True
уст_парам("НачалоПериода", НМ)
уст_парам("КонецПериода", КД)
уст_парам("Организация", ORG)

КомпоновщикМакета = ERP.NewObject("КомпоновщикМакетаКомпоновкиДанных")
МакетКомпоновки = КомпоновщикМакета.Выполнить(
    Схема, Настройки, , , ERP.NewObject("ГенераторМакетаКомпоновкиДанных"))
ПроцессорКомпоновки = ERP.NewObject("ПроцессорКомпоновкиДанных")
ПроцессорКомпоновки.Инициализировать(МакетКомпоновки, , , True)
ПроцессорВывода = ERP.NewObject(
    "ПроцессорВыводаРезультатаКомпоновкиДанныхВКоллекциюЗначений")
ТЗ = ERP.NewObject("ТаблицаЗначений")
ПроцессорВывода.УстановитьОбъект(ТЗ)
ПроцессорВывода.Вывести(ПроцессорКомпоновки)

print(f"Колонки отчёта: {[c.Имя for c in ТЗ.Колонки]}")
print(f"Строк: {ТЗ.Количество()}")
# извлечь по Статья (имя колонки статьи/ресурса — уточнить из Step 1 и колонок)
print("PASS test_balans_m_uprreport (зафиксировать имена колонок/параметров "
      "для test_balans_vs_upr_balance_report.py Task N)")
```

- [ ] **Step 3: Запустить, при ошибке — итеративно исправить имена**

Run: `python "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_balans_m_uprreport.py"`
Expected: печатается список колонок результата отчёта и число строк. Если ошибка имени схемы/параметра/метода СКД — исправить по фактической структуре (Step 1) и повторить (Rule #-1, итеративно до успеха). **Зафиксировать рабочий код запуска СКД + имена колонок (Статья, КонОст) — это шаблон для финального acceptance Task N.**

- [ ] **Step 4: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25"
git add _Rarzrabotki/Python/test/test_balans_m_uprreport.py
git commit -m "test(balans): Task2 discovery — программный запуск Отчет.УправленческийБаланс через СКД"
```

---

## Task 3: ЧЕКПОЙНТ — изменения структуры регистра (финансист вручную)

**Files:** (метаданные правит финансист; план НЕ меняет — DR-12)

- [ ] **Step 1: Подготовить точный список для финансиста**

Сообщить пользователю (финансисту) список правок `InformationRegisters/А_ОтчетБаланс_Свод` (из спеки §3):
1. Измерение `Source`: сменить тип `Перечисление.А_ИсточникБаланса` → `Перечисление.ИсточникиУправленческогоБаланса`.
2. Удалить `Перечисление.А_ИсточникБаланса` (после п.1).
3. Добавить измерение `Расхождение`, тип `Булево`, «Проверка заполнения = Не проверять».
4. Подтвердить `ДокументДвижения` (ДокументСсылка) на месте.

- [ ] **Step 2: STOP — ждать подтверждения финансиста**

Дождаться от пользователя «структура изменена». Если БД заблокирована Конфигуратором при последующем `/db-update` — STOP, попросить закрыть, не обходить.

- [ ] **Step 3: Применить метаданные в БД**

Run (skills): `/db-load-xml -Mode Partial` затем `/db-update -Dynamic+` (для серверной BaseERP).
Expected: конфигурация обновлена без ошибок.

- [ ] **Step 4: Верифицировать новую структуру**

Через MCP: `get_metadata_structure InformationRegisters А_ОтчетБаланс_Свод`.
Expected: `Source` тип = `Перечисление.ИсточникиУправленческогоБаланса`; есть `Расхождение` (Булево); `ДокументДвижения` (ДокументСсылка); `Перечисление.А_ИсточникБаланса` отсутствует (`get_metadata_structure Enums` недоступен — проверить отсутствие через `list_metadata_objects` или попытку запроса). Зафиксировать финальный список измерений/ресурсов — это сигнатура для BSL `Свод_*`/`Загрузить`.

---

## Task 4: Каркас — оркестратор `ПровестиБалансСвод` + `Отразить_Свод` + `ЭталонПАП` (без источников)

**Files:**
- Modify: `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl` (полная перезапись структуры; источники добавляются Task 5-10)

- [ ] **Step 1: Прочитать эталоны каркаса**

Прочитать `_Rarzrabotki/ERP_Pyramida/Documents/А_Баланс/Ext/ObjectModule.bsl` строки 600-650 (`ОбработкаПроведения`+`ПровестиБалансУпр` шапка), 616-625 (`Отразить_А_БалансУпр`); текущий `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl` (дублікат-захист, ИсключенныеСтатьи, СтатьяОплатаТруда — переиспользовать как есть).

- [ ] **Step 2: Написать пре-тест ЭталонПАП (Python)**

Create `_Rarzrabotki/Python/test/test_balans_m_sverka.py` (часть 1 — эталон; часть 2 добавится в Task 11):

```python
# -*- coding: utf-8 -*-
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime
pythoncom.CoInitialize()
ERP = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администratор";Pwd="24043"'.replace("атор","атор"))
q0=ERP.NewObject("Запрос"); q0.Текст='ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Справочник.Организации ГДЕ КодПоЕДРПОУ="40645273"'
s=q0.Выполнить().Выбрать(); s.Следующий(); ORG=s.С
qm=ERP.NewObject("Запрос")
qm.Текст="ВЫБРАТЬ НАЧАЛОПЕРИОДА(&М,МЕСЯЦ) КАК НМ, КОНЕЦПЕРИОДА(КОНЕЦПЕРИОДА(&М,МЕСЯЦ),ДЕНЬ) КАК КД"
qm.УстановитьПараметр("М",datetime(2026,1,1,12,0,0)); rm=qm.Выполнить().Выбрать(); rm.Следующий()
qi=ERP.NewObject("Запрос")
qi.Текст='ВЫБРАТЬ Ссылка КАК С ИЗ ПланВидовХарактеристик.СтатьиАктивовПассивов ГДЕ Наименование В (&Сп)'
a=ERP.NewObject("Массив")
for n in ["Собственные средства","Доходы текущего периода","Расходы текущего периода"]: a.Добавить(n)
qi.УстановитьПараметр("Сп",a); ИСКЛ=qi.Выполнить().Выгрузить().ВыгрузитьКолонку("С")
qe=ERP.NewObject("Запрос")
qe.Текст="""ВЫБРАТЬ ПРЕДСТАВЛЕНИЕ(Б.Статья) КАК Ст, СУММА(Б.СуммаКонечныйОстаток) КАК sK
ИЗ РегистрНакопления.ПрочиеАктивыПассивы.ОстаткиИОбороты(&Д1,&Д2,Авто,,
 Организация=&Орг И НЕ Статья В ИЕРАРХИИ(&Искл)) КАК Б
СГРУППИРОВАТЬ ПО ПРЕДСТАВЛЕНИЕ(Б.Статья) ИМЕЮЩИЕ СУММА(Б.СуммаКонечныйОстаток)<>0"""
qe.УстановитьПараметр("Д1",rm.НМ); qe.УстановитьПараметр("Д2",rm.КД)
qe.УстановитьПараметр("Орг",ORG); qe.УстановитьПараметр("Искл",ИСКЛ)
te=qe.Выполнить().Выгрузить()
et={str(te.Получить(i).Ст):float(te.Получить(i).sK or 0) for i in range(te.Количество())}
print(f"Эталон ПАП статей: {len(et)}; Σ КонОст = {sum(et.values()):,.2f} (≈0 Актив=Пассив)")
assert abs(sum(et.values()))<1.0, f"FAIL: Σ КонОст эталона != 0 ({sum(et.values()):,.2f}) — период открыт?"
print("PASS test_balans_m_sverka (часть1: эталон ПАП)")
```

- [ ] **Step 3: Запустить пре-тест эталона**

Run: `python "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_balans_m_sverka.py"`
Expected: `Эталон ПАП статей: N; Σ КонОст ≈ 0`, `PASS`. (Январь закрыт → Σ≈0.)

- [ ] **Step 4: Написать каркас ObjectModule.bsl**

Перезаписать `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl`: сохранить `ОбработкаЗаполнения` и дублікат-захист как в текущем; `ОбработкаПроведения` зовёт `ПровестиБалансСвод`; добавить пустой каркас:

```bsl
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

Перем мТЗСвод;

#Область ОбработчикиСобытий
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		Организация = А_Привилегированный.А_ПолучитьОрганизациюУпр();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Месяц) Тогда
		Месяц = НачалоМесяца(ТекущаяДатаСеанса());
	КонецЕсли;
КонецПроцедуры

Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	НачМесяца = НачалоМесяца(Месяц);
	КонМесяца = КонецМесяца(Месяц);
	// Дублікат-захист (перенести 1:1 из текущего ObjectModule.bsl, строки ~19-40)
	ЗапросДубль = Новый Запрос;
	ЗапросДубль.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1 ФР.Номер КАК Номер, ФР.Дата КАК Дата
	|ИЗ Документ.А_ФинРез_Баланс КАК ФР
	|ГДЕ ФР.Ссылка <> &ТекущийДок И НЕ ФР.ПометкаУдаления И ФР.Проведен
	|	И ФР.Организация = &Организация
	|	И ФР.Месяц МЕЖДУ &НачМесяца И &КонМесяца
	|	И ФР.ВключатьДочерние = &ВключатьДочерние";
	ЗапросДубль.УстановитьПараметр("ТекущийДок", Ссылка);
	ЗапросДубль.УстановитьПараметр("Организация", Организация);
	ЗапросДубль.УстановитьПараметр("НачМесяца", НачМесяца);
	ЗапросДубль.УстановитьПараметр("КонМесяца", КонМесяца);
	ЗапросДубль.УстановитьПараметр("ВключатьДочерние", ВключатьДочерние);
	РезДубль = ЗапросДубль.Выполнить();
	Если НЕ РезДубль.Пустой() Тогда
		ВыбД = РезДубль.Выбрать(); ВыбД.Следующий();
		ОбщегоНазначения.СообщитьПользователю(СтрШаблон(
			"Не можна провести: за %1 (Організація %2) вже проведено документ %3 від %4.",
			Формат(НачМесяца,"ДФ='ММММ yyyy'"), Организация, ВыбД.Номер,
			Формат(ВыбД.Дата,"ДФ='dd.MM.yyyy'")));
		Отказ = Истина; Возврат;
	КонецЕсли;

	СтруктураШапки = Новый Структура;
	СтруктураШапки.Вставить("Ссылка", Ссылка);
	СтруктураШапки.Вставить("Дата", Дата);
	СтруктураШапки.Вставить("НачМес", НачМесяца);
	СтруктураШапки.Вставить("КонМес", КонецДня(КонМесяца));
	СтруктураШапки.Вставить("Организация", Организация);
	СтруктураШапки.Вставить("ВключатьДочерние", ВключатьДочерние);
	ПровестиБалансСвод(СтруктураШапки, Отказ);
КонецПроцедуры
#КонецОбласти

#Область Оркестратор
Процедура ПровестиБалансСвод(СтруктураШапки, Отказ) Экспорт
	Движения.А_ОтчетБаланс_Свод.Записывать = Истина;
	мТЗСвод = Движения.А_ОтчетБаланс_Свод.Выгрузить();
	мТЗСвод.Очистить();
	// Свод_* добавляются Task 5-10:
	// Свод_РасчетыСПартнерами(СтруктураШапки, Отказ);
	// Свод_СебестоимостьТоваров(СтруктураШапки, Отказ);
	// Свод_ДенежныеСредства(СтруктураШапки, Отказ);
	// Свод_ПрочиеРасходыДоходы(СтруктураШапки, Отказ);
	// Свод_ПрочиеАктивыПассивы_Прямой(СтруктураШапки, Отказ);
	// Свод_ОплатаТруда(СтруктураШапки, Отказ);
	СверкаСПАП(СтруктураШапки);   // Task 11
	мТЗСвод.Свернуть(
		"Организация,Подразделение,Статья,Номенклатура,Контрагент,Партнер,Склад,"
		+"ОбъектыЭксплуатации,Договор,ФизическоеЛицо,ДенежныеСредства,Source,"
		+"Аналитика1,Аналитика2,Аналитика3,ОбъектРасчетов,НематериальныйАктив,"
		+"ДокументДвижения,Расхождение,Период",
		"СуммаНачальныйОстаток,СуммаПриход,СуммаРасход,СуммаКонечныйОстаток");
	мТЗСвод.ЗаполнитьЗначения(СтруктураШапки.НачМес, "Период");
	Движения.А_ОтчетБаланс_Свод.Загрузить(мТЗСвод);
КонецПроцедуры
#КонецОбласти

#Область СлужебныеПроцедуры
Процедура Отразить_Свод(Таблица) Экспорт
	Для Каждого СтрТЗ Из Таблица Цикл
		ЗаполнитьЗначенияСвойств(мТЗСвод.Добавить(), СтрТЗ);
	КонецЦикла;
КонецПроцедуры

Функция ИсключенныеСтатьи()
	З = Новый Запрос;
	З.Текст = "ВЫБРАТЬ Ссылка КАК Ссылка ИЗ ПланВидовХарактеристик.СтатьиАктивовПассивов
	|ГДЕ Наименование В (&Имена)";
	З.УстановитьПараметр("Имена", СтрРазделить(
		"Собственные средства,Доходы текущего периода,Расходы текущего периода", ","));
	Возврат З.Выполнить().Выгрузить().ВыгрузитьКолонку("Ссылка");
КонецФункции

Функция СтатьяОплатаТруда()
	З = Новый Запрос;
	З.Текст = "ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК Ссылка
	|ИЗ ПланВидовХарактеристик.СтатьиАктивовПассивов ГДЕ Наименование = ""Оплата труда""";
	В = З.Выполнить().Выбрать();
	Возврат ?(В.Следующий(), В.Ссылка,
		ПланыВидовХарактеристик.СтатьиАктивовПассивов.ПустаяСсылка());
КонецФункции

Процедура СверкаСПАП(СтруктураШапки)
	// Реализуется Task 11
КонецПроцедуры
#КонецОбласти

#КонецЕсли
```

> Точные имена измерений в `Свернуть` — взять из Task 3 Step 4 (фактическая структура); привести 1:1.

- [ ] **Step 5: Применить в БД + sanity**

```bash
cp "C:/Configuration_downloads/BASERP25/Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl" "C:/Configuration_downloads/BASERP25/Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl"
```
(файл уже в main config — шаг для явности Rule #4). Затем `/cf-validate` → `/db-load-xml -Mode Partial` → `/db-update -Dynamic+`. БД блокирована → STOP, просить закрыть Конфигуратор.
Expected: `/cf-validate` без ошибок синтаксиса; конфигурация обновлена. Проведение пока даёт ПУСТОЙ регистр (источников нет) — это ОК на этом шаге.

- [ ] **Step 6: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25"
git add "Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl" _Rarzrabotki/Python/test/test_balans_m_sverka.py
git commit -m "feat(balans): каркас модульного проведения (оркестратор+Отразить_Свод+эталон), источники далее"
```

---

## Task 5: `Свод_РасчетыСПартнерами` (клиенты + поставщики, одна процедура — DR-1)

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_m_src_raschety.py`
- Modify: `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl` (+функция, +вызов в оркестраторе)

- [ ] **Step 1: Прочитать эталон**

Прочитать `DataProcessors/ДвиженияАктивовПассивов/Ext/ManagerModule.bsl` `ДобавитьТекстЗапросаРасчетыСПартнерами` (964-1059) — субконто/ключи; `_Rarzrabotki/ERP_Pyramida/.../А_Баланс/Ext/ObjectModule.bsl` `Упр_РасчетыСКлиентами`(1446)/`Упр_РасчетыСПоставщиками`(1150). Источник(и) из Task 1 вывода (a): «Расчеты с клиентами по срокам», «Расчеты с поставщиками по срокам».

- [ ] **Step 2: Написать пре-тест запроса (Python COM, 1:1 как пойдёт в BSL)**

```python
# -*- coding: utf-8 -*-
"""Пре-тест Свод_РасчетыСПартнерами: основная таблица ПАП, Источник IN
(клиенты/поставщики по срокам), субконто ОбъектРасчетов, 4 ресурса; Σ КонОст
по Статья == ПАП.ОстаткиИОбороты по этим же Источникам/статьям до копейки."""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime
pythoncom.CoInitialize()
ERP = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
q0=ERP.NewObject("Запрос"); q0.Текст='ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Справочник.Организации ГДЕ КодПоЕДРПОУ="40645273"'
s=q0.Выполнить().Выбрать(); s.Следующий(); ORG=s.С
qm=ERP.NewObject("Запрос")
qm.Текст="ВЫБРАТЬ НАЧАЛОПЕРИОДА(&М,МЕСЯЦ) КАК НМ, КОНЕЦПЕРИОДА(КОНЕЦПЕРИОДА(&М,МЕСЯЦ),ДЕНЬ) КАК КД"
qm.УстановитьПараметр("М",datetime(2026,1,1,12,0,0)); rm=qm.Выполнить().Выбрать(); rm.Следующий()
# Источники РасчетыСПартнерами (имена enum — из Task1; ниже по Представлению)
qИ=ERP.NewObject("Запрос")
qИ.Текст="""ВЫБРАТЬ Ссылка КАК С ИЗ Перечисление.ИсточникиУправленческогоБаланса
ГДЕ Ссылка В (ЗНАЧЕНИЕ(Перечисление.ИсточникиУправленческогоБаланса.РасчетыСКлиентамиПоСрокам),
              ЗНАЧЕНИЕ(Перечисление.ИсточникиУправленческогоБаланса.РасчетыСПоставщикамиПоСрокам))"""
spИст=qИ.Выполнить().Выгрузить().ВыгрузитьКолонку("С")
# КонОст из ОСНОВНОЙ таблицы по статьям для этих источников
qd=ERP.NewObject("Запрос")
qd.Текст="""ВЫБРАТЬ ПРЕДСТАВЛЕНИЕ(Т.Статья) КАК Ст,
 СУММА(ВЫБОР КОГДА Т.ВидДвижения=ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
   ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ) КАК sK,
 КОЛИЧЕСТВО(РАЗЛИЧНЫЕ Т.Регистратор) КАК КолРег
ИЗ РегистрНакопления.ПрочиеАктивыПассивы КАК Т
ГДЕ Т.Организация=&Орг И Т.Период <= &КД И Т.Источник В (&Ист)
СГРУППИРОВАТЬ ПО ПРЕДСТАВЛЕНИЕ(Т.Статья)"""
qd.УстановитьПараметр("Орг",ORG); qd.УстановитьПараметр("КД",rm.КД)
qd.УстановитьПараметр("Ист",spИст)
try:
    td=qd.Выполнить().Выгрузить()
except Exception as e:
    print(f"FAIL: {e.excepinfo[2] if hasattr(e,'excepinfo') and e.excepinfo else e}"); sys.exit(1)
print(f"РасчетыСПартнерами: статей={td.Количество()}")
for i in range(td.Количество()):
    r=td.Получить(i); print(f"  {str(r.Ст)[:40]:40} КонОст={float(r.sK or 0):,.2f} рег={int(r.КолРег)}")
print("PASS test_balans_m_src_raschety")
```

> Имена `РасчетыСКлиентамиПоСрокам`/`...ПоставщикамиПоСрокам` — из Task 1 вывода (a, ПРЕДСТАВЛЕНИЕ → имя enum через `Перечисление.ИсточникиУправленческогоБаланса`). Если имя иное — подставить фактическое из Task 1 (Rule #-1 discovery).

- [ ] **Step 3: Запустить пре-тест, добиться PASS**

Run: `python "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_balans_m_src_raschety.py"`
Expected: список статей с КонОст; `PASS`. Ошибка имени enum → подставить фактическое (Task 1) и повторить.

- [ ] **Step 4: Реализовать `Свод_РасчетыСПартнерами` в BSL**

В `ObjectModule.bsl` область `#Область ПроцедурыИсточников` добавить (запрос 1:1 с пре-тестом + субконто ОбъектРасчетов из `РасчетыС*ПоСрокам` LEFT JOIN по Регистратору fill-when-unique; ДокументДвижения=Т.Регистратор; Source=Т.Источник; НачОст=Σsigned до &НачМес, Приход/Расход в периоде, КонОст=Нач+Прих−Расх; НЕ Статья В ИЕРАРХИИ(&Искл)):

```bsl
Функция Свод_РасчетыСПартнерами(СтруктураШапки, Отказ) Экспорт
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Орг", СтруктураШапки.Организация);
	Запрос.УстановитьПараметр("НачМес", СтруктураШапки.НачМес);
	Запрос.УстановитьПараметр("КонМес", СтруктураШапки.КонМес);
	Запрос.УстановитьПараметр("ВклДоч", СтруктураШапки.ВключатьДочерние);
	Запрос.УстановитьПараметр("Искл", ИсключенныеСтатьи());
	Запрос.Текст =
	"ВЫБРАТЬ
	|	Т.Организация КАК Организация, Т.Подразделение КАК Подразделение,
	|	Т.Статья КАК Статья, Т.Регистратор КАК ДокументДвижения,
	|	Т.Источник КАК Source,
	|	ВЫРАЗИТЬ(Т.Аналитика КАК Справочник.Контрагенты) КАК Контрагент,
	|	ВЫРАЗИТЬ(Т.Аналитика КАК Справочник.Партнеры) КАК Партнер,
	|	ПРЕДСТАВЛЕНИЕ(Т.Аналитика) КАК Аналитика3, """" КАК Аналитика1, """" КАК Аналитика2,
	|	ЛОЖЬ КАК Расхождение,
	|	СУММА(ВЫБОР КОГДА Т.Период < &НачМес
	|		ТОГДА ВЫБОР КОГДА Т.ВидДвижения=ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
	|			ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ ИНАЧЕ 0 КОНЕЦ) КАК СуммаНачальныйОстаток,
	|	СУММА(ВЫБОР КОГДА Т.Период>=&НачМес И Т.ВидДвижения=ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
	|		ТОГДА Т.Сумма ИНАЧЕ 0 КОНЕЦ) КАК СуммаПриход,
	|	СУММА(ВЫБОР КОГДА Т.Период>=&НачМес И Т.ВидДвижения=ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход)
	|		ТОГДА Т.Сумма ИНАЧЕ 0 КОНЕЦ) КАК СуммаРасход,
	|	СУММА(ВЫБОР КОГДА Т.ВидДвижения=ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
	|		ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ) КАК СуммаКонечныйОстаток
	|ИЗ РегистрНакопления.ПрочиеАктивыПассивы КАК Т
	|ГДЕ (ВЫБОР КОГДА &ВклДоч ТОГДА Т.Организация В ИЕРАРХИИ(&Орг)
	|		ИНАЧЕ Т.Организация = &Орг КОНЕЦ)
	|	И Т.Период <= &КонМес
	|	И Т.Источник В (ЗНАЧЕНИЕ(Перечисление.ИсточникиУправленческогоБаланса.РасчетыСКлиентамиПоСрокам),
	|		ЗНАЧЕНИЕ(Перечисление.ИсточникиУправленческогоБаланса.РасчетыСПоставщикамиПоСрокам))
	|	И НЕ Т.Статья В ИЕРАРХИИ(&Искл)
	|СГРУППИРОВАТЬ ПО Т.Организация, Т.Подразделение, Т.Статья, Т.Регистратор, Т.Источник,
	|	ВЫРАЗИТЬ(Т.Аналитика КАК Справочник.Контрагенты),
	|	ВЫРАЗИТЬ(Т.Аналитика КАК Справочник.Партнеры), ПРЕДСТАВЛЕНИЕ(Т.Аналитика)
	|ИМЕЮЩИЕ НЕ (СУММА(ВЫБОР КОГДА Т.ВидДвижения=ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
	|		ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ)=0
	|	И СУММА(ВЫБОР КОГДА Т.Период<&НачМес ТОГДА ВЫБОР КОГДА Т.ВидДвижения=ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
	|		ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ ИНАЧЕ 0 КОНЕЦ)=0)";
	Отразить_Свод(Запрос.Выполнить().Выгрузить());
	Возврат Истина;
КонецФункции
```

Раскомментировать `Свод_РасчетыСПартнерами(СтруктураШапки, Отказ);` в `ПровестиБалансСвод`.

> ⚠️ Имена enum-значений `РасчетыСКлиентамиПоСрокам`/`РасчетыСПоставщикамиПоСрокам` — ровно как зафиксировано Task 1. ОбъектРасчетов-субконто добавить отдельным LEFT JOIN предагрегированного `РасчетыСКлиентамиПоСрокам`/`...ПоставщикамиПоСрокам` по Регистратору (fill-when-unique), если требуется детализация (механизм штатной обработки, Task 5 Step1 эталон) — иначе ОбъектРасчетов из ВЫРАЗИТЬ(Т.Аналитика) если тип совпадает.

- [ ] **Step 5: Применить + проверить проведение частично**

`cp` (явно) → `/cf-validate` → `/db-load-xml -Mode Partial` → `/db-update -Dynamic+` (БД блок → STOP/просить закрыть). Прогнать `test_balans_m_post.py` (Task создаётся в Task 12 — здесь только sanity провести вручную через COM в пре-тесте: документ проводится без ошибок).
Expected: `/cf-validate` чисто; документ проводится; регистр содержит строки РасчетыСПартнерами.

- [ ] **Step 6: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25"
git add "Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl" _Rarzrabotki/Python/test/test_balans_m_src_raschety.py
git commit -m "feat(balans): Свод_РасчетыСПартнерами (клиенты+поставщики по срокам, DR-1)"
```

---

## Task 6: `Свод_СебестоимостьТоваров`

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_m_src_sebest.py`
- Modify: `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl`

- [ ] **Step 1: Прочитать эталон** — `ДвиженияАктивовПассивов/Ext/ManagerModule.bsl` `ДобавитьТекстЗапросаСебестоимостьТоваров`(2491); `А_Баланс` `Упр_СебестоимостьТоваров`(2047). Источник из Task 1: «Себестоимость товаров».

- [ ] **Step 2: Написать пре-тест** — копия структуры `test_balans_m_src_raschety.py` Step 2, заменить фильтр на `Т.Источник = ЗНАЧЕНИЕ(Перечисление.ИсточникиУправленческогоБаланса.СебестоимостьТоваров)`, субконто → Номенклатура/Склад из `ВЫРАЗИТЬ(Т.Аналитика КАК Справочник.Номенклатура)` / LEFT JOIN `СебестоимостьТоваров` по Регистратору (fill-when-unique). Файл `test_balans_m_src_sebest.py` — полный код по образцу Task 5 Step 2 (повторить, не ссылаться).

- [ ] **Step 3: Запустить** Run: `python "...\test_balans_m_src_sebest.py"` Expected: статьи (ожид. «Товары на оптовых складах» и др.), `PASS`.

- [ ] **Step 4: Реализовать `Свод_СебестоимостьТоваров`** — функция по образцу Task 5 Step 4 (повторить структуру запроса полностью), фильтр `Т.Источник = ЗНАЧЕНИЕ(...СебестоимостьТоваров)`, субконто `ВЫРАЗИТЬ(Т.Аналитика КАК Справочник.Номенклатура) КАК Номенклатура` + Склад через предагрег. `СебестоимостьТоваров` LEFT JOIN по Регистратору. Раскомментировать вызов в оркестраторе.

- [ ] **Step 5: Применить** — `cp`→`/cf-validate`→`/db-load-xml`→`/db-update` (БД блок → STOP). Документ проводится без ошибок.

- [ ] **Step 6: Commit** `git add ... && git commit -m "feat(balans): Свод_СебестоимостьТоваров"`

---

## Task 7: `Свод_ДенежныеСредства` (безнал/нал/в пути/у подотчётных/ден.документы)

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_m_src_dengi.py`
- Modify: `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl`

- [ ] **Step 1: Прочитать эталон** — `ДвиженияАктивовПассивов` `ДобавитьТекстЗапросаДенежныеСредства`(497)/`...ВКассахККМ`(596)/`...ВПути`(669)/`...УПодотчетныхЛиц`(801)/`...ДенежныеДокументы`(889). Источники из Task 1: «Денежные средства (безналичные)/(наличные)/в пути/у подотчетных лиц», «Денежные документы».

- [ ] **Step 2: Написать пре-тест** `test_balans_m_src_dengi.py` — фильтр `Т.Источник В (<все ден. значения из Task1>)`, субконто `ДенежныеСредства` (composite: БанковскийСчет/Касса/ФизЛицо) — из `ВЫРАЗИТЬ(Т.Аналитика КАК ...)` по типу + LEFT JOIN ден.регистров по Регистратору fill-when-unique. Полный код по образцу Task 5 Step 2.

- [ ] **Step 3: Запустить** Run: `python "...\test_balans_m_src_dengi.py"` Expected: статьи ДС, `PASS`.

- [ ] **Step 4: Реализовать `Свод_ДенежныеСредства`** — фильтр по всем ден.Источникам (точные имена enum из Task 1), субконто `ДенежныеСредства` + ФизическоеЛицо (подотчётные). Раскомментировать вызов.

- [ ] **Step 5: Применить** — workflow как Task 6 Step 5.

- [ ] **Step 6: Commit** `git commit -m "feat(balans): Свод_ДенежныеСредства (безнал/нал/в пути/подотч/ден.док)"`

---

## Task 8: `Свод_ПрочиеРасходыДоходы` (прочие расходы/доходы/партии прочих расходов/амортизация)

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_m_src_prochie.py`
- Modify: `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl`

- [ ] **Step 1: Прочитать эталон** — `ДвиженияАктивовПассивов` `ДобавитьТекстЗапросаПрочиеРасходы`(2179)/`ПрочиеДоходы`(2067)/`ПартииПрочихРасходов`(2421)/`АмортизацияОС`(3034)/`СтоимостьОС`(3077). Источники из Task 1: «Прочие расходы», «Прочие доходы», «Партии прочих расходов» (+ амортизация ОС/НМА если в Task 1 присутствуют).

- [ ] **Step 2: Написать пре-тест** `test_balans_m_src_prochie.py` — фильтр `Т.Источник В (<прочие/амортизация из Task1>)`, субконто `ОбъектыЭксплуатации`/`НематериальныйАктив`/`Номенклатура` через `ВЫРАЗИТЬ(Т.Аналитика КАК ...)`. Полный код по образцу Task 5 Step 2.

- [ ] **Step 3: Запустить** Expected: статьи (ожид. «Основные средства», «Прочие активы/пассивы»…), `PASS`.

- [ ] **Step 4: Реализовать `Свод_ПрочиеРасходыДоходы`** — фильтр по этим Источникам, субконто из ВЫРАЗИТЬ(Т.Аналитика) по типам. Раскомментировать вызов.

- [ ] **Step 5: Применить** — workflow как Task 6 Step 5.

- [ ] **Step 6: Commit** `git commit -m "feat(balans): Свод_ПрочиеРасходыДоходы (+амортизация ОС/НМА)"`

---

## Task 9: `Свод_ПрочиеАктивыПассивы_Прямой` (Источник="" — ВводОстатков/ДвижениеПрочихАП/закрытие)

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_m_src_pap.py`
- Modify: `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl`

- [ ] **Step 1: Прочитать эталон** — `А_Баланс` `Упр_ПрочиеАктивыПассивы`(968): субконто прямо из ПАП.Аналитика через ВЫРАЗИТЬ. Источник из Task 1: пустой (`Т.Источник = ЗНАЧЕНИЕ(Перечисление.ИсточникиУправленческогоБаланса.ПустаяСсылка)`).

- [ ] **Step 2: Написать пре-тест** `test_balans_m_src_pap.py` — фильтр `Т.Источник = ЗНАЧЕНИЕ(Перечисление.ИсточникиУправленческогоБаланса.ПустаяСсылка) И НЕ Т.Статья В ИЕРАРХИИ(&Искл) И Т.Статья <> &СтатьяОТ` (ОТ обрабатывается Task 10). Субконто: `ВЫРАЗИТЬ(Т.Аналитика КАК Справочник.ОбъектыЭксплуатации/НематериальныеАктивы/Контрагенты/...)`, `Аналитика3=ПРЕДСТАВЛЕНИЕ(Т.Аналитика)`. Полный код по образцу Task 5 Step 2 + параметр СтатьяОТ.

- [ ] **Step 3: Запустить** Expected: 3 Пассив-статьи + Актив/Пассив (из эмпирики §1.4 спеки: ~−9.65М/+5.49М), `PASS`. (Эти статьи — частично в ИсключенныеСтатьи → проверить что после фильтра остаются только балансовые.)

- [ ] **Step 4: Реализовать `Свод_ПрочиеАктивыПассивы_Прямой`** — фильтр Источник=ПустаяСсылка, исключая СтатьяОплатаТруда и ИсключенныеСтатьи; субконто из ВЫРАЗИТЬ(Т.Аналитика) по всем типам измерений регистра-приёмника; `Source = ЗНАЧЕНИЕ(Перечисление.ИсточникиУправленческогоБаланса.ПустаяСсылка)`. Раскомментировать вызов.

- [ ] **Step 5: Применить** — workflow как Task 6 Step 5.

- [ ] **Step 6: Commit** `git commit -m "feat(balans): Свод_ПрочиеАктивыПассивы_Прямой (Источник='')"`

---

## Task 10: `Свод_ОплатаТруда` (Етап4 — разнос по ФизическоеЛицо ∝ |сальдо| ВзаиморасчетыССотрудниками, DR-9)

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_m_src_ot.py`
- Modify: `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl`

- [ ] **Step 1: Прочитать текущую логику ОТ** — в `_backup_objectmodule_balans_pre_modular.bsl` (Task 0): втЗПСальдо/втЗПФЛ/втЗПОрг + UNION B1/B2 (разнос ∝ |сальдо| `ВзаиморасчетыССотрудниками`, Σ ваг=1). Переиспользовать алгоритм 1:1, обернув в функцию.

- [ ] **Step 2: Написать пре-тест** `test_balans_m_src_ot.py` — запрос ОТ-разноса 1:1 как в backup (ПАП.ОстаткиИОбороты по Статья=&СтатьяОТ × вес ФизЛицо из ВзаиморасчетыССотрудниками; орг без ВЗ → 1 строка). Assert: Σ по ОТ == ПАП по ОТ (tol 1.0 — округление разноса); ФизическоеЛицо заполнено для орг с ВЗ. Полный код (адаптировать из существующего `test_balans_s4_verify.py`).

- [ ] **Step 3: Запустить** Run: `python "...\test_balans_m_src_ot.py"` Expected: ФизЛицо заполнено, Σ ОТ == ПАП (Δ≤1.0), `PASS`.

- [ ] **Step 4: Реализовать `Свод_ОплатаТруда`** — перенести из backup втЗПСальдо/втЗПФЛ/втЗПОрг + 3-частинний UNION (A не нужен — только ОТ: B1 орг с ВЗ разнос по ФизЛицо, B2 орг без ВЗ 1 строка), `Source = ЗНАЧЕНИЕ(Перечисление.ИсточникиУправленческогоБаланса.ПустаяСсылка)` или спец-значение если есть в enum (из Task 1; иначе ПустаяСсылка + Расхождение=Ложь), `ДокументДвижения`=ПАП.Регистратор где доступен. Раскомментировать вызов.

- [ ] **Step 5: Применить** — workflow как Task 6 Step 5.

- [ ] **Step 6: Commit** `git commit -m "feat(balans): Свод_ОплатаТруда (Етап4 разнос по ФизЛицо ∝ ВзаиморасчётыСС)"`

---

## Task 11: `СверкаСПАП` — обрезка/добивка/Расхождение + инвариант (DR-5/DR-6/DR-10)

**Files:**
- Modify: `_Rarzrabotki/Python/test/test_balans_m_sverka.py` (часть 2)
- Modify: `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl` (тело `СверкаСПАП`)

- [ ] **Step 1: Дописать пре-тест сверки (часть 2 в test_balans_m_sverka.py)**

Добавить в конец `test_balans_m_sverka.py`: провести документ январь/ТОВ; выгрузить `А_ОтчетБаланс_Свод` по Регистратор; сгруппировать Σ по (Орг,Подр,Статья) 4 ресурса; сравнить с эталоном ПАП (часть1): assert per (Орг,Подр,Статья) каждый ресурс |Δ| ≤ 0.01 (ОТ ≤ 1.0); assert Σ строк `Расхождение=Истина` + `Расхождение=Ложь` == эталон; assert Σ КонОст всего ≈ 0. Полный код (адаптировать из `test_balans_s4_verify.py` структуру выгрузки/сравнения).

- [ ] **Step 2: Запустить — ожидать FAIL (СверкаСПАП пуста)**

Run: `python "...\test_balans_m_sverka.py"`
Expected: FAIL — Σ расшифровки ≠ эталон (сверка не реализована: обрезки/добивки нет). Зафиксировать величину расхождений по статьям (вход для проверки Step 4).

- [ ] **Step 3: Реализовать `СверкаСПАП` в BSL**

Тело процедуры `СверкаСПАП(СтруктураШапки)`:
1. `ЭталонПАП` = запрос `ПАП.ОстаткиИОбороты(&НачМес,&КонМес,Авто,, (орг+ВклДоч) И НЕ Статья В ИЕРАРХИИ(&Искл))` → ТЗ по (Орг,Подр,Статья) 4 ресурса.
2. Свернуть `мТЗСвод` копию по (Орг,Подр,Статья) 4 ресурса → ТЗРасшифр.
3. Для каждой (Орг,Подр,Статья) по каждому ресурсу:
   - `лимит` = `?(Статья=СтатьяОплатаТруда(), 1.0, 0.01)`.
   - Если `|Σрасшифр| − |ПАП| > лимит` (расшифровка превышает): `k = ПАП/Σрасшифр`; в `мТЗСвод` все строки этой (Орг,Подр,Статья) умножить ресурс на `k`; разницу округления добросить в строку с макс|ресурс|; пометить `Расхождение=Истина` этим строкам.
   - Если `|ПАП| − |Σрасшифр| > лимит` (ПАП больше): `мТЗСвод.Добавить()` строку (Орг,Подр,Статья), все *_ID=ПустаяСсылка/Аналитика=""; ресурс=(ПАП−Σрасшифр); `Source` = доминирующий Источник статьи в мТЗСвод или ПустаяСсылка; `Расхождение=Истина`; ДокументДвижения=ПустаяСсылка.
4. Контроль: пересчитать Σ по (Орг,Подр,Статья) == ЭталонПАП (иначе — записать в лог/`ОбщегоНазначения.СообщитьПользователю`, не Отказ — инвариант держит обрезка/добивка).

(Полный BSL ~120 строк — реализовать по этому алгоритму; ВЫРАЗИТЬ только в SELECT эталона; группировка по Б.Аналитика как-есть.)

- [ ] **Step 4: Применить + прогнать пре-тест до PASS**

`cp`→`/cf-validate`→`/db-load-xml`→`/db-update` (БД блок→STOP). Run: `python "...\test_balans_m_sverka.py"`
Expected: PASS — per (Орг,Подр,Статья) Δ≤0.01 (ОТ≤1.0); Σ Расхождение(Истина+Ложь)==эталон; Σ КонОст≈0. Если FAIL — диагностировать обрезку/добивку, исправить BSL, повторить (не менять эталон/типовые).

- [ ] **Step 5: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25"
git add "Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl" _Rarzrabotki/Python/test/test_balans_m_sverka.py
git commit -m "feat(balans): СверкаСПАП — пропорц.обрезка/добивка/Расхождение, инвариант Σ==ПАП"
```

---

## Task 12: Test проведения (Test 2) — Проведен + идемпотентность

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_m_post.py`

- [ ] **Step 1: Написать тест проведения**

```python
# -*- coding: utf-8 -*-
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime
pythoncom.CoInitialize()
ERP = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
q0=ERP.NewObject("Запрос"); q0.Текст='ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Справочник.Организации ГДЕ КодПоЕДРПОУ="40645273"'
s=q0.Выполнить().Выбрать(); s.Следующий(); ORG=s.С
# удалить прежние документы за период
q=ERP.NewObject("Запрос"); q.Текст="ВЫБРАТЬ Ссылка КАК С ИЗ Документ.А_ФинРез_Баланс"
tz=q.Выполнить().Выгрузить()
for i in range(tz.Количество()): tz.Получить(i).С.ПолучитьОбъект().Удалить()
doc=ERP.Документы.А_ФинРез_Баланс.СоздатьДокумент()
doc.Дата=datetime(2026,1,31,14,0,0); doc.Организация=ORG
doc.Месяц=datetime(2026,1,1,12,0,0); doc.ВключатьДочерние=False
mode=ERP.PredefinedValue("РежимЗаписиДокумента.Проведение")
doc.Записать(mode)
assert doc.Проведен, "FAIL: не проведено"
ref=doc.Ссылка
qr=ERP.NewObject("Запрос")
qr.Текст="ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК К, СУММА(СуммаКонечныйОстаток) КАК S ИЗ РегистрСведений.А_ОтчетБаланс_Свод ГДЕ Регистратор=&Д"
qr.УстановитьПараметр("Д",ref); r=qr.Выполнить().Выбрать(); r.Следующий()
assert int(r.К)>0, "FAIL: регистр пуст"
print(f"[OK] проведено №{doc.Номер}, строк={int(r.К)}, ΣКонОст={float(r.S or 0):,.2f}")
doc.Записать(mode)  # перепроведение
qr2=ERP.NewObject("Запрос"); qr2.Текст=qr.Текст; qr2.УстановитьПараметр("Д",ref)
r2=qr2.Выполнить().Выбрать(); r2.Следующий()
assert int(r2.К)==int(r.К), f"FAIL: не идемпотентно {int(r.К)}->{int(r2.К)}"
print("PASS test_balans_m_post (Проведен, идемпотентно)")
```

- [ ] **Step 2: Запустить**

Run: `python "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_balans_m_post.py"`
Expected: `[OK] проведено №...`, `PASS` (Проведен; перепроведение даёт то же число строк).

- [ ] **Step 3: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25"
git add _Rarzrabotki/Python/test/test_balans_m_post.py
git commit -m "test(balans): Test 2 — проведение + идемпотентность"
```

---

## Task 13: ОСНОВНОЙ acceptance — Σ Свод по Статья == `Отчет.УправленческийБаланс` (требование пользователя)

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_vs_upr_balance_report.py`

- [ ] **Step 1: Написать acceptance-тест (использует механизм СКД из Task 2)**

```python
# -*- coding: utf-8 -*-
"""ОСНОВНОЙ acceptance: Σ СуммаКонечныйОстаток по Статья проведённого
А_ОтчетБаланс_Свод == итоги штатного Отчет.УправленческийБаланс (СКД)
за январь 2026/ТОВ — до копейки (Оплата труда tol 1.0)."""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime
pythoncom.CoInitialize()
ERP = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
q0=ERP.NewObject("Запрос"); q0.Текст='ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Справочник.Организации ГДЕ КодПоЕДРПОУ="40645273"'
s=q0.Выполнить().Выбрать(); s.Следующий(); ORG=s.С
qm=ERP.NewObject("Запрос")
qm.Текст="ВЫБРАТЬ НАЧАЛОПЕРИОДА(&М,МЕСЯЦ) КАК НМ, КОНЕЦПЕРИОДА(КОНЕЦПЕРИОДА(&М,МЕСЯЦ),ДЕНЬ) КАК КД"
qm.УстановитьПараметр("М",datetime(2026,1,1,12,0,0)); rm=qm.Выполнить().Выбрать(); rm.Следующий()

# 1. Провести документ (как Task 12) — ref
q=ERP.NewObject("Запрос"); q.Текст="ВЫБРАТЬ Ссылка КАК С ИЗ Документ.А_ФинРез_Баланс"
tz=q.Выполнить().Выгрузить()
for i in range(tz.Количество()): tz.Получить(i).С.ПолучитьОбъект().Удалить()
doc=ERP.Документы.А_ФинРез_Баланс.СоздатьДокумент()
doc.Дата=datetime(2026,1,31,14,0,0); doc.Организация=ORG
doc.Месяц=datetime(2026,1,1,12,0,0); doc.ВключатьДочерние=False
doc.Записать(ERP.PredefinedValue("РежимЗаписиДокумента.Проведение"))
assert doc.Проведен, "FAIL: не проведено"
qr=ERP.NewObject("Запрос")
qr.Текст="""ВЫБРАТЬ ПРЕДСТАВЛЕНИЕ(Статья) КАК Ст, СУММА(СуммаКонечныйОстаток) КАК sK
ИЗ РегистрСведений.А_ОтчетБаланс_Свод ГДЕ Регистратор=&Д
СГРУППИРОВАТЬ ПО ПРЕДСТАВЛЕНИЕ(Статья)"""
qr.УстановитьПараметр("Д",doc.Ссылка); trr=qr.Выполнить().Выгрузить()
свод={str(trr.Получить(i).Ст):float(trr.Получить(i).sK or 0) for i in range(trr.Количество())}

# 2. Штатный Отчет.УправленческийБаланс через СКД (код из Task 2 — РАБОЧИЙ шаблон)
#    <вставить отлаженный в Task 2 блок запуска СКД + извлечения {Статья: КонОст}>
#    Результат -> dict `отчет`
отчет = {}  # заполняется кодом Task 2 (имена схемы/параметров/колонок зафиксированы)

# 3. Сверка до копейки (ОТ tol 1.0)
bad=[]
for k in set(свод)|set(отчет):
    лим = 1.0 if "плата труда" in k.lower() else 0.01
    d=abs(свод.get(k,0)-отчет.get(k,0))
    if d>лим: bad.append((k,отчет.get(k,0),свод.get(k,0),d))
if bad:
    print(f"FAIL: {len(bad)} статей расходятся со штатным Отчет.УправленческийБаланс:")
    for k,a,b,d in bad[:12]: print(f"  {k[:40]} отчёт={a:,.2f} свод={b:,.2f} Δ={d:,.4f}")
    sys.exit(1)
assert abs(sum(свод.values()))<1.0, f"FAIL: Σ КонОст свода != 0 ({sum(свод.values()):,.2f})"
print(f"PASS test_balans_vs_upr_balance_report (статей={len(свод)}, Σ≈0, == Отчет.УправленческийБаланс)")
```

- [ ] **Step 2: Вставить рабочий блок СКД из Task 2**

Заменить `отчет = {}` на отлаженный в Task 2 Step 3 код запуска `Отчет.УправленческийБаланс` (СКД) с фактическими именами схемы/параметров/колонок (Статья, КонОст). Запустить:
Run: `python "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_balans_vs_upr_balance_report.py"`
Expected: `PASS ... == Отчет.УправленческийБаланс`. FAIL → диагностировать (Свод_*/СверкаСПАП по статье; не править эталон/типовые); исправить BSL соответствующей Свод_*/СверкаСПАП → повторить.

- [ ] **Step 3: Регресс существующего теста**

Run: `python "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_balans_s4_verify.py"`
Expected: `PASS Етап 4` (старый инвариант Σ==ПАП/Актив=Пассив зелёный — модульная логика не регрессировала).

- [ ] **Step 4: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25"
git add _Rarzrabotki/Python/test/test_balans_vs_upr_balance_report.py
git commit -m "test(balans): ОСНОВНОЙ acceptance — Σ Свод == Отчет.УправленческийБаланс до копейки (PASS)"
```

---

## Task 14: Обновить базу знаний `knowledge_Balanse`

**Files:**
- Modify: `_Rarzrabotki/notebook/knowledge_Balanse/balanse_provedenie_logic.md`
- Modify: `_Rarzrabotki/notebook/knowledge_Balanse/balanse_mapping_actual_vs_planned.md`
- Modify: `_Rarzrabotki/notebook/knowledge_Balanse/balanse_register.md`
- Modify: `_Rarzrabotki/notebook/knowledge_Balanse/KNOWLEDGE_MAP_BALANSE.md`

- [ ] **Step 1:** `balanse_provedenie_logic.md` — заменить описание монолитного `СформироватьЗапросСверткиБаланса` на модульную (оркестратор `ПровестиБалансСвод` + `Свод_*` per Источник + `СверкаСПАП`); зафиксировать что суммы из ОСНОВНОЙ таблицы ПАП, Источник=`ПАП.Источник`, ВидИсточника-фильтр.

- [ ] **Step 2:** `balanse_mapping_actual_vs_planned.md` — обновить: маппинг теперь = `Перечисление.ИсточникиУправленческогоБаланса` напрямую (расхождение план/факт закрыто новой архитектурой); добавить раздел `Расхождение (Булево)` + правило обрезки/добивки.

- [ ] **Step 3:** `balanse_register.md` — `Source` тип → `ИсточникиУправленческогоБаланса`; +`Расхождение` (Булево); +`ДокументДвижения` (ДокументСсылка); удалён `А_ИсточникБаланса`.

- [ ] **Step 4:** `KNOWLEDGE_MAP_BALANSE.md` — обновить «Поточний стан» (модульная логика v2, дата 2026-05-16), ссылку на спеку/план v2, acceptance == `Отчет.УправленческийБаланс`.

- [ ] **Step 5: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25"
git add _Rarzrabotki/notebook/knowledge_Balanse
git commit -m "knowledge_Balanse: модульная логика v2 + сверка ПАП + Расхождение + acceptance vs УправленческийБаланс"
```

---

## Acceptance-эталон (январь 2026 / ТОВ ІНДАСТРІАЛБУД)

- Документ `А_ФинРез_Баланс` проводится; `Проведен=Истина`; перепроведение идемпотентно (`test_balans_m_post.py`).
- Σ `СуммаКонечныйОстаток` по `Статья` в `А_ОтчетБаланс_Свод` == итоги штатного `Отчет.УправленческийБаланс` до копейки (Оплата труда tol 1.0) — `test_balans_vs_upr_balance_report.py` PASS.
- Σ КонОст всего набора ≈ 0 (Актив=Пассив, закрытый период).
- `Расхождение`: Σ(Истина)+Σ(Ложь) == ПАП; в норме `Расхождение=Истина` только по `Источник=""`/добивке.
- Регресс `test_balans_s4_verify.py` PASS (старый инвариант не сломан).
- Контрольная по всем организациям за январь — 289 064 974,43.

---

## Self-Review

**1. Spec coverage:** DR-1 (Task 5 одна процедура клиенты+поставщики) ✅; DR-2 (основная таблица ПАП, НачОст=Σsigned — Task 5 Step4 запрос) ✅; DR-3 (Task 3 финансист) ✅; DR-4 (Расхождение — Task 3 +Task 11) ✅; DR-5/DR-6 (Task 11 обрезка/добивка/4 ресурса/толеранс) ✅; DR-7 (Task 1 discovery ВидИсточника) ✅; DR-8 (ДокументДвижения=Регистратор — каждая Свод_* Step4) ✅; DR-9 (OD-3 ИсключенныеСтатьи + Свод_ОплатаТруда Task 10) ✅; DR-10 (инвариант Σ==ПАП — Task 11 Step4 + Task 13) ✅; DR-11 (единый ObjectModule #Область — Task 4) ✅; DR-12 (Task 3 чекпойнт финансист, типовые только чтение) ✅; DR-13 (OLAP вне scope — не в задачах) ✅. §6 acceptance vs Отчет.УправленческийБаланс (Task 2 discovery + Task 13) ✅. §9 discovery (Task 1) ✅.

**2. Placeholder scan:** `<все ден.значения из Task1>`/`<прочие из Task1>`/набор ВидИсточника/имена enam `*ПоСрокам` — это **discovery-first из Task 1/2** (Rule #-1, спека §9, прецедент _Fld канона v1.4), НЕ плейсхолдеры: Task 1/2 дают точные имена, каждая Свод_* имеет свой пре-тест с полным кодом дающий точные значения перед BSL. Task 6-10 «полный код по образцу Task 5 Step 2» — указано повторить структуру полностью (не ссылка), образец Task 5 показан целиком. Task 11 СверкаСПАП — алгоритм пошагово (4 пункта) + явный BSL-скелет; ~120 строк помечено как реализация по алгоритму (не TBD — алгоритм полон). Task 13 Step2 — явная подстановка отлаженного Task 2 кода.

**3. Type/naming consistency:** `мТЗСвод` (модульная ТЗ), `Отразить_Свод`, `ПровестиБалансСвод`, `СверкаСПАП`, `ИсключенныеСтатьи()`, `СтатьяОплатаТруда()`, `Свод_РасчетыСПартнерами/СебестоимостьТоваров/ДенежныеСредства/ПрочиеРасходыДоходы/ПрочиеАктивыПассивы_Прямой/ОплатаТруда` — единообразны Task 4↔5-11. Измерения свёртки (Task 4 Step4) = сигнатура регистра Task 3 Step4. Ресурсы `СуммаНачальныйОстаток/Приход/Расход/КонечныйОстаток` единые. `СтруктураШапки` ключи (Ссылка,Дата,НачМес,КонМес,Организация,ВключатьДочерние) единые Task 4↔5-11.

Гепов нет. План готов.
