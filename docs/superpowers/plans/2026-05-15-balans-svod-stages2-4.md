# А_ФинРез_Баланс — Етапи 2-4 (субконто-розшифровка) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Доповнити `Документ.А_ФинРез_Баланс` так, щоб для статей-розшифровок (Етап 2: дебіторка/кредиторка/аванси; Етап 3: товари + грошові; Етап 4: оплата праці) субконто-виміри регістра `А_ОтчетБаланс_Свод` заповнювалися з регістрів-джерел через LEFT JOIN, **не змінюючи суми по статтях** (вони лишаються з ПАП, як в Етапі 1).

**Architecture:** Суми ЗАВЖДИ з `ПрочиеАктивыПассивы.ОстаткиИОбороты` (без UNION). Розшифровка субконто — LEFT JOIN до **предагрегованого** підзапиту регістра-джерела за ключем `Регистратор` (= документ-джерело, механізм штатної `Обработка.ДвиженияАктивовПассивов`). Предагрегація `СГРУППИРОВАТЬ ПО Регистратор` гарантує ≤1 рядок на ключ → захист від бага розмноження (~40М). Кожен етап — окремий цикл Rule #-1 з пост-інваріантом «Σ по Статья == Етап 1 до копійки».

**Tech Stack:** 1С:Підприємство 8.3.20, BSL, Python 3 + win32com (COM `V83.COMConnector`), skills `/cf-validate` `/db-load-xml` `/db-update`, MCP `get_metadata_structure`.

**Reference spec:** `docs/superpowers/specs/2026-05-15-balans-svod-canonical-design.md` (Canonical **v1.4**, §8 + OD-2 РЕЗОЛЮЦІЯ).

**БД:** `Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"` · Організація **ТОВ ІНДАСТРІАЛБУД (ЄДРПОУ 40645273)** · період валідації **січень 2026 (закритий)**.

**Шляхи:** worktree = `C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b`; main config = `C:\Configuration_downloads\BASERP25` (Rule #4: cp у main після кожної правки BSL).

---

## Передумови (хард-ворота, перед БУДЬ-ЯКИМ етапом)

> 🚫 **ХАРД-ЗАБОРОНА (вказівка користувача):** ТИПОВІ регістри конфігурації (`СебестоимостьТоваров`, `РасчетыСКлиентами`, `РасчетыСПоставщиками`, `ПрочиеАктивыПассивы`, `ДенежныеСредстваБезналичные/Наличные`, `АналитикаУчетаПоПартнерам` тощо) — **ТІЛЬКИ ЧИТАННЯ** (SELECT / `.Остатки` / `.Обороты` / LEFT JOIN). **ЗАБОРОНЕНО** будь-які зміни їх структури (meta-edit/XML), рухів, RecordSetModule, ManagerModule. Уся логіка/рухи — ТІЛЬКИ у кастомні `А_*` (документ `А_ФинРез_Баланс` → регістр `А_ОтчетБаланс_Свод`). Якщо крок плану виглядає як зміна типового регістра — це помилка читання: тут лише `LEFT JOIN`/`SELECT`. Сумнів → STOP, ескалація.

1. **Етап 1 у main, 3 тести PASS** (вже виконано: канон v1.4, BSL з фільтром, січень Σ=0.00). Базова `СформироватьЗапросСверткиБаланса` — у `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl`.
2. **Ключ зв'язку (канон OD-2 v1.4):** `ПАП.Регистратор = <ИсточникРегистр>.Регистратор` (НЕ `ИдентификаторФинЗаписи` — його в ПАП немає). Субконто через предагрегований підзапит джерела.
3. **Source=регістр-збагачувач (OD-1 гібрид) — передумова фінансиста:** перед Етапом 3 і 4 фінансист **вручну додає значення** до `Перечисление.А_ИсточникБаланса` (Етап 3: `СебестоимостьТоваров`, `ДенежныеСредстваБезналичные`, `ДенежныеСредстваНаличные`; Етап 4: `ПрочиеРасходы`). Для Етапу 2 значення `РасчетыСКлиентами`/`РасчетыСПоставщиками` уже існують у переліку (перевірено get_metadata_structure Етапу 1). **Перед стартом етапу 3/4 — підтвердження фінансиста + перевірка наявності enum-значень; якщо нема → STOP, ескалація.**
4. **AI НЕ міняє структуру регістра** (17 вимірів, веде фінансист). BSL писати під фактичну структуру з `get_metadata_structure InformationRegisters А_ОтчетБаланс_Свод`.
5. **БД заблокована Конфигуратором** на кроці db-load → STOP, попросити користувача закрити, **не обходити**.
6. **Черговість 2→3→4** (рішення фінансиста — не критична, але кожен стартує лише після PASS попереднього: пост-інваріант Σ).

---

## File Structure

| Файл | Відповідальність | Дія |
|---|---|---|
| `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl` | `СформироватьЗапросСверткиБаланса` — додавання LEFT JOIN предагрег. джерел по етапах | **Modify** (по етапах, інкрементально) |
| `_Rarzrabotki/Python/test/test_balans_s2_pretest.py` | Етап 2: Rule #-1 пре-тест ключа Регистратор↔РасчетыС* + унікальність ОбъектРасчетов | Create |
| `_Rarzrabotki/Python/test/test_balans_s2_verify.py` | Етап 2: проведення + Σ==Етап1 + ОбъектРасчетов заповнено | Create |
| `_Rarzrabotki/Python/test/test_balans_s3_pretest.py` | Етап 3: пре-тест СебестоимостьТоваров/ДенежныеСредства* | Create |
| `_Rarzrabotki/Python/test/test_balans_s3_verify.py` | Етап 3: верифікація | Create |
| `_Rarzrabotki/Python/test/test_balans_s4_pretest.py` | Етап 4: пре-тест зарплатні/ПрочиеРасходы | Create |
| `_Rarzrabotki/Python/test/test_balans_s4_verify.py` | Етап 4: верифікація | Create |

> Тести Етапу 1 (`test_balans_t1/t2/t3`) — НЕ чіпати, вони — регрес-контроль (пост-інваріант: Σ по Статья не змінюється).

---

## ЕТАП 2 — статті 7-10 (РасчетыСКлиентами/РасчетыСПоставщиками → ОбъектРасчетов)

Статті: `ЗадолженностьКлиентов`, `ПолученныеАвансы` (джерело `РегистрНакопления.РасчетыСКлиентами`); `ЗадолженностьПередПоставщиками`, `ВыданныеАвансы` (джерело `РегистрНакопления.РасчетыСПоставщиками`). Ключ = `Регистратор`. Субконто → вимір `ОбъектРасчетов`.

### Task 2.1: Rule #-1 пре-тест ключа Регистратор↔РасчетыС* (RED)

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_s2_pretest.py`

- [ ] **Step 1: Написати пре-тест**

```python
# -*- coding: utf-8 -*-
"""Етап 2 пре-тест (Rule #-1): чи дає ПАП.Регистратор 1:1 з РасчетыСКлиентами/
Поставщиками для статей 7-10, і чи унікальний ОбъектРасчетов на Регистратор.
Період серверно (січень 2026, закритий). Read-only."""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime

CONN = 'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"'
M = datetime(2026, 1, 1, 12, 0, 0)
pythoncom.CoInitialize()
conn = win32com.client.Dispatch("V83.COMConnector").Connect(CONN)

q0 = conn.NewObject("Запрос")
q0.Текст = 'ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Справочник.Организации ГДЕ КодПоЕДРПОУ = "40645273"'
s = q0.Выполнить().Выбрать(); s.Следующий(); org = s.С

qb = conn.NewObject("Запрос")
qb.Текст = "ВЫБРАТЬ НАЧАЛОПЕРИОДА(&М,МЕСЯЦ) КАК НМ, КОНЕЦПЕРИОДА(КОНЕЦПЕРИОДА(&М,МЕСЯЦ),ДЕНЬ) КАК КД"
qb.УстановитьПараметр("М", M)
rb = qb.Выполнить().Выбрать(); rb.Следующий(); НМ = rb.НМ; КД = rb.КД
print(f"період: {НМ} .. {КД}")

# 1) Чи має ПАП.Обороты поле Регистратор (ОстаткиИОбороты може НЕ давати Регистратор —
#    тоді лінкуємо через .Обороты, а суми лишаємо з .ОстаткиИОбороты)
q1 = conn.NewObject("Запрос")
q1.Текст = """ВЫБРАТЬ ПЕРВЫЕ 5 Б.Регистратор КАК Рег, Б.Статья КАК Статья
ИЗ РегистрНакопления.ПрочиеАктивыПассивы.Обороты(&Д1,&Д2,Регистратор,) КАК Б"""
q1.УстановитьПараметр("Д1",НМ); q1.УстановитьПараметр("Д2",КД)
try:
    t1 = q1.Выполнить().Выгрузить()
    print(f"[OK] ПАП.Обороты(...,Регистратор,) має Регистратор, рядків(приклад)={t1.Количество()}")
except Exception as e:
    info = e.excepinfo[2] if hasattr(e,'excepinfo') and e.excepinfo else str(e)
    print(f"[!] ПАП.Обороты з Регистратор: {info}")

# 2) Для статей Етапу 2: скільки ПАП-Регистраторів і чи всі вони є в РасчетыСКлиентами
ART = ["Задолженность клиентов","Полученные авансы"]
qa = conn.NewObject("Запрос")
qa.Текст = """ВЫБРАТЬ Ссылка КАК С, Наименование КАК Н
ИЗ ПланВидовХарактеристик.СтатьиАктивовПассивов ГДЕ Наименование В (&Сп)"""
arr = conn.NewObject("Массив")
for a in ART: arr.Добавить(a)
qa.УстановитьПараметр("Сп", arr)
tza = qa.Выполнить().Выгрузить()
art_refs = conn.NewObject("Массив")
for i in range(tza.Количество()): art_refs.Добавить(tza.Получить(i).С)
print(f"статей Етапу2(клієнти) знайдено: {tza.Количество()}")

q2 = conn.NewObject("Запрос")
q2.Текст = """
ВЫБРАТЬ
  КОЛИЧЕСТВО(РАЗЛИЧНЫЕ Б.Регистратор) КАК РегПАП,
  КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ВЫБОР КОГДА Р.Регистратор ЕСТЬ NULL ТОГДА NULL ИНАЧЕ Б.Регистратор КОНЕЦ) КАК РегЗматч
ИЗ РегистрНакопления.ПрочиеАктивыПассивы.Обороты(&Д1,&Д2,Регистратор,
        Организация=&Орг И Статья В ИЕРАРХИИ(&Арт)) КАК Б
  ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ РАЗЛИЧНЫЕ Регистратор КАК Регистратор
                    ИЗ РегистрНакопления.РасчетыСКлиентами
                    ГДЕ Период МЕЖДУ &Д1 И &Д2) КАК Р
  ПО Б.Регистратор = Р.Регистратор
"""
q2.УстановитьПараметр("Д1",НМ); q2.УстановитьПараметр("Д2",КД)
q2.УстановитьПараметр("Орг",org); q2.УстановитьПараметр("Арт",art_refs)
r2 = q2.Выполнить().Выбрать(); r2.Следующий()
рег = int(r2.РегПАП or 0); матч = int(r2.РегЗматч or 0)
print(f"ПАП-регістраторів(статті клієнтів)={рег}, з них є в РасчетыСКлиентами={матч}")
покриття = (матч/рег) if рег else 0
print(f"покриття Регистратор = {покриття:.1%}")

# 3) Унікальність ОбъектРасчетов на Регистратор (+Организація) у РасчетыСКлиентами
q3 = conn.NewObject("Запрос")
q3.Текст = """
ВЫБРАТЬ Регистратор КАК Рег, КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ОбъектРасчетов) КАК КолОР
ИЗ РегистрНакопления.РасчетыСКлиентами ГДЕ Период МЕЖДУ &Д1 И &Д2
СГРУППИРОВАТЬ ПО Регистратор
"""
q3.УстановитьПараметр("Д1",НМ); q3.УстановитьПараметр("Д2",КД)
t3 = q3.Выполнить().Выгрузить()
много = sum(1 for i in range(t3.Количество()) if int(t3.Получить(i).КолОР) > 1)
print(f"регістраторів РасчетыСКлиентами всього={t3.Количество()}, з >1 ОбъектРасчетов={много}")
print(f">>> ВИСНОВОК: ключ Регистратор {'ПРИДАТНИЙ' if покриття>0.5 else 'НЕ придатний — ескалація фінансисту'};")
print(f"    ОбъектРасчетов унікальний на {(t3.Количество()-много)}/{t3.Количество()} регістраторів "
      f"(решта → заповнювати порожньо за каноном OD-2).")
```

- [ ] **Step 2: Запустити**

Run: `python "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\_Rarzrabotki\Python\test\test_balans_s2_pretest.py"`
Expected: рядок `ВИСНОВОК: ключ Регистратор ПРИДАТНИЙ`, покриття >50%, виведено частку унікальних ОбъектРасчетов.
**Якщо `НЕ придатний`** → STOP, ескалація фінансисту (канон OD-2: альтернатива — `Регистратор + АналитикаУчетаПоПартнерам` як складений ключ; повторити пре-тест зі складеним ключем). НЕ робити silent-fallback.

- [ ] **Step 3: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add _Rarzrabotki/Python/test/test_balans_s2_pretest.py && git commit -m "test: Етап2 пре-тест (Rule #-1) ключ Регистратор↔РасчетыСКлиентами"
```

---

### Task 2.2: ObjectModule — LEFT JOIN предагрег. РасчетыСКлиентами/Поставщиками → ОбъектРасчетов

**Files:**
- Modify: `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl` (функція `СформироватьЗапросСверткиБаланса`)

- [ ] **Step 1: get_metadata_structure — зафіксувати фактичну структуру регістра**

MCP: `get_metadata_structure InformationRegisters А_ОтчетБаланс_Свод`. Expected 17 вимірів (зокрема `ОбъектРасчетов - Справочник.ОбъектыРасчетов`, `Source - Перечисление.А_ИсточникБаланса`). Якщо інакше — STOP, BSL під фактичну.

- [ ] **Step 2: Прочитати поточну `СформироватьЗапросСверткиБаланса` (Етап 1)**

Read `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl`. Базовий запит: `ПАП.ОстаткиИОбороты(&НачалоПериода,&КонецПериода,Авто,,(орг+ВключатьДочерние) И НЕ Статья В ИЕРАРХИИ(&ИсключенныеСтатьи))`, GROUP BY `Б.Организация,Б.Подразделение,Б.Статья,Б.Аналитика`, 17 колонок (Етап 1: `ОбъектРасчетов = ЗНАЧЕНИЕ(Справочник.ОбъектыРасчетов.ПустаяСсылка)`, `Source = ЗНАЧЕНИЕ(Перечисление.А_ИсточникБаланса.ПрочиеАктивыПассивы)`).

- [ ] **Step 3: Переписати запит — додати ВТ-розшифровку РасчетыС* і JOIN по Регистратор**

Замінити тіло функції на двоступінчасту структуру (база ПАП без UNION + предагрег. розшифровка). **УВАГА:** ОстаткиИОбороты не дає Регистратор → додаємо паралельну ВТ `ПАП.Обороты(...,Регистратор,...)` ТІЛЬКИ для маппінгу Регистратор↔(Статья,Аналитика-ключ), а суми лишаються з ОстаткиИОбороты (Step 2 пре-тесту підтвердив наявність Регистратор в .Обороты).

```bsl
Функция СформироватьЗапросСверткиБаланса()
	Возврат
	"// 1. РОЗШИФРОВКА Етап2: предагрег. РасчетыСКлиентами/Поставщиками по Регистратору
	|ВЫБРАТЬ
	|	Р.Регистратор КАК Регистратор,
	|	МИНИМУМ(Р.ОбъектРасчетов) КАК ОбъектРасчетов,
	|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ Р.ОбъектРасчетов) КАК КолОР
	|ПОМЕСТИТЬ втРасчКл
	|ИЗ РегистрНакопления.РасчетыСКлиентами КАК Р
	|ГДЕ Р.Период МЕЖДУ &НачалоПериода И &КонецПериода
	|СГРУППИРОВАТЬ ПО Р.Регистратор
	|;
	|ВЫБРАТЬ
	|	Р.Регистратор КАК Регистратор,
	|	МИНИМУМ(Р.ОбъектРасчетов) КАК ОбъектРасчетов,
	|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ Р.ОбъектРасчетов) КАК КолОР
	|ПОМЕСТИТЬ втРасчПост
	|ИЗ РегистрНакопления.РасчетыСПоставщиками КАК Р
	|ГДЕ Р.Период МЕЖДУ &НачалоПериода И &КонецПериода
	|СГРУППИРОВАТЬ ПО Р.Регистратор
	|;
	|// 2. Регистратор↔(Статья) з ПАП.Обороты (ОстаткиИОбороты не дає Регистратор)
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	Б.Регистратор КАК Регистратор,
	|	Б.Статья КАК Статья,
	|	Б.Организация КАК Организация,
	|	Б.Подразделение КАК Подразделение,
	|	Б.Аналитика КАК Аналитика
	|ПОМЕСТИТЬ втРегПАП
	|ИЗ РегистрНакопления.ПрочиеАктивыПассивы.Обороты(
	|		&НачалоПериода, &КонецПериода, Регистратор,
	|		(ВЫБОР КОГДА &ВключатьДочерние
	|			  ТОГДА Организация В ИЕРАРХИИ (&Организация)
	|			  ИНАЧЕ Организация = &Организация КОНЕЦ)
	|		И НЕ Статья В ИЕРАРХИИ (&ИсключенныеСтатьи)) КАК Б
	|;
	|// 3. ОбъектРасчетов на (Организация,Подразделение,Статья,Аналитика) —
	|//    лише коли унікальний (канон OD-2: інакше порожньо)
	|ВЫБРАТЬ
	|	РП.Организация КАК Организация,
	|	РП.Подразделение КАК Подразделение,
	|	РП.Статья КАК Статья,
	|	РП.Аналитика КАК Аналитика,
	|	МИНИМУМ(ЕСТЬNULL(К.ОбъектРасчетов, П.ОбъектРасчетов)) КАК ОбъектРасчетов,
	|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ЕСТЬNULL(К.ОбъектРасчетов, П.ОбъектРасчетов)) КАК КолОР,
	|	МАКСИМУМ(ВЫБОР КОГДА К.Регистратор ЕСТЬ NULL ТОГДА 0 ИНАЧЕ 1 КОНЕЦ) КАК ЕстьКл,
	|	МАКСИМУМ(ВЫБОР КОГДА П.Регистратор ЕСТЬ NULL ТОГДА 0 ИНАЧЕ 1 КОНЕЦ) КАК ЕстьПост
	|ПОМЕСТИТЬ втОР
	|ИЗ втРегПАП КАК РП
	|	ЛЕВОЕ СОЕДИНЕНИЕ втРасчКл КАК К ПО РП.Регистратор = К.Регистратор
	|	ЛЕВОЕ СОЕДИНЕНИЕ втРасчПост КАК П ПО РП.Регистратор = П.Регистратор
	|СГРУППИРОВАТЬ ПО РП.Организация, РП.Подразделение, РП.Статья, РП.Аналитика
	|;
	|// 4. ОСНОВНИЙ: суми з ОстаткиИОбороты (Етап1) + LEFT JOIN втОР
	|ВЫБРАТЬ
	|	Б.Организация КАК Организация,
	|	Б.Подразделение КАК Подразделение,
	|	Б.Статья КАК Статья,
	|	ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.Номенклатура)        КАК Номенклатура,
	|	ЕСТЬNULL(ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.Контрагенты),
	|	         ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.Организации)) КАК Контрагент,
	|	ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.Партнеры)             КАК Партнер,
	|	ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.Склады)               КАК Склад,
	|	ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.ОбъектыЭксплуатации)  КАК ОбъектыЭксплуатации,
	|	ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.ФизическиеЛица)       КАК ФизическоеЛицо,
	|	ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.БанковскиеСчетаОрганизаций) КАК ДенежныеСредства,
	|	ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.НематериальныеАктивы) КАК НематериальныйАктив,
	|	ЗНАЧЕНИЕ(Справочник.ДоговорыКонтрагентов.ПустаяСсылка)    КАК Договор,
	|	ВЫБОР КОГДА ОР.КолОР = 1 ТОГДА ЕСТЬNULL(ОР.ОбъектРасчетов, ЗНАЧЕНИЕ(Справочник.ОбъектыРасчетов.ПустаяСсылка))
	|	     ИНАЧЕ ЗНАЧЕНИЕ(Справочник.ОбъектыРасчетов.ПустаяСсылка) КОНЕЦ КАК ОбъектРасчетов,
	|	ВЫБОР
	|		КОГДА ЕСТЬNULL(ОР.ЕстьКл,0)=1 ТОГДА ЗНАЧЕНИЕ(Перечисление.А_ИсточникБаланса.РасчетыСКлиентами)
	|		КОГДА ЕСТЬNULL(ОР.ЕстьПост,0)=1 ТОГДА ЗНАЧЕНИЕ(Перечисление.А_ИсточникБаланса.РасчетыСПоставщиками)
	|		ИНАЧЕ ЗНАЧЕНИЕ(Перечисление.А_ИсточникБаланса.ПрочиеАктивыПассивы)
	|	КОНЕЦ КАК Source,
	|	"""" КАК Аналитика1,
	|	"""" КАК Аналитика2,
	|	ПРЕДСТАВЛЕНИЕ(Б.Аналитика) КАК Аналитика3,
	|	СУММА(Б.СуммаНачальныйОстаток) КАК СуммаНачальныйОстаток,
	|	СУММА(Б.СуммаПриход)           КАК СуммаПриход,
	|	СУММА(Б.СуммаРасход)           КАК СуммаРасход,
	|	СУММА(Б.СуммаКонечныйОстаток)  КАК СуммаКонечныйОстаток
	|ИЗ РегистрНакопления.ПрочиеАктивыПассивы.ОстаткиИОбороты(
	|		&НачалоПериода, &КонецПериода, Авто, ,
	|		(ВЫБОР КОГДА &ВключатьДочерние
	|			  ТОГДА Организация В ИЕРАРХИИ (&Организация)
	|			  ИНАЧЕ Организация = &Организация КОНЕЦ)
	|		И НЕ Статья В ИЕРАРХИИ (&ИсключенныеСтатьи)) КАК Б
	|	ЛЕВОЕ СОЕДИНЕНИЕ втОР КАК ОР
	|		ПО Б.Организация = ОР.Организация
	|			И Б.Подразделение = ОР.Подразделение
	|			И Б.Статья = ОР.Статья
	|			И Б.Аналитика = ОР.Аналитика
	|СГРУППИРОВАТЬ ПО
	|	Б.Организация, Б.Подразделение, Б.Статья, Б.Аналитика,
	|	ОР.КолОР, ОР.ОбъектРасчетов, ОР.ЕстьКл, ОР.ЕстьПост
	|ИМЕЮЩИЕ НЕ (СУММА(Б.СуммаНачальныйОстаток)=0 И СУММА(Б.СуммаПриход)=0
	|		И СУММА(Б.СуммаРасход)=0 И СУММА(Б.СуммаКонечныйОстаток)=0)";
КонецФункции
```

> ⚠️ **Якщо пре-тест (Task 2.1) показав, що `ПАП.Обороты(...,Регистратор,)` НЕ дає Регистратор** — STOP, ескалація: лінк через `АналитикаУчетаПоПартнерам` (канон OD-2 альтернатива). Не імпровізувати.

- [ ] **Step 4: Copy back у main config (Rule #4)**

```bash
cp "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b/Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl" "C:/Configuration_downloads/BASERP25/Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl"
```

- [ ] **Step 5: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl && git commit -m "feat: Етап2 — LEFT JOIN предагрег. РасчетыС* → ОбъектРасчетов (ключ Регистратор)"
```

---

### Task 2.3: Завантажити в БД

**Files:** (операції з БД; Конфигуратор закритий — інакше STOP)

- [ ] **Step 1: cf-validate**

Run:
```powershell
powershell.exe -NoProfile -File "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\.claude\skills\cf-validate\scripts\cf-validate.ps1" -ConfigPath "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b" -MaxErrors 30
```
Expected: `0 errors`. Помилка в BSL → виправити Task 2.2 Step 3, повторити.

- [ ] **Step 2: db-load-xml Partial**

Run:
```powershell
powershell.exe -NoProfile -File "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\.claude\skills\db-load-xml\scripts\db-load-xml.ps1" -V8Path "C:\Program Files\1cv8\8.3.20.1914\bin" -InfoBaseServer "SQLSERVER" -InfoBaseRef "BaseERP" -UserName "Администратор" -Password "24043" -ConfigDir "C:\Configuration_downloads\BASERP25" -Mode Partial -Files "Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl"
```
Expected: `Load completed successfully`. «База заблокована» → STOP, просити закрити Конфигуратор.

- [ ] **Step 3: db-update Dynamic+**

Run:
```powershell
powershell.exe -NoProfile -File "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\.claude\skills\db-update\scripts\db-update.ps1" -V8Path "C:\Program Files\1cv8\8.3.20.1914\bin" -InfoBaseServer "SQLSERVER" -InfoBaseRef "BaseERP" -UserName "Администратор" -Password "24043" -Dynamic "+"
```
Expected: `Обновление конфигурации успешно завершено`.

---

### Task 2.4: Test — проведення + пост-інваріант Σ + ОбъектРасчетов заповнено (GREEN)

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_s2_verify.py`

- [ ] **Step 1: Написати тест**

```python
# -*- coding: utf-8 -*-
"""Етап 2 верифікація: провести січень, Σ по Статья == Етап 1 (до копійки),
ОбъектРасчетов заповнено для статей 7-10, Актив=Пасив Σ=0.00."""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime

CONN = 'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"'
M = datetime(2026, 1, 1, 12, 0, 0)
DDATE = datetime(2026, 1, 31, 14, 0, 0)
TOL = 0.01
pythoncom.CoInitialize()
conn = win32com.client.Dispatch("V83.COMConnector").Connect(CONN)

q0 = conn.NewObject("Запрос")
q0.Текст = 'ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Справочник.Организации ГДЕ КодПоЕДРПОУ = "40645273"'
s = q0.Выполнить().Выбрать(); s.Следующий(); org = s.С

# Σ по Статья ДО (з регістра від поточного проведеного доку — еталон Етапу 1)
def sums_by_article():
    q = conn.NewObject("Запрос")
    q.Текст = """ВЫБРАТЬ ПРЕДСТАВЛЕНИЕ(Статья) КАК Н, СУММА(СуммаКонечныйОстаток) КАК sK
    ИЗ РегистрСведений.А_ОтчетБаланс_Свод СГРУППИРОВАТЬ ПО ПРЕДСТАВЛЕНИЕ(Статья)"""
    t = q.Выполнить().Выгрузить()
    return {str(t.Получить(i).Н): float(t.Получить(i).sK or 0) for i in range(t.Количество())}

before = sums_by_article()
print(f"еталон (Етап1) статей={len(before)}, Σ={sum(before.values()):,.2f}")

# Перепровести (BSL Етапу2 вже в БД)
q = conn.NewObject("Запрос")
q.Текст = "ВЫБРАТЬ Ссылка КАК С ИЗ Документ.А_ФинРез_Баланс"
tz = q.Выполнить().Выгрузить()
for i in range(tz.Количество()): tz.Получить(i).С.ПолучитьОбъект().Удалить()
doc = conn.Документы.А_ФинРез_Баланс.СоздатьДокумент()
doc.Дата=DDATE; doc.Организация=org; doc.Месяц=M; doc.ВключатьДочерние=False
mode = conn.PredefinedValue("РежимЗаписиДокумента.Проведение")
doc.Записать(mode)
assert doc.Проведен, "FAIL: не проведено"
ref = doc.Ссылка
print(f"[OK] проведено №{doc.Номер}")

after = sums_by_article()

# Пост-інваріант: Σ по кожній статті НЕ змінилась
diffs = [(k, before.get(k,0), after.get(k,0)) for k in set(before)|set(after)
         if abs(before.get(k,0)-after.get(k,0)) > TOL]
if diffs:
    print(f"FAIL: суми змінились у {len(diffs)} статтях (Етап2 НЕ має чіпати суми):")
    for k,a,b in diffs[:10]: print(f"  {k[:44]:<46} було={a:>16,.2f} стало={b:>16,.2f}")
    sys.exit(1)
print(f"[OK] пост-інваріант: Σ по статтях не змінилась")

# Актив=Пасив
assert abs(sum(after.values())) < 1.0, f"FAIL: Актив≠Пасив Σ={sum(after.values()):,.2f}"

# ОбъектРасчетов заповнено для статей 7-10
q3 = conn.NewObject("Запрос")
q3.Текст = """ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК К
ИЗ РегистрСведений.А_ОтчетБаланс_Свод
ГДЕ Регистратор=&Д И ОбъектРасчетов <> ЗНАЧЕНИЕ(Справочник.ОбъектыРасчетов.ПустаяСсылка)"""
q3.УстановитьПараметр("Д", ref)
r3 = q3.Выполнить().Выбрать(); r3.Следующий()
зап = int(r3.К)
print(f"рядків з заповненим ОбъектРасчетов = {зап}")
assert зап > 0, "FAIL: ОбъектРасчетов не заповнено жодного разу (Етап2 не спрацював)"
print("PASS Етап 2")
```

- [ ] **Step 2: Запустити**

Run: `python "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\_Rarzrabotki\Python\test\test_balans_s2_verify.py"`
Expected: `[OK] пост-інваріант: Σ по статтях не змінилась`, `рядків з заповненим ОбъектРасчетов > 0`, `PASS Етап 2`.
**Якщо «суми змінились»** → відкат: JOIN розмножив рядки. Перевірити що втРасчКл/втРасчПост предагреговані (СГРУППИРОВАТЬ ПО Регистратор) і JOIN по (Орг,Подр,Статья,Аналитика) не дублює. Виправити Task 2.2 → Task 2.3 → Task 2.4 повторно.

- [ ] **Step 3: Регрес Етапу 1 (Test 3 не зламано)**

Run: `python "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\_Rarzrabotki\Python\test\test_balans_t3_vs_pap.py"`
Expected: `PASS Test 3 — ... Актив=Пасив (Σ регістра=0.00 ...)` (суми по статтях == ПАП без змін).

- [ ] **Step 4: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add _Rarzrabotki/Python/test/test_balans_s2_verify.py && git commit -m "test: Етап2 PASS — ОбъектРасчетов заповнено, Σ по статтях == Етап1"
```

---

## ЕТАП 3 — статті 11-15 (СебестоимостьТоваров → Номенклатура/Склад; ДенежныеСредства* → ДенежныеСредства)

> **Передумова (хард):** фінансист **додав** значення `Перечисление.А_ИсточникБаланса`: `СебестоимостьТоваров`, `ДенежныеСредстваБезналичные`, `ДенежныеСредстваНаличные`. Task 3.1 Step 1 перевіряє — якщо нема → STOP, ескалація.

### Task 3.1: Вивчити блоки ДвиженияАктивовПассивов + Rule #-1 пре-тест (RED)

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_s3_pretest.py`

- [ ] **Step 1: Перевірити enum-передумову**

MCP `get_metadata_structure InformationRegisters А_ОтчетБаланс_Свод` → у типі виміру `Source` мають бути значення `СебестоимостьТоваров, ДенежныеСредстваБезналичные, ДенежныеСредстваНаличные`. **Нема → STOP, ескалація фінансисту (OD-1 передумова), Етап 3 не починати.**

- [ ] **Step 2: Вивчити логіку джерел Етапу 3 у штатній обробці**

Grep: у `C:\Configuration_downloads\BASERP25\DataProcessors\ДвиженияАктивовПассивов\Ext\ManagerModule.bsl` знайти процедури/блоки для `СебестоимостьТоваров` та грошових (патерн пошуку: `СебестоимостьТоваров`, `ДенежныеСредства`, `ДобавитьТекстЗапроса`). Прочитати знайдені блоки (offset±80 рядків). Зафіксувати: яке поле джерела дає Номенклатура/Склад (для товарів) і Касса/БанкСчет (для грошей), і чи ключ так само `Регистратор`.

- [ ] **Step 3: Написати пре-тест за знайденою логікою**

```python
# -*- coding: utf-8 -*-
"""Етап 3 пре-тест (Rule #-1): СебестоимостьТоваров→Номенклатура/Склад,
ДенежныеСредстваБезналичные/Наличные→ДенежныеСредства. Ключ Регистратор.
Поля джерел — за результатом вивчення ManagerModule (Step 2). Read-only."""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime
CONN = 'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"'
M = datetime(2026, 1, 1, 12, 0, 0)
pythoncom.CoInitialize()
conn = win32com.client.Dispatch("V83.COMConnector").Connect(CONN)
q0 = conn.NewObject("Запрос")
q0.Текст = 'ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Справочник.Организации ГДЕ КодПоЕДРПОУ = "40645273"'
s = q0.Выполнить().Выбрать(); s.Следующий(); org = s.С
qb = conn.NewObject("Запрос")
qb.Текст = "ВЫБРАТЬ НАЧАЛОПЕРИОДА(&М,МЕСЯЦ) КАК НМ, КОНЕЦПЕРИОДА(КОНЕЦПЕРИОДА(&М,МЕСЯЦ),ДЕНЬ) КАК КД"
qb.УстановитьПараметр("М", M); rb=qb.Выполнить().Выбрать(); rb.Следующий(); НМ=rb.НМ; КД=rb.КД
# Структура джерел (підтвердити поля Номенклатура/Склад/Касса/БанкСчет + Регистратор)
for reg in ["СебестоимостьТоваров","ДенежныеСредстваБезналичные","ДенежныеСредстваНаличные"]:
    q = conn.NewObject("Запрос")
    q.Текст = f"ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК К, КОЛИЧЕСТВО(РАЗЛИЧНЫЕ Регистратор) КАК Рег ИЗ РегистрНакопления.{reg} ГДЕ Период МЕЖДУ &Д1 И &Д2"
    q.УстановитьПараметр("Д1",НМ); q.УстановитьПараметр("Д2",КД)
    try:
        r=q.Выполнить().Выбрать(); r.Следующий()
        print(f"[OK] {reg}: рядків={int(r.К)}, регістраторів={int(r.Рег)}")
    except Exception as e:
        info = e.excepinfo[2] if hasattr(e,'excepinfo') and e.excepinfo else str(e)
        print(f"[FAIL] {reg}: {info}")
print(">>> Зафіксувати у Task 3.2: точні поля субконто (Номенклатура/Склад/ДенСр) "
      "і ключ Регистратор за логікою ManagerModule. Якщо ключ не Регистратор — ескалація.")
```

- [ ] **Step 4: Запустити + Commit**

Run: `python "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\_Rarzrabotki\Python\test\test_balans_s3_pretest.py"`
Expected: `[OK]` по 3 регістрах, зафіксовані поля субконто.
```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add _Rarzrabotki/Python/test/test_balans_s3_pretest.py && git commit -m "test: Етап3 пре-тест джерел СебестоимостьТоваров/ДенежныеСредства*"
```

### Task 3.2: ObjectModule — LEFT JOIN предагрег. СебестоимостьТоваров/ДенежныеСредства*

**Files:** Modify `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl`

- [ ] **Step 1: Додати ВТ-розшифровки за патерном Етапу 2**

Додати у `СформироватьЗапросСверткиБаланса` (за зразком Task 2.2 Step 3) предагреговані ВТ:
- `втСебест`: `ВЫБРАТЬ Регистратор, МИНИМУМ(Номенклатура) КАК Номенклатура, МИНИМУМ(Склад) КАК Склад, КОЛИЧЕСТВО(РАЗЛИЧНЫЕ Номенклатура) КАК КолНом ПОМЕСТИТЬ втСебест ИЗ РегистрНакопления.СебестоимостьТоваров ГДЕ Период МЕЖДУ &НачалоПериода И &КонецПериода СГРУППИРОВАТЬ ПО Регистратор` (точні імена полів — з Task 3.1 Step 2; якщо складова аналітика — за логікою ManagerModule).
- `втДенСр`: аналогічно з `ДенежныеСредстваБезналичные`+`ДенежныеСредстваНаличные` (ОБ'ЄДНАТЬ ВСЕ), поле каси/банкрахунку → composite-вимір `ДенежныеСредства`.
- LEFT JOIN до `втРегПАП` по `Регистратор` (як Етап 2), у фінальному SELECT: `Номенклатура`/`Склад` для статті `Товары на оптовых складах`; `ДенежныеСредства = ВЫРАЗИТЬ(... КАК Справочник.БанковскиеСчетаОрганизаций)` / Касса для грошових статей; `Source` = `ЗНАЧЕНИЕ(Перечисление.А_ИсточникБаланса.СебестоимостьТоваров / ДенежныеСредстваБезналичные / ДенежныеСредстваНаличные)` за тим, з якого ВТ прийшов рядок (ВЫБОР по ЕстьСебест/ЕстьДенСрБ/ЕстьДенСрН як у Етапі 2). **Суми — лишаються з ОстаткиИОбороты (без змін).** ОбъектРасчетов-гілку Етапу 2 — зберегти.

- [ ] **Step 2: cp у main (Rule #4) + Commit**

```bash
cp "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b/Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl" "C:/Configuration_downloads/BASERP25/Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl"
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl && git commit -m "feat: Етап3 — LEFT JOIN предагрег. СебестоимостьТоваров/ДенежныеСредства*"
```

### Task 3.3: Завантажити в БД

- [ ] **Step 1-3:** cf-validate → db-load-xml Partial (`Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl`) → db-update Dynamic+ — команди ідентичні Task 2.3 Step 1-3. Expected: `0 errors`, `Load completed successfully`, `Обновление конфигурации успешно завершено`.

### Task 3.4: Тест Етапу 3 + регрес

**Files:** Create `_Rarzrabotki/Python/test/test_balans_s3_verify.py`

- [ ] **Step 1: Написати тест** — копія `test_balans_s2_verify.py` (Task 2.4 Step 1) з заміною фінальної перевірки заповнення на:
```python
q3.Текст = """ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК К
ИЗ РегистрСведений.А_ОтчетБаланс_Свод
ГДЕ Регистратор=&Д И (Номенклатура <> ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка)
                   ИЛИ Склад <> ЗНАЧЕНИЕ(Справочник.Склады.ПустаяСсылка)
                   ИЛИ ДенежныеСредства <> НЕОПРЕДЕЛЕНО)"""
```
решта (пост-інваріант Σ==Етап1, Актив=Пасив Σ<1.0) — без змін.

- [ ] **Step 2: Запустити + регрес Етапів 1-2 + Commit**

Run послідовно: `test_balans_s3_verify.py`, потім `test_balans_t3_vs_pap.py` (Етап1 регрес), потім `test_balans_s2_verify.py` (Етап2 регрес).
Expected: `PASS Етап 3`; обидва регреси PASS (Σ по статтях не змінилась, ОбъектРасчетов досі заповнено).
```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add _Rarzrabotki/Python/test/test_balans_s3_verify.py && git commit -m "test: Етап3 PASS — Номенклатура/Склад/ДенСр заповнено, Σ незмінна"
```

---

## ЕТАП 4 — стаття 16 (Оплата труда → зарплатні/ПрочиеРасходы → ФизическоеЛицо)

> **Передумова (хард):** фінансист додав `Перечисление.А_ИсточникБаланса.ПрочиеРасходы` (та зарплатне джерело, якщо інше). Task 4.1 Step 1 перевіряє — нема → STOP.

### Task 4.1: Вивчити блок оплати праці + Rule #-1 пре-тест (RED)

**Files:** Create `_Rarzrabotki/Python/test/test_balans_s4_pretest.py`

- [ ] **Step 1: Enum-передумова** — MCP get_metadata_structure: у `Source` має бути `ПрочиеРасходы` (і зарплатне джерело). Нема → STOP, ескалація.

- [ ] **Step 2: Вивчити блок ManagerModule** — Grep у `DataProcessors/ДвиженияАктивовПассивов/Ext/ManagerModule.bsl`: `ОплатаТруда`, `ЗарплатаКВыплате`, `ПрочиеРасходы`, `ФизическоеЛицо`. Прочитати знайдені блоки. Зафіксувати джерело-регістр статті «Оплата труда» (ймовірно `РегистрНакопления.ЗарплатаКВыплате` або `ПрочиеРасходы`), поле що дає `ФизическоеЛицо`, і ключ.

- [ ] **Step 3: Пре-тест** — за зразком Task 3.1 Step 3, для зафіксованого джерела: КОЛИЧЕСТВО(*), КОЛИЧЕСТВО(РАЗЛИЧНЫЕ Регистратор), наявність поля ФизическоеЛицо. Expected `[OK]`. Якщо ключ ≠ Регистратор → ескалація (канон OD-2).

- [ ] **Step 4: Запустити + Commit**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add _Rarzrabotki/Python/test/test_balans_s4_pretest.py && git commit -m "test: Етап4 пре-тест джерела Оплата труда → ФизическоеЛицо"
```

### Task 4.2: ObjectModule — LEFT JOIN предагрег. зарплатне джерело → ФизическоеЛицо

**Files:** Modify `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl`

- [ ] **Step 1:** Додати ВТ `втОплТруда`: `ВЫБРАТЬ Регистратор, МИНИМУМ(ФизическоеЛицо) КАК ФизическоеЛицо, КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ФизическоеЛицо) КАК КолФЛ ПОМЕСТИТЬ втОплТруда ИЗ РегистрНакопления.<ДжерелоЗТаск4.1> ГДЕ Период МЕЖДУ &НачалоПериода И &КонецПериода СГРУППИРОВАТЬ ПО Регистратор` (точне ім'я регістра/поля — з Task 4.1 Step 2). LEFT JOIN до `втРегПАП` по `Регистратор`; у фінальному SELECT для статті «Оплата труда»: `ФизическоеЛицо` з `втОплТруда` (коли `КолФЛ=1`, інакше порожньо), `Source = ЗНАЧЕНИЕ(Перечисление.А_ИсточникБаланса.ПрочиеРасходы)`. Гілки Етапів 2-3 зберегти. Суми — без змін з ОстаткиИОбороты.

- [ ] **Step 2: cp у main (Rule #4) + Commit**

```bash
cp "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b/Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl" "C:/Configuration_downloads/BASERP25/Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl"
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl && git commit -m "feat: Етап4 — LEFT JOIN предагрег. зарплатне джерело → ФизическоеЛицо"
```

### Task 4.3: Завантажити в БД

- [ ] **Step 1-3:** cf-validate → db-load-xml Partial → db-update Dynamic+ — команди ідентичні Task 2.3 Step 1-3. Expected: `0 errors`, `Load completed successfully`, `Обновление конфигурации успешно завершено`.

### Task 4.4: Тест Етапу 4 + повний регрес

**Files:** Create `_Rarzrabotki/Python/test/test_balans_s4_verify.py`

- [ ] **Step 1: Написати тест** — копія `test_balans_s2_verify.py` з фінальною перевіркою:
```python
q3.Текст = """ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК К ИЗ РегистрСведений.А_ОтчетБаланс_Свод
ГДЕ Регистратор=&Д И ФизическоеЛицо <> ЗНАЧЕНИЕ(Справочник.ФизическиеЛица.ПустаяСсылка)"""
```
пост-інваріант Σ==Етап1 + Актив=Пасив — без змін.

- [ ] **Step 2: Запустити + ПОВНИЙ регрес + Commit**

Run послідовно: `test_balans_s4_verify.py`, потім регрес `test_balans_t3_vs_pap.py`, `test_balans_s2_verify.py`, `test_balans_s3_verify.py`.
Expected: `PASS Етап 4`; усі 3 регреси PASS (Σ по статтях == ПАП незмінна; ОбъектРасчетов/Номенклатура/Склад/ДенСр досі заповнені).
```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add _Rarzrabotki/Python/test/test_balans_s4_verify.py && git commit -m "test: Етап4 PASS — ФизическоеЛицо заповнено, повний регрес OK"
```

---

## Self-Review

**1. Spec coverage (канон v1.4):**
- §8 Етап 2 (статті 7-10 → ОбъектРасчетов, ключ Регистратор) → Task 2.1-2.4 ✅
- §8 Етап 3 (11-15 → Номенклатура/Склад/ДенежныеСредства) → Task 3.1-3.4 ✅
- §8 Етап 4 (16 → ФизическоеЛицо) → Task 4.1-4.4 ✅
- OD-2 ключ Регистратор (НЕ ИдентификаторФинЗаписи), пер-етапний Python пре-тест → Task X.1 кожного етапу ✅
- OD-2 предагрег. підзапит (≤1 рядок/ключ), пост-інваріант Σ==Етап1 → втРасчКл/втРасчПост/втСебест/… `СГРУППИРОВАТЬ ПО Регистратор`; Task X.4 пост-інваріант + регрес ✅
- OD-1 Source=регістр-збагачувач + enum-передумова фінансиста → Передумови п.3, Task 3.1/4.1 Step 1 (STOP якщо нема) ✅
- OD-3 фільтр 3 груп зберігається → базовий запит зберігає `НЕ Статья В ИЕРАРХИИ(&ИсключенныеСтатьи)` у всіх етапах ✅
- OD-5 період січень серверно, Актив=Пасив Σ=0.00 → усі тести `НАЧАЛОПЕРИОДА/КОНЕЦПЕРИОДА`, перевірка Σ<1.0 ✅
- ОбъектРасчетов унікальний на регістратор (інакше порожньо) → `ВЫБОР КОГДА КолОР=1 …` Task 2.2 ✅
- Структура регістра не міняється AI → Task 2.2 Step 1 get_metadata_structure, BSL під фактичну ✅
- Rule #4 cp у main кожного етапу → Task 2.2/3.2/4.2 ✅; БД-блок Конфигуратором STOP → Task 2.3 ✅

**2. Placeholder scan:** Етап 2 — повний конкретний BSL (ключ відомий з коду обробки). Етапи 3-4: точні поля джерел свідомо визначаються Task 3.1/4.1 Step 2 (вивчення ManagerModule) + пре-тест — це **мандат канону (per-stage Rule #-1 gate)**, не placeholder: патерн (предагрег. ВТ + LEFT JOIN по Регистратор) конкретний і повторюваний з Етапу 2; невідомі лише точні імена полів конкретного регістра-джерела, які Rule #-1 зобов'язує перевірити на реальній БД до BSL. Імена регістрів-джерел конкретні (канон §4). Жодних TBD/«error handling»/«similar to».

**3. Type consistency:** `втРегПАП`/`втРасчКл`/`втРасчПост`/`втОР`/`втСебест`/`втДенСр`/`втОплТруда` — узгоджені; ключ JOIN скрізь `Регистратор` (+Орг/Подр/Статья/Аналитика для маппінгу субконто); `Source` значення з `Перечисление.А_ИсточникБаланса` (Етап2: РасчетыСКлиентами/РасчетыСПоставщиками існують; Етап3-4: передумова фінансиста); 17 вимірів регістра — імена як в Етапі 1 (`ОбъектРасчетов`, `Номенклатура`, `Склад`, `ДенежныеСредства`, `ФизическоеЛицо`). Пост-інваріант `sums_by_article()` однаковий у s2/s3/s4 verify.

Гепів немає (Етапи 3-4 — Rule #-1 discovery-first за дизайном канону). План готовий.
