# _ОЗП_СоздатьВКассуИзФ2 — пропуск ВКассу с А_Необновлять=Истина

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Чтобы повторный запуск кнопки «Создать ВКассу по отражению ЗП» в форме `А_ОтражениеЗПпоКазне` пропускал ВКассу с `А_Необновлять = Истина` (уже отражённые в проведённом РКО) и явно сообщал об этом в логе.

**Architecture:** Внутри `_ОЗП_СоздатьВКассуИзФ2` ставится ранний-возврат гард после Find. Процедура превращается в Функцию со строковым кодом результата (`"Создано" / "Обновлено" / "Пропущено" / "Ошибка"`). Вызывающая агрегирует код в три счётчика (`КолОбработано / КолОшибок / КолПропущеноБлок`) и расширяет итоговую строку.

**Tech Stack:** BSL (1С:Предприятие 8.3, BAS ERP 2.5), Python 3 через `V83.COMConnector` (Rule #-1 предчек), MCP-skills `db-load-xml` + `db-update` для перезагрузки конфигурации.

**Spec:** [docs/superpowers/specs/2026-05-29-ozp-sozdat-vkassu-iz-f2-skip-neobnovlyat-design.md](../specs/2026-05-29-ozp-sozdat-vkassu-iz-f2-skip-neobnovlyat-design.md)

---

## File Structure

| Файл | Действие | Ответственность |
|------|----------|-----------------|
| `_Rarzrabotki/Python/test/test_ozp_skip_neobnovlyat.py` | **Create** | Python-предчек: убедиться что запрос с `А_Необновлять` принимается ERP и что в эталонных Ф2 есть хотя бы одна ВКассу с `А_Необновлять = Истина` (есть на чём smoke-тестировать). |
| `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` | **Modify** (стр. 2686-2719 — вызывающая, стр. 2723-2904 — Функция) | (1) Find-or-create запрос расширить полем `А_Необновлять`. (2) Гард на флаг. (3) Смена `Процедура → Функция`, расстановка `Возврат "Код"`. (4) В вызывающей — счётчик `КолПропущеноБлок` + switch + расширение итоговой строки. |

**Не трогаем:** `Documents/А_ОтражениеЗПпоКазне.xml` (реквизиты не меняются), `Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml` (поле уже существует), `CommonModules/А_СобытияОбъектов/*` (не наш scope).

---

## Task 1: Python-предчек запроса (Rule #-1)

**Files:**
- Create: `_Rarzrabotki/Python/test/test_ozp_skip_neobnovlyat.py`

**Цель:** до правки BSL прогнать на живой ERP именно тот запрос, который пойдёт в код, и убедиться что (а) поле `А_Необновлять` возвращается без «Поле не найдено», (б) в эталоне есть хотя бы одна ВКассу с `А_Необновлять = Истина` (иначе smoke не на чем проверить).

- [ ] **Step 1.1: Создать Python-тест**

Создать файл `C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_ozp_skip_neobnovlyat.py` со следующим содержимым:

```python
"""Pre-check для дизайна 2026-05-29: пропуск ВКассу с А_Необновлять=Истина
в процедуре _ОЗП_СоздатьВКассуИзФ2 документа А_ОтражениеЗПпоКазне.

Что проверяет:
1. Запрос с полем А_Необновлять принимается ERP (нет "Поле не найдено").
2. Для эталона А_ОтражениеЗПпоКазне №000000001 от 31.12.2025 печатает
   текущее состояние всех Ф2 → ВКассу и прогноз кода результата.
3. Сводка: сколько Ф2 даст "Создано" / "Обновлено" / "Пропущено".
"""

import sys
import win32com.client

if sys.stdout.encoding != "utf-8":
    sys.stdout.reconfigure(encoding="utf-8")

CONN_ERP = 'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"'

v8 = win32com.client.Dispatch("V83.COMConnector")
erp = v8.Connect(CONN_ERP)

print("=" * 70)
print("PRE-CHECK: _ОЗП_СоздатьВКассуИзФ2 — пропуск А_Необновлять=Истина")
print("=" * 70)

# 1. Найти эталон А_ОтражениеЗПпоКазне №000000001 от 31.12.2025
q_etalon = erp.NewObject("Запрос")
q_etalon.Text = """
ВЫБРАТЬ ПЕРВЫЕ 1
    Д.Ссылка КАК Ссылка,
    Д.Номер  КАК Номер,
    Д.Дата   КАК Дата
ИЗ Документ.А_ОтражениеЗПпоКазне КАК Д
ГДЕ Д.Номер = "000000001"
    И Д.Дата МЕЖДУ ДАТАВРЕМЯ(2025,12,1) И ДАТАВРЕМЯ(2025,12,31,23,59,59)
"""
res = q_etalon.Execute()
if res.Пустой():
    print("FAIL: эталон А_ОтражениеЗПпоКазне №000000001 от 31.12.2025 не найден")
    sys.exit(1)

выб = res.Выбрать()
выб.Следующий()
озп_ссылка = выб.Ссылка
print(f"Эталон: {озп_ссылка}")

# 2. Список уникальных Ф2 в ТЧ.РаспределениеКазна
q_f2 = erp.NewObject("Запрос")
q_f2.SetParameter("Ссылка", озп_ссылка)
q_f2.Text = """
ВЫБРАТЬ РАЗЛИЧНЫЕ
    РК.ДокРаспределениеЗП КАК Ф2
ИЗ Документ.А_ОтражениеЗПпоКазне.РаспределениеКазна КАК РК
ГДЕ РК.Ссылка = &Ссылка
    И ТИПЗНАЧЕНИЯ(РК.ДокРаспределениеЗП) = ТИП(Документ.РаспределениеФ2)
"""
ф2_список = q_f2.Execute().Выгрузить()
print(f"Уникальных Ф2 в шапке: {ф2_список.Количество()}")
print()

# 3. Главный запрос — точно тот, что пойдёт в BSL (с новым полем А_Необновлять)
q_main = erp.NewObject("Запрос")
q_main.Text = """
ВЫБРАТЬ ПЕРВЫЕ 1
    Д.Ссылка          КАК Ссылка,
    Д.Проведен        КАК Проведен,
    Д.ПометкаУдаления КАК ПометкаУдаления,
    Д.А_Необновлять   КАК А_Необновлять
ИЗ Документ.ВедомостьНаВыплатуЗарплатыВКассу КАК Д
ГДЕ Д.А_РаспределениеФ2 = &Основание
"""

# 4. Для каждой Ф2 — прогноз кода результата
прогноз = {"Создано": 0, "Обновлено": 0, "Пропущено": 0}
print(f"{'Ф2':<55} {'ВКассу':<32} {'Пров':<5} {'Уд':<4} {'НеОбн':<6} {'Прогноз'}")
print("-" * 130)
for стр_ф2 in range(1, ф2_список.Количество() + 1):
    ф2 = ф2_список[стр_ф2 - 1].Ф2
    q_main.SetParameter("Основание", ф2)
    try:
        результат = q_main.Execute()
    except Exception as e:
        info = getattr(e, "excepinfo", None)
        msg = info[2] if info else str(e)
        print(f"FAIL запрос на Ф2 {ф2}: {msg}")
        sys.exit(2)

    if результат.Пустой():
        код = "Создано"
        прогноз[код] += 1
        print(f"{str(ф2)[:55]:<55} {'(нет)':<32} {'-':<5} {'-':<4} {'-':<6} {код}")
    else:
        выб = результат.Выбрать()
        выб.Следующий()
        вкассу = выб.Ссылка
        пров = "ДА" if выб.Проведен else "нет"
        уд = "ДА" if выб.ПометкаУдаления else "нет"
        необн = "ИСТИНА" if выб.А_Необновлять else "ложь"
        код = "Пропущено" if выб.А_Необновлять else "Обновлено"
        прогноз[код] += 1
        print(f"{str(ф2)[:55]:<55} {str(вкассу)[:32]:<32} {пров:<5} {уд:<4} {необн:<6} {код}")

print("-" * 130)
print(f"Прогноз: создано={прогноз['Создано']}, обновлено={прогноз['Обновлено']}, пропущено={прогноз['Пропущено']}")

# 5. Acceptance: должен пройти запрос (нет "Поле не найдено") + полезен ли smoke
if прогноз["Пропущено"] == 0:
    print()
    print("ВНИМАНИЕ: ни одна Ф2 не даст пропуск (все А_Необновлять=Ложь).")
    print("Smoke-тест бессмысленен — нечего пропускать.")
    print("Решение: либо провести РКО для одной из ВКассу (тогда А_Необновлять=Истина),")
    print("либо указать другую А_ОтражениеЗПпоКазне, где есть проведённые РКО.")
    sys.exit(3)

print("OK: запрос принимается, есть хотя бы одна ВКассу для пропуска.")
```

- [ ] **Step 1.2: Прогнать тест**

Через mcp-инструмент `mcp__python-runner__run_command` выполнить:
```
python C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_ozp_skip_neobnovlyat.py
```

- [ ] **Step 1.3: Проверить вывод**

Ожидаемое:
- Нет строки `FAIL` или `excepinfo` — запрос с `А_Необновлять` принимается.
- В таблице ≥1 строка с прогнозом `Пропущено`.
- Финальная строка `OK: запрос принимается, есть хотя бы одна ВКассу для пропуска.`

Если ни одной `Пропущено` — остановиться, сообщить пользователю, что smoke-тест бессмысленен (нечего пропускать), и попросить указать другую А_ОтражениеЗПпоКазне или провести РКО для одной из ВКассу. **Не идти дальше в Task 2.**

- [ ] **Step 1.4: Коммит**

```
git add _Rarzrabotki/Python/test/test_ozp_skip_neobnovlyat.py
git commit -m "test(_ОЗП): pre-check запроса с А_Необновлять для скипа ВКассу

Перед правкой BSL проверяет на живой ERP что запрос с полем А_Необновлять
принимается без 'Поле не найдено' и что в эталоне А_ОтражениеЗПпоКазне
№000000001 от 31.12.2025 есть хотя бы одна ВКассу с А_Необновлять=Истина
(иначе smoke-тест бессмысленен)."
```

---

## Task 2: Правка `_ОЗП_СоздатьВКассуИзФ2` и вызывающей `СоздатьВедомостиВКассуПоОтражениюЗП`

**Files:**
- Modify: `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl:2686-2719` (вызывающая) и `:2723-2904` (тело функции)

**Цель:** одна логическая правка одного файла → один коммит. Все шаги — точечные Edit'ы.

- [ ] **Step 2.1: Расширить SELECT и сменить заголовок процедуры на функцию**

Найти на стр. 2721-2734:

```bsl
// Создаёт/обновляет ОДНУ ВКассу по одной Ф2 на основе строк РаспределениеКазна.
// СтрокиРК — Массив строк ТЧ.РаспределениеКазна, относящихся к этой Ф2.
Процедура _ОЗП_СоздатьВКассуИзФ2(Ф2Ссылка, СтрокиРК)

	// Find-or-Create по обратной ссылке Ф2→ВКассу
	ЗапросСущ = Новый Запрос;
	ЗапросСущ.УстановитьПараметр("Основание", Ф2Ссылка);
	ЗапросСущ.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	Д.Ссылка КАК Ссылка,
	|	Д.Проведен КАК Проведен,
	|	Д.ПометкаУдаления КАК ПометкаУдаления
	|ИЗ Документ.ВедомостьНаВыплатуЗарплатыВКассу КАК Д
	|ГДЕ Д.А_РаспределениеФ2 = &Основание";
	Результат = ЗапросСущ.Выполнить();
```

Заменить на:

```bsl
// Создаёт/обновляет ОДНУ ВКассу по одной Ф2 на основе строк РаспределениеКазна.
// Возвращает строковый код: "Создано" / "Обновлено" / "Пропущено" / "Ошибка".
// "Пропущено" — ВКассу есть и помечена А_Необновлять=Истина (уже занята проведённым РКО).
// СтрокиРК — Массив строк ТЧ.РаспределениеКазна, относящихся к этой Ф2.
Функция _ОЗП_СоздатьВКассуИзФ2(Ф2Ссылка, СтрокиРК)

	КодРезультата = "";

	// Find-or-Create по обратной ссылке Ф2→ВКассу
	ЗапросСущ = Новый Запрос;
	ЗапросСущ.УстановитьПараметр("Основание", Ф2Ссылка);
	ЗапросСущ.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	Д.Ссылка КАК Ссылка,
	|	Д.Проведен КАК Проведен,
	|	Д.ПометкаУдаления КАК ПометкаУдаления,
	|	Д.А_Необновлять КАК А_Необновлять
	|ИЗ Документ.ВедомостьНаВыплатуЗарплатыВКассу КАК Д
	|ГДЕ Д.А_РаспределениеФ2 = &Основание";
	Результат = ЗапросСущ.Выполнить();
```

- [ ] **Step 2.2: Вставить гард `А_Необновлять` и установить `КодРезультата` в обеих ветках**

Найти на стр. 2737-2756:

```bsl
	Если НЕ Результат.Пустой() Тогда
		Выб = Результат.Выбрать(); Выб.Следующий();
		ВыкВКассу = Выб.Ссылка.ПолучитьОбъект();
		Если Выб.ПометкаУдаления Тогда
			ВыкВКассу.УстановитьПометкуУдаления(Ложь);
		КонецЕсли;
		Если Выб.Проведен Тогда
			Попытка
				ВыкВКассу.Записать(РежимЗаписиДокумента.ОтменаПроведения);
			Исключение
				Сообщить("Не удалось распровести существующую ВКассу " + Строка(Выб.Ссылка) + ": " + ОписаниеОшибки());
				Возврат;
			КонецПопытки;
		КонецЕсли;
		ВыкВКассу.Зарплата.Очистить();
		ВыкВКассу.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Очистить();
		ВыкВКассу.Состав.Очистить();
	Иначе
		ВыкВКассу = Документы.ВедомостьНаВыплатуЗарплатыВКассу.СоздатьДокумент();
	КонецЕсли;
```

Заменить на:

```bsl
	Если НЕ Результат.Пустой() Тогда
		Выб = Результат.Выбрать(); Выб.Следующий();

		// Гард: ВКассу уже занята проведённым РКО — её нельзя перезаписывать.
		Если Выб.А_Необновлять Тогда
			Сообщить("Ф2 " + Строка(Ф2Ссылка) + " → ВКассу " + Строка(Выб.Ссылка)
				+ " пропущена (А_Необновлять=Истина, занята РКО)");
			Возврат "Пропущено";
		КонецЕсли;

		ВыкВКассу = Выб.Ссылка.ПолучитьОбъект();
		Если Выб.ПометкаУдаления Тогда
			ВыкВКассу.УстановитьПометкуУдаления(Ложь);
		КонецЕсли;
		Если Выб.Проведен Тогда
			Попытка
				ВыкВКассу.Записать(РежимЗаписиДокумента.ОтменаПроведения);
			Исключение
				Сообщить("Не удалось распровести существующую ВКассу " + Строка(Выб.Ссылка) + ": " + ОписаниеОшибки());
				Возврат "Ошибка";
			КонецПопытки;
		КонецЕсли;
		ВыкВКассу.Зарплата.Очистить();
		ВыкВКассу.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Очистить();
		ВыкВКассу.Состав.Очистить();
		КодРезультата = "Обновлено";
	Иначе
		ВыкВКассу = Документы.ВедомостьНаВыплатуЗарплатыВКассу.СоздатьДокумент();
		КодРезультата = "Создано";
	КонецЕсли;
```

- [ ] **Step 2.3: Заменить второе `Возврат;` в Исключении и финальное `КонецПроцедуры`**

Найти на стр. 2882-2904 (конец функции):

```bsl
	Попытка
		ВыкВКассу.Записать(РежимЗаписиДокумента.Проведение);
	Исключение
		Сообщить("Ошибка проведения ВКассу для Ф2 " + Строка(Ф2Ссылка) + ": " + ОписаниеОшибки());
		Возврат;
	КонецПопытки;

	// Обратная ссылка Ф2.А_ВедомостьВКассу
	Если Ф2Ссылка.А_ВедомостьВКассу <> ВыкВКассу.Ссылка Тогда
		Ф2Объект = Ф2Ссылка.ПолучитьОбъект();
		Ф2Объект.А_ВедомостьВКассу = ВыкВКассу.Ссылка;
		Ф2Объект.ОбменДанными.Загрузка = Истина;
		Ф2Объект.Записать();
	КонецЕсли;

	Сообщить("Ф2 " + Строка(Ф2Ссылка) + " → ВКассу " + Строка(ВыкВКассу.Ссылка)
		+ " | Зарплата=" + ВыкВКассу.Зарплата.Количество()
		+ " | Расшифровка=" + ВыкВКассу.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Количество()
		+ " | Состав=" + ВыкВКассу.Состав.Количество()
		+ " | Σ=" + Формат(ВыкВКассу.СуммаПоДокументу, "ЧДЦ=2")
		+ " | Проведена=" + Строка(ВыкВКассу.Проведен));

КонецПроцедуры
```

Заменить на:

```bsl
	Попытка
		ВыкВКассу.Записать(РежимЗаписиДокумента.Проведение);
	Исключение
		Сообщить("Ошибка проведения ВКассу для Ф2 " + Строка(Ф2Ссылка) + ": " + ОписаниеОшибки());
		Возврат "Ошибка";
	КонецПопытки;

	// Обратная ссылка Ф2.А_ВедомостьВКассу
	Если Ф2Ссылка.А_ВедомостьВКассу <> ВыкВКассу.Ссылка Тогда
		Ф2Объект = Ф2Ссылка.ПолучитьОбъект();
		Ф2Объект.А_ВедомостьВКассу = ВыкВКассу.Ссылка;
		Ф2Объект.ОбменДанными.Загрузка = Истина;
		Ф2Объект.Записать();
	КонецЕсли;

	Сообщить("Ф2 " + Строка(Ф2Ссылка) + " → ВКассу " + Строка(ВыкВКассу.Ссылка)
		+ " | Зарплата=" + ВыкВКассу.Зарплата.Количество()
		+ " | Расшифровка=" + ВыкВКассу.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Количество()
		+ " | Состав=" + ВыкВКассу.Состав.Количество()
		+ " | Σ=" + Формат(ВыкВКассу.СуммаПоДокументу, "ЧДЦ=2")
		+ " | Проведена=" + Строка(ВыкВКассу.Проведен));

	Возврат КодРезультата;

КонецФункции
```

- [ ] **Step 2.4: Правка вызывающей `СоздатьВедомостиВКассуПоОтражениюЗП` — счётчик + switch**

Найти на стр. 2686-2710:

```bsl
	КолОбработано = 0;
	КолОшибок = 0;
	Для Каждого КЗ Из ГруппыПоФ2 Цикл
		Ф2Ссылка = КЗ.Ключ;
		СтрокиРК = КЗ.Значение;

		Если Ф2Ссылка.ПолучитьОбъект() = Неопределено Тогда
			Сообщить("Ф2 " + Строка(Ф2Ссылка) + " не найден — пропуск.");
			КолОшибок = КолОшибок + 1;
			Продолжить;
		КонецЕсли;
		Если НЕ Ф2Ссылка.Проведен Тогда
			Сообщить("Ф2 " + Строка(Ф2Ссылка) + " не проведён — пропуск.");
			КолОшибок = КолОшибок + 1;
			Продолжить;
		КонецЕсли;

		Попытка
			_ОЗП_СоздатьВКассуИзФ2(Ф2Ссылка, СтрокиРК);
			КолОбработано = КолОбработано + 1;
		Исключение
			Сообщить("Ошибка создания ВКассу для " + Строка(Ф2Ссылка) + ": " + ОписаниеОшибки());
			КолОшибок = КолОшибок + 1;
		КонецПопытки;
	КонецЦикла;
```

Заменить на:

```bsl
	КолОбработано    = 0;
	КолОшибок        = 0;
	КолПропущеноБлок = 0;
	Для Каждого КЗ Из ГруппыПоФ2 Цикл
		Ф2Ссылка = КЗ.Ключ;
		СтрокиРК = КЗ.Значение;

		Если Ф2Ссылка.ПолучитьОбъект() = Неопределено Тогда
			Сообщить("Ф2 " + Строка(Ф2Ссылка) + " не найден — пропуск.");
			КолОшибок = КолОшибок + 1;
			Продолжить;
		КонецЕсли;
		Если НЕ Ф2Ссылка.Проведен Тогда
			Сообщить("Ф2 " + Строка(Ф2Ссылка) + " не проведён — пропуск.");
			КолОшибок = КолОшибок + 1;
			Продолжить;
		КонецЕсли;

		Попытка
			Код = _ОЗП_СоздатьВКассуИзФ2(Ф2Ссылка, СтрокиРК);
			Если      Код = "Пропущено" Тогда КолПропущеноБлок = КолПропущеноБлок + 1;
			ИначеЕсли Код = "Ошибка"    Тогда КолОшибок         = КолОшибок + 1;
			Иначе                              КолОбработано    = КолОбработано + 1;
			КонецЕсли;
		Исключение
			Сообщить("Ошибка создания ВКассу для " + Строка(Ф2Ссылка) + ": " + ОписаниеОшибки());
			КолОшибок = КолОшибок + 1;
		КонецПопытки;
	КонецЦикла;
```

- [ ] **Step 2.5: Расширить итоговую строку**

Найти на стр. 2712-2717:

```bsl
	ИтогТекст = "ОтражениеЗПпоКазне → ВКассу: создано/обновлено " + КолОбработано
		+ ", ошибок " + КолОшибок;
	Если ПропускТипов > 0 Тогда
		ИтогТекст = ИтогТекст + ", пропущено А_РаспределениеЗаработнойПлаты строк " + ПропускТипов;
	КонецЕсли;
	Сообщить(ИтогТекст);
```

Заменить на:

```bsl
	ИтогТекст = "ОтражениеЗПпоКазне → ВКассу: создано/обновлено " + КолОбработано
		+ ", ошибок " + КолОшибок;
	Если КолПропущеноБлок > 0 Тогда
		ИтогТекст = ИтогТекст + ", пропущено заблокированных (А_Необновлять) " + КолПропущеноБлок;
	КонецЕсли;
	Если ПропускТипов > 0 Тогда
		ИтогТекст = ИтогТекст + ", пропущено А_РаспределениеЗаработнойПлаты строк " + ПропускТипов;
	КонецЕсли;
	Сообщить(ИтогТекст);
```

- [ ] **Step 2.6: Грепом убедиться что других вызовов `_ОЗП_СоздатьВКассуИзФ2` нет**

Через Grep:
```
pattern: _ОЗП_СоздатьВКассуИзФ2
output_mode: content
```

Ожидаемое: только три попадания — строки определения (`Функция _ОЗП_СоздатьВКассуИзФ2`), доковая строка над ней и единственный вызов в `СоздатьВедомостиВКассуПоОтражениюЗП` (стр. 2704, теперь `Код = _ОЗП_СоздатьВКассуИзФ2(...)`). Если найдётся ещё вызов как процедуру (`_ОЗП_СоздатьВКассуИзФ2(...)` без присваивания) — добавить switch там же.

- [ ] **Step 2.7: Грепом проверить что Функция теперь имеет ровно 4 точки выхода**

Через Grep:
```
pattern: ^\s*Возврат\s+
path: Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl
output_mode: content
-n: true
```

В блоке стр. 2723-2904 должно быть ровно 4 `Возврат`:
- `Возврат "Пропущено";` (в гарде ~стр. 2742)
- `Возврат "Ошибка";` (в Исключении ОтменаПроведения, ~стр. 2756)
- `Возврат "Ошибка";` (в Исключении финального Проведения, ~стр. 2894)
- `Возврат КодРезультата;` (~стр. 2911 перед `КонецФункции`)

- [ ] **Step 2.8: Коммит**

```
git add Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl
git commit -m "feat(_ОЗП): пропуск ВКассу с А_Необновлять=Истина в _ОЗП_СоздатьВКассуИзФ2

Когда РКО выпущен и проведён из ВКассу, РасходныйКассовыйОрдер.ПриЗаписи
ставит ВКассу.А_Необновлять=Истина (см. РКО.ObjectModule.bsl:1127-1131).
Сегодня повторный запуск кнопки «Создать ВКассу по отражению ЗП» в форме
А_ОтражениеЗПпоКазне затирает такую ВКассу.

Изменения:
- Find-or-create запрос расширен полем А_Необновлять.
- После нахождения существующей ВКассу — гард: если А_Необновлять=Истина,
  Сообщить + Возврат \"Пропущено\" ДО любых модификаций (распроведения,
  снятия пометки удаления, очистки ТЧ).
- _ОЗП_СоздатьВКассуИзФ2 сменён с Процедуры на Функцию, возвращает
  строковый код: \"Создано\" / \"Обновлено\" / \"Пропущено\" / \"Ошибка\".
- В СоздатьВедомостиВКассуПоОтражениюЗП добавлен счётчик КолПропущеноБлок
  и switch по коду; итоговая строка дополнена фрагментом
  \", пропущено заблокированных (А_Необновлять) N\".

Обратимость: распроведение РКО автоматически выставляет А_Необновлять=Ложь
(формула А_Необновлять = Источник.Проведен), повторный запуск обновит ВКассу.

Spec: docs/superpowers/specs/2026-05-29-ozp-sozdat-vkassu-iz-f2-skip-neobnovlyat-design.md"
```

---

## Task 3: Загрузка XML в базу и обновление конфигурации

**Цель:** перенести правленный `ObjectModule.bsl` в живую БД ERP, чтобы Designer/Enterprise увидели новый код. Без этого smoke в Task 4 будет работать со старым байт-кодом (memory `feedback_designer_cache_invalidation`).

- [ ] **Step 3.1: Partial-load XML в базу через skill `/db-load-xml`**

Вызвать skill:
```
/db-load-xml -Mode Partial -Path "Documents\А_ОтражениеЗПпоКазне\Ext\ObjectModule.bsl"
```

Ожидаемое: skill выполнит `1cv8.exe DESIGNER /F base /N user /P pwd /LoadConfigFromFiles ... -Format Hierarchical -files "Documents\А_ОтражениеЗПпоКазне\Ext\ObjectModule.bsl" -updateConfigDumpInfo` и сообщит «Успешно». Если skill попросит указать базу — выбрать `BaseERP`.

- [ ] **Step 3.2: Обновить конфигурацию БД через skill `/db-update`**

Вызвать skill:
```
/db-update -Dynamic+
```

Ожидаемое: skill выполнит `1cv8.exe DESIGNER ... /UpdateDBCfg -Dynamic+` и сообщит «Обновление выполнено». Базу `BaseERP` указать если skill спросит.

- [ ] **Step 3.3: Сверить через MCP что новое поле в запросе принимается живой ERP**

Через `mcp__1c-workerp__execute_query` (без параметров — просто синтаксис-чек):
```sql
SELECT TOP 1 Д.Ссылка, Д.А_Необновлять
FROM Документ.ВедомостьНаВыплатуЗарплатыВКассу AS Д
```

Ожидаемое: запрос возвращает 1 строку (или пустой, если БД совсем пуста, но без `"Поле не найдено"`).

---

## Task 4: Smoke-тест в Enterprise (live)

**Цель:** убедиться что в реальной форме `А_ОтражениеЗПпоКазне` правленая функция (а) пропускает ВКассу с `А_Необновлять=Истина`, (б) логирует пропуск, (в) расширенная итоговая строка содержит счётчик заблокированных. **Этот шаг требует действий пользователя в 1С Enterprise — Claude не может выполнить нажатие кнопки.**

- [ ] **Step 4.1: Запомнить состояние ВКассу ДО прогона**

Через `mcp__1c-workerp__execute_query`:
```sql
SELECT
    Д.Ссылка        AS Ссылка,
    Д.Дата          AS Дата,
    Д.МоментВремени AS МоментВремени,
    Д.А_Необновлять AS А_Необновлять,
    Д.Проведен      AS Проведен,
    Д.СуммаПоДокументу AS Сумма
FROM Документ.ВедомостьНаВыплатуЗарплатыВКассу AS Д
WHERE Д.А_РаспределениеФ2 IN (
    SELECT РАЗЛИЧНЫЕ РК.ДокРаспределениеЗП
    FROM Документ.А_ОтражениеЗПпоКазне.РаспределениеКазна AS РК
    WHERE РК.Ссылка.Номер = '000000001'
)
ORDER BY Д.Дата
```

Сохранить таблицу-снимок (Ссылка → МоментВремени и СуммаПоДокументу) — это эталон для сверки в Step 4.4.

- [ ] **Step 4.2: Попросить пользователя выполнить smoke в Enterprise**

Сообщить пользователю:

> Открой в 1С Enterprise документ `А_ОтражениеЗПпоКазне` №000000001 от 31.12.2025, нажми кнопку «Создать ВКассу по отражению ЗП», скопируй из служебных сообщений итоговую строку и пришли её сюда.

- [ ] **Step 4.3: Проверить итоговую строку в логе**

В присланном пользователем тексте должны быть:
- Префикс `ОтражениеЗПпоКазне → ВКассу: создано/обновлено N` (N может быть 0 если все Ф2 заняты)
- Фрагмент `, пропущено заблокированных (А_Необновлять) K` где `K ≥ 1` (значение совпадает с прогнозом из Task 1, Step 1.3)
- Перед итоговой строкой — `K` строк вида `Ф2 ... → ВКассу ... пропущена (А_Необновлять=Истина, занята РКО)`

Если фрагмент `пропущено заблокированных` отсутствует — значит skill `/db-load-xml` не подтянул правку. Вернуться к Task 3, проверить что ConfigDumpInfo обновлён и Enterprise перезапущен.

- [ ] **Step 4.4: Проверить через MCP что заблокированные ВКассу не модифицированы**

Повторить запрос из Step 4.1. Для всех Ссылок, у которых в снимке было `А_Необновлять = Истина`:
- `МоментВремени` **не должен** измениться.
- `СуммаПоДокументу` **не должна** измениться.

Если у такой строки `МоментВремени` сместился — гард не сработал, либо смена сигнатуры пропустила одну из веток. Открыть `ObjectModule.bsl:2723-2920` и сверить с Task 2.

---

## Task 5: Verification — идемпотентность

**Цель:** убедиться, что вторичный прогон не делает ничего лишнего. Если после Task 4 повторно нажать кнопку — счётчики `создано/обновлено` НЕ растут для уже заблокированных ВКассу.

- [ ] **Step 5.1: Попросить пользователя повторить прогон**

Сообщить пользователю:

> Снова открой тот же А_ОтражениеЗПпоКазне №000000001, нажми кнопку «Создать ВКассу по отражению ЗП» второй раз подряд, и пришли итоговую строку.

- [ ] **Step 5.2: Сравнить итоговые строки 1-го и 2-го прогонов**

Прогон #2 должен дать **те же** значения `создано/обновлено N`, `пропущено заблокированных K`, `ошибок 0`. Если `N` вырос — идемпотентность нарушена (это значит, что после 1-го прогона у каких-то ВКассу автоматически снялся `А_Необновлять`, что не должно происходить — указывает на конфликт со связанной подпиской).

- [ ] **Step 5.3: Финальная проверка через MCP**

Повторить запрос из Step 4.1. Все `МоментВремени` всех ВКассу с `А_Необновлять = Истина` должны быть теми же, что в Step 4.1 (т.е. оба прогона их не трогали).

---

## Task 6: Финиш ветки

**Цель:** закрыть ветку через skill `superpowers:finishing-a-development-branch`.

- [ ] **Step 6.1: Убедиться что коммиты в порядке**

```
git log --oneline -5
```

Ожидаемое (от свежего к старому):
- `feat(_ОЗП): пропуск ВКассу с А_Необновлять=Истина в _ОЗП_СоздатьВКассуИзФ2`
- `test(_ОЗП): pre-check запроса с А_Необновлять для скипа ВКассу`
- `docs(spec): _ОЗП_СоздатьВКассуИзФ2 — пропуск ВКассу с А_Необновлять=Истина`

- [ ] **Step 6.2: Вызвать skill `superpowers:finishing-a-development-branch`**

Skill сам решит — push в `main` или PR. Согласно auto-memory `feedback_autonomous_finish_merge_push_pr`: merge/push/PR при завершении ветки делается автономно без переспросов.

---

## Acceptance Criteria

Дизайн считается реализованным, когда:

- [x] Spec закоммичен в `docs/superpowers/specs/`.
- [ ] Python-предчек `test_ozp_skip_neobnovlyat.py` проходит на живой ERP (запрос с `А_Необновлять` принимается, в эталоне есть ВКассу с `А_Необновлять = Истина`).
- [ ] `_ОЗП_СоздатьВКассуИзФ2` — Функция, возвращает один из четырёх строковых кодов.
- [ ] `СоздатьВедомостиВКассуПоОтражениюЗП` имеет три счётчика и switch по коду.
- [ ] Итоговая строка содержит «пропущено заблокированных (А_Необновлять) K» при `K > 0`.
- [ ] Smoke в Enterprise подтверждает: занятые ВКассу не пере-проведены (МоментВремени и Σ не изменились).
- [ ] Повторный прогон идемпотентен (счётчики не растут).
- [ ] Ветка финиширована через `superpowers:finishing-a-development-branch`.
