# Подотчётне: Субконто1=ФЛ (3721) + ПередачаНач по строковой статье — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** (A) проставлять `Субконто1 = ФизическоеЛицо` в подотчётной строке удержания (regl-субконто счёта 3721); (B) `А_ПередачаНачисленийМеждуПодразделениями` проводить удержания по строковой `СтатьяАктивовПассивов`/`АналитикаАктивовПассивов` (Гуняга 400 → «Денежные средства (у подотчетных лиц)» без аналитики), вместо хардкода «Прочие пассивы».

**Architecture:** Правки в 2 файлах: `А_ОтражениеЗПпоКазне/ObjectModule.bsl` (ЗаписатьУдержанияВТЧ_Бух +Субконто1; _ПНМП_ЗаполнитьУдержания прокидывает статью/аналитику) и `А_ПередачаНачисленийМеждуПодразделениями/ObjectModule.bsl` (ОбработкаПроведения блок Удержания — per-row статья/аналитика; _ПАП_ДобавитьДвижение обобщён). Типовой ОЗФУ читает статью/аналитику/Субконто1 из строки УдержаннаяЗарплата — не трогаем.

**Tech Stack:** 1C BSL, Python COM (BaseERP), db-load-xml/db-update. Среда: `Ref="BaseERP";Usr="Администратор";Pwd="24043"`, v8 8.3.20.1914, python C:\Python313. Спека: `docs/superpowers/specs/2026-06-04-podzvitne-subconto-peredacha-design.md`.

**Коммиты:** только релевантные файлы. Статья «Денежные средства (у подотчетных лиц)» = «Без аналитики» → ПАП-аналитику НЕ ставим.

---

## Task 1: RED (Python COM, Rule #-1)

**Files:** Create `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/red_podzvitne_subconto.py`

- [ ] **Step 1: Скрипт** — (а) ПередачаНач №1 ПАП-движения Гуняги: сейчас статья «Прочие пассивы»; (б) тест нового запроса удержаний с ВЫБОР-fallback (на текущих строках статья пуста → «Прочие пассивы»).

```python
# -*- coding: utf-8 -*-
import io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client as w
erp = w.Dispatch("V83.COMConnector").Connect('Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"')

# (а) ПередачаНач №1 — ПАП по статьям (Гуняга-сумма 400 сейчас на «Прочие пассивы»)
q = erp.NewObject("Запрос")
q.УстановитьПараметр("Осн", erp.Документы.А_ОтражениеЗПпоКазне.НайтиПоНомеру("000000001"))
q.Текст = '''ВЫБРАТЬ ПАП.Статья.Наименование КАК Статья, ПАП.Подразделение.Наименование КАК Подр,
 СУММА(ВЫБОР КОГДА ПАП.ВидДвижения=ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) ТОГДА ПАП.Сумма ИНАЧЕ -ПАП.Сумма КОНЕЦ) КАК Сум
ИЗ РегистрНакопления.ПрочиеАктивыПассивы КАК ПАП
ГДЕ ВЫРАЗИТЬ(ПАП.Регистратор КАК Документ.А_ПередачаНачисленийМеждуПодразделениями).А_ДокументОснование = &Осн
СГРУППИРОВАТЬ ПО ПАП.Статья.Наименование, ПАП.Подразделение.Наименование'''
print("(а) ПередачаНач №1 ПАП по (статья, подр):")
try:
    t = q.Выполнить().Выгрузить()
    for i in range(t.Количество()):
        r = t.Получить(i); print(f"   {r.Статья[:34]:34s} | {r.Подр[:16]:16s} | {r.Сум:>10.2f}")
except Exception as e:
    info = e.excepinfo[2] if hasattr(e,"excepinfo") and e.excepinfo else str(e)
    print("   запрос поправить:", info)

# (б) тест нового блока Удержания (ВЫБОР-fallback + аналитика) — на текущем ПередачаНач №1
СтПрочПас = erp.ПланыВидовХарактеристик.СтатьиАктивовПассивов.НайтиПоНаименованию("Прочие пассивы")
q2 = erp.NewObject("Запрос")
q2.УстановитьПараметр("Осн", erp.Документы.А_ОтражениеЗПпоКазне.НайтиПоНомеру("000000001"))
q2.УстановитьПараметр("СтПрочПас", СтПрочПас)
q2.Текст = '''ВЫБРАТЬ
 ВЫБОР КОГДА У.СтатьяАктивовПассивов = ЗНАЧЕНИЕ(ПланВидовХарактеристик.СтатьиАктивовПассивов.ПустаяСсылка)
   ТОГДА &СтПрочПас ИНАЧЕ У.СтатьяАктивовПассивов КОНЕЦ КАК Статья,
 У.АналитикаАктивовПассивов КАК Аналитика, У.ИсходноеПодразделение КАК Исх, У.ЦелевоеПодразделение КАК Цел,
 СУММА(У.Сумма) КАК Сумма
ИЗ Документ.А_ПередачаНачисленийМеждуПодразделениями.Удержания КАК У
ГДЕ У.Ссылка.А_ДокументОснование = &Осн
СГРУППИРОВАТЬ ПО
 ВЫБОР КОГДА У.СтатьяАктивовПассивов = ЗНАЧЕНИЕ(ПланВидовХарактеристик.СтатьиАктивовПассивов.ПустаяСсылка)
   ТОГДА &СтПрочПас ИНАЧЕ У.СтатьяАктивовПассивов КОНЕЦ,
 У.АналитикаАктивовПассивов, У.ИсходноеПодразделение, У.ЦелевоеПодразделение'''
print("(б) тест нового блока Удержания (синтаксис + текущие строки):")
try:
    t2 = q2.Выполнить().Выгрузить()
    print(f"   OK, строк={t2.Количество()}")
    for i in range(t2.Количество()):
        r = t2.Получить(i)
        ст = r.Статья.Наименование if erp.ЗначениеЗаполнено(r.Статья) else "?"
        print(f"   {ст[:30]:30s} | {r.Исх} -> {r.Цел} | {r.Сумма}")
except Exception as e:
    info = e.excepinfo[2] if hasattr(e,"excepinfo") and e.excepinfo else str(e)
    print("   FAIL запрос:", info)
```

- [ ] **Step 2: Запустить** — `& "C:\Python313\python.exe" "...\red_podzvitne_subconto.py"`. Expected: (а) Гуняга-сумма видна на «Прочие пассивы»; (б) запрос OK (синтаксис верен), текущие строки → «Прочие пассивы» (статья пуста).
- [ ] **Step 3: Commit** `git add <скрипт>; git commit -m "test(подотчётне2): RED — ПередачаНач Гуняга на Прочие пассивы, новый запрос OK"`.

---

## Task 2: Часть A — Субконто1=ФЛ

**Files:** Modify `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` (`ЗаписатьУдержанияВТЧ_Бух`, подотчётная ветка ~767)

- [ ] **Step 1: Edit**

old_string:
```bsl
		Если ЭтоПодотчетне Тогда
			НоваяСтрока.СтатьяАктивовПассивов = СтатьяПодотчетные;
			НоваяСтрока.СчетУчета             = СчетУчета3721;
		Иначе
```
new_string:
```bsl
		Если ЭтоПодотчетне Тогда
			НоваяСтрока.СтатьяАктивовПассивов = СтатьяПодотчетные;
			НоваяСтрока.СчетУчета             = СчетУчета3721;
			НоваяСтрока.Субконто1             = СотрудникERP.ФизическоеЛицо; // regl-субконто 3721 (статья «Без аналитики» → ПАП-аналитику не ставим)
		Иначе
```

---

## Task 3: Часть B.1 — _ПНМП_ЗаполнитьУдержания прокидывает статью/аналитику

**Files:** Modify `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` (`_ПНМП_ЗаполнитьУдержания` ~3995)

- [ ] **Step 1: Edit**

old_string:
```bsl
Процедура _ПНМП_ЗаполнитьУдержания(Док, СотрудникПоФЛ, ОргБухПоФЛ, ПодрУдержПоФЛ)
	ПустоеДоп = Новый Структура;
	Для Каждого Стр Из УдержанияБухгалтерия Цикл
		Если Стр.Сумма = 0 Тогда Продолжить; КонецЕсли;
		// УдержанияБухгалтерия (вхід zup_1) НЕ має підрозділу → беремо те саме, що ОЗФУ проставив
		// у УдержаннаяЗарплата (Fix 2: дочірній ОЗФУ — net-zero проти ПАП «Прочие пассивы»).
		ИсхПодр = ?(ЗначениеЗаполнено(Стр.ПодразделениеПредприятия), Стр.ПодразделениеПредприятия,
			ПодрУдержПоФЛ[Стр.ФизическоеЛицо]);
		_ПНМП_ДобавитьСтроку(Док.Удержания, Стр.ФизическоеЛицо, СотрудникПоФЛ[Стр.ФизическоеЛицо],
			ОргБухПоФЛ[Стр.ФизическоеЛицо], ИсхПодр, Стр.Сумма, ПустоеДоп);
	КонецЦикла;
КонецПроцедуры
```
new_string:
```bsl
Процедура _ПНМП_ЗаполнитьУдержания(Док, СотрудникПоФЛ, ОргБухПоФЛ, ПодрУдержПоФЛ)
	Для Каждого Стр Из УдержанияБухгалтерия Цикл
		Если Стр.Сумма = 0 Тогда Продолжить; КонецЕсли;
		// УдержанияБухгалтерия (вхід zup_1) НЕ має підрозділу → беремо те саме, що ОЗФУ проставив
		// у УдержаннаяЗарплата (Fix 2: дочірній ОЗФУ — net-zero проти ПАП «Прочие пассивы»).
		ИсхПодр = ?(ЗначениеЗаполнено(Стр.ПодразделениеПредприятия), Стр.ПодразделениеПредприятия,
			ПодрУдержПоФЛ[Стр.ФизическоеЛицо]);
		// Статья/аналитика ПАП — зі строки-джерела (підзвітні → «Денежные средства (у подотчетных лиц)»,
		// аліменти → «Прочие пассивы»); ПередачаНач проводить за ними.
		Доп = Новый Структура("СтатьяАктивовПассивов, АналитикаАктивовПассивов",
			Стр.СтатьяАктивовПассивов, Стр.АналитикаАктивовПассивов);
		_ПНМП_ДобавитьСтроку(Док.Удержания, Стр.ФизическоеЛицо, СотрудникПоФЛ[Стр.ФизическоеЛицо],
			ОргБухПоФЛ[Стр.ФизическоеЛицо], ИсхПодр, Стр.Сумма, Доп);
	КонецЦикла;
КонецПроцедуры
```

---

## Task 4: Часть B.2 — ПередачаНач проводит по строковой статье/аналитике

**Files:** Modify `Documents/А_ПередачаНачисленийМеждуПодразделениями/Ext/ObjectModule.bsl`

- [ ] **Step 1: Блок Удержания запроса (ОбработкаПроведения)**

old_string:
```bsl
		|ВЫБРАТЬ
		|	&СтПрочПас,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыНалогов.ПустаяСсылка),
		|	У.ИсходноеПодразделение,
		|	У.ЦелевоеПодразделение,
		|	СУММА(У.Сумма)
		|ИЗ
		|	Документ.А_ПередачаНачисленийМеждуПодразделениями.Удержания КАК У
		|ГДЕ
		|	У.Ссылка = &Ссылка
		|СГРУППИРОВАТЬ ПО
		|	У.ИсходноеПодразделение, У.ЦелевоеПодразделение";
```
new_string:
```bsl
		|ВЫБРАТЬ
		|	ВЫБОР КОГДА У.СтатьяАктивовПассивов = ЗНАЧЕНИЕ(ПланВидовХарактеристик.СтатьиАктивовПассивов.ПустаяСсылка)
		|		ТОГДА &СтПрочПас ИНАЧЕ У.СтатьяАктивовПассивов КОНЕЦ,
		|	У.АналитикаАктивовПассивов,
		|	У.ИсходноеПодразделение,
		|	У.ЦелевоеПодразделение,
		|	СУММА(У.Сумма)
		|ИЗ
		|	Документ.А_ПередачаНачисленийМеждуПодразделениями.Удержания КАК У
		|ГДЕ
		|	У.Ссылка = &Ссылка
		|СГРУППИРОВАТЬ ПО
		|	ВЫБОР КОГДА У.СтатьяАктивовПассивов = ЗНАЧЕНИЕ(ПланВидовХарактеристик.СтатьиАктивовПассивов.ПустаяСсылка)
		|		ТОГДА &СтПрочПас ИНАЧЕ У.СтатьяАктивовПассивов КОНЕЦ,
		|	У.АналитикаАктивовПассивов,
		|	У.ИсходноеПодразделение, У.ЦелевоеПодразделение";
```

- [ ] **Step 2: Переименовать колонку-аналитику в блоке 1 (Начисления) для ясности**

old_string:
```bsl
		|	&СтОТ КАК Статья,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыНалогов.ПустаяСсылка) КАК ТипНалога,
```
new_string:
```bsl
		|	&СтОТ КАК Статья,
		|	ЗНАЧЕНИЕ(Перечисление.ТипыНалогов.ПустаяСсылка) КАК Аналитика,
```

- [ ] **Step 3: Переименовать `Выборка.ТипНалога` → `Выборка.Аналитика` в вызовах**

old_string:
```bsl
		_ПАП_ДобавитьДвижение(Выборка.Статья, Выборка.ТипНалога, Выборка.Исходное, ВидДвИсх, Сум);
		_ПАП_ДобавитьДвижение(Выборка.Статья, Выборка.ТипНалога, Выборка.Целевое, ВидДвЦел, Сум);
```
new_string:
```bsl
		_ПАП_ДобавитьДвижение(Выборка.Статья, Выборка.Аналитика, Выборка.Исходное, ВидДвИсх, Сум);
		_ПАП_ДобавитьДвижение(Выборка.Статья, Выборка.Аналитика, Выборка.Целевое, ВидДвЦел, Сум);
```

- [ ] **Step 4: Обобщить `_ПАП_ДобавитьДвижение` (ТипНалога → Аналитика)**

old_string:
```bsl
// Добавляет одну запись движения ПрочиеАктивыПассивы. Аналитика заполняется только если
// передан непустой ТипНалога (для статьи «Налоги»); иначе остаётся пустой (ОТ / Прочие пассивы).
Процедура _ПАП_ДобавитьДвижение(Статья, ТипНалога, Подразделение, ВидДв, Сумма)

	Запись = Движения.ПрочиеАктивыПассивы.Добавить();
	Запись.Период        = Дата;
	Запись.ВидДвижения   = ВидДв;
	Запись.Организация   = Организация;
	Запись.Подразделение = Подразделение;
	Запись.Статья        = Статья;
	Если ЗначениеЗаполнено(ТипНалога) Тогда
		Запись.Аналитика = ТипНалога;
	КонецЕсли;
```
new_string:
```bsl
// Добавляет одну запись движения ПрочиеАктивыПассивы. Аналитика заполняется только если передана
// непустой (для «Налоги» = ТипНалога; для удержаний — Удержания.АналитикаАктивовПассивов; иначе пусто,
// напр. «Оплата труда» / «Прочие пассивы» / «Денежные средства (у подотчетных лиц)» — статья без аналитики).
Процедура _ПАП_ДобавитьДвижение(Статья, Аналитика, Подразделение, ВидДв, Сумма)

	Запись = Движения.ПрочиеАктивыПассивы.Добавить();
	Запись.Период        = Дата;
	Запись.ВидДвижения   = ВидДв;
	Запись.Организация   = Организация;
	Запись.Подразделение = Подразделение;
	Запись.Статья        = Статья;
	Если ЗначениеЗаполнено(Аналитика) Тогда
		Запись.Аналитика = Аналитика;
	КонецЕсли;
```

- [ ] **Step 5: Code review + commit обоих файлов**

```bash
git add "Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl" "Documents/А_ПередачаНачисленийМеждуПодразделениями/Ext/ObjectModule.bsl"
git commit -m "feat(подотчётне2): Субконто1=ФЛ (3721) + ПередачаНач по строковой статье/аналитике удержаний"
```

---

## Task 5: Загрузка в БД

- [ ] **Step 1:** `/db-load-xml -Mode Partial` обоих `ObjectModule.bsl` + `/db-update -Dynamic+`. (Дизайнер на BaseERP должен быть закрыт.) Файлы: `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl,Documents/А_ПередачаНачисленийМеждуПодразделениями/Ext/ObjectModule.bsl`.

---

## Task 6: GREEN-8a — ТЧ-уровень (in-memory)

**Files:** Create `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/green_podzvitne_subconto.py`

- [ ] **Step 1: Скрипт** — заполнить ОЗП №1 в памяти (`ЗагрузитьНалогиИУдержанияИзЗарплатыБух`); проверить, что у строки Гуняги `Субконто1 = ФизическоеЛицо` (Гуняга) и статья «Денежные средства (у подотчетных лиц)»; алименты — Субконто1=Контрагент как раньше.

```python
# -*- coding: utf-8 -*-
import io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client as w
erp = w.Dispatch("V83.COMConnector").Connect('Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
ob = erp.Документы.А_ОтражениеЗПпоКазне.НайтиПоНомеру("000000001").ПолучитьОбъект()
ob.ЗагрузитьНалогиИУдержанияИзЗарплатыБух()
ok = False
for i in range(ob.УдержанияБухгалтерия.Количество()):
    s = ob.УдержанияБухгалтерия.Получить(i)
    флн = s.ФизическоеЛицо.Наименование if erp.ЗначениеЗаполнено(s.ФизическоеЛицо) else ""
    суб = s.Субконто1.Наименование if erp.ЗначениеЗаполнено(s.Субконто1) else "<нет>"
    ст = s.СтатьяАктивовПассивов.Наименование if erp.ЗначениеЗаполнено(s.СтатьяАктивовПассивов) else "<нет>"
    сч = s.СчетУчета.Код if erp.ЗначениеЗаполнено(s.СчетУчета) else "<нет>"
    print(f"   {флн[:22]:22s} | счёт={сч} | статья={ст[:28]:28s} | Субконто1={суб}")
    if флн.startswith("Гуняга"):
        ok = (сч == "3721" and флн.startswith(суб.split()[0]) if суб != "<нет>" else False) and ст.startswith("Денежные средства")
print("ИТОГ Гуняга Субконто1=ФЛ:", "GREEN ✅" if ok else "RED ❌")
```

- [ ] **Step 2: Запустить** — Expected: Гуняга счёт 3721, статья «Денежные средства (у подотчетных лиц)», Субконто1=Гуняга (GREEN ✅); алименты Субконто1=контрагент-получатель.
- [ ] **Step 3: Commit** `test(подотчётне2): GREEN-8a — Гуняга Субконто1=ФЛ`.

---

## Task 7: Handoff — UI-перепроведение

- [ ] **Step 1:** Пользователю: ОЗП №1 → **Заполнить всё** → **Создать ОЗФУ** (провести) → **Создать ПередачаНач** (провести). Только UI. СТОП до подтверждения.

---

## Task 8: GREEN-8b (после UI-перепроведения)

**Files:** Create `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/green_podzvitne_subconto_reconciliation.py`

- [ ] **Step 1: Скрипт** — (а) ОЗФУ Гуняга: УдержаннаяЗарплата строка счёт 3721 + Субконто1=Гуняга; (б) ПередачаНач №1 ПАП: статья «Денежные средства (у подотчетных лиц)» Σ400 (КМД-2 Приход/Строительство Расход), аналитика пуста; алименты на «Прочие пассивы» без изменений; (в) КонтрольБаланса не вырос.
- [ ] **Step 2: Прогнать**, зафиксировать GREEN. **Step 3: Commit**.

---

## Task 9: Knowledge + память
- [ ] LESSONS §25 (подотчётне: Субконто1=ФЛ для 3721; ПередачаНач per-row статья; статья «Без аналитики»). KNOWLEDGE_MAP строка. Память — дополнить topic-файл удержаний. Commit.

---

## Self-Review
- **Покрытие спеки:** §3 ЧастьA → Task 2; §4 B.1 → Task 3; §4 B.2 → Task 4; RED → Task 1; загрузка → Task 5; GREEN-8a → Task 6; UI → Task 7; GREEN-8b → Task 8.
- **Плейсхолдеры:** old/new дословны; скрипты полны.
- **Согласованность:** `Аналитика` (переименование) едина в блоке1/вызовах/процедуре; `Доп` структура с `СтатьяАктивовПассивов,АналитикаАктивовПассивов` ↔ имена реквизитов ТЧ.
