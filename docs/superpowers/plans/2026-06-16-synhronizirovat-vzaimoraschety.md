# СинхронизироватьВзаиморасчеты — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Перепрофилировать обработку `СинхронизироватьВзаиморасчеты` (сейчас — дословный клон товарной `СинхронизироватьТоварыТолькоТовары`) под контроль расхождений взаиморасчётов по контрагентам ERP↔BuhBud, все 3 фазы.

**Architecture:** Двухуровневая форма-«трилистник» (мастер `ТаблицаКонтрагентов` → позиции `ТаблицаРасхождений` Контрагент×Договор×Валюта → `ТаблицаДокументов`). Фаза 1 — сверка остатков (ERP `РасчетыСКлиентами`+`РасчетыСПоставщиками` через `АналитикаУчетаПоПартнерам`; BuhBud `Хозрасчетный` расчётные счета через COM, сопоставление по `А_ИдКод`). Фаза 2 — анализ документов через `СоответствияОбъектовИнформационныхБаз` по Регистратору (= реальный документ-источник, проверено). Фаза 3 — действия Розпровести/Провести/Перепровести в ЕРП (UI-сессия) / На обмін в BuhBud.

**Tech Stack:** 1C:Enterprise 8.3 BSL, внешняя обработка (.epf), COM `V83.COMConnector` к BuhBud, MCP `1c-workerp`, Python COM тесты (`mcp__python-runner__run_command`), skills `epf-build`/`form-validate`/`meta-validate`/`1c-bsl-review`.

**Спека:** `docs/superpowers/specs/2026-06-16-synhronizirovat-vzaimoraschety-design.md`

**Донор запросов сверки:** `_Rarzrabotki/Отчеты/А_СравнитьОстаткиВзаиморасчетыКонтрагентыЕРПсBASБухгалтерия/Ext/ObjectModule.bsl`

**Шаблон-сиблинг (структура BSL/формы Фаз 1-3):** `_Rarzrabotki/Обработки/СинхронизироватьТоварыТолькоТовары/` (читать как образец логики; целевой клон уже содержит этот код дословно — задача в его перепрофилировании).

---

## Соглашения по «тестам» в 1С (Rule #-1)

В 1С нет pytest. «Тест» = прогон запроса/логики на **живой базе** ПЕРЕД вставкой в BSL:
- ERP-запросы → `mcp__1c-workerp__execute_query` (период задавать литералом `ДАТАВРЕМЯ(...)`, НЕ параметром — строковый параметр периода не коэрцится в виртуальной таблице).
- BuhBud COM-запросы и smoke → Python скрипт через `mcp__python-runner__run_command` (интерпретатор Python313, `win32com`).
- XML/форма → skills `meta-validate` / `form-validate`.
- Сборка → skill `epf-build`; smoke открытия → Python COM `ВнешниеОбработки.Создать`.

**Эталонный контрагент для тестов** (есть движения, янв-2026):
- Контрагент.А_ИдКод = `2b026266-3aa5-11ef-80d2-00155d235309` (ТОВ АСТАРТА АГРО ПРОТЕЇН)
- Договор.А_ИдКод = `480159a5-7fe7-11ef-80d3-00155d235309`, Валюта 980 (грн)

---

## File Structure

| Файл | Ответственность | Действие |
|---|---|---|
| `_Rarzrabotki/Обработки/СинхронизироватьВзаиморасчеты.xml` | метаданные: объектные реквизиты + 3 ТЧ | Modify (схема) |
| `…/СинхронизироватьВзаиморасчеты/Ext/ObjectModule.bsl` | Фазы 1-3 (запросы + логика) | Modify (ядро) |
| `…/Forms/Форма.xml` | метаданные формы | (обычно без правок) |
| `…/Forms/Форма/Ext/Form.xml` | раскладка формы: таблицы/колонки/команды | Modify (колонки) |
| `…/Forms/Форма/Ext/Form/Module.bsl` | модуль формы: команды, оформление, резюме | Modify |
| `_Rarzrabotki/Python/test/test_synhron_vzaimoraschety.py` | Python COM верификация запросов | Create |
| `_Rarzrabotki/Обработки/СинхронизироватьВзаиморасчеты.epf` | собранная обработка | Rebuild |
| зеркало в `C:/Configuration_downloads/BASERP25/_Rarzrabotki/…` | основная конфигурация (Rule #4) | Copy |

Каждая задача = самодостаточное изменение + проверка + коммит.

---

## Task 1: Python COM-верификация всех запросов (Rule #-1 RED→GREEN)

Цель — зафиксировать рабочие тексты 4 запросов на живой базе ДО переноса в BSL. ERP-стороны (Фаза 1 UNION, Фаза 2 прямой) уже проверены через MCP; здесь добавляем BuhBud-стороны (через COM) и закрепляем всё одним скриптом.

**Files:**
- Create: `_Rarzrabotki/Python/test/test_synhron_vzaimoraschety.py`

- [ ] **Step 1: Написать скрипт-харнес**

```python
import win32com.client, sys
if sys.stdout.encoding != 'utf-8':
    sys.stdout.reconfigure(encoding='utf-8')

v8 = win32com.client.Dispatch("V83.COMConnector")
erp = v8.Connect('Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
buh = v8.Connect('Srvr="localhost";Ref="bas_industrialbud";Usr="cfo";Pwd="2442"')

Н = erp.Дата(2026,1,1); К = erp.Дата(2026,1,31,23,59,59)
КИ = "2b026266-3aa5-11ef-80d2-00155d235309"
ДИ = "480159a5-7fe7-11ef-80d3-00155d235309"

def erp_query(text, **p):
    q = erp.NewObject("Запрос"); q.Текст = text
    for k,v in p.items(): q.УстановитьПараметр(k,v)
    return q.Выполнить().Выгрузить()

# 1) Фаза 1 ERP (UNION РасчетыСКлиентами + РасчетыСПоставщиками)
tz = erp_query(ТЕКСТ_ЕРП_ОСТАТКИ, НачалоПериода=Н, КонецПериода=К)
print("ERP остатки строк:", tz.Количество())

# 2) Фаза 2 прямой ERP
tz2 = erp_query(ТЕКСТ_ЕРП_ОБОРОТЫ_ДОК, НоменклатураЗаглушка=0)  # см. текст ниже
# (период литералом внутри текста; КИ/ДИ параметрами)

# 3) Фаза 1 BuhBud COM
qb = buh.NewObject("Запрос"); qb.Текст = ТЕКСТ_BUH_ОСТАТКИ
# спСчет, ВалютаГривня, масКодПоЕДРПОУ — заполнить как в доноре
print("BuhBud остатки строк:", qb.Выполнить().Выгрузить().Количество())

# 4) Фаза 2 обратный BuhBud COM
qb2 = buh.NewObject("Запрос"); qb2.Текст = ТЕКСТ_BUH_ОБОРОТЫ_ДОК
print("BuhBud документы строк:", qb2.Выполнить().Выгрузить().Количество())
```

Полные тексты запросов — в Tasks 3-4 (ниже). Скрипт прогоняет каждый и печатает `Количество()`.

- [ ] **Step 2: Прогнать**

Run: `mcp__python-runner__run_command` → `python _Rarzrabotki/Python/test/test_synhron_vzaimoraschety.py`
Expected: все 4 печати без исключений, `> 0` строк по эталонному контрагенту (для BuhBud-остатков — `>= 0`).

- [ ] **Step 3: Сверка Σ с отчётом-донором (тест №3)**

В скрипте добавить суммирование КонОстаток по эталонному контрагенту из (1) ERP и (3) BuhBud; вручную сравнить с отчётом `А_СравнитьОстаткиВзаиморасчеты…` за тот же период/контрагента. Допуск `Окр(Δ,2)=0`.
Expected: совпадает (или зафиксировать known-diff и причину).

- [ ] **Step 4: Commit**

```bash
git add _Rarzrabotki/Python/test/test_synhron_vzaimoraschety.py
git commit -m "test: Python COM верификация запросов СинхронизироватьВзаиморасчеты"
```

---

## Task 2: XML — объектные реквизиты + схема 3 ТЧ

**Files:**
- Modify: `_Rarzrabotki/Обработки/СинхронизироватьВзаиморасчеты.xml`

Текущая схема (товарная): объектные `Номенклатура`,`Склад`; ТЧ `ТаблицаРасхождений` (Номенклатура,Склад,…), `ТаблицаДокументов` (Номенклатура,Склад,Количество…), `ТаблицаСкладов`. Преобразовать в взаиморасчётную (имена/типы — см. §5 спеки).

- [ ] **Step 1: Объектные реквизиты**

Оставить `НачалоПериода`,`ОкончаниеПериода` (Дата), `ПоказыватьТолькоРасхождения` (Булево). Реквизит `Номенклатура` → `Контрагент` (тип `СправочникСсылка.Контрагенты`); `Склад` → `Договор` (тип `СправочникСсылка.ДоговорыКонтрагентов`). Имена `<Name>` менять, `uuid` оставлять прежние.

- [ ] **Step 2: ТЧ `ТаблицаРасхождений`**

Поля: `Раздел`(Строка 25), `Контрагент`(СправочникСсылка.Контрагенты), `Договор`(СправочникСсылка.ДоговорыКонтрагентов), `Валюта`(СправочникСсылка.Валюты), `НачОстатокЕРП`,`НачОстатокБух`,`ПриходЕРП`,`ПриходБух`,`РасходЕРП`,`РасходБух`,`ОстатокЕРП`,`ОстатокБух`,`Разница`(Число 15,2), `Синхронизировать`(Булево), `КонтрагентКлюч`(Строка 50),`ДоговорКлюч`(Строка 50),`ВалютаКод`(Строка 10).
(Переименование из товарных: Номенклатура→Контрагент, Склад→Договор; добавить Валюта/ВалютаКод/ДоговорКлюч; НоменклатураКлюч→КонтрагентКлюч, СкладКлюч→ДоговорКлюч.)

- [ ] **Step 3: ТЧ `ТаблицаДокументов`**

Поля: `Раздел`(Строка 25),`Контрагент`,`Договор`,`ТипДокумента`(Строка 100),`ДокументЕРП`(тип: `ДокументСсылка.<все регистраторы РасчетыСКлиентами/Поставщиками>` ИЛИ проще — составной из ключевых: РеализацияТоваровУслуг, АктВыполненныхРабот, ПриходныйКассовыйОрдер, РасходныйКассовыйОрдер, ПоступлениеБезналичныхДенежныхСредств, СписаниеБезналичныхДенежныхСредств, ВзаимозачетЗадолженности, ЗаказКлиента, ВводОстатков; + Строка для fallback),`ДокументБух`(Строка 150),`СуммаЕРП`,`СуммаБух`(Число 15,2),`Действие`(Строка 50),`Статус`(Строка 150),`UIDДокумента`(Строка 50),`UIDДокументаБух`(Строка 50),`ЕстьСоответствие`(Булево),`Синхронизировать`(Булево),`ЕстьРасхождение`(Булево).
(Переименование: Номенклатура→Контрагент, Склад→Договор, Количество*→Сумма*.)

- [ ] **Step 4: ТЧ `ТаблицаСкладов` → `ТаблицаКонтрагентов`**

Переименовать ТЧ. Поля: `Контрагент`(СправочникСсылка.Контрагенты),`КонтрагентКлюч`(Строка 50),`Договоров`(Число),`СуммаРазницы`(Число 15,2),`Документов`,`Действий`,`ТребуютРешения`(Число),`Синхронизировать`(Булево).
(Из товарных: Склад→Контрагент, СкладКлюч→КонтрагентКлюч, Позиций→Договоров.)

- [ ] **Step 5: Валидация XML**

Run: skill `/meta-validate` для `DataProcessor.СинхронизироватьВзаиморасчеты` (или `epf-validate` после сборки).
Expected: без ошибок схемы.

- [ ] **Step 6: Commit**

```bash
git add _Rarzrabotki/Обработки/СинхронизироватьВзаиморасчеты.xml
git commit -m "feat: схема ТЧ СинхронизироватьВзаиморасчеты (Контрагент×Договор×Валюта)"
```

---

## Task 3: ObjectModule — Фаза 1 (сверка остатков)

**Files:**
- Modify: `…/СинхронизироватьВзаиморасчеты/Ext/ObjectModule.bsl` (область `Фаза1_ПорівнянняЗалишків`)

Функцию `ПодключитьсяКBuhBud()` оставить без изменений. Переписать тело Фазы 1.

- [ ] **Step 1: `СравнитьОстатки()` — каркас**

Очистить `ТаблицаРасхождений`,`ТаблицаДокументов`,`ТаблицаКонтрагентов`. Подключиться к BuhBud. Вызвать `СравнитьОстатки_Взаиморасчеты(V83)`. Освободить V83. Вызвать `ЗаполнитьСводПоКонтрагентам()`. Вернуть строку-резюме ("Контрагентів з розбіжностями: …, позицій: …").

- [ ] **Step 2: `ПолучитьОстаткиЕРП()` — UNION (текст ПРОВЕРЕН через MCP)**

Возвращает ТЗ: КонтрагентИдКод, ДоговорИдКод, ВалютаКод, НачОстаток, Приход, Расход, КонОстаток, Контрагент(ссылка), Договор(ссылка), ВалютаСсылка, Раздел.

```
ВЫБРАТЬ
	ВЫБОР КОГДА ВЗ.Сторона = 1 ТОГДА "Клієнт" ИНАЧЕ "Постачальник" КОНЕЦ КАК Раздел,
	ВЗ.Контрагент КАК Контрагент,
	ВЗ.КонтрагентИдКод КАК КонтрагентИдКод,
	ВЗ.Договор КАК Договор,
	ВЗ.ДоговорИдКод КАК ДоговорИдКод,
	ВЗ.ВалютаСсылка КАК ВалютаСсылка,
	ВЗ.ВалютаКод КАК ВалютаКод,
	СУММА(ВЗ.НачОстаток) КАК НачОстаток,
	СУММА(ВЗ.Приход) КАК Приход,
	СУММА(ВЗ.Расход) КАК Расход,
	СУММА(ВЗ.КонОстаток) КАК КонОстаток
ИЗ (
	ВЫБРАТЬ
		1 КАК Сторона,
		регА.Контрагент КАК Контрагент,
		регА.Контрагент.А_ИдКод КАК КонтрагентИдКод,
		регА.Договор КАК Договор,
		регА.Договор.А_ИдКод КАК ДоговорИдКод,
		регА.Договор.ВалютаВзаиморасчетов КАК ВалютаСсылка,
		регА.Договор.ВалютаВзаиморасчетов.Код КАК ВалютаКод,
		Расч.СуммаНачальныйОстаток КАК НачОстаток,
		Расч.СуммаПриход КАК Приход,
		Расч.СуммаРасход КАК Расход,
		Расч.СуммаКонечныйОстаток КАК КонОстаток
	ИЗ РегистрНакопления.РасчетыСКлиентами.ОстаткиИОбороты(&НачалоПериода, &КонецПериода, , , ) КАК Расч
		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.АналитикаУчетаПоПартнерам КАК регА
		ПО Расч.АналитикаУчетаПоПартнерам = регА.КлючАналитики
	ОБЪЕДИНИТЬ ВСЕ
	ВЫБРАТЬ
		2,
		регА.Контрагент, регА.Контрагент.А_ИдКод,
		регА.Договор, регА.Договор.А_ИдКод,
		регА.Договор.ВалютаВзаиморасчетов, регА.Договор.ВалютаВзаиморасчетов.Код,
		Расч.СуммаНачальныйОстаток, Расч.СуммаПриход, Расч.СуммаРасход, Расч.СуммаКонечныйОстаток
	ИЗ РегистрНакопления.РасчетыСПоставщиками.ОстаткиИОбороты(&НачалоПериода, &КонецПериода, , , ) КАК Расч
		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.АналитикаУчетаПоПартнерам КАК регА
		ПО Расч.АналитикаУчетаПоПартнерам = регА.КлючАналитики
) КАК ВЗ
СГРУППИРОВАТЬ ПО
	ВЗ.Сторона, ВЗ.Контрагент, ВЗ.КонтрагентИдКод, ВЗ.Договор, ВЗ.ДоговорИдКод, ВЗ.ВалютаСсылка, ВЗ.ВалютаКод
```

Параметры: `НачалоПериода`=НачалоПериода, `КонецПериода`=КонецДня(ОкончаниеПериода). Применить фильтры по объектным `Контрагент`/`Договор`, если заполнены (через `&Фильтр… ИЛИ …` как в товарах).

- [ ] **Step 3: `ПолучитьОстаткиBuhBud()` — COM (адаптировать донора, non-ПоДням ветку)**

Взять текст из донора `…/Отчеты/А_СравнитьОстаткиВзаиморасчеты…/Ext/ObjectModule.bsl` строки 335-428 (ветка «Иначе»), + список счетов строки 209-223, + `ВалютаГривня` строка 206, + `КонвертироватьТЗ_BuhBud` строки 502-532. Колонки результата: КонтрагентИдКод, ДоговорИдКод, ВалютаКод, НачОстаток, Приход, Расход, КонОстаток (переименовать СуммаНачальныйОстаток_BuhBud→НачОстаток и т.д. при выгрузке). Передача через `V83.ЗначениеВСтрокуВнутр`→`ЗначениеИзСтрокиВнутр`. Конвертацию ИдКод→ссылки (Контрагент/Договор/Валюта) сделать через временную таблицу (как `КонвертироватьТЗ_BuhBud`). Список ЕДРПОУ организаций `А_ВБалансе` собрать как в товарном Фаза 1 (строки 189-208 сиблинга).

- [ ] **Step 4: `СвестиТаблицы_Взаиморасчеты(ТЗ_ЕРП, ТЗ_Бух)`**

Индекс `Соответствие` по ключу `КонтрагентКлюч|ДоговорКлюч|ВалютаКод` (БЕЗ Раздела). Влить ERP (+= Нач/Приход/Расход/Кон в ветки ЕРП), влить BuhBud (для отсутствующих в ERP — найти ссылки по `НайтиПоРеквизиту("А_ИдКод", …)` и Валюту по Коду). По каждому ключу: `Разница = ОстатокЕРП − ОстатокБух`; если `Окр(Разница,2)=0 И Окр(НачЕРП−НачБух,2)=0 И Окр(ПриходЕРП−ПриходБух,2)=0 И Окр(РасходЕРП−РасходБух,2)=0` — пропустить. Иначе добавить строку в `ТаблицаРасхождений` (Раздел из ERP-ветки или по знаку; Контрагент/Договор/Валюта ссылками; ключи строками; Синхронизировать=Истина). НЕ исключать никакие контрагенты (решение пользователя: все без исключений).

- [ ] **Step 5: `ЗаполнитьСводПоКонтрагентам()`**

Свернуть `ТаблицаРасхождений` по `КонтрагентКлюч` в `ТаблицаКонтрагентов`: Договоров = число позиций, СуммаРазницы += |Разница|, Синхронизировать=Истина.

- [ ] **Step 6: Прогон Фазы 1**

Прогнать через MCP/Python тексты Step 2-3 на эталонном контрагенте; затем (после сборки в Task 8) фактический прогон формы. Здесь — проверить тексты запросов на живой базе (Rule #-1).
Expected: ERP и BuhBud возвращают строки, ключи стыкуются.

- [ ] **Step 7: Commit**

```bash
git add "_Rarzrabotki/Обработки/СинхронизироватьВзаиморасчеты/Ext/ObjectModule.bsl"
git commit -m "feat: Фаза 1 сверка остатков взаиморасчётов ERP↔BuhBud"
```

---

## Task 4: ObjectModule — Фаза 2 (анализ документов)

**Files:**
- Modify: `…/Ext/ObjectModule.bsl` (область `Фаза2_АналізДокументів`)

- [ ] **Step 1: `АнализироватьДокументы(ИндексКонтрагента = -1)`**

Каркас как в товарах (область строки 475-559 сиблинга): цель — отмеченные строки `ТаблицаКонтрагентов`; сброс счётчиков; найти узел обмена в BuhBud; по каждому контрагенту — по всем его позициям `ТаблицаРасхождений` вызвать `АнализДокументов_Взаиморасчеты(V83, СтрРасх, УзелОбмена)`; пересчитать счётчики Документов/Действий/ТребуютРешения. Переименовать Склад→Контрагент в навигации.

- [ ] **Step 2: `АнализДокументов_Взаиморасчеты()` — прямой запрос (ПРОВЕРЕН через MCP)**

```
ВЫБРАТЬ
	Об.Регистратор КАК Регистратор,
	ПРЕДСТАВЛЕНИЕ(Об.Регистратор) КАК РегистраторПредст,
	Об.Сумма КАК Сумма,
	Соотв.УникальныйИдентификаторПриемника КАК UIDвBuhBud,
	Соотв.ТипПриемника КАК ТипПриемника,
	Соотв.ТипИсточника КАК ТипИсточника
ИЗ (
	ВЫБРАТЬ Об1.Регистратор КАК Регистратор, Об1.СуммаПриход - Об1.СуммаРасход КАК Сумма
	ИЗ РегистрНакопления.РасчетыСКлиентами.Обороты(
		&НачалоПериода, &КонецПериода, Авто,
		АналитикаУчетаПоПартнерам.Контрагент.А_ИдКод = &КонтрагентИдКод
			И АналитикаУчетаПоПартнерам.Договор.А_ИдКод = &ДоговорИдКод) КАК Об1
	ОБЪЕДИНИТЬ ВСЕ
	ВЫБРАТЬ Об2.Регистратор, Об2.СуммаПриход - Об2.СуммаРасход
	ИЗ РегистрНакопления.РасчетыСПоставщиками.Обороты(
		&НачалоПериода, &КонецПериода, Авто,
		АналитикаУчетаПоПартнерам.Контрагент.А_ИдКод = &КонтрагентИдКод
			И АналитикаУчетаПоПартнерам.Договор.А_ИдКод = &ДоговорИдКод) КАК Об2
) КАК Об
	ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СоответствияОбъектовИнформационныхБаз КАК Соотв
	ПО Об.Регистратор = Соотв.УникальныйИдентификаторИсточника
		И Соотв.УзелИнформационнойБазы ССЫЛКА ПланОбмена.ОбменУправлениеПредприятиемБухгалтерия20
```

> Примечание: при пустом `ДоговорИдКод` (договор без А_ИдКод) фильтр `… = ""` корректен — позиции без договора сматчатся между собой. Параметры: период литералом/значением, `КонтрагентИдКод`=СтрРасх.КонтрагентКлюч, `ДоговорИдКод`=СтрРасх.ДоговорКлюч.

Обработка каждого документа — `ОбробитиДокументиЕРП()` (адаптация строк 866-929 сиблинга): Сумма вместо Количество; определение `ТипДокумента` через `ТипИсточника`/Метаданные; при наличии `UIDвBuhBud` → `ПроверитьДокументВBuhBud()` (строки 933-986 сиблинга, без изменений логики, Количество→Сумма); иначе Действие="Розпровести в ЕРП". Поля строки: Контрагент/Договор из СтрРасх; Раздел из СтрРасх.

- [ ] **Step 3: `АнализДокументовBuhBud_Взаиморасчеты()` — обратный COM-запрос**

Адаптировать товарный обратный анализ (строки 609-859 сиблинга) на расчётные счета и субконто Контрагент/Договор. Текст BuhBud-запроса (периодичность Регистратор):

```
ВЫБРАТЬ
	ВЗ.Регистратор КАК Регистратор,
	ПРЕДСТАВЛЕНИЕ(ВЗ.Регистратор) КАК РегистраторПредст,
	СУММА(ВЗ.СуммаНетто) КАК СуммаИтого
ИЗ (
	ВЫБРАТЬ
		Ост.Регистратор КАК Регистратор,
		Ост.СуммаОборотДт - Ост.СуммаОборотКт КАК СуммаНетто
	ИЗ РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты(
			&НачалоПериода, &КонецПериода, Регистратор, , Счет В ИЕРАРХИИ (&Счет), , ) КАК Ост
		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ПланСчетов.Хозрасчетный.ВидыСубконто КАК ПозК
			ПО Ост.Счет = ПозК.Ссылка
				И ПозК.ВидСубконто = ЗНАЧЕНИЕ(ПланВидовХарактеристик.ВидыСубконтоХозрасчетные.Контрагенты)
		ЛЕВОЕ СОЕДИНЕНИЕ ПланСчетов.Хозрасчетный.ВидыСубконто КАК ПозД
			ПО Ост.Счет = ПозД.Ссылка
				И ПозД.ВидСубконто = ЗНАЧЕНИЕ(ПланВидовХарактеристик.ВидыСубконтоХозрасчетные.Договоры)
	ГДЕ
		(Ост.СуммаОборотДт <> 0 ИЛИ Ост.СуммаОборотКт <> 0)
		И Ост.Организация.КодПоЕДРПОУ В (&масКодПоЕДРПОУ)
		И (ВЫБОР
			КОГДА ПозК.НомерСтроки = 1 ТОГДА ВЫРАЗИТЬ(Ост.Субконто1 КАК Справочник.Контрагенты).А_ИдКод
			КОГДА ПозК.НомерСтроки = 2 ТОГДА ВЫРАЗИТЬ(Ост.Субконто2 КАК Справочник.Контрагенты).А_ИдКод
			КОГДА ПозК.НомерСтроки = 3 ТОГДА ВЫРАЗИТЬ(Ост.Субконто3 КАК Справочник.Контрагенты).А_ИдКод
			ИНАЧЕ "" КОНЕЦ) = &КонтрагентИдКод
		И ((ВЫБОР
			КОГДА ПозД.НомерСтроки = 1 ТОГДА ВЫРАЗИТЬ(Ост.Субконто1 КАК Справочник.ДоговорыКонтрагентов).А_ИдКод
			КОГДА ПозД.НомерСтроки = 2 ТОГДА ВЫРАЗИТЬ(Ост.Субконто2 КАК Справочник.ДоговорыКонтрагентов).А_ИдКод
			КОГДА ПозД.НомерСтроки = 3 ТОГДА ВЫРАЗИТЬ(Ост.Субконто3 КАК Справочник.ДоговорыКонтрагентов).А_ИдКод
			ИНАЧЕ "" КОНЕЦ) = &ДоговорИдКод
			ИЛИ ПозД.НомерСтроки ЕСТЬ NULL)
) КАК ВЗ
СГРУППИРОВАТЬ ПО ВЗ.Регистратор
ИМЕЮЩИЕ СУММА(ВЗ.СуммаНетто) <> 0
```

Логика обработки (карты ВжеЗнайдені/МапаПоUIDБух/МапаПоПредставлению; матчинг **по Контрагент+Договор**, НЕ по Разделу; UID документа BuhBud через `V83.XMLСтрока(...)`; обратный резолв в ERP через `СоответствияОбъектовИнформационныхБаз`; действия «На обмін в BuhBud»/«Провести в ЕРП»/«Переобмін») — как в товарах, заменив Номенклатура/Склад→Контрагент/Договор, Количество→Сумма. Финальная нормализация: если СуммаЕРП==СуммаБух (Окр,2) и ≠0 → Статус="Збігається", Синхронизировать=Ложь.

- [ ] **Step 4: Прогон Фазы 2**

Прогнать тексты Step 2-3 через MCP/Python на эталонном контрагенте.
Expected: прямой возвращает документы (Поступление безнал и т.д.) с UIDБух; обратный отрабатывает без исключений.

- [ ] **Step 5: Commit**

```bash
git add "_Rarzrabotki/Обработки/СинхронизироватьВзаиморасчеты/Ext/ObjectModule.bsl"
git commit -m "feat: Фаза 2 анализ документов взаиморасчётов (прямой+обратный)"
```

---

## Task 5: ObjectModule — Фаза 3 (действия синхронизации)

**Files:**
- Modify: `…/Ext/ObjectModule.bsl` (область `Фаза3_Синхронізація`)

Логика Фазы 3 у товаров и взаиморасчётов **идентична** (работает с UID/типом документа, не с предметной аналитикой). Менять минимум.

- [ ] **Step 1: Переименования в Фазе 3**

В `ВыполнитьСинхронизацию`, `ВыполнитьДействиеСтроки`, `РаспровестиВЕРП`, `ПровестиВЕРП`, `ПоставитьНаОбменВBuhBud`, `ПерепровестиВЕРП`, `НайтиСсылкуЕРПДляПерепроведения` — заменить упоминания предметных полей (если есть Номенклатура/Склад в сообщениях) на Контрагент/Договор. Поля UID/ТипДокумента/ДокументЕРП/Действие/Статус — без изменений. Подтвердить, что `ПерепровестиВЕРП` идёт через объект (UI-сессия), без `V83`-перепровода (память `feedback_com_repost_skips_registrator_raschetov`).

- [ ] **Step 2: Проверка через bsl-review (частично)**

Run: skill `/1c-bsl-review` на ObjectModule.bsl.
Expected: нет запретных имён (`Ссылка`,`Выполнить`,`МетаДанные`), нет критичных замечаний.

- [ ] **Step 3: Commit**

```bash
git add "_Rarzrabotki/Обработки/СинхронизироватьВзаиморасчеты/Ext/ObjectModule.bsl"
git commit -m "feat: Фаза 3 действия синхронизации (адаптация под взаиморасчёты)"
```

---

## Task 6: Форма — Form.xml (колонки/таблицы/команды)

**Files:**
- Modify: `…/Forms/Форма/Ext/Form.xml`

- [ ] **Step 1: Таблица мастера**

`ТаблицаСкладовФорма` → `ТаблицаКонтрагентовФорма` (источник `Объект.ТаблицаКонтрагентов`). Колонки: Контрагент, Договоров, СуммаРазницы, Документов, Действий, ТребуютРешения, Синхронизировать. Команда `АнализТекущегоСчета`→`АнализТекущегоКонтрагента`.

- [ ] **Step 2: Таблица позиций**

`ТаблицаРасхожденийФорма` (источник `Объект.ТаблицаРасхождений`): колонки Раздел, Контрагент, Договор, Валюта, НачОстатокЕРП/Бух, ПриходЕРП/Бух, РасходЕРП/Бух, ОстатокЕРП/Бух, Разница, Синхронизировать. (Номенклатура→Контрагент, Склад→Договор, +Валюта.)

- [ ] **Step 3: Таблица документов**

`ТаблицаДокументовФорма`: Раздел, Контрагент, Договор, ТипДокумента, ДокументЕРП, ДокументБух, СуммаЕРП, СуммаБух, Действие, Статус, Синхронизировать. (Количество→Сумма.)

- [ ] **Step 4: Валидация формы**

Run: skill `/form-validate` (передавать `Форма.xml`, не Form.xml напрямую — память `partial_load_form_module`).
Expected: без ошибок (ложные WARN по внешним обработкам игнор — память `form_validate_tilde_spisok_false_positive`).

- [ ] **Step 5: Commit**

```bash
git add "_Rarzrabotki/Обработки/СинхронизироватьВзаиморасчеты/Forms/"
git commit -m "feat: форма СинхронизироватьВзаиморасчеты — колонки взаиморасчётов"
```

---

## Task 7: Форма — Module.bsl (команды, оформление, резюме)

**Files:**
- Modify: `…/Forms/Форма/Ext/Form/Module.bsl`

- [ ] **Step 1: Переименования и резюме**

`ТаблицаСкладов`→`ТаблицаКонтрагентов`, `СкладКлюч`→`КонтрагентКлюч`, `Склад`→`Контрагент`/`Договор`, `Номенклатура`→`Контрагент` во всех обработчиках (`ОбновитьРезюмеИЗаголовки`, `НастроитьУсловноеОформление`, фильтр `ПрименитьФильтрДокументов`, команды). Текст резюме — про «контрагентів/договорів» вместо «складів/товарів». Цвет надписи — `ЦветаСтиля`, не XML (память `epf_load_xdto_textcolor`).

- [ ] **Step 2: Команда «Анализ текущего контрагента»**

`АнализТекущегоСчета`→`АнализТекущегоКонтрагента`; навигация по `ТаблицаКонтрагентовФорма.ТекущиеДанные`; индекс `Объект.ТаблицаКонтрагентов.Индекс(...)`.

- [ ] **Step 3: Условное оформление**

Поля оформления переключить на `ТаблицаКонтрагентовФорма`/`ТаблицаДокументовФорма`/`ТаблицаРасхожденийФорма`; статусы (ОК/Збігається/ПОМИЛКА/успех) без изменений.

- [ ] **Step 4: Проверка имён через bsl-review**

Run: skill `/1c-bsl-review` на Module.bsl формы.
Expected: нет ссылок на удалённые поля/таблицы; `&НаКлиенте`/`&НаСервере` корректны.

- [ ] **Step 5: Commit**

```bash
git add "_Rarzrabotki/Обработки/СинхронизироватьВзаиморасчеты/Forms/Форма/Ext/Form/Module.bsl"
git commit -m "feat: модуль формы СинхронизироватьВзаиморасчеты"
```

---

## Task 8: Сборка .epf + COM-smoke открытия

**Files:**
- Rebuild: `_Rarzrabotki/Обработки/СинхронизироватьВзаиморасчеты.epf`

- [ ] **Step 1: Сборка**

Run: skill `/epf-build` для `_Rarzrabotki/Обработки/СинхронизироватьВзаиморасчеты`.
Expected: `.epf` создан без «Исключение XDTO» (если есть — искать TextColor в Form.xml, память `epf_load_xdto_textcolor`).

- [ ] **Step 2: COM-smoke открытия (память `win32com_epf_smoke_gotchas`)**

```python
import win32com.client
v8 = win32com.client.Dispatch("V83.COMConnector")
erp = v8.Connect('Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
обр = erp.ВнешниеОбработки.Создать(r"C:\...\СинхронизироватьВзаиморасчеты.epf", False)
print("OK создан объект обработки:", обр)
```

Run: `mcp__python-runner__run_command`.
Expected: объект создаётся без ошибок компиляции модулей.

- [ ] **Step 3: Commit**

```bash
git add "_Rarzrabotki/Обработки/СинхронизироватьВзаиморасчеты.epf"
git commit -m "build: сборка СинхронизироватьВзаиморасчеты.epf + COM-smoke"
```

---

## Task 9: Перенос в основную конфигурацию + приёмка

**Files:**
- Copy: worktree → `C:/Configuration_downloads/BASERP25/_Rarzrabotki/…`

- [ ] **Step 1: Копирование (Rule #4)**

```bash
cp -r "<worktree>/_Rarzrabotki/Обработки/СинхронизироватьВзаиморасчеты.xml" \
      "<worktree>/_Rarzrabotki/Обработки/СинхронизироватьВзаиморасчеты" \
      "<worktree>/_Rarzrabotki/Обработки/СинхронизироватьВзаиморасчеты.epf" \
      "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Обработки/"
```

- [ ] **Step 2: Финальный bsl-review**

Run: skill `/1c-bsl-review` на ObjectModule.bsl + Module.bsl.
Expected: без критичных замечаний.

- [ ] **Step 3: Acceptance-чеклист (см. §10 спеки)**

- [ ] Python COM-тесты 4 запросов — PASS.
- [ ] Σ КонОстаток по эталонному контрагенту = отчёт-донор.
- [ ] epf-build + COM-smoke — PASS.
- [ ] Фаза 2 на 1-2 контрагентах — документы/действия/статусы корректны.
- [ ] Фаза 3 на 1-2 документах (UI) — отрабатывает, РСППС цел.

- [ ] **Step 4: Передать пользователю на приёмку в UI**

Сообщить: открыть `.epf` в «Дополнительные обработки», прогнать на периоде, проверить расхождения; массовую историю перепроводить самому в UI (память `feedback_user_reposts_history_self`).

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "chore: перенос СинхронизироватьВзаиморасчеты в основную конфигурацию"
```

---

## Self-Review (выполнен при написании плана)

- **Покрытие спеки:** §4 Фаза1→Task3, Фаза2→Task4, Фаза3→Task5; §5 модель→Task2; §6 валюта→Task3 Step3; §7 форма→Task6-7; §8 тесты→Task1 + Step’ы прогона; §9 перенос→Task9; §10 acceptance→Task9 Step3. Все разделы покрыты.
- **Плейсхолдеры:** тексты ERP-запросов (Task3 Step2, Task4 Step2) проверены через MCP; BuhBud-запросы (Task3 Step3, Task4 Step3) приведены полностью / с точной ссылкой на строки донора. Ссылки на строки сиблинга — указывают на читаемый образец, не плейсхолдер.
- **Согласованность имён:** ТЧ `ТаблицаКонтрагентов`/`ТаблицаРасхождений`/`ТаблицаДокументов`; поля КонтрагентКлюч/ДоговорКлюч/ВалютаКод; команда `АнализТекущегоКонтрагента`; форменные таблицы `…Форма` — единообразны между Task2/3/4/6/7.
