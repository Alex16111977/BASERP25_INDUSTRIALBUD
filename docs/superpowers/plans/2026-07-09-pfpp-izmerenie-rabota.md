# Измерение «Работа» в отчёте А_ПланФактныйПроизводствоПолный — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить самостоятельное измерение «Работа» (`СправочникСсылка.Номенклатура`) в отчёт А_ПланФактныйПроизводствоПолный — вариант A (структура группировок по умолчанию не меняется).

**Architecture:** Программная компоновка СКД: `ПриКомпоновкеРезультата` → `ПолучитьДанные(Период, СС)` строит плоскую ТЗ (6 веток UNION ALL) → внешний набор «Детали». Колонка `Работа` добавляется во все 6 веток сразу после `Этап` (труд-план `Р.Работа`, EV `ВРМ.Работа`, остальные — пустая ссылка Номенклатуры), в финальный SELECT + СГРУППИРОВАТЬ ПО, в `СоздатьПустуюТаблицу`, в синт. строки `ЗаполнитьПланНаФакт`; в Template.xml — поле-измерение. Спека: `docs/superpowers/specs/2026-07-09-pfpp-izmerenie-rabota-design.md`.

**Tech Stack:** 1С BSL (внешний отчёт .erf), СКД Template.xml (правка Python-скриптом — файл с табами), Python COM (`V83.COMConnector`, `C:\Python313\python.exe`), сборка `.claude/skills/epf-build/scripts/epf-build.ps1`.

**Рабочие пути:**
- Основная конфигурация (исходники, править ЗДЕСЬ): `C:\Configuration_downloads\BASERP25\_Rarzrabotki\Отчеты\А_ПланФактныйПроизводствоПолный\...`
- Git-worktree (зеркало для коммитов): `C:\Configuration_downloads\BASERP25\.claude\worktrees\funny-spence-f6c97b\_Rarzrabotki\...`
- Тесты: `C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\`
- Подключение: `Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"`

**Эталоны регрессии (весь период, менять НЕЛЬЗЯ):**
- СС «МД МХП ОРІЛЬ / СТІЛ МД МХП ОРІЛЬ»: Труд ПланЧасы=2795, ФактЧасы=2389; Материалы ФактГрн=1620137.97, ПланНаФактГрн=625675.03 (последний — только через .erf, это BSL-постобработка).
- СС «МД Вооз ЕМS Мала 2026 / Цех_МД Вооз ЕМS Мала 2026»: Σ ВиконанняЧасы (блок «1. Труд») = 2401.52.

---

### Task 1: Python COM тест нового запроса (Rule #-1, RED-фаза)

**Files:**
- Create: `C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_pfpp_rabota_razrez.py`

- [ ] **Step 1: Написать тест с ПОЛНЫМ новым запросом (1:1 будущий BSL)**

Текст запроса — дословно текущий запрос модуля со вставками `Работа` (5-я колонка каждой ветки, после Этап). Инварианты: строка с непустой Работой ⇒ Блок=«1. Труд» и Работа=Аналитика; труд-строка с ПланЧасы≠0 или ВиконанняЧасы≠0 ⇒ Работа непустая. Эталоны: ОРІЛЬ 2795/2389/1620137.97, ВООЗ 2401.52.

```python
# -*- coding: utf-8 -*-
# Rule #-1: НОВЫЙ запрос ПолучитьДанные с измерением Работа (9-я колонка грейна, после Этап).
# Проверка: выполнение, инварианты Работы, эталоны ОРІЛЬ (2795/2389/1620137.97) и ВООЗ (2401.52).
import win32com.client, sys, os, datetime
if sys.stdout.encoding != 'utf-8':
    sys.stdout.reconfigure(encoding='utf-8')
ERR = os.path.join(os.environ["TEMP"], "_pfpp_rabota_err.txt")
erp = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
def f(x): return float(x) if x is not None else 0.0
def пусто(ref):
    return ref is None or (hasattr(ref, 'Пустая') and ref.Пустая())
def уид(ref): return str(erp.String(ref.УникальныйИдентификатор()))

ТЕКСТ = """
ВЫБРАТЬ
    СС.Ссылка КАК СС,
    СС.Подразделение КАК Подр,
    СС.СкладПроизводство КАК СкладВир,
    СС.СкладМонтаж КАК СкладМонт
ПОМЕСТИТЬ ВТ_СС
ИЗ
    Справочник.А_СтруктураСебестоимости КАК СС
ГДЕ
    СС.Ссылка = &СС
;
ВЫБРАТЬ
    Р.Работа КАК Работа,
    МАКСИМУМ(Р.НаВыезде) КАК НаВыезде
ПОМЕСТИТЬ ВТ_РаботыСС
ИЗ
    Справочник.А_СтруктураСебестоимости.Работы КАК Р
        ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_СС КАК ВТ_СС
        ПО Р.Ссылка = ВТ_СС.СС
СГРУППИРОВАТЬ ПО
    Р.Работа
;
ВЫБРАТЬ
    ВТ_СС.Подр КАК Подразделение,
    "1. Труд" КАК Блок,
    ВЫБОР КОГДА Р.НаВыезде ТОГДА "Монтаж" ИНАЧЕ "Производство" КОНЕЦ КАК Категория,
    Р.Этап КАК Этап,
    Р.Работа КАК Работа,
    ЗНАЧЕНИЕ(Справочник.А_ОбщиеНазванияНоменклатуры.ПустаяСсылка) КАК ОбщееНазвание,
    НЕОПРЕДЕЛЕНО КАК Документ,
    Р.Работа КАК Аналитика,
    ЗНАЧЕНИЕ(Справочник.УпаковкиЕдиницыИзмерения.ПустаяСсылка) КАК Единица,
    Р.Количество КАК ПланЧасы,
    0 КАК ФактЧасы,
    0 КАК ВиконанняЧасы,
    0 КАК ПланКол,
    0 КАК ФактКол,
    Р.Сумма КАК ПланГрн,
    0 КАК ФактГрн
ПОМЕСТИТЬ ВТ_Детали
ИЗ
    Справочник.А_СтруктураСебестоимости.Работы КАК Р
        ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_СС КАК ВТ_СС
        ПО Р.Ссылка = ВТ_СС.СС

ОБЪЕДИНИТЬ ВСЕ

ВЫБРАТЬ
    ВТ_СС.Подр,
    "2. Материалы",
    ВЫБОР КОГДА К.Этап.Наименование ПОДОБНО "%Монтаж%" ТОГДА "Монтаж" ИНАЧЕ "Производство" КОНЕЦ,
    К.Этап,
    ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка),
    К.Номенклатура.А_ОбщееНазваниеНоменклатуры,
    К.Ссылка,
    К.Номенклатура,
    К.Номенклатура.ЕдиницаИзмерения,
    0,
    0,
    0,
    К.Количество,
    0,
    К.Сумма,
    0
ИЗ
    Справочник.А_СтруктураСебестоимости.Комплектующие КАК К
        ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_СС КАК ВТ_СС
        ПО К.Ссылка = ВТ_СС.СС

ОБЪЕДИНИТЬ ВСЕ

ВЫБРАТЬ
    Т.Подразделение,
    "1. Труд",
    "Производство",
    НЕОПРЕДЕЛЕНО,
    ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка),
    ЗНАЧЕНИЕ(Справочник.А_ОбщиеНазванияНоменклатуры.ПустаяСсылка),
    НЕОПРЕДЕЛЕНО,
    Т.Сотрудник.СотрудникЕРП.ФизическоеЛицо,
    ЗНАЧЕНИЕ(Справочник.УпаковкиЕдиницыИзмерения.ПустаяСсылка),
    0,
    Т.ЧасыФакт,
    0,
    0,
    0,
    0,
    0
ИЗ
    РегистрНакопления.А_ДанныеТабелирования КАК Т
        ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_СС КАК ВТ_СС
        ПО Т.Подразделение = ВТ_СС.Подр
ГДЕ
    Т.Период МЕЖДУ &НачП И &КонП

ОБЪЕДИНИТЬ ВСЕ

ВЫБРАТЬ
    ВЗ.Подразделение,
    "1. Труд",
    "Производство",
    НЕОПРЕДЕЛЕНО,
    ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка),
    ЗНАЧЕНИЕ(Справочник.А_ОбщиеНазванияНоменклатуры.ПустаяСсылка),
    НЕОПРЕДЕЛЕНО,
    ВЗ.ФизическоеЛицо,
    ЗНАЧЕНИЕ(Справочник.УпаковкиЕдиницыИзмерения.ПустаяСсылка),
    0,
    0,
    0,
    0,
    0,
    0,
    ВЗ.СуммаВзаиморасчетов
ИЗ
    РегистрНакопления.А_ВзаиморасчетыССотрудниками КАК ВЗ
        ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_СС КАК ВТ_СС
        ПО ВЗ.Подразделение = ВТ_СС.Подр
ГДЕ
    ВЗ.Регистратор ССЫЛКА Документ.ОтражениеЗарплатыВФинансовомУчете
    И ВЗ.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
    И ВЗ.Период МЕЖДУ &НачП И &КонП

ОБЪЕДИНИТЬ ВСЕ

ВЫБРАТЬ
    ВТ_СС.Подр,
    "2. Материалы",
    "Производство",
    НЕОПРЕДЕЛЕНО,
    ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка),
    СТ.АналитикаУчетаНоменклатуры.Номенклатура.А_ОбщееНазваниеНоменклатуры,
    ВЫРАЗИТЬ(СТ.Регистратор КАК Документ.ПриобретениеТоваровУслуг),
    СТ.АналитикаУчетаНоменклатуры.Номенклатура,
    СТ.АналитикаУчетаНоменклатуры.Номенклатура.ЕдиницаИзмерения,
    0,
    0,
    0,
    0,
    СТ.Количество,
    0,
    СТ.СтоимостьБезНДС
ИЗ
    РегистрНакопления.СебестоимостьТоваров КАК СТ
        ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_СС КАК ВТ_СС
        ПО ВЫРАЗИТЬ(СТ.Регистратор КАК Документ.ПриобретениеТоваровУслуг).Подразделение = ВТ_СС.Подр
ГДЕ
    СТ.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
    И СТ.РазделУчета = ЗНАЧЕНИЕ(Перечисление.РазделыУчетаСебестоимостиТоваров.ТоварыНаСкладах)
    И СТ.Регистратор ССЫЛКА Документ.ПриобретениеТоваровУслуг
    И СТ.Период МЕЖДУ &НачП И &КонП

ОБЪЕДИНИТЬ ВСЕ

ВЫБРАТЬ
    ВТ_СС.Подр,
    "1. Труд",
    ВЫБОР КОГДА ЕСТЬNULL(РС.НаВыезде, ЛОЖЬ) ТОГДА "Монтаж" ИНАЧЕ "Производство" КОНЕЦ,
    ВРМ.Этап,
    ВРМ.Работа,
    ЗНАЧЕНИЕ(Справочник.А_ОбщиеНазванияНоменклатуры.ПустаяСсылка),
    НЕОПРЕДЕЛЕНО,
    ВРМ.Работа,
    ЗНАЧЕНИЕ(Справочник.УпаковкиЕдиницыИзмерения.ПустаяСсылка),
    0,
    0,
    ВРМ.ЧасыВыполнение,
    0,
    0,
    0,
    0
ИЗ
    РегистрНакопления.А_ВыполнениеРаботМодулей КАК ВРМ
        ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_СС КАК ВТ_СС
        ПО ВРМ.СтруктураСебестоимости = ВТ_СС.СС
        ЛЕВОЕ СОЕДИНЕНИЕ ВТ_РаботыСС КАК РС
        ПО ВРМ.Работа = РС.Работа
ГДЕ
    ВРМ.Период МЕЖДУ &НачП И &КонП
;
ВЫБРАТЬ
    ВТ_Детали.Подразделение КАК Подразделение,
    ВТ_Детали.Блок КАК Блок,
    ВТ_Детали.Категория КАК Категория,
    ВТ_Детали.Этап КАК Этап,
    ВТ_Детали.Работа КАК Работа,
    ВТ_Детали.ОбщееНазвание КАК ОбщееНазвание,
    ВТ_Детали.Аналитика КАК Аналитика,
    ВТ_Детали.Единица КАК Единица,
    ВТ_Детали.Документ КАК Документ,
    СУММА(ВТ_Детали.ПланЧасы) КАК ПланЧасы,
    СУММА(ВТ_Детали.ФактЧасы) КАК ФактЧасы,
    СУММА(ВТ_Детали.ВиконанняЧасы) КАК ВиконанняЧасы,
    СУММА(ВТ_Детали.ПланКол) КАК ПланКол,
    СУММА(ВТ_Детали.ФактКол) КАК ФактКол,
    СУММА(ВТ_Детали.ПланГрн) КАК ПланГрн,
    СУММА(ВТ_Детали.ФактГрн) КАК ФактГрн,
    СУММА(ВТ_Детали.ФактЧасы) - СУММА(ВТ_Детали.ПланЧасы) КАК ОтклонениеЧасы,
    СУММА(ВТ_Детали.ФактКол) - СУММА(ВТ_Детали.ПланКол) КАК ОтклонениеКол,
    СУММА(ВТ_Детали.ФактГрн) - СУММА(ВТ_Детали.ПланГрн) КАК ОтклонениеГрн,
    ВЫБОР
        КОГДА СУММА(ВТ_Детали.ПланЧасы) = 0 ТОГДА 0
        ИНАЧЕ СУММА(ВТ_Детали.ФактЧасы) / СУММА(ВТ_Детали.ПланЧасы) * 100
    КОНЕЦ КАК ПроцентЧасы,
    ВЫБОР
        КОГДА СУММА(ВТ_Детали.ПланКол) = 0 ТОГДА 0
        ИНАЧЕ СУММА(ВТ_Детали.ФактКол) / СУММА(ВТ_Детали.ПланКол) * 100
    КОНЕЦ КАК ПроцентКол,
    ВЫБОР
        КОГДА СУММА(ВТ_Детали.ПланГрн) = 0 ТОГДА 0
        ИНАЧЕ СУММА(ВТ_Детали.ФактГрн) / СУММА(ВТ_Детали.ПланГрн) * 100
    КОНЕЦ КАК ПроцентГрн,
    СУММА(ВТ_Детали.ФактЧасы) - СУММА(ВТ_Детали.ВиконанняЧасы) КАК ОтклонениеВиконання,
    ВЫБОР
        КОГДА СУММА(ВТ_Детали.ПланЧасы) = 0 ТОГДА 0
        ИНАЧЕ СУММА(ВТ_Детали.ВиконанняЧасы) / СУММА(ВТ_Детали.ПланЧасы) * 100
    КОНЕЦ КАК ПроцентВиконання,
    0 КАК ПланНаФактГрн,
    СУММА(ВТ_Детали.ФактГрн) КАК ПрогнозГрн,
    СУММА(ВТ_Детали.ФактГрн) - СУММА(ВТ_Детали.ПланГрн) КАК ОтклонениеПрогнозГрн,
    ВЫБОР
        КОГДА СУММА(ВТ_Детали.ПланГрн) = 0 ТОГДА 0
        ИНАЧЕ СУММА(ВТ_Детали.ФактГрн) / СУММА(ВТ_Детали.ПланГрн) * 100
    КОНЕЦ КАК ПроцентПрогнозГрн
ИЗ
    ВТ_Детали КАК ВТ_Детали
СГРУППИРОВАТЬ ПО
    ВТ_Детали.Подразделение,
    ВТ_Детали.Блок,
    ВТ_Детали.Категория,
    ВТ_Детали.Этап,
    ВТ_Детали.Работа,
    ВТ_Детали.ОбщееНазвание,
    ВТ_Детали.Аналитика,
    ВТ_Детали.Единица,
    ВТ_Детали.Документ
"""

НП = datetime.datetime(2020, 1, 1); КП = datetime.datetime(2030, 12, 31, 23, 59, 59)

def прогнать(имя_сс):
    сс = erp.Справочники.А_СтруктураСебестоимости.НайтиПоНаименованию(имя_сс)
    assert not сс.Пустая(), f"СС не найдена: {имя_сс}"
    q = erp.NewObject("Запрос")
    q.УстановитьПараметр("СС", сс.Ссылка)
    q.УстановитьПараметр("НачП", НП); q.УстановитьПараметр("КонП", КП)
    q.Текст = ТЕКСТ
    try:
        тз = q.Выполнить().Выгрузить()
    except Exception as e:
        info = getattr(e, "excepinfo", None)
        open(ERR, "w", encoding="utf-8").write(str(info[2]) if info and len(info) > 2 else str(e))
        print(f"QUERY ERROR -> {ERR}"); raise
    cols = [c.Имя for c in тз.Колонки]
    assert "Работа" in cols, "нет колонки Работа"
    план_ч = факт_ч = вик_ч = мат_фг = 0.0
    с_работой = труд_план_строк = 0
    for i in range(тз.Количество()):
        r = тз.Получить(i)
        раб_пусто = пусто(r.Работа)
        if not раб_пусто:
            с_работой += 1
            assert r.Блок == "1. Труд", f"Работа заполнена вне Труда: {r.Блок}"
            assert not пусто(r.Аналитика) and уид(r.Работа) == уид(r.Аналитика), \
                "Работа != Аналитика у строки труда"
        if r.Блок == "1. Труд":
            план_ч += f(r.ПланЧасы); факт_ч += f(r.ФактЧасы); вик_ч += f(r.ВиконанняЧасы)
            if f(r.ПланЧасы) != 0 or f(r.ВиконанняЧасы) != 0:
                труд_план_строк += 1
                assert not раб_пусто, "у труд-план/EV строки Работа пустая"
        if r.Блок == "2. Материалы":
            мат_фг += f(r.ФактГрн)
            assert раб_пусто, "у материалов Работа должна быть пустой"
    print(f"[{имя_сс[:20]}...] строк={тз.Количество()} с_работой={с_работой} "
          f"труд_план/EV={труд_план_строк}")
    print(f"  Труд: ПланЧасы={план_ч:.2f} ФактЧасы={факт_ч:.2f} ВиконанняЧасы={вик_ч:.2f}; "
          f"Материалы ФактГрн={мат_фг:.2f}")
    return план_ч, факт_ч, вик_ч, мат_фг, с_работой

пч, фч, _, мфг, сраб = прогнать("МД МХП ОРІЛЬ / СТІЛ МД МХП ОРІЛЬ")
assert abs(пч - 2795) < 0.01, f"ОРІЛЬ ПланЧасы {пч} != 2795"
assert abs(фч - 2389) < 0.01, f"ОРІЛЬ ФактЧасы {фч} != 2389"
assert abs(мфг - 1620137.97) < 0.01, f"ОРІЛЬ Материалы ФактГрн {мфг} != 1620137.97"
assert сраб > 0, "ОРІЛЬ: ни одной строки с Работой"

_, _, вик, _, сраб2 = прогнать("МД Вооз ЕМS Мала 2026 / Цех_МД Вооз ЕМS Мала 2026")
assert abs(вик - 2401.52) < 0.01, f"ВООЗ ВиконанняЧасы {вик} != 2401.52"
assert сраб2 > 0, "ВООЗ: ни одной строки с Работой"

print("\nQUERY TEST PASS")
```

- [ ] **Step 2: Запустить тест — новый запрос должен пройти на живой базе**

Run: `"C:/Python313/python.exe" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test/test_pfpp_rabota_razrez.py"`
Expected: `QUERY TEST PASS`, эталоны сходятся. При QUERY ERROR — читать `%TEMP%\_pfpp_rabota_err.txt` (кодировка cp1251-ошибки 1С пишутся в utf-8-файл), чинить текст запроса, повторять. В BSL запрос переносится ТОЛЬКО после зелёного теста.

- [ ] **Step 3: Commit (в worktree)**

```bash
cp "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test/test_pfpp_rabota_razrez.py" "C:/Configuration_downloads/BASERP25/.claude/worktrees/funny-spence-f6c97b/_Rarzrabotki/Python/test/"
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/funny-spence-f6c97b"
git add "_Rarzrabotki/Python/test/test_pfpp_rabota_razrez.py"
git commit -m "test(ПФПП): Python COM тест запроса с измерением Работа (Rule #-1)"
```

---

### Task 2: Правка ObjectModule.bsl (основная конфигурация)

**Files:**
- Modify: `C:\Configuration_downloads\BASERP25\_Rarzrabotki\Отчеты\А_ПланФактныйПроизводствоПолный\Ext\ObjectModule.bsl` (запрос ~100–330, `ЗаполнитьПланНаФакт` ~434–446, `СоздатьПустуюТаблицу` ~504–511)

Все правки — инструментом Edit (файл UTF-8 c BOM, отступы — ТАБЫ: в old/new строках запроса ведущий символ — таб, после `|` — таб). 10 точечных правок.

- [ ] **Step 1: Ветка 1 (труд-план) — после `Р.Этап КАК Этап,`**

old_string:
```
	|	Р.Этап КАК Этап,
```
new_string:
```
	|	Р.Этап КАК Этап,
	|	Р.Работа КАК Работа,
```

- [ ] **Step 2: Ветка 2 (материалы-план) — после `К.Этап,`**

old_string:
```
	|	К.Этап,
```
new_string:
```
	|	К.Этап,
	|	ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка),
```

- [ ] **Step 3: Ветка 3 (табель) — блок уникален по `Т.Подразделение`**

old_string:
```
	|	Т.Подразделение,
	|	""1. Труд"",
	|	""Производство"",
	|	НЕОПРЕДЕЛЕНО,
	|	ЗНАЧЕНИЕ(Справочник.А_ОбщиеНазванияНоменклатуры.ПустаяСсылка),
```
new_string:
```
	|	Т.Подразделение,
	|	""1. Труд"",
	|	""Производство"",
	|	НЕОПРЕДЕЛЕНО,
	|	ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка),
	|	ЗНАЧЕНИЕ(Справочник.А_ОбщиеНазванияНоменклатуры.ПустаяСсылка),
```

- [ ] **Step 4: Ветка 4 (ЗП) — блок уникален по `ВЗ.Подразделение`**

old_string:
```
	|	ВЗ.Подразделение,
	|	""1. Труд"",
	|	""Производство"",
	|	НЕОПРЕДЕЛЕНО,
	|	ЗНАЧЕНИЕ(Справочник.А_ОбщиеНазванияНоменклатуры.ПустаяСсылка),
```
new_string:
```
	|	ВЗ.Подразделение,
	|	""1. Труд"",
	|	""Производство"",
	|	НЕОПРЕДЕЛЕНО,
	|	ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка),
	|	ЗНАЧЕНИЕ(Справочник.А_ОбщиеНазванияНоменклатуры.ПустаяСсылка),
```

- [ ] **Step 5: Ветка 5 (материалы-факт) — блок уникален по `СТ.АналитикаУчетаНоменклатуры`**

old_string:
```
	|	""Производство"",
	|	НЕОПРЕДЕЛЕНО,
	|	СТ.АналитикаУчетаНоменклатуры.Номенклатура.А_ОбщееНазваниеНоменклатуры,
```
new_string:
```
	|	""Производство"",
	|	НЕОПРЕДЕЛЕНО,
	|	ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка),
	|	СТ.АналитикаУчетаНоменклатуры.Номенклатура.А_ОбщееНазваниеНоменклатуры,
```

- [ ] **Step 6: Ветка 6 (EV) — после `ВРМ.Этап,`**

old_string:
```
	|	ВРМ.Этап,
```
new_string:
```
	|	ВРМ.Этап,
	|	ВРМ.Работа,
```

- [ ] **Step 7: Финальный SELECT — после `ВТ_Детали.Этап КАК Этап,`**

old_string:
```
	|	ВТ_Детали.Этап КАК Этап,
```
new_string:
```
	|	ВТ_Детали.Этап КАК Этап,
	|	ВТ_Детали.Работа КАК Работа,
```

- [ ] **Step 8: СГРУППИРОВАТЬ ПО — после `ВТ_Детали.Этап,`**

old_string:
```
	|	ВТ_Детали.Этап,
```
new_string:
```
	|	ВТ_Детали.Этап,
	|	ВТ_Детали.Работа,
```

- [ ] **Step 9: `СоздатьПустуюТаблицу` — колонка после Этап**

old_string:
```
	ТЗ.Колонки.Добавить("Этап", Новый ОписаниеТипов("СправочникСсылка.А_ЭтапыРабот"));
```
new_string:
```
	ТЗ.Колонки.Добавить("Этап", Новый ОписаниеТипов("СправочникСсылка.А_ЭтапыРабот"));
	ТЗ.Колонки.Добавить("Работа", Новый ОписаниеТипов("СправочникСсылка.Номенклатура"));
```

- [ ] **Step 10: `ЗаполнитьПланНаФакт` — синт. строки EAC**

old_string:
```
		СинтСтрока.Аналитика = ПустаяНоменклатура;
```
new_string:
```
		СинтСтрока.Аналитика = ПустаяНоменклатура;
		СинтСтрока.Работа = ПустаяНоменклатура;
```

- [ ] **Step 11: Верификация правок**

Run: `grep -c "КАК Работа" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Отчеты/А_ПланФактныйПроизводствоПолный/Ext/ObjectModule.bsl"` → Expected: `3` (ВТ_РаботыСС + ветка 1 + финальный SELECT).
Run: `grep -c "ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка)" ...ObjectModule.bsl` → Expected: `4` (ветки 2/3/4/5).
Синтаксис проверит сборка (Task 4) и smoke (Task 5) — коммит после Task 5.

---

### Task 3: Поле «Работа» в Template.xml (Python-скриптом)

**Files:**
- Create: `<scratchpad>\insert_rabota_field.py` (одноразовый скрипт)
- Modify: `C:\Configuration_downloads\BASERP25\_Rarzrabotki\Отчеты\А_ПланФактныйПроизводствоПолный\Templates\ОсновнаяСхемаКомпоновкиДанных\Ext\Template.xml`

Template.xml — UTF-8 БЕЗ BOM, LF, отступы табами → правка на уровне БАЙТОВ (bytes.replace), кодировка/переводы строк не трогаются.

- [ ] **Step 1: Написать и выполнить скрипт вставки**

```python
# -*- coding: utf-8 -*-
# Вставка поля-измерения Работа после поля Этап в Template.xml (байтовая правка, табы сохраняются).
import xml.etree.ElementTree as ET

P = r"C:\Configuration_downloads\BASERP25\_Rarzrabotki\Отчеты\А_ПланФактныйПроизводствоПолный\Templates\ОсновнаяСхемаКомпоновкиДанных\Ext\Template.xml"

ANCHOR = ("\t<field xsi:type=\"DataSetFieldField\">\n"
          "\t\t<dataPath>Этап</dataPath>\n"
          "\t\t<field>Этап</field>\n"
          "\t\t<title xsi:type=\"v8:LocalStringType\"><v8:item><v8:lang>ru</v8:lang><v8:content>Этап</v8:content></v8:item></title>\n"
          "\t\t<valueType><v8:Type>cfg:CatalogRef.А_ЭтапыРабот</v8:Type></valueType>\n"
          "\t\t<role><dcscom:dimension>true</dcscom:dimension></role>\n"
          "\t</field>\n").encode("utf-8")

NEW = ("\t<field xsi:type=\"DataSetFieldField\">\n"
       "\t\t<dataPath>Работа</dataPath>\n"
       "\t\t<field>Работа</field>\n"
       "\t\t<title xsi:type=\"v8:LocalStringType\"><v8:item><v8:lang>ru</v8:lang><v8:content>Работа</v8:content></v8:item></title>\n"
       "\t\t<valueType><v8:Type>cfg:CatalogRef.Номенклатура</v8:Type></valueType>\n"
       "\t\t<role><dcscom:dimension>true</dcscom:dimension></role>\n"
       "\t</field>\n").encode("utf-8")

data = open(P, "rb").read()
assert data.count(ANCHOR) == 1, f"якорь Этап найден {data.count(ANCHOR)} раз (ожидался 1)"
assert data.count(b"<dataPath>Работа</dataPath>") == 0, "поле Работа уже есть"
data2 = data.replace(ANCHOR, ANCHOR + NEW)
assert len(data2) == len(data) + len(NEW)
open(P, "wb").write(data2)

ET.parse(P)  # well-formed или исключение
print("OK: поле Работа вставлено, XML well-formed")
```

Run: `"C:/Python313/python.exe" "<scratchpad>/insert_rabota_field.py"`
Expected: `OK: поле Работа вставлено, XML well-formed`. Если якорь не найден 1 раз — сверить табы/строки якоря с реальным файлом (Read строк 30–36), поправить ANCHOR, повторить.

- [ ] **Step 2: Валидация схемы скилом**

Invoke skill `skd-validate` для `...Templates\ОсновнаяСхемаКомпоновкиДанных\Ext\Template.xml`.
Expected: валидация без ошибок (новое поле-измерение корректно). `settingsVariant` не менялся — вариант A.

---

### Task 4: Сборка .erf

**Files:**
- Source: `C:\Configuration_downloads\BASERP25\_Rarzrabotki\Отчеты\А_ПланФактныйПроизводствоПолный.xml`
- Output (временный, до зелёного smoke): `%TEMP%\pfpp_rabota.erf`

- [ ] **Step 1: Собрать .erf во временный файл**

```powershell
& "C:\Configuration_downloads\BASERP25\.claude\skills\epf-build\scripts\epf-build.ps1" `
  -V8Path "C:\Program Files\1cv8\8.3.20.1914\bin" `
  -InfoBaseServer "SQLSERVER" -InfoBaseRef "BaseERP" `
  -UserName "Администратор" -Password "24043" `
  -SourceFile "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Отчеты\А_ПланФактныйПроизводствоПолный.xml" `
  -OutputFile "$env:TEMP\pfpp_rabota.erf"
```

Expected: сборка OK, файл `%TEMP%\pfpp_rabota.erf` создан (размер ~15 КБ, свежая дата).
Если «Ошибка блокировки» / база занята — Конфигуратор открыт: сообщить пользователю, попросить закрыть, повторить сборку.

---

### Task 5: Smoke нового .erf + регрессия EAC

**Files:**
- Create: `C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_pfpp_rabota_smoke.py`

- [ ] **Step 1: Написать smoke-тест**

```python
# -*- coding: utf-8 -*-
# Smoke: .erf с измерением Работа. Эталоны ОРІЛЬ/ВООЗ, инварианты Работы, композиция
# с дефолтными настройками (вариант A — вид не менялся) и с группировкой по Работе.
# Путь .erf: env PFPP_ERF или %TEMP%\pfpp_rabota.erf.
import win32com.client, sys, os
if sys.stdout.encoding != 'utf-8':
    sys.stdout.reconfigure(encoding='utf-8')
def f(x): return float(x) if x is not None else 0.0
def пусто(ref):
    return ref is None or (hasattr(ref, 'Пустая') and ref.Пустая())
ERF = os.environ.get("PFPP_ERF") or os.path.join(os.environ["TEMP"], "pfpp_rabota.erf")
assert os.path.exists(ERF), f"нет .erf: {ERF}"
erp = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
отчет = erp.ВнешниеОтчеты.Создать(ERF)
def период():
    return erp.NewObject("СтандартныйПериод")
def тип(имя):
    return erp.NewObject("ОписаниеТипов", имя).Типы().Получить(0)

# --- ОРІЛЬ: эталоны + инварианты ---
сс_о = erp.Справочники.А_СтруктураСебестоимости.НайтиПоНаименованию("МД МХП ОРІЛЬ / СТІЛ МД МХП ОРІЛЬ")
тз = отчет.ПолучитьДанные(период(), сс_о.Ссылка)
cols = [c.Имя for c in тз.Колонки]
assert "Работа" in cols, "нет колонки Работа в ТЗ"
пч = фч = мфг = пнф = 0.0
раб_имя = None
for i in range(тз.Количество()):
    r = тз.Получить(i)
    if r.Блок == "1. Труд":
        пч += f(r.ПланЧасы); фч += f(r.ФактЧасы)
    if r.Блок == "2. Материалы":
        мфг += f(r.ФактГрн); пнф += f(r.ПланНаФактГрн)
        assert пусто(r.Работа), "у материалов/синт.строк Работа не пустая"
    if not пусто(r.Работа):
        assert r.Блок == "1. Труд"
        if раб_имя is None: раб_имя = str(r.Работа.Наименование)
print(f"ОРІЛЬ: ПланЧасы={пч:.2f} ФактЧасы={фч:.2f} МатФактГрн={мфг:.2f} ПланНаФакт={пнф:.2f}")
assert abs(пч - 2795) < 0.01 and abs(фч - 2389) < 0.01
assert abs(мфг - 1620137.97) < 0.01 and abs(пнф - 625675.03) < 0.01

# --- ВООЗ: EV-эталон + Работа у EV-строк ---
сс_в = erp.Справочники.А_СтруктураСебестоимости.НайтиПоНаименованию("МД Вооз ЕМS Мала 2026 / Цех_МД Вооз ЕМS Мала 2026")
тз_в = отчет.ПолучитьДанные(период(), сс_в.Ссылка)
вик = 0.0; раб_в = None
for i in range(тз_в.Количество()):
    r = тз_в.Получить(i)
    if r.Блок == "1. Труд":
        вик += f(r.ВиконанняЧасы)
        if f(r.ВиконанняЧасы) != 0:
            assert not пусто(r.Работа), "EV-строка без Работы"
            if раб_в is None: раб_в = str(r.Работа.Наименование)
print(f"ВООЗ: ВиконанняЧасы={вик:.2f} работа_EV='{раб_в}'")
assert abs(вик - 2401.52) < 0.01

# --- Поле доступно в настройках (отбор) ---
дп = отчет.КомпоновщикНастроек.Настройки.Отбор.ДоступныеПоляОтбора
поле = erp.NewObject("ПолеКомпоновкиДанных", "Работа")
assert дп.НайтиПоле(поле) is not None, "поле Работа недоступно в отборе"

# --- Композиция 1: дефолтные настройки (вариант A — вид прежний) ---
схема = отчет.ПолучитьМакет("ОсновнаяСхемаКомпоновкиДанных")
наст = отчет.КомпоновщикНастроек.ПолучитьНастройки()
вн = erp.NewObject("Структура"); вн.Вставить("Детали", тз)
м = erp.NewObject("КомпоновщикМакетаКомпоновкиДанных").Выполнить(схема, наст)
пр = erp.NewObject("ПроцессорКомпоновкиДанных"); пр.Инициализировать(м, вн)
д = erp.NewObject("ТабличныйДокумент")
в = erp.NewObject("ПроцессорВыводаРезультатаКомпоновкиДанныхВТабличныйДокумент")
в.УстановитьДокумент(д); в.Вывести(пр)
print(f"Композиция (дефолт): H={д.ВысотаТаблицы} W={д.ШиринаТаблицы}")
assert д.ВысотаТаблицы > 0

# --- Композиция 2: группировка по Работе (ВООЗ) ---
наст2 = отчет.КомпоновщикНастроек.ПолучитьНастройки()
наст2.Структура.Очистить()
гр = наст2.Структура.Добавить(тип("ГруппировкаКомпоновкиДанных"))
пг = гр.ПоляГруппировки.Элементы.Добавить(тип("ПолеГруппировкиКомпоновкиДанных"))
пг.Поле = erp.NewObject("ПолеКомпоновкиДанных", "Работа")
гр.Выбор.Элементы.Добавить(тип("АвтоВыбранноеПолеКомпоновкиДанных"))
вн2 = erp.NewObject("Структура"); вн2.Вставить("Детали", тз_в)
м2 = erp.NewObject("КомпоновщикМакетаКомпоновкиДанных").Выполнить(схема, наст2)
пр2 = erp.NewObject("ПроцессорКомпоновкиДанных"); пр2.Инициализировать(м2, вн2)
д2 = erp.NewObject("ТабличныйДокумент")
в2 = erp.NewObject("ПроцессорВыводаРезультатаКомпоновкиДанныхВТабличныйДокумент")
в2.УстановитьДокумент(д2); в2.Вывести(пр2)
тексты = set()
for rr in range(1, min(д2.ВысотаТаблицы, 80) + 1):
    for cc in range(1, д2.ШиринаТаблицы + 1):
        try:
            t = str(д2.Область(rr, cc, rr, cc).Текст).strip()
            if t: тексты.add(t)
        except Exception:
            pass
print(f"Композиция (Работа): H={д2.ВысотаТаблицы}; работа '{раб_в}' видна={раб_в in тексты}")
assert д2.ВысотаТаблицы > 1
assert раб_в in тексты, "наименование работы не видно в группировке по Работе"

print("\nSMOKE PASS")
```

- [ ] **Step 2: Запустить smoke**

Run: `"C:/Python313/python.exe" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test/test_pfpp_rabota_smoke.py"`
Expected: `SMOKE PASS`, все эталоны сходятся.
Contingency при расхождении эталонов (живые данные): взять старый .erf из git worktree (`git show HEAD:"_Rarzrabotki/Отчеты/А_ПланФактныйПроизводствоПолный.erf" > %TEMP%\pfpp_old.erf`), в одной сессии прогнать `ПолучитьДанные` старым и новым отчётом, сравнить Σ по (Блок, Категория) — дифф должен быть нулевым; эталоны промта тогда считать устаревшими и доложить пользователю фактические цифры.

- [ ] **Step 3: Регрессия старого эталонного smoke (EAC)**

```bash
cp "$TEMP/pfpp_rabota.erf" "$TEMP/pfpp_eac.erf"
"C:/Python313/python.exe" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test/test_pfpp_eac.py"
```
Expected: `SMOKE PASS` (EAC-колонки, независимые границы периода, Прогноз/План на факт в композиции).

- [ ] **Step 4: Commit smoke-теста (в worktree)**

```bash
cp "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test/test_pfpp_rabota_smoke.py" "C:/Configuration_downloads/BASERP25/.claude/worktrees/funny-spence-f6c97b/_Rarzrabotki/Python/test/"
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/funny-spence-f6c97b"
git add "_Rarzrabotki/Python/test/test_pfpp_rabota_smoke.py"
git commit -m "test(ПФПП): smoke .erf с измерением Работа (эталоны ОРІЛЬ/ВООЗ, композиция по Работе)"
```

---

### Task 6: Раскладка (Rule #4), коммит, push, память

**Files:**
- Copy: `.erf` из `%TEMP%` → основная конфигурация; BSL/Template.xml/.erf → worktree
- Modify: память `planfakt_proizvodstvo_polny_report.md`

- [ ] **Step 1: Разложить артефакты**

```bash
MAIN="C:/Configuration_downloads/BASERP25"
WT="C:/Configuration_downloads/BASERP25/.claude/worktrees/funny-spence-f6c97b"
REL="_Rarzrabotki/Отчеты"
cp "$TEMP/pfpp_rabota.erf" "$MAIN/$REL/А_ПланФактныйПроизводствоПолный.erf"
cp "$MAIN/$REL/А_ПланФактныйПроизводствоПолный/Ext/ObjectModule.bsl" "$WT/$REL/А_ПланФактныйПроизводствоПолный/Ext/ObjectModule.bsl"
cp "$MAIN/$REL/А_ПланФактныйПроизводствоПолный/Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml" "$WT/$REL/А_ПланФактныйПроизводствоПолный/Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml"
cp "$MAIN/$REL/А_ПланФактныйПроизводствоПолный.erf" "$WT/$REL/А_ПланФактныйПроизводствоПолный.erf"
```

- [ ] **Step 2: Commit + push**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/funny-spence-f6c97b"
git add "_Rarzrabotki/Отчеты/А_ПланФактныйПроизводствоПолный/Ext/ObjectModule.bsl" \
        "_Rarzrabotki/Отчеты/А_ПланФактныйПроизводствоПолный/Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml" \
        "_Rarzrabotki/Отчеты/А_ПланФактныйПроизводствоПолный.erf"
git commit -m "feat(ПФПП): измерение «Работа» (Номенклатура) — разрез для группировки/отбора, вариант A (дефолтная структура не менялась)"
git push -u origin claude/promt-measurement-production-report-49301e
```
Expected: push OK (ветка claude/*; при отказе push из-за больших файлов истории — см. память `git_push_large_files_cherry_pick`, доложить пользователю).

- [ ] **Step 3: Обновить память проекта**

В `C:\Users\support1c\.claude\projects\C--Configuration-downloads-BASERP25\memory\planfakt_proizvodstvo_polny_report.md` дописать абзац: 2026-07-09 добавлено 9-е измерение «Работа» (`СправочникСсылка.Номенклатура`, после Этап; труд-план `Р.Работа`, EV `ВРМ.Работа`, остальные ветки пустая ссылка; синт. строки EAC — пустая; вариант A — дефолтная структура не менялась; поле доступно в настройках для группировки/отбора).

- [ ] **Step 4: Финальная сводка пользователю**

Отчитаться: что сделано, эталоны, коммиты. ОБЯЗАТЕЛЬНО предупредить: сохранённый пользовательский вариант отчёта скрывает новые поля — в отчёте «Ещё → Установить стандартные настройки», после чего «Работа» появится в доступных полях настроек.

---

## Self-review (по спеке)

- Спека §1 (запрос, 6 веток + финальный SELECT + GROUP BY) → Task 1 (тест), Task 2 Steps 1–8. ✓
- Спека §2 (СоздатьПустуюТаблицу, ЗаполнитьПланНаФакт, ЗаполнитьЭтапФактМатериалов не трогать) → Task 2 Steps 9–10; ЗаполнитьЭтапФактМатериалов нигде не правится. ✓
- Спека §3 (Template.xml скриптом, вариант A: settingsVariant не трогать) → Task 3. ✓
- Спека «Процесс» (Rule #-1, сборка, smoke-эталоны, Rule #4, предупреждение о настройках) → Tasks 1, 4, 5, 6. ✓
- Типы согласованы: колонка/поле «Работа» = `СправочникСсылка.Номенклатура` / `cfg:CatalogRef.Номенклатура` везде. ✓
