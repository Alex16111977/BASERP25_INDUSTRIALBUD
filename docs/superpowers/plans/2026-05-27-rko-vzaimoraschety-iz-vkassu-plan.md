# Движения А_ВзСС в РКО из ВКассу.А_Расшифровки — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** При проведении `РасходныйКассовыйОрдер` с заполненной `Ведомость = ВедомостьНаВыплатуЗарплатыВКассу` — заполнять регистр `А_ВзаиморасчетыССотрудниками` **в разрезе подразделений** (СтрПредпр) на основе ТЧ `ВКассу.А_РасшифровкаВыплатыЗарплатаПоФизлицам`. Одноразовый фикс эталона: ВКассу 000Ц-000009 ↔ РКО N0000052986.

**Architecture:** Расширить процедуру `ЗаполнитьИзРКО` в `AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl` развилкой по типу `ДокОбъект.Ведомость`. Подразделение получаем через `Рш.Подразделение.Подразделение` (точечная навигация ПодрОрг → СтрПредпр). Старая ветка (ТЧ.ВыплатаЗаработнойПлаты) сохраняется для обратной совместимости.

**Tech Stack:** BSL 1С 8.3.20, MCP execute_query для pre-check запроса, Python COM для тестов, db-load-xml + db-update для доставки.

**Спецификация:** `docs/superpowers/specs/2026-05-27-rko-vzaimoraschety-iz-vkassu-design.md`

---

## File Structure

| Файл | Действие | Назначение |
|---|---|---|
| `AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl` | Modify | Процедура `ЗаполнитьИзРКО` — добавить развилку для случая `Ведомость = наша ВКассу` |
| `_Rarzrabotki/Python/test/test_rko_vzs_iz_vkassu.py` | Create | Сценарий: ВКассу 000Ц-000009.Касса → 2 Касса Спецтехника; РКО N0000052986.Ведомость = ВКассу; провести; проверить движения А_ВзСС (Σ=348 800, разрез по СтрПредпр) |

---

## Task 1: Pre-flight — проверить точечную навигацию ПодрОрг.Подразделение в запросе

Перед изменением BSL — проверяем через MCP что `Рш.Подразделение.Подразделение` корректно возвращает СтрПредпр на реальных данных ВКассу 000Ц-000009. Это CLAUDE.md Rule #-1 (Python COM/MCP перед BSL).

- [ ] **Step 1: Проверить точечную навигацию в запросе MCP**

Run:
```
mcp__1c-workerp__execute_query({
  "query_text": "ВЫБРАТЬ ПЕРВЫЕ 10 Рш.Подразделение.Наименование КАК ПодрОрг, Рш.Подразделение.Подразделение.Наименование КАК СтрПредпр, Рш.ФизическоеЛицо.Наименование КАК ФЛ, Рш.КВыплате КАК КВыплате ИЗ Документ.ВедомостьНаВыплатуЗарплатыВКассу.А_РасшифровкаВыплатыЗарплатаПоФизлицам КАК Рш ГДЕ Рш.Ссылка.Номер = \"000Ц-000009\""
})
```

Expected: 10 строк. Колонки `ПодрОрг`, `СтрПредпр`, `ФЛ`, `КВыплате` заполнены. У каждой строки СтрПредпр непустая (например «Производство», «Глобино-2», «ЦО» и т.д.). Это подтверждает что `Подразделение.Подразделение` работает.

Если СтрПредпр везде пустая — фейл. Нужна другая стратегия (например хранить СтрПредпр напрямую в ТЧ А_Расшифровка через миграцию).

- [ ] **Step 2: Проверить агрегат по новому запросу (предварительный smoke)**

Run:
```
mcp__1c-workerp__execute_query({
  "query_text": "ВЫБРАТЬ Рш.Подразделение.Подразделение.Наименование КАК СтрПредпр, Рш.ФизическоеЛицо.Наименование КАК ФЛ, СУММА(Рш.КВыплате) КАК Σ ИЗ Документ.ВедомостьНаВыплатуЗарплатыВКассу.А_РасшифровкаВыплатыЗарплатаПоФизлицам КАК Рш ГДЕ Рш.Ссылка.Номер = \"000Ц-000009\" СГРУППИРОВАТЬ ПО Рш.Подразделение.Подразделение, Рш.ФизическоеЛицо ИМЕЮЩИЕ СУММА(Рш.КВыплате) <> 0 УПОРЯДОЧИТЬ ПО Рш.ФизическоеЛицо.Наименование"
})
```

Expected: ~21-31 строк (по числу уникальных (СтрПредпр, ФЛ)). Σ всех строк = 348 800 (общая Σ ВКассу). Это и есть **точно то что заведёт регистр А_ВзСС** после Task 2.

Зафиксировать: ожидаемое число строк регистра = N (запомнить).

---

## Task 2: Расширить процедуру `ЗаполнитьИзРКО` в ManagerModule

**Files:**
- Modify: `AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl` (стр. 271-318 — процедура `ЗаполнитьИзРКО`)

- [ ] **Step 1: Прочитать существующий блок Запрос.Текст**

Run:
```
Read("AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl", offset=271, limit=50)
```

Запомнить точные границы: от `Запрос = Новый Запрос;` до `ИМЕЮЩИЕ СУММА(ТЧ.Сумма) <> 0";`

- [ ] **Step 2: Заменить блок через Edit**

Старая строка (одно вхождение в файле):
```bsl
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Док", ДокОбъект.Ссылка);
	Запрос.Текст =
	"ВЫБРАТЬ
	|	Д.Организация КАК Организация,
	|	Д.Подразделение КАК Подразделение,
	|	ТЧ.ФизическоеЛицо КАК ФизическоеЛицо,
	|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход) КАК ВидДвижения,
	|	СУММА(ТЧ.Сумма) КАК СуммаВзаиморасчетов
	|ИЗ
	|	Документ.РасходныйКассовыйОрдер КАК Д
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.РасходныйКассовыйОрдер.ВыплатаЗаработнойПлаты КАК ТЧ
	|			ПО ТЧ.Ссылка = Д.Ссылка
	|	ГДЕ
	|	Д.Ссылка = &Док
	|	И Д.ХозяйственнаяОперация В (
	|		ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.ВыплатаЗарплатыЧерезКассу),
	|		ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.ВыплатаЗарплатыРаздатчиком),
	|		ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.ВыплатаЗарплатыРаботнику))
	|СГРУППИРОВАТЬ ПО
	|	Д.Организация, Д.Подразделение, ТЧ.ФизическоеЛицо
	|ИМЕЮЩИЕ СУММА(ТЧ.Сумма) <> 0";
```

> Точные пробелы (табы) и переносы строк нужно скопировать из `Read` результата. В случае несовпадения форматирования — использовать Python helper (как делали в Шаге 8 Ф2-модуля).

Заменить на:
```bsl
	// Развилка: если РКО.Ведомость = наша ВКассу → берём из её А_Расшифровки
	// (закрытие в разрезе подразделений через Рш.Подразделение.Подразделение → СтрПредпр).
	// Иначе → старая логика (ТЧ.ВыплатаЗаработнойПлаты + 3 хозоперации).
	ЭтоНашаВКассу = Ложь;
	Если ЗначениеЗаполнено(ДокОбъект.Ведомость) Тогда
		Попытка
			ЭтоНашаВКассу = (ТипЗнч(ДокОбъект.Ведомость) = Тип("ДокументСсылка.ВедомостьНаВыплатуЗарплатыВКассу"));
		Исключение
			ЭтоНашаВКассу = Ложь;
		КонецПопытки;
	КонецЕсли;

	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Док", ДокОбъект.Ссылка);

	Если ЭтоНашаВКассу Тогда
		// НОВАЯ логика: источник = ВКассу.А_РасшифровкаВыплатыЗарплатаПоФизлицам.
		// Подразделение в строке регистра = ПодрОрг.Подразделение (СтрПредпр).
		// Шапочное Подр РКО игнорируется — берём реальное из каждой строки расшифровки.
		Запрос.Текст =
		"ВЫБРАТЬ
		|	Д.Организация КАК Организация,
		|	Рш.Подразделение.Подразделение КАК Подразделение,
		|	Рш.ФизическоеЛицо КАК ФизическоеЛицо,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход) КАК ВидДвижения,
		|	СУММА(Рш.КВыплате) КАК СуммаВзаиморасчетов
		|ИЗ
		|	Документ.РасходныйКассовыйОрдер КАК Д
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ВедомостьНаВыплатуЗарплатыВКассу.А_РасшифровкаВыплатыЗарплатаПоФизлицам КАК Рш
		|			ПО Рш.Ссылка = Д.Ведомость
		|ГДЕ
		|	Д.Ссылка = &Док
		|СГРУППИРОВАТЬ ПО
		|	Д.Организация, Рш.Подразделение.Подразделение, Рш.ФизическоеЛицо
		|ИМЕЮЩИЕ СУММА(Рш.КВыплате) <> 0";
	Иначе
		// СТАРАЯ логика (ТЧ.ВыплатаЗаработнойПлаты, 3 хозоперации) — без изменений
		Запрос.Текст =
		"ВЫБРАТЬ
		|	Д.Организация КАК Организация,
		|	Д.Подразделение КАК Подразделение,
		|	ТЧ.ФизическоеЛицо КАК ФизическоеЛицо,
		|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход) КАК ВидДвижения,
		|	СУММА(ТЧ.Сумма) КАК СуммаВзаиморасчетов
		|ИЗ
		|	Документ.РасходныйКассовыйОрдер КАК Д
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.РасходныйКассовыйОрдер.ВыплатаЗаработнойПлаты КАК ТЧ
		|			ПО ТЧ.Ссылка = Д.Ссылка
		|	ГДЕ
		|	Д.Ссылка = &Док
		|	И Д.ХозяйственнаяОперация В (
		|		ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.ВыплатаЗарплатыЧерезКассу),
		|		ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.ВыплатаЗарплатыРаздатчиком),
		|		ЗНАЧЕНИЕ(Перечисление.ХозяйственныеОперации.ВыплатаЗарплатыРаботнику))
		|	СГРУППИРОВАТЬ ПО
		|	Д.Организация, Д.Подразделение, ТЧ.ФизическоеЛицо
		|	ИМЕЮЩИЕ СУММА(ТЧ.Сумма) <> 0";
	КонецЕсли;
```

(Остальная часть процедуры — `Результат = Запрос.Выполнить().Выгрузить()` и цикл `Для Каждого СтрТЗ Из Результат Цикл` — **не меняется**, она универсальна для обеих веток.)

- [ ] **Step 3: Доставка через db-load-xml + db-update**

Run:
```powershell
& "C:\Configuration_downloads\BASERP25\.claude\skills\db-load-xml\scripts\db-load-xml.ps1" -V8Path "C:\Program Files\1cv8\8.3.20.1914\bin" -InfoBaseServer "SQLSERVER" -InfoBaseRef "BaseERP" -UserName "Администратор" -Password "24043" -ConfigDir "C:\Configuration_downloads\BASERP25" -Mode Partial -Files "AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl"
```

Expected: `Load completed successfully`

```powershell
& "C:\Configuration_downloads\BASERP25\.claude\skills\db-update\scripts\db-update.ps1" -V8Path "C:\Program Files\1cv8\8.3.20.1914\bin" -InfoBaseServer "SQLSERVER" -InfoBaseRef "BaseERP" -UserName "Администратор" -Password "24043" -Dynamic "+"
```

Expected: `Обновление конфигурации успешно завершено`

- [ ] **Step 4: Commit**

```bash
git add "AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl"
git commit -m "$(cat <<'EOF'
feat(rko-vzs): развилка по Ведомость — наша ВКассу → А_Расшифровка

AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl,
процедура ЗаполнитьИзРКО — добавлена развилка по ТипЗнч(ДокОбъект.Ведомость):

- Если Ведомость = ВедомостьНаВыплатуЗарплатыВКассу → новая ветка:
  * Источник: ТЧ.А_РасшифровкаВыплатыЗарплатаПоФизлицам
  * Подразделение: Рш.Подразделение.Подразделение (ПодрОрг → СтрПредпр)
  * Группировка per (Орг, СтрПредпр, ФЛ) — закрытие в разрезе подразделений
  * Σ = Σ КВыплате
  * ВидДвижения = Расход

- Иначе → старая логика (ТЧ.ВыплатаЗаработнойПлаты, 3 хозоперации) —
  без изменений, обратная совместимость.

Остаток процедуры (цикл записи) универсален для обеих веток.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Тест-фикс эталона + проверка движений

**Files:**
- Create: `_Rarzrabotki/Python/test/test_rko_vzs_iz_vkassu.py`

- [ ] **Step 1: Написать тестовый сценарий**

Создать `_Rarzrabotki/Python/test/test_rko_vzs_iz_vkassu.py`:

```python
"""
Тест: проведение РКО N0000052986 со ссылкой на ВКассу 000Ц-000009 → движения
А_ВзаиморасчетыССотрудниками в разрезе подразделений.

Действия:
1. Сменить ВКассу.Касса на "2 Касса Спецтехника" (матч с РКО).
2. Установить РКО.Ведомость = ВКассу.
3. Провести РКО.
4. Проверить движения регистра.
5. Откат (опционально): распровести РКО, очистить Ведомость, вернуть Кассу.
"""
import io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding="utf-8")

import win32com.client

CONN = 'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"'
EXPECTED_SUM = 348800.00
v8 = win32com.client.Dispatch("V83.COMConnector")
erp = v8.Connect(CONN)

# 1. Найти документы
def find_doc(meta_name, number):
    q = erp.NewObject("Запрос")
    q.Текст = f'ВЫБРАТЬ ПЕРВЫЕ 1 Д.Ссылка КАК Ссылка ИЗ Документ.{meta_name} КАК Д ГДЕ Д.Номер = "{number}"'
    rs = q.Выполнить()
    if rs.Пустой(): return None
    sel = rs.Выбрать(); sel.Следующий()
    return sel.Ссылка

rko_ref = find_doc("РасходныйКассовыйОрдер", "N0000052986")
vk_ref  = find_doc("ВедомостьНаВыплатуЗарплатыВКассу", "000Ц-000009")
if rko_ref is None or vk_ref is None:
    print("FAIL: эталоны не найдены")
    sys.exit(1)
print(f"РКО:    {rko_ref}")
print(f"ВКассу: {vk_ref}")

# 2. Сменить Кассу в ВКассу
kassa_target = erp.Справочники.Кассы.НайтиПоНаименованию("2 Касса Спецтехника", True)
if not kassa_target or kassa_target.Пустая():
    print("FAIL: касса '2 Касса Спецтехника' не найдена")
    sys.exit(1)

vk = vk_ref.ПолучитьОбъект()
kassa_old = vk.Касса
vk.Касса = kassa_target
vk.ОбменДанными.Загрузка = True  # блокируем типовую логику чтобы не перезаполнять ТЧ
vk.Записать(erp.РежимЗаписиДокумента.Запись)
print(f"OK: ВКассу.Касса '{kassa_old.Наименование}' → '{kassa_target.Наименование}'")

# 3. Установить РКО.Ведомость и провести
rko = rko_ref.ПолучитьОбъект()
ved_old = rko.Ведомость
rko.Ведомость = vk_ref
try:
    rko.Записать(erp.РежимЗаписиДокумента.Проведение)
    print(f"OK: РКО проведён (Ведомость = {vk_ref})")
except Exception as e:
    info = e.excepinfo[2] if hasattr(e, "excepinfo") and e.excepinfo else str(e)
    print(f"FAIL: проведение упало: {info}")
    sys.exit(1)

# 4. Проверить движения регистра
q = erp.NewObject("Запрос")
q.Текст = '''ВЫБРАТЬ
	Р.Подразделение.Наименование КАК СтрПредпр,
	Р.ФизическоеЛицо.Наименование КАК ФЛ,
	Р.СуммаВзаиморасчетов КАК Σ
ИЗ РегистрНакопления.А_ВзаиморасчетыССотрудниками КАК Р
ГДЕ Р.Регистратор = &Док
УПОРЯДОЧИТЬ ПО Р.Подразделение, Р.ФизическоеЛицо'''
q.УстановитьПараметр("Док", rko_ref)
rs = q.Выполнить()

cnt = 0
sum_total = 0.0
under_lines = {}  # СтрПредпр → Σ
sel = rs.Выбрать()
while sel.Следующий():
    cnt += 1
    s = float(sel.Σ)
    sum_total += s
    under_lines.setdefault(str(sel.СтрПредпр), 0.0)
    under_lines[str(sel.СтрПредпр)] += s

print(f"\nДвижения регистра А_ВзСС:")
print(f"  Кол строк:        {cnt}")
print(f"  Σ:                {sum_total:,.2f}")
print(f"  Разрез по СтрПредпр:")
for spp, s in sorted(under_lines.items()):
    print(f"    {spp}: Σ={s:,.2f}")

fail = False
if abs(sum_total - EXPECTED_SUM) > 0.01:
    print(f"FAIL: Σ движений {sum_total:,.2f} != ожидаемое {EXPECTED_SUM:,.2f}")
    fail = True
if cnt < 2:
    print(f"FAIL: ожидали ≥2 строки (разрез по СтрПредпр), получили {cnt}")
    fail = True
if len(under_lines) < 2:
    print(f"FAIL: ожидали ≥2 уникальных СтрПредпр (разрез), получили {len(under_lines)}: {list(under_lines.keys())}")
    fail = True

if fail:
    sys.exit(1)
print(f"\nPASS: движения А_ВзСС корректны (Σ={sum_total:,.2f}, {cnt} строк, {len(under_lines)} подразделений)")
```

- [ ] **Step 2: Запустить тест**

Run:
```
C:\Python313\python.exe "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_rko_vzs_iz_vkassu.py"
```

Expected:
```
РКО:    ...
ВКассу: ...
OK: ВКассу.Касса 'Касса ЗП' → '2 Касса Спецтехника'
OK: РКО проведён (Ведомость = ...)

Движения регистра А_ВзСС:
  Кол строк:        ~21-31
  Σ:                348,800.00
  Разрез по СтрПредпр:
    Автокран XCMG: Σ=...
    Глобино-2: Σ=...
    ...

PASS: движения А_ВзСС корректны (Σ=348,800.00, N строк, M подразделений)
```

Если упало:
- "Σ != 348 800" — диагностировать запрос (возможно `Рш.Подразделение.Подразделение` возвращает NULL для каких-то строк → они исключаются из СГРУППИРОВАТЬ).
- "1 уникальная СтрПредпр" — точечная навигация даёт одинаковые значения (значит у всех 31 строк один и тот же СтрПредпр? Это возможно если все ПодрОрг ведут на одно СтрПредпр).
- Ошибка проведения — может быть типовое ограничение по СпособуВыплаты ВКассу или другая логика.

- [ ] **Step 3: Commit**

```bash
git add "_Rarzrabotki/Python/test/test_rko_vzs_iz_vkassu.py"
git commit -m "$(cat <<'EOF'
test(rko-vzs): smoke движений А_ВзСС в РКО из ВКассу.А_Расшифровки

Сценарий: ВКассу 000Ц-000009 (Σ=348 800, 31 строк А_Расшифровки) →
сменить Кассу → "2 Касса Спецтехника"; РКО N0000052986.Ведомость = ВКассу;
провести → проверить движения регистра.

Проверки:
- Кол строк регистра ≥ 2 (разрез по подразделениям)
- Σ СуммаВзаиморасчетов = 348 800
- Уникальных СтрПредпр ≥ 2 (доказывает что закрытие "в разрезе")

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Регрессия + knowledge

**Files:**
- Create: `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/14_rko_vzaimoraschety_iz_vkassu.md`
- Modify: `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/KNOWLEDGE_MAP.md` (cross-ref)

- [ ] **Step 1: Регрессия существующих тестов**

Run:
```
C:\Python313\python.exe "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_create_vedomost_vkassu_iz_f2_smoke.py"
```
Expected: PASS (изменили только ManagerModule регистра — Ф2 не задет).

```
C:\Python313\python.exe "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_otrazhenie_vkassu_smoke.py"
```
Expected: PASS.

```
C:\Python313\python.exe "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_vkassu_subform_metadata.py"
```
Expected: PASS.

- [ ] **Step 2: Создать knowledge файл 14**

Создать `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/14_rko_vzaimoraschety_iz_vkassu.md`:

```markdown
# 14. Движения А_ВзаиморасчетыССотрудниками в РКО из ВКассу.А_Расшифровки

> Реализовано 2026-05-27 (вечер).
> Spec: [`docs/superpowers/specs/2026-05-27-rko-vzaimoraschety-iz-vkassu-design.md`](../../../docs/superpowers/specs/2026-05-27-rko-vzaimoraschety-iz-vkassu-design.md).
> Plan: [`docs/superpowers/plans/2026-05-27-rko-vzaimoraschety-iz-vkassu-plan.md`](../../../docs/superpowers/plans/2026-05-27-rko-vzaimoraschety-iz-vkassu-plan.md).

---

## 1. Что сделано

Расширена процедура `ЗаполнитьИзРКО` в `AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl`. Добавлена развилка по типу `ДокОбъект.Ведомость`:

| Случай | Источник данных | Подразделение в регистре |
|---|---|---|
| `Ведомость = ВедомостьНаВыплатуЗарплатыВКассу` (наша) | ТЧ.А_РасшифровкаВыплатыЗарплатаПоФизлицам | `Рш.Подразделение.Подразделение` (ПодрОрг → СтрПредпр) — **разрез по подразделениям** |
| Ведомость пустая или другого типа | ТЧ.ВыплатаЗаработнойПлаты + 3 хозоперации (старая логика, без изменений) | `Д.Подразделение` (СтрПредпр из шапки) |

## 2. Поток данных

```
[РКО проведено с Ведомость = наша ВКассу]
                ↓
   AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl
   Процедура ЗаполнитьИзРКО(ДокОбъект):
     ├─ ЭтоНашаВКассу = ТипЗнч(Ведомость) = Тип("ДокументСсылка.ВедомостьНаВыплатуЗарплатыВКассу")
     ├─ Если Истина → запрос по ТЧ.А_РасшифровкаВыплатыЗарплатаПоФизлицам:
     │      Подразделение = Рш.Подразделение.Подразделение
     │      Σ = СУММА(Рш.КВыплате)
     │      Группировка per (Орг, СтрПредпр, ФЛ)
     ├─ Иначе → старая логика (ТЧ.ВыплатаЗаработнойПлаты + 3 хозоперации)
     └─ Цикл: для каждой строки результата → Запись в регистр (ВидДвижения=Расход)
                ↓
   Регистр накопления А_ВзСС наполнен в разрезе подразделений.
   Сальдо Приход − Расход (по ФЛ × СтрПредпр) = чистый долг работодателя перед ФЛ.
```

## 3. Уроки

### Урок №1: точечная навигация в запросе ПодрОрг → СтрПредпр

`Справочник.ПодразделенияОрганизаций.Подразделение` (тип `Справочник.СтруктураПредприятия`) — стандартная связь в БАС ERP. В запросе можно ссылаться напрямую через точку: `Рш.Подразделение.Подразделение`. Это даёт автоматический LEFT JOIN на справочник.

### Урок №2: фильтр выбора ведомости в РКО матчит по Кассе

Если `РКО.Касса ≠ ВКассу.Касса` — типовой подбор ведомости не находит её. Решение для эталона: вручную сменить Кассу в ВКассу через MCP/COM (через `vk.ОбменДанными.Загрузка = True` чтобы не запускать типовую логику).

### Урок №3: разрез по подразделениям — ключевая особенность

В шапке РКО Подразделение одно. В строках А_Расшифровки могут быть **разные** подразделения. Новая логика → N движений регистра per (Орг, СтрПредпр, ФЛ). Это и есть «закрыть выплаты в разрезе подразделений» по требованию пользователя.

### Урок №4: обратная совместимость через развилку

Старая ветка (ТЧ.ВыплатаЗаработнойПлаты + 3 хозоперации) полностью сохранена. РКО без нашей ВКассу или с другой ведомостью работает по типовому пути.

## 4. Эталонный сценарий

| Параметр | Значение |
|---|---|
| РКО | `N0000052986` от 08.12.2025 (Касса = «2 Касса Спецтехника», Σ=348 800 UAH) |
| ВКассу | `000Ц-000009` от 10.12.2025 (после смены Кассы → «2 Касса Спецтехника») |
| ТЧ А_Расшифровка | 31 строка, Σ КВыплате = 348 800 |
| Движения А_ВзСС | N строк per (Орг, СтрПредпр, ФЛ), Σ=348 800, все ВидДвижения=Расход |

## 5. Тесты

| Файл | Назначение | Статус |
|---|---|---|
| `test_rko_vzs_iz_vkassu.py` | Смена кассы + Ведомость + проведение + проверка движений | ✅ PASS |

## 6. Cross-references

- ManagerModule: `AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl`
- Эталон-паттерн (закрытие по подразделениям в ОтражениеЗП): процедура `ЗаполнитьИзОтраженияЗП` в том же файле
- [11_vedomost_iz_raspredeleniyaF2.md](11_vedomost_iz_raspredeleniyaF2.md) — ТЧ.А_Расшифровка структура (13 полей)
- [13_vkassu_podforma_rashifrovka.md](13_vkassu_podforma_rashifrovka.md) — подформа редактирования
- [KNOWLEDGE_MAP.md](KNOWLEDGE_MAP.md)
```

- [ ] **Step 3: Обновить KNOWLEDGE_MAP**

Edit `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/KNOWLEDGE_MAP.md`:

Найти:
```
| 14 | [13_vkassu_podforma_rashifrovka.md](13_vkassu_podforma_rashifrovka.md) | **🟢 РЕАЛИЗОВАНО + ОБНОВЛЕНО 2026-05-27** — локальная форма А_ФормаСпискаРасшифровкиПоФЛ
```

После неё добавить новую строку:
```
| 15 | [14_rko_vzaimoraschety_iz_vkassu.md](14_rko_vzaimoraschety_iz_vkassu.md) | **🟢 РЕАЛИЗОВАНО 2026-05-27 (вечер)** — Движения А_ВзаиморасчетыССотрудниками в РКО (когда Ведомость = наша ВКассу) на основе ТЧ.А_Расшифровки в разрезе подразделений (СтрПредпр). Эталон: РКО N0000052986 + ВКассу 000Ц-000009 → Σ=348 800 |
```

И сдвинуть номера строк LESSONS.md / FINDINGS.md с 15 на 16.

- [ ] **Step 4: Commit knowledge**

```bash
git add "_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/14_rko_vzaimoraschety_iz_vkassu.md" "_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/KNOWLEDGE_MAP.md"
git commit -m "$(cat <<'EOF'
docs(knowledge): файл 14 — движения А_ВзСС в РКО из ВКассу.А_Расшифровки

Описание новой логики в AccumulationRegisters/А_ВзСС/ManagerModule.bsl
процедура ЗаполнитьИзРКО:
- развилка по ТипЗнч(ДокОбъект.Ведомость)
- источник = ТЧ.А_Расшифровка, разрез по СтрПредпр через
  Рш.Подразделение.Подразделение
- 4 урока: точечная навигация в запросе, фильтр Кассы в выборе ведомости,
  разрез по подразделениям как ключевая особенность, обратная совместимость

Cross-ref в KNOWLEDGE_MAP.md (строка 15).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 5: Финальный отчёт**

Сообщить пользователю:
- Все тесты PASS (новый + 3 регрессионных)
- ВКассу 000Ц-000009 теперь связана с РКО N0000052986 (Касса синхронизирована, Ведомость установлена, РКО проведён)
- Движения регистра А_ВзаиморасчетыССотрудниками заполнены в разрезе подразделений
- Жду `push` если нужно отправить в `origin/main`

---

## Self-Review (выполнено)

**1. Spec coverage:**
- §3 «Изменение в ЗаполнитьИзРКО» → Task 2
- §4 «Одноразовый фикс эталона» → Task 3 (тест и есть фикс — он сам делает смену кассы + установку ведомости + проведение)
- §5 «Поведение» → проверяется в Task 3 (через тест) + регрессия Task 4 (старая ветка)
- §6 «Out-of-scope» → не реализуется
- §7 «Тестирование» → Task 3
- §8 «Acceptance критерии» → проверяются через тест в Task 3

**2. Placeholder scan:** все шаги содержат конкретный код. Pre-flight даёт точное число ожидаемых строк (фиксируется в Task 1 Step 2).

**3. Type consistency:**
- `Документ.ВедомостьНаВыплатуЗарплатыВКассу` — везде идентично
- `Рш.Подразделение.Подразделение` — везде идентично (BSL и Python COM)
- Поле `КВыплате` (после унификации ТЧ) — везде идентично
- Имя регистра `А_ВзаиморасчетыССотрудниками` — везде идентично

Плану — зелёный свет.

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-27-rko-vzaimoraschety-iz-vkassu-plan.md`. Two execution options:**

**1. Subagent-Driven (recommended)** — диспатчим свежего subagent на каждую задачу, ревью между задачами, быстрая итерация.

**2. Inline Execution** — выполняем задачи в этой сессии через `superpowers:executing-plans`.

**Which approach?**
