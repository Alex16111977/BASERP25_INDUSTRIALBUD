# Подотчётное удержание («підзвітних ГК») в А_ОтражениеЗПпоКазне — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Загружать удержание сотрудника типа «Утримання підзвітних ГК» (Код 00009, возврат подотчётных при увольнении) из zup_1 в `А_ОтражениеЗПпоКазне`, со счётом 3721 и ПАП-статьёй «Денежные средства (у подотчетных лиц)», чтобы оно вычиталось из движения `А_ВзаиморасчетыССотрудниками` (Гуняга Кон.остаток → 0) и не светилось разницей в отчёте сверки.

**Architecture:** Две правки в `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl`: (1) расширить фильтр запроса удержаний к zup_1 — кроме исполнительных листов брать ВидРасчета.Код="00009"; (2) в `ЗаписатьУдержанияВТЧ_Бух` разветвить по типу — для подотчётного удержания счёт 3721 + статья «Денежные средства (у подотчетных лиц)», без Получателя. Типовой ОЗФУ-механизм проведения читает `СтатьяАктивовПассивов` прямо из строки `УдержаннаяЗарплата` (ManagerModule стр.596/620) — его НЕ трогаем. А_ВзСС-заполнитель вычитает любую `УдержаннаяЗарплата` (блок 3) → не трогаем.

**Tech Stack:** 1C BSL, Python 3.13 + win32com (BaseERP + zup_1), skills db-load-xml/db-update.

**Среда:** BaseERP `Usr="Администратор";Pwd="24043"`; zup_1 `Usr="cfo";Pwd="2442"`; рабочая папка `C:\Configuration_downloads\BASERP25` (ветка `claude/bas-doc-skills`); v8 `8.3.20.1914`; python `C:\Python313\python.exe`. Промт: `_Rarzrabotki/notebook/prompts/investigate_gunyaga_uderzhanie_podzvitnyh.md`.

**Подтверждено (COM):** ЕРП Хозрасчетный 3721 «Расчеты с подотчетными лицами в национальной валюте» (ActivePassive); ПВХ СтатьиАктивовПассивов «Денежные средства (у подотчетных лиц)»; zup_1 ВидРасчета «Утримання  підзвітних ГК» Код=00009; типовое проведение ОЗФУ берёт статью из строки УдержаннаяЗарплата.

**Коммиты:** только релевантные файлы (`git add <путь>`), НЕ `-A` (в дереве чужой WIP + удаление .epf).

---

## Task 1: RED ground-truth (Python COM)

**Files:**
- Create: `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/red_podzvitne_gunyaga.py`

- [ ] **Step 1: Скрипт**

```python
# -*- coding: utf-8 -*-
import io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client as w
v8 = w.Dispatch("V83.COMConnector")
erp = v8.Connect('Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
z = v8.Connect('Srvr="SQLSERVER";Ref="zup_1";Usr="cfo";Pwd="2442"')

# (а) zup_1: удержание Гуняги Код 00009
q = z.NewObject("Запрос")
q.Текст = '''ВЫБРАТЬ У.ВидРасчета.Код КАК Код, У.ВидРасчета.Наименование КАК Вид, СУММА(У.Результат) КАК Сумма,
 МАКСИМУМ(ВЫБОР КОГДА У.ДокументОснование<>ЗНАЧЕНИЕ(Документ.ИсполнительныйЛист.ПустаяСсылка) ТОГДА 1 ИНАЧЕ 0 КОНЕЦ) КАК ЕстьИЛ
ИЗ РегистрРасчета.УдержанияРаботниковОрганизаций КАК У
ГДЕ У.ПериодРегистрации МЕЖДУ ДАТАВРЕМЯ(2025,12,1,0,0,0) И ДАТАВРЕМЯ(2025,12,31,23,59,59)
 И У.Результат<>0 И У.Сотрудник.Наименование ПОДОБНО "Гуняга%"
СГРУППИРОВАТЬ ПО У.ВидРасчета.Код, У.ВидРасчета.Наименование'''
t = q.Выполнить().Выгрузить()
print("(а) zup_1 удержания Гуняги:")
for i in range(t.Количество()):
    r = t.Получить(i); print(f"   Код={r.Код} | {r.Вид} | Σ={r.Сумма} | ЕстьИЛ={r.ЕстьИЛ}")

# (б) ЕРП: 3721 и статья — есть?
acc = erp.ПланыСчетов.Хозрасчетный.НайтиПоКоду("3721")
st = erp.ПланыВидовХарактеристик.СтатьиАктивовПассивов.НайтиПоНаименованию("Денежные средства (у подотчетных лиц)")
print(f"(б) ЕРП 3721 заполнен={erp.ЗначениеЗаполнено(acc)} | статья заполнена={erp.ЗначениеЗаполнено(st)}")

# (в) ОЗП №1: есть ли Гуняга в УдержанияБухгалтерия (ожидаем НЕТ)
ref = erp.Документы.А_ОтражениеЗПпоКазне.НайтиПоНомеру("000000001")
ob = ref.ПолучитьОбъект()
n = 0
for i in range(ob.УдержанияБухгалтерия.Количество()):
    s = ob.УдержанияБухгалтерия.Получить(i)
    if erp.ЗначениеЗаполнено(s.ФизическоеЛицо) and s.ФизическоеЛицо.Наименование.startswith("Гуняга"): n += 1
print(f"(в) ОЗП №1 УдержанияБухгалтерия строк Гуняги = {n} (RED: 0)")
```

- [ ] **Step 2: Запустить**

Run: `& "C:\Python313\python.exe" "_Rarzrabotki\notebook\knowledge_А_ОтражениеЗПпоКазне\Python\test\red_podzvitne_gunyaga.py"`
Expected: (а) Код=00009 «Утримання підзвітних ГК» Σ=400 ЕстьИЛ=0; (б) 3721 True / статья True; (в) 0 строк Гуняги.

- [ ] **Step 3: Commit**

```bash
git add "_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/red_podzvitne_gunyaga.py"
git commit -m "test(подотчётне): RED — Гуняга Код 00009 Σ400 не-ИЛ, не в УдержанияБухгалтерия"
```

---

## Task 2: Правка 1 — расширить фильтр запроса удержаний

**Files:**
- Modify: `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` (`ЗагрузитьНалогиИУдержанияИзЗарплатыБух`, `ТекстЗапросаУдержаний` ~510-539)

- [ ] **Step 1: Добавить ВидРасчета.Код в SELECT**

old_string:
```bsl
		|	У.Сотрудник.Наименование КАК СотрудникНаименование,
		|	У.ВидРасчета.Наименование КАК ВидУдержания,
		|	СУММА(У.Результат) КАК Сумма,
```
new_string:
```bsl
		|	У.Сотрудник.Наименование КАК СотрудникНаименование,
		|	У.ВидРасчета.Наименование КАК ВидУдержания,
		|	У.ВидРасчета.Код КАК КодВида,
		|	СУММА(У.Результат) КАК Сумма,
```

- [ ] **Step 2: Расширить фильтр WHERE (ИЛ ИЛИ Код 00009)**

old_string:
```bsl
		|	И У.Результат <> 0
		|	И У.ДокументОснование <> ЗНАЧЕНИЕ(Документ.ИсполнительныйЛист.ПустаяСсылка)
		|	И КодыОрг.КодПоЕДРПОУ В (&масКодПоЕДРПОУ)
```
new_string:
```bsl
		|	И У.Результат <> 0
		|	И (У.ДокументОснование <> ЗНАЧЕНИЕ(Документ.ИсполнительныйЛист.ПустаяСсылка)
		|		ИЛИ У.ВидРасчета.Код = ""00009"")
		|	И КодыОрг.КодПоЕДРПОУ В (&масКодПоЕДРПОУ)
```

- [ ] **Step 3: Добавить ВидРасчета.Код в GROUP BY**

old_string:
```bsl
		|СГРУППИРОВАТЬ ПО
		|	У.Сотрудник.Физлицо.КодПоДРФО,
		|	У.Сотрудник.Наименование,
		|	У.ВидРасчета.Наименование,
		|	У.ДокументОснование.Номер,
```
new_string:
```bsl
		|СГРУППИРОВАТЬ ПО
		|	У.Сотрудник.Физлицо.КодПоДРФО,
		|	У.Сотрудник.Наименование,
		|	У.ВидРасчета.Наименование,
		|	У.ВидРасчета.Код,
		|	У.ДокументОснование.Номер,
```

---

## Task 3: Правка 2 — ветвление в ЗаписатьУдержанияВТЧ_Бух

**Files:**
- Modify: `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` (`ЗаписатьУдержанияВТЧ_Бух` ~708-771)

- [ ] **Step 1: Кэшировать подотчётные ссылки (рядом с алиментными)**

old_string:
```bsl
	ПрочиеПассивы = ПланыВидовХарактеристик.СтатьиАктивовПассивов.НайтиПоНаименованию("Прочие пассивы");
	СчетУчета6853 = ПланыСчетов.Хозрасчетный.НайтиПоКоду("6853");
```
new_string:
```bsl
	ПрочиеПассивы = ПланыВидовХарактеристик.СтатьиАктивовПассивов.НайтиПоНаименованию("Прочие пассивы");
	СчетУчета6853 = ПланыСчетов.Хозрасчетный.НайтиПоКоду("6853");
	СтатьяПодотчетные = ПланыВидовХарактеристик.СтатьиАктивовПассивов.НайтиПоНаименованию("Денежные средства (у подотчетных лиц)");
	СчетУчета3721 = ПланыСчетов.Хозрасчетный.НайтиПоКоду("3721");
```

- [ ] **Step 2: Пропустить поиск Получателя для подотчётного + разветвить заполнение строки**

old_string:
```bsl
		КонтрагентERP = НайтиКонтрагентаПоУд_Бух(
			СокрЛП("" + Ряд.ЕДРПОУПолучателя),
			СокрЛП("" + Ряд.ИННПолучателя),
			СокрЛП("" + Ряд.ИмяПолучателя));

		Если НЕ ЗначениеЗаполнено(КонтрагентERP) Тогда
			КолПомилокКонтрагент = КолПомилокКонтрагент + 1;
			Сообщить("Удержания: не знайдено контрагента '" + Ряд.ИмяПолучателя
				+ "' (ЄДРПОУ=" + Ряд.ЕДРПОУПолучателя + ", ІНН=" + Ряд.ИННПолучателя + ")");
		КонецЕсли;

		НоваяСтрока = УдержанияБухгалтерия.Добавить();
		НоваяСтрока.Организация             = ОрганизацияERP;
		НоваяСтрока.ФизическоеЛицо          = СотрудникERP.ФизическоеЛицо;
		НоваяСтрока.ВидОперации             = ВидОперацииАлименты;
		НоваяСтрока.СтатьяАктивовПассивов   = ПрочиеПассивы;
		НоваяСтрока.Сумма                   = Ряд.Сумма;
		НоваяСтрока.СчетУчета               = СчетУчета6853;
		Если ЗначениеЗаполнено(КонтрагентERP) Тогда
			НоваяСтрока.Субконто1 = КонтрагентERP;
		КонецЕсли;
		НоваяСтрока.ГруппаУчетаНачислений   = ГруппаУчетаЗарплата;
```
new_string:
```bsl
		ЭтоПодотчетне = (СокрЛП("" + Ряд.КодВида) = "00009");

		КонтрагентERP = Неопределено;
		Если НЕ ЭтоПодотчетне Тогда
			КонтрагентERP = НайтиКонтрагентаПоУд_Бух(
				СокрЛП("" + Ряд.ЕДРПОУПолучателя),
				СокрЛП("" + Ряд.ИННПолучателя),
				СокрЛП("" + Ряд.ИмяПолучателя));
			Если НЕ ЗначениеЗаполнено(КонтрагентERP) Тогда
				КолПомилокКонтрагент = КолПомилокКонтрагент + 1;
				Сообщить("Удержания: не знайдено контрагента '" + Ряд.ИмяПолучателя
					+ "' (ЄДРПОУ=" + Ряд.ЕДРПОУПолучателя + ", ІНН=" + Ряд.ИННПолучателя + ")");
			КонецЕсли;
		КонецЕсли;

		НоваяСтрока = УдержанияБухгалтерия.Добавить();
		НоваяСтрока.Организация             = ОрганизацияERP;
		НоваяСтрока.ФизическоеЛицо          = СотрудникERP.ФизическоеЛицо;
		НоваяСтрока.ВидОперации             = ВидОперацииАлименты;
		НоваяСтрока.Сумма                   = Ряд.Сумма;
		НоваяСтрока.ГруппаУчетаНачислений   = ГруппаУчетаЗарплата;
		Если ЭтоПодотчетне Тогда
			// Возврат подотчётных средств при увольнении: счёт 3721, статья «Денежные средства (у подотчетных лиц)», без получателя.
			НоваяСтрока.СтатьяАктивовПассивов = СтатьяПодотчетные;
			НоваяСтрока.СчетУчета             = СчетУчета3721;
		Иначе
			// Алименты / исполнительные листы: счёт 6853, «Прочие пассивы», Субконто1 = Получатель.
			НоваяСтрока.СтатьяАктивовПассивов = ПрочиеПассивы;
			НоваяСтрока.СчетУчета             = СчетУчета6853;
			Если ЗначениеЗаполнено(КонтрагентERP) Тогда
				НоваяСтрока.Субконто1 = КонтрагентERP;
			КонецЕсли;
		КонецЕсли;
```

- [ ] **Step 3: Code review + commit правок**

`/1c-bsl-review` на обе процедуры или визуальная сверка (табы, имена). Затем:
```bash
git add "Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl"
git commit -m "feat(подотчётне): грузить удержание Код 00009 на счёт 3721 + статья Денежные средства (у подотчетных лиц)"
```

---

## Task 4: Загрузка в БД

- [ ] **Step 1: Partial load + update**

`/db-load-xml -Mode Partial` для `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` (метаданные не менялись — можно только .bsl; для надёжности включить и `Documents/А_ОтражениеЗПпоКазне.xml`) + `/db-update -Dynamic+`.
Команда (как в прошлых задачах): `epf-build/db-load-xml.ps1 ... -InfoBaseServer "SQLSERVER" -InfoBaseRef "BaseERP" -UserName "Администратор" -Password "24043" -ConfigDir "C:\Configuration_downloads\BASERP25" -Mode Partial -Files "Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl"`.
Expected: загрузка + динамическое обновление без ошибок.

---

## Task 5: GREEN-8a — ТЧ-уровень (in-memory)

**Files:**
- Create: `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/green_podzvitne_gunyaga.py`

- [ ] **Step 1: Скрипт — заполнить в памяти БЕЗ записи**

```python
# -*- coding: utf-8 -*-
import io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client as w
erp = w.Dispatch("V83.COMConnector").Connect('Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
ref = erp.Документы.А_ОтражениеЗПпоКазне.НайтиПоНомеру("000000001")
ob = ref.ПолучитьОбъект()
ob.ЗагрузитьНалогиИУдержанияИзЗарплатыБух()   # перезагрузка ТЧ из zup_1 (в памяти)
print("УдержанияБухгалтерия Гуняги после правки:")
ok = False
for i in range(ob.УдержанияБухгалтерия.Количество()):
    s = ob.УдержанияБухгалтерия.Получить(i)
    if erp.ЗначениеЗаполнено(s.ФизическоеЛицо) and s.ФизическоеЛицо.Наименование.startswith("Гуняга"):
        ст = s.СтатьяАктивовПассивов.Наименование if erp.ЗначениеЗаполнено(s.СтатьяАктивовПассивов) else "<пусто>"
        сч = s.СчетУчета.Код if erp.ЗначениеЗаполнено(s.СчетУчета) else "<пусто>"
        пол = s.Субконто1.Наименование if erp.ЗначениеЗаполнено(s.Субконто1) else "<нет>"
        print(f"   Σ={s.Сумма} | статья={ст} | счёт={сч} | Субконто1={пол}")
        ok = (abs(s.Сумма-400)<0.01 and ст=="Денежные средства (у подотчетных лиц)" and сч=="3721"
              and not erp.ЗначениеЗаполнено(s.Субконто1))
print("ИТОГ:", "GREEN ✅" if ok else "RED ❌ (см. выше)")
# Регрессия: алименты (Дубінько/Старіков) на месте, счёт 6853
al = sum(1 for i in range(ob.УдержанияБухгалтерия.Количество())
         if erp.ЗначениеЗаполнено(ob.УдержанияБухгалтерия.Получить(i).СчетУчета)
         and ob.УдержанияБухгалтерия.Получить(i).СчетУчета.Код=="6853")
print(f"Алиментных строк (счёт 6853): {al} (ожидаем как до правки, >0)")
```

- [ ] **Step 2: Запустить**

Run: `& "C:\Python313\python.exe" "_Rarzrabotki\notebook\knowledge_А_ОтражениеЗПпоКазне\Python\test\green_podzvitne_gunyaga.py"`
Expected: Гуняга Σ=400, статья «Денежные средства (у подотчетных лиц)», счёт 3721, Субконто1 нет → GREEN ✅; алиментных строк (6853) > 0 (регрессия чистая).

- [ ] **Step 3: Commit**

```bash
git add "_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/green_podzvitne_gunyaga.py"
git commit -m "test(подотчётне): GREEN-8a — Гуняга 400 на 3721 + статья подотчётных (in-memory)"
```

---

## Task 6: Handoff — UI-перепроведение

- [ ] **Step 1: Сообщить пользователю**

Перепровести `А_ОтражениеЗПпоКазне №000000001` через UI (Заполнить всё → Создать ОЗФУ → провести). ТОЛЬКО UI (COM задваивает ТЧ ОЗФУ — LESSONS §19). **СТОП до подтверждения.**

---

## Task 7: GREEN-8b — регистр + баланс (после UI-перепроведения)

**Files:**
- Create: `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/green_podzvitne_reconciliation.py`

- [ ] **Step 1: Сверка А_ВзСС + ПАП баланс**

Скрипт: (а) `А_ВзаиморасчетыССотрудниками` Форма1 Гуняга знак.остаток на 31.12.2025 → **0** (был 400); (б) движения `ПрочиеАктивыПассивы` дочернего ОЗФУ: появилась пара со статьёй «Денежные средства (у подотчетных лиц)» Σ400 + «Оплата труда» уменьшилась на 400; КонтрольБаланса по Гуняге не вырос. (в) регрессия: алиментные удержания (Дубінько/Старіков) дают ПАП «Прочие пассивы» как раньше.

- [ ] **Step 2: Прогнать отчёт сверки** (UI или COM-зеркало `verify_665_vzss_report.py`-стиль): Гуняга `Разн.кон.остаток = 0`.

- [ ] **Step 3: Commit**

```bash
git add "_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/green_podzvitne_reconciliation.py"
git commit -m "test(подотчётне): GREEN-8b — Гуняга А_ВзСС=0, ПАП пара подотчётных, баланс цел"
```

---

## Task 8: Knowledge + память

- [ ] **Step 1: LESSONS.md §24** — удержания НЕ только по исполнительным листам; «підзвітних ГК» (Код 00009) → счёт 3721 + статья «Денежные средства (у подотчетных лиц)»; ветвление в `ЗаписатьУдержанияВТЧ_Бух`; типовой ОЗФУ читает статью из строки.
- [ ] **Step 2: KNOWLEDGE_MAP.md** — строка про подотчётные удержания.
- [ ] **Step 3: Commit** `docs(knowledge): LESSONS §24 — подотчётные удержания (Код 00009 → 3721)`.
- [ ] **Step 4: Память** — topic-файл/строка про путь загрузки удержаний (фильтр ИЛ + ветвление по виду).

---

## Self-Review

- **Покрытие промта:** §6.1 фильтр → Task 2; §6.2 ветвление 3721/статья → Task 3; §7 RED → Task 1; §8a → Task 5; §8b/8c → Task 7; §9 регрессия алиментов → Task 5/7; загрузка → Task 4; handoff → Task 6. Покрыто.
- **Плейсхолдеры:** old/new_string дословны (из чтения кода); скрипты полны. Открытый пункт — точный `ВидОперации` подотчётной строки: оставлен `ВидОперацииАлименты` (статья выставляется явно, ПАП читает статью из строки — стр.596/620 типового ОЗФУ); GREEN-8b (Task 7) валидирует пару ПАП, при mis-post скорректировать ВидОперации.
- **Согласованность:** `КодВида` (SELECT Task 2) ↔ `Ряд.КодВида` (Task 3); `СтатьяПодотчетные`/`СчетУчета3721` объявлены (Task 3 Step 1) и используются (Step 2); «00009» един.
