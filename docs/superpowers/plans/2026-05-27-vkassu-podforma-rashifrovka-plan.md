# Подформа редактирования А_Расшифровки в ВКассу — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Создать локальную форму `А_ФормаСпискаРасшифровкиПоФЛ` в составе документа `ВедомостьНаВыплатуЗарплатыВКассу` для редактирования ТЧ `А_РасшифровкаВыплатыЗарплатаПоФизлицам` (с колонкой `СтатьяДвиженияДенежныхСредств`), и встроить её в форму документа ВКассу вместо типовой `CommonForms.РедактированиеЗарплатыСтрокиВедомости` (при двойном клике на ТЧ Состав и при нажатии «Изменить зарплату»).

**Architecture:** Паттерн 1:1 как у `Документ.ОтражениеЗарплатыВФинансовомУчете.Форма.ФормаСпискаФизическихЛиц`. Локальная форма с реквизитом-ТЧ. Передача данных через временное хранилище. Возврат изменений через `ОписаниеОповещения` → серверная процедура в основной форме (удалить старые строки этого ФЛ + добавить новые).

**Tech Stack:** BSL (1С 8.3.20), XML формы 1С (управляемая форма), db-load-xml/db-update PowerShell skills для доставки, Python COM (V83.COMConnector + pywin32) для тестирования метаданных.

**Спецификация:** `docs/superpowers/specs/2026-05-27-vkassu-podforma-rashifrovka-design.md`

---

## File Structure

| Файл | Действие | Назначение |
|---|---|---|
| `Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml` | Modify | Добавить `<Form>А_ФормаСпискаРасшифровкиПоФЛ</Form>` в `<ChildObjects>` |
| `Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/А_ФормаСпискаРасшифровкиПоФЛ.xml` | Create | Метаданные новой формы (короткий XML) |
| `Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/А_ФормаСпискаРасшифровкиПоФЛ/Ext/Form.xml` | Create | Сама форма (TableBox + кнопки) — на основе шаблона `ФормаСпискаФизическихЛиц` |
| `Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/А_ФормаСпискаРасшифровкиПоФЛ/Ext/Form/Module.bsl` | Create | Модуль формы (ПриСозданииНаСервере, ОК, Отмена, ВыбратьИЗакрыть) |
| `Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/ФормаДокумента/Ext/Form/Module.bsl` | Modify | Переопределить `СоставВыбор` и `РедактироватьЗарплатуСтроки` + добавить 4 новые процедуры (`А_ОткрытьРасшифровкуДляФЛ`, `А_ПодготовитьПараметрыНаСервере`, `А_РасшифровкаЗакрыта`, `А_РасшифровкаЗакрытаНаСервере`) |
| `_Rarzrabotki/Python/test/test_vkassu_subform_metadata.py` | Create | Smoke: новая форма зарегистрирована в метаданных, имеет реквизит-ТЧ |
| `_Rarzrabotki/Python/test/test_vkassu_subform_returns_changes.py` | Create | Программный smoke: симуляция возврата изменений (удалить старые ФЛ + добавить новые) на ВКассу 000Ц-000009 |

---

## Task 1: Pre-flight — изучить шаблон ФормаСпискаФизическихЛиц

Нам нужен **точный** реверс-инжиниринг шаблона: какие неймспейсы, как описаны реквизиты формы, как описан TableBox и кнопки. Это позволит написать наш Form.xml корректно.

**Files:**
- Read: `Documents/ОтражениеЗарплатыВФинансовомУчете/Forms/ФормаСпискаФизическихЛиц/Ext/Form.xml`
- Read: `Documents/ОтражениеЗарплатыВФинансовомУчете/Forms/ФормаСпискаФизическихЛиц.xml` (метаданное формы)
- Read: `Documents/ОтражениеЗарплатыВФинансовомУчете.xml` (как зарегистрирована форма в составе документа)

- [ ] **Step 1: Прочитать метаданное формы шаблона**

Run:
```
Read("Documents/ОтражениеЗарплатыВФинансовомУчете/Forms/ФормаСпискаФизическихЛиц.xml")
```
Зафиксировать:
- Корневой неймспейс (`xmlns:...`)
- UUID формы (`uuid="..."`)
- Свойства `<Properties>`: Name, Synonym, Comment, FormType, IncludeHelpInContents

Зафиксировать в комментарий плана: **UUID шаблона = `<значение>`**. Для нашей формы нужен **новый UUID** — сгенерируем в шаге 4.

- [ ] **Step 2: Прочитать структуру Form.xml шаблона**

Run:
```
Read("Documents/ОтражениеЗарплатыВФинансовомУчете/Forms/ФормаСпискаФизическихЛиц/Ext/Form.xml", limit=100)
```
Зафиксировать:
- Корневой элемент `<Form xmlns:...>`
- Attributes (FormItems) — секция атрибутов формы
- Items (FormItems) — секция элементов
- Commands — секция команд

- [ ] **Step 3: Прочитать как форма зарегистрирована в составе документа**

Run:
```
Grep("ФормаСпискаФизическихЛиц", "Documents/ОтражениеЗарплатыВФинансовомУчете.xml")
```
Зафиксировать формат строки регистрации формы в `<ChildObjects>`.

- [ ] **Step 4: Сгенерировать новый UUID для нашей формы**

В Python:
```python
import uuid
print(str(uuid.uuid4()))
```

Записать новый UUID — будем использовать его в Task 2.

- [ ] **Step 5: Commit заметок (опциональный — если хочется зафиксировать в файле план обновлений)**

Препрочитанные сведения — в комментарии будущих файлов. Прямого коммита нет.

---

## Task 2: Создать форму А_ФормаСпискаРасшифровкиПоФЛ (XML + Module)

Создаём 3 файла формы: метаданное формы (Forms/Name.xml), Form.xml, Form/Module.bsl.

**Files:**
- Create: `Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/А_ФормаСпискаРасшифровкиПоФЛ.xml`
- Create: `Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/А_ФормаСпискаРасшифровкиПоФЛ/Ext/Form.xml`
- Create: `Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/А_ФормаСпискаРасшифровкиПоФЛ/Ext/Form/Module.bsl`
- Create (helper): `_Rarzrabotki/Python/scaffold_subform.py`

- [ ] **Step 1: Написать Python helper для скаффолда формы**

Создать `_Rarzrabotki/Python/scaffold_subform.py`. Этот скрипт:
1. Читает шаблон `Documents/ОтражениеЗарплатыВФинансовомУчете/Forms/ФормаСпискаФизическихЛиц/Ext/Form.xml`
2. Делает строковые замены:
   - Имя формы и связанной ТЧ
   - Колонки (оставляет только нужные: ФизическоеЛицо, Сотрудник, Подразделение, СтатьяДвиженияДенежныхСредств, НаправлениеДеятельности, ОтработаноЧасов, Сумма)
3. Записывает с правильным CRLF.

Содержимое скрипта:

```python
"""
Scaffold: создать локальную форму А_ФормаСпискаРасшифровкиПоФЛ в составе
документа ВедомостьНаВыплатуЗарплатыВКассу. Использует ФормаСпискаФизическихЛиц
как шаблон.
"""
import io, sys, os, uuid as uuid_mod

sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")

NEW_UUID = "<СГЕНЕРИРОВАННЫЙ_UUID_ИЗ_TASK_1_STEP_4>"

ROOT = r"C:\Configuration_downloads\BASERP25"
NEW_FORM_DIR = os.path.join(ROOT, "Documents",
    "ВедомостьНаВыплатуЗарплатыВКассу", "Forms",
    "А_ФормаСпискаРасшифровкиПоФЛ")
os.makedirs(os.path.join(NEW_FORM_DIR, "Ext", "Form"), exist_ok=True)

# 1. МЕТАДАННОЕ ФОРМЫ — Forms/А_ФормаСпискаРасшифровкиПоФЛ.xml
META_XML = '''<?xml version="1.0" encoding="UTF-8"?>
<MetaDataObject xmlns="http://v8.1c.ru/8.3/MDClasses" xmlns:app="http://v8.1c.ru/8.2/managed-application/core" xmlns:cfg="http://v8.1c.ru/8.1/data/enterprise/current-config" xmlns:cmi="http://v8.1c.ru/8.2/managed-application/cmi" xmlns:ent="http://v8.1c.ru/8.1/data/enterprise" xmlns:lf="http://v8.1c.ru/8.2/managed-application/logform" xmlns:style="http://v8.1c.ru/8.1/data/ui/style" xmlns:sys="http://v8.1c.ru/8.1/data/ui/fonts/system" xmlns:v8="http://v8.1c.ru/8.1/data/core" xmlns:v8ui="http://v8.1c.ru/8.1/data/ui" xmlns:web="http://v8.1c.ru/8.1/data/ui/colors/web" xmlns:win="http://v8.1c.ru/8.1/data/ui/colors/windows" xmlns:xen="http://v8.1c.ru/8.3/xcf/enums" xmlns:xpr="http://v8.1c.ru/8.3/xcf/predef" xmlns:xr="http://v8.1c.ru/8.3/xcf/readable" xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" version="2.16">
\t<Form uuid="''' + NEW_UUID + '''">
\t\t<Properties>
\t\t\t<Name>А_ФормаСпискаРасшифровкиПоФЛ</Name>
\t\t\t<Synonym>
\t\t\t\t<v8:item>
\t\t\t\t\t<v8:lang>ru</v8:lang>
\t\t\t\t\t<v8:content>А расшифровка по статье ДДС</v8:content>
\t\t\t\t</v8:item>
\t\t\t</Synonym>
\t\t\t<Comment/>
\t\t\t<FormType>Managed</FormType>
\t\t\t<IncludeHelpInContents>false</IncludeHelpInContents>
\t\t\t<UsePurposes>
\t\t\t\t<v8:Value xsi:type="app:ApplicationUsePurpose">PersonalComputer</v8:Value>
\t\t\t\t<v8:Value xsi:type="app:ApplicationUsePurpose">MobileDevice</v8:Value>
\t\t\t</UsePurposes>
\t\t\t<ExtendedPresentation/>
\t\t</Properties>
\t</Form>
</MetaDataObject>
'''

with open(os.path.join(NEW_FORM_DIR, "..", "А_ФормаСпискаРасшифровкиПоФЛ.xml"), "wb") as f:
    f.write(META_XML.replace("\n", "\r\n").encode("utf-8"))
print("OK: метаданное формы создано")

# 2. FORM.XML (читаем шаблон → модифицируем)
TEMPLATE_FORM = os.path.join(ROOT, "Documents",
    "ОтражениеЗарплатыВФинансовомУчете", "Forms",
    "ФормаСпискаФизическихЛиц", "Ext", "Form.xml")

with open(TEMPLATE_FORM, "rb") as f:
    template_bytes = f.read()
template_text = template_bytes.decode("utf-8")
# Преобразование LF→CRLF не нужно (читаем как есть), запишем CRLF в конце

# Простые замены имени ТЧ и колонок:
replacements = {
    "НачисленнаяЗарплатаИВзносыПоФизлицам": "А_РасшифровкаВыплатыЗарплатаПоФизлицам",
    # А_РасшифровкаВыплатыЗарплатаПоФизлицам имеет 7 реквизитов вместо ~10 в шаблоне.
    # Колонки шаблона могут отличаться. Текстовая замена названия ТЧ сохранит структуру,
    # но не уберёт лишние/неактуальные колонки. Этот скрипт делает только base-замены;
    # окончательная правка Form.xml вручную через Edit-инструмент в следующих шагах.
}
out_text = template_text
for old, new in replacements.items():
    out_text = out_text.replace(old, new)

NEW_FORM_XML = os.path.join(NEW_FORM_DIR, "Ext", "Form.xml")
with open(NEW_FORM_XML, "wb") as f:
    # Если шаблон уже с CRLF — оставляем как есть
    f.write(out_text.encode("utf-8"))
print(f"OK: Form.xml создан (с заменой имён ТЧ). Дальше — ручная правка колонок и кнопок.")

# 3. MODULE.BSL — наш модуль с нуля
MODULE_BSL = '''#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

\tЕсли Параметры.Свойство("АвтоТест") Тогда
\t\tВозврат;
\tКонецЕсли;

\tЕсли Параметры.Свойство("Заголовок") И ЗначениеЗаполнено(Параметры.Заголовок) Тогда
\t\tЗаголовок = Параметры.Заголовок;
\tКонецЕсли;

\tЕсли Параметры.Свойство("ФизическоеЛицо") Тогда
\t\tФизическоеЛицо = Параметры.ФизическоеЛицо;
\tКонецЕсли;

\tЕсли Параметры.Свойство("Организация") Тогда
\t\tОрганизация = Параметры.Организация;
\tКонецЕсли;

\tЕсли Параметры.Свойство("АдресВХранилище") И ЗначениеЗаполнено(Параметры.АдресВХранилище) Тогда
\t\tТЗ = ПолучитьИзВременногоХранилища(Параметры.АдресВХранилище);
\t\tА_РасшифровкаВыплатыЗарплатаПоФизлицам.Загрузить(ТЗ);
\tКонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, ТекстПредупреждения, СтандартнаяОбработка)

\tЕсли Модифицированность Тогда
\t\tОтказ = Истина;
\t\tТекст = НСтр("ru='Данные были изменены. Сохранить изменения?'");
\t\tОповещение = Новый ОписаниеОповещения("ВопросПередЗакрытием", ЭтотОбъект);
\t\tПоказатьВопрос(Оповещение, Текст, РежимДиалогаВопрос.ДаНетОтмена);
\tКонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ВопросПередЗакрытием(РезВопрос, ДопПарам) Экспорт

\tЕсли РезВопрос = КодВозвратаДиалога.Да Тогда
\t\tВыбратьИЗакрыть();
\tИначеЕсли РезВопрос = КодВозвратаДиалога.Нет Тогда
\t\tМодифицированность = Ложь;
\t\tЗакрыть();
\tКонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
Процедура А_РасшифровкаВыплатыЗарплатаПоФизлицамПриНачалеРедактирования(Элемент, НоваяСтрока, Копирование)

\tЕсли НоваяСтрока И НЕ Копирование Тогда
\t\tЭлемент.ТекущиеДанные.ФизическоеЛицо = ФизическоеЛицо;
\t\tЭлемент.ТекущиеДанные.ИдентификаторСтроки = Новый УникальныйИдентификатор();
\tКонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
\tВыбратьИЗакрыть();
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
\tМодифицированность = Ложь;
\tЗакрыть();
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура ВыбратьИЗакрыть()

\tРезультат = Новый Структура;
\tРезультат.Вставить("Модифицированность", Модифицированность);
\tРезультат.Вставить("ФизическоеЛицо", ФизическоеЛицо);
\tРезультат.Вставить("АдресВХранилище", А_АдресВХранилище());

\tМодифицированность = Ложь;
\tЗакрыть(Результат);

КонецПроцедуры

&НаСервере
Функция А_АдресВХранилище()
\tТЗ = А_РасшифровкаВыплатыЗарплатаПоФизлицам.Выгрузить();
\tВозврат ПоместитьВоВременноеХранилище(ТЗ, УникальныйИдентификатор);
КонецФункции

#КонецОбласти
'''
NEW_MODULE = os.path.join(NEW_FORM_DIR, "Ext", "Form", "Module.bsl")
with open(NEW_MODULE, "wb") as f:
    f.write(MODULE_BSL.replace("\n", "\r\n").encode("utf-8"))
print("OK: Module.bsl создан")

print("\nDONE — все 3 файла формы созданы. Далее: ручная правка Form.xml + регистрация в Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml")
```

- [ ] **Step 2: Запустить scaffold**

ПЕРЕД запуском подставить в скрипт `NEW_UUID` из Task 1 Step 4.

Run:
```
C:\Python313\python.exe "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\scaffold_subform.py"
```
Expected: `OK: метаданное формы создано` + `OK: Form.xml создан` + `OK: Module.bsl создан`.

- [ ] **Step 3: Проверить созданные файлы**

Run:
```
ls "Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/А_ФормаСпискаРасшифровкиПоФЛ/Ext/"
ls "Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/А_ФормаСпискаРасшифровкиПоФЛ/Ext/Form/"
```
Expected:
- `Form.xml` (>10KB — копия шаблона с заменами)
- `Form/Module.bsl` (~3KB — наш модуль)
- Файл `Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/А_ФормаСпискаРасшифровкиПоФЛ.xml` создан.

- [ ] **Step 4: Ручная правка Form.xml — TableBox колонки**

Form.xml после копирования содержит **много лишних колонок** из шаблона `ФормаСпискаФизическихЛиц` (ПодразделениеПредприятия, ВидОперации, СпособОтраженияЗарплатыВБухучете, ВзносыВсего, Налог, ГруппаУчетаНачислений и др.).

Нам нужны только 7 колонок из ТЧ А_Расшифровка:
- ФизическоеЛицо, Сотрудник, Подразделение, СтатьяДвиженияДенежныхСредств, НаправлениеДеятельности, ОтработаноЧасов, Сумма

Через Edit/Grep:
1. Прочитать Form.xml → найти все блоки `<Column ...>` внутри `<Table name="А_РасшифровкаВыплатыЗарплатаПоФизлицам">`.
2. Удалить блоки колонок, которых нет в нашей ТЧ.
3. Если каких-то наших колонок нет — добавить их по шаблону существующих.

Проверка: список финальных колонок в Form.xml через `grep -E '<Column name=' Form.xml | grep -v ResStatic` — должно быть 7 строк (плюс служебные если есть).

- [ ] **Step 5: Ручная правка Form.xml — атрибуты формы**

Form.xml в секции `<Attributes>` после копирования содержит атрибуты шаблона (`Сводно`, `ПодразделениеПредприятия`, `ВидОперации`, `АдресВХранилище` и др.).

Нужно:
1. Оставить (или адаптировать): `А_РасшифровкаВыплатыЗарплатаПоФизлицам` (ТЧ-реквизит), `ФизическоеЛицо`, `Организация`.
2. Удалить остальные нерелевантные атрибуты.

Проверка через `grep '<Attribute' Form.xml`.

- [ ] **Step 6: Ручная правка Form.xml — команды/кнопки**

Form.xml имеет команды шаблона. Нам нужны:
- `ОК` — `Action=ОК`
- `Отмена` — `Action=Отмена`

Удалить лишние команды (ЗаполнитьИзНалогов и т.д.).

Кнопки в `<ChildItems>` корневого `<Form>` — у TableBox убедиться что есть стандартные команды Add/Delete/Copy, и в подвале формы кнопки ОК/Отмена.

- [ ] **Step 7: Удалить scaffold-скрипт (он одноразовый)**

Run:
```bash
rm "_Rarzrabotki/Python/scaffold_subform.py"
```

- [ ] **Step 8: Commit (без доставки)**

```bash
git add "Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/А_ФормаСпискаРасшифровкиПоФЛ.xml" "Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/А_ФормаСпискаРасшифровкиПоФЛ/"
git commit -m "$(cat <<'EOF'
feat(vkassu-subform): создана локальная форма А_ФормаСпискаРасшифровкиПоФЛ

В составе документа ВедомостьНаВыплатуЗарплатыВКассу — новая форма для
редактирования ТЧ А_РасшифровкаВыплатыЗарплатаПоФизлицам с колонкой
СтатьяДвиженияДенежныхСредств. Паттерн как у ОтражениеЗПвФинУчёте
ФормаСпискаФизическихЛиц.

Файлы:
- Forms/А_ФормаСпискаРасшифровкиПоФЛ.xml (метаданное)
- Forms/А_ФормаСпискаРасшифровкиПоФЛ/Ext/Form.xml (TableBox+кнопки)
- Forms/А_ФормаСпискаРасшифровкиПоФЛ/Ext/Form/Module.bsl
  (ПриСозданииНаСервере + ОК/Отмена + ВыбратьИЗакрыть)

Регистрация в составе документа и доставка — в следующих коммитах.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Зарегистрировать форму в метаданных документа

Файл `Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml` имеет секцию `<ChildObjects>` где перечислены все ТЧ + формы + макеты документа. Нужно добавить нашу новую форму.

**Files:**
- Modify: `Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml`

- [ ] **Step 1: Найти секцию ChildObjects → Forms**

Run:
```
Grep("<Form>", "Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml")
```
Expected: видим `<Form>ФормаДокумента</Form>` и `<Form>ФормаСписка</Form>`.

- [ ] **Step 2: Добавить ссылку на новую форму**

Через Edit добавить ПОСЛЕ `<Form>ФормаСписка</Form>`:
```xml
<Form>А_ФормаСпискаРасшифровкиПоФЛ</Form>
```

- [ ] **Step 3: Проверить что Edit сработал**

Run:
```
Grep("А_ФормаСпискаРасшифровкиПоФЛ", "Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml")
```
Expected: одна строка `<Form>А_ФормаСпискаРасшифровкиПоФЛ</Form>`.

- [ ] **Step 4: Commit**

```bash
git add "Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml"
git commit -m "$(cat <<'EOF'
chore(vkassu-subform): регистрация формы А_ФормаСпискаРасшифровкиПоФЛ
в составе документа ВедомостьНаВыплатуЗарплатыВКассу

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Переопределить обработчики в ФормаДокумента ВКассу

В `Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/ФормаДокумента/Ext/Form/Module.bsl`:
- стр.233 переопределить `СоставВыбор`
- стр.404-407 переопределить `РедактироватьЗарплатуСтроки`
- В конец файла добавить 4 новые процедуры.

**Files:**
- Modify: `Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/ФормаДокумента/Ext/Form/Module.bsl`

- [ ] **Step 1: Переопределить СоставВыбор (стр. 232-234)**

Найти точную строку:
```bsl
&НаКлиенте
Процедура СоставВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	ВзаиморасчетыССотрудникамиКлиентРасширенный.ВедомостьСоставВыбор(ЭтаФорма, Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)	
КонецПроцедуры
```

Заменить через Edit на:
```bsl
&НаКлиенте
Процедура СоставВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	СтрС = Объект.Состав.НайтиПоИдентификатору(ВыбраннаяСтрока);
	Если СтрС = Неопределено Тогда Возврат; КонецЕсли;
	А_ОткрытьРасшифровкуДляФЛ(СтрС.ФизическоеЛицо);
КонецПроцедуры
```

- [ ] **Step 2: Переопределить РедактироватьЗарплатуСтроки (стр. 404-407)**

Найти:
```bsl
&НаКлиенте
Процедура РедактироватьЗарплатуСтроки(ДанныеСтроки) Экспорт
	ВзаиморасчетыССотрудникамиКлиент.ВедомостьРедактироватьЗарплатуСтроки(ЭтаФорма, ДанныеСтроки);	
КонецПроцедуры
```

Заменить через Edit на:
```bsl
&НаКлиенте
Процедура РедактироватьЗарплатуСтроки(ДанныеСтроки) Экспорт
	Если ДанныеСтроки <> Неопределено И ЗначениеЗаполнено(ДанныеСтроки.ФизическоеЛицо) Тогда
		А_ОткрытьРасшифровкуДляФЛ(ДанныеСтроки.ФизическоеЛицо);
	КонецЕсли;
КонецПроцедуры
```

- [ ] **Step 3: Добавить 4 новые процедуры в конец файла**

Через append (Python helper или Edit на последний `КонецОбласти` если он есть, или просто после последней процедуры).

Если файл оканчивается на стандартный текст — Edit:

Старая последняя строка (нужно найти grep'ом — типично что-то вроде `КонецПроцедуры` + `КонецЕсли` или конец файла):

```
Read("Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/ФормаДокумента/Ext/Form/Module.bsl", offset=<последние_20_строк>)
```

Затем Edit (или Python helper с CRLF если плохо ловится):

Добавить в конец:
```bsl

&НаКлиенте
Процедура А_ОткрытьРасшифровкуДляФЛ(ФЛ)

	Если НЕ ЗначениеЗаполнено(ФЛ) Тогда
		Возврат;
	КонецЕсли;

	ПарамФормы = А_ПодготовитьПараметрыНаСервере(ФЛ);
	ОткрытьФорму(
		"Документ.ВедомостьНаВыплатуЗарплатыВКассу.Форма.А_ФормаСпискаРасшифровкиПоФЛ",
		ПарамФормы, ЭтаФорма, , , ,
		Новый ОписаниеОповещения("А_РасшифровкаЗакрыта", ЭтотОбъект, Новый Структура("ФЛ", ФЛ)));

КонецПроцедуры

&НаСервере
Функция А_ПодготовитьПараметрыНаСервере(ФЛ)

	ТЗ = Объект.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Выгрузить(
		Новый Структура("ФизическоеЛицо", ФЛ));

	АдрХр = ПоместитьВоВременноеХранилище(ТЗ, УникальныйИдентификатор);

	Парам = Новый Структура;
	Парам.Вставить("АдресВХранилище",  АдрХр);
	Парам.Вставить("ФизическоеЛицо",   ФЛ);
	Парам.Вставить("Организация",      Объект.Организация);
	Парам.Вставить("Заголовок",        Строка(ФЛ) + ": расшифровка по статье");

	Возврат Парам;

КонецФункции

&НаКлиенте
Процедура А_РасшифровкаЗакрыта(Результат, ДопПарам) Экспорт

	Если Результат = Неопределено Тогда Возврат; КонецЕсли;
	Если НЕ Результат.Свойство("Модифицированность") ИЛИ НЕ Результат.Модифицированность Тогда
		Возврат;
	КонецЕсли;

	А_РасшифровкаЗакрытаНаСервере(Результат.АдресВХранилище, ДопПарам.ФЛ);
	Модифицированность = Истина;

КонецПроцедуры

&НаСервере
Процедура А_РасшифровкаЗакрытаНаСервере(АдрХр, ФЛ)

	Удаляемые = Объект.А_РасшифровкаВыплатыЗарплатаПоФизлицам.НайтиСтроки(
		Новый Структура("ФизическоеЛицо", ФЛ));
	Для Каждого СтрУд Из Удаляемые Цикл
		Объект.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Удалить(СтрУд);
	КонецЦикла;

	НоваяТЗ = ПолучитьИзВременногоХранилища(АдрХр);
	Для Каждого СтрН Из НоваяТЗ Цикл
		НоваяСтр = Объект.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Добавить();
		ЗаполнитьЗначенияСвойств(НоваяСтр, СтрН);
		Если НЕ ЗначениеЗаполнено(НоваяСтр.ИдентификаторСтроки) Тогда
			НоваяСтр.ИдентификаторСтроки = Новый УникальныйИдентификатор();
		КонецЕсли;
	КонецЦикла;

КонецПроцедуры
```

- [ ] **Step 4: Commit без доставки**

```bash
git add "Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/ФормаДокумента/Ext/Form/Module.bsl"
git commit -m "$(cat <<'EOF'
feat(vkassu-subform): переопределены СоставВыбор + РедактироватьЗарплатуСтроки

В Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/ФормаДокумента:
- СоставВыбор (стр.232-234): открывает нашу А_ФормаСпискаРасшифровкиПоФЛ
  вместо типовой ВзаиморасчетыССотрудникамиКлиентРасширенный.ВедомостьСоставВыбор.
- РедактироватьЗарплатуСтроки (стр.404-407): тоже наша форма вместо типовой
  ВзаиморасчетыССотрудникамиКлиент.ВедомостьРедактироватьЗарплатуСтроки.
- Добавлены 4 новые процедуры: А_ОткрытьРасшифровкуДляФЛ,
  А_ПодготовитьПараметрыНаСервере, А_РасшифровкаЗакрыта, А_РасшифровкаЗакрытаНаСервере.

Возврат изменений: удалить старые строки этого ФЛ → добавить новые из подформы.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Доставка через db-load-xml + db-update

**Files:**
- Deliver: `Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml` (новая ссылка на форму)
- Deliver: `Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/А_ФормаСпискаРасшифровкиПоФЛ.xml` (вся новая форма)
- Deliver: `Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/ФормаДокумента.xml` (правка обработчиков)

- [ ] **Step 1: Проверить что Designer закрыт**

Спросить пользователя:
> Designer закрыт? Готов к доставке новой формы + правок?

Дождаться подтверждения. Если открыт — попросить закрыть.

- [ ] **Step 2: Partial-load — документ + 2 формы**

Run:
```powershell
& "C:\Configuration_downloads\BASERP25\.claude\skills\db-load-xml\scripts\db-load-xml.ps1" -V8Path "C:\Program Files\1cv8\8.3.20.1914\bin" -InfoBaseServer "SQLSERVER" -InfoBaseRef "BaseERP" -UserName "Администратор" -Password "24043" -ConfigDir "C:\Configuration_downloads\BASERP25" -Mode Partial -Files "Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml,Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/А_ФормаСпискаРасшифровкиПоФЛ.xml,Documents/ВедомостьНаВыплатуЗарплатыВКассу/Forms/ФормаДокумента.xml"
```

Expected: `Load completed successfully`.

Если упало с ошибкой XML — проверить что Form.xml корректен (нет невалидных колонок, ссылки на ТЧ правильные).

- [ ] **Step 3: Dynamic update**

Run:
```powershell
& "C:\Configuration_downloads\BASERP25\.claude\skills\db-update\scripts\db-update.ps1" -V8Path "C:\Program Files\1cv8\8.3.20.1914\bin" -InfoBaseServer "SQLSERVER" -InfoBaseRef "BaseERP" -UserName "Администратор" -Password "24043" -Dynamic "+"
```

Expected: `Database configuration updated successfully` + `Обновление конфигурации успешно завершено`.

Если в логе ошибка типа «Реструктуризация» — динамическое обновление невозможно (новый объект метаданных). В таком случае удалить `-Dynamic+` (т.е. сделать full DB update — `-Dynamic ""`). Спросить пользователя — возможно нужно перезапустить сервер 1С.

- [ ] **Step 4: Commit chore-доставки (только если нужно — обычно нет файлов для коммита)**

Если динамическое обновление прошло — никаких файлов для коммита. Двигаемся к Task 6.

---

## Task 6: Python COM smoke — метаданные формы зарегистрированы

**Files:**
- Create: `_Rarzrabotki/Python/test/test_vkassu_subform_metadata.py`

- [ ] **Step 1: Написать тест проверки метаданных**

Создать `_Rarzrabotki/Python/test/test_vkassu_subform_metadata.py`:

```python
"""
Smoke метаданных: проверяет что:
1. Форма Документ.ВедомостьНаВыплатуЗарплатыВКассу.Форма.А_ФормаСпискаРасшифровкиПоФЛ
   существует в метаданных.
2. У этой формы есть реквизит А_РасшифровкаВыплатыЗарплатаПоФизлицам.
"""
import io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding="utf-8")

import win32com.client

CONN = 'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"'
v8 = win32com.client.Dispatch("V83.COMConnector")
erp = v8.Connect(CONN)

# 1. Найти форму через Метаданные
try:
    meta_doc = erp.Метаданные.Документы.Найти("ВедомостьНаВыплатуЗарплатыВКассу")
    if meta_doc is None:
        print("FAIL: документ ВедомостьНаВыплатуЗарплатыВКассу не найден")
        sys.exit(1)

    form_meta = None
    for i in range(meta_doc.Формы.Количество()):
        f = meta_doc.Формы.Получить(i)
        if str(f.Имя) == "А_ФормаСпискаРасшифровкиПоФЛ":
            form_meta = f
            break

    if form_meta is None:
        print("FAIL: форма А_ФормаСпискаРасшифровкиПоФЛ не найдена в составе документа")
        sys.exit(1)
    print(f"OK: форма '{form_meta.Имя}' зарегистрирована")
except Exception as e:
    info = e.excepinfo[2] if hasattr(e, "excepinfo") and e.excepinfo else str(e)
    print(f"FAIL: ошибка доступа к метаданным: {info}")
    sys.exit(1)

# 2. Открыть форму (без UI — через получение объекта)
try:
    f = erp.ПолучитьФорму("Документ.ВедомостьНаВыплатуЗарплатыВКассу.Форма.А_ФормаСпискаРасшифровкиПоФЛ", None, None, None, False, {"АвтоТест": True})
    if f is None:
        print("FAIL: ПолучитьФорму вернул None")
        sys.exit(1)
    print(f"OK: форма получена (Заголовок='{f.Заголовок}')")
except Exception as e:
    info = e.excepinfo[2] if hasattr(e, "excepinfo") and e.excepinfo else str(e)
    print(f"FAIL: ПолучитьФорму упало: {info}")
    sys.exit(1)

# 3. Проверить наличие реквизита А_РасшифровкаВыплатыЗарплатаПоФизлицам
try:
    attr = f.А_РасшифровкаВыплатыЗарплатаПоФизлицам
    if attr is None:
        print("FAIL: реквизит А_РасшифровкаВыплатыЗарплатаПоФизлицам не найден")
        sys.exit(1)
    print(f"OK: реквизит А_РасшифровкаВыплатыЗарплатаПоФизлицам существует (тип ТЗ, строк={attr.Количество()})")
except Exception as e:
    info = e.excepinfo[2] if hasattr(e, "excepinfo") and e.excepinfo else str(e)
    print(f"FAIL: доступ к реквизиту упал: {info}")
    sys.exit(1)

print("\nPASS: метаданные формы корректны")
```

- [ ] **Step 2: Запустить тест**

Run:
```
C:\Python313\python.exe "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_vkassu_subform_metadata.py"
```

Expected: `PASS: метаданные формы корректны`.

Если упало на шаге «ПолучитьФорму» — возможно проблема с Form.xml (нет команд, не определены свойства). Прочитать сообщение и исправить.

- [ ] **Step 3: Commit smoke-теста**

```bash
git add "_Rarzrabotki/Python/test/test_vkassu_subform_metadata.py"
git commit -m "$(cat <<'EOF'
test(vkassu-subform): smoke метаданных формы А_ФормаСпискаРасшифровкиПоФЛ

Проверяет:
1. Форма зарегистрирована в составе документа ВКассу
2. Форму можно получить через ПолучитьФорму (с параметром АвтоТест=Истина)
3. Реквизит А_РасшифровкаВыплатыЗарплатаПоФизлицам существует и доступен

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: Python COM smoke — возврат изменений работает

Программно симулируем сценарий «открыть подформу → изменить строку → закрыть с ОК → изменения попали в ТЧ документа».

**Files:**
- Create: `_Rarzrabotki/Python/test/test_vkassu_subform_returns_changes.py`

- [ ] **Step 1: Написать тест**

Создать `_Rarzrabotki/Python/test/test_vkassu_subform_returns_changes.py`:

```python
"""
Smoke возврата изменений: симулирует «удалить старые строки ФЛ + добавить новые
из подформы» — точно так же как делает А_РасшифровкаЗакрытаНаСервере в основной
форме ВКассу.

Сценарий:
1. ВКассу 000Ц-000009 (из теста smoke ОтражениеЗП — Ф2 №000000026)
2. Выбрать любого ФЛ из А_Расшифровки
3. Запомнить его строки → изменить одну сумму
4. Применить паттерн возврата: удалить старые строки этого ФЛ, добавить изменённые
5. Записать ВКассу (БЕЗ проведения — не теряем тест на проведение)
6. Перепрочитать → убедиться что изменение сохранилось
7. Откатить изменение (вернуть к исходной сумме) — чтобы тест был идемпотентным
"""
import io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding="utf-8")

import win32com.client

CONN = 'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"'
v8 = win32com.client.Dispatch("V83.COMConnector")
erp = v8.Connect(CONN)

# 1. Найти ВКассу
q = erp.NewObject("Запрос")
q.Текст = '''ВЫБРАТЬ ПЕРВЫЕ 1 Вед.Ссылка КАК Ссылка
ИЗ Документ.ВедомостьНаВыплатуЗарплатыВКассу КАК Вед
ГДЕ Вед.Номер = "000Ц-000009" И НЕ Вед.ПометкаУдаления'''
rs = q.Выполнить()
if rs.Пустой():
    print("FAIL: ВКассу 000Ц-000009 не найдена")
    sys.exit(1)
sel = rs.Выбрать(); sel.Следующий()
vk_ref = sel.Ссылка
vk = vk_ref.ПолучитьОбъект()
print(f"ВКассу: {vk_ref}")

# 2. Выбрать любого ФЛ с непустой строкой в Расшифровке
if vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Количество() == 0:
    print("FAIL: ТЧ А_Расшифровка пуста")
    sys.exit(1)

ФЛ_тест = None
for i in range(vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Количество()):
    r = vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Получить(i)
    if r.ФизическоеЛицо and not r.ФизическоеЛицо.Пустая():
        ФЛ_тест = r.ФизическоеЛицо
        break

if ФЛ_тест is None:
    print("FAIL: не нашли ни одной строки с заполненным ФЛ")
    sys.exit(1)
print(f"Тестируем для ФЛ: {ФЛ_тест}")

# 3. Сохранить исходные строки этого ФЛ
исходные = []
for i in range(vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Количество()):
    r = vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Получить(i)
    if erp.string(r.ФизическоеЛицо.УникальныйИдентификатор()) == erp.string(ФЛ_тест.УникальныйИдентификатор()):
        исходные.append({
            "Сумма": float(r.Сумма),
            "Часы": float(r.ОтработаноЧасов),
        })

if len(исходные) == 0:
    print("FAIL: не нашли строки для теста")
    sys.exit(1)
print(f"Исходных строк для {ФЛ_тест}: {len(исходные)}")
print(f"Σ исх. = {sum(s['Сумма'] for s in исходные):,.2f}")

# 4. Симуляция возврата: удалить старые строки ФЛ → добавить копию с одной изменённой строкой
УДАЛЕНО = 0
for i in reversed(range(vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Количество())):
    r = vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Получить(i)
    if erp.string(r.ФизическоеЛицо.УникальныйИдентификатор()) == erp.string(ФЛ_тест.УникальныйИдентификатор()):
        vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Удалить(r)
        УДАЛЕНО += 1
print(f"Удалено: {УДАЛЕНО} строк")

# Добавить новую строку (тест — увеличить сумму на 1)
тест_прибавка = 1.0
новая = vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Добавить()
новая.ФизическоеЛицо = ФЛ_тест
новая.Сумма = исходные[0]["Сумма"] + тест_прибавка
новая.ОтработаноЧасов = исходные[0]["Часы"]
print(f"Добавлено: 1 новая строка с Σ = {новая.Сумма:,.2f}")

# 5. Записать (БЕЗ проведения — не теряем уже проведённый документ)
vk.ОбменДанными.Загрузка = True  # блокируем типовую логику
vk.Записать(erp.РежимЗаписиДокумента.Запись)
print("OK: ВКассу записана")

# 6. Перепрочитать и проверить
vk2 = vk_ref.ПолучитьОбъект()
найдено = 0
сумма_найдена = 0.0
for i in range(vk2.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Количество()):
    r = vk2.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Получить(i)
    if erp.string(r.ФизическоеЛицо.УникальныйИдентификатор()) == erp.string(ФЛ_тест.УникальныйИдентификатор()):
        найдено += 1
        сумма_найдена += float(r.Сумма)

ожидаемая_сумма = исходные[0]["Сумма"] + тест_прибавка
if найдено != 1:
    print(f"FAIL: ожидали 1 строку для {ФЛ_тест}, нашли {найдено}")
    sys.exit(1)
if abs(сумма_найдена - ожидаемая_сумма) > 0.01:
    print(f"FAIL: Σ = {сумма_найдена:,.2f}, ожидали {ожидаемая_сумма:,.2f}")
    sys.exit(1)
print(f"  Проверка: 1 строка, Σ = {сумма_найдена:,.2f} (ожидали {ожидаемая_сумма:,.2f}) ✓")

# 7. Откат — восстановить исходное состояние
vk3 = vk_ref.ПолучитьОбъект()
for i in reversed(range(vk3.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Количество())):
    r = vk3.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Получить(i)
    if erp.string(r.ФизическоеЛицо.УникальныйИдентификатор()) == erp.string(ФЛ_тест.УникальныйИдентификатор()):
        vk3.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Удалить(r)

for ис in исходные:
    нс = vk3.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Добавить()
    нс.ФизическоеЛицо = ФЛ_тест
    нс.Сумма = ис["Сумма"]
    нс.ОтработаноЧасов = ис["Часы"]

vk3.ОбменДанными.Загрузка = True
vk3.Записать(erp.РежимЗаписиДокумента.Запись)
print("OK: исходное состояние восстановлено")

print("\nPASS: паттерн возврата изменений работает корректно")
```

- [ ] **Step 2: Запустить тест**

Run:
```
C:\Python313\python.exe "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_vkassu_subform_returns_changes.py"
```

Expected: `PASS: паттерн возврата изменений работает корректно`.

- [ ] **Step 3: Commit**

```bash
git add "_Rarzrabotki/Python/test/test_vkassu_subform_returns_changes.py"
git commit -m "$(cat <<'EOF'
test(vkassu-subform): smoke возврата изменений — паттерн А_РасшифровкаЗакрытаНаСервере

Симулирует сценарий "удалить старые строки ФЛ + добавить новые из подформы"
напрямую через COM (без UI). Проверяет что после Записи ВКассу изменения
сохраняются в ТЧ А_Расшифровка. После проверки восстанавливает исходное
состояние (идемпотентность).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: Регрессия + ручная UI-проверка

**Files:** не правим — только запускаем существующие тесты.

- [ ] **Step 1: Регрессия Ф2/ОтражениеЗП тестов**

Run:
```
C:\Python313\python.exe "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_create_vedomost_vkassu_iz_f2_smoke.py"
C:\Python313\python.exe "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_create_vedomost_vkassu_iz_f2_idempotency.py"
C:\Python313\python.exe "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_create_vedomost_vkassu_iz_f2_rashifrovka.py"
C:\Python313\python.exe "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_otrazhenie_vkassu_smoke.py"
C:\Python313\python.exe "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_otrazhenie_vkassu_idempotency.py"
C:\Python313\python.exe "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_otrazhenie_vkassu_statyaddc.py"
C:\Python313\python.exe "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_vkassu_subform_metadata.py"
C:\Python313\python.exe "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_vkassu_subform_returns_changes.py"
```

Expected: все 8 тестов `PASS`.

- [ ] **Step 2: Ручная UI-проверка**

Попросить пользователя:
1. Открыть `Документ.ВедомостьНаВыплатуЗарплатыВКассу 000Ц-000009 от 10.12.2025` в Enterprise.
2. Двойной клик на сотруднике (например «Васіліу Руслан Георгійович»).
3. Должна открыться **наша** форма с заголовком «Васіліу Руслан Георгійович: расшифровка по статье» и **2+ строки** с колонкой «Статья ДДС» = «Зарплата механизаторов».
4. Изменить сумму в одной строке → ОК → проверить что в основной форме появилась модифицированность.
5. Записать ВКассу → перепрочитать → значение сохранилось.
6. Двойной клик ещё раз → восстановить исходную сумму → ОК → Записать.

Если форма не открывается / открывается типовая БСП-форма — диагностировать через grep по log/Comments или через Edit-исправление.

- [ ] **Step 3: Knowledge update — файл 13 (опционально)**

Создать `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/13_vkassu_podforma_rashifrovka.md` с кратким описанием:
- Зачем (типовая не редактирует А_Расшифровку)
- Что (локальная форма А_ФормаСпискаРасшифровкиПоФЛ)
- Уроки (UUID нужен новый, паттерн ОписаниеОповещения, удалить-добавить вместо update-in-place)
- Cross-ref к spec и plan

Commit:
```bash
git add "_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/13_vkassu_podforma_rashifrovka.md"
git commit -m "docs(knowledge): файл 13 — подформа редактирования А_Расшифровки в ВКассу

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

- [ ] **Step 4: Финал**

Сообщить пользователю что работа готова, тесты PASS. Жду «push» от пользователя для отправки в `origin/main`.

---

## Self-Review (выполнено)

**1. Spec coverage:**
- §2 «Архитектурное решение» → Task 2 (создание формы по паттерну)
- §3 «Новая форма» → Task 2 (XML + Module)
- §4 «Изменения в ФормаДокумента» → Task 4 (переопределение + новые процедуры)
- §5 «Поведение» → Task 7 (smoke возврата) + Task 8 (ручная UI)
- §6 «Out-of-scope» → не реализуется
- §7 «Тестирование» → Task 6 + Task 7
- §8 «Acceptance критерии» → каждый пункт покрыт соответствующим тестом или UI-проверкой

**2. Placeholder scan:** все шаги содержат конкретный код. Псевдокода нет. UUID указан как «подставить в Task 1 Step 4» — это шаг плана, не placeholder.

**3. Type consistency:**
- Имя процедуры `А_ОткрытьРасшифровкуДляФЛ` — везде одинаковое (Task 4)
- Имя формы `А_ФормаСпискаРасшифровкиПоФЛ` — везде одинаковое (Task 2-6)
- Реквизит формы `А_РасшифровкаВыплатыЗарплатаПоФизлицам` — везде одинаковый
- Структура результата `Модифицированность`/`ФизическоеЛицо`/`АдресВХранилище` — единая

Плану — зелёный свет.

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-27-vkassu-podforma-rashifrovka-plan.md`. Two execution options:**

**1. Subagent-Driven (recommended)** — диспатчим свежего subagent на каждую задачу, ревью между задачами, быстрая итерация. **REQUIRED SUB-SKILL:** `superpowers:subagent-driven-development`.

**2. Inline Execution** — выполняем задачи в этой сессии через `superpowers:executing-plans`, batch с чекпоинтами для ревью.

**Which approach?**
