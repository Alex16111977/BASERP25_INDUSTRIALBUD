# Перенос управленческой ЗП (Форма2) на корень дерева — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Расширить кнопку «Создать передачу начислений» так, чтобы управленческая ЗП (`ФормаPL=Форма2`) переносилась с подразделения начисления на КОРЕНЬ его дерева `СтруктураПредприятия`, движениями ПАП + А_ВзСС (net-zero).

**Architecture:** Симметрия с Формой1. Источник — те же строки `НачисленнаяЗарплатаИВзносыПоФизлицам` нашего документа, фильтр `ФормаPL=Форма2`. Отличие — целевое подразделение = `_ПНМП_КореньСтрП(ПодразделениеПредприятия)` (подъём по `.Родитель`). Документ-приёмник и его проведение НЕ меняются (ПАП без измерения ФормаPL). В `А_ВзСС` хардкод `Форма1` заменяется чтением `ФормаPL` из строки ТЧ.

**Tech Stack:** 1С BSL (8.3.20+), Python COM (pywin32, `V83.COMConnector`), MCP `1c-workerp`, навыки `/db-load-xml` + `/db-update`. RED = Python COM тест (Rule #-1), GREEN = BSL.

**Спека:** `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/PROMPT_ПередачаНачисленийФорма2_Корень.md`

---

## File Structure

| Файл | Ответственность | Действие |
|---|---|---|
| `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` (область `ЕРП_СоздатьПередачуНачисленийМеждуПодразделениями`, ≈3740-3906) | Заполнение ТЧ передачи; + Ф2-ветка на корень | Modify: +4 процедуры, +1 вызов |
| `AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl` · `ЗаполнитьИзПередачиНачислений` (≈397-478) | Движения А_ВзСС из передачи | Modify: запрос (+ФормаPL) + строка записи |
| `Documents/А_ПередачаНачисленийМеждуПодразделениями/Ext/ObjectModule.bsl` · `ОбработкаПроведения` | Движения ПАП+А_ВзСС | **NO CHANGE** (ПАП без ФормаPL) |
| `…/knowledge_А_ОтражениеЗПпоКазне/Python/test/discovery_peredacha_forma2.py` | RED discovery | DONE |
| `…/knowledge_А_ОтражениеЗПпоКазне/Python/test/smoke_peredacha_forma2.py` | GREEN приёмка | Create |

**Рабочий каталог = основная конфигурация** `C:\Configuration_downloads\BASERP25` (не worktree) → копировать не нужно (Rule #4 выполнен правкой на месте); требуется загрузка в БД (Задача 6).

---

## Эталон + контрольные числа (из discovery, №000000005 от 30.04.2026)

| Метрика | Значение |
|---|---|
| Ф2 строк-кандидатов (ФормаPL=Ф2, ВзносыВсего=0, не удержание, Сумма≠0) | **312** |
| Σ Ф2 кандидатов | **3 976 060,04** |
| no-op (корень = исходному: ЦО/Производство/Спецтехника/Строительство/Логистика) | **109 строк, Σ 1 501 897,72** |
| **Σ Ф2_перенос (корень ≠ исходному)** | **2 474 162,32** |
| Новых Ф2-строк в ТЧ Начисления после кнопки | **203** (= 312 − 109) |
| Без ПодразделениеПредприятия | 0 |

Карта корней (выборка): `Глобино-2`→`Строительство`, `Кернел Кухня`→`Производство`, `Автокран XCMG`→`Спецтехника`, `MAN №4`→`Логистика`, `КГ "Подгорцы"`→`Девелопмент`.

---

## Task 1: Discovery (RED) — DONE

**Files:** `…/Python/test/discovery_peredacha_forma2.py`

- [x] Прогнан; числа выше зафиксированы. Σ Ф2 = no-op + перенос (1 501 897,72 + 2 474 162,32 = 3 976 060,04 ✓).

---

## Task 2: BSL — хелперы «корня» `_ПНМП_КореньСтрП` + `_ПНМП_КореньПоИмени`

**Files:** Modify `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` (в конец области `ЕРП_СоздатьПередачуНачисленийМеждуПодразделениями`, перед `#КонецОбласти` ≈3905)

- [ ] **Step 1 (RED): протестировать запрос `_ПНМП_КореньПоИмени` через COM**

Run (python-runner): инлайн-проверка, что корень «Строительство» резолвится:
```python
q = erp.NewObject("Запрос")
q.Text = ("ВЫБРАТЬ ПЕРВЫЕ 1 СП.Ссылка КАК Ссылка ИЗ Справочник.СтруктураПредприятия КАК СП "
          "ГДЕ СП.Наименование = &Имя И СП.Родитель = ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)")
q.УстановитьПараметр("Имя", "Строительство")
print("Пусто:", q.Выполнить().Пустой())  # Ожидается: False
```
Expected: `Пусто: False`

- [ ] **Step 2 (GREEN): добавить обе функции**

```bsl
// Корінь дерева СтруктураПредприятия: підйом по .Родитель до верхнього рівня.
Функция _ПНМП_КореньСтрП(СтрП)
	Тек = СтрП;
	Пока ЗначениеЗаполнено(Тек) И ЗначениеЗаполнено(Тек.Родитель) Цикл
		Тек = Тек.Родитель;
	КонецЦикла;
	Возврат Тек;
КонецФункции

// Фоллбек: корінь (Родитель порожній) за точним найменуванням.
Функция _ПНМП_КореньПоИмени(Имя)
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Имя", Имя);
	Запрос.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1 СП.Ссылка КАК Ссылка
	|ИЗ Справочник.СтруктураПредприятия КАК СП
	|ГДЕ СП.Наименование = &Имя
	|	И СП.Родитель = ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)";
	Рез = Запрос.Выполнить();
	Если Рез.Пустой() Тогда
		Возврат Справочники.СтруктураПредприятия.ПустаяСсылка();
	КонецЕсли;
	Выборка = Рез.Выбрать(); Выборка.Следующий();
	Возврат Выборка.Ссылка;
КонецФункции
```

- [ ] **Step 3: Commit** (вместе с Task 3-4 одним коммитом — см. Task 4 Step 4).

---

## Task 3: BSL — Ф2-филлер `_ПНМП_ЗаполнитьНачисленияФорма2` + `_ПНМП_ДобавитьСтрокуКорень`

**Files:** Modify `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` (рядом с `_ПНМП_ЗаполнитьНачисления` ≈3863)

- [ ] **Step 1: добавить обе процедуры**

> ⚠️ Поле исходного подразделения — скопировать ТОЧНЫЙ токен из соседней рабочей строки `_ПНМП_ЗаполнитьНачисления` (≈3871): `Стр.ПодразделениеПредприятия`.

```bsl
// ТЧ Начисления (Форма2) ← «Зарплата управл» Ф2 (take-home NET управлінської частини)
// напряму з НачисленнаяЗарплатаИВзносыПоФизлицам. Ціль = корінь дерева підрозділу нарахування
// (управлінська ЗП «спливає» на верхній рівень). ФормаPL = Форма2.
Процедура _ПНМП_ЗаполнитьНачисленияФорма2(Док, СотрудникПоФЛ)
	Ф2 = Перечисления.А_ФормыPL.Форма2;
	Для Каждого Стр Из НачисленнаяЗарплатаИВзносыПоФизлицам Цикл
		Если Стр.ФормаPL <> Ф2 ИЛИ Стр.Сумма = 0 ИЛИ Стр.ВзносыВсего <> 0 Тогда Продолжить; КонецЕсли;
		Способ = Стр.СпособОтраженияЗарплатыВБухучете;
		Если ЗначениеЗаполнено(Способ) И Способ.А_ЭтоУдержание Тогда Продолжить; КонецЕсли;
		ОргБух = ?(ЗначениеЗаполнено(Стр.ОрганизацияБухгалтерия), Стр.ОрганизацияБухгалтерия, Организация);
		_ПНМП_ДобавитьСтрокуКорень(Док.Начисления, Стр.ФизическоеЛицо,
			СотрудникПоФЛ[Стр.ФизическоеЛицо], ОргБух, Стр.ПодразделениеПредприятия, Стр.Сумма);
	КонецЦикла;
КонецПроцедуры

// Додає рядок Начисления з ЦІЛЬОВИМ = корінь дерева підрозділу нарахування. ФормаPL = Форма2.
// Перенос пропускається, якщо корінь не знайдено або корінь = вихідному (no-op).
Процедура _ПНМП_ДобавитьСтрокуКорень(ТЧ, ФЛ, Сотрудник, ОргБух, ИсхПодр, Сумма)
	Если Сумма = 0 ИЛИ НЕ ЗначениеЗаполнено(ФЛ) ИЛИ НЕ ЗначениеЗаполнено(ИсхПодр) Тогда
		Возврат;
	КонецЕсли;
	ЦелПодр = _ПНМП_КореньСтрП(ИсхПодр);
	Если НЕ ЗначениеЗаполнено(ЦелПодр) Тогда
		ЦелПодр = _ПНМП_КореньПоИмени("Строительство");
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(ЦелПодр) ИЛИ ЦелПодр = ИсхПодр Тогда
		Возврат;
	КонецЕсли;
	Стр = ТЧ.Добавить();
	Стр.ФизическоеЛицо         = ФЛ;
	Стр.Сотрудник              = Сотрудник;
	Стр.ОрганизацияБухгалтерия = ОргБух;
	Стр.ИсходноеПодразделение  = ИсхПодр;
	Стр.ЦелевоеПодразделение   = ЦелПодр;
	Стр.ФормаPL                = Перечисления.А_ФормыPL.Форма2;
	Стр.Сумма                  = Сумма;
	Стр.ПериодВзаиморасчетов   = НачалоМесяца(Дата);
	Стр.ДокОснованиеСтроки     = Ссылка;
	Стр.ИдентификаторСтроки    = Строка(Новый УникальныйИдентификатор());
КонецПроцедуры
```

---

## Task 4: BSL — вызов Ф2-филлера в `СоздатьПередачуНачисленийМеждуПодразделениями`

**Files:** Modify `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl:3769`

- [ ] **Step 1: добавить 4-й вызов после трёх Ф1-филлеров**

Было (≈3767-3769):
```bsl
	_ПНМП_ЗаполнитьНачисления(Док, СотрудникПоФЛ);
	_ПНМП_ЗаполнитьНалоги(Док, СотрудникПоФЛ, ОргБухПоФЛ);
	_ПНМП_ЗаполнитьУдержания(Док, СотрудникПоФЛ, ОргБухПоФЛ);
```
Стало:
```bsl
	_ПНМП_ЗаполнитьНачисления(Док, СотрудникПоФЛ);
	_ПНМП_ЗаполнитьНалоги(Док, СотрудникПоФЛ, ОргБухПоФЛ);
	_ПНМП_ЗаполнитьУдержания(Док, СотрудникПоФЛ, ОргБухПоФЛ);
	_ПНМП_ЗаполнитьНачисленияФорма2(Док, СотрудникПоФЛ);   // управленческая ЗП → корень
```

- [ ] **Step 2: 1c-bsl-review правок ObjectModule** (опц., быстрый чек на антипаттерны).

- [ ] **Step 3: Commit Tasks 2-4**

```bash
git add "Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl"
git commit -m "feat(ПНМП): перенос Форма2 управл.ЗП на корень дерева подразделения"
```

---

## Task 5: BSL — `ЗаполнитьИзПередачиНачислений` читает `ФормаPL`

**Files:** Modify `AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl` (`ЗаполнитьИзПередачиНачислений`, запрос ≈410-459 + запись ≈469)

- [ ] **Step 1 (RED): протестировать модифицированный запрос через COM на существующей передаче**

Запрос (с `ФормаPL`) — выполнить через python-runner на ссылке текущей передачи (Find по А_ДокументОснование = №000000005), убедиться, что не падает и отдаёт колонку ФормаPL. Текст — как в Step 2 ниже.
Expected: выборка без ошибки, есть колонка `ФормаPL` со значениями `Форма1` (пока Ф2-строк ещё нет).

- [ ] **Step 2 (GREEN): заменить текст запроса**

Найти присвоение `Запрос.Текст = "…"` в `ЗаполнитьИзПередачиНачислений` и заменить весь текст на:
```bsl
	"ВЫБРАТЬ
	|	Н.ИсходноеПодразделение КАК Подразделение,
	|	Н.ФизическоеЛицо КАК ФизическоеЛицо,
	|	-Н.Сумма КАК Знач,
	|	Н.ОрганизацияБухгалтерия КАК ОрганизацияБухгалтерия,
	|	Н.ФормаPL КАК ФормаPL
	|ПОМЕСТИТЬ втСтроки
	|ИЗ
	|	Документ.А_ПередачаНачисленийМеждуПодразделениями.Начисления КАК Н
	|ГДЕ
	|	Н.Ссылка = &Док
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	Н.ЦелевоеПодразделение,
	|	Н.ФизическоеЛицо,
	|	Н.Сумма,
	|	Н.ОрганизацияБухгалтерия,
	|	Н.ФормаPL
	|ИЗ
	|	Документ.А_ПередачаНачисленийМеждуПодразделениями.Начисления КАК Н
	|ГДЕ
	|	Н.Ссылка = &Док
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	&Орг КАК Организация,
	|	С.Подразделение КАК Подразделение,
	|	С.ФизическоеЛицо КАК ФизическоеЛицо,
	|	ВЫБОР
	|		КОГДА СУММА(С.Знач) > 0
	|			ТОГДА ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
	|		ИНАЧЕ ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход)
	|	КОНЕЦ КАК ВидДвижения,
	|	ВЫБОР
	|		КОГДА СУММА(С.Знач) > 0
	|			ТОГДА СУММА(С.Знач)
	|		ИНАЧЕ -СУММА(С.Знач)
	|	КОНЕЦ КАК СуммаВзаиморасчетов,
	|	С.ОрганизацияБухгалтерия КАК ОрганизацияБухгалтерия,
	|	С.ФормаPL КАК ФормаPL
	|ИЗ
	|	втСтроки КАК С
	|
	|СГРУППИРОВАТЬ ПО
	|	С.Подразделение,
	|	С.ФизическоеЛицо,
	|	С.ОрганизацияБухгалтерия,
	|	С.ФормаPL
	|
	|ИМЕЮЩИЕ
	|	СУММА(С.Знач) <> 0";
```

- [ ] **Step 3 (GREEN): заменить хардкод записи**

Было (≈469):
```bsl
		Запись.ФормаPL             = Перечисления.А_ФормыPL.Форма1;
```
Стало:
```bsl
		Запись.ФормаPL             = СтрТЗ.ФормаPL;
```

- [ ] **Step 4: Commit**

```bash
git add "AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl"
git commit -m "feat(А_ВзСС): ЗаполнитьИзПередачиНачислений — ФормаPL из строки ТЧ (не хардкод Форма1)"
```

---

## Task 6: Загрузка изменений в БД

**Files:** обе модификации выше.

- [ ] **Step 1: загрузить XML в БД (Partial)**

Навык `/db-load-xml -Mode Partial` для двух файлов (auto `-updateConfigDumpInfo`):
- `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl`
- `AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl`

- [ ] **Step 2: обновить конфигурацию БД**

Навык `/db-update -Dynamic+` (серверная база BaseERP).
Expected: «Обновление успешно завершено», без ошибок синтаксиса BSL.

---

## Task 7: Приёмка (GREEN) — smoke на №000000005

**Files:** Create `…/knowledge_А_ОтражениеЗПпоКазне/Python/test/smoke_peredacha_forma2.py`

- [ ] **Step 1: написать smoke-тест**

```python
# -*- coding: utf-8 -*-
import win32com.client, sys
if sys.stdout.encoding != 'utf-8':
    sys.stdout.reconfigure(encoding='utf-8')
v8 = win32com.client.Dispatch("V83.COMConnector")
erp = v8.Connect('Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')

def найти_отражение(номер):
    q = erp.NewObject("Запрос")
    q.Text = "ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК Ссылка ИЗ Документ.А_ОтражениеЗПпоКазне ГДЕ Номер = &Н"
    q.УстановитьПараметр("Н", номер); s = q.Выполнить().Выбрать()
    return s.Ссылка if s.Следующий() else None

отр = найти_отражение("000000005")
отр.ПолучитьОбъект().СоздатьПередачуНачисленийМеждуПодразделениями()  # боевой путь кнопки

# найти созданный документ передачи по А_ДокументОснование
qp = erp.NewObject("Запрос")
qp.Text = ("ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК Ссылка ИЗ Документ.А_ПередачаНачисленийМеждуПодразделениями "
           "ГДЕ А_ДокументОснование = &Осн")
qp.УстановитьПараметр("Осн", отр); sp = qp.Выполнить().Выбрать(); sp.Следующий()
пер = sp.Ссылка
print("Передача:", пер.Номер, "Проведен =", пер.Проведен)

# 1) ТЧ Начисления: Ф2-строки
qn = erp.NewObject("Запрос")
qn.Text = ("ВЫБРАТЬ Н.ФормаPL КАК Ф, КОЛИЧЕСТВО(*) КАК К, СУММА(Н.Сумма) КАК С "
           "ИЗ Документ.А_ПередачаНачисленийМеждуПодразделениями.Начисления КАК Н "
           "ГДЕ Н.Ссылка = &Д СГРУППИРОВАТЬ ПО Н.ФормаPL")
qn.УстановитьПараметр("Д", пер); вн = qn.Выполнить().Выбрать()
print("ТЧ Начисления по ФормаPL:")
while вн.Следующий():
    print(f"  {erp.XMLСтрока(вн.Ф)}: {вн.К} строк, Σ {round(float(вн.С),2)}")

# 2) ПАП net-zero по статье Оплата труда от этого документа
qpap = erp.NewObject("Запрос")
qpap.Text = ("ВЫБРАТЬ СУММА(ВЫБОР КОГДА П.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) "
             "ТОГДА П.Сумма ИНАЧЕ -П.Сумма КОНЕЦ) КАК Нетто "
             "ИЗ РегистрНакопления.ПрочиеАктивыПассивы КАК П "
             "ГДЕ П.Регистратор = &Д И П.Статья.Наименование = &Ст")
qpap.УстановитьПараметр("Д", пер); qpap.УстановитьПараметр("Ст", "Оплата труда")
r = qpap.Выполнить().Выбрать(); r.Следующий()
print("ПАП(Оплата труда) net (ожид. ~0):", round(float(r.Нетто or 0), 2))

# 3) А_ВзСС: суммы по ФормаPL
qv = erp.NewObject("Запрос")
qv.Text = ("ВЫБРАТЬ В.ФормаPL КАК Ф, СУММА(ВЫБОР КОГДА В.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход) "
           "ТОГДА В.СуммаВзаиморасчетов ИНАЧЕ -В.СуммаВзаиморасчетов КОНЕЦ) КАК Нетто "
           "ИЗ РегистрНакопления.А_ВзаиморасчетыССотрудниками КАК В "
           "ГДЕ В.Регистратор = &Д СГРУППИРОВАТЬ ПО В.ФормаPL")
qv.УстановитьПараметр("Д", пер); вв = qv.Выполнить().Выбрать()
print("А_ВзСС net по ФормаPL (ожид. ~0 каждая):")
while вв.Следующий():
    print(f"  {erp.XMLСтрока(вв.Ф)}: {round(float(вв.Нетто or 0),2)}")
```

- [ ] **Step 2: прогнать; проверить инварианты**

Run: `C:\Python313\python.exe …\smoke_peredacha_forma2.py`
Expected:
- ТЧ Начисления: `Форма2: 203 строк, Σ 2 474 162,32` (+ Форма1 без изменений) — допуск ±0.05 по округлению;
- ПАП(Оплата труда) net ≈ 0;
- А_ВзСС net по Форма1 ≈ 0 и по Форма2 ≈ 0 (зеркало);
- Передача `Проведен = True`.

- [ ] **Step 3: идемпотентность** — повторно вызвать `СоздатьПередачуНачисленийМеждуПодразделениями()`, перечитать → те же 203 Ф2-строки, Σ 2 474 162,32 (Δ=0).

- [ ] **Step 4: Commit smoke**

```bash
git add "_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/discovery_peredacha_forma2.py" "_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/smoke_peredacha_forma2.py"
git commit -m "test(ПНМП Ф2): discovery + smoke — Σ перенос 2 474 162,32 на корни, net-zero ПАП/А_ВзСС"
```

---

## Task 8: Обновить knowledge + финал

- [ ] **Step 1:** дописать в [16_peredacha_nachisleniy.md](../../_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/16_peredacha_nachisleniy.md) короткий раздел «Форма2 → корень» (статус РЕАЛИЗОВАНО, числа эталона, ссылка на промт/план).
- [ ] **Step 2:** обновить память (`MEMORY.md` + файл) — ПНМП Ф2 на корень DONE.
- [ ] **Step 3:** финал ветки автономно (merge/push/PR) — по `feedback_autonomous_finish_merge_push_pr`.

---

## Self-Review (заполняется при написании плана)

- **Spec coverage:** §5.1→T2, §5.2/§5.3→T3-4, §5.4→T5, §5.5→нет правок (зафиксировано), §7 discovery→T1, §9 приёмка→T7. ✅
- **No placeholders:** числа эталона реальные (T1); BSL полный; запросы полные. ✅
- **Type consistency:** `_ПНМП_КореньСтрП`/`_ПНМП_КореньПоИмени`/`_ПНМП_ДобавитьСтрокуКорень`/`_ПНМП_ЗаполнитьНачисленияФорма2` — имена едины в T2/T3/T4. ✅
