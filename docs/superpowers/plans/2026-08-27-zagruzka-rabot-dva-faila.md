# План: обработка «Загрузка работ в СС по двум файлам в одну СС»

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Внешняя обработка BaseERP: загрузка работ из N Excel-файлов (лист «Проект СС») в одну `А_СтруктураСебестоимости` с номером Модуля на файл; перегружаются только модули из таблицы.

**Architecture:** Тонкая форма (клиент читает Excel через COM, таблица файлов = ТЧ обработки) + движок в `Ext/ObjectModule.bsl` (транзакция, удаление строк загружаемых модулей, get-or-create этапов/работ, контроль итогов в разрезе модуля с откатом). База — движок «Загрузка работ в СС для базы ЕРП».

**Tech Stack:** 1С 8.3 BSL, XML-исходники EPF, Python COM (win32com + openpyxl) для тестов, epf-build (BaseERPRazr при занятом конфигураторе).

**Спека:** `docs/superpowers/specs/2026-08-27-zagruzka-rabot-dva-faila-design.md`

**Рабочий каталог обработки (main, untracked):**
`C:\Configuration_downloads\BASERP25\_Rarzrabotki\Загрузка СС\` — далее `$СС`.

**Эталоны (из инспекции файлов):** S1 → 44 строки, ΣКол 1336.99, ΣСумма 328 530.00; S3 → 44 строки, ΣКол 1336.99, ΣСумма 326 230.00.

**GUID (сгенерированы, использовать ровно эти):**

| Назначение | GUID |
|---|---|
| ExternalDataProcessor uuid | `b52d93c8-ae37-4b17-b704-53f300e0109b` |
| ObjectId | `705e41f8-d3d9-462e-840d-403ba6aa458d` |
| GeneratedType Object TypeId | `cc8397c1-6d53-4b36-864e-abe59c8bb224` |
| GeneratedType Object ValueId | `35ca7d99-6d36-4fd7-b272-32fe0d21ded6` |
| Attribute ДокСтруктураСебестоимости | `c27fb63f-e234-498b-b1e4-4496033ab08c` |
| Form uuid (Forms/Форма.xml) | `66c19866-d62c-4207-8313-f964ad618a27` |
| TabularSection Файлы uuid | `f9203d18-94a3-4d1a-8757-4d6344d27658` |
| TS TypeId | `3cc977df-d731-4de9-a6e0-7446b3a0aa54` |
| TS ValueId | `af15c6d2-d0d2-4fb0-b412-5f93dd15efa3` |
| TSRow TypeId | `38073d93-929a-48fb-bec9-055f9af5b046` |
| TSRow ValueId | `40109760-72f2-4b9a-987f-ca1d1f70fa42` |
| ТЧ Attribute ИмяФайла | `922a89f4-9273-4c33-8388-c67a3fa771ab` |
| ТЧ Attribute Модуль | `9d76298d-461f-40ad-8f70-e1e89ed68569` |

ClassId `c3831ec8-d8d5-4f93-8a22-f9bfae07327f` НЕ трогать (общий для всех внешних обработок этого типа).

---

### Task 1: Python COM тест запросов контроля по модулям (Rule #-1)

**Files:**
- Create: `_Rarzrabotki/Python/test/test_zagruzka_rabot_dva_faila_queries.py`

- [ ] **Step 1.1: Написать тест запросов** — три запроса движка 1:1 как будут в BSL, параметр `Модули` — Массив чисел:

```python
# -*- coding: utf-8 -*-
"""RED (Rule #-1): запросы движка «Загрузка работ в СС по двум файлам» на живой BaseERP."""
import sys
import win32com.client

if sys.stdout.encoding != 'utf-8':
    sys.stdout.reconfigure(encoding='utf-8')

v8 = win32com.client.Dispatch("V83.COMConnector")
erp = v8.Connect('Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')

# Любая существующая СС для подстановки параметра
q = erp.NewObject("Запрос")
q.Text = "ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка ИЗ Справочник.А_СтруктураСебестоимости ГДЕ НЕ ЭтоГруппа"
sel = q.Execute().Выбрать()
assert sel.Следующий(), "нет ни одной СС"
ss_ref = sel.Ссылка

modules = erp.NewObject("Массив")
modules.Добавить(1)
modules.Добавить(3)

QUERIES = {
    "итоги по модулям": """
ВЫБРАТЬ
	Р.Модуль КАК Модуль,
	КОЛИЧЕСТВО(*) КАК Строк,
	СУММА(Р.Количество) КАК СуммаКоличество,
	СУММА(Р.Сумма) КАК СуммаСумма
ИЗ
	Справочник.А_СтруктураСебестоимости.Работы КАК Р
ГДЕ
	Р.Ссылка = &Структура
	И Р.Модуль В (&Модули)
СГРУППИРОВАТЬ ПО
	Р.Модуль
""",
    "итоги по модулям и этапам": """
ВЫБРАТЬ
	Р.Модуль КАК Модуль,
	Р.Этап КАК Этап,
	СУММА(Р.Сумма) КАК СуммаСумма
ИЗ
	Справочник.А_СтруктураСебестоимости.Работы КАК Р
ГДЕ
	Р.Ссылка = &Структура
	И Р.Модуль В (&Модули)
СГРУППИРОВАТЬ ПО
	Р.Модуль,
	Р.Этап
""",
    "всего строк ТЧ": """
ВЫБРАТЬ
	КОЛИЧЕСТВО(*) КАК ВсегоСтрок
ИЗ
	Справочник.А_СтруктураСебестоимости.Работы КАК Р
ГДЕ
	Р.Ссылка = &Структура
""",
    "инфо-срез": """
ВЫБРАТЬ
	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ Р.Работа) КАК УникальныхРабот,
	СУММА(ВЫБОР КОГДА Р.Этап = ЗНАЧЕНИЕ(Справочник.А_ЭтапыРабот.ПустаяСсылка) ТОГДА 1 ИНАЧЕ 0 КОНЕЦ) КАК БезЭтапа,
	СУММА(ВЫБОР КОГДА Р.Работа = ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка) ТОГДА 1 ИНАЧЕ 0 КОНЕЦ) КАК БезРаботы,
	СУММА(ВЫБОР КОГДА Р.Единица = ЗНАЧЕНИЕ(Справочник.УпаковкиЕдиницыИзмерения.ПустаяСсылка) ТОГДА 1 ИНАЧЕ 0 КОНЕЦ) КАК БезЕдиницы
ИЗ
	Справочник.А_СтруктураСебестоимости.Работы КАК Р
ГДЕ
	Р.Ссылка = &Структура
""",
}

fails = 0
for name, text in QUERIES.items():
    q = erp.NewObject("Запрос")
    q.Text = text
    q.SetParameter("Структура", ss_ref)
    if "&Модули" in text:
        q.SetParameter("Модули", modules)
    try:
        r = q.Execute().Выгрузить()
        print(f"OK  [{name}] строк={r.Количество()}")
    except Exception as e:
        fails += 1
        msg = e.excepinfo[2] if hasattr(e, 'excepinfo') and e.excepinfo else str(e)
        print(f"FAIL [{name}]: {msg}")

print("ИТОГ:", "OK" if fails == 0 else f"FAIL x{fails}")
sys.exit(0 if fails == 0 else 1)
```

- [ ] **Step 1.2: Запустить** — `python _Rarzrabotki/Python/test/test_zagruzka_rabot_dva_faila_queries.py` (через mcp python-runner). Ожидание: 4×OK, «ИТОГ: OK». При FAIL — чинить текст запроса ЗДЕСЬ, в BSL переносить только зелёный вариант.

---

### Task 2: Метаданные — корневой XML + Forms/Форма.xml

**Files:**
- Modify: `$СС\Загрузка работ в СС по двум файлам в одну СС.xml` (полная замена содержимого)
- Modify: `$СС\Загрузка работ в СС по двум файлам в одну СС\Forms\Форма.xml` (uuid)

- [ ] **Step 2.1: Переписать корневой XML целиком.** Имя `ЗагрузкаРаботВССПоДвумФайлам`, синоним «Загрузка работ в СС по двум файлам в одну СС», реквизит `ИмяФайла` удалён, добавлена ТЧ `Файлы` (ИмяФайла Строка-переменная, Модуль Число 10.0), все GUID из таблицы выше, ClassId прежний. Шапка `<?xml ...?><MetaDataObject ...>` — скопировать без изменений из текущего файла (namespaces идентичны). Содержимое `ExternalDataProcessor`:
  - `<ExternalDataProcessor uuid="b52d93c8-...">`, InternalInfo: ClassId прежний, ObjectId `705e41f8-...`, GeneratedType `ExternalDataProcessorObject.ЗагрузкаРаботВССПоДвумФайлам` TypeId `cc8397c1-...` / ValueId `35ca7d99-...`.
  - Properties: Name/Synonym новые, `DefaultForm) ExternalDataProcessor.ЗагрузкаРаботВССПоДвумФайлам.Form.Форма`.
  - ChildObjects: Attribute `ДокСтруктураСебестоимости` (uuid `c27fb63f-...`, тип `cfg:CatalogRef.А_СтруктураСебестоимости`, остальные свойства — как в текущем файле), затем TabularSection по образцу из `_Rarzrabotki/Обработки/Загрузка выполнения работ.xml` (блок `<TabularSection>` со `StandardAttributes/LineNumber`), с GeneratedType-именами `ExternalDataProcessorTabularSection.ЗагрузкаРаботВССПоДвумФайлам.Файлы` / `ExternalDataProcessorTabularSectionRow.ЗагрузкаРаботВССПоДвумФайлам.Файлы`; атрибуты ТЧ:
    - `ИмяФайла`: `xs:string`, `StringQualifiers Length 0 / Variable`, синоним «Имя файла»;
    - `Модуль`: `xs:decimal`, `NumberQualifiers Digits 10 / FractionDigits 0 / AllowedSign Any`, `FillValue xsi:type="xs:decimal">0`, синоним «Модуль».
  - `<Form>Форма</Form>` в конце ChildObjects.
- [ ] **Step 2.2: Forms/Форма.xml** — заменить uuid формы на `66c19866-d62c-4207-8313-f964ad618a27` (остальное без изменений).
- [ ] **Step 2.3: Контроль** — навык `epf-validate` по корневому XML; вручную diff-проверка: ClassId не изменился, `ВнешняяОбработка1` нигде не встречается (`grep -r "ВнешняяОбработка1" "$СС\Загрузка работ в СС по двум файлам в одну СС"*`).

---

### Task 3: Движок — Ext/ObjectModule.bsl

**Files:**
- Modify: `$СС\Загрузка работ в СС по двум файлам в одну СС\Ext\ObjectModule.bsl` (полная замена)

- [ ] **Step 3.1: Записать модуль.** База — ObjectModule «Загрузка работ в СС для базы ЕРП» с изменениями:
  1. Протокол: `+СохраненоЧужихСтрок`, `Контроль` строится в разрезе модуля.
  2. Перед циклом добавления: множество модулей, частичное удаление вместо `Очистить()`:

```bsl
	// Множество загружаемых модулей: их строки перезаписываются, чужие модули не трогаем.
	МодулиЗагрузки = Новый Соответствие;
	МассивМодулей = Новый Массив;
	Для Каждого СтрокаДанных Из МассивСтрок Цикл
		МодульСтроки = ПолучитьЧисло(СтрокаДанных, "Модуль");
		Если МодулиЗагрузки.Получить(МодульСтроки) = Неопределено Тогда
			МодулиЗагрузки.Вставить(МодульСтроки, Истина);
			МассивМодулей.Добавить(МодульСтроки);
		КонецЕсли;
	КонецЦикла;
```

внутри транзакции:

```bsl
		ОбъектСеб = СтруктураСеб.ПолучитьОбъект();
		Индекс = ОбъектСеб.Работы.Количество() - 1;
		Пока Индекс >= 0 Цикл
			Если МодулиЗагрузки.Получить(ОбъектСеб.Работы[Индекс].Модуль) <> Неопределено Тогда
				ОбъектСеб.Работы.Удалить(Индекс);
			КонецЕсли;
			Индекс = Индекс - 1;
		КонецЦикла;
		Результат.СохраненоЧужихСтрок = ОбъектСеб.Работы.Количество();
```

  3. `НоваяСтрока.Модуль = ПолучитьЧисло(СтрокаДанных, "Модуль");` при добавлении.
  4. `ИтогиExcel` — Соответствие Модуль → Структура{Строк, Количество, Сумма, ПоЭтапам:Соответствие}.
  5. `ПроверитьКонтрольныеИтоги(СтруктураСсылка, ИтогиExcel, МассивМодулей, СохраненоЧужих, Результат)` — запросы из Task 1: итоги по модулям (сверка Строк/ΣКол/ΣСумма на модуль, допуск 0.005), по модулям+этапам, всего строк ТЧ (= СохраненоЧужих + Загружено), инфо-срез. Расхождение → `ВызватьИсключение` → откат транзакции снаружи.
  6. Журнал регистрации: событие `ЗагрузкаРаботВССПоДвумФайлам.Ошибка`.
  Остальное (нормализация, кэши этапов/работ/единиц, get-or-create, `ПолучитьЧисло`/`ПолучитьСтроку`, `ПревышаетДопуск`) — 1:1 из ЕРП-версии.
- [ ] **Step 3.2: grep-чек парности** (память feedback_bsl_konec_funkcii_grep_check): число `Функция ` = число `КонецФункции`, `Процедура ` = `КонецПроцедуры`; поиск запретных имён локальных переменных `Ссылка`, `Выполнить` — не должно быть.

---

### Task 4: Форма — Form.xml + Module.bsl

**Files:**
- Modify: `$СС\...\Forms\Форма\Ext\Form.xml` (полная замена)
- Modify: `$СС\...\Forms\Форма\Ext\Form\Module.bsl` (полная замена)

- [ ] **Step 4.1: Form.xml.** Элементы: InputField `ДокСтруктураСебестоимости` (DataPath `Объект.ДокСтруктураСебестоимости`); Table `Файлы` (DataPath `Объект.Файлы`) с колонками InputField `ФайлыИмяФайла` (DataPath `Объект.Файлы.ИмяФайла`, `<ChoiceButton>true</ChoiceButton>`, событие `<Event name="StartChoice">ФайлыИмяФайлаНачалоВыбора</Event>`, Width 60) и InputField `ФайлыМодуль` (DataPath `Объект.Файлы.Модуль`, Width 8); AutoCommandBar с кнопкой команды `ЗагрузитьРаботы`. Атрибут `Объект` — MainAttribute, тип `cfg:ExternalDataProcessorObject.ЗагрузкаРаботВССПоДвумФайлам`, `<Save><Field>Объект.ДокСтруктураСебестоимости</Field></Save>`. Все id уникальны, команда с `id="1"`, Action без xsi:type (правила XML из CLAUDE.md). Шаблон Table — из `Обработки/Загрузка выполнения работ/Forms/Форма/Ext/Form.xml` (без ReadOnly/ChangeRowSet-ограничений — таблица редактируемая).
- [ ] **Step 4.2: Module.bsl** — клиентские процедуры:

```bsl
&НаКлиенте
Процедура ЗагрузитьРаботы(Команда)

	Если НЕ ЗначениеЗаполнено(Объект.ДокСтруктураСебестоимости) Тогда
		Сообщить("Не выбрана структура себестоимости");
		Возврат;
	КонецЕсли;
	Если Объект.Файлы.Количество() = 0 Тогда
		Сообщить("Таблица файлов пуста - добавьте хотя бы одну строку");
		Возврат;
	КонецЕсли;

	ЗанятыеМодули = Новый Соответствие;
	НомерСтроки = 0;
	Для Каждого СтрокаФайла Из Объект.Файлы Цикл
		НомерСтроки = НомерСтроки + 1;
		Если ПустаяСтрока(СтрокаФайла.ИмяФайла) Тогда
			Сообщить("Строка " + НомерСтроки + ": не указан файл");
			Возврат;
		КонецЕсли;
		Если СтрокаФайла.Модуль <= 0 Тогда
			Сообщить("Строка " + НомерСтроки + ": не указан номер модуля (> 0)");
			Возврат;
		КонецЕсли;
		Если ЗанятыеМодули.Получить(СтрокаФайла.Модуль) <> Неопределено Тогда
			Сообщить("Строка " + НомерСтроки + ": модуль " + СтрокаФайла.Модуль + " указан дважды");
			Возврат;
		КонецЕсли;
		ЗанятыеМодули.Вставить(СтрокаФайла.Модуль, Истина);
	КонецЦикла;

	ОбщийМассив = Новый Массив;
	Для Каждого СтрокаФайла Из Объект.Файлы Цикл
		МассивСтрок = ПрочитатьExcelВМассив(СтрокаФайла.ИмяФайла, СтрокаФайла.Модуль);
		Если МассивСтрок = Неопределено Тогда
			Сообщить("Загрузка отменена: файл не прочитан - " + СтрокаФайла.ИмяФайла);
			Возврат;
		КонецЕсли;
		Для Каждого СтрокаДанных Из МассивСтрок Цикл
			ОбщийМассив.Добавить(СтрокаДанных);
		КонецЦикла;
	КонецЦикла;

	Протокол = ЗагрузитьНаСервере(ОбщийМассив);
	ВывестиПротокол(Протокол);

КонецПроцедуры
```

`ПрочитатьExcelВМассив(ИмяФайла, Модуль)` — 1:1 из ЕРП-версии + `Стр.Модуль = Модуль` (структура `"Этап, Работа, Единица, Количество, Цена, Сумма, Модуль"`). `НормИмяКлиент`, `ЧислоИзЯчейки` — без изменений. Выбор файла:

```bsl
&НаКлиенте
Процедура ФайлыИмяФайлаНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Открытие);
	Диалог.Фильтр = "Excel (*.xlsx)|*.xlsx|Excel 97-2003 (*.xls)|*.xls|Все файлы (*.*)|*.*";
	Диалог.МножественныйВыбор = Ложь;
	Диалог.Заголовок = "Выберите файл сметы";
	Диалог.Показать(Новый ОписаниеОповещения("ВыборФайлаЗавершение", ЭтотОбъект));
КонецПроцедуры

&НаКлиенте
Процедура ВыборФайлаЗавершение(ВыбранныеФайлы, ДополнительныеПараметры) Экспорт
	Если ВыбранныеФайлы = Неопределено ИЛИ ВыбранныеФайлы.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	ТекущиеДанные = Элементы.Файлы.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	ТекущиеДанные.ИмяФайла = ВыбранныеФайлы[0];
	Если ТекущиеДанные.Модуль = 0 Тогда
		ТекущиеДанные.Модуль = МодульИзИмениФайла(ВыбранныеФайлы[0]);
	КонецЕсли;
КонецПроцедуры

// Ищет в имени файла фрагмент S<цифры> (латиница или кириллица, без цифры слева) -> номер модуля.
&НаКлиенте
Функция МодульИзИмениФайла(Знач ПутьКФайлу)
	Части = СтрРазделить(СтрЗаменить(ПутьКФайлу, "/", "\"), "\");
	Имя = Части[Части.ВГраница()];
	Цифры = "0123456789";
	Для Позиция = 1 По СтрДлина(Имя) - 1 Цикл
		Буква = Сред(Имя, Позиция, 1);
		Если Буква <> "S" И Буква <> "s" И Буква <> "С" И Буква <> "с" Тогда
			Продолжить;
		КонецЕсли;
		Если Позиция > 1 И Найти(Цифры, Сред(Имя, Позиция - 1, 1)) > 0 Тогда
			Продолжить;
		КонецЕсли;
		НомерТекст = "";
		Сдвиг = Позиция + 1;
		Пока Сдвиг <= СтрДлина(Имя) И Найти(Цифры, Сред(Имя, Сдвиг, 1)) > 0 Цикл
			НомерТекст = НомерТекст + Сред(Имя, Сдвиг, 1);
			Сдвиг = Сдвиг + 1;
		КонецЦикла;
		Если НЕ ПустаяСтрока(НомерТекст) Тогда
			Возврат Число(НомерТекст);
		КонецЕсли;
	КонецЦикла;
	Возврат 0;
КонецФункции
```

`ЗагрузитьНаСервере` — как в ЕРП-версии. `ВывестиПротокол` — + строка «Сохранено строк других модулей: N».
- [ ] **Step 4.3: Контроль** — навык `form-validate` по форме; grep-чек парности процедур/функций модуля.

---

### Task 5: Сборка .epf

- [ ] **Step 5.1:** Навык `epf-build`: корневой XML → `$СС\Загрузка работ в СС по двум файлам в одну СС.epf`. База сборки: `Srvr="SQLSERVER";Ref="BaseERPRazr";Usr="Администратор";Pwd="24043"` (конфигуратор BaseERP занят — правило из CLAUDE.md). Убедиться: в каталоге исходников нет .bak/посторонних файлов (память erf_build_classid_and_stray_files).
- [ ] **Step 5.2:** Проверка: .epf существует, размер > 10 КБ, свежий mtime.

---

### Task 6: Smoke-тест (полный путь пользователя)

**Files:**
- Create: `_Rarzrabotki/Python/test/test_zagruzka_rabot_dva_faila.py`

- [ ] **Step 6.1: Написать smoke.** Фазы:
  1. `erp.ВнешниеОбработки.Создать(EPF, False)`;
  2. get-or-create СС `__ТЕСТ Загрузка работ 2 файла` (НЕ удалять — память feedback_no_doc_delete_in_tests);
  3. чтение S1/S3 через openpyxl с клиентским маппингом (лист «Проект СС», C=«Робота», граница «загальновиробничі витрати», E/G/I/J/K/M) → массивы структур 1С (`erp.NewObject("Структура")`, поля Этап/Работа/Единица/Количество/Цена/Сумма/Модуль);
  4. Прогон 1 (S1→М1 + S3→М3): `Ошибка=False`, `КонтрольПройден=True`, `Загружено=88`; запросом по ТЧ: М1 = 44 строки / ΣСумма 328530.00 / ΣКол 1336.99, М3 = 44 / 326230.00 / 1336.99;
  5. Прогон 2 (повтор): `СозданоРабот=0`, `СозданоЭтапов=0`, итоги те же (идемпотентность);
  6. Прогон 3 (только S3→М3): `СохраненоЧужихСтрок=44`, М1 нетронут (44/328530.00), М3 перезаписан;
  7. Итог `SMOKE OK` / exit 1.
  Допуски сравнения сумм: 0.01 (1С хранит 2 знака).
- [ ] **Step 6.2: Запустить.** Ожидание: `SMOKE OK`. При падении — systematic-debugging, чинить исходники → пересборка (Task 5) → повторный прогон.

---

### Task 7: Финализация

- [ ] **Step 7.1:** Самопроверка по чеклисту `/1c-bsl-review` (сам, без субагентов-ревьюеров — память feedback_controller_verifies_no_reviewer_subagents): транзакции парные, Попытка/Исключение с журналом, никакого `Ссылка`/`Выполнить` как локальных имён, клиент/сервер директивы корректны.
- [ ] **Step 7.2:** Убедиться, что все файлы обработки лежат в основном каталоге `C:\Configuration_downloads\BASERP25\_Rarzrabotki\Загрузка СС\` (они там и редактируются — Rule #4 соблюдён по построению; в git они не входят).
- [ ] **Step 7.3:** Коммит плана в ветку worktree; итоговый отчёт пользователю (эталоны, протокол smoke, путь к .epf).
