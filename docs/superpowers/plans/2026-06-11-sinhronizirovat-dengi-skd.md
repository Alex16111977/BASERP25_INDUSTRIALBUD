# СинхронизироватьДеньги v6 (СКД) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Верх формы = СКД-таблица отчёта `А_СравнитьОстаткиДенегЕРПсBASБухгалтерия` с расшифровкой по клику в анализ документов (Фаза 2), режим «По дням».

**Спека/промт:** `docs/superpowers/specs/2026-06-11-sinhronizirovat-dengi-skd-design.md` (одобрена).

**Источники:** отчёт `_Rarzrabotki/Отчеты/А_СравнитьОстаткиДенегЕРПсBASБухгалтерия` (ObjectModule: `ПолучитьОстаткиЕРП_Бух`, `ПолучитьОстаткиBuhBud`; Templates/ОсновнаяСхемаКомпоновкиДанных), обработка v5.

### Task 1: COM-тесты запросов «По дням» (Rule #-1)
- [ ] `test_sinhronizirovat_dengi_skd_queries.py`: Q9 = ERP `ОстаткиИОбороты(&Н,&К,День,,)`; Q10 = BuhBud `Хозрасчетный.ОстаткиИОбороты(&Н,&К,День,,Счет В ИЕРАРХИИ(СчетаВБанках),,)` с валютной развилкой (тексты 1:1 из отчёта, И:123-132 / И:199-230). Период 2026-06-01..07. Ожидание: OK строк>0 у обоих.
- [ ] Запуск GREEN → commit.

### Task 2: Шаблон СКД + описатель
- [ ] Копия `Templates/ОсновнаяСхемаКомпоновкиДанных.xml` (+ `…/Ext/Template.xml`) из отчёта в `СинхронизироватьДеньги/Templates/`, в descriptor-XML шаблона новый uuid.
- [ ] Описатель обработки: + Attribute `ПоДням` (Булево, после ПоказыватьТолькоРасхождения); удалить TabularSection `ТаблицаРасхождений` целиком (с GeneratedType); в ChildObjects перед `<Form>Форма</Form>` добавить `<Template>ОсновнаяСхемаКомпоновкиДанных</Template>`.
- [ ] ET.parse валидация, commit.

### Task 3: ObjectModule v6
- [ ] Удалить: `СравнитьОстатки`, `ПолучитьОстаткиЕРП`, `ПолучитьОстаткиBuhBud` (старую), `СвестиТаблицы`, `АнализироватьДокументы` (старый цикл по ТЧ).
- [ ] Вставить из отчёта 1:1: `ПолучитьОстаткиЕРП_Бух`, `ПолучитьОстаткиBuhBud` (отчётная версия, со ссылками).
- [ ] Новые экспортные:
  - `ПостроитьДанныеСравнения()` — 4 ТЗ для внешних наборов (по флагу ПоДням), COM-подключение внутри; Неопределено при ошибке подключения.
  - `АнализироватьДокументыПоСчетам(МассивСчетов, НачПериодаАнализ, КонПериодаАнализ)` — подключение, узел обмена, цикл `АнализДокументов_ПоСчету`, заполнение `ЕстьРасхождение`, возврат итога-строки. ТаблицаДокументов.Очистить() в начале.
  - `АнализироватьВсеРасхождения()` — свод 4 расхождений по счёту из таблиц сравнения (Соответствие счёт→суммы ЕРП/УПП), счета с |любое|>0.005 → `АнализироватьДокументыПоСчетам` за весь период.
- [ ] Параметризация: `АнализДокументов_ПоСчету(V83, БанковскийСчетСсылка, НачП, КонП, УзелОбмена)`; `ОбробитиДокументиЕРП(..., БанковскийСчетСсылка, ...)`; `ПроверитьДокументыBuhBud(..., БанковскийСчетСсылка, НачП, КонП, ...)` — все обращения к `СтрРасх.*` и реквизитам периода заменить параметрами.
- [ ] commit.

### Task 4: Форма v6
- [ ] Form.xml: удалить Table `ТаблицаРасхожденийФорма`, `ГруппаКнопкиРасхождений`, команды/кнопки `ВыбратьВсе`/`СнятьВыделение`; добавить CheckBoxField `ПоДням` в ГруппаПериод; добавить `SpreadsheetDocumentField` `РезультатСКД` (DataPath на реквизит формы, Event `DetailProcessing`); реквизиты формы: `РезультатСКД` (ТабличныйДокумент), `АдресРасшифровки` (Строка), `СчетОтбораДокументов` (CatalogRef.БанковскиеСчетаОрганизаций); кнопка+команда `АнализВсехРасхождений` в командную панель. Тип/теги SpreadsheetDocumentField — по образцу из корпуса (grep).
- [ ] Module.bsl: `СформироватьНаСервере` — `ПостроитьДанныеСравнения` + схема из `ПолучитьМакет`, `НастройкиПоУмолчанию`, параметры Период (СтандартныйПериод)/ПоДням, отбор по `ФильтрБанковскийСчет`, компоновка с `ДанныеРасшифровки` → `РезультатСКД`, адрес в `АдресРасшифровки`; `РезультатСКДОбработкаРасшифровки` — сервером достать `БанковскийСчетКасса`(+`ПериодДень`) из полей элемента расшифровки и родителей → анализ за период/день → `СчетОтбораДокументов` → фильтр; `АнализВсехРасхождений`; `ПрименитьФильтрДокументов` по `СчетОтбораДокументов`+`ЕстьРасхождение`; убрать обработчики верхней таблицы; УО верхней удалить (нижней — остаётся).
- [ ] commit.

### Task 5: Сборка и проверки
- [ ] Правка `test_sinhronizirovat_dengi_smoke_v3.py`/`_v4.py` под новый API: вместо СравнитьОстатки/ТаблицаРасхождений — `обр.АнализироватьДокументыПоСчетам(массив_из_счёта, НачП, КонП)` (COM-массив erp.NewObject("Массив")).
- [ ] Новый `test_sinhronizirovat_dengi_smoke_v6.py`: `ПостроитьДанныеСравнения()` строки>0 в ЕРП/УПП наборах; сверка расхождения кон.остатка по счёту ОТП с цифрой v5 (3 981 888,89 за 01.01-30.06 — пересчитать фактом, допуск на дрейф живой базы: просто >0 и печать).
- [ ] form-validate (известные ложные игнор), epf-build против BaseERP, прогнать все 3 смока GREEN.
- [ ] Копия в main-каталог, merge, push.

**Self-review:** покрытие спеки — Task1↔проверки, Task2-4↔архитектура 1-5, Task5↔критерии приёмки; типы согласованы (СчетОтбораДокументов = ссылка из расшифровки; периоды параметрами).
