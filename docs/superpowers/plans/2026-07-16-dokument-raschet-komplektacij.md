# Документ «РасчетКомплектаций» (BASEBuh / bas_industrialbud) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Новый непроводимый документ `РасчетКомплектаций` в конфигурации BAS Бухгалтерія — сохраняемое рабочее место списания по нормам СС (порт движка обработки СозданиеКомплектацийБух) + две новые ТЧ списания (за нормами read-only / понад норму редактируемая) + создание КомплектацияНоменклатуры/ПередачаМалоценныхАктивовВЭксплуатацию из сохранённых таблиц + 5 портированных печатных форм + новый «Акт на списання понаднормових матеріалів».

**Architecture:** Вариант A спеки — движок в модуле объекта документа (контекст `ЭтотОбъект`, имена реквизитов/ТЧ 1:1 с обработкой → минимальный дифф порта); тонкая форма (transform-порт Form.xml обработки + 2 новые закладки); 4 СКД-макета копируются как макеты документа с новыми UUID.

**Tech Stack:** XML-выгрузка 2.13 (`_Rarzrabotki/BASEBuh`), скилы meta-compile/meta-validate/form-validate/db-load-xml/db-update, Python COM (win32com, `Srvr="SQLSERVER";Ref="bas_industrialbud";Usr="cfo";Pwd="2442"`), python-runner (ТОЛЬКО `python "<полный путь>"` — §27).

**Спека:** `C:\Configuration_downloads\BASERP25\docs\superpowers\prompts\2026-07-16-dokument-raschet-komplektacij.md`

---

## Константы плана (пути)

| Метка | Путь |
|---|---|
| `CFG` | `C:\Configuration_downloads\BASERP25\_Rarzrabotki\BASEBuh` (свежий дамп 16.07 11:45 — правим ПРЯМО здесь, main-каталог) |
| `OBR` | `C:\Configuration_downloads\BASERP25\_Rarzrabotki\Обработки\Создания комплектаций для бух учета` (папка) |
| `OBR.xml` | тот же путь + `.xml` (манифест) |
| `TEST` | `C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test` |
| `SKILLS` | `C:\Configuration_downloads\BASERP25\.claude\skills` |

Рабочие Python-скрипты патчей: `TEST\doc_kompl_*.py`. Все пишут XML **UTF-8 без BOM**, BSL — **UTF-8 with BOM + CRLF** (канон 1С; сверять с существующими файлами дампа). Шапка Python-скриптов: `sys.stdout.reconfigure(encoding='utf-8')`.

## Ключевые грабли (усвоить ДО старта; полный список — LESSONS.md §1–53)

- **§7**: никакого `Соответствие` с ключом `Неопределено` (в движке уже исправлено — не регрессировать).
- **§14/§34**: `<v8:lang>ru</v8:lang>` без uk-сиблинга = пустые подписи в uk-интерфейсе. В метаданных конфигурации давать ru+uk пары.
- **§39**: UUID обёрток макетов — только НОВЫЕ (uuid4), перед загрузкой grep-проверка уникальности по всему CFG.
- **§40**: перед вставкой в Form.xml сканировать реальный max id.
- **§27**: python-runner — только полный путь скрипта.
- **§48**: Python `str(comRef)` = мусор; имена через `.Наименование`, строки через `buh.String(...)`; перечисления сравнивать через `buh.String(знач)`.
- **§49-аналог**: db-load-xml/db-update упадут, если открыт Конфигуратор bas_industrialbud — проверять `Get-CimInstance Win32_Process -Filter "Name='1cv8.exe'"`.
- Rule #-1: новые запросы (дубли, автоподбор остатка) — сперва Python COM.
- Rule #4: файлы правим в main-каталоге CFG (Designer/загрузка оттуда); скрипты и docs — коммитим в worktree-ветку.

---

### Task 0: Preflight

**Files:** нет изменений.

- [ ] **Step 0.1**: Проверить, что Конфигуратор bas_industrialbud закрыт:
```powershell
Get-CimInstance Win32_Process -Filter "Name='1cv8.exe'" | Select-Object -ExpandProperty CommandLine
```
Expected: нет строки `DESIGNER` с `bas_industrialbud`. Если есть — попросить пользователя закрыть, НЕ продолжать загрузку (правки файлов можно).

- [ ] **Step 0.2**: Проверить git-трекинг дампа: `cd C:\Configuration_downloads\BASERP25 && git check-ignore -v "_Rarzrabotki/BASEBuh/Configuration.xml" ; git ls-files "_Rarzrabotki/BASEBuh/Configuration.xml"`. Если дамп tracked — в конце (Task 11) скопировать изменённые файлы в worktree и закоммитить; если ignored/untracked — коммитим только docs+скрипты.

- [ ] **Step 0.3**: Зафиксировать эталон обработки: `grep -c "" "OBR\Ext\ObjectModule.bsl"` → 1186; засечь md5 файлов обработки (обработку НЕ меняем — регрессия должна быть нулевой):
```bash
md5sum "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Обработки/Создания комплектаций для бух учета.xml" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Обработки/Создания комплектаций для бух учета/Ext/ObjectModule.bsl" > "C:/Users/SUPPOR~1/.../scratchpad/obr_baseline.md5"
```
(точный scratchpad-путь из окружения сессии).

- [ ] **Step 0.4**: Проверить наличие БСП-модуля `Пользователи` в BuhBud: `ls "CFG\CommonModules" | grep -x "Пользователи"` и что у него есть `ТекущийПользователь` (`grep -l "Функция ТекущийПользователь" "CFG\CommonModules\Пользователи\Ext\Module.bsl"`). Если нет — в ОбработкаЗаполнения использовать `ПараметрыСеанса.ТекущийПользователь` (проверить `grep "ТекущийПользователь" "CFG\Ext\SessionParameters.xml"` — не факт, см. fallback в Task 4 Step 4.6).

### Task 1: Rule #-1 — Python COM тесты новых запросов (RED)

**Files:**
- Create: `TEST\doc_kompl_query_precheck.py`

- [ ] **Step 1.1**: Написать и прогнать тест двух НОВЫХ запросов на живой базе. Запрос дублей тестируем на структурно идентичном `Документ.КомплектацияНоменклатуры` (наш документ ещё не загружен — проверяем синтаксический паттерн: соединение с ТЧ + `В (&Массив)` + диапазон дат); запрос автоподбора остатка — целиком боевой.

```python
# -*- coding: utf-8 -*-
"""Rule #-1 pre-check двух новых запросов документа РасчетКомплектаций."""
import win32com.client, sys, datetime
if sys.stdout.encoding != 'utf-8':
    sys.stdout.reconfigure(encoding='utf-8')

v8 = win32com.client.Dispatch("V83.COMConnector")
buh = v8.Connect('Srvr="SQLSERVER";Ref="bas_industrialbud";Usr="cfo";Pwd="2442"')

# --- 1. Паттерн запроса дублей (на КомплектацияНоменклатуры: шапка+ТЧ Комплектующие) ---
q = buh.NewObject("Запрос")
q.Text = """
ВЫБРАТЬ РАЗЛИЧНЫЕ
	Док.Ссылка КАК Ссылка
ИЗ
	Документ.КомплектацияНоменклатуры КАК Док
		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.КомплектацияНоменклатуры.Комплектующие КАК Строки
		ПО Строки.Ссылка = Док.Ссылка
ГДЕ
	НЕ Док.ПометкаУдаления
	И Док.Ссылка <> &Ссылка
	И Док.Дата МЕЖДУ &НачалоДня И &КонецДня
	И Строки.Номенклатура В (&СписокЗначений)
"""
q.SetParameter("Ссылка", buh.Документы.КомплектацияНоменклатуры.ПустаяСсылка())
q.SetParameter("НачалоДня", datetime.datetime(2026, 1, 1, 0, 0, 0))
q.SetParameter("КонецДня", datetime.datetime(2026, 12, 31, 23, 59, 59))
arr = buh.NewObject("Массив")
q.SetParameter("СписокЗначений", arr)
try:
    r = q.Execute().Выгрузить()
    print(f"OK дубль-паттерн, рядків={r.Количество()}")
except Exception as e:
    print(f"FAIL дубль-паттерн: {e.excepinfo[2] if hasattr(e,'excepinfo') and e.excepinfo else e}")
    sys.exit(1)

# --- 2. Автоподбор остатка строки (боевой текст ДанныеОстаткаДляСтроки) ---
q2 = buh.NewObject("Запрос")
q2.Text = """
ВЫБРАТЬ ПЕРВЫЕ 1
	Ост.Счет КАК Счет,
	ВЫРАЗИТЬ(Ост.Субконто1 КАК Справочник.Номенклатура).БазоваяЕдиницаИзмерения КАК Единица,
	ВЫРАЗИТЬ(Ост.Субконто1 КАК Справочник.Номенклатура).ОбщееНазваниеНоменклатуры КАК ОбщееНазвание,
	Ост.КоличествоОстаток КАК Остаток,
	Ост.СуммаОстаток КАК СуммаОстатка
ИЗ
	РегистрБухгалтерии.Хозрасчетный.Остатки(&НаДату, Счет В ИЕРАРХИИ (&Счета), , ) КАК Ост
ГДЕ
	Ост.КоличествоОстаток > 0
	И ВЫРАЗИТЬ(Ост.Субконто1 КАК Справочник.Номенклатура) = &Номенклатура
	И ВЫРАЗИТЬ(Ост.Субконто3 КАК Справочник.Склады) = &Склад
	И (&Организация = ЗНАЧЕНИЕ(Справочник.Организации.ПустаяСсылка)
		ИЛИ Ост.Организация = &Организация)

УПОРЯДОЧИТЬ ПО
	Остаток УБЫВ
"""
q2.SetParameter("НаДату", datetime.datetime(2026, 7, 7, 23, 59, 59))
sch = buh.NewObject("Массив")
for kod in ("20", "22", "28"):
    s = buh.ПланыСчетов.Хозрасчетный.НайтиПоКоду(kod)
    sch.Добавить(s)
q2.SetParameter("Счета", sch)
q2.SetParameter("Организация", buh.Справочники.Организации.ПустаяСсылка())
# живые Номенклатура+Склад: взять первую строку остатков 2051
q0 = buh.NewObject("Запрос")
q0.Text = """
ВЫБРАТЬ ПЕРВЫЕ 1
	ВЫРАЗИТЬ(Ост.Субконто1 КАК Справочник.Номенклатура) КАК Ном,
	ВЫРАЗИТЬ(Ост.Субконто3 КАК Справочник.Склады) КАК Скл
ИЗ РегистрБухгалтерии.Хозрасчетный.Остатки(&НаДату, Счет В ИЕРАРХИИ (&Счета), , ) КАК Ост
ГДЕ Ост.КоличествоОстаток > 0
"""
q0.SetParameter("НаДату", datetime.datetime(2026, 7, 7, 23, 59, 59))
q0.SetParameter("Счета", sch)
vb = q0.Execute().Выбрать()
assert vb.Следующий(), "нет остатков для теста"
q2.SetParameter("Номенклатура", vb.Ном)
q2.SetParameter("Склад", vb.Скл)
try:
    r2 = q2.Execute().Выгрузить()
    assert r2.Количество() == 1, "автоподбор вернул не 1 строку"
    st = r2.Получить(0)
    цена = round(st.СуммаОстатка / st.Остаток, 2) if st.Остаток else 0
    print(f"OK автоподбор: счёт={st.Счет.Код}, остаток={st.Остаток}, цена(безПДВ)={цена}")
except Exception as e:
    print(f"FAIL автоподбор: {e.excepinfo[2] if hasattr(e,'excepinfo') and e.excepinfo else e}")
    sys.exit(1)
print("PRECHECK PASS")
```

- [ ] **Step 1.2**: Запуск: `python "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\doc_kompl_query_precheck.py"` через python-runner. Expected: `PRECHECK PASS`. Если FAIL — чинить текст запроса ЗДЕСЬ и только потом переносить в BSL (Task 4).

### Task 2: Метаданные — Enum + Document через meta-compile

**Files:**
- Create: `TEST\doc_kompl_meta.json`
- Create (генерятся): `CFG\Enums\СтатусыРасчетаКомплектаций.xml`, `CFG\Documents\РасчетКомплектаций.xml`
- Modify (авто): `CFG\Configuration.xml`

- [ ] **Step 2.1**: Написать `TEST\doc_kompl_meta.json` (batch: Enum + Document; **posting: Deny**; имена ТЧ/колонок 1:1 с обработкой, включая украинские `ДокументиКомплектації`/`ЕкономіяСума`):

```json
[
  {
    "type": "Enum",
    "name": "СтатусыРасчетаКомплектаций",
    "values": ["Черновик", "РасчетВыполнен", "ДокументыСозданы"]
  },
  {
    "type": "Document",
    "name": "РасчетКомплектаций",
    "posting": "Deny",
    "numberLength": 11,
    "numberAllowedLength": "Fixed",
    "attributes": [
      "Спецификация: CatalogRef.СтруктураСебестоимости | req",
      "Период: Date",
      "Организация: CatalogRef.Организации",
      "ИсключатьЭтапы: Boolean",
      "Статус: EnumRef.СтатусыРасчетаКомплектаций",
      "Ответственный: CatalogRef.Пользователи",
      "Комментарий: String(0)"
    ],
    "tabularSections": {
      "СкладыОстатков": ["Склад: CatalogRef.Склады"],
      "СчетаОстатков": ["Счет: ChartOfAccountsRef.Хозрасчетный"],
      "СчетаМалоценки": ["Счет: ChartOfAccountsRef.Хозрасчетный"],
      "Этапы": ["Этап: CatalogRef.ЭтапыРабот"],
      "ТабличнаяЧастьОстатков": [
        "Номенклатура: CatalogRef.Номенклатура",
        "ОбщееНазвание: CatalogRef.ОбщиеНазванияНоменклатуры",
        "Склад: CatalogRef.Склады",
        "Счет: ChartOfAccountsRef.Хозрасчетный",
        "Остаток: Number(15,3)",
        "Норма: Number(15,3)",
        "ВНорме: Number(15,3)",
        "ПонадНорму: Number(15,3)",
        "Экономия: Number(15,3)",
        "Единица: CatalogRef.КлассификаторЕдиницИзмерения",
        "ЕдиницаСС: CatalogRef.КлассификаторЕдиницИзмерения",
        "СуммаОстатка: Number(15,2)",
        "НормаСумма: Number(15,2)",
        "ВНормеСумма: Number(15,2)",
        "ПонадНормуСумма: Number(15,2)",
        "ЕкономіяСума: Number(15,2)",
        "НормаНоменкл: Number(15,3)",
        "НормаСуммаНоменкл: Number(15,2)",
        "Этап: CatalogRef.ЭтапыРабот"
      ],
      "СписаниеПоНормам": [
        "Номенклатура: CatalogRef.Номенклатура",
        "ОбщееНазвание: CatalogRef.ОбщиеНазванияНоменклатуры",
        "Склад: CatalogRef.Склады",
        "Счет: ChartOfAccountsRef.Хозрасчетный",
        "Единица: CatalogRef.КлассификаторЕдиницИзмерения",
        "Количество: Number(15,3)",
        "Цена: Number(15,2)",
        "Сумма: Number(15,2)"
      ],
      "СписаниеСверхНормы": [
        "Номенклатура: CatalogRef.Номенклатура",
        "ОбщееНазвание: CatalogRef.ОбщиеНазванияНоменклатуры",
        "Склад: CatalogRef.Склады",
        "Счет: ChartOfAccountsRef.Хозрасчетный",
        "Единица: CatalogRef.КлассификаторЕдиницИзмерения",
        "Количество: Number(15,3)",
        "Цена: Number(15,2)",
        "Сумма: Number(15,2)",
        "Причина: String(200)"
      ],
      "ДокументиКомплектації": [
        "Склад: CatalogRef.Склады",
        "ДокументПоНормам: DocumentRef.КомплектацияНоменклатуры",
        "ДокументДодаткова: DocumentRef.КомплектацияНоменклатуры"
      ],
      "ДокументиМалоценки": [
        "Склад: CatalogRef.Склады",
        "ДокументПоНормам: DocumentRef.ПередачаМалоценныхАктивовВЭксплуатацию",
        "ДокументДодаткова: DocumentRef.ПередачаМалоценныхАктивовВЭксплуатацию",
        "ФизЛицо: CatalogRef.ФизическиеЛица",
        "СпособОтраженияРасходов: CatalogRef.СпособыОтраженияРасходовПоАмортизации"
      ]
    }
  }
]
```

- [ ] **Step 2.2**: `powershell.exe -NoProfile -File C:\Configuration_downloads\BASERP25\.claude\skills\meta-compile\scripts\meta-compile.ps1 -JsonPath "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\doc_kompl_meta.json" -OutputDir "C:\Configuration_downloads\BASERP25\_Rarzrabotki\BASEBuh"`. Expected: exit 0, созданы оба XML, Configuration.xml дополнен.

- [ ] **Step 2.3**: Проверить регистрацию: `grep -n "РасчетКомплектаций\|СтатусыРасчетаКомплектаций" "CFG\Configuration.xml"` → по 1 строке `<Document>`/`<Enum>` в алфавитном месте. Если meta-compile НЕ зарегистрировал — вставить хирургически Python-скриптом (re.sub по якорю соседней строки, xml.dom.minidom parse-проверка).

- [ ] **Step 2.4**: Пост-патч `TEST\doc_kompl_patch_meta.py`: (а) ru+uk синонимы для документа, перечисления, ВСЕХ реквизитов/ТЧ/колонок по таблице ниже; (б) `Период` — квалификатор `<DateFractions>Date</DateFractions>` (проверить, что meta-compile сгенерил Date; если DateTime — заменить); (в) всем колонкам типа `ChartOfAccountsRef` — `<ChoiceFoldersAndItems>FoldersAndItems</ChoiceFoldersAndItems>`; (г) документу — `<DefaultObjectForm>Document.РасчетКомплектаций.Form.ФормаДокумента</DefaultObjectForm>` (форма появится в Task 6). Скрипт: читать/писать UTF-8 (BOM как в исходнике), в конце `xml.dom.minidom.parse` обоих файлов.

Таблица синонимов uk (ru = имя с пробелами, авто из meta-compile):
| Объект/реквизит | uk |
|---|---|
| Документ РасчетКомплектаций | Розрахунок комплектацій за СС |
| Enum СтатусыРасчетаКомплектаций | Статуси розрахунку комплектацій |
| Черновик / РасчетВыполнен / ДокументыСозданы | Чернетка / Розрахунок виконано / Документи створено |
| Спецификация | Специфікація (норми) |
| Период | Залишки на дату |
| Организация | Організація |
| ИсключатьЭтапы | Виключати етапи зі списку |
| Статус | Статус |
| Ответственный | Відповідальний |
| Комментарий | Коментар |
| СкладыОстатков | Склади залишків |
| СчетаОстатков | Рахунки залишків |
| СчетаМалоценки | Рахунки малоцінки |
| Этапы | Етапи |
| ТабличнаяЧастьОстатков | Аналіз залишків |
| СписаниеПоНормам | Списання за нормами |
| СписаниеСверхНормы | Списання понад норму |
| ДокументиКомплектації | Документи комплектації |
| ДокументиМалоценки | Документи малоцінки |
| колонки ТабличнаяЧастьОстатков | 1:1 из манифеста обработки (Залишок, Норма (еталон), В нормі (по нормам), Понад норму (додаткова), Рахунок, Економія, Одиниця виміру залишків, Одиниця виміру СС, Сума залишку, Сума норми, Сума в нормі, Сума понад, Економія (грн), Норма (номенкл.), Сума норми (номенкл.), Етап, Загальна назва (еталон)) |
| колонки списания: Номенклатура/ОбщееНазвание/Склад/Счет/Единица/Количество/Цена/Сумма/Причина | Номенклатура / Загальна назва / Склад / Рахунок / Одиниця / Кількість / Ціна (без ПДВ) / Сума (без ПДВ) / Причина (обґрунтування) |
| колонки документов: Склад/ДокументПоНормам/ДокументДодаткова/ФизЛицо/СпособОтраженияРасходов | Склад / Документ «По нормам» / Документ «Понад норму» / МВО (фізична особа) / Спосіб відображення витрат |

- [ ] **Step 2.5**: `powershell.exe -NoProfile -File SKILLS\meta-validate\scripts\meta-validate.ps1 -ObjectPath "Documents/РасчетКомплектаций.xml"` (из каталога CFG; и то же для Enum). Expected: 0 ошибок.

### Task 3: Модуль объекта документа (порт движка)

**Files:**
- Create: `CFG\Documents\РасчетКомплектаций\Ext\ObjectModule.bsl`
- Create: `TEST\doc_kompl_build_module.py` (скрипт сборки модуля из исходника обработки)

Скрипт читает `OBR\Ext\ObjectModule.bsl` (1186 строк) и собирает новый модуль по правилам ниже; НЕ редактируем вручную 1000+ строк.

- [ ] **Step 3.1**: Правила сборки (скрипт = последовательность вырезаний/замен/вставок; после каждой — контроль парности `Процедура/КонецПроцедуры`, `#Область/#КонецОбласти`):

1. **Удалить** (БСП-регистрация не нужна документу): `СведенияОВнешнейОбработке` (стр. 8–21 с комментарием), `ПолучитьТаблицуКоманд` (975–984), `ДобавитьКоманду` (985–993) — резать по сигнатурам, не по номерам строк (страховка от сдвига).
2. **Заменить все** `СозданиеКомплектацийБух.` → `РасчетКомплектаций.` (строки ИмяСобытия журнала).
3. **РассчитатьАнализ**: после строки `ПопередитиПроРозбіжністьОдиниць();` вставить:
```bsl
	ЗаполнитьТаблицыСписания();
	Статус = Перечисления.СтатусыРасчетаКомплектаций.РасчетВыполнен;
```
4. **ЗаполнитьДокументыПоАнализу** — замены внутри:
   - guard `Если ТабличнаяЧастьОстатков.Количество() = 0 Тогда` → `Если СписаниеПоНормам.Количество() = 0 И СписаниеСверхНормы.Количество() = 0 Тогда`;
   - после `ЗафиксироватьТранзакцию();` вставить:
```bsl
		Если Результат.Заповнено > 0 Тогда
			Статус = Перечисления.СтатусыРасчетаКомплектаций.ДокументыСозданы;
		КонецЕсли;
```
5. **ПодготовитьСтрокиПоДокументам** — заменить ЦЕЛИКОМ (источник строк теперь таблицы списания; карты документов и семейства — как раньше):
```bsl
// Строит соответствие «документ -> ТЗ строк» из СОХРАНЁННЫХ таблиц списания:
// СписаниеПоНормам -> документы «ПоНормам», СписаниеСверхНормы -> документы «Додаткова»
// (ручные правки понаднормы попадают в документы). Семейство (комплектация/малоценка) — по счёту строки.
Функция ПодготовитьСтрокиПоДокументам(Результат, ЗаполнятьВНорме, ЗаполнятьПонад)

	// Семейство «комплектация»: склад -> документы КомплектацияНоменклатуры.
	ДокПоНормамКомпл = Новый Соответствие;
	ДокДопКомпл = Новый Соответствие;
	Для Каждого Стр Из ДокументиКомплектації Цикл
		Если ЗначениеЗаполнено(Стр.Склад) Тогда
			ДокПоНормамКомпл.Вставить(Стр.Склад, Стр.ДокументПоНормам);
			ДокДопКомпл.Вставить(Стр.Склад, Стр.ДокументДодаткова);
		КонецЕсли;
	КонецЦикла;

	// Семейство «малоценка»: склад -> документы ПередачаМалоценныхАктивовВЭксплуатацию.
	ДокПоНормамМал = Новый Соответствие;
	ДокДопМал = Новый Соответствие;
	Для Каждого Стр Из ДокументиМалоценки Цикл
		Если ЗначениеЗаполнено(Стр.Склад) Тогда
			ДокПоНормамМал.Вставить(Стр.Склад, Стр.ДокументПоНормам);
			ДокДопМал.Вставить(Стр.Склад, Стр.ДокументДодаткова);
		КонецЕсли;
	КонецЦикла;

	МножинаМал = МножинаСчетовМалоценки();
	СтрокиПоДокументам = Новый Соответствие;
	ПропущенныеСклады = Новый Соответствие;

	Если ЗаполнятьВНорме Тогда
		Для Каждого Стр Из СписаниеПоНормам Цикл
			Если НЕ Стр.Количество > 0 Тогда
				Продолжить;
			КонецЕсли;
			Если СчетМалоценки(Стр.Счет, МножинаМал) Тогда
				ДокКарта = ДокПоНормамМал;
			Иначе
				ДокКарта = ДокПоНормамКомпл;
			КонецЕсли;
			РаспределитьВДокумент(СтрокиПоДокументам, ПропущенныеСклады,
				ДокКарта.Получить(Стр.Склад), Стр.Склад, Стр.Номенклатура, Стр.Количество, Стр.Счет, Стр.Сумма);
		КонецЦикла;
	КонецЕсли;

	Если ЗаполнятьПонад Тогда
		Для Каждого Стр Из СписаниеСверхНормы Цикл
			Если НЕ Стр.Количество > 0 Тогда
				Продолжить;
			КонецЕсли;
			Если СчетМалоценки(Стр.Счет, МножинаМал) Тогда
				ДокКарта = ДокДопМал;
			Иначе
				ДокКарта = ДокДопКомпл;
			КонецЕсли;
			РаспределитьВДокумент(СтрокиПоДокументам, ПропущенныеСклады,
				ДокКарта.Получить(Стр.Склад), Стр.Склад, Стр.Номенклатура, Стр.Количество, Стр.Счет, Стр.Сумма);
		КонецЦикла;
	КонецЕсли;

	Результат.Пропущено = ПропущенныеСклады.Количество();
	Возврат СтрокиПоДокументам;

КонецФункции
```
6. **Вставить НОВЫЕ процедуры** в конец региона СлужебныеПроцедурыИФункции (перед `#КонецОбласти`):
```bsl
// Перезаполняет сохраняемые таблицы списания из анализа. Суммы — БЕЗ НДС (как пойдут в документы).
Процедура ЗаполнитьТаблицыСписания()
	СписаниеПоНормам.Очистить();
	СписаниеСверхНормы.Очистить();
	КоэффНДС = КоэффициентНДС();
	Для Каждого Стр Из ТабличнаяЧастьОстатков Цикл
		Если Стр.ВНорме > 0 Тогда
			ЗаполнитьСтрокуСписания(СписаниеПоНормам.Добавить(), Стр,
				Стр.ВНорме, Окр(Стр.ВНормеСумма / КоэффНДС, 2));
		КонецЕсли;
		Если Стр.ПонадНорму > 0 Тогда
			ЗаполнитьСтрокуСписания(СписаниеСверхНормы.Добавить(), Стр,
				Стр.ПонадНорму, Окр(Стр.ПонадНормуСумма / КоэффНДС, 2));
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

// Общий филлер строки таблицы списания из строки анализа.
Процедура ЗаполнитьСтрокуСписания(НС, СтрАнализа, Количество, Сумма)
	НС.Номенклатура = СтрАнализа.Номенклатура;
	НС.ОбщееНазвание = СтрАнализа.ОбщееНазвание;
	НС.Склад = СтрАнализа.Склад;
	НС.Счет = СтрАнализа.Счет;
	НС.Единица = СтрАнализа.Единица;
	НС.Количество = Количество;
	НС.Сумма = Сумма;
	НС.Цена = ?(Количество > 0, Окр(Сумма / Количество, 2), 0);
КонецПроцедуры

// Данные остатка для ручной строки понаднормы: Счет/Цена(без ПДВ)/Единица/ОбщееНазвание/Остаток.
// Неопределено — если остатка нет. Счета — объединение обоих списков (комплектация + малоценка).
Функция ДанныеОстаткаДляСтроки(Номенклатура, Склад) Экспорт
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	Ост.Счет КАК Счет,
	|	ВЫРАЗИТЬ(Ост.Субконто1 КАК Справочник.Номенклатура).БазоваяЕдиницаИзмерения КАК Единица,
	|	ВЫРАЗИТЬ(Ост.Субконто1 КАК Справочник.Номенклатура).ОбщееНазваниеНоменклатуры КАК ОбщееНазвание,
	|	Ост.КоличествоОстаток КАК Остаток,
	|	Ост.СуммаОстаток КАК СуммаОстатка
	|ИЗ
	|	РегистрБухгалтерии.Хозрасчетный.Остатки(&НаДату, Счет В ИЕРАРХИИ (&Счета), , ) КАК Ост
	|ГДЕ
	|	Ост.КоличествоОстаток > 0
	|	И ВЫРАЗИТЬ(Ост.Субконто1 КАК Справочник.Номенклатура) = &Номенклатура
	|	И ВЫРАЗИТЬ(Ост.Субконто3 КАК Справочник.Склады) = &Склад
	|	И (&Организация = ЗНАЧЕНИЕ(Справочник.Организации.ПустаяСсылка)
	|		ИЛИ Ост.Организация = &Организация)
	|
	|УПОРЯДОЧИТЬ ПО
	|	Остаток УБЫВ";
	Запрос.УстановитьПараметр("НаДату", ?(ЗначениеЗаполнено(Период), КонецДня(Период), Неопределено));
	Запрос.УстановитьПараметр("Счета", ВсеСчетаСписком());
	Запрос.УстановитьПараметр("Номенклатура", Номенклатура);
	Запрос.УстановитьПараметр("Склад", Склад);
	Запрос.УстановитьПараметр("Организация", Организация);
	Выборка = Запрос.Выполнить().Выбрать();
	Если НЕ Выборка.Следующий() Тогда
		Возврат Неопределено;
	КонецЕсли;
	Результат = Новый Структура("Счет, Единица, ОбщееНазвание, Остаток, Цена");
	ЗаполнитьЗначенияСвойств(Результат, Выборка);
	Результат.Цена = ?(Выборка.Остаток > 0, Окр(Выборка.СуммаОстатка / Выборка.Остаток, 2), 0);
	Возврат Результат;
КонецФункции

// Другие документы-расчёты по той же спецификации, тому же дню остатков и с пересечением складов.
Функция НайтиДубликатыРасчета() Экспорт
	Дубли = Новый Массив;
	СкладыМассив = СкладыСписком();
	Если СкладыМассив.Количество() = 0 ИЛИ НЕ ЗначениеЗаполнено(Спецификация) Тогда
		Возврат Дубли;
	КонецЕсли;
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	Док.Ссылка КАК Ссылка
	|ИЗ
	|	Документ.РасчетКомплектаций КАК Док
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.РасчетКомплектаций.СкладыОстатков КАК Склады
	|		ПО Склады.Ссылка = Док.Ссылка
	|ГДЕ
	|	НЕ Док.ПометкаУдаления
	|	И Док.Ссылка <> &Ссылка
	|	И Док.Спецификация = &Спецификация
	|	И Док.Период МЕЖДУ &НачалоДня И &КонецДня
	|	И Склады.Склад В (&Склады)";
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	Запрос.УстановитьПараметр("Спецификация", Спецификация);
	Запрос.УстановитьПараметр("НачалоДня", НачалоДня(Период));
	Запрос.УстановитьПараметр("КонецДня", КонецДня(Период));
	Запрос.УстановитьПараметр("Склады", СкладыМассив);
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Дубли.Добавить(Выборка.Ссылка);
	КонецЦикла;
	Возврат Дубли;
КонецФункции

// «Акт на списання понаднормових матеріалів» — программный ТабДок по ТЧ СписаниеСверхНормы.
Функция СформироватьПечатьАкт() Экспорт
	ТабДок = Новый ТабличныйДокумент;
	ТабДок.АвтоМасштаб = Истина;
	ТабДок.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;

	Заголовок = ТабДок.Область(1, 1, 1, 9);
	Заголовок.Объединить();
	Заголовок.Текст = "АКТ на списання понаднормових матеріалів";
	Заголовок.Шрифт = Новый Шрифт(, 14, Истина);
	Заголовок.ГоризонтальноеПоложение = ГоризонтальноеПоложениеТекста.Центр;

	НомерСтроки = 3;
	Параметры = Новый Массив;
	Параметры.Добавить("Документ: Розрахунок комплектацій № " + Номер + " від " + Формат(Дата, "ДФ=dd.MM.yyyy"));
	Параметры.Добавить("Організація: " + Строка(Организация));
	Параметры.Добавить("Специфікація: " + Строка(Спецификация));
	Параметры.Добавить("Залишки на дату: " + Формат(Период, "ДФ=dd.MM.yyyy"));
	Параметры.Добавить("Склади: " + СкладыСтрокой());
	Для Каждого Текст Из Параметры Цикл
		Обл = ТабДок.Область(НомерСтроки, 1, НомерСтроки, 9);
		Обл.Объединить();
		Обл.Текст = Текст;
		Обл.Шрифт = Новый Шрифт(, 9);
		НомерСтроки = НомерСтроки + 1;
	КонецЦикла;
	НомерСтроки = НомерСтроки + 1;

	Заголовки = Новый Массив;
	Заголовки.Добавить("№");
	Заголовки.Добавить("Номенклатура");
	Заголовки.Добавить("Склад");
	Заголовки.Добавить("Рахунок");
	Заголовки.Добавить("Одиниця");
	Заголовки.Добавить("Кількість");
	Заголовки.Добавить("Ціна, грн");
	Заголовки.Добавить("Сума, грн");
	Заголовки.Добавить("Причина (обґрунтування)");
	ВывестиСтрокуПечати(ТабДок, НомерСтроки, Заголовки, Истина, Новый Цвет(210, 224, 240));
	НомерСтроки = НомерСтроки + 1;

	НомерПП = 0;
	ИтогоСума = 0;
	Для Каждого Стр Из СписаниеСверхНормы Цикл
		НомерПП = НомерПП + 1;
		Значения = Новый Массив;
		Значения.Добавить(НомерПП);
		Значения.Добавить(Строка(Стр.Номенклатура));
		Значения.Добавить(Строка(Стр.Склад));
		Значения.Добавить(?(ЗначениеЗаполнено(Стр.Счет), Стр.Счет.Код, ""));
		Значения.Добавить(Строка(Стр.Единица));
		Значения.Добавить(Стр.Количество);
		Значения.Добавить(Стр.Цена);
		Значения.Добавить(Стр.Сумма);
		Значения.Добавить(Стр.Причина);
		ВывестиСтрокуПечати(ТабДок, НомерСтроки, Значения, Ложь);
		ИтогоСума = ИтогоСума + Стр.Сумма;
		НомерСтроки = НомерСтроки + 1;
	КонецЦикла;

	Итоги = Новый Массив;
	Итоги.Добавить("Разом");
	Итоги.Добавить("");
	Итоги.Добавить("");
	Итоги.Добавить("");
	Итоги.Добавить("");
	Итоги.Добавить("");
	Итоги.Добавить("");
	Итоги.Добавить(ИтогоСума);
	Итоги.Добавить("");
	ВывестиСтрокуПечати(ТабДок, НомерСтроки, Итоги, Истина, Новый Цвет(232, 232, 232));
	НомерСтроки = НомерСтроки + 2;

	Подписи = Новый Массив;
	Подписи.Добавить("Голова комісії: ______________________   ______________________");
	Подписи.Добавить("Член комісії:   ______________________   ______________________");
	Подписи.Добавить("Член комісії:   ______________________   ______________________");
	Подписи.Добавить("МВО:            ______________________   ______________________");
	Для Каждого Текст Из Подписи Цикл
		Обл = ТабДок.Область(НомерСтроки, 1, НомерСтроки, 9);
		Обл.Объединить();
		Обл.Текст = Текст;
		НомерСтроки = НомерСтроки + 1;
	КонецЦикла;

	ТабДок.Область(1, 1, 1, 1).ШиринаКолонки = 5;
	ТабДок.Область(1, 2, 1, 2).ШиринаКолонки = 45;
	ТабДок.Область(1, 3, 1, 3).ШиринаКолонки = 26;
	ТабДок.Область(1, 4, 1, 4).ШиринаКолонки = 10;
	ТабДок.Область(1, 5, 1, 5).ШиринаКолонки = 10;
	ТабДок.Область(1, 6, 1, 6).ШиринаКолонки = 12;
	ТабДок.Область(1, 7, 1, 7).ШиринаКолонки = 12;
	ТабДок.Область(1, 8, 1, 8).ШиринаКолонки = 14;
	ТабДок.Область(1, 9, 1, 9).ШиринаКолонки = 40;

	Возврат ТабДок;
КонецФункции
```
7. **Вставить события документа** — НОВЫЙ регион в конец модуля (после `#КонецОбласти` служебного региона):
```bsl
#Область ОбработчикиСобытий

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	Статус = Перечисления.СтатусыРасчетаКомплектаций.Черновик;
	Период = НачалоДня(ТекущаяДата());
	Ответственный = Пользователи.ТекущийПользователь();
	Если СчетаОстатков.Количество() = 0 Тогда
		Для Каждого Код Из СтрРазделить("20,28", ",") Цикл
			Сч = ПланыСчетов.Хозрасчетный.НайтиПоКоду(Код);
			Если ЗначениеЗаполнено(Сч) Тогда
				СчетаОстатков.Добавить().Счет = Сч;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	Если СчетаМалоценки.Количество() = 0 Тогда
		Сч22 = ПланыСчетов.Хозрасчетный.НайтиПоКоду("22");
		Если ЗначениеЗаполнено(Сч22) Тогда
			СчетаМалоценки.Добавить().Счет = Сч22;
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Статус) Тогда
		Статус = Перечисления.СтатусыРасчетаКомплектаций.Черновик;
	КонецЕсли;
	// Контроль дублей: предупреждение, НЕ отказ.
	Дубли = НайтиДубликатыРасчета();
	Если Дубли.Количество() > 0 Тогда
		Сообщить("Увага: вже існує розрахунок за цією специфікацією/датою/складами: "
			+ Строка(Дубли[0]) + ?(Дубли.Количество() > 1, " (та ще " + (Дубли.Количество() - 1) + ")", ""));
	КонецЕсли;
КонецПроцедуры

#КонецОбласти
```
8. Шапку-комментарий модуля (строки 1–4) заменить на описание документа (2-3 строки).
9. Все остальные процедуры (СкладыСписком … ДанныеПоЭтапам, печати, ПолучитьОстатки/ПолучитьНормы/ЗаполнитьАнализ и пр.) — **копировать вербатим без изменений**.

- [ ] **Step 3.2**: Запустить сборку: `python "TEST\doc_kompl_build_module.py"`. Скрипт в конце: (а) пишет файл UTF-8 BOM + CRLF; (б) печатает контроль: число процедур/функций (ожидаем 42 − 3 удалённых + 6 новых = 45), парность регионов, отсутствие строк `СведенияОВнешнейОбработке|ПолучитьТаблицуКоманд|ДобавитьКоманду`, наличие всех 6 новых сигнатур. Expected: все контроли PASS.

- [ ] **Step 3.3**: Если в Task 0 Step 0.4 модуль `Пользователи.ТекущийПользователь()` не подтвердился — заменить строку на `Ответственный = Справочники.Пользователи.НайтиПоНаименованию(ИмяПользователя());` (fallback; отметить в отчёте).

### Task 4: Макеты СКД (4 шт.)

**Files:**
- Create: `CFG\Documents\РасчетКомплектаций\Templates\{МакетПланФакт, МакетПланФактЕтапи, МакетАнализСС, МакетАнализССОдна}.xml` + `...\<Имя>\Ext\Template.xml`
- Modify: `CFG\Documents\РасчетКомплектаций.xml` (регистрация `<Template>`)
- Create: `TEST\doc_kompl_copy_templates.py`

- [ ] **Step 4.1**: Скрипт: для каждого из 4 макетов копировать `OBR\Templates\<Имя>\Ext\Template.xml` → как есть (uk-титулы/appearance уже вшиты); создать обёртку `<Имя>.xml` по образцу обёртки обработки (см. research), но `uuid` = **новый uuid4** (python `uuid.uuid4()`); синонимы оставить uk (+добавить ru-пару с тем же текстом не нужно — Synonym uk достаточен, макеты в UI не видны).
- [ ] **Step 4.2**: В `CFG\Documents\РасчетКомплектаций.xml` перед `</ChildObjects>` (закрывающим ChildObjects ДОКУМЕНТА, не ТЧ!) вставить 4 строки `<Template>Имя</Template>` (порядок: ПланФакт, ПланФактЕтапи, АнализСС, АнализССОдна). Якорь вставки — последняя `</TabularSection>` документа.
- [ ] **Step 4.3**: Контроль §39: `grep -rho 'Template uuid="[^"]*"' CFG --include="*.xml" | sort | uniq -d` → пусто (нет дублей uuid во ВСЕЙ конфигурации). Также `xml.dom.minidom.parse` всех 8 файлов.

### Task 5: Форма документа (transform-порт)

**Files:**
- Create: `CFG\Documents\РасчетКомплектаций\Forms\ФормаДокумента.xml` (обёртка)
- Create: `CFG\Documents\РасчетКомплектаций\Forms\ФормаДокумента\Ext\Form.xml`
- Create: `CFG\Documents\РасчетКомплектаций\Forms\ФормаДокумента\Ext\Form\Module.bsl`
- Modify: `CFG\Documents\РасчетКомплектаций.xml` (`<Form>ФормаДокумента</Form>` после Attribute-блока, перед TabularSection — порядок ChildObjects: Attribute → Form → TabularSection → Template)
- Create: `TEST\doc_kompl_build_form.py`

- [ ] **Step 5.1 — обёртка формы** (uuid4 новый):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<MetaDataObject xmlns="http://v8.1c.ru/8.3/MDClasses" xmlns:app="http://v8.1c.ru/8.2/managed-application/core" xmlns:cfg="http://v8.1c.ru/8.1/data/enterprise/current-config" xmlns:cmi="http://v8.1c.ru/8.2/managed-application/cmi" xmlns:ent="http://v8.1c.ru/8.1/data/enterprise" xmlns:lf="http://v8.1c.ru/8.2/managed-application/logform" xmlns:style="http://v8.1c.ru/8.1/data/ui/style" xmlns:sys="http://v8.1c.ru/8.1/data/ui/fonts/system" xmlns:v8="http://v8.1c.ru/8.1/data/core" xmlns:v8ui="http://v8.1c.ru/8.1/data/ui" xmlns:web="http://v8.1c.ru/8.1/data/ui/colors/web" xmlns:win="http://v8.1c.ru/8.1/data/ui/colors/windows" xmlns:xen="http://v8.1c.ru/8.3/xcf/enums" xmlns:xpr="http://v8.1c.ru/8.3/xcf/predef" xmlns:xr="http://v8.1c.ru/8.3/xcf/readable" xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" version="2.13">
	<Form uuid="{UUID4}">
		<Properties>
			<Name>ФормаДокумента</Name>
			<Synonym>
				<v8:item>
					<v8:lang>ru</v8:lang>
					<v8:content>Форма документа</v8:content>
				</v8:item>
				<v8:item>
					<v8:lang>uk</v8:lang>
					<v8:content>Форма документа</v8:content>
				</v8:item>
			</Synonym>
			<Comment/>
			<FormType>Managed</FormType>
			<IncludeHelpInContents>false</IncludeHelpInContents>
			<UsePurposes>
				<v8:Value xsi:type="app:ApplicationUsePurpose">PlatformApplication</v8:Value>
				<v8:Value xsi:type="app:ApplicationUsePurpose">MobilePlatformApplication</v8:Value>
			</UsePurposes>
		</Properties>
	</Form>
</MetaDataObject>
```
(Сверить набор Properties с существующей обёрткой `CFG\Documents\КомплектацияНоменклатуры\Forms\ФормаДокумента.xml` — взять её как эталон, поменяв uuid/владельца.)

- [ ] **Step 5.2 — Form.xml трансформом** (`doc_kompl_build_form.py` читает `OBR\Forms\Форма\Ext\Form.xml`, 1308 строк, max id=326):
1. MainAttribute Объект: `cfg:ExternalDataProcessorObject.СозданиеКомплектацийБух` → `cfg:DocumentObject.РасчетКомплектаций`.
2. Удалить `<Title>…</Title>` формы и `<AutoTitle>false</AutoTitle>` (авто-заголовок документа); `<AutoSaveDataInSettings>Use</AutoSaveDataInSettings>` и секцию SaveData (если есть `<SaveData>`/`<SettingsStorage>` со ссылками Объект.*) — удалить.
3. В начало ChildItems (перед CommandBar) вставить группу шапки (id из диапазона 400+, свободного):
   - UsualGroup `ГруппаШапка` (Horizontal): InputField `Номер` (DataPath=Объект.Номер, ReadOnly), InputField `ДатаДокумента` (DataPath=Объект.Дата), InputField `Статус` (DataPath=Объект.Статус, ReadOnly), InputField `Ответственный` (DataPath=Объект.Ответственный), InputField `Комментарий` (DataPath=Объект.Комментарий, ширина 30).
   Каждый элемент — по образцу существующих InputField формы (клон структуры узла Период с заменой имени/DataPath/Title; титулы uk: «Номер», «Дата», «Статус», «Відповідальний», «Коментар»; + companion ExtendedTooltip/ContextMenu с уникальными id).
4. В Pages после `СтраницаАнализ` вставить 2 новые Page:
   - **СтраницаПоНормам** «Списання за нормами»: Table `СписаниеПоНормам` DataPath=`Объект.СписаниеПоНормам`, ReadOnly=true, Footer=true; колонки InputField: Номенклатура, ОбщееНазвание, Склад, Счет, Единица, Количество, Цена, Сумма (DataPath=Объект.СписаниеПоНормам.<Кол>; FooterDataPath у Количество/Сумма → реквизиты формы ИтогоНормКоличество/ИтогоНормСумма НЕ создаём — вместо этого подвал без датапаса пропускаем, итоги считает Module.bsl в отдельные поля НЕ надо: используем встроенный Footer c ItogФормулами нельзя — упрощение: Footer=false, под таблицей UsualGroup c 2 read-only InputField ИтогоНормКоличество/ИтогоНормСумма — реквизиты формы decimal 15,3/15,2, пересчёт в модуле после расчёта/записи).
   - **СтраницаПонадНорму** «Списання понад норму»: Table `СписаниеСверхНормы` DataPath=`Объект.СписаниеСверхНормы` (редактируемая: ChangeRowSet по умолчанию true — НЕ ставить ReadOnly), колонки: Номенклатура (Events: OnChange=СверхНормыНоменклатураПриИзменении), Склад (OnChange=СверхНормыСкладПриИзменении), Счет, Единица, Количество (OnChange=СверхНормыКоличествоПриИзменении), Цена (OnChange=СверхНормыЦенаПриИзменении), Сумма (ReadOnly=true), Причина; под таблицей группа с ИтогоСверхКоличество/ИтогоСверхСумма (реквизиты формы, read-only).
5. В CommandBar добавить: Button `КнопкаПечатьАкт` (Form.Command.ПечатьАкт, «Акт понаднормового списання») после КнопкаПечатьАнализССОдна.
6. Commands: добавить `ПечатьАкт` (Action=ПечатьАкт).
7. Attributes: добавить 4 реквизита формы: ИтогоНормКоличество (Number 15,3), ИтогоНормСумма (15,2), ИтогоСверхКоличество (15,3), ИтогоСверхСумма (15,2).
8. Все новые id — начиная с 400 (скан подтверждает max=326); в конце скрипта: parse + проверка уникальности id + вывод нового max id.
9. Событий формы: добавить `<Event name="BeforeWriteAtServer">ПередЗаписьюНаСервере</Event>`? — НЕ нужно (контроль дублей в модуле объекта). Оставить OnCreateAtServer.

- [ ] **Step 5.3 — Module.bsl формы**: старт от модуля обработки (328 строк), правки:
1. `ПриСозданииНаСервере` — вместо преднаполнения счетов (теперь в ОбработкаЗаполнения объекта) оставить: цвета итогов, `УстановитьУсловноеОформлениеСтрок()`, дефолт Период только если пусто И новый (`Объект.Ссылка.Пустая()`), затем `ПрименитьОтборИИтоги()` и `ПересчитатьИтогиСписания()` (восстановление вида при открытии сохранённого документа):
```bsl
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Элементы.ИтогоВНорме.ЦветФона = Новый Цвет(214, 240, 214);
	Элементы.ИтогоПонад.ЦветФона  = Новый Цвет(250, 235, 200);
	УстановитьУсловноеОформлениеСтрок();
	ПересчитатьИтогиСписания();
КонецПроцедуры
```
2. `ПриОткрытии(Отказ)` &НаКлиенте — новый: `ПрименитьОтборИИтоги();` (перестройка ТаблицаАнализа из сохранённой ТЧ).
3. `Рассчитать(Команда)` — добавить подтверждение при статусе «Документи створено» (асинхронный вопрос) и запись формы после расчёта:
```bsl
&НаКлиенте
Процедура Рассчитать(Команда)
	Если Объект.Статус = ПредопределенноеЗначение("Перечисление.СтатусыРасчетаКомплектаций.ДокументыСозданы") Тогда
		Оповещение = Новый ОписаниеОповещения("РассчитатьПродолжение", ЭтотОбъект);
		ПоказатьВопрос(Оповещение,
			"Документи вже створено за цим розрахунком. Перезаповнити розрахунок?",
			РежимДиалогаВопрос.ДаНет);
	Иначе
		РассчитатьПродолжение(КодВозвратаДиалога.Да, Неопределено);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура РассчитатьПродолжение(РезультатВопроса, ДопПараметры) Экспорт
	Если РезультатВопроса <> КодВозвратаДиалога.Да Тогда
		Возврат;
	КонецЕсли;
	РассчитатьНаСервере();
	ПрименитьОтборИИтоги();
	Записать();
КонецПроцедуры
```
(`РассчитатьНаСервере` — как в обработке, без изменений; после него добавить вызов `ПересчитатьИтогиСписания()` в конце серверной процедуры.)
4. `ЗаполнитьДокументыНаСервере` — без изменений; но клиентские обёртки 3 режимов дополняются записью:
```bsl
&НаКлиенте
Процедура ЗаполнитьОбе(Команда)
	ЗаполнитьДокументыНаСервере(Истина, Истина);
	Записать();
КонецПроцедуры
```
(аналогично ТолькоВНорме/ТолькоПонад).
5. Новые обработчики понаднормы:
```bsl
&НаКлиенте
Процедура СверхНормыКоличествоПриИзменении(Элемент)
	Стр = Элементы.СписаниеСверхНормы.ТекущиеДанные;
	Если Стр = Неопределено Тогда
		Возврат;
	КонецЕсли;
	Стр.Сумма = Окр(Стр.Количество * Стр.Цена, 2);
	ПроверитьОстатокСтрокиНаСервере(Стр.Номенклатура, Стр.Склад, Стр.Количество);
	ПересчитатьИтогиСписания();
КонецПроцедуры

&НаКлиенте
Процедура СверхНормыЦенаПриИзменении(Элемент)
	Стр = Элементы.СписаниеСверхНормы.ТекущиеДанные;
	Если Стр = Неопределено Тогда
		Возврат;
	КонецЕсли;
	Стр.Сумма = Окр(Стр.Количество * Стр.Цена, 2);
	ПересчитатьИтогиСписания();
КонецПроцедуры

&НаКлиенте
Процедура СверхНормыНоменклатураПриИзменении(Элемент)
	ПодобратьДанныеСтрокиСверхНормы();
КонецПроцедуры

&НаКлиенте
Процедура СверхНормыСкладПриИзменении(Элемент)
	ПодобратьДанныеСтрокиСверхНормы();
КонецПроцедуры

&НаКлиенте
Процедура ПодобратьДанныеСтрокиСверхНормы()
	Стр = Элементы.СписаниеСверхНормы.ТекущиеДанные;
	Если Стр = Неопределено ИЛИ НЕ ЗначениеЗаполнено(Стр.Номенклатура) ИЛИ НЕ ЗначениеЗаполнено(Стр.Склад) Тогда
		Возврат;
	КонецЕсли;
	Данные = ДанныеОстаткаНаСервере(Стр.Номенклатура, Стр.Склад);
	Если Данные = Неопределено Тогда
		Сообщить("Залишок за цією номенклатурою на складі не знайдено (рахунок/ціну не підібрано).");
		Возврат;
	КонецЕсли;
	Стр.Счет = Данные.Счет;
	Стр.Единица = Данные.Единица;
	Стр.ОбщееНазвание = Данные.ОбщееНазвание;
	Стр.Цена = Данные.Цена;
	Стр.Сумма = Окр(Стр.Количество * Стр.Цена, 2);
	ПересчитатьИтогиСписания();
КонецПроцедуры

&НаСервере
Функция ДанныеОстаткаНаСервере(Номенклатура, Склад)
	ОбъектДокумента = РеквизитФормыВЗначение("Объект");
	Возврат ОбъектДокумента.ДанныеОстаткаДляСтроки(Номенклатура, Склад);
КонецФункции

&НаСервере
Процедура ПроверитьОстатокСтрокиНаСервере(Номенклатура, Склад, Количество)
	ОбъектДокумента = РеквизитФормыВЗначение("Объект");
	Данные = ОбъектДокумента.ДанныеОстаткаДляСтроки(Номенклатура, Склад);
	Если Данные = Неопределено Тогда
		Сообщить("Увага: залишку немає — списуємо без покриття залишком.");
	ИначеЕсли Количество > Данные.Остаток Тогда
		Сообщить("Увага: кількість " + Количество + " перевищує залишок " + Данные.Остаток + ".");
	КонецЕсли;
КонецПроцедуры

&НаСервере
Процедура ПересчитатьИтогиСписания()
	ИтогоНормКоличество = Объект.СписаниеПоНормам.Итог("Количество");
	ИтогоНормСумма      = Объект.СписаниеПоНормам.Итог("Сумма");
	ИтогоСверхКоличество = Объект.СписаниеСверхНормы.Итог("Количество");
	ИтогоСверхСумма      = Объект.СписаниеСверхНормы.Итог("Сумма");
КонецПроцедуры
```
⚠️ `ПересчитатьИтогиСписания` вызывается и с клиента (в обработчиках) — держать &НаСервере нельзя для каждого чиха; **правка**: сделать &НаКлиенте вариант — `Объект.СписаниеСверхНормы` на клиенте — ДанныеФормыКоллекция, у неё нет `.Итог()`. Решение: клиентская процедура с ручным циклом:
```bsl
&НаКлиенте
Процедура ПересчитатьИтогиСписания()
	ИтогоНормКоличество = 0;
	ИтогоНормСумма = 0;
	Для Каждого Стр Из Объект.СписаниеПоНормам Цикл
		ИтогоНормКоличество = ИтогоНормКоличество + Стр.Количество;
		ИтогоНормСумма = ИтогоНормСумма + Стр.Сумма;
	КонецЦикла;
	ИтогоСверхКоличество = 0;
	ИтогоСверхСумма = 0;
	Для Каждого Стр Из Объект.СписаниеСверхНормы Цикл
		ИтогоСверхКоличество = ИтогоСверхКоличество + Стр.Количество;
		ИтогоСверхСумма = ИтогоСверхСумма + Стр.Сумма;
	КонецЦикла;
КонецПроцедуры
```
и в `ПриСозданииНаСервере` НЕ вызывать её (клиентская) — вызвать в `ПриОткрытии`. В серверной `РассчитатьНаСервере` итоги пересчитаются после возврата на клиент (в РассчитатьПродолжение уже есть ПрименитьОтборИИтоги — добавить и ПересчитатьИтогиСписания).
6. Печать Акта:
```bsl
&НаКлиенте
Процедура ПечатьАкт(Команда)
	ТабДок = ПечатьАктНаСервере();
	Если ТабДок <> Неопределено Тогда
		ТабДок.Показать("Акт на списання понаднормових матеріалів");
	КонецЕсли;
КонецПроцедуры

&НаСервере
Функция ПечатьАктНаСервере()
	Если Объект.СписаниеСверхНормы.Количество() = 0 Тогда
		Сообщить("Таблиця «Списання понад норму» порожня.");
		Возврат Неопределено;
	КонецЕсли;
	ОбъектДокумента = РеквизитФормыВЗначение("Объект");
	Возврат ОбъектДокумента.СформироватьПечатьАкт();
КонецФункции
```
7. Остальные процедуры (ПрименитьОтборИИтоги, отборы, ФильтрПоПолю, печати 5 шт., ВывестиРезультатЗаполнения, УстановитьУсловноеОформлениеСтрок) — вербатим из модуля обработки.

- [ ] **Step 5.4**: Регистрация в `CFG\Documents\РасчетКомплектаций.xml`: вставить `<Form>ФормаДокумента</Form>` после последнего `</Attribute>` шапки (перед первым `<TabularSection`), проверить `<DefaultObjectForm>` (Task 2 Step 2.4 already). Parse-контроль.

- [ ] **Step 5.5**: `powershell.exe -NoProfile -File SKILLS\form-validate\scripts\form-validate.ps1 -FormPath "Documents/РасчетКомплектаций/Forms/ФормаДокумента"` (из CFG). Expected: 0 ошибок (ложные «~Список.X» неактуальны — динсписков нет).

### Task 6: Подсистема и роли

**Files:**
- Modify: `CFG\Subsystems\Склад\Subsystems\СкладГлавное.xml` (+Content item)
- Modify: `CFG\Subsystems\Склад\Subsystems\СкладГлавное\Ext\CommandInterface.xml` (+Command)
- Modify: `CFG\Roles\ДобавлениеИзменениеДанныхБухгалтерии\Ext\Rights.xml`, `CFG\Roles\ЧтениеДанныхБухгалтерии\Ext\Rights.xml`
- Create: `TEST\doc_kompl_register.py`

- [ ] **Step 6.1**: В `СкладГлавное.xml` после строки `<xr:Item xsi:type="xr:MDObjectRef">Document.КомплектацияНоменклатуры</xr:Item>` вставить `<xr:Item xsi:type="xr:MDObjectRef">Document.РасчетКомплектаций</xr:Item>`; в `CommandInterface.xml` после блока команды КомплектацияНоменклатуры вставить:
```xml
		<Command name="Document.РасчетКомплектаций.StandardCommand.OpenList">
			<CommandGroup>NavigationPanelOrdinary</CommandGroup>
		</Command>
```
- [ ] **Step 6.2**: Роли (surgical, ролям НЕ копировать файлы целиком — вставка `<object>`-блока перед закрывающим `</Rights>`); **без RLS** (у нас Организация опциональна — RLS-шаблон по организации скрыл бы документы с пустой организацией):
  - `ДобавлениеИзменениеДанныхБухгалтерии`:
```xml
	<object>
		<name>Document.РасчетКомплектаций</name>
		<right>
			<name>Read</name>
			<value>true</value>
		</right>
		<right>
			<name>Insert</name>
			<value>true</value>
		</right>
		<right>
			<name>Update</name>
			<value>true</value>
		</right>
		<right>
			<name>View</name>
			<value>true</value>
		</right>
		<right>
			<name>InteractiveInsert</name>
			<value>true</value>
		</right>
		<right>
			<name>Edit</name>
			<value>true</value>
		</right>
		<right>
			<name>InteractiveSetDeletionMark</name>
			<value>true</value>
		</right>
		<right>
			<name>InteractiveClearDeletionMark</name>
			<value>true</value>
		</right>
		<right>
			<name>InputByString</name>
			<value>true</value>
		</right>
	</object>
```
  - `ЧтениеДанныхБухгалтерии`: тот же блок, но только Read/View/InputByString.
  - ПолныеПрава/АдминистраторСистемы: `setForNewObjects=true` → ничего не делаем.
- [ ] **Step 6.3**: `xml.dom.minidom.parse` всех 4 файлов. `role-validate` по обеим ролям (скил role-validate, путь аналогично meta-validate). Expected: 0 ошибок.

### Task 7: Загрузка в базу (db-load-xml Partial + db-update)

**Files:** нет новых (загрузка).

- [ ] **Step 7.1**: Проверка Конфигуратора (как Task 0 Step 0.1) — должен быть закрыт.
- [ ] **Step 7.2**: Частичная загрузка (пути форм/ролей/подсистем — ТОЛЬКО корневые; V8Path из `.v8-project.json` = `C:\Program Files\1cv8\8.3.20.1914\bin`):
```powershell
powershell.exe -NoProfile -File C:\Configuration_downloads\BASERP25\.claude\skills\db-load-xml\scripts\db-load-xml.ps1 `
  -V8Path "C:\Program Files\1cv8\8.3.20.1914\bin" `
  -InfoBaseServer "localhost" -InfoBaseRef "bas_industrialbud" `
  -UserName "cfo" -Password "2442" `
  -ConfigDir "C:\Configuration_downloads\BASERP25\_Rarzrabotki\BASEBuh" `
  -Mode Partial `
  -Files "Configuration.xml,Enums/СтатусыРасчетаКомплектаций.xml,Documents/РасчетКомплектаций.xml,Documents/РасчетКомплектаций/Ext/ObjectModule.bsl,Documents/РасчетКомплектаций/Forms/ФормаДокумента.xml,Documents/РасчетКомплектаций/Templates/МакетПланФакт.xml,Documents/РасчетКомплектаций/Templates/МакетПланФактЕтапи.xml,Documents/РасчетКомплектаций/Templates/МакетАнализСС.xml,Documents/РасчетКомплектаций/Templates/МакетАнализССОдна.xml,Subsystems/Склад/Subsystems/СкладГлавное.xml,Roles/ДобавлениеИзменениеДанныхБухгалтерии.xml,Roles/ЧтениеДанныхБухгалтерии.xml"
```
Expected: exit 0. (Если сервер localhost не примет — повторить с `-InfoBaseServer "SQLSERVER"`; это одна машина.)
- [ ] **Step 7.3**: Обновление БД: сначала динамически:
```powershell
powershell.exe -NoProfile -File C:\Configuration_downloads\BASERP25\.claude\skills\db-update\scripts\db-update.ps1 `
  -V8Path "C:\Program Files\1cv8\8.3.20.1914\bin" -InfoBaseServer "localhost" -InfoBaseRef "bas_industrialbud" `
  -UserName "cfo" -Password "2442" -Dynamic "+"
```
Expected: exit 0. Если отказ (требуется монопольный) — повторить без `-Dynamic` в нерабочее окно / сообщить пользователю, что нужно выгнать сеансы; НЕ рубить сеансы самостоятельно.
- [ ] **Step 7.4**: COM-проверка появления объекта: короткий скрипт `python -` НЕЛЬЗЯ (§27) → `TEST\doc_kompl_check_loaded.py`: подключиться, `buh.Метаданные.Документы.Найти("РасчетКомплектаций")` не None, `buh.Метаданные.Перечисления.Найти("СтатусыРасчетаКомплектаций")` не None, у документа 9 ТЧ и 4 макета (`Метаданные().Макеты` — §39-диагностика). Expected: все OK.

### Task 8: Smoke-тесты (COM, get-or-create, БЕЗ удаления/проведения)

**Files:**
- Create: `TEST\smoke_doc_kompl_analiz.py`
- Create: `TEST\smoke_doc_kompl_zapolnenie.py`
- Create: `TEST\smoke_doc_kompl_pechat.py`
- Create: `TEST\smoke_doc_kompl_dubli.py`

Общий хелпер в каждом скрипте (get-or-create по маркеру):
```python
MARKER = "SMOKE_DOC_KOMPL_v1"
def get_or_create_doc(buh):
    q = buh.NewObject("Запрос")
    q.Text = 'ВЫБРАТЬ ПЕРВЫЕ 1 Д.Ссылка ИЗ Документ.РасчетКомплектаций КАК Д ГДЕ Д.Комментарий = &М'
    q.SetParameter("М", MARKER)
    r = q.Execute().Выгрузить()
    if r.Количество() > 0:
        return r.Получить(0).Ссылка.ПолучитьОбъект()
    d = buh.Документы.РасчетКомплектаций.СоздатьДокумент()
    d.Дата = buh.ТекущаяДата()
    d.Заполнить(None)          # ОбработкаЗаполнения: статус/период/счета-дефолты
    d.Комментарий = MARKER
    return d
```

- [ ] **Step 8.1 — smoke_doc_kompl_analiz.py**: настройки эталона FINDINGS (Спецификация `000000004`, склад «СТІЛ МД МХП ОРІЛЬ виробничий» — найти ПОДОБНО; Период = 07.07.2026): очистить/заполнить СкладыОстатков; `doc.РассчитатьАнализ()`; проверки:
  - строк > 0; по КАЖДОЙ строке `Остаток == ВНорме + ПонадНорму` (float-допуск 1e-6);
  - `Σ СписаниеПоНормам.Количество == Σ ВНорме(по строкам >0)`; `Σ СписаниеСверхНормы.Количество == Σ ПонадНорму(>0)`;
  - для 3 случайных строк списания: `abs(Сумма*1.2 − соотв. ВНормеСумма/ПонадНормуСумма) <= 0.02`;
  - `buh.String(doc.Статус) == "Розрахунок виконано"`;
  - `doc.Записать()` → перечитать по номеру → ТЧ на месте (persist), Комментарий=MARKER.
  Expected: `ANALIZ PASS`.
- [ ] **Step 8.2 — smoke_doc_kompl_zapolnenie.py**: у документа из 8.1: создать/найти тестовые целевые доки по маркеру в Комментарии (`SMOKE_DOC_KOMPL_ЦЕЛЬ_КН_НОРМ` и т.п.): 2×КомплектацияНоменклатуры + 2×ПередачаМалоценныхАктивовВЭксплуатацию (`СоздатьДокумент(); Дата=Тек; Организация=орг склада; Записать()` — Запись, НЕ проведение); прописать в ДокументиКомплектації/ДокументиМалоценки строки склада; **отредактировать** одну строку СверхНормы (Количество += 1; Сумма пересчитать в скрипте как Окр(Кол*Цена,2)) и добавить одну ручную строку (Номенклатура/Склад из остатков, данные через `doc.ДанныеОстаткаДляСтроки(ном, скл)` — COM-вызов экспортной функции); `рез = doc.ЗаполнитьДокументыПоАнализу(True, True)`; проверки:
  - `рез.Заповнено >= 1`, `рез.Помилки.Количество() == 0`;
  - в целевой комплектации «Додаткова»: строка с отредактированным количеством присутствует (правки дошли!); все строки: СчетУчетаБУ заполнен, счёт 22 отсутствует; Сумма > 0;
  - в целевой малоценке: ТЧ МалоценныеАктивы непуста, все счета — 22x, НалоговоеНазначение заполнено;
  - `buh.String(doc.Статус) == "Документи створено"`; `doc.Записать()`.
  Expected: `ZAPOLNENIE PASS`.
- [ ] **Step 8.3 — smoke_doc_kompl_pechat.py**: по документу из 8.1: 6 печатей COM-инспекцией:
  - `СформироватьПечать(doc.ТабличнаяЧастьОстатков)` → ВысотаТаблицы > 5;
  - `СформироватьПечатьПланФакт/ПланФактЕтапи/АнализСС/АнализССОдна(doc.ТабличнаяЧастьОстатков)` → каждый ВысотаТаблицы > 5 и ШиринаТаблицы > 5 (макеты нашлись — не «Недопустимое значение параметра» §39);
  - `СформироватьПечатьАкт()` → высота > 8; найти в области текст «АКТ на списання понаднормових матеріалів» (1 раз, шапка объединена §9) и текст причины отредактированной строки.
  Expected: `PECHAT PASS`.
- [ ] **Step 8.4 — smoke_doc_kompl_dubli.py**: создать ВТОРОЙ документ с теми же Спецификация/Период/склады (маркер `SMOKE_DOC_KOMPL_v1_ДУБЛЬ`); `дубли = doc2.НайтиДубликатыРасчета()` → `дубли.Количество() >= 1`; `doc2.Записать()` НЕ падает. Expected: `DUBLI PASS`.
- [ ] **Step 8.5**: Прогнать все 4 через python-runner (полные пути). При любом FAIL — исправлять код/метаданные (Task 3–5), пере-грузить (Task 7 Steps 7.2–7.3 — только изменённые файлы) и повторить.

### Task 9: Регрессия обработки + валидаторы + ревью

- [ ] **Step 9.1**: md5-сверка файлов обработки с baseline из Task 0 Step 0.3 → идентичны (обработку не трогали).
- [ ] **Step 9.2**: Быстрая регрессия обработки: прогнать существующий `TEST\smoke_komplektacii_summy.py` → баланс 0 (живая база не сломана загрузкой).
- [ ] **Step 9.3**: `/1c-bsl-review` на `CFG\Documents\РасчетКомплектаций\Ext\ObjectModule.bsl` и Module.bsl формы → 0 критичных/высоких.
- [ ] **Step 9.4**: Контрольная выгрузка: `db-dump-xml` частично объектом Document.РасчетКомплектаций во временный каталог + diff с нашими файлами (расхождение только LF/CRLF — не дефект).

### Task 10: Adversarial-ревью (ultracode)

- [ ] **Step 10.1**: Workflow: 3 независимых верификатора диффа (корректность порта движка: инварианты источника строк заполнения; форма: DataPath/id/companion; метаданные: типы/синонимы/регистрация) — каждый возвращает CONFIRMED-дефекты; чинить подтверждённое, пере-грузить, пере-прогнать smoke.

### Task 11: Финализация

- [ ] **Step 11.1**: Если BASEBuh tracked в git (Task 0 Step 0.2) — скопировать все созданные/изменённые файлы CFG в worktree тем же путём.
- [ ] **Step 11.2**: git add (docs/plans+prompts, TEST\doc_kompl_*.py, smoke_doc_kompl_*.py, при трекинге — файлы BASEBuh) → commit на ветке worktree: `feat(конфигурация BuhBud): документ РасчетКомплектаций — сохраняемое рабочее место списания за СС`.
- [ ] **Step 11.3**: Итоговый отчёт пользователю: что сделано, полные пути, инструкция проверки в клиенте (Склад → Розрахунок комплектацій за СС; проверить закладки/кнопки/печати — управляемую форму COM не рендерит), список smoke PASS.

---

## Self-review (выполнен)

- **Покрытие спеки**: метаданные §4 → Task 2; форма §5 → Task 5; движок §6 → Task 3; поведение §7 → Task 3.6 + 5.3; права §8 → Task 6; инфраструктура §9 → Task 1/7; тесты §10 → Task 8; критерии §11 → Task 8/9/11. Печати 6 шт. → Task 4 (макеты) + 3.6 (Акт) + 5.3 (кнопки).
- **Типы согласованы**: ЗаполнитьТаблицыСписания/ЗаполнитьСтрокуСписания/ДанныеОстаткаДляСтроки/НайтиДубликатыРасчета/СформироватьПечатьАкт — одинаковые имена в Task 3 (объявление), Task 5 (вызовы из формы), Task 8 (COM-вызовы).
- **Плейсхолдеров нет**; строки «копировать вербатим X–Y» — намеренная инструкция копирования, не заглушка.
