# Счёт 665 в отчёт сверки А_ВзСС↔Бухгалтерия — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Добавить бухгалтерский счёт 665 (наряду с 661/663) в BuhBud-сторону внешнего СКД-отчёта `А_СравнитьОстаткиВзаиморасчетыСотрудникиЕРПсBASБухгалтерия`.

**Architecture:** Одна функциональная правка — расширить IN-список счетов в COM-запросе к BuhBud (`ПолучитьОстаткиBuhBud`). 665 имеет идентичное 661/663 субконто (ФизическиеЛица, пассив) → сливается в те же бакеты ФЛ×Орг. Плюс обновление текстовых меток «661/663»→«661/663/665». Пересборка `.erf` (erf-build). ЕРП-сторона и СКД-структура не меняются.

**Tech Stack:** 1C BSL (внешний отчёт СКД), Python 3.13 + win32com (BuhBud + BaseERP), skill `erf-build`/`erf-validate`.

**Среда:** BuhBud `Srvr="localhost";Ref="bas_industrialbud";Usr="cfo";Pwd="2442"`; BaseERP `...Ref="BaseERP";Usr="Администратор";Pwd="24043"`; python `C:\Python313\python.exe`; v8 `C:\Program Files\1cv8\8.3.20.1914\bin`. Папка отчёта: `_Rarzrabotki/Отчеты/А_СравнитьОстаткиВзаиморасчетыСотрудникиЕРПсBASБухгалтерия`. Спека: `docs/superpowers/specs/2026-06-04-665-vzss-report-design.md`.

**Коммиты:** только файлы отчёта/скриптов (`git add <путь>`), НЕ `git add -A`.

---

## Task 1: RED/GREEN COM-зеркало запроса (665 surfaces + 661/663 без регрессии)

**Files:**
- Create: `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/verify_665_vzss_report.py`

- [ ] **Step 1: Написать скрипт-зеркало**

Скрипт повторяет BuhBud-сторону отчёта (фильтр орг по `А_ВБалансе` из ЕРП, агрегация по ФЛ×ЕДРПОУ, Кт−Дт)
для двух наборов счетов и показывает дельту 665.

```python
# -*- coding: utf-8 -*-
import io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client as w
v8 = w.Dispatch("V83.COMConnector")
buh = v8.Connect('Srvr="localhost";Ref="bas_industrialbud";Usr="cfo";Pwd="2442"')
erp = v8.Connect('Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')

# ЕДРПОУ организаций в балансе (как фильтр отчёта)
qo = erp.NewObject("Запрос")
qo.Текст = 'ВЫБРАТЬ КодПоЕДРПОУ КАК Е ИЗ Справочник.Организации ГДЕ А_ВБалансе И КодПоЕДРПОУ<>""'
to = qo.Выполнить().Выгрузить()
edrpou = sorted({str(to.Получить(i).Е).strip() for i in range(to.Количество())})

def buh_side(codes):
    inlist = ", ".join(f'"{c}"' for c in codes)
    q = buh.NewObject("Запрос")
    arr = buh.NewObject("Массив")
    for e in edrpou: arr.Добавить(e)
    q.УстановитьПараметр("Орг", arr)
    q.Текст = f'''ВЫБРАТЬ
 ВЫРАЗИТЬ(Ост.Субконто1 КАК Справочник.ФизическиеЛица).А_ИдКод КАК ИдКод,
 ВЫРАЗИТЬ(Ост.Субконто1 КАК Справочник.ФизическиеЛица).Наименование КАК ФЛ,
 Ост.Организация.КодПоЕДРПОУ КАК Е,
 СУММА(Ост.СуммаОборотКт) КАК Кт, СУММА(Ост.СуммаОборотДт) КАК Дт,
 СУММА(Ост.СуммаКонечныйОстатокКт-Ост.СуммаКонечныйОстатокДт) КАК Кон
ИЗ РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты(ДАТАВРЕМЯ(2025,12,1,0,0,0),ДАТАВРЕМЯ(2025,12,31,23,59,59)) КАК Ост
ГДЕ Ост.Счет.Код В ({inlist}) И Ост.Субконто1 ССЫЛКА Справочник.ФизическиеЛица
 И Ост.Организация.КодПоЕДРПОУ В (&Орг)
СГРУППИРОВАТЬ ПО ВЫРАЗИТЬ(Ост.Субконто1 КАК Справочник.ФизическиеЛица).А_ИдКод,
 ВЫРАЗИТЬ(Ост.Субконто1 КАК Справочник.ФизическиеЛица).Наименование, Ост.Организация.КодПоЕДРПОУ'''
    t = q.Выполнить().Выгрузить()
    d = {}
    for i in range(t.Количество()):
        r = t.Получить(i)
        d[(str(r.ИдКод).strip(), str(r.Е).strip())] = (r.ФЛ, round(r.Кт,2), round(r.Дт,2), round(r.Кон,2))
    return d

old = buh_side(["661","663"])
new = buh_side(["661","663","665"])
print(f"Орг в балансе: {edrpou}")
print(f"661/663: {len(old)} строк | 661/663/665: {len(new)} строк")

# Регрессия: ключи без 665-движений не изменились
changed = [k for k in old if old[k] != new.get(k)]
print(f"\n=== Изменённые/новые из-за 665 ({len([k for k in new if new[k]!=old.get(k)])}) ===")
for k in sorted(set(new) - set(old)) + sorted(changed):
    fl, kt, dt, kon = new[k]
    o = old.get(k)
    print(f"  ИдКод={k[0]} ЕДРПОУ={k[1]} {fl[:20]:20s} | НОВ Кт={kt} Дт={dt} Кон={kon} | СТАР={o}")
print(f"\nКонтроль Алов (ожид. кон=0, выплата=21433.49):")
for k,v in new.items():
    if v[0].startswith("Алов"): print(f"  {k} -> {v}")
```

- [ ] **Step 2: Запустить (baseline)**

Run: `& "C:\Python313\python.exe" "_Rarzrabotki\notebook\knowledge_А_ОтражениеЗПпоКазне\Python\test\verify_665_vzss_report.py"`
Expected: 661/663/665 строк >= 661/663; среди новых — Алов (ІНДЕПТ СТІЛ, ЕДРПОУ 44590697) Кт 21433.49 / Дт 21433.49 / Кон 0; ключи без 665 не изменились (регрессия чистая).

- [ ] **Step 3: Commit**

```bash
git add "_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/verify_665_vzss_report.py"
git commit -m "test(отчёт665): COM-зеркало — 665 surfaces, 661/663 без регрессии"
```

---

## Task 2: Правка запроса + комментарии (ObjectModule.bsl)

**Files:**
- Modify: `_Rarzrabotki/Отчеты/А_СравнитьОстаткиВзаиморасчетыСотрудникиЕРПсBASБухгалтерия/Ext/ObjectModule.bsl`

- [ ] **Step 1: Функциональная правка (IN-список счетов)**

old_string:
```bsl
		|	Ост.Счет.Код В (""661"", ""663"")
```
new_string:
```bsl
		|	Ост.Счет.Код В (""661"", ""663"", ""665"")
```

- [ ] **Step 2: Комментарий блока 2 (строка ~17)**

old_string:
```bsl
	// 2. BuhBud: Хозрасчетный 661/663, субконто1 ФЛ, разрез Организация(ЕДРПОУ)
```
new_string:
```bsl
	// 2. BuhBud: Хозрасчетный 661/663/665, субконто1 ФЛ, разрез Организация(ЕДРПОУ)
```

- [ ] **Step 3: Комментарий функции (строка ~122)**

old_string:
```bsl
// BuhBud (COM): РегБух.Хозрасчетный.ОстаткиИОбороты, счета 661/663, субконто1=ФЛ, разрез Организация(ЕДРПОУ).
```
new_string:
```bsl
// BuhBud (COM): РегБух.Хозрасчетный.ОстаткиИОбороты, счета 661/663/665, субконто1=ФЛ, разрез Организация(ЕДРПОУ).
```

- [ ] **Step 4: Сообщение об ошибке (строка ~186)**

old_string:
```bsl
		Сообщить("Помилка запиту BuhBud (661/663): " + ОписаниеОшибки());
```
new_string:
```bsl
		Сообщить("Помилка запиту BuhBud (661/663/665): " + ОписаниеОшибки());
```

---

## Task 3: Метки заголовков и справки

**Files:**
- Modify: `.../Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml`
- Modify: `_Rarzrabotki/Отчеты/А_СравнитьОстаткиВзаиморасчетыСотрудникиЕРПсBASБухгалтерия.xml`
- Modify: `.../Ext/Help/ru.html`, `.../Ext/Help/uk.html`

- [ ] **Step 1: Заголовок СКД (Template.xml) — ru и uk**

old_string:
```
				<v8:content>Сверка взаиморасчётов с сотрудниками: ЕРП (Форма1) vs Бухгалтерия 661/663</v8:content>
```
new_string:
```
				<v8:content>Сверка взаиморасчётов с сотрудниками: ЕРП (Форма1) vs Бухгалтерия 661/663/665</v8:content>
```

old_string:
```
				<v8:content>Звірка взаєморозрахунків зі співробітниками: ЕРП (Форма1) vs Бухгалтерія 661/663</v8:content>
```
new_string:
```
				<v8:content>Звірка взаєморозрахунків зі співробітниками: ЕРП (Форма1) vs Бухгалтерія 661/663/665</v8:content>
```

- [ ] **Step 2: Синоним отчёта (.xml) — uk и ru**

old_string:
```
					<v8:content>Звірка взаєморозрахунків зі співробітниками: ЕРП (Форма1) ↔ Бухгалтерія 661/663</v8:content>
```
new_string:
```
					<v8:content>Звірка взаєморозрахунків зі співробітниками: ЕРП (Форма1) ↔ Бухгалтерія 661/663/665</v8:content>
```

old_string:
```
					<v8:content>Сверка взаиморасчётов с сотрудниками: ЕРП (Форма1) ↔ Бухгалтерия 661/663</v8:content>
```
new_string:
```
					<v8:content>Сверка взаиморасчётов с сотрудниками: ЕРП (Форма1) ↔ Бухгалтерия 661/663/665</v8:content>
```

- [ ] **Step 3: Help ru.html (строки 3, 6)**

old_string: `бухгалтерских счетов <b>661 + 663</b>` → new_string: `бухгалтерских счетов <b>661 + 663 + 665</b>`
old_string: `пассивные счета 661/663 берутся` → new_string: `пассивные счета 661/663/665 берутся`

- [ ] **Step 4: Help uk.html (строки 3, 6)**

old_string: `бухгалтерських рахунків <b>661 + 663</b>` → new_string: `бухгалтерських рахунків <b>661 + 663 + 665</b>`
old_string: `пасивні рахунки 661/663 беруться` → new_string: `пасивні рахунки 661/663/665 беруться`

- [ ] **Step 5: Commit правок исходника**

```bash
git add "_Rarzrabotki/Отчеты/А_СравнитьОстаткиВзаиморасчетыСотрудникиЕРПсBASБухгалтерия/" "_Rarzrabotki/Отчеты/А_СравнитьОстаткиВзаиморасчетыСотрудникиЕРПсBASБухгалтерия.xml"
git commit -m "feat(отчёт665): добавить счёт 665 в BuhBud-сторону сверки А_ВзСС"
```

---

## Task 4: Пересборка .erf

**Files:**
- Modify: `_Rarzrabotki/Отчеты/А_СравнитьОстаткиВзаиморасчетыСотрудникиЕРПсBASБухгалтерия.erf`

- [ ] **Step 1: erf-build из папки-исходника**

Через skill `erf-build`: собрать `.erf` из `_Rarzrabotki/Отчеты/А_СравнитьОстаткиВзаиморасчетыСотрудникиЕРПсBASБухгалтерия/`
в файл `....erf` (v8 `C:\Program Files\1cv8\8.3.20.1914\bin`). Опц. `erf-validate`.
Expected: сборка без ошибок, `.erf` обновлён.

- [ ] **Step 2: Commit .erf**

```bash
git add "_Rarzrabotki/Отчеты/А_СравнитьОстаткиВзаиморасчетыСотрудникиЕРПсBASБухгалтерия.erf"
git commit -m "build(отчёт665): пересобранный .erf со счётом 665"
```

---

## Task 5: GREEN + handoff

- [ ] **Step 1: GREEN — повторный прогон COM-зеркала**

Run: `& "C:\Python313\python.exe" "_Rarzrabotki\notebook\knowledge_А_ОтражениеЗПпоКазне\Python\test\verify_665_vzss_report.py"`
Expected: 665-строки присутствуют (Алов ІНДЕПТ СТІЛ Кт 21433.49 / Дт 21433.49 / Кон 0); ключи без 665 не изменились.
(Зеркало = та же логика, что в .erf; полный прогон отчёта в UI — за пользователем.)

- [ ] **Step 2: Handoff**

Сообщить пользователю: `.erf` пересобран в основной конфигурации. Перезагрузить внешний отчёт в карточке
«Дополнительные обработки и отчёты» (или открыть файл), сформировать за декабрь 2025 — в Бух-стороне появятся 665.

---

## Self-Review

- **Покрытие спеки:** §5.1 запрос → Task 2 Step 1; §5.2 комментарии → Task 2; §5.3-5.5 метки → Task 3; §5.6 сборка → Task 4; §7 RED/GREEN → Task 1/5. Покрыто.
- **Плейсхолдеры:** old/new_string дословны; скрипт полон. Нет TBD.
- **Согласованность:** строка запроса `В ("661", "663", "665")` едина между спекой, Task 2 и COM-зеркалом (Task 1).
