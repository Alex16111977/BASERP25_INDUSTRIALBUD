# ПланФактССБухгалтерія: «Аналітика» строкой, ОН плана из СС, домики по реквизиту — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** В бухгалтерском отчёте «План на факт за матеріалами» разрез «Аналітика» становится текстом (план `СокрЛП(К.НоменклатураСС)`, факт `СокрЛП(Номенклатура.Наименование)`) с отдельным измерением «Номенклатура» у факта; общая назва плана берётся из строки СС; множитель плана — реквизит `СС.КоличествоДомов` (0 → 1).

**Architecture:** Внешний отчёт с программной компоновкой СКД: BSL-функция `ПолучитьДанные(Период)` строит плоскую ТЗ (цикл по зонтикам, UNION план/факт, постобработки ETC), схема получает её внешним набором `Детали`. Меняются: текст запроса грейна, карта «назва → етап», синтетические строки ETC, описание ТЗ, два поля схемы СКД. Спека: `docs/superpowers/specs/2026-09-04-planfaktss-analitika-on-domiki-design.md`.

**Tech Stack:** 1С:Предприятие 8.3.20, BSL, СКД (DataCompositionSchema XML), Python 3 + `win32com` (`V83.COMConnector`) для тестов, PowerShell `epf-build.ps1` для сборки `.erf`. База `Srvr="SQLSERVER";Ref="bas_industrialbud";Usr="cfo";Pwd="2442"` (для COM из Python подходит и `Srvr="localhost"`).

---

## Карта файлов

| Файл | Роль | Действие |
|---|---|---|
| `_Rarzrabotki/Отчеты/ПланФактССБухгалтерія.xml` | корень внешнего отчёта (uuid, ClassId) | не менять; добавить в git |
| `_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Ext/ObjectModule.bsl` | запрос грейна, постобработки, ТЗ | **изменить** |
| `_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml` | схема СКД (поля набора «Детали») | **изменить** |
| `_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Templates/ОсновнаяСхемаКомпоновкиДанных.xml` | корневой файл макета | не менять |
| `_Rarzrabotki/Отчеты/ПланФактССБухгалтерія.erf` | собранный отчёт (в git НЕ добавлять) | пересобрать |
| `_Rarzrabotki/Python/test/test_planfaktss_grain.py` | контракт запроса грейна (Rule #-1) | переписать |
| `_Rarzrabotki/Python/test/test_planfaktss_mapping.py` | карта «назва → етап» | изменить запрос |
| `_Rarzrabotki/Python/test/test_planfaktss_poluchit_dannye.py` | контракт ТЗ `ПолучитьДанные` через .erf | переписать |
| `_Rarzrabotki/Python/test/test_planfaktss_vata_oon.py` | кейс «Мінеральна вата 50» | создать |
| `_Rarzrabotki/Python/test/test_planfaktss_etc.py` | инвариант ETC | дополнить проверкой синтетики |
| `_Rarzrabotki/Python/test/smoke_planfaktss_report.py` | полный путь `СформироватьОтчет` | без изменений, прогнать |
| `_Rarzrabotki/Python/planfaktss_raskhozhdeniya_on.py` | выгрузка 67 расхождений ОН | создать |
| `_Rarzrabotki/Отчеты/ПланФактССБухгалтерія_расхождения_ОН.md` | список для чистки данных | создать (скриптом) |
| `_Rarzrabotki/Отчеты/ПРОМТ_ПланФактССБухгалтерія_Материали.md` | самодостаточный промт отчёта | обновить §1, §2, §4.1, §5 |
| `C:\Users\support1c\.claude\projects\C--Configuration-downloads-BASERP25\memory\planfaktss_buhbud_report.md` + `MEMORY.md` | память проекта | создать + строка в индексе |

**Соглашения для всех задач:**
- Рабочий каталог — worktree `C:\Configuration_downloads\BASERP25\.claude\worktrees\youthful-bohr-fb6ce3`. Обозначение `<WT>` = этот путь, `<MAIN>` = `C:\Configuration_downloads\BASERP25`.
- Оба исходника отчёта — UTF-8 **с BOM**, концы строк **CRLF**, отступы табами. После каждой правки прогонять нормализацию (Task 5, шаг 8).
- Python-тесты запускать из `<MAIN>\_Rarzrabotki\Python\test` (они ссылаются на `.erf` в `<MAIN>`).
- В именах карточек СС IRS стоит **кириллическая «С»** («IRС»), у подразделений — латинская «C». В тестах имена нормализуются функцией `лат()` (кириллическая U+0421 → латинская C), чтобы не зависеть от раскладки.

---

### Task 0: Исходники отчёта в git (baseline)

Файлы отчёта в главном каталоге никогда не добавлялись в git, в worktree их нет. Нужен baseline-коммит, чтобы дальнейший diff был читаем.

**Files:**
- Copy: `<MAIN>/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія.xml` → `<WT>/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія.xml`
- Copy: `<MAIN>/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/` → `<WT>/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/`

- [ ] **Step 1: Скопировать исходники в worktree**

```bash
cp "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія.xml" "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія.xml"
cp -r "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія" "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3/_Rarzrabotki/Отчеты/"
```

- [ ] **Step 2: Проверить, что скопировались ровно 4 файла и .erf не попал**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3" && find "_Rarzrabotki/Отчеты/ПланФактССБухгалтерія" -type f && ls "_Rarzrabotki/Отчеты/" | grep "ПланФактССБухгалтерія"
```

Expected: `Ext/ObjectModule.bsl`, `Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml`, `Templates/ОсновнаяСхемаКомпоновкиДанных.xml`, корневой `ПланФактССБухгалтерія.xml`; файла `.erf` в worktree нет.

- [ ] **Step 3: Commit baseline**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3" && git add "_Rarzrabotki/Отчеты/ПланФактССБухгалтерія.xml" "_Rarzrabotki/Отчеты/ПланФактССБухгалтерія" && git commit -m "chore(ПланФактССБухгалтерія): исходники отчёта в git (baseline до доработки 04.09.2026)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 1: Контракт нового запроса грейна (Rule #-1, тест до BSL)

Новый текст запроса сначала живёт в Python-тесте и гоняется на живой базе. Только после PASS он переносится в BSL (Task 5).

**Files:**
- Rewrite: `<MAIN>/_Rarzrabotki/Python/test/test_planfaktss_grain.py` (и копия в `<WT>` — см. шаг 3)

- [ ] **Step 1: Переписать тест целиком**

```python
# -*- coding: utf-8 -*-
"""Контракт данных отчёта ПланФактСС (bas_industrialbud): множители домиков, типы аналитики, эталоны.

С 04.09.2026:
  - множитель плана = СС.КоличествоДомов (0 -> 1), подсчёт дочерних подразделений по имени убран;
  - «Аналитика» — строка: план СокрЛП(К.НоменклатураСС), факт СокрЛП(Номенклатура.Наименование);
  - «Номенклатура» — ссылка только у факта;
  - общая назва плана — из строки СС (К.ОбщееНазвание), карточная — только при пустой строке.
ТЕКСТ_ГРЕЙНА обязан быть 1:1 с Запрос.Текст в ВыполнитьЗапросПоЗонтику (без удвоения кавычек).
Эталон «План IRS = 11 296 725,24» — выгрузка ЕРП-отчёта C:\\!OBMIN-OLD-NEW\\отчеты\\Звіт.xlsx
(период по 31.07.2026), он же = сумма норм СС IRS × 6/7. Факт с ЕРП НЕ сверяем.
"""
import sys

if sys.stdout.encoding != 'utf-8':
    sys.stdout.reconfigure(encoding='utf-8')

import win32com.client

CONN = 'Srvr="SQLSERVER";Ref="bas_industrialbud";Usr="cfo";Pwd="2442"'


def лат(s):
    """Кириллическая «С» (U+0421) -> латинская «C»: имена СС и подразделений IRS набраны по-разному."""
    return s.replace("\u0421", "C").replace("\u0441", "c")


МНОЖИТЕЛИ = {
    "МД IRC 2026 15 м (МД IRC 2026)": 6,
    "МД IRC 2026 30 м (МД IRC 2026)": 7,
    "МД Вооз ЕМS Мала 2026 / Цех_МД Вооз ЕМS Мала 2026": 1,
    "МД МХП ОРІЛЬ / СТІЛ МД МХП ОРІЛЬ": 1,
    "МД ПРООН Черкаси  ДСНС / СТІЛ Цех МД ПРООН": 1,
    "МД ООН 2026": 10,
    "МД ООН Админ": 4,
}
МНОЖИТЕЛИ = {лат(k): v for k, v in МНОЖИТЕЛИ.items()}

ЗОНТИК_IRS = "МД IRC 2026"
ЭТАЛОН_ПЛАН_IRS = 11296725.24
ЭТАЛОН_ФАКТ_IRS = 4595459.79

ТЕКСТ_ГРЕЙНА = """
ВЫБРАТЬ
	СС.Ссылка КАК СС,
	СС.Подразделение КАК Подр,
	ВЫБОР
		КОГДА СС.КоличествоДомов > 0
			ТОГДА СС.КоличествоДомов
		ИНАЧЕ 1
	КОНЕЦ КАК Домиков
ПОМЕСТИТЬ ВТ_СС
ИЗ
	Справочник.СтруктураСебестоимости КАК СС
ГДЕ
	НЕ СС.ЭтоГруппа
	И СС.Активная
	И НЕ СС.ПометкаУдаления
	И СС.Подразделение = &Зонтик
;
////////////////////////////////////////////////////////////////////////////////
ВЫБРАТЬ
	ВТ_СС.Подр КАК Подразделение,
	ВЫРАЗИТЬ(ВЫБОР
			КОГДА К.Этап.Наименование ПОДОБНО "%Монтаж%"
				ТОГДА "Монтаж"
			ИНАЧЕ "Виробництво"
		КОНЕЦ КАК СТРОКА(50)) КАК Категория,
	К.Этап КАК Этап,
	ЕСТЬNULL(ВЫБОР
			КОГДА К.ОбщееНазвание <> ЗНАЧЕНИЕ(Справочник.ОбщиеНазванияНоменклатуры.ПустаяСсылка)
				ТОГДА К.ОбщееНазвание
			ИНАЧЕ К.Номенклатура.ОбщееНазваниеНоменклатуры
		КОНЕЦ, ЗНАЧЕНИЕ(Справочник.ОбщиеНазванияНоменклатуры.ПустаяСсылка)) КАК ОбщееНазвание,
	СокрЛП(К.НоменклатураСС) КАК Аналитика,
	ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка) КАК Номенклатура,
	К.Единица КАК Единица,
	ВТ_СС.СС КАК Документ,
	К.Количество * ВТ_СС.Домиков КАК ПланКол,
	К.Сумма * ВТ_СС.Домиков КАК ПланГрн,
	0 КАК ФактКол,
	0 КАК ФактГрн
ПОМЕСТИТЬ ВТ_Детали
ИЗ
	Справочник.СтруктураСебестоимости.Комплектующие КАК К
		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_СС КАК ВТ_СС
		ПО К.Ссылка = ВТ_СС.СС

ОБЪЕДИНИТЬ ВСЕ

ВЫБРАТЬ
	&Зонтик,
	ВЫРАЗИТЬ("Виробництво" КАК СТРОКА(50)),
	ЗНАЧЕНИЕ(Справочник.ЭтапыРабот.ПустаяСсылка),
	Т.Номенклатура.ОбщееНазваниеНоменклатуры,
	СокрЛП(Т.Номенклатура.Наименование),
	Т.Номенклатура,
	Т.ЕдиницаИзмерения,
	Д.Ссылка,
	0,
	0,
	Т.Количество,
	ВЫБОР
		КОГДА Д.СуммаВключаетНДС
			ТОГДА Т.Сумма
		ИНАЧЕ Т.Сумма + Т.СуммаНДС
	КОНЕЦ
ИЗ
	Документ.ПоступлениеТоваровУслуг.Товары КАК Т
		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ПоступлениеТоваровУслуг КАК Д
		ПО Т.Ссылка = Д.Ссылка
ГДЕ
	Д.Проведен
	И Д.Дата МЕЖДУ &НачП И &КонП
	И Д.Подразделение В ИЕРАРХИИ (&Зонтик)
;
////////////////////////////////////////////////////////////////////////////////
ВЫБРАТЬ
	ВТ_Детали.Подразделение КАК Подразделение,
	ВТ_Детали.Категория КАК Категория,
	ВТ_Детали.Этап КАК Этап,
	ВТ_Детали.ОбщееНазвание КАК ОбщееНазвание,
	ВТ_Детали.Аналитика КАК Аналитика,
	ВТ_Детали.Номенклатура КАК Номенклатура,
	ВТ_Детали.Единица КАК Единица,
	ВТ_Детали.Документ КАК Документ,
	СУММА(ВТ_Детали.ПланКол) КАК ПланКол,
	СУММА(ВТ_Детали.ПланГрн) КАК ПланГрн,
	СУММА(ВТ_Детали.ФактКол) КАК ФактКол,
	СУММА(ВТ_Детали.ФактГрн) КАК ФактГрн
ИЗ
	ВТ_Детали КАК ВТ_Детали
СГРУППИРОВАТЬ ПО
	ВТ_Детали.Подразделение,
	ВТ_Детали.Категория,
	ВТ_Детали.Этап,
	ВТ_Детали.ОбщееНазвание,
	ВТ_Детали.Аналитика,
	ВТ_Детали.Номенклатура,
	ВТ_Детали.Единица,
	ВТ_Детали.Документ
"""


def подключиться():
    v8 = win32com.client.Dispatch("V83.COMConnector")
    return v8.Connect(CONN)


def тест_множители(b):
    z = b.NewObject("Запрос")
    z.Текст = """
    ВЫБРАТЬ
    	СС.Наименование КАК СС,
    	ВЫБОР КОГДА СС.КоличествоДомов > 0 ТОГДА СС.КоличествоДомов ИНАЧЕ 1 КОНЕЦ КАК Домиков
    ИЗ
    	Справочник.СтруктураСебестоимости КАК СС
    ГДЕ
    	НЕ СС.ЭтоГруппа
    	И СС.Активная
    	И НЕ СС.ПометкаУдаления
    """
    t = z.Выполнить().Выгрузить()
    факт = {лат(t[i].СС): int(t[i].Домиков) for i in range(t.Количество())}
    assert факт == МНОЖИТЕЛИ, f"Множители домиков (КоличествоДомов) разошлись: {факт}"
    print("OK: множители домиков", факт)


def найти_зонтик(b, имя):
    z = b.NewObject("Запрос")
    z.Текст = """
    ВЫБРАТЬ РАЗЛИЧНЫЕ СС.Подразделение КАК Подр
    ИЗ Справочник.СтруктураСебестоимости КАК СС
    ГДЕ НЕ СС.ЭтоГруппа И СС.Активная И НЕ СС.ПометкаУдаления
    	И СС.Подразделение <> ЗНАЧЕНИЕ(Справочник.Подразделения.ПустаяСсылка)
    """
    зонтики = z.Выполнить().Выгрузить()
    for i in range(зонтики.Количество()):
        if лат(зонтики[i].Подр.Наименование) == лат(имя):
            return зонтики[i].Подр
    raise AssertionError(f"Не найден зонтик {имя!r}")


def выполнить_грейн(b, зонтик, нач, кон):
    z = b.NewObject("Запрос")
    z.Текст = ТЕКСТ_ГРЕЙНА
    z.УстановитьПараметр("Зонтик", зонтик)
    z.УстановитьПараметр("НачП", нач)
    z.УстановитьПараметр("КонП", кон)
    return z.Выполнить().Выгрузить()


def тест_суммы(b):
    z = b.NewObject("Запрос")
    z.Текст = "ВЫБРАТЬ ДАТАВРЕМЯ(1, 1, 1) КАК Н, КОНЕЦПЕРИОДА(ДАТАВРЕМЯ(2026, 7, 31), ДЕНЬ) КАК К"
    гр = z.Выполнить().Выгрузить()[0]
    зонтик = найти_зонтик(b, ЗОНТИК_IRS)
    t = выполнить_грейн(b, зонтик, гр.Н, гр.К)

    план = sum(t[i].ПланГрн for i in range(t.Количество()))
    факт = sum(t[i].ФактГрн for i in range(t.Количество()))

    # Независимая проверка плана: сумма норм × КоличествоДомов напрямую по ТЧ.
    z = b.NewObject("Запрос")
    z.Текст = """
    ВЫБРАТЬ
    	СУММА(К.Сумма * ВЫБОР КОГДА К.Ссылка.КоличествоДомов > 0 ТОГДА К.Ссылка.КоличествоДомов ИНАЧЕ 1 КОНЕЦ) КАК План
    ИЗ Справочник.СтруктураСебестоимости.Комплектующие КАК К
    ГДЕ НЕ К.Ссылка.ЭтоГруппа И К.Ссылка.Активная И НЕ К.Ссылка.ПометкаУдаления
    	И К.Ссылка.Подразделение = &Зонтик
    """
    z.УстановитьПараметр("Зонтик", зонтик)
    план_прямой = z.Выполнить().Выгрузить()[0].План
    assert abs(план - план_прямой) < 0.01, f"План IRS из грейна {план:,.2f} != норма×домов {план_прямой:,.2f}"
    assert abs(план - ЭТАЛОН_ПЛАН_IRS) < 0.01, (
        f"План IRS = {план:,.2f}, эталон {ЭТАЛОН_ПЛАН_IRS:,.2f}. Если норма×домов ({план_прямой:,.2f}) "
        f"совпала с грейном — СС IRS правили после 06.08.2026; обновить ЭТАЛОН_ПЛАН_IRS только после "
        f"подтверждения пользователем.")
    assert abs(факт - ЭТАЛОН_ФАКТ_IRS) < 0.01, f"Факт IRS = {факт:,.2f}, эталон {ЭТАЛОН_ФАКТ_IRS:,.2f}"
    print(f"OK: IRS план={план:,.2f} факт={факт:,.2f}, строк={t.Количество()}")


def тест_типы_аналитики(b):
    z = b.NewObject("Запрос")
    z.Текст = "ВЫБРАТЬ ДАТАВРЕМЯ(2026, 1, 1) КАК Н, КОНЕЦПЕРИОДА(ДАТАВРЕМЯ(2026, 9, 30), ДЕНЬ) КАК К"
    гр = z.Выполнить().Выгрузить()[0]
    зонтик = найти_зонтик(b, "МД ООН Админ")
    t = выполнить_грейн(b, зонтик, гр.Н, гр.К)
    план_строк = факт_строк = 0
    for i in range(t.Количество()):
        r = t[i]
        assert isinstance(r.Аналитика, str), f"Аналитика не строка: {type(r.Аналитика)} в строке {i}"
        assert r.Аналитика == r.Аналитика.strip(), f"Аналитика не обрезана СокрЛП: {r.Аналитика!r}"
        если_факт = (r.ФактКол != 0) or (r.ФактГрн != 0)
        если_план = (r.ПланКол != 0) or (r.ПланГрн != 0)
        if если_факт:
            факт_строк += 1
            assert not r.Номенклатура.Пустая(), f"У факта пустая Номенклатура: {r.Аналитика!r}"
            assert r.Аналитика == r.Номенклатура.Наименование.strip(), (
                f"Факт: Аналитика {r.Аналитика!r} != СокрЛП(Наименование) {r.Номенклатура.Наименование.strip()!r}")
        if если_план:
            план_строк += 1
            assert r.Номенклатура.Пустая(), f"У плана заполнена Номенклатура: {r.Аналитика!r}"
            assert r.Аналитика != "", "У плана пустая Аналитика (НоменклатураСС пуст?)"
    assert план_строк > 0 and факт_строк > 0, f"план_строк={план_строк} факт_строк={факт_строк}"
    print(f"OK: типы аналитики ООН Админ — план {план_строк} строк, факт {факт_строк} строк")


if __name__ == "__main__":
    b = подключиться()
    тест_множители(b)
    тест_суммы(b)
    тест_типы_аналитики(b)
    print("GRAIN CONTRACT PASSED")
```

- [ ] **Step 2: Прогнать — запрос должен выполниться на живой базе**

```bash
cd "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test" && python test_planfaktss_grain.py
```

Expected: три строки `OK: …` и `GRAIN CONTRACT PASSED`. Если падает `тест_суммы` по `ЭТАЛОН_ПЛАН_IRS`, но «грейн = норма×домов» прошёл — СС IRS правили после 06.08.2026: **не** править эталон самостоятельно, сообщить пользователю цифру и спросить. Если падает синтаксис запроса — исправлять текст здесь (в BSL он ещё не перенесён).

- [ ] **Step 3: Скопировать тест в worktree и закоммитить**

```bash
cp "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test/test_planfaktss_grain.py" "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3/_Rarzrabotki/Python/test/test_planfaktss_grain.py"
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3" && git add _Rarzrabotki/Python/test/test_planfaktss_grain.py && git commit -m "test(ПланФактССБухгалтерія): контракт грейна — КоличествоДомов, строковая Аналитика, Номенклатура у факта

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: Карта «назва → етап» по назве строки СС

**Files:**
- Modify: `<MAIN>/_Rarzrabotki/Python/test/test_planfaktss_mapping.py` (константа `ТЕКСТ` и имя зонтика IRS)

- [ ] **Step 1: Заменить константу `ТЕКСТ`**

Старый блок (от `ТЕКСТ = """` до закрывающих `"""`) заменить на:

```python
ТЕКСТ = """
ВЫБРАТЬ
	ЕСТЬNULL(ВЫБОР
			КОГДА К.ОбщееНазвание <> ЗНАЧЕНИЕ(Справочник.ОбщиеНазванияНоменклатуры.ПустаяСсылка)
				ТОГДА К.ОбщееНазвание.Наименование
			ИНАЧЕ К.Номенклатура.ОбщееНазваниеНоменклатуры.Наименование
		КОНЕЦ, "") КАК ОбщееНазвание,
	К.Этап КАК Этап,
	ВЫБОР
		КОГДА К.Этап.Наименование ПОДОБНО "%Монтаж%"
			ТОГДА "Монтаж"
		ИНАЧЕ "Виробництво"
	КОНЕЦ КАК Категория
ИЗ
	Справочник.СтруктураСебестоимости.Комплектующие КАК К
ГДЕ
	К.Ссылка.Подразделение = &Зонтик
	И НЕ К.Ссылка.ЭтоГруппа
	И К.Ссылка.Активная
	И НЕ К.Ссылка.ПометкаУдаления
	И К.Этап <> ЗНАЧЕНИЕ(Справочник.ЭтапыРабот.ПустаяСсылка)
	И (К.ОбщееНазвание <> ЗНАЧЕНИЕ(Справочник.ОбщиеНазванияНоменклатуры.ПустаяСсылка)
		ИЛИ К.Номенклатура.ОбщееНазваниеНоменклатуры <> ЗНАЧЕНИЕ(Справочник.ОбщиеНазванияНоменклатуры.ПустаяСсылка))
"""
```

- [ ] **Step 2: Поправить имя зонтика IRS в проверке**

Старое:
```python
        if подр.Наименование == "МД IRS 2026":
```
Новое:
```python
        if подр.Наименование.replace("\u0421", "C") == "МД IRC 2026":
```

- [ ] **Step 3: Прогнать**

```bash
cd "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test" && python test_planfaktss_mapping.py
```

Expected: по каждому зонтику `записей карты = N` (N > 0 у IRS), `MAPPING PASSED`.

- [ ] **Step 4: Копия в worktree + commit**

```bash
cp "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test/test_planfaktss_mapping.py" "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3/_Rarzrabotki/Python/test/test_planfaktss_mapping.py"
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3" && git add _Rarzrabotki/Python/test/test_planfaktss_mapping.py && git commit -m "test(ПланФактССБухгалтерія): карта назва→етап по назве строки СС

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: Контракт ТЗ `ПолучитьДанные` (RED против старого .erf)

**Files:**
- Rewrite: `<MAIN>/_Rarzrabotki/Python/test/test_planfaktss_poluchit_dannye.py`

- [ ] **Step 1: Переписать тест целиком**

```python
# -*- coding: utf-8 -*-
"""ПолучитьДанные отчёта ПланФактСС возвращает грейн с ожидаемыми колонками, типами и эталонами."""
import os
import sys

if sys.stdout.encoding != 'utf-8':
    sys.stdout.reconfigure(encoding='utf-8')

import win32com.client

ERF = r"C:\Configuration_downloads\BASERP25\_Rarzrabotki\Отчеты\ПланФактССБухгалтерія.erf"
CONN = 'Srvr="SQLSERVER";Ref="bas_industrialbud";Usr="cfo";Pwd="2442"'

ЭТАЛОН_ПЛАН_IRS = 11296725.24
ЭТАЛОН_ФАКТ_IRS = 4595459.79

ОЖИДАЕМЫЕ_КОЛОНКИ = [
    "Подразделение", "Категория", "Этап", "ОбщееНазвание", "Аналитика", "Номенклатура", "Единица", "Документ",
    "ПланКол", "ЦенаПлан", "ПланГрн", "ФактКол", "ЦенаФакт", "ФактГрн",
    "ОтклонениеКол", "ОтклонениеГрн", "ПроцентКол", "ПроцентГрн",
    "ПланНаФактГрн", "ПрогнозГрн", "ОтклонениеПрогнозГрн", "ПроцентПрогнозГрн",
]


def лат(s):
    return s.replace("\u0421", "C").replace("\u0441", "c")


def main():
    assert os.path.exists(ERF), f"Не собран .erf: {ERF}"

    v8 = win32com.client.Dispatch("V83.COMConnector")
    b = v8.Connect(CONN)
    отчёт = b.ВнешниеОтчеты.Создать(ERF)

    период = b.NewObject("СтандартныйПериод")
    z = b.NewObject("Запрос")
    z.Текст = "ВЫБРАТЬ КОНЕЦПЕРИОДА(ДАТАВРЕМЯ(2026, 7, 31), ДЕНЬ) КАК К"
    период.ДатаОкончания = z.Выполнить().Выгрузить()[0].К

    тз = отчёт.ПолучитьДанные(период)
    assert тз.Количество() > 0, "ПолучитьДанные вернул пустую таблицу"

    колонки = [c.Имя for c in тз.Колонки]
    assert колонки == ОЖИДАЕМЫЕ_КОЛОНКИ, f"Состав колонок разошёлся: {колонки}"

    план = факт = 0.0
    план_строк = факт_строк = синтетика = 0
    for i in range(тз.Количество()):
        r = тз[i]
        assert isinstance(r.Аналитика, str), f"Аналитика не строка в строке {i}: {type(r.Аналитика)}"
        факт_ли = (r.ФактКол != 0) or (r.ФактГрн != 0)
        план_ли = (r.ПланКол != 0) or (r.ПланГрн != 0)
        if факт_ли:
            факт_строк += 1
            assert not r.Номенклатура.Пустая(), f"Факт без Номенклатуры: {r.Аналитика!r}"
            assert r.Аналитика == r.Номенклатура.Наименование.strip(), (
                f"Факт: {r.Аналитика!r} != {r.Номенклатура.Наименование.strip()!r}")
        elif план_ли:
            план_строк += 1
            assert r.Номенклатура.Пустая(), f"План с Номенклатурой: {r.Аналитика!r}"
            assert r.Аналитика != "", "План с пустой Аналитикой"
        elif r.ПланНаФактГрн != 0:
            # Синтетическая строка ETC: ни плана, ни факта, только прогноз.
            синтетика += 1
            assert r.Номенклатура.Пустая(), "Синтетическая строка ETC с Номенклатурой"
            assert r.Документ.Пустая(), "Синтетическая строка ETC с Документом"
        if лат(r.Подразделение.Наименование) == "МД IRC 2026":
            план += r.ПланГрн
            факт += r.ФактГрн
    assert abs(план - ЭТАЛОН_ПЛАН_IRS) < 0.01, f"План IRS = {план:,.2f}"
    assert abs(факт - ЭТАЛОН_ФАКТ_IRS) < 0.01, f"Факт IRS = {факт:,.2f}"
    assert план_строк > 0 and факт_строк > 0 and синтетика > 0, (
        f"план_строк={план_строк} факт_строк={факт_строк} синтетика={синтетика}")

    print(f"OK: строк={тз.Количество()} (план {план_строк}, факт {факт_строк}, ETC {синтетика}), "
          f"IRS план={план:,.2f} факт={факт:,.2f}")
    print("ПОЛУЧИТЬДАННЫЕ PASSED")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Прогнать против СТАРОГО .erf — должен упасть**

```bash
cd "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test" && python test_planfaktss_poluchit_dannye.py
```

Expected: `AssertionError: Состав колонок разошёлся` (в старой ТЗ нет колонки `Номенклатура`). Это RED.

- [ ] **Step 3: Копия в worktree + commit**

```bash
cp "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test/test_planfaktss_poluchit_dannye.py" "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3/_Rarzrabotki/Python/test/test_planfaktss_poluchit_dannye.py"
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3" && git add _Rarzrabotki/Python/test/test_planfaktss_poluchit_dannye.py && git commit -m "test(ПланФактССБухгалтерія): контракт ТЗ ПолучитьДанные — колонка Номенклатура, строковая Аналитика (RED)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: Кейс «Мінеральна вата 50» (RED) + синтетика в ETC-тесте

**Files:**
- Create: `<MAIN>/_Rarzrabotki/Python/test/test_planfaktss_vata_oon.py`
- Modify: `<MAIN>/_Rarzrabotki/Python/test/test_planfaktss_etc.py`

- [ ] **Step 1: Создать тест кейса «вата»**

```python
# -*- coding: utf-8 -*-
"""Кейс пользователя 04.09.2026: план «Мінеральна вата 100» не должен уезжать под «Мінеральна вата 50».

В СС «МД ООН Админ» строки IZOVAT 100 привязаны к карточке «EKOBOARD 50*610*1250» с карточной
общей назвой «Мінеральна вата 50». Отчёт обязан брать общую назву плана из СТРОКИ СС:
  - план под ОН «Мінеральна вата 100» = сумма Количество строк СС с этой ОН × КоличествоДомов;
  - под ОН «Мінеральна вата 50» нет плановых строк с аналитикой «EKOBOARD…»;
  - факт закупок по-прежнему классифицируется карточной ОН.
"""
import os
import sys

if sys.stdout.encoding != 'utf-8':
    sys.stdout.reconfigure(encoding='utf-8')

import win32com.client

ERF = r"C:\Configuration_downloads\BASERP25\_Rarzrabotki\Отчеты\ПланФактССБухгалтерія.erf"
CONN = 'Srvr="SQLSERVER";Ref="bas_industrialbud";Usr="cfo";Pwd="2442"'
СС_ИМЯ = "МД ООН Админ"


def план_по_тч(b, он_имя):
    z = b.NewObject("Запрос")
    z.Текст = """
    ВЫБРАТЬ
    	СУММА(К.Количество * ВЫБОР КОГДА К.Ссылка.КоличествоДомов > 0 ТОГДА К.Ссылка.КоличествоДомов ИНАЧЕ 1 КОНЕЦ) КАК Кол
    ИЗ Справочник.СтруктураСебестоимости.Комплектующие КАК К
    ГДЕ К.Ссылка.Наименование = &СС И К.Ссылка.Активная И НЕ К.Ссылка.ПометкаУдаления
    	И К.ОбщееНазвание.Наименование = &ОН
    """
    z.УстановитьПараметр("СС", СС_ИМЯ)
    z.УстановитьПараметр("ОН", он_имя)
    v = z.Выполнить().Выгрузить()[0].Кол
    return float(v or 0)


def main():
    assert os.path.exists(ERF), f"Не собран .erf: {ERF}"
    v8 = win32com.client.Dispatch("V83.COMConnector")
    b = v8.Connect(CONN)
    отчёт = b.ВнешниеОтчеты.Создать(ERF)

    z = b.NewObject("Запрос")
    z.Текст = "ВЫБРАТЬ ДАТАВРЕМЯ(2026, 7, 1) КАК Н, КОНЕЦПЕРИОДА(ДАТАВРЕМЯ(2026, 9, 30), ДЕНЬ) КАК К"
    гр = z.Выполнить().Выгрузить()[0]
    период = b.NewObject("СтандартныйПериод")
    период.ДатаНачала = гр.Н
    период.ДатаОкончания = гр.К

    тз = отчёт.ПолучитьДанные(период)

    план100 = план50 = факт100 = факт50 = 0.0
    ekoboard_под_50 = []
    for i in range(тз.Количество()):
        r = тз[i]
        if r.Подразделение.Наименование != СС_ИМЯ or r.ОбщееНазвание.Пустая():
            continue
        он = r.ОбщееНазвание.Наименование.strip()
        if он == "Мінеральна вата 100":
            план100 += r.ПланКол
            факт100 += r.ФактКол
        elif он == "Мінеральна вата 50":
            план50 += r.ПланКол
            факт50 += r.ФактКол
            if r.ПланКол != 0 and r.Аналитика.upper().startswith("EKOBOARD"):
                ekoboard_под_50.append((r.Аналитика, r.ПланКол))

    ожид100 = план_по_тч(b, "Мінеральна вата 100")
    ожид50 = план_по_тч(b, "Мінеральна вата 50")
    assert ожид100 > 0 and ожид50 > 0, f"В ТЧ нет строк: вата100={ожид100} вата50={ожид50}"
    assert abs(план100 - ожид100) < 0.001, f"План «вата 100» {план100} != ТЧ×домов {ожид100}"
    assert abs(план50 - ожид50) < 0.001, f"План «вата 50» {план50} != ТЧ×домов {ожид50}"
    assert not ekoboard_под_50, f"Под «вата 50» остался план EKOBOARD: {ekoboard_под_50}"
    # Замер 04.09.2026: факт «вата 100» = 198 (EKOBOARD 100), «вата 50» = 54 (EKOBOARD 50). Закупки только растут.
    assert факт100 >= 198 and факт50 >= 54, f"Факт разошёлся с замером 04.09.2026: вата100={факт100} вата50={факт50}"

    print(f"OK: вата100 план={план100} факт={факт100}; вата50 план={план50} факт={факт50}")
    print("VATA CASE PASSED")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Прогнать против старого .erf — должен упасть**

```bash
cd "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test" && python test_planfaktss_vata_oon.py
```

Expected: `AssertionError: План «вата 100» 0.0 != ТЧ×домов 1000.0` (старый отчёт кладёт эти строки под «вата 50»). Это RED.

- [ ] **Step 3: Дополнить `test_planfaktss_etc.py` проверкой синтетических строк**

В цикле `for i in range(тз.Количество()):` после строки `прогноз += r.ПрогнозГрн` добавить:

```python
        if r.ПланНаФактГрн != 0 and r.ПланГрн == 0 and r.ФактГрн == 0:
            # Синтетическая строка ETC: текстовая аналитика (пустая при заполненной ОН), пустые ссылки.
            assert isinstance(r.Аналитика, str), f"Синтетика: Аналитика не строка ({type(r.Аналитика)})"
            assert r.Номенклатура.Пустая(), "Синтетика: заполнена Номенклатура"
            assert r.Документ.Пустая(), "Синтетика: заполнен Документ"
            if not r.ОбщееНазвание.Пустая():
                assert r.Аналитика == "", f"Синтетика при ОН: Аналитика должна быть пустой, а не {r.Аналитика!r}"
```

- [ ] **Step 4: Прогнать ETC-тест против старого .erf — падает на колонке**

```bash
cd "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test" && python test_planfaktss_etc.py
```

Expected: `AttributeError` на `r.Номенклатура` (в старой ТЗ колонки нет). RED.

- [ ] **Step 5: Копии в worktree + commit**

```bash
cp "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test/test_planfaktss_vata_oon.py" "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3/_Rarzrabotki/Python/test/test_planfaktss_vata_oon.py"
cp "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test/test_planfaktss_etc.py" "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3/_Rarzrabotki/Python/test/test_planfaktss_etc.py"
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3" && git add _Rarzrabotki/Python/test/test_planfaktss_vata_oon.py _Rarzrabotki/Python/test/test_planfaktss_etc.py && git commit -m "test(ПланФактССБухгалтерія): кейс «Мінеральна вата 50» + синтетика ETC (RED)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: BSL — модуль объекта

**Files:**
- Modify: `<WT>/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Ext/ObjectModule.bsl`

Правки делать инструментом Edit (точное совпадение `old_string`, включая табы). Строки ниже — по состоянию baseline (Task 0).

- [ ] **Step 1: Шапка модуля — инварианты (строки 25–32)**

Old:
```
//   - план = норма * число подчинённых подразделений драйвера (домиков): норма в бухбазе
//     заведена на один дім, реквизита КоличествоДомов тут нет;
//   - плановые строки без номенклатуры не терять — fallback на ТЧ-реквизиты
//     ОбщееНазвание и НоменклатураСС (у IRS так заведено почти всё).
//
// Все запросы проверены Python COM на живой bas_industrialbud (Rule #-1).
// Спека: docs/superpowers/specs/2026-08-06-planfaktss-materialy-buhbud-design.md
// Промт: _Rarzrabotki/Отчеты/ПРОМТ_ПланФактССБухгалтерія_Материали.md
```
New:
```
//   - план = норма * СС.КоличествоДомов (0 -> 1): норма заведена на один дім. Подсчёт домиков по
//     имени дочернего подразделения убран 04.09.2026: имена карточек IRS набраны с кириллической
//     «С» («IRС»), подразделения — с латинской, сопоставление молча давало 2 вместо 6/7;
//   - загальна назва плана — ИЗ СТРОКИ СС (К.ОбщееНазвание); карточная — только при пустой строке.
//     Факт классифицируется карточной назвой. Иначе строка спецификации «Мінеральна вата 100»
//     с карточкой «EKOBOARD 50» уезжала под «вату 50» (кейс пользователя 04.09.2026);
//   - «Аналітика» — ТЕКСТ: план СокрЛП(К.НоменклатураСС), факт СокрЛП(Номенклатура.Наименование).
//     Ссылка на карточку живёт в отдельном измерении Номенклатура (только у факта).
//
// Все запросы проверены Python COM на живой bas_industrialbud (Rule #-1).
// Спека: docs/superpowers/specs/2026-08-06-planfaktss-materialy-buhbud-design.md
//        docs/superpowers/specs/2026-09-04-planfaktss-analitika-on-domiki-design.md
// Промт: _Rarzrabotki/Отчеты/ПРОМТ_ПланФактССБухгалтерія_Материали.md
```

- [ ] **Step 2: Комментарий и запрос `ВыполнитьЗапросПоЗонтику` (строки 154–284)**

Old (весь блок от комментария функции до `Возврат Запрос.Выполнить().Выгрузить();` не включая):
```
// Грейн по ОДНОМУ зонтику: UNION плановой и фактовой веток.
//
// План умножается на число домиков = количество непосредственных детей драйвера
// (драйвер = дочернее подразделение с именем карточки СС, иначе само СС.Подразделение).
// Факт ни с чем не соединяется, только ГДЕ: при нескольких активных карточках на зонтике
// «ПО ИСТИНА» умножил бы его на их число.
// ⚠️ «В ИЕРАРХИИ (столбец ВТ)» запрещено платформой — только параметр или подзапрос.
Функция ВыполнитьЗапросПоЗонтику(Зонтик, НачП, КонП)

	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Зонтик", Зонтик);
	Запрос.УстановитьПараметр("НачП", НачП);
	Запрос.УстановитьПараметр("КонП", КонП);
	Запрос.Текст =
	"ВЫБРАТЬ
	|	СС.Ссылка КАК СС,
	|	СС.Подразделение КАК Подр,
	|	ЕСТЬNULL(Дочь.Ссылка, СС.Подразделение) КАК Драйвер
	|ПОМЕСТИТЬ ВТ_Драйвер
	|ИЗ
	|	Справочник.СтруктураСебестоимости КАК СС
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Подразделения КАК Дочь
	|		ПО Дочь.Родитель = СС.Подразделение
	|			И Дочь.Наименование = СС.Наименование
	|			И НЕ Дочь.ПометкаУдаления
	|ГДЕ
	|	НЕ СС.ЭтоГруппа
	|	И СС.Активная
	|	И НЕ СС.ПометкаУдаления
	|	И СС.Подразделение = &Зонтик
	|;
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	Д.СС КАК СС,
	|	Д.Подр КАК Подр,
	|	ВЫБОР
	|		КОГДА КОЛИЧЕСТВО(РАЗЛИЧНЫЕ П.Ссылка) = 0
	|			ТОГДА 1
	|		ИНАЧЕ КОЛИЧЕСТВО(РАЗЛИЧНЫЕ П.Ссылка)
	|	КОНЕЦ КАК Домиков
	|ПОМЕСТИТЬ ВТ_СС
	|ИЗ
	|	ВТ_Драйвер КАК Д
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Подразделения КАК П
	|		ПО П.Родитель = Д.Драйвер
	|			И НЕ П.ПометкаУдаления
	|СГРУППИРОВАТЬ ПО
	|	Д.СС,
	|	Д.Подр
	|;
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_СС.Подр КАК Подразделение,
	|	ВЫРАЗИТЬ(ВЫБОР
	|			КОГДА К.Этап.Наименование ПОДОБНО ""%Монтаж%""
	|				ТОГДА ""Монтаж""
	|			ИНАЧЕ ""Виробництво""
	|		КОНЕЦ КАК СТРОКА(50)) КАК Категория,
	|	К.Этап КАК Этап,
	|	ВЫБОР
	|		КОГДА К.Номенклатура = ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка)
	|			ТОГДА К.ОбщееНазвание
	|		ИНАЧЕ К.Номенклатура.ОбщееНазваниеНоменклатуры
	|	КОНЕЦ КАК ОбщееНазвание,
	|	ВЫБОР
	|		КОГДА К.Номенклатура = ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка)
	|			ТОГДА К.НоменклатураСС
	|		ИНАЧЕ К.Номенклатура
	|	КОНЕЦ КАК Аналитика,
	|	К.Единица КАК Единица,
	|	ВТ_СС.СС КАК Документ,
	|	К.Количество * ВТ_СС.Домиков КАК ПланКол,
	|	К.Сумма * ВТ_СС.Домиков КАК ПланГрн,
	|	0 КАК ФактКол,
	|	0 КАК ФактГрн
	|ПОМЕСТИТЬ ВТ_Детали
	|ИЗ
	|	Справочник.СтруктураСебестоимости.Комплектующие КАК К
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_СС КАК ВТ_СС
	|		ПО К.Ссылка = ВТ_СС.СС
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	&Зонтик,
	|	ВЫРАЗИТЬ(""Виробництво"" КАК СТРОКА(50)),
	|	ЗНАЧЕНИЕ(Справочник.ЭтапыРабот.ПустаяСсылка),
	|	Т.Номенклатура.ОбщееНазваниеНоменклатуры,
	|	Т.Номенклатура,
	|	Т.ЕдиницаИзмерения,
	|	Д.Ссылка,
	|	0,
	|	0,
	|	Т.Количество,
	|	ВЫБОР
	|		КОГДА Д.СуммаВключаетНДС
	|			ТОГДА Т.Сумма
	|		ИНАЧЕ Т.Сумма + Т.СуммаНДС
	|	КОНЕЦ
	|ИЗ
	|	Документ.ПоступлениеТоваровУслуг.Товары КАК Т
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ПоступлениеТоваровУслуг КАК Д
	|		ПО Т.Ссылка = Д.Ссылка
	|ГДЕ
	|	Д.Проведен
	|	И Д.Дата МЕЖДУ &НачП И &КонП
	|	И Д.Подразделение В ИЕРАРХИИ (&Зонтик)
	|;
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Детали.Подразделение КАК Подразделение,
	|	ВТ_Детали.Категория КАК Категория,
	|	ВТ_Детали.Этап КАК Этап,
	|	ВТ_Детали.ОбщееНазвание КАК ОбщееНазвание,
	|	ВТ_Детали.Аналитика КАК Аналитика,
	|	ВТ_Детали.Единица КАК Единица,
	|	ВТ_Детали.Документ КАК Документ,
	|	СУММА(ВТ_Детали.ПланКол) КАК ПланКол,
	|	СУММА(ВТ_Детали.ПланГрн) КАК ПланГрн,
	|	СУММА(ВТ_Детали.ФактКол) КАК ФактКол,
	|	СУММА(ВТ_Детали.ФактГрн) КАК ФактГрн
	|ИЗ
	|	ВТ_Детали КАК ВТ_Детали
	|СГРУППИРОВАТЬ ПО
	|	ВТ_Детали.Подразделение,
	|	ВТ_Детали.Категория,
	|	ВТ_Детали.Этап,
	|	ВТ_Детали.ОбщееНазвание,
	|	ВТ_Детали.Аналитика,
	|	ВТ_Детали.Единица,
	|	ВТ_Детали.Документ";
```
New:
```
// Грейн по ОДНОМУ зонтику: UNION плановой и фактовой веток.
//
// План умножается на СС.КоличествоДомов (0 -> 1) — как в ЕРП-прообразе.
// Загальна назва плана — из строки СС, карточная — при пустой строке; «Аналитика» — текст.
// Факт ни с чем не соединяется, только ГДЕ: при нескольких активных карточках на зонтике
// «ПО ИСТИНА» умножил бы его на их число.
// ⚠️ «В ИЕРАРХИИ (столбец ВТ)» запрещено платформой — только параметр или подзапрос.
// Текст 1:1 с ТЕКСТ_ГРЕЙНА в test_planfaktss_grain.py (Rule #-1).
Функция ВыполнитьЗапросПоЗонтику(Зонтик, НачП, КонП)

	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Зонтик", Зонтик);
	Запрос.УстановитьПараметр("НачП", НачП);
	Запрос.УстановитьПараметр("КонП", КонП);
	Запрос.Текст =
	"ВЫБРАТЬ
	|	СС.Ссылка КАК СС,
	|	СС.Подразделение КАК Подр,
	|	ВЫБОР
	|		КОГДА СС.КоличествоДомов > 0
	|			ТОГДА СС.КоличествоДомов
	|		ИНАЧЕ 1
	|	КОНЕЦ КАК Домиков
	|ПОМЕСТИТЬ ВТ_СС
	|ИЗ
	|	Справочник.СтруктураСебестоимости КАК СС
	|ГДЕ
	|	НЕ СС.ЭтоГруппа
	|	И СС.Активная
	|	И НЕ СС.ПометкаУдаления
	|	И СС.Подразделение = &Зонтик
	|;
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_СС.Подр КАК Подразделение,
	|	ВЫРАЗИТЬ(ВЫБОР
	|			КОГДА К.Этап.Наименование ПОДОБНО ""%Монтаж%""
	|				ТОГДА ""Монтаж""
	|			ИНАЧЕ ""Виробництво""
	|		КОНЕЦ КАК СТРОКА(50)) КАК Категория,
	|	К.Этап КАК Этап,
	|	ЕСТЬNULL(ВЫБОР
	|			КОГДА К.ОбщееНазвание <> ЗНАЧЕНИЕ(Справочник.ОбщиеНазванияНоменклатуры.ПустаяСсылка)
	|				ТОГДА К.ОбщееНазвание
	|			ИНАЧЕ К.Номенклатура.ОбщееНазваниеНоменклатуры
	|		КОНЕЦ, ЗНАЧЕНИЕ(Справочник.ОбщиеНазванияНоменклатуры.ПустаяСсылка)) КАК ОбщееНазвание,
	|	СокрЛП(К.НоменклатураСС) КАК Аналитика,
	|	ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка) КАК Номенклатура,
	|	К.Единица КАК Единица,
	|	ВТ_СС.СС КАК Документ,
	|	К.Количество * ВТ_СС.Домиков КАК ПланКол,
	|	К.Сумма * ВТ_СС.Домиков КАК ПланГрн,
	|	0 КАК ФактКол,
	|	0 КАК ФактГрн
	|ПОМЕСТИТЬ ВТ_Детали
	|ИЗ
	|	Справочник.СтруктураСебестоимости.Комплектующие КАК К
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_СС КАК ВТ_СС
	|		ПО К.Ссылка = ВТ_СС.СС
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	&Зонтик,
	|	ВЫРАЗИТЬ(""Виробництво"" КАК СТРОКА(50)),
	|	ЗНАЧЕНИЕ(Справочник.ЭтапыРабот.ПустаяСсылка),
	|	Т.Номенклатура.ОбщееНазваниеНоменклатуры,
	|	СокрЛП(Т.Номенклатура.Наименование),
	|	Т.Номенклатура,
	|	Т.ЕдиницаИзмерения,
	|	Д.Ссылка,
	|	0,
	|	0,
	|	Т.Количество,
	|	ВЫБОР
	|		КОГДА Д.СуммаВключаетНДС
	|			ТОГДА Т.Сумма
	|		ИНАЧЕ Т.Сумма + Т.СуммаНДС
	|	КОНЕЦ
	|ИЗ
	|	Документ.ПоступлениеТоваровУслуг.Товары КАК Т
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ПоступлениеТоваровУслуг КАК Д
	|		ПО Т.Ссылка = Д.Ссылка
	|ГДЕ
	|	Д.Проведен
	|	И Д.Дата МЕЖДУ &НачП И &КонП
	|	И Д.Подразделение В ИЕРАРХИИ (&Зонтик)
	|;
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Детали.Подразделение КАК Подразделение,
	|	ВТ_Детали.Категория КАК Категория,
	|	ВТ_Детали.Этап КАК Этап,
	|	ВТ_Детали.ОбщееНазвание КАК ОбщееНазвание,
	|	ВТ_Детали.Аналитика КАК Аналитика,
	|	ВТ_Детали.Номенклатура КАК Номенклатура,
	|	ВТ_Детали.Единица КАК Единица,
	|	ВТ_Детали.Документ КАК Документ,
	|	СУММА(ВТ_Детали.ПланКол) КАК ПланКол,
	|	СУММА(ВТ_Детали.ПланГрн) КАК ПланГрн,
	|	СУММА(ВТ_Детали.ФактКол) КАК ФактКол,
	|	СУММА(ВТ_Детали.ФактГрн) КАК ФактГрн
	|ИЗ
	|	ВТ_Детали КАК ВТ_Детали
	|СГРУППИРОВАТЬ ПО
	|	ВТ_Детали.Подразделение,
	|	ВТ_Детали.Категория,
	|	ВТ_Детали.Этап,
	|	ВТ_Детали.ОбщееНазвание,
	|	ВТ_Детали.Аналитика,
	|	ВТ_Детали.Номенклатура,
	|	ВТ_Детали.Единица,
	|	ВТ_Детали.Документ";
```

- [ ] **Step 3: `МаппингОбщееНазваниеЭтап` — комментарий и запрос (строки 315–348)**

Old:
```
// Соответствие: нормализованное ИМЯ загальної назви -> Структура{Этап, Категория}.
// Ключ — ИМЯ, а не ссылка: в справочнике ОбщиеНазванияНоменклатуры есть ДУБЛИ карточек
// с одинаковым именем, по ссылке план и факт могут не сойтись.
// При нескольких этапах одного имени берём максимум по Строка(Этап) — детерминизм.
Функция МаппингОбщееНазваниеЭтап(Зонтик)

	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Зонтик", Зонтик);
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ВЫБОР
	|		КОГДА К.Номенклатура = ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка)
	|			ТОГДА К.ОбщееНазвание.Наименование
	|		ИНАЧЕ К.Номенклатура.ОбщееНазваниеНоменклатуры.Наименование
	|	КОНЕЦ КАК ОбщееНазвание,
	|	К.Этап КАК Этап,
	|	ВЫБОР
	|		КОГДА К.Этап.Наименование ПОДОБНО ""%Монтаж%""
	|			ТОГДА ""Монтаж""
	|		ИНАЧЕ ""Виробництво""
	|	КОНЕЦ КАК Категория
	|ИЗ
	|	Справочник.СтруктураСебестоимости.Комплектующие КАК К
	|ГДЕ
	|	К.Ссылка.Подразделение = &Зонтик
	|	И НЕ К.Ссылка.ЭтоГруппа
	|	И К.Ссылка.Активная
	|	И НЕ К.Ссылка.ПометкаУдаления
	|	И К.Этап <> ЗНАЧЕНИЕ(Справочник.ЭтапыРабот.ПустаяСсылка)
	|	И ВЫБОР
	|		КОГДА К.Номенклатура = ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка)
	|			ТОГДА К.ОбщееНазвание
	|		ИНАЧЕ К.Номенклатура.ОбщееНазваниеНоменклатуры
	|	КОНЕЦ <> ЗНАЧЕНИЕ(Справочник.ОбщиеНазванияНоменклатуры.ПустаяСсылка)";
```
New:
```
// Соответствие: нормализованное ИМЯ загальної назви -> Структура{Этап, Категория}.
// Ключ — ИМЯ, а не ссылка: в справочнике ОбщиеНазванияНоменклатуры есть ДУБЛИ карточек
// с одинаковым именем, по ссылке план и факт могут не сойтись.
// Назва берётся из СТРОКИ СС — тем же правилом, что и у плана в грейне (карточная — при пустой).
// При нескольких этапах одного имени берём максимум по Строка(Этап) — детерминизм.
// Текст 1:1 с ТЕКСТ в test_planfaktss_mapping.py.
Функция МаппингОбщееНазваниеЭтап(Зонтик)

	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Зонтик", Зонтик);
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ЕСТЬNULL(ВЫБОР
	|			КОГДА К.ОбщееНазвание <> ЗНАЧЕНИЕ(Справочник.ОбщиеНазванияНоменклатуры.ПустаяСсылка)
	|				ТОГДА К.ОбщееНазвание.Наименование
	|			ИНАЧЕ К.Номенклатура.ОбщееНазваниеНоменклатуры.Наименование
	|		КОНЕЦ, """") КАК ОбщееНазвание,
	|	К.Этап КАК Этап,
	|	ВЫБОР
	|		КОГДА К.Этап.Наименование ПОДОБНО ""%Монтаж%""
	|			ТОГДА ""Монтаж""
	|		ИНАЧЕ ""Виробництво""
	|	КОНЕЦ КАК Категория
	|ИЗ
	|	Справочник.СтруктураСебестоимости.Комплектующие КАК К
	|ГДЕ
	|	К.Ссылка.Подразделение = &Зонтик
	|	И НЕ К.Ссылка.ЭтоГруппа
	|	И К.Ссылка.Активная
	|	И НЕ К.Ссылка.ПометкаУдаления
	|	И К.Этап <> ЗНАЧЕНИЕ(Справочник.ЭтапыРабот.ПустаяСсылка)
	|	И (К.ОбщееНазвание <> ЗНАЧЕНИЕ(Справочник.ОбщиеНазванияНоменклатуры.ПустаяСсылка)
	|		ИЛИ К.Номенклатура.ОбщееНазваниеНоменклатуры <> ЗНАЧЕНИЕ(Справочник.ОбщиеНазванияНоменклатуры.ПустаяСсылка))";
```

- [ ] **Step 4: Синтетические строки в `ЗаполнитьПланНаФакт` (строки 426–441)**

Old:
```
		Образец = Данные.Образец;
		НоваяСтрока = ТЗ.Добавить();
		НоваяСтрока.Подразделение = Образец.Подразделение;
		НоваяСтрока.Категория = Образец.Категория;
		НоваяСтрока.Этап = Образец.Этап;
		НоваяСтрока.ОбщееНазвание = Образец.ОбщееНазвание;
		Если ЗначениеЗаполнено(Образец.ОбщееНазвание) Тогда
			// Типизированные пустые ссылки: иначе расшифровка плодит пустые строки-дубли.
			НоваяСтрока.Аналитика = ПустаяНоменклатура;
			НоваяСтрока.Единица = ПустаяЕдиница;
		Иначе
			НоваяСтрока.Аналитика = Образец.Аналитика;
			НоваяСтрока.Единица = Образец.Единица;
		КонецЕсли;
		НоваяСтрока.Документ = ПустойДокумент;
		НоваяСтрока.ПланНаФактГрн = ПланНаФакт;
```
New:
```
		Образец = Данные.Образец;
		НоваяСтрока = ТЗ.Добавить();
		НоваяСтрока.Подразделение = Образец.Подразделение;
		НоваяСтрока.Категория = Образец.Категория;
		НоваяСтрока.Этап = Образец.Этап;
		НоваяСтрока.ОбщееНазвание = Образец.ОбщееНазвание;
		НоваяСтрока.Номенклатура = ПустаяНоменклатура;
		Если ЗначениеЗаполнено(Образец.ОбщееНазвание) Тогда
			// Итог по загальній назві: пустая аналитика и типизированная пустая единица —
			// иначе расшифровка плодит пустые строки-дубли.
			НоваяСтрока.Аналитика = "";
			НоваяСтрока.Единица = ПустаяЕдиница;
		Иначе
			НоваяСтрока.Аналитика = Образец.Аналитика;
			НоваяСтрока.Единица = Образец.Единица;
		КонецЕсли;
		НоваяСтрока.Документ = ПустойДокумент;
		НоваяСтрока.ПланНаФактГрн = ПланНаФакт;
```

- [ ] **Step 5: `КлючГруппыПланНаФакт` (строки 446–466)**

Old:
```
// Ключ группы нетто. Пустая строка = строка в прогнозе не участвует.
Функция КлючГруппыПланНаФакт(СтрокаТЗ)

	Префикс = Строка(СтрокаТЗ.Подразделение.УникальныйИдентификатор());

	Если ЗначениеЗаполнено(СтрокаТЗ.ОбщееНазвание) Тогда
		Возврат Префикс + "|ОН|" + ВРег(СокрЛП(СтрокаТЗ.ОбщееНазвание.Наименование));
	КонецЕсли;

	Если ТипЗнч(СтрокаТЗ.Аналитика) = Тип("СправочникСсылка.Номенклатура")
		И ЗначениеЗаполнено(СтрокаТЗ.Аналитика) Тогда
		Возврат Префикс + "|НОМ|" + Строка(СтрокаТЗ.Аналитика.УникальныйИдентификатор());
	КонецЕсли;

	Если ТипЗнч(СтрокаТЗ.Аналитика) = Тип("Строка") И ЗначениеЗаполнено(СтрокаТЗ.Аналитика) Тогда
		Возврат Префикс + "|ТЕКСТ|" + ВРег(СокрЛП(СтрокаТЗ.Аналитика));
	КонецЕсли;

	Возврат "";

КонецФункции
```
New:
```
// Ключ группы нетто. Пустая строка = строка в прогнозе не участвует.
// Без загальної назви план и факт неттуются по ТЕКСТУ аналитики (у обеих веток он текст):
// сходятся только при точном совпадении СокрЛП(НоменклатураСС) и СокрЛП(Наименование карточки).
Функция КлючГруппыПланНаФакт(СтрокаТЗ)

	Префикс = Строка(СтрокаТЗ.Подразделение.УникальныйИдентификатор());

	Если ЗначениеЗаполнено(СтрокаТЗ.ОбщееНазвание) Тогда
		Возврат Префикс + "|ОН|" + ВРег(СокрЛП(СтрокаТЗ.ОбщееНазвание.Наименование));
	КонецЕсли;

	Если ЗначениеЗаполнено(СокрЛП(СтрокаТЗ.Аналитика)) Тогда
		Возврат Префикс + "|ТЕКСТ|" + ВРег(СокрЛП(СтрокаТЗ.Аналитика));
	КонецЕсли;

	Возврат "";

КонецФункции
```

- [ ] **Step 6: `СоздатьПустуюТаблицу` (строки 489–530)**

Old:
```
// Пустая ТЗ грейна. Имена и типы колонок обязаны совпадать с полями набора "Детали"
// схемы СКД — рассинхрон даёт битые данные без ошибки.
// 7 измерений + 14 показателей.
Функция СоздатьПустуюТаблицу()

	ТЗ = Новый ТаблицаЗначений;
	ОписЧисло = Новый ОписаниеТипов("Число", Новый КвалификаторыЧисла(18, 3, ДопустимыйЗнак.Любой));
	ОписСтрока50 = Новый ОписаниеТипов("Строка", Новый КвалификаторыСтроки(50));

	МассивАналитика = Новый Массив;
	МассивАналитика.Добавить(Тип("СправочникСсылка.Номенклатура"));
	МассивАналитика.Добавить(Тип("Строка")); // план без карточки = текст НоменклатураСС

	МассивДокумент = Новый Массив;
	МассивДокумент.Добавить(Тип("ДокументСсылка.ПоступлениеТоваровУслуг"));
	МассивДокумент.Добавить(Тип("СправочникСсылка.СтруктураСебестоимости"));

	ТЗ.Колонки.Добавить("Подразделение", Новый ОписаниеТипов("СправочникСсылка.Подразделения"));
	ТЗ.Колонки.Добавить("Категория", ОписСтрока50);
	ТЗ.Колонки.Добавить("Этап", Новый ОписаниеТипов("СправочникСсылка.ЭтапыРабот"));
	ТЗ.Колонки.Добавить("ОбщееНазвание", Новый ОписаниеТипов("СправочникСсылка.ОбщиеНазванияНоменклатуры"));
	ТЗ.Колонки.Добавить("Аналитика", Новый ОписаниеТипов(МассивАналитика, , Новый КвалификаторыСтроки(200)));
	ТЗ.Колонки.Добавить("Единица", Новый ОписаниеТипов("СправочникСсылка.КлассификаторЕдиницИзмерения"));
	ТЗ.Колонки.Добавить("Документ", Новый ОписаниеТипов(МассивДокумент));
```
New:
```
// Пустая ТЗ грейна. Имена и типы колонок обязаны совпадать с полями набора "Детали"
// схемы СКД — рассинхрон даёт битые данные без ошибки.
// 8 измерений + 14 показателей.
Функция СоздатьПустуюТаблицу()

	ТЗ = Новый ТаблицаЗначений;
	ОписЧисло = Новый ОписаниеТипов("Число", Новый КвалификаторыЧисла(18, 3, ДопустимыйЗнак.Любой));
	ОписСтрока50 = Новый ОписаниеТипов("Строка", Новый КвалификаторыСтроки(50));
	ОписСтрока200 = Новый ОписаниеТипов("Строка", Новый КвалификаторыСтроки(200));

	МассивДокумент = Новый Массив;
	МассивДокумент.Добавить(Тип("ДокументСсылка.ПоступлениеТоваровУслуг"));
	МассивДокумент.Добавить(Тип("СправочникСсылка.СтруктураСебестоимости"));

	ТЗ.Колонки.Добавить("Подразделение", Новый ОписаниеТипов("СправочникСсылка.Подразделения"));
	ТЗ.Колонки.Добавить("Категория", ОписСтрока50);
	ТЗ.Колонки.Добавить("Этап", Новый ОписаниеТипов("СправочникСсылка.ЭтапыРабот"));
	ТЗ.Колонки.Добавить("ОбщееНазвание", Новый ОписаниеТипов("СправочникСсылка.ОбщиеНазванияНоменклатуры"));
	ТЗ.Колонки.Добавить("Аналитика", ОписСтрока200); // план: СокрЛП(НоменклатураСС), факт: СокрЛП(Наименование)
	ТЗ.Колонки.Добавить("Номенклатура", Новый ОписаниеТипов("СправочникСсылка.Номенклатура")); // только у факта
	ТЗ.Колонки.Добавить("Единица", Новый ОписаниеТипов("СправочникСсылка.КлассификаторЕдиницИзмерения"));
	ТЗ.Колонки.Добавить("Документ", Новый ОписаниеТипов(МассивДокумент));
```

- [ ] **Step 7: Grep-контроль баланса процедур и отсутствия старых конструкций**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Ext" && echo "Функция=$(grep -c '^Функция ' ObjectModule.bsl) КонецФункции=$(grep -c '^КонецФункции' ObjectModule.bsl) Процедура=$(grep -c '^Процедура ' ObjectModule.bsl) КонецПроцедуры=$(grep -c '^КонецПроцедуры' ObjectModule.bsl)"; grep -n -E "ВТ_Драйвер|МассивАналитика|\|НОМ\||Дочь\." ObjectModule.bsl || echo "старых конструкций нет"
```

Expected: `Функция=9 КонецФункции=9 Процедура=6 КонецПроцедуры=6` (число функций/процедур не менялось), затем `старых конструкций нет`.

- [ ] **Step 8: Нормализация BOM + CRLF**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3" && python - <<'EOF'
p = "_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Ext/ObjectModule.bsl"
b = open(p, "rb").read()
t = b.decode("utf-8-sig").replace("\r\n", "\n").replace("\n", "\r\n")
open(p, "wb").write(b"\xef\xbb\xbf" + t.encode("utf-8"))
b = open(p, "rb").read()
print("BOM", b[:3] == b"\xef\xbb\xbf", "LF-only", b.count(b"\n") - b.count(b"\r\n"))
EOF
```

Expected: `BOM True LF-only 0`.

- [ ] **Step 9: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3" && git add "_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Ext/ObjectModule.bsl" && git commit -m "feat(ПланФактССБухгалтерія): Аналітика текстом + Номенклатура у факта, ОН плана из строки СС, домики по КоличествоДомов

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 6: Схема СКД — тип «Аналітика» и новое поле «Номенклатура»

**Files:**
- Modify: `<WT>/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml` (строки 93–117 baseline)

- [ ] **Step 1: Заменить блок поля `Аналитика` и вставить поле `Номенклатура` следом**

Old (весь блок поля `Аналитика`, строки 93–117):
```xml
		<field xsi:type="DataSetFieldField">
			<dataPath>Аналитика</dataPath>
			<field>Аналитика</field>
			<title xsi:type="v8:LocalStringType">
				<v8:item>
					<v8:lang>ru</v8:lang>
					<v8:content>Аналітика</v8:content>
				</v8:item>
				<v8:item>
					<v8:lang>uk</v8:lang>
					<v8:content>Аналітика</v8:content>
				</v8:item>
			</title>
			<role>
				<dcscom:dimension>true</dcscom:dimension>
			</role>
			<valueType>
				<v8:Type>xs:string</v8:Type>
				<v8:Type xmlns:d5p1="http://v8.1c.ru/8.1/data/enterprise/current-config">d5p1:CatalogRef.Номенклатура</v8:Type>
				<v8:StringQualifiers>
					<v8:Length>200</v8:Length>
					<v8:AllowedLength>Variable</v8:AllowedLength>
				</v8:StringQualifiers>
			</valueType>
		</field>
```
New:
```xml
		<field xsi:type="DataSetFieldField">
			<dataPath>Аналитика</dataPath>
			<field>Аналитика</field>
			<title xsi:type="v8:LocalStringType">
				<v8:item>
					<v8:lang>ru</v8:lang>
					<v8:content>Аналітика</v8:content>
				</v8:item>
				<v8:item>
					<v8:lang>uk</v8:lang>
					<v8:content>Аналітика</v8:content>
				</v8:item>
			</title>
			<role>
				<dcscom:dimension>true</dcscom:dimension>
			</role>
			<valueType>
				<v8:Type>xs:string</v8:Type>
				<v8:StringQualifiers>
					<v8:Length>200</v8:Length>
					<v8:AllowedLength>Variable</v8:AllowedLength>
				</v8:StringQualifiers>
			</valueType>
		</field>
		<field xsi:type="DataSetFieldField">
			<dataPath>Номенклатура</dataPath>
			<field>Номенклатура</field>
			<title xsi:type="v8:LocalStringType">
				<v8:item>
					<v8:lang>ru</v8:lang>
					<v8:content>Номенклатура</v8:content>
				</v8:item>
				<v8:item>
					<v8:lang>uk</v8:lang>
					<v8:content>Номенклатура</v8:content>
				</v8:item>
			</title>
			<role>
				<dcscom:dimension>true</dcscom:dimension>
			</role>
			<valueType>
				<v8:Type xmlns:d5p1="http://v8.1c.ru/8.1/data/enterprise/current-config">d5p1:CatalogRef.Номенклатура</v8:Type>
			</valueType>
		</field>
```

- [ ] **Step 2: Нормализация BOM + CRLF**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3" && python - <<'EOF'
p = "_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml"
b = open(p, "rb").read()
t = b.decode("utf-8-sig").replace("\r\n", "\n").replace("\n", "\r\n")
open(p, "wb").write(b"\xef\xbb\xbf" + t.encode("utf-8"))
b = open(p, "rb").read()
print("BOM", b[:3] == b"\xef\xbb\xbf", "LF-only", b.count(b"\n") - b.count(b"\r\n"))
EOF
```

Expected: `BOM True LF-only 0`.

- [ ] **Step 3: Валидация схемы + контроль порядка полей**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3" && powershell.exe -NoProfile -File .claude/skills/skd-validate/scripts/skd-validate.ps1 -Path "_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml"; grep -n "<dataPath>" "_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml" | sed -n '1,9p'
```

Expected: валидация без ошибок; порядок первых полей `Подразделение, Категория, Этап, ОбщееНазвание, Аналитика, Номенклатура, Единица, Документ, ПланКол`. Если скрипта skd-validate в worktree нет (`.claude/` не в git) — запустить его из `<MAIN>/.claude/skills/skd-validate/scripts/skd-validate.ps1` с тем же `-Path`, указав полный путь к файлу в worktree.

- [ ] **Step 4: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3" && git add "_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml" && git commit -m "feat(ПланФактССБухгалтерія): СКД — Аналітика строкой, измерение Номенклатура

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 7: Копия в основной каталог, сборка .erf, GREEN всех тестов, smoke

**Files:**
- Copy: `<WT>/…/Ext/ObjectModule.bsl` → `<MAIN>/…/Ext/ObjectModule.bsl`
- Copy: `<WT>/…/Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml` → `<MAIN>/…/Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml`
- Build: `<MAIN>/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія.erf`

- [ ] **Step 1: Скопировать два исходника в основной каталог (Rule #4)**

```bash
cp "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Ext/ObjectModule.bsl" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Ext/ObjectModule.bsl"
cp "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml"
cd "C:/Configuration_downloads/BASERP25" && diff -q "_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Ext/ObjectModule.bsl" ".claude/worktrees/youthful-bohr-fb6ce3/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Ext/ObjectModule.bsl" && echo "bsl синхронен"
```

Expected: `bsl синхронен`.

- [ ] **Step 2: Собрать .erf против bas_industrialbud**

```bash
cd "C:/Configuration_downloads/BASERP25" && powershell.exe -NoProfile -File "C:/Configuration_downloads/BASERP25/.claude/skills/epf-build/scripts/epf-build.ps1" -InfoBaseServer "SQLSERVER" -InfoBaseRef "bas_industrialbud" -UserName "cfo" -Password "2442" -SourceFile "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія.xml" -OutputFile "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія.erf"
```

Expected: сборка без ошибок, файл `.erf` с новым временем. Если «Ошибка блокировки информационной базы для конфигурирования» — Конфигуратор `bas_industrialbud` открыт: попросить пользователя его закрыть. Собирать через `BaseERPRazr` **нельзя** — метаданные другие (нет `Справочник.СтруктураСебестоимости` в нужном виде).

- [ ] **Step 3: GREEN — все тесты отчёта**

```bash
cd "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test" && python test_planfaktss_grain.py && python test_planfaktss_mapping.py && python test_planfaktss_poluchit_dannye.py && python test_planfaktss_vata_oon.py && python test_planfaktss_etc.py && python smoke_planfaktss_report.py
```

Expected (последние строки каждого): `GRAIN CONTRACT PASSED`, `MAPPING PASSED`, `ПОЛУЧИТЬДАННЫЕ PASSED`, `VATA CASE PASSED`, `ETC INVARIANT PASSED`, `SMOKE PASSED`. Любое падение — чинить в worktree, повторять Step 1–3 (не править файлы в `<MAIN>` напрямую).

- [ ] **Step 4: Сообщить пользователю о ручном шаге**

В «Додаткові звіти й обробки» карточка «ПланФактССБухгалтерія» хранит старый `.erf` — пользователь перезагружает файл из `<MAIN>\_Rarzrabotki\Отчеты\ПланФактССБухгалтерія.erf` сам (кнопка «Загрузить из файла»). После этого в форме отчёта: «Ещё → Установить стандартные настройки», иначе сохранённый вариант спрячет поле «Номенклатура».

---

### Task 8: Список расхождений «ОН строки СС ≠ ОН карточки» для чистки данных

**Files:**
- Create: `<WT>/_Rarzrabotki/Python/planfaktss_raskhozhdeniya_on.py`
- Generate: `<MAIN>/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія_расхождения_ОН.md` (и копия в `<WT>`)

- [ ] **Step 1: Создать скрипт**

```python
# -*- coding: utf-8 -*-
"""Строки активных СС (bas_industrialbud), где общая назва СТРОКИ СС не совпадает с общей назвой КАРТОЧКИ.

С 04.09.2026 отчёт ПланФактССБухгалтерія берёт общую назву плана из строки СС, а факт закупок
классифицируется карточной назвой. По строкам из этого списка план и факт разъедутся по разным
«Загальна назва» и не будут неттоваться в «План на факт». Лечится данными: выровнять назву
в строке СС или в карточке номенклатуры.

Запуск:  python planfaktss_raskhozhdeniya_on.py
Выход:   _Rarzrabotki/Отчеты/ПланФактССБухгалтерія_расхождения_ОН.md
"""
import os
import sys
import datetime

if sys.stdout.encoding != 'utf-8':
    sys.stdout.reconfigure(encoding='utf-8')

import win32com.client

CONN = 'Srvr="SQLSERVER";Ref="bas_industrialbud";Usr="cfo";Pwd="2442"'
OUT = r"C:\Configuration_downloads\BASERP25\_Rarzrabotki\Отчеты\ПланФактССБухгалтерія_расхождения_ОН.md"

ТЕКСТ = """
ВЫБРАТЬ
	К.Ссылка.Наименование КАК СС,
	К.НомерСтроки КАК N,
	К.Этап.Наименование КАК Этап,
	К.ОбщееНазвание.Наименование КАК ОН_Строки,
	К.НоменклатураСС КАК ТекстСС,
	К.Номенклатура.Наименование КАК Карточка,
	К.Номенклатура.ОбщееНазваниеНоменклатуры.Наименование КАК ОН_Карточки,
	К.Количество КАК Кол,
	К.Сумма КАК Сумма
ИЗ
	Справочник.СтруктураСебестоимости.Комплектующие КАК К
ГДЕ
	НЕ К.Ссылка.ЭтоГруппа
	И К.Ссылка.Активная
	И НЕ К.Ссылка.ПометкаУдаления
	И К.Номенклатура <> ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка)
	И К.ОбщееНазвание <> ЗНАЧЕНИЕ(Справочник.ОбщиеНазванияНоменклатуры.ПустаяСсылка)
	И К.Номенклатура.ОбщееНазваниеНоменклатуры <> ЗНАЧЕНИЕ(Справочник.ОбщиеНазванияНоменклатуры.ПустаяСсылка)
	И СокрЛП(К.ОбщееНазвание.Наименование) <> СокрЛП(К.Номенклатура.ОбщееНазваниеНоменклатуры.Наименование)
УПОРЯДОЧИТЬ ПО
	СС, N
"""


def md(s):
    return str(s if s is not None else "").replace("|", "\\|").strip()


def main():
    v8 = win32com.client.Dispatch("V83.COMConnector")
    b = v8.Connect(CONN)
    z = b.NewObject("Запрос")
    z.Текст = ТЕКСТ
    t = z.Выполнить().Выгрузить()

    строки = []
    по_сс = {}
    for i in range(t.Количество()):
        r = t[i]
        строки.append(r)
        по_сс[r.СС] = по_сс.get(r.СС, 0) + 1

    with open(OUT, "w", encoding="utf-8", newline="\n") as f:
        f.write("# ПланФактССБухгалтерія — расхождения общей назвы: строка СС ≠ карточка номенклатуры\n\n")
        f.write(f"Снято {datetime.date.today():%d.%m.%Y}, база bas_industrialbud, активные СС. Всего строк: {len(строки)}.\n\n")
        f.write("Отчёт берёт общую назву плана из строки СС, факт — из карточки. По этим строкам план и факт\n"
                "попадут под разные «Загальна назва» и не будут неттоваться в «План на факт».\n"
                "Чтобы сошлись — выровнять назву в строке СС или в карточке (решение по каждой строке за пользователем).\n\n")
        f.write("| СС | строк |\n|---|---:|\n")
        for k, v in sorted(по_сс.items()):
            f.write(f"| {md(k)} | {v} |\n")
        f.write("\n| СС | N | Етап | ОН строки СС | НоменклатураСС | Карточка | ОН карточки | Кол | Сумма |\n")
        f.write("|---|---:|---|---|---|---|---|---:|---:|\n")
        for r in строки:
            f.write(f"| {md(r.СС)} | {r.N} | {md(r.Этап)} | {md(r.ОН_Строки)} | {md(r.ТекстСС)} | {md(r.Карточка)} | "
                    f"{md(r.ОН_Карточки)} | {r.Кол} | {r.Сумма} |\n")

    print(f"OK: {len(строки)} строк → {OUT}")
    for k, v in sorted(по_сс.items()):
        print(f"  {k}: {v}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Запустить и проверить итог**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3/_Rarzrabotki/Python" && python planfaktss_raskhozhdeniya_on.py
```

Expected: `OK: 67 строк → …` и разбивка по СС (ВООЗ 37, ПРООН 12, ОРІЛЬ 11, ООН Админ 5, ООН 2026 1, IRS 15 м 1 — по замеру 04.09.2026; если СС правили, число изменится, это не ошибка).

- [ ] **Step 3: Копия .md в worktree + commit**

```bash
cp "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія_расхождения_ОН.md" "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3/_Rarzrabotki/Отчеты/ПланФактССБухгалтерія_расхождения_ОН.md"
cp "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3/_Rarzrabotki/Python/planfaktss_raskhozhdeniya_on.py" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/planfaktss_raskhozhdeniya_on.py"
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3" && git add _Rarzrabotki/Python/planfaktss_raskhozhdeniya_on.py "_Rarzrabotki/Отчеты/ПланФактССБухгалтерія_расхождения_ОН.md" && git commit -m "docs(ПланФактССБухгалтерія): список расхождений ОН строки СС ↔ карточки для чистки данных

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 9: Документация — промт отчёта и память проекта

**Files:**
- Modify: `<WT>/_Rarzrabotki/Отчеты/ПРОМТ_ПланФактССБухгалтерія_Материали.md` (+ копия в `<MAIN>`)
- Create: `C:\Users\support1c\.claude\projects\C--Configuration-downloads-BASERP25\memory\planfaktss_buhbud_report.md`
- Modify: `C:\Users\support1c\.claude\projects\C--Configuration-downloads-BASERP25\memory\MEMORY.md`

- [ ] **Step 1: Скопировать промт в worktree (он в git не отслеживался — проверить `git ls-files`)**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3" && git ls-files "_Rarzrabotki/Отчеты/ПРОМТ_ПланФактССБухгалтерія_Материали.md" | grep -q . && echo "в git" || cp "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Отчеты/ПРОМТ_ПланФактССБухгалтерія_Материали.md" "_Rarzrabotki/Отчеты/ПРОМТ_ПланФактССБухгалтерія_Материали.md"
```

- [ ] **Step 2: Вставить блок изменений 04.09.2026 сразу после заголовка §0 «Задача одной фразой»**

Найти строку `## 0. Задача одной фразой` и **перед** ней вставить:

```markdown
## 0a. Изменения 04.09.2026 (переопределяют §1, §2 п.4, §4.1, §5 ниже)

Спека: `docs/superpowers/specs/2026-09-04-planfaktss-analitika-on-domiki-design.md`.

1. **Реквизит `СС.КоличествоДомов` в бухбазе ЕСТЬ** (появился после 06.08.2026). Множитель плана =
   `ВЫБОР КОГДА СС.КоличествоДомов > 0 ТОГДА СС.КоличествоДомов ИНАЧЕ 1 КОНЕЦ`. Подсчёт дочерних
   подразделений по имени карточки СС (ВТ_Драйвер) удалён: имена карточек IRS набраны с кириллической
   «С» («IRС»), подразделения — с латинской, сопоставление молча давало 2 вместо 6/7.
2. **Общая назва плана — из строки СС** (`К.ОбщееНазвание`), карточная
   (`К.Номенклатура.ОбщееНазваниеНоменклатуры`) — только при пустой строке (сегодня таких 0 из 1417).
   Факт по-прежнему классифицируется карточной назвой. Причина: в СС «МД ООН Админ» строки
   «Мінеральна вата 100» привязаны к карточке «EKOBOARD 50» с карточной назвой «Мінеральна вата 50» —
   план уезжал под чужую назву. Тот же источник назвы — в карте «назва → етап».
   Следствие: 67 строк плана с назвой-синонимом в строке СС не сойдутся с фактом по «Загальна назва»
   (список: `ПланФактССБухгалтерія_расхождения_ОН.md`, скрипт `_Rarzrabotki/Python/planfaktss_raskhozhdeniya_on.py`).
3. **«Аналітика» — Строка(200)**: план `СокрЛП(К.НоменклатураСС)`, факт `СокрЛП(Т.Номенклатура.Наименование)`.
   Новое измерение **`Номенклатура`** (`СправочникСсылка.Номенклатура`) заполнено только у факта; в дефолтную
   структуру не входит. Синтетические строки ETC: `Аналитика = ""` при заполненной ОН. Ключ ETC-группы без ОН —
   `ТЕКСТ|ВРег(СокрЛП(Аналитика))` у обеих веток (ветка `НОМ|uuid` удалена).
4. Измерений в наборе «Детали» — **8**: `Подразделение, Категория, Этап, ОбщееНазвание, Аналитика, Номенклатура, Единица, Документ`.
5. Актуальный текст запроса грейна — `_Rarzrabotki/Python/test/test_planfaktss_grain.py` (`ТЕКСТ_ГРЕЙНА`),
   он 1:1 с BSL; текст в §5 ниже — исторический (06.08.2026).
6. Подразделение IRS переименовано в «МД IRC 2026»; тесты нормализуют кириллическую «С» → латинскую.

```

- [ ] **Step 3: Пометить устаревшие места в §1 и §2**

В таблице §1 строку
```
| Реквизит `КоличествоДомов` | **отсутствует**; в обмене (`Бухгалтерия --> ERP`) не передаётся |
```
заменить на
```
| Реквизит `КоличествоДомов` | ~~отсутствует~~ **есть с сентября 2026** (см. §0a п.1); в обмене (`Бухгалтерия --> ERP`) не передаётся |
```
В §2 п.4 строку
```
   `К.ОбщееНазвание`; аналитика — из `К.НоменклатураСС` (текст). У IRS так заведено почти всё.
```
заменить на
```
   `К.ОбщееНазвание`; аналитика — из `К.НоменклатураСС` (текст). У IRS так заведено почти всё.
   **С 04.09.2026 правило обратное: назва плана ВСЕГДА из строки СС, карточная — запасной вариант (§0a п.2).**
```

- [ ] **Step 4: Копия промта в основной каталог**

```bash
cp "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3/_Rarzrabotki/Отчеты/ПРОМТ_ПланФактССБухгалтерія_Материали.md" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Отчеты/ПРОМТ_ПланФактССБухгалтерія_Материали.md"
```

- [ ] **Step 5: Память проекта — новый файл**

Создать `C:\Users\support1c\.claude\projects\C--Configuration-downloads-BASERP25\memory\planfaktss_buhbud_report.md`:

```markdown
---
name: planfaktss_buhbud_report
description: Отчёт ПланФактССБухгалтерія (bas_industrialbud) — «План на факт за матеріалами»: домики по СС.КоличествоДомов, ОН плана из строки СС, «Аналітика» текстом
metadata:
  type: project
---

Внешний отчёт `_Rarzrabotki/Отчеты/ПланФактССБухгалтерія.erf` (BuhBud, `cfo/2442`) — бухгалтерский близнец [[planfakt_proizvodstvo_polny_report]], только материалы. Программная компоновка, внешний набор «Детали». Спеки: `docs/superpowers/specs/2026-08-06-planfaktss-materialy-buhbud-design.md`, `…/2026-09-04-planfaktss-analitika-on-domiki-design.md`. Промт: `_Rarzrabotki/Отчеты/ПРОМТ_ПланФактССБухгалтерія_Материали.md` (§0a — изменения 04.09.2026).

**Решения пользователя 04.09.2026:**
- «Аналітика» — Строка(200): план `СокрЛП(К.НоменклатураСС)`, факт `СокрЛП(Номенклатура.Наименование)`; ссылка на карточку — в отдельном измерении `Номенклатура` только у факта (не в дефолтной структуре).
- Общая назва плана — из **строки СС** (`К.ОбщееНазвание`), карточная — лишь при пустой строке; факт — карточной назвой. Кейс: строки «Мінеральна вата 100» с карточкой EKOBOARD 50 уезжали под «вату 50». Принятое следствие: 67 план-строк с назвой-синонимом не сойдутся с фактом (`ПланФактССБухгалтерія_расхождения_ОН.md`) — лечится данными.
- Множитель = `СС.КоличествоДомов` (0→1), реквизит в BuhBud появился после 06.08. Подсчёт детей по имени убран: имена карточек IRS с кириллической «С» («IRС»), подразделения — латинская «C» → давало 2 вместо 6/7.

**Why:** отчёт должен читаться как спецификация СС, а не как карточки номенклатуры; сравнение имён с разными раскладками молча ломает множитель.
**How to apply:** при правках — сначала Python COM тест (`test_planfaktss_grain.py` держит ТЕКСТ_ГРЕЙНА 1:1 с BSL), сборка .erf ТОЛЬКО против `bas_industrialbud`, затем `smoke_planfaktss_report.py`; после перезагрузки .erf в «Доп. отчётах» — «Установить стандартные настройки». Файлы отчёта и промт до 04.09 в git не были — теперь в ветке.
```

- [ ] **Step 6: Строка в `MEMORY.md`**

В `C:\Users\support1c\.claude\projects\C--Configuration-downloads-BASERP25\memory\MEMORY.md` после строки, начинающейся с `- [Отчёт А_ПланФактныйПроизводствоПолный](planfakt_proizvodstvo_polny_report.md)`, добавить:

```markdown
- [Отчёт ПланФактССБухгалтерія (BuhBud)](planfaktss_buhbud_report.md) — домики по СС.КоличествоДомов (имена IRS с кириллической «С»), ОН плана из строки СС, «Аналітика» текстом + измерение Номенклатура у факта
```

- [ ] **Step 7: Commit документации**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3" && git add "_Rarzrabotki/Отчеты/ПРОМТ_ПланФактССБухгалтерія_Материали.md" && git commit -m "docs(ПланФактССБухгалтерія): промт — изменения 04.09.2026 (КоличествоДомов, ОН из строки СС, Аналітика текстом)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 10: Финальная проверка и передача

- [ ] **Step 1: Убедиться, что main и worktree синхронны по всем изменённым файлам**

```bash
cd "C:/Configuration_downloads/BASERP25" && for f in "_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Ext/ObjectModule.bsl" "_Rarzrabotki/Отчеты/ПланФактССБухгалтерія/Templates/ОсновнаяСхемаКомпоновкиДанных/Ext/Template.xml" "_Rarzrabotki/Python/test/test_planfaktss_grain.py" "_Rarzrabotki/Python/test/test_planfaktss_mapping.py" "_Rarzrabotki/Python/test/test_planfaktss_poluchit_dannye.py" "_Rarzrabotki/Python/test/test_planfaktss_vata_oon.py" "_Rarzrabotki/Python/test/test_planfaktss_etc.py" "_Rarzrabotki/Python/planfaktss_raskhozhdeniya_on.py" "_Rarzrabotki/Отчеты/ПРОМТ_ПланФактССБухгалтерія_Материали.md" "_Rarzrabotki/Отчеты/ПланФактССБухгалтерія_расхождения_ОН.md"; do diff -q "$f" ".claude/worktrees/youthful-bohr-fb6ce3/$f" >/dev/null && echo "OK  $f" || echo "DIFF $f"; done
```

Expected: все строки `OK`.

- [ ] **Step 2: Повторный полный прогон тестов (evidence before claims)**

```bash
cd "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test" && python test_planfaktss_grain.py && python test_planfaktss_mapping.py && python test_planfaktss_poluchit_dannye.py && python test_planfaktss_vata_oon.py && python test_planfaktss_etc.py && python smoke_planfaktss_report.py && echo "ALL PLANFAKTSS TESTS PASSED"
```

Expected: `ALL PLANFAKTSS TESTS PASSED`.

- [ ] **Step 3: `git status` чистый, лог ветки**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/youthful-bohr-fb6ce3" && git status --short && git log --oneline -12
```

Expected: `git status` пуст; в логе коммиты Task 0–9.

- [ ] **Step 4: Отчёт пользователю**

Сообщить: что изменилось (три пункта), числа кейса «вата» из `test_planfaktss_vata_oon.py`, план IRS из грейна, где лежит список 67 расхождений, ручные шаги (перезагрузить .erf в «Доп. отчётах», «Установить стандартные настройки»). Дальше — навык `superpowers:finishing-a-development-branch` (merge в `claude/main` по правилам памяти `feedback_merge_prep_only_branch_files` и `git_push_large_files_cherry_pick`).
