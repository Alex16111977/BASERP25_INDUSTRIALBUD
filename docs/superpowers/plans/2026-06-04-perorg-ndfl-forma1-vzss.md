# НДФЛ Форма1 по организации (per-org А_ВзСС) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Закрепить НДФЛ/ВС за юр.лицом из `НалогиБухгалтерия.Организация` (явная per-org цифра) и провести его сквозняком до баланса, чтобы отчёт сверки ЕРП(Форма1) vs Бух 661/663 по каждому (ФЛ × юр.лицо) = 0.

**Architecture:** 4 точечные правки тега `ОрганизацияБухгалтерия` в `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` (суммы инвариантны, Σ-preserving). Заполнитель движения регистра `А_ВзаиморасчетыССотрудниками` не трогаем — он уже группирует по ОргБух. Верификация: Python COM (заполнение в памяти) для ТЧ-уровня; UI-перепроведение (силами пользователя) для регистр-уровня.

**Tech Stack:** 1C BSL (BAS ERP 2.5), Python 3.13 + win32com (`V83.COMConnector`), skills `db-load-xml`/`db-update`.

**Среда:** база `Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"`; рабочая папка `C:\Configuration_downloads\BASERP25` (ветка `claude/bas-doc-skills`); python `C:\Python313\python.exe`. Спека: `docs/superpowers/specs/2026-06-04-perorg-ndfl-forma1-vzss-design.md`.

**Коммиты:** только релевантные файлы (`git add <путь>`), НЕ `git add -A` — в дереве 1040 несвязанных WIP-файлов.

---

## Task 1: RED — ground-truth данных (Python COM)

**Files:**
- Create: `knowledge_А_ОтражениеЗПпоКазне/Python/test/red_perorg_ndfl_groundtruth.py`

Цель RED: доказать, что (а) `НалогиБухгалтерия.Организация` заполнена per-org для всех 11 A-ФЛ и даёт точный сплит; (б) текущий регистр А_ВзСС Воронцова разъезжается 57 250 / 81 696,50. Если (а) не выполняется хоть для одного ФЛ — Правка 1 для него не сработает (упадёт в fallback на шапку) → это надо знать ДО правок.

- [ ] **Step 1: Написать скрипт ground-truth**

```python
# -*- coding: utf-8 -*-
import win32com.client, sys
if sys.stdout.encoding != 'utf-8':
    sys.stdout.reconfigure(encoding='utf-8')
erp = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')

A_FL = ["Воронцов","Манжула","Аулова","Шостак","Соколенко","Шинкаренко",
        "Шкурат","Пономаренко О","Травінський","Скрипник С","Пономаренко В"]

# (а) НалогиБухгалтерия per (ФЛ, Организация, ТипНалога) по декабрьским А_ОтражениеЗПпоКазне
q = erp.NewObject("Запрос")
q.Текст = """
ВЫБРАТЬ
    НБ.Сотрудник.ФизическоеЛицо.Наименование КАК ФЛ,
    НБ.Организация.Наименование КАК Орг,
    СУММА(НБ.СуммаНДФЛ) КАК СуммаНДФЛ
ИЗ Документ.А_ОтражениеЗПпоКазне.НалогиБухгалтерия КАК НБ
ГДЕ НБ.Ссылка.Дата МЕЖДУ &Д1 И &Д2 И НБ.СуммаНДФЛ <> 0
СГРУППИРОВАТЬ ПО НБ.Сотрудник.ФизическоеЛицо.Наименование, НБ.Организация.Наименование
УПОРЯДОЧИТЬ ПО ФЛ, Орг
"""
q.УстановитьПараметр("Д1", erp.NewObject("Дата","2025-12-01"))
q.УстановитьПараметр("Д2", erp.NewObject("Дата","2025-12-31"))
t = q.Выполнить().Выгрузить()
print("=== (а) НалогиБухгалтерия per-org (НДФЛ+ВС) ===")
empty_org = 0
for i in range(t.Количество()):
    r = t.Получить(i)
    fl = r.ФЛ or ""
    if any(a.split()[0] in fl for a in A_FL):
        org = r.Орг or "<ПУСТО>"
        if not r.Орг: empty_org += 1
        print(f"  {fl:28s} | {org:30s} | {r.СуммаНДФЛ:>12.2f}")
print(f"  >>> строк с ПУСТОЙ Организация среди A-ФЛ: {empty_org} (должно быть 0)")

# (б) Текущий регистр А_ВзСС Форма1 Воронцова per ОргБух (ОЗФУ-регистратор, Приход)
q2 = erp.NewObject("Запрос")
q2.Текст = """
ВЫБРАТЬ
    ВзС.ОрганизацияБухгалтерия.Наименование КАК ОргБух,
    СУММА(ВЫБОР КОГДА ВзС.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
        ТОГДА ВзС.СуммаВзаиморасчетов ИНАЧЕ 0 КОНЕЦ) КАК Приход
ИЗ РегистрНакопления.А_ВзаиморасчетыССотрудниками КАК ВзС
ГДЕ ВзС.ФизическоеЛицо.Наименование ПОДОБНО "Воронцов%"
    И ВзС.ФормаPL = ЗНАЧЕНИЕ(Перечисление.А_ФормыPL.Форма1)
    И ВЫРАЗИТЬ(ВзС.Регистратор КАК Документ.ОтражениеЗарплатыВФинансовомУчете).Ссылка ЕСТЬ НЕ NULL
СГРУППИРОВАТЬ ПО ВзС.ОрганизацияБухгалтерия.Наименование
"""
print("=== (б) Текущий А_ВзСС Форма1 Воронцова per ОргБух (ОЗФУ Приход) — ожидаем разлёт 57250 / 81696.50 ===")
t2 = q2.Выполнить().Выгрузить()
for i in range(t2.Количество()):
    r = t2.Получить(i)
    print(f"  {r.ОргБух or '<ПУСТО>':30s} | Приход={r.Приход:>12.2f}")
```

- [ ] **Step 2: Запустить и зафиксировать baseline**

Run: `& "C:\Python313\python.exe" "knowledge_А_ОтражениеЗПпоКазне\Python\test\red_perorg_ndfl_groundtruth.py"`
Expected: блок (а) — 0 строк с пустой Организация; Воронцов ДЖИ ≈4 703,50 + ІНД ≈36 800. Блок (б) — Воронцов ДЖИ 57 250 / ІНД 81 696,50 (текущий разлёт). **Если в (а) есть пустая Организация — застопориться и пересмотреть scope для тех ФЛ.**

- [ ] **Step 3: Commit**

```bash
git add "knowledge_А_ОтражениеЗПпоКазне/Python/test/red_perorg_ndfl_groundtruth.py"
git commit -m "test(ОЗП): RED ground-truth — НалогиБухгалтерия per-org + текущий разлёт А_ВзСС"
```

---

## Task 2: GREEN-харнесс (падающий до правок)

**Files:**
- Create: `knowledge_А_ОтражениеЗПпоКазне/Python/test/green_perorg_ndfl_inmemory.py`

Харнесс заполняет документ в памяти (БЕЗ записи) и проверяет per-org тег у `НачисленныйНДФЛ` и gross-up «Удержание». До правок (и до загрузки поля в БД) — RED.

- [ ] **Step 1: Написать харнесс**

```python
# -*- coding: utf-8 -*-
import win32com.client, sys
if sys.stdout.encoding != 'utf-8':
    sys.stdout.reconfigure(encoding='utf-8')
erp = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')

НОМЕР = "000000001"  # Воронцов-канарейка; при необходимости расширить на список из Task 8
ref = erp.Документы.А_ОтражениеЗПпоКазне.НайтиПоНомеру(НОМЕР)
assert erp.ЗначениеЗаполнено(ref), f"Документ {НОМЕР} не найден"
об = ref.ПолучитьОбъект()

# Заполнение в памяти (4 процедуры внутри оркестратора) — БЕЗ записи
об.ЗаполнитьДляОтражения_ДокументОтражениеЗарплатыВФинансовомУчете()

Ф1 = erp.Перечисления.А_ФормыPL.Форма1
def xs(v): return erp.XMLСтрока(v) if erp.ЗначениеЗаполнено(v) else ""

# НачисленныйНДФЛ per (ФЛ, ОргБух)
print("=== НачисленныйНДФЛ per (ФЛ, ОргБух) ===")
ndfl, empty = {}, 0
for i in range(об.НачисленныйНДФЛ.Количество()):
    s = об.НачисленныйНДФЛ.Получить(i)
    if xs(s.ФормаPL) != xs(Ф1): continue
    fl = s.ФизическоеЛицо.Наименование if erp.ЗначениеЗаполнено(s.ФизическоеЛицо) else ""
    org = s.ОрганизацияБухгалтерия.Наименование if erp.ЗначениеЗаполнено(s.ОрганизацияБухгалтерия) else "<ПУСТО>"
    if not erp.ЗначениеЗаполнено(s.ОрганизацияБухгалтерия): empty += 1
    ndfl[(fl,org)] = ndfl.get((fl,org),0)+s.Сумма
for (fl,org),v in sorted(ndfl.items()):
    print(f"  {fl:28s} | {org:30s} | {v:>12.2f}")
print(f"  >>> НДФЛ-строк Ф1 с ПУСТОЙ ОргБух: {empty} (GREEN: 0)")

# gross-up «Удержание» per (ФЛ, ОргБух): А_ЭтоУдержание, ВзносыВсего=0
print("=== gross-up «Удержание» Ф1 per (ФЛ, ОргБух) ===")
ud, empty2 = {}, 0
for i in range(об.НачисленнаяЗарплатаИВзносыПоФизлицам.Количество()):
    s = об.НачисленнаяЗарплатаИВзносыПоФизлицам.Получить(i)
    sp = s.СпособОтраженияЗарплатыВБухучете
    if xs(s.ФормаPL)!=xs(Ф1) or not erp.ЗначениеЗаполнено(sp) or not sp.А_ЭтоУдержание: continue
    if s.ВзносыВсего != 0: continue
    fl = s.ФизическоеЛицо.Наименование if erp.ЗначениеЗаполнено(s.ФизическоеЛицо) else ""
    org = s.ОрганизацияБухгалтерия.Наименование if erp.ЗначениеЗаполнено(s.ОрганизацияБухгалтерия) else "<ПУСТО>"
    if not erp.ЗначениеЗаполнено(s.ОрганизацияБухгалтерия): empty2 += 1
    ud[(fl,org)] = ud.get((fl,org),0)+s.Сумма
for (fl,org),v in sorted(ud.items()):
    print(f"  {fl:28s} | {org:30s} | {v:>12.2f}")
print(f"  >>> «Удержание»-строк Ф1 с ПУСТОЙ ОргБух: {empty2} (GREEN: 0)")
print("ИТОГ: GREEN если обе суммы пустых = 0 и Воронцов НДФЛ ДЖИ≈4703.50 / ІНД≈36800")
```

- [ ] **Step 2: Запустить — ожидать RED**

Run: `& "C:\Python313\python.exe" "knowledge_А_ОтражениеЗПпоКазне\Python\test\green_perorg_ndfl_inmemory.py"`
Expected до правок: либо ошибка «поле ОрганизацияБухгалтерия не существует» (если метаданные не в БД), либо пустые ОргБух у НДФЛ-строк (>0). Это RED. **Если ошибка про поле — Task 6 (загрузка метаданных) выполнить раньше, см. примечание.**

- [ ] **Step 3: Commit**

```bash
git add "knowledge_А_ОтражениеЗПпоКазне/Python/test/green_perorg_ndfl_inmemory.py"
git commit -m "test(ОЗП): GREEN-харнесс per-org НДФЛ/Удержание (in-memory, падает до правок)"
```

> **Примечание о порядке:** если Step 2 падает «поле не существует» — поле `НачисленныйНДФЛ.ОрганизацияБухгалтерия` ещё не в БД. Тогда выполнить загрузку метаданных (Task 6, только XML документа) ДО Task 3, затем продолжить. Код-правки (Task 3-5) можно делать в любом случае — они на диске.

---

## Task 3: Правка 1 — `_ОтрЗП_ЗаполнитьСтрокуНДФЛ`

**Files:**
- Modify: `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` (процедура `_ОтрЗП_ЗаполнитьСтрокуНДФЛ`, ~2248)

- [ ] **Step 1: Внести правку (Edit)**

old_string:
```bsl
	НоваяСтрока.Организация = Организация;
	НоваяСтрока.Сумма = Сумма;
```
new_string:
```bsl
	НоваяСтрока.Организация = Организация;
	// per-org ОргБух з НалогиБухгалтерия.Организация (точна організація податку, не пропорція)
	НоваяСтрока.ОрганизацияБухгалтерия = ?(ЗначениеЗаполнено(СтрокаНалога.Организация), СтрокаНалога.Организация, Организация);
	НоваяСтрока.Сумма = Сумма;
```

- [ ] **Step 2: Проверка** — `/1c-bsl-review` на изменённую процедуру или визуальная сверка отступов (табы). Commit вместе с Task 4-5 (один логический коммит правок) — см. Task 5 Step.

---

## Task 4: Правка 2 — `_ОтрЗП_ВыровнятьУдержаниеСНДФЛ`

**Files:**
- Modify: `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` (процедура `_ОтрЗП_ВыровнятьУдержаниеСНДФЛ`, ~2278)

Три правки внутри процедуры: ключ агрегации (блок 1), убрать `ОргБухК` (блок 2), брать ОргБух из ключа (блок 4).

- [ ] **Step 1: Блок 1 — ключ (ФЛ|Подр|ОргБух)**

old_string:
```bsl
	// 1) НачисленныйНДФЛ Ф1 per (ФЛ, Подр) — зберігаємо посилання
	НДФЛКлюч = Новый Соответствие;
	Для Каждого Стр Из НачисленныйНДФЛ Цикл
		Если Стр.ФормаPL <> Ф1 Тогда Продолжить; КонецЕсли;
		К = XMLСтрока(Стр.ФизическоеЛицо) + "|" + XMLСтрока(Стр.ПодразделениеПредприятия);
		Зап = НДФЛКлюч[К];
		Если Зап = Неопределено Тогда
			Зап = Новый Структура("ФЛ, Подр, Сумма", Стр.ФизическоеЛицо, Стр.ПодразделениеПредприятия, 0);
			НДФЛКлюч.Вставить(К, Зап);
		КонецЕсли;
		Зап.Сумма = Зап.Сумма + Стр.Сумма;
	КонецЦикла;
```
new_string:
```bsl
	// 1) НачисленныйНДФЛ Ф1 per (ФЛ, Подр, ОргБух) — зберігаємо посилання + per-org ОргБух
	НДФЛКлюч = Новый Соответствие;
	Для Каждого Стр Из НачисленныйНДФЛ Цикл
		Если Стр.ФормаPL <> Ф1 Тогда Продолжить; КонецЕсли;
		К = XMLСтрока(Стр.ФизическоеЛицо) + "|" + XMLСтрока(Стр.ПодразделениеПредприятия) + "|" + XMLСтрока(Стр.ОрганизацияБухгалтерия);
		Зап = НДФЛКлюч[К];
		Если Зап = Неопределено Тогда
			Зап = Новый Структура("ФЛ, Подр, ОргБух, Сумма", Стр.ФизическоеЛицо, Стр.ПодразделениеПредприятия, Стр.ОрганизацияБухгалтерия, 0);
			НДФЛКлюч.Вставить(К, Зап);
		КонецЕсли;
		Зап.Сумма = Зап.Сумма + Стр.Сумма;
	КонецЦикла;
```

- [ ] **Step 2: Блок 2 — убрать ОргБухК**

old_string:
```bsl
	// 2) Старі gross-up «Удержание» Ф1 (А_ЭтоУдержание, ВзносыВсего=0): шаблон per ФЛ,
	//    ОргБух per (ФЛ,Подр), індекси на видалення.
	Шаблон  = Новый Соответствие;   // ФЛ → Структура(Способ, ВидОперации, ГруппаУчета)
	ОргБухК = Новый Соответствие;   // "ФЛ|Подр" → ОрганизацияБухгалтерия
	НаУдаление = Новый Массив;
	Для і = 0 По НачисленнаяЗарплатаИВзносыПоФизлицам.Количество() - 1 Цикл
		Стр = НачисленнаяЗарплатаИВзносыПоФизлицам[і];
		Если Стр.ФормаPL <> Ф1 Тогда Продолжить; КонецЕсли;
		Способ = Стр.СпособОтраженияЗарплатыВБухучете;
		Если НЕ (ЗначениеЗаполнено(Способ) И Способ.А_ЭтоУдержание) Тогда Продолжить; КонецЕсли;
		Если Стр.ВзносыВсего <> 0 Тогда Продолжить; КонецЕсли;
		Если Шаблон[Стр.ФизическоеЛицо] = Неопределено Тогда
			Т = Новый Структура("Способ, ВидОперации, ГруппаУчета",
				Стр.СпособОтраженияЗарплатыВБухучете, Стр.ВидОперации, Стр.ГруппаУчетаНачислений);
			Шаблон.Вставить(Стр.ФизическоеЛицо, Т);
		КонецЕсли;
		Если ЗначениеЗаполнено(Стр.ОрганизацияБухгалтерия) Тогда
			ОргБухК.Вставить(XMLСтрока(Стр.ФизическоеЛицо) + "|" + XMLСтрока(Стр.ПодразделениеПредприятия),
				Стр.ОрганизацияБухгалтерия);
		КонецЕсли;
		НаУдаление.Добавить(і);
	КонецЦикла;
```
new_string:
```bsl
	// 2) Старі gross-up «Удержание» Ф1 (А_ЭтоУдержание, ВзносыВсего=0): шаблон per ФЛ + індекси на видалення.
	//    ОргБух більше НЕ беремо зі старих рядків — джерело тепер НачисленныйНДФЛ.ОрганизацияБухгалтерия (per-org).
	Шаблон  = Новый Соответствие;   // ФЛ → Структура(Способ, ВидОперации, ГруппаУчета)
	НаУдаление = Новый Массив;
	Для і = 0 По НачисленнаяЗарплатаИВзносыПоФизлицам.Количество() - 1 Цикл
		Стр = НачисленнаяЗарплатаИВзносыПоФизлицам[і];
		Если Стр.ФормаPL <> Ф1 Тогда Продолжить; КонецЕсли;
		Способ = Стр.СпособОтраженияЗарплатыВБухучете;
		Если НЕ (ЗначениеЗаполнено(Способ) И Способ.А_ЭтоУдержание) Тогда Продолжить; КонецЕсли;
		Если Стр.ВзносыВсего <> 0 Тогда Продолжить; КонецЕсли;
		Если Шаблон[Стр.ФизическоеЛицо] = Неопределено Тогда
			Т = Новый Структура("Способ, ВидОперации, ГруппаУчета",
				Стр.СпособОтраженияЗарплатыВБухучете, Стр.ВидОперации, Стр.ГруппаУчетаНачислений);
			Шаблон.Вставить(Стр.ФизическоеЛицо, Т);
		КонецЕсли;
		НаУдаление.Добавить(і);
	КонецЦикла;
```

- [ ] **Step 3: Блок 4 — ОргБух из ключа НДФЛ**

old_string:
```bsl
	// 4) Додати рядки «Удержание» = НачисленныйНДФЛ per (ФЛ, Подр)
	Для Каждого КЗ Из НДФЛКлюч Цикл
		З = КЗ.Значение;
		Если З.Сумма = 0 Тогда Продолжить; КонецЕсли;
		Т = Шаблон[З.ФЛ];
		Если Т = Неопределено Тогда Продолжить; КонецЕсли; // у ФЛ не було gross-up «Удержание» — пропускаємо
		НовСтр = НачисленнаяЗарплатаИВзносыПоФизлицам.Добавить();
		НовСтр.Организация                      = Организация;
		НовСтр.ФизическоеЛицо                   = З.ФЛ;
		НовСтр.ПодразделениеПредприятия         = З.Подр;
		НовСтр.ВидОперации                      = Т.ВидОперации;
		НовСтр.ГруппаУчетаНачислений            = Т.ГруппаУчета;
		НовСтр.СпособОтраженияЗарплатыВБухучете = Т.Способ;
		НовСтр.Сумма                            = З.Сумма;
		НовСтр.ВзносыВсего                      = 0;
		НовСтр.ФормаPL                          = Ф1;
		ОБ = ОргБухК[КЗ.Ключ];
		Если ЗначениеЗаполнено(ОБ) Тогда
			НовСтр.ОрганизацияБухгалтерия = ОБ;
		КонецЕсли;
	КонецЦикла;
```
new_string:
```bsl
	// 4) Додати рядки «Удержание» = НачисленныйНДФЛ per (ФЛ, Подр, ОргБух)
	Для Каждого КЗ Из НДФЛКлюч Цикл
		З = КЗ.Значение;
		Если З.Сумма = 0 Тогда Продолжить; КонецЕсли;
		Т = Шаблон[З.ФЛ];
		Если Т = Неопределено Тогда Продолжить; КонецЕсли; // у ФЛ не було gross-up «Удержание» — пропускаємо
		НовСтр = НачисленнаяЗарплатаИВзносыПоФизлицам.Добавить();
		НовСтр.Организация                      = Организация;
		НовСтр.ФизическоеЛицо                   = З.ФЛ;
		НовСтр.ПодразделениеПредприятия         = З.Подр;
		НовСтр.ВидОперации                      = Т.ВидОперации;
		НовСтр.ГруппаУчетаНачислений            = Т.ГруппаУчета;
		НовСтр.СпособОтраженияЗарплатыВБухучете = Т.Способ;
		НовСтр.Сумма                            = З.Сумма;
		НовСтр.ВзносыВсего                      = 0;
		НовСтр.ФормаPL                          = Ф1;
		Если ЗначениеЗаполнено(З.ОргБух) Тогда
			НовСтр.ОрганизацияБухгалтерия = З.ОргБух;
		КонецЕсли;
	КонецЦикла;
```

---

## Task 5: Правка 3 (ОЗФУ override) + Правка 4 (ПНМП Налоги)

**Files:**
- Modify: `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` (`СоздатьДокументОтражениеЗарплатыВФинансовомУчете` ~3215; `_ПНМП_ЗаполнитьНалоги` ~3960)

- [ ] **Step 1: Правка 3 — заменить override ОргБух у НДФЛ-копии в ОЗФУ**

old_string (контекст со строкой СпособОтражения для уникальности — это блок НачисленныйНДФЛ, НЕ УдержаннаяЗарплата):
```bsl
		НоваяСтр.СпособОтраженияЗарплатыВБухучете = Справочники.СпособыОтраженияЗарплатыВБухучете.ПустаяСсылка();
		НоваяСтр.ОрганизацияБухгалтерия = _ОЗП_ОргБухИзКарты(КартаОргБух, СтрБ.ФизическоеЛицо, СтрБ.ПодразделениеПредприятия);
```
new_string:
```bsl
		НоваяСтр.СпособОтраженияЗарплатыВБухучете = Справочники.СпособыОтраженияЗарплатыВБухучете.ПустаяСсылка();
		// Пріоритет — per-org ОргБух з рядка-джерела (Правка 1); карта «Зарплата управл» лише як fallback.
		НоваяСтр.ОрганизацияБухгалтерия = ?(ЗначениеЗаполнено(СтрБ.ОрганизацияБухгалтерия), СтрБ.ОрганизацияБухгалтерия,
			_ОЗП_ОргБухИзКарты(КартаОргБух, СтрБ.ФизическоеЛицо, СтрБ.ПодразделениеПредприятия));
```

- [ ] **Step 2: Правка 4 — per-org в `_ПНМП_ЗаполнитьНалоги`**

old_string:
```bsl
	// НДФЛ / ВС
	Для Каждого Стр Из НачисленныйНДФЛ Цикл
		Если Стр.ФормаPL <> Ф1 ИЛИ Стр.Сумма = 0 Тогда Продолжить; КонецЕсли;
		Доп = Новый Структура("ТипНалога, КодДохода", Стр.ТипНалога, Стр.КодДохода);
		_ПНМП_ДобавитьСтроку(Док.Налоги, Стр.ФизическоеЛицо, СотрудникПоФЛ[Стр.ФизическоеЛицо],
			ОргБухПоФЛ[Стр.ФизическоеЛицо], Стр.ПодразделениеПредприятия, Стр.Сумма, Доп);
	КонецЦикла;
	// ЕСВ (ВзносыВсего)
	Для Каждого Стр Из НачисленнаяЗарплатаИВзносыПоФизлицам Цикл
		Если Стр.ФормаPL <> Ф1 ИЛИ Стр.ВзносыВсего = 0 Тогда Продолжить; КонецЕсли;
		Доп = Новый Структура("ТипНалога", Перечисления.ТипыНалогов.НачисленныйЕСВ);
		_ПНМП_ДобавитьСтроку(Док.Налоги, Стр.ФизическоеЛицо, СотрудникПоФЛ[Стр.ФизическоеЛицо],
			ОргБухПоФЛ[Стр.ФизическоеЛицо], Стр.ПодразделениеПредприятия, Стр.ВзносыВсего, Доп);
	КонецЦикла;
```
new_string:
```bsl
	// НДФЛ / ВС — ОргБух per-org із самого рядка НачисленныйНДФЛ (Правка 1), fallback на мапу
	Для Каждого Стр Из НачисленныйНДФЛ Цикл
		Если Стр.ФормаPL <> Ф1 ИЛИ Стр.Сумма = 0 Тогда Продолжить; КонецЕсли;
		ОргБухСтр = ?(ЗначениеЗаполнено(Стр.ОрганизацияБухгалтерия), Стр.ОрганизацияБухгалтерия, ОргБухПоФЛ[Стр.ФизическоеЛицо]);
		Доп = Новый Структура("ТипНалога, КодДохода", Стр.ТипНалога, Стр.КодДохода);
		_ПНМП_ДобавитьСтроку(Док.Налоги, Стр.ФизическоеЛицо, СотрудникПоФЛ[Стр.ФизическоеЛицо],
			ОргБухСтр, Стр.ПодразделениеПредприятия, Стр.Сумма, Доп);
	КонецЦикла;
	// ЕСВ (ВзносыВсего) — ОргБух per-org із рядка НачисленнаяЗарплата (ПроставитьОргБухВсемФорма1), fallback на мапу
	Для Каждого Стр Из НачисленнаяЗарплатаИВзносыПоФизлицам Цикл
		Если Стр.ФормаPL <> Ф1 ИЛИ Стр.ВзносыВсего = 0 Тогда Продолжить; КонецЕсли;
		ОргБухСтр = ?(ЗначениеЗаполнено(Стр.ОрганизацияБухгалтерия), Стр.ОрганизацияБухгалтерия, ОргБухПоФЛ[Стр.ФизическоеЛицо]);
		Доп = Новый Структура("ТипНалога", Перечисления.ТипыНалогов.НачисленныйЕСВ);
		_ПНМП_ДобавитьСтроку(Док.Налоги, Стр.ФизическоеЛицо, СотрудникПоФЛ[Стр.ФизическоеЛицо],
			ОргБухСтр, Стр.ПодразделениеПредприятия, Стр.ВзносыВсего, Доп);
	КонецЦикла;
```

- [ ] **Step 3: Code review всех правок** — `/1c-bsl-review Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` (или визуальная сверка: табы-отступы, нет битых имён).

- [ ] **Step 4: Commit правок (4 точки, один коммит)**

```bash
git add "Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl"
git commit -m "fix(ОЗП): НДФЛ/ВС Форма1 per-org (НалогиБухгалтерия.Организация сквозняком до А_ВзСС)"
```

---

## Task 6: Загрузка в БД

**Files:** —

- [ ] **Step 1: Partial load + update**

Загрузить из файлов в серверную БД (skills): метаданные документа (новое поле) + ObjectModule.
- `/db-load-xml -Mode Partial` для `Documents/А_ОтражениеЗПпоКазне.xml` и `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` (auto `-updateConfigDumpInfo`).
- `/db-update -Dynamic+`.

Expected: загрузка без ошибок; конфигурация БД обновлена. (Если харнесс Task 2 падал «поле не существует» — этот шаг выполнить ДО Task 7; код к этому моменту уже на диске.)

---

## Task 7: GREEN-8a — ТЧ-уровень (in-memory)

**Files:** — (использует `green_perorg_ndfl_inmemory.py` из Task 2)

- [ ] **Step 1: Запустить харнесс**

Run: `& "C:\Python313\python.exe" "knowledge_А_ОтражениеЗПпоКазне\Python\test\green_perorg_ndfl_inmemory.py"`
Expected GREEN: НДФЛ-строк Ф1 с пустой ОргБух = 0; «Удержание»-строк Ф1 с пустой ОргБух = 0; Воронцов НДФЛ ДЖИ ≈ 4 703,50 / ІНД ≈ 36 800; «Удержание» зеркалит НДФЛ per-org.

- [ ] **Step 2: Расширить на все 11 A-ФЛ** — если доки разные (см. Task 8), прогнать харнесс по каждому документу из списка; для всех 11 ОргБух непустая и сплит = `НалогиБухгалтерия.Организация`.

---

## Task 8: Список документов для UI-перепроведения + handoff

**Files:**
- Create: `knowledge_А_ОтражениеЗПпоКазне/Python/test/list_docs_for_repost.py`

- [ ] **Step 1: Запросить документы, содержащие 11 A-ФЛ (декабрь 2025)**

```python
# -*- coding: utf-8 -*-
import win32com.client, sys
if sys.stdout.encoding != 'utf-8':
    sys.stdout.reconfigure(encoding='utf-8')
erp = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
q = erp.NewObject("Запрос")
q.Текст = """
ВЫБРАТЬ РАЗЛИЧНЫЕ Док.Ссылка.Номер КАК Номер, Док.Ссылка.Дата КАК Дата
ИЗ Документ.А_ОтражениеЗПпоКазне.НалогиБухгалтерия КАК Док
ГДЕ Док.Ссылка.Дата МЕЖДУ &Д1 И &Д2 И Док.СуммаНДФЛ <> 0
    И (Док.Сотрудник.ФизическоеЛицо.Наименование ПОДОБНО "Воронцов%"
        ИЛИ Док.Сотрудник.ФизическоеЛицо.Наименование ПОДОБНО "Манжула%"
        ИЛИ Док.Сотрудник.ФизическоеЛицо.Наименование ПОДОБНО "Аулова%"
        ИЛИ Док.Сотрудник.ФизическоеЛицо.Наименование ПОДОБНО "Шостак%"
        ИЛИ Док.Сотрудник.ФизическоеЛицо.Наименование ПОДОБНО "Соколенко%"
        ИЛИ Док.Сотрудник.ФизическоеЛицо.Наименование ПОДОБНО "Шинкаренко%"
        ИЛИ Док.Сотрудник.ФизическоеЛицо.Наименование ПОДОБНО "Шкурат%"
        ИЛИ Док.Сотрудник.ФизическоеЛицо.Наименование ПОДОБНО "Пономаренко%"
        ИЛИ Док.Сотрудник.ФизическоеЛицо.Наименование ПОДОБНО "Травінськ%"
        ИЛИ Док.Сотрудник.ФизическоеЛицо.Наименование ПОДОБНО "Скрипник С%")
УПОРЯДОЧИТЬ ПО Дата, Номер
"""
q.УстановитьПараметр("Д1", erp.NewObject("Дата","2025-12-01"))
q.УстановитьПараметр("Д2", erp.NewObject("Дата","2025-12-31"))
t = q.Выполнить().Выгрузить()
print("=== Документы А_ОтражениеЗПпоКазне для UI-перепроведения ===")
for i in range(t.Количество()):
    r = t.Получить(i); print(f"  №{r.Номер}  {r.Дата}")
```

- [ ] **Step 2: Запустить, зафиксировать список**

Run: `& "C:\Python313\python.exe" "knowledge_А_ОтражениеЗПпоКазне\Python\test\list_docs_for_repost.py"`

- [ ] **Step 3: Commit + handoff пользователю**

```bash
git add "knowledge_А_ОтражениеЗПпоКазне/Python/test/list_docs_for_repost.py"
git commit -m "test(ОЗП): список документов для UI-перепроведения (11 A-ФЛ декабрь)"
```
Передать пользователю список и инструкцию: для каждого документа в UI 1С — Заполнить всё → Создать ОЗФУ → Создать ПередачаНач → провести. **СТОП до подтверждения пользователя.**

---

## Task 9: GREEN-8b — регистр-уровень (ПОСЛЕ UI-перепроведения)

**Files:**
- Create: `knowledge_А_ОтражениеЗПпоКазне/Python/test/green_reconciliation_perorg.py`

- [ ] **Step 1: Сверка per (ФЛ × юр.лицо) А_ВзСС Форма1 vs Бух 661/663**

Скрипт: для 11 A-ФЛ собрать А_ВзСС Форма1 кон.остаток per (ФЛ, ОргБух) и сверить с Бух 661/663 (Кт−Дт) per (ФЛ, ЕДРПОУ). GREEN: по каждому (ФЛ × юр.лицо) `Разн.кон.остаток = 0` (Воронцов ДЖИ 0 / ІНД 0). (Эталонная логика сверки — `_Rarzrabotki/Отчеты/А_СравнитьОстаткиВзаиморасчетыСотрудникиЕРПсBASБухгалтерия`; ключ ФЛ = А_ИдКод + ЕДРПОУ, знак Кт−Дт — см. память `sravnit_vzss_sotrudniki_report`.)

- [ ] **Step 2: Регрессия**

- Батрак: Форма1 Строительство 15 400→0, Форма2 Производство 20 300→0 (без изменений).
- Σ(Ф1+Ф2) per ФЛ для 11 A-ФЛ — равна доправочной (только перераспределение НДФЛ между юр.лицами).
- Прогнать отчёт сверки целиком: итог по 11 A-ФЛ ушёл в 0; класс B/C/D диагноза не изменился.

- [ ] **Step 3: Commit**

```bash
git add "knowledge_А_ОтражениеЗПпоКазне/Python/test/green_reconciliation_perorg.py"
git commit -m "test(ОЗП): GREEN регистр-уровень — per (ФЛ×юр.лицо) сверка = 0 после UI-перепроведения"
```

---

## Task 10: Knowledge + финал

**Files:**
- Modify: `knowledge_А_ОтражениеЗПпоКазне/LESSONS.md` (§23), `_Rarzrabotki/notebook/knowledge/KNOWLEDGE_MAP.md`

- [ ] **Step 1: Записать урок §23** — суть: per-org НДФЛ/ВС из `НалогиБухгалтерия.Организация`, 4 правки тега, заполнитель А_ВзСС уже org-aware, перепроведение только UI.
- [ ] **Step 2: Строка в KNOWLEDGE_MAP.md.**
- [ ] **Step 3: Commit** `docs(knowledge): LESSONS §23 + KNOWLEDGE_MAP — per-org НДФЛ Форма1`.
- [ ] **Step 4: Обновить память** (`feedback`/`project`) о результате, если есть нетривиальный вывод.

---

## Self-Review (выполнить после написания плана)

- **Покрытие спеки:** §6 Правки 1-4 → Task 3/4/5; §9 RED → Task 1-2; загрузка → Task 6; GREEN-8a → Task 7; UI-список → Task 8; GREEN-8b + регрессия → Task 9; §11 артефакты/knowledge → Task 10. Покрыто.
- **Плейсхолдеры:** все old/new_string дословны; Python-скрипты полны. Нет TBD.
- **Согласованность имён:** `НДФЛКлюч`, структура `("ФЛ, Подр, ОргБух, Сумма")`, поле `З.ОргБух` — едины между блоками Task 4; `_ОЗП_ОргБухИзКарты(КартаОргБух, ФЛ, Подр)` — сигнатура совпадает с кодом; `ОргБухСтр` объявлена и используется в Task 5.
