# «Загрузка материалов в СС» — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Переделать внешнюю обработку «Загрузка материалов в СС» так, чтобы она читала лист «Проект СС» Excel-шаблона производства и заполняла ТЧ `Комплектующие` элемента `Справочник.СтруктураСебестоимости` в базе **BuhBud** строками-материалами.

**Architecture:** Тонкая форма (`&НаКлиенте`, COM `Excel.Application`) читает лист «Проект СС» по имени, фильтрует строки `C="Матеріал"`, собирает массив структур и передаёт на сервер; движок `Ext/ObjectModule.bsl` в транзакции очищает ТЧ `Комплектующие`, делает get-or-create этапа (`Справочник.ЭтапыРабот`) и заполняет строки (НоменклатураСС/ЕдиницаСС — строки, Количество/Сумма — числа, Этап — ссылка). Ссылочные `Номенклатура`/`ОбщееНазвание`/`Единица` не заполняются.

**Tech Stack:** 1С:Предприятие 8.3 (BSL, управляемая форма, внешняя обработка .epf), Python 3.11 + win32com (`V83.COMConnector`) + openpyxl для smoke-теста, навык `epf-build`.

**Spec:** `docs/superpowers/specs/2026-07-08-zagruzka-materialov-v-ss-design.md`

---

## Пути и окружение (прочитать до старта)

- **Worktree (git, редактируем здесь):**
  `C:\Configuration_downloads\BASERP25\.claude\worktrees\pensive-maxwell-b838b2`
  Ветка: `claude/hungry-mcnulty-ca2a24`.
- **Main (Designer/сервер/COM грузят отсюда — сюда `cp` после каждой правки, Rule #4):**
  `C:\Configuration_downloads\BASERP25`
- **Каталог обработки (относительный, есть и в worktree, и в main):**
  `_Rarzrabotki\Загрузка СС\Загрузка материалов в СС\`
- **Итоговый .epf:** `_Rarzrabotki\Загрузка СС\Загрузка материалов в СС.epf` (main)
- **BuhBud COM:** `Srvr="SQLSERVER";Ref="BuhBud";Usr="cfo";Pwd="2442"`
- **Excel-шаблоны (лист «Проект СС»):**
  - `_Rarzrabotki\Загрузка СС\Виробництво\IRC 15м2 НОВИЙ ШАблон  СС Виробництво 16-06-2026_.xlsx`
  - `_Rarzrabotki\Загрузка СС\Виробництво\IRC 30м2 НОВИЙ ШАблон  СС Виробництво 16-06-2026_.xlsx`

**Проверенные факты (COM/openpyxl):**
- ТЧ `Комплектующие`: `НоменклатураСС` (Строка), `ЕдиницаСС` (Строка), `Количество` (Число), `Сумма` (Число), `Этап` (ссылка → `Справочник.ЭтапыРабот`, неиерархический). Прочие `Номенклатура`/`ОбщееНазвание`/`Единица` — ссылки, **не трогаем**.
- Все 8 этапов шаблона уже есть в `Справочник.ЭтапыРабот` (создаваться не будут).
- Колонки листа «Проект СС» (1-based): C=3 (тип), E=5 (Этап), G=7 (наименование), I=9 (ед.), J=10 (кол-во), O=15 (сумма).
- Ожидаемая загрузка (фильтр `C="Матеріал"` + непустое G): **15м2 → 168 строк, Σ Сумма = 705624.01**; **30м2 → 170 строк, Σ Сумма = 1007520.94**. 16 пропущенных строк — пустые строки-заглушки («Монтаж будинку», всё по нулям).

---

## Task 1: RED — Python COM smoke-тест движка

**Files:**
- Create: `_Rarzrabotki\Python\test\test_zagruzka_materialov.py`

- [ ] **Step 1: Написать smoke-тест (RED)**

Создать файл `C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_zagruzka_materialov.py`:

```python
# -*- coding: utf-8 -*-
"""Smoke внешней обработки «Загрузка материалов в СС» на базе BuhBud.
Ожидание вычисляется из того же Excel через openpyxl, факт — из ТЧ Комплектующие
временной СтруктураСебестоимости после вызова движка ЗагрузитьМатериалыИзМассива."""
import sys, os
sys.stdout.reconfigure(encoding="utf-8")
import win32com.client
import openpyxl

EPF = r"C:\Configuration_downloads\BASERP25\_Rarzrabotki\Загрузка СС\Загрузка материалов в СС.epf"
XLSX = r"C:\Configuration_downloads\BASERP25\_Rarzrabotki\Загрузка СС\Виробництво\IRC 15м2 НОВИЙ ШАблон  СС Виробництво 16-06-2026_.xlsx"
TESTNAME = "__ТЕСТ Загрузка материалов СС"
CONN = 'Srvr="SQLSERVER";Ref="BuhBud";Usr="cfo";Pwd="2442"'

def num(v):
    try:
        return float(v) if v not in (None, "") else 0.0
    except Exception:
        return 0.0

def read_expected(path):
    wb = openpyxl.load_workbook(path, data_only=True)
    ws = wb["Проект СС"]
    def g(r, c): return ws.cell(row=r, column=c).value
    rows = []
    for r in range(2, ws.max_row + 1):
        c = g(r, 3)
        if (str(c).strip() if c is not None else "") != "Матеріал":
            continue
        gname = (str(g(r, 7)).strip() if g(r, 7) is not None else "")
        if gname == "":
            continue
        rows.append({
            "Этап": (str(g(r, 5)).strip() if g(r, 5) is not None else ""),
            "НоменклатураСС": gname,
            "ЕдиницаСС": (str(g(r, 9)).strip() if g(r, 9) is not None else ""),
            "Количество": num(g(r, 10)),
            "Сумма": num(g(r, 15)),
        })
    return rows

def get_or_create_struct(erp):
    q = erp.NewObject("Запрос")
    q.Text = "ВЫБРАТЬ Ссылка ИЗ Справочник.СтруктураСебестоимости ГДЕ Наименование = &Н"
    q.УстановитьПараметр("Н", TESTNAME)
    sel = q.Execute().Выбрать()
    if sel.Следующий():
        return sel.Ссылка
    obj = erp.Справочники.СтруктураСебестоимости.СоздатьЭлемент()
    obj.Наименование = TESTNAME
    obj.Записать()
    return obj.Ссылка

def build_array(erp, rows):
    mass = erp.NewObject("Массив")
    for row in rows:
        s = erp.NewObject("Структура")
        s.Вставить("Этап", row["Этап"])
        s.Вставить("НоменклатураСС", row["НоменклатураСС"])
        s.Вставить("ЕдиницаСС", row["ЕдиницаСС"])
        s.Вставить("Количество", row["Количество"])
        s.Вставить("Сумма", row["Сумма"])
        mass.Добавить(s)
    return mass

def tc_totals(erp, ref):
    q = erp.NewObject("Запрос")
    q.Text = ("ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК К, СУММА(Сумма) КАК С, "
              "СУММА(ВЫБОР КОГДА Этап <> ЗНАЧЕНИЕ(Справочник.ЭтапыРабот.ПустаяСсылка) ТОГДА 1 ИНАЧЕ 0 КОНЕЦ) КАК СЭтап, "
              "СУММА(ВЫБОР КОГДА НоменклатураСС <> \"\" ТОГДА 1 ИНАЧЕ 0 КОНЕЦ) КАК СНаим "
              "ИЗ Справочник.СтруктураСебестоимости.Комплектующие ГДЕ Ссылка = &Р")
    q.УстановитьПараметр("Р", ref)
    sel = q.Execute().Выбрать()
    sel.Следующий()
    return int(sel.К or 0), float(sel.С or 0), int(sel.СЭтап or 0), int(sel.СНаим or 0)

def main():
    assert os.path.exists(EPF), f"НЕ найден .epf: {EPF} (собери обработку — Task 5)"
    exp = read_expected(XLSX)
    exp_cnt = len(exp)
    exp_sum = round(sum(r["Сумма"] for r in exp), 2)
    print(f"ОЖИДАНИЕ (Excel): строк={exp_cnt}, ΣСумма={exp_sum}")

    v8 = win32com.client.Dispatch("V83.COMConnector")
    erp = v8.Connect(CONN)
    ref = get_or_create_struct(erp)
    mass = build_array(erp, exp)

    obr = erp.ВнешниеОбработки.Создать(EPF, False)
    res = obr.ЗагрузитьМатериалыИзМассива(ref, mass)
    print(f"ПРОТОКОЛ: Ошибка={res.Ошибка} Текст='{res.ТекстОшибки}' "
          f"Загружено={res.Загружено} СозданоЭтапов={res.СозданоЭтапов} Пропущено={res.Пропущено}")
    assert res.Ошибка is False, f"движок вернул ошибку: {res.ТекстОшибки}"
    assert int(res.Загружено) == exp_cnt, f"Загружено {res.Загружено} != {exp_cnt}"

    k, s, s_etap, s_naim = tc_totals(erp, ref)
    print(f"ФАКТ (ТЧ): строк={k}, ΣСумма={round(s,2)}, сЭтапом={s_etap}, сНаим={s_naim}")
    assert k == exp_cnt, f"строк в ТЧ {k} != {exp_cnt}"
    assert abs(s - exp_sum) < 0.05, f"ΣСумма {s} != {exp_sum}"
    assert s_naim == exp_cnt, "не все НоменклатураСС заполнены"
    assert s_etap == exp_cnt, "не все Этап заполнены (ссылка)"

    # идемпотентность: повторный прогон — те же строки, 0 новых этапов
    res2 = obr.ЗагрузитьМатериалыИзМассива(ref, mass)
    k2, s2, _, _ = tc_totals(erp, ref)
    assert k2 == exp_cnt, f"идемпотентность: строк {k2} != {exp_cnt}"
    assert int(res2.СозданоЭтапов) == 0, f"идемпотентность: создано этапов {res2.СозданоЭтапов} != 0"
    print("OK: smoke пройден (загрузка + суммы + этапы + идемпотентность)")

if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Прогнать тест — убедиться, что RED**

Run:
```
python "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_zagruzka_materialov.py"
```
Expected: **FAIL** — `AssertionError: НЕ найден .epf ...` (обработка ещё не собрана) либо, если старый .epf есть, `AttributeError`/COM-ошибка на `ЗагрузитьМатериалыИзМассива` (метода нет в байт-копии). Это ожидаемый красный.

---

## Task 2: Движок — `Ext/ObjectModule.bsl`

**Files:**
- Modify (полная перезапись): `_Rarzrabotki\Загрузка СС\Загрузка материалов в СС\Ext\ObjectModule.bsl` (в worktree)

- [ ] **Step 1: Перезаписать ObjectModule.bsl новым содержимым**

Записать файл (worktree-путь) целиком:

```bsl

#Область ПрограммныйИнтерфейс

// Загружает строки материалов в ТЧ Комплектующие переданной структуры себестоимости.
// СтруктураСеб - СправочникСсылка.СтруктураСебестоимости
// МассивСтрок  - Массив структур {Этап, НоменклатураСС, ЕдиницаСС, Количество, Сумма}
// Возврат      - Структура с протоколом загрузки.
Функция ЗагрузитьМатериалыИзМассива(СтруктураСеб, МассивСтрок) Экспорт

	Результат = Новый Структура;
	Результат.Вставить("Загружено", 0);
	Результат.Вставить("СозданоЭтапов", 0);
	Результат.Вставить("Пропущено", 0);
	Результат.Вставить("Ошибка", Ложь);
	Результат.Вставить("ТекстОшибки", "");

	Если НЕ ЗначениеЗаполнено(СтруктураСеб) Тогда
		Результат.Ошибка = Истина;
		Результат.ТекстОшибки = "Не указана структура себестоимости";
		Возврат Результат;
	КонецЕсли;

	КэшЭтапов = ПостроитьКэшЭтапов();

	НачатьТранзакцию();
	Попытка
		ОбъектСеб = СтруктураСеб.ПолучитьОбъект();
		ОбъектСеб.Комплектующие.Очистить();

		Для Каждого СтрокаДанных Из МассивСтрок Цикл
			НаимМатериала = СокрЛП("" + СтрокаДанных.НоменклатураСС);
			Если ПустаяСтрока(НаимМатериала) Тогда
				Результат.Пропущено = Результат.Пропущено + 1;
				Продолжить;
			КонецЕсли;

			Этап = ПолучитьИлиСоздатьЭтап(СтрокаДанных.Этап, КэшЭтапов, Результат);

			НоваяСтрока = ОбъектСеб.Комплектующие.Добавить();
			НоваяСтрока.Этап = Этап;
			НоваяСтрока.НоменклатураСС = НаимМатериала;
			НоваяСтрока.ЕдиницаСС = СокрЛП("" + СтрокаДанных.ЕдиницаСС);
			НоваяСтрока.Количество = ПолучитьЧисло(СтрокаДанных, "Количество");
			НоваяСтрока.Сумма = ПолучитьЧисло(СтрокаДанных, "Сумма");
			Результат.Загружено = Результат.Загружено + 1;
		КонецЦикла;

		ОбъектСеб.Записать();
		ЗафиксироватьТранзакцию();
	Исключение
		ОтменитьТранзакцию();
		Результат.Ошибка = Истина;
		Результат.ТекстОшибки = ОписаниеОшибки();
		ЗаписьЖурналаРегистрации("ЗагрузкаМатериаловВСС.Ошибка",
			УровеньЖурналаРегистрации.Ошибка, , СтруктураСеб, Результат.ТекстОшибки);
	КонецПопытки;

	Возврат Результат;

КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Возвращает числовое значение поля структуры по имени (пусто/не число -> 0).
Функция ПолучитьЧисло(СтрокаДанных, ИмяПоля)
	Если НЕ СтрокаДанных.Свойство(ИмяПоля) Тогда
		Возврат 0;
	КонецЕсли;
	Значение = СтрокаДанных[ИмяПоля];
	Если НЕ ЗначениеЗаполнено(Значение) Тогда
		Возврат 0;
	КонецЕсли;
	Попытка
		Возврат Число(Значение);
	Исключение
		Возврат 0;
	КонецПопытки;
КонецФункции

// Нормализация: убрать концевые пробелы + схлопнуть любые серии пробелов в один.
Функция НормализоватьНаименование(Знач Текст) Экспорт
	Текст = СокрЛП("" + Текст);
	Пока Найти(Текст, "  ") > 0 Цикл
		Текст = СтрЗаменить(Текст, "  ", " ");
	КонецЦикла;
	Возврат Текст;
КонецФункции

// Кэш этапов: НРег(нормализованное наименование) -> Ссылка.
Функция ПостроитьКэшЭтапов()
	Кэш = Новый Соответствие;
	Запрос = Новый Запрос("ВЫБРАТЬ Наименование, Ссылка ИЗ Справочник.ЭтапыРабот");
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Ключ = НРег(НормализоватьНаименование(Выборка.Наименование));
		Если Кэш.Получить(Ключ) = Неопределено Тогда
			Кэш.Вставить(Ключ, Выборка.Ссылка);
		КонецЕсли;
	КонецЦикла;
	Возврат Кэш;
КонецФункции

Функция ПолучитьИлиСоздатьЭтап(Знач Наим, КэшЭтапов, Результат)
	НормИмя = НормализоватьНаименование(Наим);
	Если ПустаяСтрока(НормИмя) Тогда
		Возврат Справочники.ЭтапыРабот.ПустаяСсылка();
	КонецЕсли;
	Ключ = НРег(НормИмя);
	Найденное = КэшЭтапов.Получить(Ключ);
	Если Найденное <> Неопределено Тогда
		Возврат Найденное;
	КонецЕсли;

	НовыйЭтап = Справочники.ЭтапыРабот.СоздатьЭлемент();
	НовыйЭтап.Наименование = НормИмя;
	НовыйЭтап.Записать();
	КэшЭтапов.Вставить(Ключ, НовыйЭтап.Ссылка);
	Результат.СозданоЭтапов = Результат.СозданоЭтапов + 1;
	Возврат НовыйЭтап.Ссылка;
КонецФункции

#КонецОбласти
```

- [ ] **Step 2: Скопировать в main (Rule #4)**

Run:
```
cp "C:/Configuration_downloads/BASERP25/.claude/worktrees/pensive-maxwell-b838b2/_Rarzrabotki/Загрузка СС/Загрузка материалов в СС/Ext/ObjectModule.bsl" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Загрузка СС/Загрузка материалов в СС/Ext/ObjectModule.bsl"
```
Expected: без вывода (успех).

---

## Task 3: Модуль формы — `Forms/Форма/Ext/Form/Module.bsl`

**Files:**
- Modify (полная перезапись): `_Rarzrabotki\Загрузка СС\Загрузка материалов в СС\Forms\Форма\Ext\Form\Module.bsl` (в worktree)

- [ ] **Step 1: Перезаписать модуль формы новым содержимым**

Записать файл (worktree-путь) целиком:

```bsl

&НаКлиенте
Процедура ЗагрузитьМатериалы(Команда)

	Если НЕ ЗначениеЗаполнено(Объект.ИмяФайла) Тогда
		Сообщить("Не указан путь к файлу Excel (реквизит «Имя файла»)");
		Возврат;
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Объект.ДокСтруктураСебестоимости) Тогда
		Сообщить("Не выбрана структура себестоимости");
		Возврат;
	КонецЕсли;

	МассивСтрок = ПрочитатьExcelВМассив(Объект.ИмяФайла);
	Если МассивСтрок = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Протокол = ЗагрузитьНаСервере(МассивСтрок);
	ВывестиПротокол(Протокол);

КонецПроцедуры

&НаКлиенте
Функция ПрочитатьExcelВМассив(ИмяФайла)

	Попытка
		Excel = Новый COMОбъект("Excel.Application");
	Исключение
		Сообщить("Не удалось запустить Excel: " + ОписаниеОшибки());
		Возврат Неопределено;
	КонецПопытки;

	МассивСтрок = Новый Массив;
	Попытка
		Excel.Workbooks.Open(ИмяФайла);
		Лист = Excel.WorkSheets("Проект СС");
		ВсегоСтрок = Лист.Cells(1, 1).SpecialCells(11).Row;

		Для Ряд = 2 По ВсегоСтрок Цикл
			ТипСтроки = СокрЛП("" + Лист.Cells(Ряд, 3).Text);
			Если ТипСтроки <> "Матеріал" Тогда
				Продолжить;
			КонецЕсли;

			НаимМатериала = СокрЛП("" + Лист.Cells(Ряд, 7).Text);
			Если ПустаяСтрока(НаимМатериала) Тогда
				Продолжить;
			КонецЕсли;

			Стр = Новый Структура("Этап, НоменклатураСС, ЕдиницаСС, Количество, Сумма");
			Стр.Этап = СокрЛП("" + Лист.Cells(Ряд, 5).Text);
			Стр.НоменклатураСС = НаимМатериала;
			Стр.ЕдиницаСС = СокрЛП("" + Лист.Cells(Ряд, 9).Text);
			Стр.Количество = ЧислоИзЯчейки(Лист.Cells(Ряд, 10).Value);
			Стр.Сумма = ЧислоИзЯчейки(Лист.Cells(Ряд, 15).Value);
			МассивСтрок.Добавить(Стр);
		КонецЦикла;

		Excel.ActiveWorkbook.Close(Ложь);
		Excel.Quit();
		Excel = Неопределено;
	Исключение
		ТекстОшибки = ОписаниеОшибки();
		Попытка
			Excel.Quit();
		Исключение
		КонецПопытки;
		Excel = Неопределено;
		Сообщить("Ошибка чтения Excel: " + ТекстОшибки);
		Возврат Неопределено;
	КонецПопытки;

	Возврат МассивСтрок;

КонецФункции

&НаКлиенте
Функция ЧислоИзЯчейки(Значение)
	Если НЕ ЗначениеЗаполнено(Значение) Тогда
		Возврат 0;
	КонецЕсли;
	Попытка
		Возврат Число(Значение);
	Исключение
		Возврат 0;
	КонецПопытки;
КонецФункции

&НаСервере
Функция ЗагрузитьНаСервере(МассивСтрок)
	ОбъектОбработки = РеквизитФормыВЗначение("Объект");
	Возврат ОбъектОбработки.ЗагрузитьМатериалыИзМассива(
		ОбъектОбработки.ДокСтруктураСебестоимости, МассивСтрок);
КонецФункции

&НаКлиенте
Процедура ВывестиПротокол(Протокол)
	Если Протокол.Ошибка Тогда
		Сообщить("ОШИБКА загрузки: " + Протокол.ТекстОшибки);
		Возврат;
	КонецЕсли;
	Сообщить("Загрузка завершена.");
	Сообщить("  Загружено материалов: " + Протокол.Загружено);
	Сообщить("  Создано этапов:       " + Протокол.СозданоЭтапов);
	Сообщить("  Пропущено пустых:     " + Протокол.Пропущено);
КонецПроцедуры
```

- [ ] **Step 2: Скопировать в main (Rule #4)**

Run:
```
cp "C:/Configuration_downloads/BASERP25/.claude/worktrees/pensive-maxwell-b838b2/_Rarzrabotki/Загрузка СС/Загрузка материалов в СС/Forms/Форма/Ext/Form/Module.bsl" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Загрузка СС/Загрузка материалов в СС/Forms/Форма/Ext/Form/Module.bsl"
```
Expected: без вывода (успех).

---

## Task 4: Переименовать команду/кнопку формы — `Forms/Форма/Ext/Form.xml`

**Files:**
- Modify: `_Rarzrabotki\Загрузка СС\Загрузка материалов в СС\Forms\Форма\Ext\Form.xml` (в worktree)

Внутреннее имя объекта `ВнешняяОбработка1` НЕ меняем (это только машинное имя, совпадает с образцом). Меняем только имя команды/кнопки/подписи, чтобы Action совпал с процедурой `ЗагрузитьМатериалы`. ID не трогаем.

- [ ] **Step 1: Заменить блок кнопки в AutoCommandBar**

Edit — old:
```xml
			<Button name="ФормаЗагрузитьРаботы" id="7">
				<Type>CommandBarButton</Type>
				<CommandName>Form.Command.ЗагрузитьРаботы</CommandName>
				<ExtendedTooltip name="ФормаЗагрузитьРаботыРасширеннаяПодсказка" id="8"/>
			</Button>
```
new:
```xml
			<Button name="ФормаЗагрузитьМатериалы" id="7">
				<Type>CommandBarButton</Type>
				<CommandName>Form.Command.ЗагрузитьМатериалы</CommandName>
				<ExtendedTooltip name="ФормаЗагрузитьМатериалыРасширеннаяПодсказка" id="8"/>
			</Button>
```

- [ ] **Step 2: Заменить блок Command**

Edit — old:
```xml
		<Command name="ЗагрузитьРаботы" id="1">
			<Title>
				<v8:item>
					<v8:lang>ru</v8:lang>
					<v8:content>Загрузить работы</v8:content>
				</v8:item>
			</Title>
			<ToolTip>
				<v8:item>
					<v8:lang>ru</v8:lang>
					<v8:content>Загрузить работы</v8:content>
				</v8:item>
			</ToolTip>
			<Action>ЗагрузитьРаботы</Action>
		</Command>
```
new:
```xml
		<Command name="ЗагрузитьМатериалы" id="1">
			<Title>
				<v8:item>
					<v8:lang>ru</v8:lang>
					<v8:content>Загрузить материалы</v8:content>
				</v8:item>
			</Title>
			<ToolTip>
				<v8:item>
					<v8:lang>ru</v8:lang>
					<v8:content>Загрузить материалы</v8:content>
				</v8:item>
			</ToolTip>
			<Action>ЗагрузитьМатериалы</Action>
		</Command>
```

- [ ] **Step 3: Скопировать в main (Rule #4)**

Run:
```
cp "C:/Configuration_downloads/BASERP25/.claude/worktrees/pensive-maxwell-b838b2/_Rarzrabotki/Загрузка СС/Загрузка материалов в СС/Forms/Форма/Ext/Form.xml" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Загрузка СС/Загрузка материалов в СС/Forms/Форма/Ext/Form.xml"
```
Expected: без вывода (успех).

---

## Task 5: Собрать .epf против BuhBud + preflight

**Files:**
- Create/Update: `_Rarzrabotki\Загрузка СС\Загрузка материалов в СС.epf` (main)

- [ ] **Step 1: Сборка через навык epf-build**

Использовать навык `epf-build`: source — каталог обработки в **main**
`C:\Configuration_downloads\BASERP25\_Rarzrabotki\Загрузка СС\Загрузка материалов в СС`
(корневой `Загрузка материалов в СС.xml`), база сборки — **BuhBud**
(`Srvr="SQLSERVER";Ref="BuhBud"`), выход —
`C:\Configuration_downloads\BASERP25\_Rarzrabotki\Загрузка СС\Загрузка материалов в СС.epf`.

Причина BuhBud: реквизит `ДокСтруктураСебестоимости` имеет тип
`CatalogRef.СтруктураСебестоимости`, который есть только в BuhBud.

Ожидание: сборка без ошибок, .epf создан.

- [ ] **Step 2: Preflight — открыть .epf самому и проверить наличие метода**

Run:
```
python -c "import win32com.client as w; v8=w.Dispatch('V83.COMConnector'); erp=v8.Connect('Srvr=\"SQLSERVER\";Ref=\"BuhBud\";Usr=\"cfo\";Pwd=\"2442\"'); o=erp.ВнешниеОбработки.Создать(r'C:\Configuration_downloads\BASERP25\_Rarzrabotki\Загрузка СС\Загрузка материалов в СС.epf', False); print('создана, метод есть:', hasattr(o,'ЗагрузитьМатериалыИзМассива'))"
```
Expected: `создана, метод есть: True`. Если `False`/ошибка — вернуться к Task 2/5 (см. [[epf_build_stub_isolation_gotcha]], [[win32com_epf_smoke_gotchas]]).

---

## Task 6: GREEN — прогнать smoke-тест

- [ ] **Step 1: Запустить smoke (15м2)**

Run:
```
python "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_zagruzka_materialov.py"
```
Expected (PASS):
```
ОЖИДАНИЕ (Excel): строк=168, ΣСумма=705624.01
ПРОТОКОЛ: Ошибка=False Текст='' Загружено=168 СозданоЭтапов=0 Пропущено=16
ФАКТ (ТЧ): строк=168, ΣСумма=705624.01, сЭтапом=168, сНаим=168
OK: smoke пройден (загрузка + суммы + этапы + идемпотентность)
```

- [ ] **Step 2: Проверить второй шаблон (30м2)**

Временно поменять в тесте константу `XLSX` на путь 30м2 (или прогнать однострочником с подменой) и убедиться: `строк=170, ΣСумма=1007520.94`. Затем вернуть `XLSX` на 15м2.

Expected: `Загружено=170`, `ΣСумма=1007520.94`, `Пропущено=16`.

---

## Task 7: Ревью, синхронизация, коммит

- [ ] **Step 1: BSL code review**

Прогнать навык `/1c-bsl-review` по:
- `_Rarzrabotki\Загрузка СС\Загрузка материалов в СС\Ext\ObjectModule.bsl`
- `_Rarzrabotki\Загрузка СС\Загрузка материалов в СС\Forms\Форма\Ext\Form\Module.bsl`

Устранить замечания уровня «критично»/«важно». Косметику — по усмотрению.

- [ ] **Step 2: Убедиться, что main = worktree (Rule #4)**

Run (сверить, что нет расхождений по трём файлам обработки):
```
diff "C:/Configuration_downloads/BASERP25/.claude/worktrees/pensive-maxwell-b838b2/_Rarzrabotki/Загрузка СС/Загрузка материалов в СС/Ext/ObjectModule.bsl" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Загрузка СС/Загрузка материалов в СС/Ext/ObjectModule.bsl" && echo SAME_OBJ
diff "C:/Configuration_downloads/BASERP25/.claude/worktrees/pensive-maxwell-b838b2/_Rarzrabotki/Загрузка СС/Загрузка материалов в СС/Forms/Форма/Ext/Form/Module.bsl" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Загрузка СС/Загрузка материалов в СС/Forms/Форма/Ext/Form/Module.bsl" && echo SAME_FORM
diff "C:/Configuration_downloads/BASERP25/.claude/worktrees/pensive-maxwell-b838b2/_Rarzrabotki/Загрузка СС/Загрузка материалов в СС/Forms/Форма/Ext/Form.xml" "C:/Configuration_downloads/BASERP25/_Rarzrabotki/Загрузка СС/Загрузка материалов в СС/Forms/Форма/Ext/Form.xml" && echo SAME_XML
```
Expected: `SAME_OBJ`, `SAME_FORM`, `SAME_XML`.

- [ ] **Step 3: Коммит в worktree**

Run (из каталога worktree):
```
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/pensive-maxwell-b838b2"
git add "_Rarzrabotki/Загрузка СС/Загрузка материалов в СС" "_Rarzrabotki/Python/test/test_zagruzka_materialov.py" "docs/superpowers/plans/2026-07-08-zagruzka-materialov-v-ss.md"
git commit -m "feat(СС): обработка «Загрузка материалов в СС» — лист Проект СС → Комплектующие (BuhBud)"
```
Expected: коммит создан. Примечание: `.epf` — бинарник, добавить его отдельно, если так принято в репо (образец `Загрузка работ в СС.epf` в git есть).

- [ ] **Step 4: Обновить память проекта**

Записать memory-файл (по образцу [[zagruzka_rabot_v_ss]]): новый факт про «Загрузка материалов в СС» (BuhBud, лист «Проект СС», фильтр C="Матеріал", строковые НоменклатураСС/ЕдиницаСС, Этап→ЭтапыРабот, 168/170 строк, .epf собран против BuhBud), добавить строку в `MEMORY.md`.

---

## Self-Review (выполнено автором плана)

**1. Покрытие спеки:**
- §2 источник (лист по имени, фильтр C, колонки E/G/I/J/O) → Task 3.
- §3 приёмник (строковые НоменклатураСС/ЕдиницаСС, Этап-ссылка get-or-create, пустые ссылки) → Task 2.
- §4.3 режим «очищать и перезаписывать» → Task 2 (`Комплектующие.Очистить()`).
- §4.4 XML (тип уже корректен, косметика имени команды) → Task 4.
- §5 сборка/проверка (Python COM, epf-build BuhBud, smoke, review, cp) → Tasks 1,5,6,7.
- §6 критерии готовности → Task 6 (168/170, Σ, идемпотентность) + Task 7.

**2. Плейсхолдеры:** нет TBD/«добавить обработку ошибок» — весь код приведён целиком.

**3. Согласованность имён:** метод `ЗагрузитьМатериалыИзМассива` (движок) вызывается в форме (Task 3), в smoke (Task 1), проверяется в preflight (Task 5); поля ТЧ `НоменклатураСС/ЕдиницаСС/Количество/Сумма/Этап` одинаковы в Task 2 и в smoke-запросе Task 1; команда/Action `ЗагрузитьМатериалы` согласованы между Form.xml (Task 4) и модулем формы (Task 3).
