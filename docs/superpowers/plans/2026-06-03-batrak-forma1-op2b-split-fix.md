# Фикс сплита Форма1/Форма2 для Op2b-ФЛ — План реализации

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans (inline) или subagent-driven-development. Шаги — чекбоксы.

**Goal:** Устранить перекос Форма1↔Форма2 у 47 ФЛ без `НачисленияУпр` (Батрак: Ф1 17 717,12 → 15 400), направив ветку Op2b в тот же сплиттер, что и Op2a.

**Architecture:** В `_ОтрЗП_ЗаполнитьНачисленияЗП` (Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl) тело цикла Op2b заменяется вызовом `_ОтрЗП_СоздатьСтрокиNETФЛ_Новый(ФизЛицо, NET=ΣКазнаНачисления, НДФЛДляФЛ, Справочные)`. Результат = сплит `РаспределениеКазна.ФормаPL` (Ф1=Казна_Ф1, Ф2=Казна_Ф2). Op2a не трогаем. Валидация — Python COM (fill-in-memory + реконсиляция), затем UI-перепроведение цепочки №1.

**Tech Stack:** 1С BSL 8.3, V83.COMConnector, Python 3.13 (pywin32), skills `/db-load-xml`, `/db-update`.

**Спецификация:** `docs/superpowers/specs/2026-06-03-batrak-forma1-op2b-split-fix-design.md`

---

## Файлы

- Modify: `C:\Configuration_downloads\BASERP25\Documents\А_ОтражениеЗПпоКазне\Ext\ObjectModule.bsl` (живой код, ветка `claude/bas-doc-skills`; правим основную конфигурацию — её грузит Designer/сервер).
- Create: `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/green_op2b_fix.py` (GREEN-реконсиляция).

---

## Task 1: Заменить тело цикла Op2b на унифицированный сплиттер

**Files:** Modify `Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl` (внутри `_ОтрЗП_ЗаполнитьНачисленияЗП`, цикл `Для Каждого КЗ Из СумНачПоСотруднику`).

- [ ] **Step 1: Применить Edit** — заменить блок (текущие строки 2085–2149: от `// Отримуємо ФізОсобу` до конца НДФЛ-блока перед `КолДобавленоKasni = КолДобавленоKasni + 1`).

**OLD (удаляем):**
```bsl
		// Отримуємо ФізОсобу для цього Сотрудника (через кеш ФизЛица)
		ФизЛицо = Справочные.ФизЛица[Сотрудник];
		Если НЕ ЗначениеЗаполнено(ФизЛицо) Тогда
			// Fallback: використати Сотрудник.ФизическоеЛицо напряму
			Попытка
				ФизЛицо = Сотрудник.ФизическоеЛицо;
			Исключение
				Продолжить;
			КонецПопытки;
		КонецЕсли;

		Если НЕ ЗначениеЗаполнено(ФизЛицо) Тогда
			Продолжить;
		КонецЕсли;

		// Пропускаємо якщо цю ФізОсобу вже обробив Op 2a
		Если ОброблениФизЛица[ФизЛицо] <> Неопределено Тогда
			Продолжить;
		КонецЕсли;

		// НДФЛ/ВС НЕ додаємо до СумНачисления — піде окремим рядком
		НДФЛДляФЛ = СуммаНДФЛПоФизЛицу[ФизЛицо];
		Если НДФЛДляФЛ = Неопределено Тогда
			НДФЛДляФЛ = 0;
		КонецЕсли;
		// СумНачисления залишається чистою (без НДФЛ)

		// Беремо БазовыеНачисления розподіл з РаспределениеКазна і розподіляємо
		ДанныеРаспр = Неопределено;
		ДанныеСотрудника = РаспредПоПодразделениям[Сотрудник];
		Если ДанныеСотрудника <> Неопределено Тогда
			ДанныеРаспр = ДанныеСотрудника[КлючБазовые];
			Если ДанныеРаспр <> Неопределено И ДанныеРаспр.Итого = 0 Тогда
				ДанныеРаспр = Неопределено;
			КонецЕсли;
		КонецЕсли;

		// --- Розподіл чистої СумНачисления ---
		Если ДанныеРаспр = Неопределено Тогда
			// Fallback: 1 рядок ЦО
			НоваяСтрока = НачисленнаяЗарплатаИВзносыПоФизлицам.Добавить();
			_ОтрЗП_ЗаполнитьСтрокуНачисленияЗП_ПоФизЛицу(НоваяСтрока, ФизЛицо, СумНачисления, Справочные.ПодразделениеЦО, Неопределено, Справочные);
		Иначе
			_ОтрЗП_РаспределитьNETПоПодр(ФизЛицо, СумНачисления, ДанныеРаспр, Справочные);
		КонецЕсли;

		// --- НДФЛ/ВС: окремий рядок(и) ---
		Если НДФЛДляФЛ <> 0 И ДанныеСотрудника <> Неопределено Тогда
			ДанныеНДФЛРаспр = Неопределено;
			Для Каждого КЗ_Налог Из ДанныеСотрудника Цикл
				Если КЗ_Налог.Ключ = КлючБазовые Тогда Продолжить КонецЕсли;
				Если КЗ_Налог.Ключ = Справочные.НалогЕСВФОТ Тогда Продолжить КонецЕсли;
				ДанныеНДФЛРаспр = КЗ_Налог.Значение;
				Прервать;
			КонецЦикла;
			Если ДанныеНДФЛРаспр <> Неопределено И ДанныеНДФЛРаспр.Итого > 0 Тогда
				_ОтрЗП_РаспределитьNETПоПодр(ФизЛицо, НДФЛДляФЛ, ДанныеНДФЛРаспр, Справочные);
			Иначе
				НоваяСтрока = НачисленнаяЗарплатаИВзносыПоФизлицам.Добавить();
				_ОтрЗП_ЗаполнитьСтрокуНачисленияЗП_ПоФизЛицу(НоваяСтрока, ФизЛицо, НДФЛДляФЛ, Справочные.ПодразделениеЦО, Неопределено, Справочные);
			КонецЕсли;
		ИначеЕсли НДФЛДляФЛ <> 0 Тогда
			НоваяСтрока = НачисленнаяЗарплатаИВзносыПоФизлицам.Добавить();
			_ОтрЗП_ЗаполнитьСтрокуНачисленияЗП_ПоФизЛицу(НоваяСтрока, ФизЛицо, НДФЛДляФЛ, Справочные.ПодразделениеЦО, Неопределено, Справочные);
		КонецЕсли;
```

**NEW (вставляем):**
```bsl
		// Отримуємо ФізОсобу для цього Сотрудника (через кеш ФизЛица)
		ФизЛицо = Справочные.ФизЛица[Сотрудник];
		Если НЕ ЗначениеЗаполнено(ФизЛицо) Тогда
			// Fallback: використати Сотрудник.ФизическоеЛицо напряму
			Попытка
				ФизЛицо = Сотрудник.ФизическоеЛицо;
			Исключение
				Продолжить;
			КонецПопытки;
		КонецЕсли;

		Если НЕ ЗначениеЗаполнено(ФизЛицо) Тогда
			Продолжить;
		КонецЕсли;

		// Пропускаємо якщо цю ФізОсобу вже обробив Op 2a
		Если ОброблениФизЛица[ФизЛицо] <> Неопределено Тогда
			Продолжить;
		КонецЕсли;

		НДФЛДляФЛ = СуммаНДФЛПоФизЛицу[ФизЛицо];
		Если НДФЛДляФЛ = Неопределено Тогда
			НДФЛДляФЛ = 0;
		КонецЕсли;

		// ЄДИНИЙ сплітер (як Op2a): NET = Σ РаспределениеКазна.СуммаНачисления (усі форми).
		// Ф1 take-home = clamp(GROSSбух − НДФЛ, 0, NET) = Казна_Ф1; Ф2 = Казна_Ф2;
		// «Удержание» Ф1 = НДФЛ; сплит по підрозділах за вагами РаспределениеКазна (ФормаPL).
		// Було: агрегат + коефіцієнт-розмітка _ОтрЗП_РазметитьФормаPL, що схлопувала Ф1+Ф2
		//       і давала перекіс (Батрак Ф1=17717.12 замість 15400).
		_ОтрЗП_СоздатьСтрокиNETФЛ_Новый(ФизЛицо, СумНачисления, НДФЛДляФЛ, Справочные);
```

- [ ] **Step 2: Скопировать в worktree (если правили основную) для git-учёта** — НЕ требуется: правим основную конфигурацию напрямую (Rule #4). Worktree от `main` функций сплита не содержит.

- [ ] **Step 3: Статическая проверка BSL** — открыть отредактированный фрагмент, убедиться: парные `Если/КонецЕсли`, вызов `_ОтрЗП_СоздатьСтрокиNETФЛ_Новый` с 4 аргументами (ФизЛицо, СумНачисления, НДФЛДляФЛ, Справочные), цикл закрыт `КонецЦикла` (строка с `КолДобавленоKasni = КолДобавленоKasni + 1; ОброблениФизЛица.Вставить(...)` остаётся ПОСЛЕ вызова).

---

## Task 2: Загрузить конфигурацию

**Files:** —

- [ ] **Step 1: Partial-load изменённого модуля**

Skill: `/db-load-xml` с `-Mode Partial` для `Documents/А_ОтражениеЗПпоКазне` (auto `-updateConfigDumpInfo`).

- [ ] **Step 2: Обновить БД**

Skill: `/db-update` с `-Dynamic+` (серверная база BaseERP).

Ожидается: обновление без ошибок; новый код `_ОтрЗП_ЗаполнитьНачисленияЗП` активен на сервере.

---

## Task 3: Алгоритм GREEN — Python COM реконсиляция (read-only, без записи)

**Files:** Create `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/green_op2b_fix.py`

- [ ] **Step 1: Написать GREEN-тест**

```python
# -*- coding: utf-8 -*-
# GREEN: после фикса Op2b — заполнить док №1 В ПАМЯТИ (новый код), сверить ВСЕ ФЛ.
# Критерии: Op2b Ф1==Казна_Ф1 и Ф2==Казна_Ф2; Σ(Ф1+Ф2)==NET (инвариант); Op2a без дефектов;
# Батрак Ф1=15400 Ф2=20300. Без записи (LESSONS §19).
import win32com.client, sys
if sys.stdout.encoding != 'utf-8':
    sys.stdout.reconfigure(encoding='utf-8')
v8 = win32com.client.Dispatch("V83.COMConnector")
erp = v8.Connect('Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
def en(v): return erp.XMLСтрока(v) if erp.ЗначениеЗаполнено(v) else ""
def fl(v): return erp.ЗначениеЗаполнено(v)
def uid(r): return erp.String(r.УникальныйИдентификатор())

q = erp.NewObject("Запрос")
q.Текст = ("ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Документ.А_ОтражениеЗПпоКазне "
           "ГДЕ Номер = \"000000001\" И Дата МЕЖДУ ДАТАВРЕМЯ(2025,12,1,0,0,0) И ДАТАВРЕМЯ(2025,12,31,23,59,59)")
s = q.Выполнить().Выбрать(); s.Следующий()
obj = s.С.ПолучитьОбъект()
print("Заполнение В ПАМЯТИ новым кодом...")
obj.ЗаполнитьНачисленияУпризЗуп2()
obj.ЗагрузитьНалогиИУдержанияИзЗарплатыБух()
obj.ЗаполнитьОтражениеЗарплатыВФинансовомУчетеИзБазЗП()
obj.ЗаполнитьДляОтражения_ДокументОтражениеЗарплатыВФинансовомУчете()

D = {}
def rec(u, n):
    d = D.get(u)
    if d is None:
        d = dict(name=n, gross=0.0, ndfl=0.0, netupr=0.0, kf1=0.0, kf2=0.0,
                 f1zp=0.0, f2zp=0.0, f1ud=0.0, ondfl=0.0)
        D[u] = d
    return d
for r in obj.НачисленияБух:
    if fl(r.ФизЛицо): rec(uid(r.ФизЛицо), str(r.ФизЛицо.Наименование))['gross'] += float(r.Сумма)
for r in obj.НалогиБухгалтерия:
    if fl(r.Сотрудник):
        f = r.Сотрудник.ФизическоеЛицо
        if fl(f) and en(r.ТипНалога) in ("НДФЛ","ВоенныйСбор"):
            rec(uid(f), str(f.Наименование))['ndfl'] += float(r.СуммаНДФЛ)
for r in obj.НачисленияУпр:
    if fl(r.ФизЛицо): rec(uid(r.ФизЛицо), str(r.ФизЛицо.Наименование))['netupr'] += float(r.Сумма)
for r in obj.РаспределениеКазна:
    if not fl(r.Сотрудник): continue
    f = r.Сотрудник.ФизическоеЛицо
    if not fl(f): continue
    sn = float(r.СуммаНачисления)
    if sn == 0: continue
    ud = False; sp = r.СтатьяДвиженияДенежныхСредств
    if fl(sp):
        sp2 = sp.А_СпособОтраженияЗарплатыВБухучете
        if fl(sp2): ud = bool(sp2.А_ЭтоУдержание)
    if ud: continue
    d = rec(uid(f), str(f.Наименование))
    if en(r.ФормаPL) == "Форма1": d['kf1'] += sn
    elif en(r.ФормаPL) == "Форма2": d['kf2'] += sn
for r in obj.НачисленнаяЗарплатаИВзносыПоФизлицам:
    if not fl(r.ФизическоеЛицо): continue
    d = rec(uid(r.ФизическоеЛицо), str(r.ФизическоеЛицо.Наименование))
    f = en(r.ФормаPL); summ = float(r.Сумма); vzn = float(r.ВзносыВсего)
    if vzn != 0: continue
    ud = False; sp = r.СпособОтраженияЗарплатыВБухучете
    if fl(sp): ud = bool(sp.А_ЭтоУдержание)
    if f == "Форма1":
        if ud: d['f1ud'] += summ
        else: d['f1zp'] += summ
    elif f == "Форма2":
        if not ud: d['f2zp'] += summ
for r in obj.НачисленныйНДФЛ:
    if fl(r.ФизическоеЛицо) and en(r.ФормаPL) == "Форма1":
        rec(uid(r.ФизическоеЛицо), str(r.ФизическоеЛицо.Наименование))['ondfl'] += float(r.Сумма)

bad_op2b = []; bad_inv = []; op2a = op2b = 0
for u, d in D.items():
    is2a = d['netupr'] > 0
    if is2a: op2a += 1
    else: op2b += 1
    # инвариант: Ф1+Ф2 «Зарплата управл» == NET (=НачисленияУпр для 2a, =Казна-итог для 2b)
    net = d['netupr'] if is2a else (d['kf1'] + d['kf2'])
    if abs((d['f1zp'] + d['f2zp']) - net) > 0.02:
        bad_inv.append((d['name'], d['f1zp'] + d['f2zp'], net))
    # Op2b: Ф1 должно == Казна_Ф1, Ф2 == Казна_Ф2
    if not is2a:
        if abs(d['f1zp'] - d['kf1']) > 0.02 or abs(d['f2zp'] - d['kf2']) > 0.02:
            bad_op2b.append((d['name'], d['f1zp'], d['kf1'], d['f2zp'], d['kf2']))

print(f"\nФЛ всего={len(D)} Op2a={op2a} Op2b={op2b}")
print(f"Op2b с Ф1!=Казна_Ф1 или Ф2!=Казна_Ф2: {len(bad_op2b)}  (ОЖИД 0)")
for x in bad_op2b[:20]: print("   ", x)
print(f"Нарушение инварианта Ф1+Ф2!=NET: {len(bad_inv)}  (ОЖИД 0)")
for x in bad_inv[:20]: print("   ", x)
for u, d in D.items():
    if d['name'].startswith("Батрак"):
        print(f"\nБАТРАК: Ф1zp={d['f1zp']:.2f} (ОЖИД 15400) Ф2zp={d['f2zp']:.2f} (ОЖИД 20300) "
              f"Удерж={d['f1ud']:.2f} НДФЛ={d['ondfl']:.2f}")
        print("  Ф1 take-home (Ф1zp) =", "OK" if abs(d['f1zp']-15400)<0.02 else "FAIL")
ok = (len(bad_op2b) == 0 and len(bad_inv) == 0)
print("\n=== GREEN ===" if ok else "\n=== FAIL ===")
erp = None
```

- [ ] **Step 2: Запустить**

Run: `C:/Python313/python.exe "_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/green_op2b_fix.py"`
Expected:
```
Op2b с Ф1!=Казна_Ф1 или Ф2!=Казна_Ф2: 0  (ОЖИД 0)
Нарушение инварианта Ф1+Ф2!=NET: 0  (ОЖИД 0)
БАТРАК: Ф1zp=15400.00 ... Ф2zp=20300.00 ...
=== GREEN ===
```
Если `bad_op2b` или `bad_inv` ≠ 0 — остановиться, разобрать (особенно ФЛ с премиями Ф2 — проверить ветку Ф2Прем/Ф2Непрем в `_ОтрЗП_СоздатьСтрокиNETФЛ_Новый`).

---

## Task 4: End-to-end — перепроведение цепочки №1 через UI + отчёт-сверка

**Files:** —

- [ ] **Step 1: Перепровести цепочку №1 ТОЛЬКО через UI 1С** (LESSONS §19 — COM-пересоздание ОЗФУ задваивает ТЧ):
  `А_ОтражениеЗПпоКазне №000000001` → «Заполнить» → «Создать документы» (ОЗФУ) → «Создать ПередачаНачислений» → провести всю цепочку. *(шаг пользователя; ассистент даёт точную последовательность кнопок).*

- [ ] **Step 2: Перегенерировать отчёт** `А_СравнитьОстаткиВзаиморасчетыСотрудникиЕРПсBASБухгалтерия`, период 01–31.12.2025.
Expected: Батрак Кон.остаток ЕРП = 0, Разн.кон.остаток = 0; ~42 ФЛ bug-класса → 0; Тужиков/Корнієнко — остаётся отдельный ЕРП↔Бух gap (вне scope, §7 спеки).

- [ ] **Step 3: Регрессия** — отчёт «Взаиморасчёты для баланса»: Батрак Ф1 Строительство Приход=Расход=15 400→0; Ф2 Производство 20 300→0. Эталоны (№000000005 и др.): Σ ОЗФУ/ПАП, баланс свода — без изменений.

---

## Task 5: Финализация

- [ ] **Step 1: Урок** — добавить LESSONS §22 (Op2b коэффициент-перекос; fallback должен идти через единый сплиттер; источник истины РаспределениеКазна.ФормаPL) + строку в `KNOWLEDGE_MAP.md`.

- [ ] **Step 2: Память** — короткая запись в авто-память (Op2b fix, blast-radius 47 ФЛ/92 911,51, edge-cases Тужиков/Корнієнко вне scope).

- [ ] **Step 3: Commit** — закоммитить правку `ObjectModule.bsl` + spec + plan + Python-тесты на ветке `claude/bas-doc-skills`.

```bash
git add "Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl" docs/superpowers/ "_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/Python/test/"
git commit -m "fix(ОЗП): Op2b сплит Ф1/Ф2 через единый _ОтрЗП_СоздатьСтрокиNETФЛ_Новый (Батрак 17717.12→15400)"
```

- [ ] **Step 4: Out-of-scope** — предложить отдельную задачу по ЕРП↔Бух gap (Тужиков 3 756,26, Корнієнко 768,38 и класс §21).

---

## Self-review

- **Spec coverage:** §2 корень → Task 1; §3 blast-radius/верификация → Task 3; §4 решение → Task 1; §8 GREEN → Task 3+4; §9 раскатка → Task 2+4; §7 out-of-scope → Task 4 Step 2 + Task 5 Step 4. ✓
- **Placeholders:** нет TBD; весь код приведён (BSL old/new, green_op2b_fix.py). ✓
- **Type consistency:** `_ОтрЗП_СоздатьСтрокиNETФЛ_Новый(ФизЛицо, NET, НДФЛДляФЛ, Справочные)` — сигнатура из строки 1824 ObjectModule.bsl; `СумНачисления` = NET (Σ Казна). ✓
