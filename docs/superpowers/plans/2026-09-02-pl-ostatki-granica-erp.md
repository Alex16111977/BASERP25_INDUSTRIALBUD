# А_ОтчетPL_ПолныйЕРП_Остатки: граница 30.06.2026, после — только ЕРП — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Отчёт `А_ОтчетPL_ПолныйЕРП_Остатки` читает всё по 30.06.2026 из документов/остатков, всё после — только из регистров ЕРП (месячные и накопительные колонки); Спецтехника и ЦО получают объектный слой остатков данными.

**Architecture:** Один патч-скрипт с ассертами применяет одинаковые текстовые замены к копии запроса `v12_full.sql` (→ `v14_full.sql`, RED-фаза через COM) и к литералу запроса в модуле отчёта; модуль получает константу `ГраницаОстатков()` вместо `МесяцДокументаОстатков()`. Миграция данных — COM-скрипт, создающий две копии документов остатков в объектном слое. Приёмка — полным путём (вариант + компоновка) с эталонами из независимых запросов.

**Tech Stack:** 1С:Предприятие 8.3.20 (BAS ERP 2.5), BSL, язык запросов 1С, Python 3.13 + pywin32 (`V83.COMConnector`), скиллы `db-load-xml` / `db-update` / `epf-build` (PowerShell).

**Спека:** `docs/superpowers/specs/2026-09-02-pl-ostatki-granica-erp-design.md`

---

## Важные условия среды

- Объекты отчёта в git не отслеживаются и в worktree отсутствуют. **Все правки модуля — в основном каталоге** `C:\Configuration_downloads\BASERP25\Reports\А_ОтчетPL_ПолныйЕРП_Остатки\Ext\ObjectModule.bsl` (оттуда грузит Designer). В конце файлы копируются в worktree для коммита.
- Python: `/c/Python313/python` (Git Bash). База: `Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"`.
- Модуль — UTF-8 с BOM, CRLF. Патч-скрипт нормализует EOL и возвращает как было.
- Грабли запросов: алиасы `В`, `ПО`, `ИЗ`, `КАК` зарезервированы; `Комментарий` — безлимитная строка, в ГДЕ только через `ВЫРАЗИТЬ(… КАК Строка(N))`; COM сдвигает даты на −3 ч — даты периода брать из запроса `ДАТАВРЕМЯ(...)`.
- Скрипты 1С (`db-load-xml`, `db-update`, `epf-build`) — пакетный Конфигуратор: если Конфигуратор BaseERP открыт пользователем, загрузка упадёт или зависнет — попросить закрыть и повторить.

## File Structure

| Файл | Ответственность |
|---|---|
| `_Rarzrabotki/Python/test/create_ostatki_obekt_sloy_2026-09-02.py` (новый) | миграция данных: копии остатков ЦО/Спецтехники в объектный слой, контроль сумм |
| `_Rarzrabotki/Python/test/patch_v14_2026-09-02.py` (новый) | единый набор замен: `sql` → `v14_full.sql`, `bsl` → модуль, `check` → паритет модуль == sql |
| `_Rarzrabotki/Python/test/v14_full.sql` (новый) | копия запроса v14 для Python-тестов (1:1 с модулем) |
| `_Rarzrabotki/Python/test/test_otchetpl_ostatki_v14_2026-09-02.py` (новый) | RED-тест запроса через COM: инварианты + суммы по «Операционный доход» |
| `_Rarzrabotki/Python/test/set_variants_svod_2026-09-02.py` (новый) | `СводыНаправлений = Истина` у двух свод-вариантов |
| `_Rarzrabotki/Python/test/acceptance_otchetpl_ostatki_granica_2026-09-02.py` (новый) | приёмка полным путём, 5 сценариев + инварианты |
| `Reports/А_ОтчетPL_ПолныйЕРП_Остатки/Ext/ObjectModule.bsl` (основной каталог) | модуль отчёта: запрос, параметры, константа границы |
| `_Rarzrabotki/Отчеты/А_ОтчетPL_ПолныйЕРП_Остатки/Ext/ObjectModule.bsl` + `.erf` | зеркальный внешний отчёт |
| `_Rarzrabotki/notebook/knowledge_А_ОтчетPL_ПолныйЕРП_Остатки/*.md`, `knowledge_А_ОтчетPL_ПолныйЕРП/02_gibrid_granica.md` | база знаний |
| `C:\Users\support1c\.claude\projects\C--Configuration-downloads-BASERP25\memory\vvod_ostatkov_pl_kontur.md`, `otchetpl_polny_erp_gibrid.md` | память проекта |

---

### Task 1: Миграция данных — объектный слой остатков для Спецтехники и ЦО

**Files:**
- Create: `_Rarzrabotki/Python/test/create_ostatki_obekt_sloy_2026-09-02.py`

- [ ] **Step 1: Написать скрипт миграции с контролем**

```python
# -*- coding: utf-8 -*-
"""Объектный слой остатков PL для корней направлений, которые ведутся ТОЛЬКО сводом
(решение финансиста 02.09.2026): Спецтехника и ЦО.

Копирует ТЧ «Остатки» документа-источника (свод-слой корня) в новый документ
А_ВводОстатковОтчетаPL той же датой/организацией, но СводНаправления = Ложь в каждой строке.
Идемпотентно: объектный документ корня ищется по ТЧ (Подразделение = корень, слой Ложь)
и при повторном запуске перезаполняется, а не дублируется.
Контроль: по каждому корню суммы 4 ресурсов регистра в слое Ложь == слою Истина;
суммы свод-слоя по регистру до и после — равны.
"""
import datetime
import sys

import win32com.client

sys.stdout.reconfigure(encoding='utf-8')
erp = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
S = erp.String
ЦЕЛЬ = datetime.datetime(2026, 6, 30, 23, 59, 59)
КОРНИ = [
    ("Спецтехника", "Остатки PL на 30.06.2026 — лист «PL_Спецтехника_Свод» (объектный слой)"),
    ("ЦО", "Остатки PL на 30.06.2026 — лист «PL_ЦО» (объектный слой)"),
]
ПРОВАЛОВ = [0]


def чек(имя, ок, подробно=""):
    if not ок:
        ПРОВАЛОВ[0] += 1
    print("  [%s] %s%s" % ("OK  " if ок else "FAIL", имя, ("  — " + подробно) if подробно else ""))


def запрос(текст, **парам):
    q = erp.NewObject("Запрос")
    q.Text = текст
    for k, v in парам.items():
        q.SetParameter(k, v)
    return q.Execute().Выгрузить()


def документы_корня(имя, свод):
    r = запрос("""
    ВЫБРАТЬ РАЗЛИЧНЫЕ Стр.Ссылка КАК ДокСсылка
    ИЗ Документ.А_ВводОстатковОтчетаPL.Остатки КАК Стр
    ГДЕ Стр.Ссылка.Проведен И НЕ Стр.Ссылка.ПометкаУдаления
      И Стр.Подразделение.Наименование = &Имя И Стр.СводНаправления = &Свод""",
        Имя=имя, Свод=свод)
    return [r.Получить(i).ДокСсылка for i in range(r.Количество())]


def суммы_слоя(имя, свод):
    r = запрос("""
    ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК Строк,
        СУММА(Ост.СуммаФ1СНачалаГода) КАК Ф1СНГ, СУММА(Ост.СуммаФ2СНачалаГода) КАК Ф2СНГ,
        СУММА(Ост.СуммаФ1Накопительно) КАК Ф1Нак, СУММА(Ост.СуммаФ2Накопительно) КАК Ф2Нак
    ИЗ РегистрНакопления.А_ОстаткиОтчетаPL КАК Ост
    ГДЕ Ост.Подразделение.Наименование = &Имя И Ост.СводНаправления = &Свод""", Имя=имя, Свод=свод)
    s = r.Получить(0)
    return (int(s.Строк or 0), round(float(s.Ф1СНГ or 0), 2), round(float(s.Ф2СНГ or 0), 2),
            round(float(s.Ф1Нак or 0), 2), round(float(s.Ф2Нак or 0), 2))


def итог_свод_слоя():
    r = запрос("""
    ВЫБРАТЬ СУММА(Ост.СуммаФ1СНачалаГода + Ост.СуммаФ2СНачалаГода) КАК СНГ,
        СУММА(Ост.СуммаФ1Накопительно + Ост.СуммаФ2Накопительно) КАК Нак
    ИЗ РегистрНакопления.А_ОстаткиОтчетаPL КАК Ост ГДЕ Ост.СводНаправления""")
    s = r.Получить(0)
    return (round(float(s.СНГ or 0), 2), round(float(s.Нак or 0), 2))


свод_до = итог_свод_слоя()
print("свод-слой до миграции: СНГ=%.2f Нак=%.2f" % свод_до)

for имя, коммент in КОРНИ:
    print("=" * 80)
    print(имя)
    источники = документы_корня(имя, True)
    чек("ровно один свод-документ", len(источники) == 1, "найдено %d" % len(источники))
    if len(источники) != 1:
        continue
    src = источники[0]
    существующие = документы_корня(имя, False)
    чек("объектных документов до запуска 0 или 1", len(существующие) <= 1, "найдено %d" % len(существующие))
    if len(существующие) > 1:
        continue
    Об = существующие[0].ПолучитьОбъект() if существующие else erp.Документы.А_ВводОстатковОтчетаPL.СоздатьДокумент()

    # Дата: COM сдвигает на −3 ч — выставляем, читаем обратно, корректируем дельту.
    Об.Дата = ЦЕЛЬ
    факт = Об.Дата
    дельта = ЦЕЛЬ - datetime.datetime(факт.year, факт.month, факт.day, факт.hour, факт.minute, факт.second)
    if дельта:
        Об.Дата = ЦЕЛЬ + дельта
    Об.Организация = src.Организация
    Об.Ответственный = src.Ответственный
    Об.Комментарий = коммент
    Об.Остатки.Очистить()
    n = 0
    for i in range(src.Остатки.Количество()):
        s = src.Остатки.Получить(i)
        стр = Об.Остатки.Добавить()
        стр.Подразделение = s.Подразделение
        стр.СтатьяPL = s.СтатьяPL
        стр.СводНаправления = False
        стр.СуммаФ1СНачалаГода = s.СуммаФ1СНачалаГода
        стр.СуммаФ2СНачалаГода = s.СуммаФ2СНачалаГода
        стр.СуммаФ1Накопительно = s.СуммаФ1Накопительно
        стр.СуммаФ2Накопительно = s.СуммаФ2Накопительно
        n += 1
    Об.Записать(erp.РежимЗаписиДокумента.Проведение)
    print("  проведён №%s, строк %d, комментарий: %s" % (S(Об.Номер), n, коммент))

    a = суммы_слоя(имя, True)
    b = суммы_слоя(имя, False)
    чек("строк одинаково", a[0] == b[0], "свод %d / объект %d" % (a[0], b[0]))
    чек("Ф1СНГ равны", a[1] == b[1], "%.2f / %.2f" % (a[1], b[1]))
    чек("Ф2СНГ равны", a[2] == b[2], "%.2f / %.2f" % (a[2], b[2]))
    чек("Ф1Нак равны", a[3] == b[3], "%.2f / %.2f" % (a[3], b[3]))
    чек("Ф2Нак равны", a[4] == b[4], "%.2f / %.2f" % (a[4], b[4]))

свод_после = итог_свод_слоя()
print("=" * 80)
чек("свод-слой регистра не изменился", свод_до == свод_после, "%s -> %s" % (свод_до, свод_после))
print("ПРОВАЛОВ: %d" % ПРОВАЛОВ[0])
sys.exit(1 if ПРОВАЛОВ[0] else 0)
```

- [ ] **Step 2: Запустить миграцию**

Run: `cd /c/Configuration_downloads/BASERP25 && /c/Python313/python "_Rarzrabotki/Python/test/create_ostatki_obekt_sloy_2026-09-02.py"`
Expected: два блока «проведён №…, строк 35» (Спецтехника) и «строк 30» (ЦО), все чеки OK, `ПРОВАЛОВ: 0`, свод-слой до/после равен.

- [ ] **Step 3: Повторный запуск — идемпотентность**

Run: тот же скрипт ещё раз.
Expected: «объектных документов до запуска 0 или 1» OK, документы перезаписаны (те же номера), `ПРОВАЛОВ: 0`. Проверка запросом: `ВЫБРАТЬ Ост.СводНаправления, КОЛИЧЕСТВО(*) ИЗ РегистрНакопления.А_ОстаткиОтчетаPL … ГДЕ Подразделение.Наименование В ("Спецтехника","ЦО") СГРУППИРОВАТЬ ПО …` → по 2 строки на слой (35+30 в каждом).

---

### Task 2: RED — патч-скрипт и `v14_full.sql`, тест запроса через COM

**Files:**
- Create: `_Rarzrabotki/Python/test/patch_v14_2026-09-02.py`
- Create: `_Rarzrabotki/Python/test/v14_full.sql` (генерируется)
- Create: `_Rarzrabotki/Python/test/test_otchetpl_ostatki_v14_2026-09-02.py`

- [ ] **Step 1: Написать патч-скрипт (единый набор замен для sql и bsl)**

```python
# -*- coding: utf-8 -*-
"""Патч запроса отчёта А_ОтчетPL_ПолныйЕРП_Остатки: v12 -> v14 (граница 30.06.2026, после — только ЕРП).

  python patch_v14_2026-09-02.py check-v12  -> литерал запроса модуля == v12_full.sql (проверка извлекателя)
  python patch_v14_2026-09-02.py sql        -> v12_full.sql -> v14_full.sql (RED-фаза)
  python patch_v14_2026-09-02.py bsl        -> те же замены в модуле + параметры + константа
  python patch_v14_2026-09-02.py check      -> литерал запроса модуля == v14_full.sql

Каждая замена — с ассертом на число вхождений; пустых шаблонов нет.
"""
import io
import os
import sys

sys.stdout.reconfigure(encoding='utf-8')
ТЕСТ_ДИР = os.path.dirname(os.path.abspath(__file__))
V12 = os.path.join(ТЕСТ_ДИР, "v12_full.sql")
V14 = os.path.join(ТЕСТ_ДИР, "v14_full.sql")
МОДУЛЬ = r"C:\Configuration_downloads\BASERP25\Reports\А_ОтчетPL_ПолныйЕРП_Остатки\Ext\ObjectModule.bsl"

# Замены в форме SQL (без префикса «\t|»); порядок важен.
ЗАМЕНЫ = [
    # втСводныеПодразделения: документы ∪ свод-слой регистра остатков
    ("\tИ НЕ ДокСвод.ПометкаУдаления\n;\n",
     "\tИ НЕ ДокСвод.ПометкаУдаления\n\nОБЪЕДИНИТЬ\n\nВЫБРАТЬ РАЗЛИЧНЫЕ\n\tОст.Подразделение\nИЗ\n"
     "\tРегистрНакопления.А_ОстаткиОтчетаPL КАК Ост\nГДЕ\n\tОст.СводНаправления\n;\n", 1),
    # ЕРП-расходы
    ("\tР.Период МЕЖДУ ДАТАВРЕМЯ(1, 1, 1) И &КонецПериода\n",
     "\tР.Период МЕЖДУ &НачалоЕРП И &КонецПериода\n", 1),
    ("\tИ ГОД(Р.Период) * 100 + МЕСЯЦ(Р.Период) = &ПоследнийМесяц\n", "", 1),
    # ЕРП-выручка
    ("\t\tВыр.Период <= &КонецПериода\n\t\tИ Выр.Активность\n\t\tИ Выр.СуммаВыручки <> 0\n"
     "\t\tИ ГОД(Выр.Период) * 100 + МЕСЯЦ(Выр.Период) = &ПоследнийМесяц\n",
     "\t\tВыр.Период МЕЖДУ &НачалоЕРП И &КонецПериода\n\t\tИ Выр.Активность\n\t\tИ Выр.СуммаВыручки <> 0\n", 1),
    # ЕРП-прочие доходы
    ("\t\tПрД.Период <= &КонецПериода\n", "\t\tПрД.Период МЕЖДУ &НачалоЕРП И &КонецПериода\n", 1),
    ("\t\tИ ГОД(ПрД.Период) * 100 + МЕСЯЦ(ПрД.Период) = &ПоследнийМесяц\n", "", 1),
    # ЕРП-CoGS
    ("\t\tВыр.Период <= &КонецПериода\n\t\tИ Выр.Активность\n\t\tИ Выр.Стоимость <> 0\n",
     "\t\tВыр.Период МЕЖДУ &НачалоЕРП И &КонецПериода\n\t\tИ Выр.Активность\n\t\tИ Выр.Стоимость <> 0\n", 1),
    ("\t\tИ ГОД(Выр.Период) * 100 + МЕСЯЦ(Выр.Период) = &ПоследнийМесяц\n", "", 1),
    # ВПериоде — мёртвое поле (10 мест)
    ("\tД.ВПериоде КАК ВПериоде,\n", "", 1),
    ("\t\tВЫБОР КОГДА Выр.Период >= &НачалоПериода ТОГДА 1 ИНАЧЕ 0 КОНЕЦ КАК ВПериоде,\n", "", 2),
    ("\t\tВЫБОР КОГДА ПрД.Период >= &НачалоПериода ТОГДА 1 ИНАЧЕ 0 КОНЕЦ,\n", "", 1),
    ("\tД.ВПериоде,\n", "", 1),
    ("\tС.ВПериоде КАК ВПериоде,\n", "", 1),
    ("\tС.ВПериоде,\n", "", 1),
    ("\tЕРПД.ВПериоде КАК ВПериоде,\n", "", 1),
    ("\tЕРПС.ВПериоде,\n", "", 1),
    ("\tЕРПД.ВПериоде,\n", "", 1),
    # документы А_ОтчетPL -> месячные колонки: только по границу
    ("\tИ ГОД(Док.Дата) * 100 + МЕСЯЦ(Док.Дата) <> &ПоследнийМесяц\n",
     "\tИ Док.Дата <= &ГраницаОстатков\n", 1),
]
# После удаления ветки «документы -> накопительные»
ЗАМЕНЫ_ПОСЛЕ = [
    ("\t\tПО КЛ.КолонкаСорт В (890000, 900000)\nГДЕ\n\tОст.СводНаправления = &СводыНаправлений\n",
     "\t\tПО КЛ.КолонкаСорт = 900000\n\t\t\tИЛИ (КЛ.КолонкаСорт = 890000 И &ОстаткиВГодуПериода = ИСТИНА)\n"
     "ГДЕ\n\tОст.Период = &ГраницаОстатков\n\tИ Ост.СводНаправления = &СводыНаправлений\n", 1),
]
# Только модуль: параметры и функция границы
ЗАМЕНЫ_МОДУЛЬ = [
    ('\t// Последний МЕСЯЦ периода отчёта — ЕДИНСТВЕННЫЙ, факт которого берётся из регистров\n'
     '\t// ЕРП. Все более ранние месяцы идут ТОЛЬКО из документов А_ОтчетPL: месяц без\n'
     '\t// документа даёт ноль, исключений (в том числе для амортизации) больше нет.\n'
     '\tЗапрос.УстановитьПараметр("ПоследнийМесяц", Год(КонецПериода) * 100 + Месяц(КонецПериода));\n', '', 1),
    ('\t// Переходная база накопительных колонок — СТРОГО из документа остатков\n'
     '\t// (решение финансиста 28.08.2026). Без проведённого документа отчёт не строится.\n'
     '\tЗапрос.УстановитьПараметр("МесяцОстатков", МесяцДокументаОстатков());\n',
     '\t// Граница источников — константа (решение финансиста 02.09.2026): по ГраницаОстатков()\n'
     '\t// включительно — документы А_ОтчетPL и остатки, после — только регистры ЕРП.\n'
     '\tЗапрос.УстановитьПараметр("ГраницаОстатков", ГраницаОстатков());\n'
     '\tЗапрос.УстановитьПараметр("НачалоЕРП", НачалоМесяца(ДобавитьМесяц(ГраницаОстатков(), 1)));\n'
     '\tЗапрос.УстановитьПараметр("ОстаткиВГодуПериода", Год(ГраницаОстатков()) = Год(КонецПериода));\n', 1),
]
НОВАЯ_ФУНКЦИЯ = (
    "// Граница источников факта (решение финансиста 02.09.2026): остатки PL зафиксированы\n"
    "// документами А_ВводОстатковОтчетаPL на эту дату и больше не вводятся; всё позже — регистры ЕРП.\n"
    "Функция ГраницаОстатков()\n\n\tВозврат Дата(2026, 6, 30, 23, 59, 59);\n\nКонецФункции\n")


def в_модуль(s):
    строки = s.split("\n")
    return "\n".join(("\t|" + l) if i < len(строки) - 1 else l for i, l in enumerate(строки))


def применить(текст, замены, форма):
    for старое, новое, n in замены:
        assert старое, "пустой шаблон замены"
        if форма == "bsl":
            старое, новое = в_модуль(старое), в_модуль(новое)
        k = текст.count(старое)
        assert k == n, "ожидалось %d вхождений, найдено %d: %r" % (n, k, старое[:70])
        текст = текст.replace(старое, новое)
    return текст


def удалить_ветку_документов_в_накопительные(текст):
    якорь = "ПО КЛ.КолонкаСорт = 900000\n"
    assert текст.count(якорь) == 1, текст.count(якорь)
    p = текст.index(якорь)
    start = текст.rfind("ОБЪЕДИНИТЬ ВСЕ", 0, p)
    end = текст.find("ОБЪЕДИНИТЬ ВСЕ", p)
    assert 0 < start < p < end
    кусок = текст[start:end]
    assert "&МесяцОстатков" in кусок and "Документ.А_ОтчетPL.ДанныеОтчета" in кусок, "не та ветка"
    assert 1200 < len(кусок) < 3500, len(кусок)
    return текст[:start] + текст[end:]


def патч_запроса(текст, форма):
    текст = применить(текст, ЗАМЕНЫ, форма)
    текст = удалить_ветку_документов_в_накопительные(текст)
    текст = применить(текст, ЗАМЕНЫ_ПОСЛЕ, форма)
    return текст


def читать(p):
    b = io.open(p, "rb").read()
    bom = b.startswith(b"\xef\xbb\xbf")
    t = b.decode("utf-8-sig")
    crlf = "\r\n" in t
    return t.replace("\r\n", "\n"), bom, crlf


def писать(p, t, bom, crlf):
    if crlf:
        t = t.replace("\n", "\r\n")
    io.open(p, "wb").write((b"\xef\xbb\xbf" if bom else b"") + t.encode("utf-8"))


НАЧАЛО_ЛИТЕРАЛА = '\tЗапрос.Текст =\n\t"ВЫБРАТЬ РАЗЛИЧНЫЕ\n'


def литерал_модуля(t):
    s = t.index(НАЧАЛО_ЛИТЕРАЛА) + len('\tЗапрос.Текст =\n\t"')
    e = t.index('";\n', s)
    строки = t[s:e].split("\n")
    out = [строки[0]] + [(l[2:] if l.startswith("\t|") else l) for l in строки[1:]]
    return ("\n".join(out) + "\n").replace('""', '"')


def проверить(sql_path):
    модуль, _, _ = читать(МОДУЛЬ)
    sql, _, _ = читать(sql_path)
    a, b = литерал_модуля(модуль), sql
    if a == b:
        print("OK: литерал модуля == %s (%d символов)" % (os.path.basename(sql_path), len(a)))
        return 0
    for i, (x, y) in enumerate(zip(a.split("\n"), b.split("\n"))):
        if x != y:
            print("РАСХОЖДЕНИЕ строка %d:\n  модуль: %r\n  sql:    %r" % (i + 1, x, y))
            break
    print("FAIL: длины %d / %d" % (len(a), len(b)))
    return 1


режим = sys.argv[1] if len(sys.argv) > 1 else ""
if режим == "check-v12":
    sys.exit(проверить(V12))
elif режим == "sql":
    t, bom, crlf = читать(V12)
    t14 = патч_запроса(t, "sql")
    assert "&ПоследнийМесяц" not in t14 and "&МесяцОстатков" not in t14 and "ВПериоде" not in t14
    писать(V14, t14, bom, crlf)
    print("OK: v14_full.sql записан (%d символов)" % len(t14))
elif режим == "bsl":
    t, bom, crlf = читать(МОДУЛЬ)
    assert "Функция МесяцДокументаОстатков()" in t, "модуль уже пропатчен?"
    t = патч_запроса(t, "bsl")
    t = применить(t, ЗАМЕНЫ_МОДУЛЬ, "raw")
    s = t.index("// Месяц (ГГГГ*100+ММ) единственного проведённого документа ввода остатков PL.")
    e = t.index("КонецФункции", s) + len("КонецФункции\n")
    assert "Функция МесяцДокументаОстатков()" in t[s:e]
    t = t[:s] + НОВАЯ_ФУНКЦИЯ + t[e:]
    assert "ПоследнийМесяц" not in t and "МесяцОстатков" not in t and "ВПериоде" not in t
    assert t.count("Функция ГраницаОстатков()") == 1 and t.count("КонецФункции") == t.count("Функция ")
    писать(МОДУЛЬ, t, bom, crlf)
    print("OK: модуль пропатчен (%d символов)" % len(t))
elif режим == "check":
    sys.exit(проверить(V14))
else:
    print(__doc__)
    sys.exit(2)
```

- [ ] **Step 2: Проверить извлекатель литерала на текущем состоянии (v12)**

Run: `cd /c/Configuration_downloads/BASERP25 && /c/Python313/python "_Rarzrabotki/Python/test/patch_v14_2026-09-02.py" check-v12`
Expected: `OK: литерал модуля == v12_full.sql (… символов)`. Если FAIL — смотреть первую расходящуюся строку и править `литерал_модуля` (не sql-файл).

- [ ] **Step 3: Сгенерировать `v14_full.sql`**

Run: `/c/Python313/python "_Rarzrabotki/Python/test/patch_v14_2026-09-02.py" sql`
Expected: `OK: v14_full.sql записан`. Все ассерты по числу вхождений прошли.

- [ ] **Step 4: Написать RED-тест запроса**

```python
# -*- coding: utf-8 -*-
"""RED-тест запроса v14 (Rule #-1): гоняем v14_full.sql через COM ДО переноса в BSL.

Правило (02.09.2026): месяцы <= 202606 — документы (месячные) / остатки (накопительные);
месяцы > 202606 — только регистры ЕРП, в месячных и накопительных.
Эталоны считаются независимыми запросами к регистрам/остаткам; строка «Операционный доход»
(группа статей PL код 000000006). Август живой — сравниваем с запросом того же момента.
"""
import io
import os
import sys

import win32com.client

sys.stdout.reconfigure(encoding='utf-8')
ТЕСТ_ДИР = os.path.dirname(os.path.abspath(__file__))
SQL = io.open(os.path.join(ТЕСТ_ДИР, sys.argv[1] if len(sys.argv) > 1 else "v14_full.sql"), encoding='utf-8-sig').read()

erp = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
S = erp.String
ПРОВАЛОВ = [0]
ГРУППА_ОД = "000000006"


def чек(имя, ок, подробно=""):
    if not ок:
        ПРОВАЛОВ[0] += 1
    print("  [%s] %s%s" % ("OK  " if ок else "FAIL", имя, ("  — " + подробно) if подробно else ""))


def запрос(текст, **парам):
    q = erp.NewObject("Запрос")
    q.Text = текст
    for k, v in парам.items():
        q.SetParameter(k, v)
    return q.Execute().Выгрузить()


def скаляр(текст, **парам):
    r = запрос(текст, **парам)
    return r.Получить(0).Ссылка if r.Количество() else None


def число(текст, **парам):
    r = запрос(текст, **парам)
    return round(float(r.Получить(0).Сумма or 0), 2)


def описание(имя, квал=None):
    м = erp.NewObject("Массив")
    м.Добавить(erp.NewObject("ОписаниеТипов", имя).Типы().Получить(0))
    return erp.NewObject("ОписаниеТипов", м) if квал is None else erp.NewObject("ОписаниеТипов", м, квал)


def дата(г, м, д, ч=0, мин=0, с=0):
    return запрос("ВЫБРАТЬ ДАТАВРЕМЯ(%d,%d,%d,%d,%d,%d) КАК Д" % (г, м, д, ч, мин, с)).Получить(0).Д


def конец_месяца(г, м):
    return запрос("ВЫБРАТЬ КОНЕЦПЕРИОДА(ДАТАВРЕМЯ(%d,%d,1), МЕСЯЦ) КАК Д" % (г, м)).Получить(0).Д


ПОДР = lambda имя: скаляр('ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка ИЗ Справочник.СтруктураПредприятия ГДЕ Наименование = &Имя', Имя=имя)
ДДС_COGS = скаляр('ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка ИЗ Справочник.СтатьиДвиженияДенежныхСредств ГДЕ А_ПриёмникСебестоимостиПродажPL')
СТ_ТОВАР = скаляр(
    'ВЫБРАТЬ ПЕРВЫЕ 1 СД.Ссылка КАК Ссылка, ВЫБОР КОГДА СД.Наименование = "Прочие доходи" ТОГДА 0 ИНАЧЕ 1 КОНЕЦ КАК Приоритет '
    'ИЗ ПланВидовХарактеристик.СтатьиДоходов КАК СД ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.А_Статьи_PL.Статьи КАК СтатьиТЧ '
    'ПО СтатьиТЧ.СтатьяДвиженияДенежныхСредств = СД.А_СтатьяДвиженияДенежныхСредств '
    'И СтатьиТЧ.Ссылка.Код = "000000002" И НЕ СтатьиТЧ.Ссылка.ПометкаУдаления '
    'ГДЕ НЕ СД.ПометкаУдаления УПОРЯДОЧИТЬ ПО Приоритет, СД.Наименование')
ПУСТО = дата(1, 1, 1)
ГРАНИЦА = дата(2026, 6, 30, 23, 59, 59)
НАЧАЛО_ЕРП = дата(2026, 7, 1)


def колонки(месяцы):
    кол = erp.NewObject("ТаблицаЗначений")
    кол.Колонки.Добавить("КолонкаСорт", описание("Число", erp.NewObject("КвалификаторыЧисла", 10, 0)))
    кол.Колонки.Добавить("КолонкаПредставление", описание("Строка", erp.NewObject("КвалификаторыСтроки", 30)))
    кол.Колонки.Добавить("МесяцДата", описание("Дата"))
    for м in месяцы:
        с = кол.Добавить(); с.КолонкаСорт = м; с.КолонкаПредставление = str(м); с.МесяцДата = ПУСТО
    с = кол.Добавить(); с.КолонкаСорт = 890000; с.КолонкаПредставление = "PL с начала года"; с.МесяцДата = ПУСТО
    с = кол.Добавить(); с.КолонкаСорт = 900000; с.КолонкаПредставление = "Накопительный PL"; с.МесяцДата = ПУСТО
    return кол


def прогон(свод, м1, м2):
    месяцы = [2026 * 100 + м for м in range(м1, м2 + 1)]
    z = erp.NewObject("Запрос")
    z.Text = SQL
    z.SetParameter("НачалоПериода", дата(2026, м1, 1))
    z.SetParameter("КонецПериода", конец_месяца(2026, м2))
    z.SetParameter("СводыНаправлений", свод)
    z.SetParameter("КолонкиPL", колонки(месяцы))
    z.SetParameter("ДДСCoGS", ДДС_COGS)
    z.SetParameter("СтатьяДоходовТовар", СТ_ТОВАР)
    z.SetParameter("ВыводитьФиксы", False)
    z.SetParameter("КлючНачалаГода", 202601)
    z.SetParameter("ГраницаОстатков", ГРАНИЦА)
    z.SetParameter("НачалоЕРП", НАЧАЛО_ЕРП)
    z.SetParameter("ОстаткиВГодуПериода", True)
    return z.Execute().Выгрузить()


def суммы_од(таб, корень):
    """{КолонкаСорт: сумма} по строкам группы ОД внутри поддерева корня (включая сам корень)."""
    r = запрос("""
    ВЫБРАТЬ Т.Подразделение КАК Подразделение, Т.Группа КАК Группа, Т.КолонкаСорт КАК КолонкаСорт,
        Т.СуммаИтого КАК СуммаИтого
    ПОМЕСТИТЬ втТ ИЗ &Т КАК Т
    ;
    ВЫБРАТЬ Т.КолонкаСорт КАК Кол, СУММА(Т.СуммаИтого) КАК Сумма
    ИЗ втТ КАК Т
    ГДЕ Т.Подразделение В ИЕРАРХИИ (&Корень) И Т.Группа.Код = &Код
    СГРУППИРОВАТЬ ПО Т.КолонкаСорт""", Т=таб, Корень=корень, Код=ГРУППА_ОД)
    return {int(r.Получить(i).Кол): round(float(r.Получить(i).Сумма or 0), 2) for i in range(r.Количество())}


def инварианты(таб, имя):
    r = запрос("""
    ВЫБРАТЬ Т.КолонкаСорт КАК КолонкаСорт, Т.Документ КАК Документ
    ПОМЕСТИТЬ втТ ИЗ &Т КАК Т
    ;
    ВЫБРАТЬ
        СУММА(ВЫБОР КОГДА Т.КолонкаСорт > 202606 И Т.КолонкаСорт < 890000 И Т.Документ ССЫЛКА Документ.А_ОтчетPL ТОГДА 1 ИНАЧЕ 0 КОНЕЦ) КАК ДокПослеГраницы,
        СУММА(ВЫБОР КОГДА Т.КолонкаСорт > 0 И Т.КолонкаСорт <= 202606 И НЕ Т.Документ ССЫЛКА Документ.А_ОтчетPL ТОГДА 1 ИНАЧЕ 0 КОНЕЦ) КАК ЕРПДоГраницы,
        СУММА(ВЫБОР КОГДА Т.КолонкаСорт В (890000, 900000) И Т.Документ ССЫЛКА Документ.А_ОтчетPL ТОГДА 1 ИНАЧЕ 0 КОНЕЦ) КАК ДокВНакопительных,
        СУММА(ВЫБОР КОГДА Т.КолонкаСорт В (890000, 900000) И Т.Документ ССЫЛКА Документ.А_ВводОстатковОтчетаPL ТОГДА 1 ИНАЧЕ 0 КОНЕЦ) КАК ОстаткиВНакопительных
    ИЗ втТ КАК Т""", Т=таб).Получить(0)
    чек(имя + ": после границы нет строк А_ОтчетPL", int(r.ДокПослеГраницы or 0) == 0, str(r.ДокПослеГраницы))
    чек(имя + ": до границы нет строк ЕРП", int(r.ЕРПДоГраницы or 0) == 0, str(r.ЕРПДоГраницы))
    чек(имя + ": в накопительных нет А_ОтчетPL", int(r.ДокВНакопительных or 0) == 0, str(r.ДокВНакопительных))
    чек(имя + ": в накопительных есть регистратор остатков", int(r.ОстаткиВНакопительных or 0) > 0, str(r.ОстаткиВНакопительных))


def остатки_од(корень, свод, ресурс):
    return число("""
    ВЫБРАТЬ СУММА(Ост.СуммаФ1%s + Ост.СуммаФ2%s) КАК Сумма
    ИЗ РегистрНакопления.А_ОстаткиОтчетаPL КАК Ост
    ГДЕ Ост.Подразделение = &П И Ост.СводНаправления = &Свод И Ост.СтатьяPL.Группа.Код = &Код""" % (ресурс, ресурс),
        П=корень, Свод=свод, Код=ГРУППА_ОД)


def ерп_од(корень, м):
    н, к = дата(2026, м, 1), конец_месяца(2026, м)
    выручка = число("""
    ВЫБРАТЬ СУММА(Выр.СуммаВыручки) КАК Сумма ИЗ РегистрНакопления.ВыручкаИСебестоимостьПродаж КАК Выр
    ГДЕ Выр.Активность И Выр.Подразделение В ИЕРАРХИИ (&К) И Выр.Период МЕЖДУ &Н И &КП""", К=корень, Н=н, КП=к)
    прочие = число("""
    ВЫБРАТЬ СУММА(ПрД.Сумма) КАК Сумма ИЗ РегистрНакопления.ПрочиеДоходы КАК ПрД
    ГДЕ ПрД.Активность И ПрД.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
      И ПрД.Подразделение В ИЕРАРХИИ (&К) И ПрД.Период МЕЖДУ &Н И &КП
      И ПрД.СтатьяДоходов.А_СтатьяДвиженияДенежныхСредств В
        (ВЫБРАТЬ Ст.СтатьяДвиженияДенежныхСредств ИЗ Справочник.А_Статьи_PL.Статьи КАК Ст ГДЕ Ст.Ссылка.Группа.Код = &Код)""",
        К=корень, Н=н, КП=к, Код=ГРУППА_ОД)
    return round(выручка + прочие, 2)


def документ_од(подр, свод, м):
    return число("""
    ВЫБРАТЬ СУММА(Стр.Сумма) КАК Сумма
    ИЗ Документ.А_ОтчетPL.ДанныеОтчета КАК Стр ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.А_ОтчетPL КАК Док ПО Стр.Ссылка = Док.Ссылка
    ГДЕ Док.Проведен И Док.Подразделение = &П И Док.ВключатьДочерние = &Свод
      И ГОД(Док.Дата) * 100 + МЕСЯЦ(Док.Дата) = &М И Стр.Статья.Группа.Код = &Код""", П=подр, Свод=свод, М=2026 * 100 + м, Код=ГРУППА_ОД)


def равно(a, b):
    return abs(a - b) < 0.01


def сценарий(имя, корень_имя, свод, м1, м2, месяцы_док=()):
    print("=" * 96)
    print("%s — %s, свод=%s, %02d..%02d.2026" % (имя, корень_имя, свод, м1, м2))
    корень = ПОДР(корень_имя)
    таб = прогон(свод, м1, м2)
    чек("запрос выполнился", таб.Количество() > 0, "строк %d" % таб.Количество())
    инварианты(таб, имя)
    факт = суммы_од(таб, корень)
    ерп = {м: ерп_од(корень, м) for м in range(7, м2 + 1)}
    for м in range(max(м1, 7), м2 + 1):
        чек("месяц %d = ЕРП" % (202600 + м), равно(факт.get(202600 + м, 0), ерп[м]), "%.2f / %.2f" % (факт.get(202600 + м, 0), ерп[м]))
    for м in месяцы_док:
        ож = документ_од(корень, свод, м)
        чек("месяц %d = документ" % (202600 + м), равно(факт.get(202600 + м, 0), ож), "%.2f / %.2f" % (факт.get(202600 + м, 0), ож))
    снг = остатки_од(корень, свод, "СНачалаГода") + sum(ерп.values())
    нак = остатки_од(корень, свод, "Накопительно") + sum(ерп.values())
    чек("890000 = остатки СНГ + ЕРП 07..%02d" % м2, равно(факт.get(890000, 0), снг), "%.2f / %.2f" % (факт.get(890000, 0), снг))
    чек("900000 = остатки Нак + ЕРП 07..%02d" % м2, равно(факт.get(900000, 0), нак), "%.2f / %.2f" % (факт.get(900000, 0), нак))
    return факт


с1 = сценарий("С1", "Спецтехника", True, 7, 8)
чек("С1: июль ОД = 3 205 500,00", равно(с1.get(202607, 0), 3205500.00), "%.2f" % с1.get(202607, 0))
чек("С1: остатки ОД СНГ = 12 404 410,34", равно(остатки_од(ПОДР("Спецтехника"), True, "СНачалаГода"), 12404410.34))
сценарий("С2", "Строительство", True, 7, 8)
сценарий("С3", "МК Кривий ріг", False, 1, 8, месяцы_док=(5, 6))
с4 = сценарий("С4", "Спецтехника", True, 8, 8)
чек("С4: 890000 = С1 890000 (июль вне периода, но в накопительных)", равно(с4.get(890000, 0), с1.get(890000, 0)))
с5 = сценарий("С5", "Спецтехника", False, 7, 8)
чек("С5: объект 890000 = свод 890000", равно(с5.get(890000, 0), с1.get(890000, 0)), "%.2f / %.2f" % (с5.get(890000, 0), с1.get(890000, 0)))
чек("С5: объект 900000 = свод 900000", равно(с5.get(900000, 0), с1.get(900000, 0)))
сценарий("С6", "ЦО", False, 7, 8)
print("=" * 96)
print("ПРОВАЛОВ: %d" % ПРОВАЛОВ[0])
sys.exit(1 if ПРОВАЛОВ[0] else 0)
```

- [ ] **Step 5: Прогнать RED-тест на v14 — добиться зелёного (синтаксис и цифры)**

Run: `/c/Python313/python "_Rarzrabotki/Python/test/test_otchetpl_ostatki_v14_2026-09-02.py"`
Expected: все чеки OK, `ПРОВАЛОВ: 0`. Если запрос падает синтаксически — текст ошибки указывает строку `v14_full.sql`; править `ЗАМЕНЫ` в патч-скрипте, перегенерировать sql (`sql`), повторить. Если расходятся суммы ЕРП — сверить формулу `ерп_од` с ветками запроса (карта уровней исключает пустое подразделение; `В ИЕРАРХИИ` — тоже).

- [ ] **Step 6: Контроль — тот же тест на v12 должен быть КРАСНЫМ**

Run: `/c/Python313/python "_Rarzrabotki/Python/test/test_otchetpl_ostatki_v14_2026-09-02.py" v12_full.sql`
Expected: ошибка `Не задано значение параметра "ПоследнийМесяц"` либо провалы «месяц 202607 = ЕРП» — старый запрос июль не даёт. Это подтверждает, что тест различает правила.

---

### Task 3: Перенос запроса в модуль отчёта

**Files:**
- Modify: `C:\Configuration_downloads\BASERP25\Reports\А_ОтчетPL_ПолныйЕРП_Остатки\Ext\ObjectModule.bsl` (запрос 700-1484, параметры 1486-1503, функция 2005-2028)

- [ ] **Step 1: Резервная копия модуля**

Run: `cp "/c/Configuration_downloads/BASERP25/Reports/А_ОтчетPL_ПолныйЕРП_Остатки/Ext/ObjectModule.bsl" "/c/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test/ObjectModule_v12_backup_2026-09-02.bsl"`

- [ ] **Step 2: Применить патч к модулю**

Run: `/c/Python313/python "_Rarzrabotki/Python/test/patch_v14_2026-09-02.py" bsl`
Expected: `OK: модуль пропатчен`. Ассерты: `ПоследнийМесяц`, `МесяцОстатков`, `ВПериоде` в модуле отсутствуют; одна `Функция ГраницаОстатков()`; число `Функция ` == число `КонецФункции`.

- [ ] **Step 3: Паритет модуль == v14_full.sql**

Run: `/c/Python313/python "_Rarzrabotki/Python/test/patch_v14_2026-09-02.py" check`
Expected: `OK: литерал модуля == v14_full.sql`.

- [ ] **Step 4: Синтаксический grep-чек BSL**

Run: `cd "/c/Configuration_downloads/BASERP25/Reports/А_ОтчетPL_ПолныйЕРП_Остатки/Ext" && grep -c "КонецФункци[яи]" ObjectModule.bsl; grep -c "^Функция \|^	Функция " ObjectModule.bsl; grep -n "ГраницаОстатков\|НачалоЕРП\|ОстаткиВГодуПериода" ObjectModule.bsl | head -20; grep -n "КонецФункция" ObjectModule.bsl`
Expected: последний grep пуст (нет «КонецФункция»); параметры `ГраницаОстатков`/`НачалоЕРП`/`ОстаткиВГодуПериода` встречаются и в запросе (`&…`), и в `УстановитьПараметр`; `Функция ГраницаОстатков()` есть.

---

### Task 4: Загрузка модуля в базу и обновление конфигурации

**Files:**
- Read only: `.claude/skills/db-load-xml/scripts/db-load-xml.ps1`, `.claude/skills/db-update/scripts/db-update.ps1` (основной каталог)

- [ ] **Step 1: Файл списка (UTF-8 с BOM) с одним путём**

Run:
```bash
printf '\xef\xbb\xbfReports/А_ОтчетPL_ПолныйЕРП_Остатки/Ext/ObjectModule.bsl\r\n' > "/c/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test/load_list_2026-09-02.txt"
```

- [ ] **Step 2: Частичная загрузка**

Run (из основного каталога):
```bash
cd /c/Configuration_downloads/BASERP25 && powershell.exe -NoProfile -File ".claude/skills/db-load-xml/scripts/db-load-xml.ps1" -InfoBaseServer "localhost" -InfoBaseRef "BaseERP" -UserName "Администратор" -Password "24043" -ConfigDir "C:\Configuration_downloads\BASERP25" -Mode Partial -ListFile "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\load_list_2026-09-02.txt"
```
Expected: скрипт сообщает об успешной загрузке (код возврата 0). Если «база заблокирована / конфигуратор запущен» — попросить пользователя закрыть Конфигуратор BaseERP и повторить. Никогда не включать в список `Configuration.xml`.

- [ ] **Step 3: Обновить конфигурацию БД**

Run:
```bash
cd /c/Configuration_downloads/BASERP25 && powershell.exe -NoProfile -File ".claude/skills/db-update/scripts/db-update.ps1" -InfoBaseServer "localhost" -InfoBaseRef "BaseERP" -UserName "Администратор" -Password "24043"
```
Expected: код возврата 0.

- [ ] **Step 4: Убедиться, что база видит новый код**

Run (COM, READ-ONLY): построить отчёт за 01.07–31.08.2026, Спецтехника, свод — как в приёмке Task 6 (можно уже запустить `acceptance_otchetpl_ostatki_granica_2026-09-02.py` после Task 6). Быстрый признак: в шапке есть и «Июль 2026», и «Август 2026». Если только «Август» — база на старом байт-коде (кеш Designer): повторить Step 2–3, проверить, что использовался `-updateConfigDumpInfo` (скилл добавляет сам).

---

### Task 5: Свод-варианты — `СводыНаправлений = Истина`

**Files:**
- Create: `_Rarzrabotki/Python/test/set_variants_svod_2026-09-02.py`

- [ ] **Step 1: Написать скрипт**

```python
# -*- coding: utf-8 -*-
"""У свод-вариантов отчёта А_ОтчетPL_ПолныйЕРП_Остатки сохранить параметр СводыНаправлений = Истина
(решение 02.09.2026): интерактивный отчёт должен совпадать с книгой (обработка выгрузки ставит Истина сама)."""
import sys
import win32com.client

sys.stdout.reconfigure(encoding='utf-8')
erp = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
S = erp.String
ВАРИАНТЫ = ["PL: Месяц, PL с начала года (для свода)",
            "PL: (без направления ЦО): Месяц, PL с начала года"]
ПРОВАЛОВ = 0
for имя in ВАРИАНТЫ:
    q = erp.NewObject("Запрос")
    q.Text = ('ВЫБРАТЬ Вар.Ссылка КАК Ссылка ИЗ Справочник.ВариантыОтчетов КАК Вар '
              'ГДЕ Вар.Наименование = &Имя И НЕ Вар.ПометкаУдаления '
              'И ВЫРАЗИТЬ(Вар.Отчет КАК Справочник.ИдентификаторыОбъектовМетаданных).ПолноеИмя = "Отчет.А_ОтчетPL_ПолныйЕРП_Остатки"')
    q.SetParameter("Имя", имя)
    r = q.Execute().Выгрузить()
    if r.Количество() != 1:
        print("FAIL: вариант «%s» — найдено %d" % (имя, r.Количество())); ПРОВАЛОВ += 1; continue
    об = r.Получить(0).Ссылка.ПолучитьОбъект()
    н = об.Настройки.Получить()
    э = н.ПараметрыДанных.Элементы.Найти("СводыНаправлений")
    if э is None:
        print("FAIL: «%s» — нет параметра СводыНаправлений" % имя); ПРОВАЛОВ += 1; continue
    было = bool(э.Значение)
    э.Значение = True
    э.Использование = True
    об.Настройки = erp.NewObject("ХранилищеЗначения", н)
    об.Записать()
    # перечитать
    н2 = r.Получить(0).Ссылка.Настройки.Получить()
    э2 = н2.ПараметрыДанных.Элементы.Найти("СводыНаправлений")
    ок = bool(э2.Значение) and bool(э2.Использование)
    print("[%s] «%s»: было %s -> стало %s" % ("OK  " if ок else "FAIL", имя, было, bool(э2.Значение)))
    ПРОВАЛОВ += 0 if ок else 1
print("ПРОВАЛОВ:", ПРОВАЛОВ)
sys.exit(1 if ПРОВАЛОВ else 0)
```

- [ ] **Step 2: Запустить**

Run: `/c/Python313/python "_Rarzrabotki/Python/test/set_variants_svod_2026-09-02.py"`
Expected: два `OK … было False -> стало True`, `ПРОВАЛОВ: 0`. Если `NewObject("ХранилищеЗначения", н)` через COM не принимает настройки — выполнить то же самое кодом 1С через `mcp__1c-workerp__execute_code`: `Об = Вар.ПолучитьОбъект(); Н = Об.Настройки.Получить(); Э = Н.ПараметрыДанных.Элементы.Найти("СводыНаправлений"); Э.Значение = Истина; Э.Использование = Истина; Об.Настройки = Новый ХранилищеЗначения(Н); Об.Записать();`.

---

### Task 6: Приёмка полным путём

**Files:**
- Create: `_Rarzrabotki/Python/test/acceptance_otchetpl_ostatki_granica_2026-09-02.py`

- [ ] **Step 1: Написать приёмочный тест (вариант + компоновка)**

```python
# -*- coding: utf-8 -*-
"""Приёмка отчёта А_ОтчетPL_ПолныйЕРП_Остатки по правилу 02.09.2026 — ПОЛНЫЙ путь пользователя:
вариант из Справочник.ВариантыОтчетов -> Период / отбор / СводыНаправлений / флажки колонок -> СкомпоноватьРезультат.
Строка «Операционный доход», колонки «Сумма, грн» каждой группы шапки.
Эталоны — независимые запросы (остатки + регистры ЕРП); константы только червень и июль 3 205 500,00.
"""
import re
import sys

import win32com.client

sys.stdout.reconfigure(encoding='utf-8')
erp = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
S = erp.String
ПРОВАЛОВ = [0]
ГРУППА_ОД = "000000006"
ОБЪЕКТ = "PL: Месяц, PL с начала года"
СВОД = "PL: Месяц, PL с начала года (для свода)"


def чек(имя, ок, подробно=""):
    if not ок:
        ПРОВАЛОВ[0] += 1
    print("  [%s] %s%s" % ("OK  " if ок else "FAIL", имя, ("  — " + подробно) if подробно else ""))


def запрос(текст, **парам):
    q = erp.NewObject("Запрос")
    q.Text = текст
    for k, v in парам.items():
        q.SetParameter(k, v)
    return q.Execute().Выгрузить()


def скаляр(текст, **парам):
    r = запрос(текст, **парам)
    return r.Получить(0).Ссылка if r.Количество() else None


def число(текст, **парам):
    return round(float(запрос(текст, **парам).Получить(0).Сумма or 0), 2)


def дата(г, м, д, ч=0, мин=0, с=0):
    return запрос("ВЫБРАТЬ ДАТАВРЕМЯ(%d,%d,%d,%d,%d,%d) КАК Д" % (г, м, д, ч, мин, с)).Получить(0).Д


def конец_месяца(г, м):
    return запрос("ВЫБРАТЬ КОНЕЦПЕРИОДА(ДАТАВРЕМЯ(%d,%d,1), МЕСЯЦ) КАК Д" % (г, м)).Получить(0).Д


ПОДР = lambda имя: скаляр('ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка ИЗ Справочник.СтруктураПредприятия ГДЕ Наименование = &Имя', Имя=имя)
ВАР = lambda имя: скаляр('ВЫБРАТЬ ПЕРВЫЕ 1 Вар.Ссылка КАК Ссылка ИЗ Справочник.ВариантыОтчетов КАК Вар ГДЕ Вар.Наименование = &Имя', Имя=имя)


def остатки_од(корень, свод, ресурс):
    return число("""
    ВЫБРАТЬ СУММА(Ост.СуммаФ1%s + Ост.СуммаФ2%s) КАК Сумма ИЗ РегистрНакопления.А_ОстаткиОтчетаPL КАК Ост
    ГДЕ Ост.Подразделение = &П И Ост.СводНаправления = &Свод И Ост.СтатьяPL.Группа.Код = &Код""" % (ресурс, ресурс),
        П=корень, Свод=свод, Код=ГРУППА_ОД)


def ерп_од(корень, м):
    н, к = дата(2026, м, 1), конец_месяца(2026, м)
    выручка = число("""
    ВЫБРАТЬ СУММА(Выр.СуммаВыручки) КАК Сумма ИЗ РегистрНакопления.ВыручкаИСебестоимостьПродаж КАК Выр
    ГДЕ Выр.Активность И Выр.Подразделение В ИЕРАРХИИ (&К) И Выр.Период МЕЖДУ &Н И &КП""", К=корень, Н=н, КП=к)
    прочие = число("""
    ВЫБРАТЬ СУММА(ПрД.Сумма) КАК Сумма ИЗ РегистрНакопления.ПрочиеДоходы КАК ПрД
    ГДЕ ПрД.Активность И ПрД.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
      И ПрД.Подразделение В ИЕРАРХИИ (&К) И ПрД.Период МЕЖДУ &Н И &КП
      И ПрД.СтатьяДоходов.А_СтатьяДвиженияДенежныхСредств В
        (ВЫБРАТЬ Ст.СтатьяДвиженияДенежныхСредств ИЗ Справочник.А_Статьи_PL.Статьи КАК Ст ГДЕ Ст.Ссылка.Группа.Код = &Код)""",
        К=корень, Н=н, КП=к, Код=ГРУППА_ОД)
    return round(выручка + прочие, 2)


def документ_од(подр, свод, м):
    return число("""
    ВЫБРАТЬ СУММА(Стр.Сумма) КАК Сумма
    ИЗ Документ.А_ОтчетPL.ДанныеОтчета КАК Стр ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.А_ОтчетPL КАК Док ПО Стр.Ссылка = Док.Ссылка
    ГДЕ Док.Проведен И Док.Подразделение = &П И Док.ВключатьДочерние = &Свод
      И ГОД(Док.Дата) * 100 + МЕСЯЦ(Док.Дата) = &М И Стр.Статья.Группа.Код = &Код""", П=подр, Свод=свод, М=2026 * 100 + м, Код=ГРУППА_ОД)


def сформировать(имя_варианта, свод, подразделение, м1, м2):
    отчет = erp.Отчеты.А_ОтчетPL_ПолныйЕРП_Остатки.Создать()
    к = отчет.КомпоновщикНастроек
    к.Инициализировать(erp.NewObject("ИсточникДоступныхНастроекКомпоновкиДанных", отчет.СхемаКомпоновкиДанных))
    к.ЗагрузитьНастройки(ВАР(имя_варианта).Настройки.Получить())
    н = к.Настройки
    п = erp.NewObject("СтандартныйПериод")
    п.ДатаНачала = дата(2026, м1, 1)
    п.ДатаОкончания = конец_месяца(2026, м2)
    for имя, значение in (("Период", п), ("СводыНаправлений", свод), ("ВыводитьСНачалаГода", True),
                          ("ВыводитьНакопительный", True), ("ВыводитьМесяцы", True)):
        э = н.ПараметрыДанных.Элементы.Найти(имя)
        э.Использование = True
        э.Значение = значение

    def обойти(элементы):
        for i in range(элементы.Количество()):
            о = элементы.Получить(i)
            try:
                поле = S(о.ЛевоеЗначение)
            except Exception:
                try:
                    обойти(о.Элементы)
                except Exception:
                    pass
                continue
            if поле == "Подразделение":
                видср = S(о.ВидСравнения)
                сп = erp.NewObject("СписокЗначений")
                сп.Добавить(подразделение)
                if видср == "В группе из списка":
                    о.ПравоеЗначение = сп; о.Использование = True
                elif видср == "В списке":
                    о.Использование = False
                elif "Не в" not in видср:
                    о.ПравоеЗначение = сп; о.Использование = True
    обойти(н.Отбор.Элементы)
    таб = erp.NewObject("ТабличныйДокумент")
    отчет.СкомпоноватьРезультат(таб)
    return таб


def число_из(текст):
    t = текст.replace("\xa0", "").replace(" ", "").replace(",", ".")
    return float(t) if re.fullmatch(r"-?\d+(\.\d+)?", t) else 0.0


def строка_од(таб):
    """{заголовок группы: сумма 'Сумма, грн'} для строки «Операционный доход»."""
    в, ш = int(таб.ВысотаТаблицы), int(таб.ШиринаТаблицы)
    ячейка = lambda r, c: S(таб.Область(r, c, r, c).Текст).strip()
    шапка = None
    for r in range(1, в + 1):
        if ячейка(r, 1) == "Строка PL":
            шапка = r
            break
    assert шапка, "не найдена шапка «Строка PL»"
    группы = {}
    текущая = None
    for c in range(2, ш + 1):
        t = ячейка(шапка, c)
        if t:
            текущая = t
        if ячейка(шапка + 1, c).startswith("Сумма, грн") and текущая:
            группы[текущая] = c
    строка = None
    for r in range(шапка + 2, в + 1):
        if ячейка(r, 1) in ("Операционный доход", "Операційний дохід"):
            строка = r
            break
    assert строка, "не найдена строка «Операционный доход»"
    return {г: число_из(ячейка(строка, c)) for г, c in группы.items()}


def равно(a, b):
    return abs(a - b) < 0.01


def сценарий(имя, вариант, корень_имя, свод, м1, м2, месяцы_док=()):
    print("=" * 96)
    print("%s — «%s», %s, свод=%s, %02d..%02d.2026" % (имя, вариант, корень_имя, свод, м1, м2))
    корень = ПОДР(корень_имя)
    таб = сформировать(вариант, свод, корень, м1, м2)
    од = строка_од(таб)
    print("  колонки:", ", ".join("%s=%.2f" % kv for kv in од.items()))
    имена = {7: "Июль 2026", 8: "Август 2026", 5: "Май 2026", 6: "Июнь 2026"}
    ерп = {м: ерп_од(корень, м) for м in range(7, м2 + 1)}
    for м in range(max(м1, 7), м2 + 1):
        чек("колонка «%s» есть и = ЕРП" % имена[м], имена[м] in од and равно(од[имена[м]], ерп[м]),
            "%.2f / %.2f" % (од.get(имена[м], -1), ерп[м]))
    for м in месяцы_док:
        ож = документ_од(корень, свод, м)
        чек("колонка «%s» = документ" % имена[м], равно(од.get(имена[м], 0), ож), "%.2f / %.2f" % (од.get(имена[м], 0), ож))
    снг = остатки_од(корень, свод, "СНачалаГода") + sum(ерп.values())
    нак = остатки_од(корень, свод, "Накопительно") + sum(ерп.values())
    чек("«PL с начала года» = остатки + ЕРП", равно(од.get("PL с начала года", 0), снг), "%.2f / %.2f" % (од.get("PL с начала года", 0), снг))
    чек("«Накопительный PL» = остатки + ЕРП", равно(од.get("Накопительный PL", 0), нак), "%.2f / %.2f" % (од.get("Накопительный PL", 0), нак))
    return од


с1 = сценарий("С1", СВОД, "Спецтехника", True, 7, 8)
чек("С1: июль = 3 205 500,00", равно(с1.get("Июль 2026", 0), 3205500.00))
чек("С1: «PL с начала года» = 12 404 410,34 + 3 205 500,00 + август",
    равно(с1.get("PL с начала года", 0), 12404410.34 + 3205500.00 + ерп_од(ПОДР("Спецтехника"), 8)))
сценарий("С2", СВОД, "Строительство", True, 7, 8)
сценарий("С3", ОБЪЕКТ, "МК Кривий ріг", False, 1, 8, месяцы_док=(5, 6))
с4 = сценарий("С4", СВОД, "Спецтехника", True, 8, 8)
чек("С4: нет колонки «Июль 2026», но «PL с начала года» = С1", "Июль 2026" not in с4 and равно(с4.get("PL с начала года", 0), с1.get("PL с начала года", 0)))
с5 = сценарий("С5", ОБЪЕКТ, "Спецтехника", False, 7, 8)
чек("С5: объект «PL с начала года» = свод", равно(с5.get("PL с начала года", 0), с1.get("PL с начала года", 0)))
чек("С5: объект «Накопительный PL» = свод", равно(с5.get("Накопительный PL", 0), с1.get("Накопительный PL", 0)))
сценарий("С6", ОБЪЕКТ, "ЦО", False, 7, 8)
print("=" * 96)
print("ПРОВАЛОВ: %d" % ПРОВАЛОВ[0])
sys.exit(1 if ПРОВАЛОВ[0] else 0)
```

- [ ] **Step 2: Запустить приёмку**

Run: `/c/Python313/python "_Rarzrabotki/Python/test/acceptance_otchetpl_ostatki_granica_2026-09-02.py"`
Expected: `ПРОВАЛОВ: 0`. Типичные причины провалов: (а) база на старом коде — колонки «Июль» нет → Task 4; (б) параметр варианта не найден — имя параметра в СКД отличается (проверить `ПараметрыДанных.Элементы`); (в) расхождение ЕРП-эталона на копейки из-за живого августа — перезапустить (эталон считается тем же прогоном).

- [ ] **Step 3: Регрессия книги (июль неизменен)**

Run: `/c/Python313/python "_Rarzrabotki/Python/test/acceptance_kniga_svody_celi_2026-08-31.py"`
Expected: как до правки — 5/5 зелёные (июль как последний месяц в обоих правилах = ЕРП).

- [ ] **Step 4: Пометить снятые с регламента тесты**

В начало docstring файлов `test_otchetpl_ostatki_v12_2026-08-31.py` и `acceptance_otchetpl_ostatki_smoke_2026-08-31.py` добавить первой строкой:
`RETIRED 02.09.2026: правило источника изменено (граница 30.06.2026, после — только ЕРП); паритет с v11/родителем невозможен by design. Актуальные тесты: test_otchetpl_ostatki_v14_2026-09-02.py, acceptance_otchetpl_ostatki_granica_2026-09-02.py.`

Run (Python, вставка после первой строки `"""`):
```bash
cd /c/Configuration_downloads/BASERP25/_Rarzrabotki/Python/test && /c/Python313/python - <<'EOF'
import io
метка = "RETIRED 02.09.2026: правило источника изменено (граница 30.06.2026, после — только ЕРП); паритет с v11/родителем невозможен by design. Актуальные тесты: test_otchetpl_ostatki_v14_2026-09-02.py, acceptance_otchetpl_ostatki_granica_2026-09-02.py.\n"
for p in ("test_otchetpl_ostatki_v12_2026-08-31.py", "acceptance_otchetpl_ostatki_smoke_2026-08-31.py"):
    s = io.open(p, encoding="utf-8").read()
    if "RETIRED 02.09.2026" in s: continue
    i = s.index('"""') + 3
    s = s[:i] + метка + s[i:]
    io.open(p, "w", encoding="utf-8", newline="").write(s)
    print("помечен:", p)
EOF
```
Expected: `помечен: …` для обоих.

---

### Task 7: Зеркальный внешний отчёт `.erf`

**Files:**
- Modify: `_Rarzrabotki/Отчеты/А_ОтчетPL_ПолныйЕРП_Остатки/Ext/ObjectModule.bsl`
- Rebuild: `_Rarzrabotki/Отчеты/А_ОтчетPL_ПолныйЕРП_Остатки.erf`

- [ ] **Step 1: Скопировать модуль в клон и сверить**

Run:
```bash
cd /c/Configuration_downloads/BASERP25 && cp "Reports/А_ОтчетPL_ПолныйЕРП_Остатки/Ext/ObjectModule.bsl" "_Rarzrabotki/Отчеты/А_ОтчетPL_ПолныйЕРП_Остатки/Ext/ObjectModule.bsl" && cmp "Reports/А_ОтчетPL_ПолныйЕРП_Остатки/Ext/ObjectModule.bsl" "_Rarzrabotki/Отчеты/А_ОтчетPL_ПолныйЕРП_Остатки/Ext/ObjectModule.bsl" && echo IDENTICAL
```
Expected: `IDENTICAL`.

- [ ] **Step 2: Пересобрать .erf через базу-копию (Конфигуратор BaseERP может быть занят)**

Run:
```bash
cd /c/Configuration_downloads/BASERP25 && powershell.exe -NoProfile -File ".claude/skills/epf-build/scripts/epf-build.ps1" -InfoBaseServer "SQLSERVER" -InfoBaseRef "BaseERPRazr" -UserName "Администратор" -Password "24043" -SourceFile "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Отчеты\А_ОтчетPL_ПолныйЕРП_Остатки.xml" -OutputFile "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Отчеты\А_ОтчетPL_ПолныйЕРП_Остатки.erf"
```
Expected: .erf пересобран (дата файла свежая). Если BaseERPRazr не знает `А_ОстаткиОтчетаPL` (ошибка ссылок метаданных) — собрать против `localhost/BaseERP`, когда Конфигуратор свободен. `xr:ClassId` в корневом XML не регенерировать.

---

### Task 8: База знаний и память

**Files:**
- Modify: `_Rarzrabotki/notebook/knowledge_А_ОтчетPL_ПолныйЕРП_Остатки/KNOWLEDGE_MAP.md`, `01_dannye_i_istochniki.md`, `02_skd_i_variant.md`, `03_svyazi_i_vygruzka.md`
- Modify: `_Rarzrabotki/notebook/knowledge_А_ОтчетPL_ПолныйЕРП/02_gibrid_granica.md`
- Modify: память `vvod_ostatkov_pl_kontur.md`, `otchetpl_polny_erp_gibrid.md`, `MEMORY.md`

- [ ] **Step 1: KNOWLEDGE_MAP.md — таблица закона и инварианты**

Заменить раздел «🔴 Закон источника факта (исключений нет)» (таблица + абзац про `&МесяцОстатков`) на:

```markdown
## 🔴 Закон источника факта (решение финансиста 02.09.2026, исключений нет)

Граница — **константа** `ГраницаОстатков() = 30.06.2026 23:59:59` в модуле отчёта.
Документы `А_ОтчетPL` и `А_ВводОстатковОтчетаPL` больше не загружаются: остатки разовые.

| Колонка | Месяцы ≤ 30.06.2026 | Месяцы ≥ 01.07.2026 |
|---|---|---|
| Месячные | `Документ.А_ОтчетPL` (слой = `ВключатьДочерние`) | ТОЛЬКО регистры ЕРП (ПрочиеРасходы, ВыручкаИСебестоимостьПродаж, ПрочиеДоходы) |
| «PL с начала года» (890000) | РН `А_ОстаткиОтчетаPL`, `…СНачалаГода` (только если год границы = год конца периода) | + ЕРП за ВСЕ месяцы от 01.07.2026 по конец периода, включая месяцы вне периода |
| «Накопительный PL» (900000) | РН `А_ОстаткиОтчетаPL`, `…Накопительно` | + те же ЕРП |
| «План итого» (900100) | `Документ.А_БюджетМесяц` (ВидПериода = Объект) | |

Документы `А_ОтчетPL` с датой после границы не читаются никуда. Внутри месяца источники не смешиваются.
Период целиком до границы: накопительные = остатки на 30.06.2026 как есть.
```

Инвариант 1 заменить на: «Закон источника выше. Документы `А_ОтчетPL`/`А_ВводОстатковОтчетаPL` заморожены — дыры после 30.06.2026 не бывает (ЕРП), дыры до — не чинятся».
Инвариант 3 заменить на: «Граница — константа в модуле (`ГраницаОстатков()`), документом не вычисляется».
В таблице «Живой код» строку «Копия запроса» → `_Rarzrabotki/Python/test/v14_full.sql`; «Модуль (запрос v12 …)» → «запрос v14».
Добавить инвариант 7: «Спецтехника и ЦО представлены в ОБОИХ слоях остатков (объектный слой — копии документов, 02.09.2026); в объектном режиме январь–июнь по ним пустые, накопительные = копия + ЕРП».

- [ ] **Step 2: 01_dannye_i_istochniki.md**

Раздел «Запрос v12» → «Запрос v14»: ветки 1–2 — «только месяцы > границы и ≤ конца периода (в месячные колонки — попавшие в период, в накопительные — все)»; ветка 3 — «только `Док.Дата <= ГраницаОстатков` и в периоде»; ветку 4 удалить; ветка 5 — добавить «`Ост.Период = &ГраницаОстатков`; 890000 только при `&ОстаткиВГодуПериода`». Абзац «Параметры запроса»: убрать `ПоследнийМесяц`, `МесяцОстатков`; добавить `ГраницаОстатков`, `НачалоЕРП` (= 01.07.2026), `ОстаткиВГодуПериода`. В разделе «Точка фиксации» добавить абзац: «02.09.2026 — объектный слой для Спецтехники и ЦО: два документа-копии (комментарий «… (объектный слой)»), скрипт `create_ostatki_obekt_sloy_2026-09-02.py`».

- [ ] **Step 3: 02_skd_i_variant.md и 03_svyazi_i_vygruzka.md**

02: заголовок «Аналитика ЕРП-строк последнего месяца» → «Аналитика ЕРП-строк (месяцы после границы)». 03: строка «РН ЕРП … факт последнего месяца» → «факт всех месяцев после 30.06.2026»; строка родителя → «остался на старом правиле (последний месяц = ЕРП); с 02.09.2026 паритет с Остатки не ожидается»; таблица тестов: добавить `test_otchetpl_ostatki_v14_2026-09-02.py`, `acceptance_otchetpl_ostatki_granica_2026-09-02.py`, `create_ostatki_obekt_sloy_2026-09-02.py`, `set_variants_svod_2026-09-02.py`; пометить v12-тест и smoke как RETIRED; в «Грабли» добавить: «документ `А_ОтчетPL` с датой > 30.06.2026 игнорируется полностью; накопительные включают ЕРП-месяцы вне периода; свод-варианты хранят `СводыНаправлений = Да`».

- [ ] **Step 4: Родительская KB — предупреждение**

В `knowledge_А_ОтчетPL_ПолныйЕРП/02_gibrid_granica.md` сразу после заголовка первого уровня вставить:

```markdown
> ⚠ **С 02.09.2026 это правило действует ТОЛЬКО для родителя `А_ОтчетPL_ПолныйЕРП`.**
> Отчёт-клон `А_ОтчетPL_ПолныйЕРП_Остатки` живёт по другому закону: по 30.06.2026 — документы/остатки,
> после — только регистры ЕРП (см. `knowledge_А_ОтчетPL_ПолныйЕРП_Остатки/KNOWLEDGE_MAP.md`). Не переносить.
```

- [ ] **Step 5: Память проекта**

`vvod_ostatkov_pl_kontur.md` и `otchetpl_polny_erp_gibrid.md`: заменить формулу накопительных на «остатки на 30.06.2026 (константа) + ЕРП все месяцы после»; дописать: «Спецтехника/ЦО — объектный слой копиями остатков (02.09.2026)»; «свод-варианты хранят Да». В `MEMORY.md` обновить хуки этих двух строк.

---

### Task 9: Коммит в worktree

**Files:**
- Copy into worktree: `Reports/А_ОтчетPL_ПолныйЕРП_Остатки.xml`, `Reports/А_ОтчетPL_ПолныйЕРП_Остатки/`, новые/изменённые файлы `_Rarzrabotki/Python/test/`, KB, план

- [ ] **Step 1: Скопировать в worktree и закоммитить**

Run:
```bash
W=/c/Configuration_downloads/BASERP25/.claude/worktrees/pl-report-period-ytd-sum-3fe74c; M=/c/Configuration_downloads/BASERP25
mkdir -p "$W/Reports" "$W/_Rarzrabotki/Python/test" "$W/_Rarzrabotki/notebook/knowledge_А_ОтчетPL_ПолныйЕРП_Остатки" "$W/_Rarzrabotki/notebook/knowledge_А_ОтчетPL_ПолныйЕРП" "$W/_Rarzrabotki/Отчеты/А_ОтчетPL_ПолныйЕРП_Остатки/Ext"
cp -r "$M/Reports/А_ОтчетPL_ПолныйЕРП_Остатки" "$M/Reports/А_ОтчетPL_ПолныйЕРП_Остатки.xml" "$W/Reports/"
for f in create_ostatki_obekt_sloy_2026-09-02.py patch_v14_2026-09-02.py v14_full.sql test_otchetpl_ostatki_v14_2026-09-02.py set_variants_svod_2026-09-02.py acceptance_otchetpl_ostatki_granica_2026-09-02.py test_otchetpl_ostatki_v12_2026-08-31.py acceptance_otchetpl_ostatki_smoke_2026-08-31.py; do cp "$M/_Rarzrabotki/Python/test/$f" "$W/_Rarzrabotki/Python/test/"; done
cp "$M/_Rarzrabotki/notebook/knowledge_А_ОтчетPL_ПолныйЕРП_Остатки/"*.md "$W/_Rarzrabotki/notebook/knowledge_А_ОтчетPL_ПолныйЕРП_Остатки/"
cp "$M/_Rarzrabotki/notebook/knowledge_А_ОтчетPL_ПолныйЕРП/02_gibrid_granica.md" "$W/_Rarzrabotki/notebook/knowledge_А_ОтчетPL_ПолныйЕРП/"
cp "$M/_Rarzrabotki/Отчеты/А_ОтчетPL_ПолныйЕРП_Остатки/Ext/ObjectModule.bsl" "$W/_Rarzrabotki/Отчеты/А_ОтчетPL_ПолныйЕРП_Остатки/Ext/"
cd "$W" && git add "Reports/А_ОтчетPL_ПолныйЕРП_Остатки" "Reports/А_ОтчетPL_ПолныйЕРП_Остатки.xml" "_Rarzrabotki/Python/test" "_Rarzrabotki/notebook/knowledge_А_ОтчетPL_ПолныйЕРП_Остатки" "_Rarzrabotki/notebook/knowledge_А_ОтчетPL_ПолныйЕРП/02_gibrid_granica.md" "_Rarzrabotki/Отчеты/А_ОтчетPL_ПолныйЕРП_Остатки/Ext/ObjectModule.bsl" docs/superpowers/plans
git commit -m "feat(А_ОтчетPL_ПолныйЕРП_Остатки): граница 30.06.2026 константой, после — только ЕРП; объектный слой остатков Спецтехника/ЦО; тесты v14

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```
Expected: коммит создан; `git status --short` по этим путям пуст.

---

## Self-review (выполнен при написании)

- Спека §2 (константа, закон) → Task 2 (`ЗАМЕНЫ`, `ЗАМЕНЫ_МОДУЛЬ`, `НОВАЯ_ФУНКЦИЯ`), Task 3. Правило «период до границы = остатки как есть» — следствие условий (ЕРП-диапазон пуст), проверяется не отдельно, а инвариантом «до границы нет ЕРП». Год 2027 — параметр `ОстаткиВГодуПериода`.
- §3 (семь точек запроса) → `ЗАМЕНЫ` + `удалить_ветку_документов_в_накопительные` + `ЗАМЕНЫ_ПОСЛЕ`; втСводныеПодразделения — первая замена.
- §4 (миграция) → Task 1; §5 (варианты) → Task 5; §6 (не меняется) — тексты/комментарии не трогаем, кроме комментариев при удаляемых/добавляемых строках параметров.
- §7 (проверка) → Task 2 Step 4–6 (RED), Task 6 (приёмка С1–С6 + регрессия книги + RETIRED-метки). Сценарий 6 спеки (инварианты) — `инварианты()` в RED-тесте + `С5/С6` в приёмке.
- §8 (доставка) → Task 4, 7, 8, 9.
- Имена параметров одинаковы в патче, RED-тесте и модуле: `ГраницаОстатков`, `НачалоЕРП`, `ОстаткиВГодуПериода`, `КлючНачалаГода`.
