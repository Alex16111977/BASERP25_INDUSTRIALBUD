# ВКассу из РаспределениеФ2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) или superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Реализовать создание `Документ.ВедомостьНаВыплатуЗарплатыВКассу` из `Документ.РаспределениеФ2` по кнопке + автотриггеру с двумя ТЧ (Зарплата + новая А_РасшифровкаВыплатыЗарплатаПоФизлицам).

**Architecture:** Паттерн Find-Or-Create + Cleanup по образцу `А_ОтгрузкаПродукции.СоздатьДокументОказанияУслугМеждуПодразделениямиПоОтгрузкаПродукции`. Триггер: кнопка в форме Ф2 + `ПриЗаписи` родителя (с защитой от рекурсии через `ОбменДанными.Загрузка`). Шапка ведомости: `Подразделение = "ЦО"` (предопределённое в `ТОВ "ІНДАСТРІАЛБУД"`). 1 Ф2 ↔ 1 ВКассу (1:1). Все правки — в основной конфигурации `C:\Configuration_downloads\BASERP25\`, никаких CFE.

**Tech Stack:** 1С:Предприятие 8.3.20, BAS ERP 2.5 INDUSTRIALBUD. Доставка через `/db-load-xml -Mode Partial` + `/db-update -Dynamic+`. Тестирование: Python 3.13 + win32com (V83.COMConnector). Connection `Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"`.

**Spec:** `docs/superpowers/specs/2026-05-26-vedomost-vkassu-iz-raspredeleniyaf2-design.md`

**Эталонный документ для smoke-теста:** `Документ.РаспределениеФ2 №000000026` от 10.12.2025 12:00:00, Σ ТЧ Сотрудники = 348 800 UAH (19 ФЛ), Направление «Спецтехника», Период выплаты 01.11.2025–30.11.2025, ТЧ Распределение = 2 строки (Автокран XCMG × Спецтехника × 26ч + 47ч).

**Уже сделано пользователем вручную:**
- `Документ.РаспределениеФ2.А_ВедомостьВКассу` (тип `Документ.ВедомостьНаВыплатуЗарплатыВКассу`) — реквизит шапки ✅
- `Документ.ВедомостьНаВыплатуЗарплатыВКассу.А_РаспределениеФ2` (тип `Документ.РаспределениеФ2`) — реквизит шапки ✅

---

## File Structure

### Файлы для МОДИФИКАЦИИ

| Путь | Что меняем | Кто отвечает |
|---|---|---|
| `Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml` | Добавляем `<TabularSection>` `А_РасшифровкаВыплатыЗарплатаПоФизлицам` (8 реквизитов) после ТЧ `ВзносыФОТ` | Task 2 |
| `Documents/РаспределениеФ2/Ext/ObjectModule.bsl` | Добавляем область `#Область ЕРП_СоздатьВедомостьНаВыплатуВКассу` + процедуру `ПриЗаписи` + 4 утиля | Tasks 4-6 |
| `Documents/РаспределениеФ2/Forms/ФормаДокумента/Ext/Form.xml` | +`<Command name="СоздатьВедомостьВКассу" id="3">` | Task 7 |
| `Documents/РаспределениеФ2/Forms/ФормаДокумента/Ext/Form/Module.bsl` | +обработчики команды (клиент + сервер) | Task 7 |

### Файлы для СОЗДАНИЯ

| Путь | Назначение |
|---|---|
| `_Rarzrabotki/Python/test/test_create_vedomost_vkassu_iz_f2_preflight.py` | Pre-flight: проверка эталона Ф2 №000000026 в базе |
| `_Rarzrabotki/Python/test/test_create_vedomost_vkassu_iz_f2_smoke.py` | Smoke: создание ведомости + инвариант сумм A=B=−C |
| `_Rarzrabotki/Python/test/test_create_vedomost_vkassu_iz_f2_idempotency.py` | Идемпотентность: повторный вызов = тот же UUID |
| `_Rarzrabotki/Python/test/test_create_vedomost_vkassu_iz_f2_rashifrovka.py` | Содержание ТЧ А_Расшифровка корректно по ТЧ Распределение Ф2 |
| `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/11_vedomost_iz_raspredeleniyaF2.md` | Knowledge — итоговая документация механизма |

---

## Task 1: Pre-flight — Python COM проверка эталона

**Цель:** Подтвердить наличие документа `РаспределениеФ2 №000000026` и его содержимое перед написанием BSL. По правилу CLAUDE.md Rule #-1.

**Files:**
- Create: `_Rarzrabotki/Python/test/test_create_vedomost_vkassu_iz_f2_preflight.py`

- [ ] **Step 1: Создать скрипт preflight**

Создать файл `_Rarzrabotki/Python/test/test_create_vedomost_vkassu_iz_f2_preflight.py`:

```python
"""
Pre-flight: проверка эталонного документа РаспределениеФ2 №000000026 от 10.12.2025.
Перед написанием BSL подтвердить структуру + содержимое.
"""
import io
import sys

sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding="utf-8")

import win32com.client

CONN_ERP = 'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"'


def main():
    v8 = win32com.client.Dispatch("V83.COMConnector")
    erp = v8.Connect(CONN_ERP)

    q = erp.NewObject("Запрос")
    q.Текст = """
ВЫБРАТЬ ПЕРВЫЕ 1
    Док.Ссылка КАК Ссылка,
    Док.Номер  КАК Номер,
    Док.Дата   КАК Дата,
    Док.Организация.Наименование КАК Орг,
    Док.Направление.Наименование КАК Направление,
    Док.КассаДатаС КАК КассаДатаС,
    Док.КассаДатаПо КАК КассаДатаПо,
    Док.Проведен КАК Проведен,
    Док.А_ВедомостьВКассу КАК А_ВедомостьВКассу
ИЗ Документ.РаспределениеФ2 КАК Док
ГДЕ Док.Номер = "000000026" И Док.Проведен
"""
    rs = q.Выполнить()
    if rs.Пустой():
        print("FAIL: РаспределениеФ2 №000000026 не найден или не проведён")
        sys.exit(1)

    sel = rs.Выбрать(); sel.Следующий()
    print(f"OK: {sel.Номер} от {sel.Дата}")
    print(f"  Орг: {sel.Орг}")
    print(f"  Направление: {sel.Направление}")
    print(f"  Период выплаты: {sel.КассаДатаС} .. {sel.КассаДатаПо}")
    print(f"  А_ВедомостьВКассу (текущая): {sel.А_ВедомостьВКассу}")

    obj = sel.Ссылка.ПолучитьОбъект()
    print(f"\nТЧ Сотрудники: {obj.Сотрудники.Количество()} строк")
    sum_sotr = 0
    for i in range(obj.Сотрудники.Количество()):
        r = obj.Сотрудники.Получить(i)
        sum_sotr += float(r.СуммаНачисления)
    print(f"  Σ СуммаНачисления = {sum_sotr:,.2f}")

    print(f"\nТЧ Распределение: {obj.Распределение.Количество()} строк")
    sum_rasp = 0
    sum_hours = 0
    for i in range(obj.Распределение.Количество()):
        r = obj.Распределение.Получить(i)
        sum_rasp += float(r.СуммаНачисления)
        sum_hours += float(r.ОтработаноЧасов)
        if i < 5:
            print(f"  - {r.Сотрудник} | {r.Подразделение} | {r.Направление} | "
                  f"{r.ОтработаноЧасов}ч | {r.СуммаНачисления:,.2f}")
    print(f"  Σ ОтработаноЧасов = {sum_hours}")
    print(f"  Σ СуммаНачисления = {sum_rasp:,.2f}")

    # Проверка эталона
    EXPECTED_SUM = 348800.00
    if abs(sum_sotr - EXPECTED_SUM) > 0.01:
        print(f"\nFAIL: Σ Сотрудники = {sum_sotr:,.2f}, ожидалось {EXPECTED_SUM:,.2f}")
        sys.exit(1)

    print(f"\nPASS: эталон РаспределениеФ2 №000000026 соответствует ожиданиям")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Запустить скрипт через mcp__python-runner**

```
mcp__python-runner__run_command(command='C:\\Python313\\python.exe "C:\\Configuration_downloads\\BASERP25\\_Rarzrabotki\\Python\\test\\test_create_vedomost_vkassu_iz_f2_preflight.py"')
```

Expected:
```
OK: 000000026 от 2025-12-10 12:00:00...
  Орг: ...
  Направление: Спецтехника
  Период выплаты: 2025-11-01 .. 2025-11-30
  А_ВедомостьВКассу (текущая): <пусто>
ТЧ Сотрудники: 19 строк
  Σ СуммаНачисления = 348,800.00
ТЧ Распределение: 2 строки
  Σ СуммаНачисления = 3,200.00 (примерно — это разнесение часов, не сумма выплат)
PASS: эталон РаспределениеФ2 №000000026 соответствует ожиданиям
```

- [ ] **Step 3: Commit**

```bash
git add _Rarzrabotki/Python/test/test_create_vedomost_vkassu_iz_f2_preflight.py
git commit -m "$(cat <<'EOF'
test(vedomost-vkassu-f2): pre-flight проверка эталона РаспределениеФ2 №000000026

CLAUDE.md Rule #-1: Python COM тест перед BSL.
Эталон: Σ ТЧ Сотрудники = 348 800 UAH (19 ФЛ), 2 строки ТЧ Распределение.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: Добавить ТЧ `А_РасшифровкаВыплатыЗарплатаПоФизлицам` в ВКассу.xml

**Цель:** Добавить новую ТЧ с 8 реквизитами после существующей ТЧ `ВзносыФОТ`.

**Files:**
- Modify: `Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml` (вставка нового `<TabularSection>` после строки с закрытием ТЧ `ВзносыФОТ`)

- [ ] **Step 1: Найти место вставки**

Прочитать `Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml` — найти где заканчивается последняя ТЧ `ВзносыФОТ`. Используй grep:

```
Grep pattern: "</TabularSection>" path: "Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml" output_mode: "content" -n: true
```

Последний `</TabularSection>` — это конец ТЧ `ВзносыФОТ`. Новую ТЧ вставлять **сразу после неё** (перед `</Properties>` документа). Обычно это в районе строки 3650-3700, но точное число найдёт grep.

- [ ] **Step 2: Подготовить XML-блок новой ТЧ**

Создать переменную с XML новой ТЧ:

```xml
		<TabularSection uuid="00000000-0000-0000-0000-000000000000">
			<InternalInfo>
				<xr:GeneratedType name="DocumentTabularSection.ВедомостьНаВыплатуЗарплатыВКассу.А_РасшифровкаВыплатыЗарплатаПоФизлицам" category="TabularSection">
					<xr:TypeId>00000000-0000-0000-0000-000000000000</xr:TypeId>
					<xr:ValueId>00000000-0000-0000-0000-000000000000</xr:ValueId>
				</xr:GeneratedType>
				<xr:GeneratedType name="DocumentTabularSectionRow.ВедомостьНаВыплатуЗарплатыВКассу.А_РасшифровкаВыплатыЗарплатаПоФизлицам" category="TabularSectionRow">
					<xr:TypeId>00000000-0000-0000-0000-000000000000</xr:TypeId>
					<xr:ValueId>00000000-0000-0000-0000-000000000000</xr:ValueId>
				</xr:GeneratedType>
			</InternalInfo>
			<Properties>
				<Name>А_РасшифровкаВыплатыЗарплатаПоФизлицам</Name>
				<Synonym>
					<v8:item>
						<v8:lang>ru</v8:lang>
						<v8:content>А Расшифровка выплаты зарплата по физлицам</v8:content>
					</v8:item>
				</Synonym>
				<Comment/>
				<ToolTip/>
				<FillChecking>DontCheck</FillChecking>
				<StandardAttributes>
					<xr:StandardAttribute name="LineNumber">
						<xr:FillChecking>DontCheck</xr:FillChecking>
					</xr:StandardAttribute>
				</StandardAttributes>
				<Use>ForFolderAndItem</Use>
			</Properties>
			<ChildObjects>
				<Attribute uuid="00000000-0000-0000-0000-000000000000">
					<Properties>
						<Name>ФизическоеЛицо</Name>
						<Synonym><v8:item><v8:lang>ru</v8:lang><v8:content>Физическое лицо</v8:content></v8:item></Synonym>
						<Type><v8:Type>cfg:CatalogRef.ФизическиеЛица</v8:Type></Type>
						<MinValue xsi:nil="true"/><MaxValue xsi:nil="true"/>
						<FillFromFillingValue>false</FillFromFillingValue>
						<FillValue xsi:nil="true"/>
						<FillChecking>DontCheck</FillChecking>
						<ChoiceParameterLinks/><ChoiceParameters/>
						<QuickChoice>Auto</QuickChoice>
						<CreateOnInput>Auto</CreateOnInput>
						<ChoiceForm/>
						<LinkByType/>
						<ChoiceHistoryOnInput>Auto</ChoiceHistoryOnInput>
						<FullTextSearch>Use</FullTextSearch>
						<DataHistory>Use</DataHistory>
					</Properties>
				</Attribute>
				<Attribute uuid="00000000-0000-0000-0000-000000000000">
					<Properties>
						<Name>Сотрудник</Name>
						<Synonym><v8:item><v8:lang>ru</v8:lang><v8:content>Сотрудник</v8:content></v8:item></Synonym>
						<Type><v8:Type>cfg:CatalogRef.Сотрудники</v8:Type></Type>
						<MinValue xsi:nil="true"/><MaxValue xsi:nil="true"/>
						<FillFromFillingValue>false</FillFromFillingValue>
						<FillValue xsi:nil="true"/>
						<FillChecking>DontCheck</FillChecking>
						<ChoiceParameterLinks/><ChoiceParameters/>
						<QuickChoice>Auto</QuickChoice>
						<CreateOnInput>Auto</CreateOnInput>
						<ChoiceForm/>
						<LinkByType/>
						<ChoiceHistoryOnInput>Auto</ChoiceHistoryOnInput>
						<FullTextSearch>Use</FullTextSearch>
						<DataHistory>Use</DataHistory>
					</Properties>
				</Attribute>
				<Attribute uuid="00000000-0000-0000-0000-000000000000">
					<Properties>
						<Name>Подразделение</Name>
						<Synonym><v8:item><v8:lang>ru</v8:lang><v8:content>Подразделение</v8:content></v8:item></Synonym>
						<Type><v8:Type>cfg:CatalogRef.ПодразделенияОрганизаций</v8:Type></Type>
						<MinValue xsi:nil="true"/><MaxValue xsi:nil="true"/>
						<FillFromFillingValue>false</FillFromFillingValue>
						<FillValue xsi:nil="true"/>
						<FillChecking>DontCheck</FillChecking>
						<ChoiceParameterLinks/><ChoiceParameters/>
						<QuickChoice>Auto</QuickChoice>
						<CreateOnInput>Auto</CreateOnInput>
						<ChoiceForm/>
						<LinkByType/>
						<ChoiceHistoryOnInput>Auto</ChoiceHistoryOnInput>
						<FullTextSearch>Use</FullTextSearch>
						<DataHistory>Use</DataHistory>
					</Properties>
				</Attribute>
				<Attribute uuid="00000000-0000-0000-0000-000000000000">
					<Properties>
						<Name>НаправлениеДеятельности</Name>
						<Synonym><v8:item><v8:lang>ru</v8:lang><v8:content>Направление деятельности</v8:content></v8:item></Synonym>
						<Type><v8:Type>cfg:CatalogRef.НаправленияДеятельности</v8:Type></Type>
						<MinValue xsi:nil="true"/><MaxValue xsi:nil="true"/>
						<FillFromFillingValue>false</FillFromFillingValue>
						<FillValue xsi:nil="true"/>
						<FillChecking>DontCheck</FillChecking>
						<ChoiceParameterLinks/><ChoiceParameters/>
						<QuickChoice>Auto</QuickChoice>
						<CreateOnInput>Auto</CreateOnInput>
						<ChoiceForm/>
						<LinkByType/>
						<ChoiceHistoryOnInput>Auto</ChoiceHistoryOnInput>
						<FullTextSearch>Use</FullTextSearch>
						<DataHistory>Use</DataHistory>
					</Properties>
				</Attribute>
				<Attribute uuid="00000000-0000-0000-0000-000000000000">
					<Properties>
						<Name>СтатьяДвиженияДенежныхСредств</Name>
						<Synonym><v8:item><v8:lang>ru</v8:lang><v8:content>Статья движения денежных средств</v8:content></v8:item></Synonym>
						<Type><v8:Type>cfg:CatalogRef.СтатьиДвиженияДенежныхСредств</v8:Type></Type>
						<MinValue xsi:nil="true"/><MaxValue xsi:nil="true"/>
						<FillFromFillingValue>false</FillFromFillingValue>
						<FillValue xsi:nil="true"/>
						<FillChecking>DontCheck</FillChecking>
						<ChoiceParameterLinks/><ChoiceParameters/>
						<QuickChoice>Auto</QuickChoice>
						<CreateOnInput>Auto</CreateOnInput>
						<ChoiceForm/>
						<LinkByType/>
						<ChoiceHistoryOnInput>Auto</ChoiceHistoryOnInput>
						<FullTextSearch>Use</FullTextSearch>
						<DataHistory>Use</DataHistory>
					</Properties>
				</Attribute>
				<Attribute uuid="00000000-0000-0000-0000-000000000000">
					<Properties>
						<Name>ОтработаноЧасов</Name>
						<Synonym><v8:item><v8:lang>ru</v8:lang><v8:content>Отработано часов</v8:content></v8:item></Synonym>
						<Type><v8:Type>v8:Number</v8:Type><v8:NumberQualifiers><v8:Digits>10</v8:Digits><v8:FractionDigits>2</v8:FractionDigits><v8:AllowedSign>Any</v8:AllowedSign></v8:NumberQualifiers></Type>
						<MinValue xsi:nil="true"/><MaxValue xsi:nil="true"/>
						<FillFromFillingValue>false</FillFromFillingValue>
						<FillValue xsi:type="xs:decimal">0</FillValue>
						<FillChecking>DontCheck</FillChecking>
						<ChoiceParameterLinks/><ChoiceParameters/>
						<DataHistory>Use</DataHistory>
					</Properties>
				</Attribute>
				<Attribute uuid="00000000-0000-0000-0000-000000000000">
					<Properties>
						<Name>Сумма</Name>
						<Synonym><v8:item><v8:lang>ru</v8:lang><v8:content>Сумма</v8:content></v8:item></Synonym>
						<Type><v8:Type>v8:Number</v8:Type><v8:NumberQualifiers><v8:Digits>15</v8:Digits><v8:FractionDigits>2</v8:FractionDigits><v8:AllowedSign>Any</v8:AllowedSign></v8:NumberQualifiers></Type>
						<MinValue xsi:nil="true"/><MaxValue xsi:nil="true"/>
						<FillFromFillingValue>false</FillFromFillingValue>
						<FillValue xsi:type="xs:decimal">0</FillValue>
						<FillChecking>DontCheck</FillChecking>
						<ChoiceParameterLinks/><ChoiceParameters/>
						<DataHistory>Use</DataHistory>
					</Properties>
				</Attribute>
				<Attribute uuid="00000000-0000-0000-0000-000000000000">
					<Properties>
						<Name>ИдентификаторСтроки</Name>
						<Synonym><v8:item><v8:lang>ru</v8:lang><v8:content>Идентификатор строки</v8:content></v8:item></Synonym>
						<Type><v8:Type>v8:UUID</v8:Type></Type>
						<MinValue xsi:nil="true"/><MaxValue xsi:nil="true"/>
						<FillFromFillingValue>false</FillFromFillingValue>
						<FillValue xsi:nil="true"/>
						<FillChecking>DontCheck</FillChecking>
						<ChoiceParameterLinks/><ChoiceParameters/>
						<DataHistory>Use</DataHistory>
					</Properties>
				</Attribute>
			</ChildObjects>
		</TabularSection>
```

⚠️ Все `uuid="00000000..."` — placeholders. Сгенерировать через Python (Step 3) или вручную через `New-Guid` в PowerShell.

- [ ] **Step 3: Сгенерировать UUID для ТЧ и реквизитов**

В PowerShell или Python:

```
mcp__python-runner__run_command(command='C:\\Python313\\python.exe -c "import uuid; [print(uuid.uuid4()) for _ in range(11)]"')
```

Сохранить 11 UUID: 1 для самой ТЧ, 2 для xr:TypeId/ValueId TabularSection, 2 для xr:TypeId/ValueId TabularSectionRow, 8 для 8 Attribute (=11 шт. — но xr:TypeId/ValueId это **новые** UUID, поэтому 1+2+2+8 = 13).

Корректировка: 1 для самой ТЧ + 2 для GeneratedType TabularSection + 2 для GeneratedType TabularSectionRow + 8 для атрибутов = **13 UUID**.

Запустить:
```
mcp__python-runner__run_command(command='C:\\Python313\\python.exe -c "import uuid; [print(uuid.uuid4()) for _ in range(13)]"')
```

Подставить в XML-блок (заменить все `00000000-0000-0000-0000-000000000000` на реальные UUID).

- [ ] **Step 4: Использовать Skill meta-edit (предпочтительно) для безопасной вставки**

```
Skill: meta-edit
args: добавь в Документ.ВедомостьНаВыплатуЗарплатыВКассу табличную часть А_РасшифровкаВыплатыЗарплатаПоФизлицам с 8 реквизитами: ФизическоеЛицо (СправочникСсылка.ФизическиеЛица), Сотрудник (СправочникСсылка.Сотрудники), Подразделение (СправочникСсылка.ПодразделенияОрганизаций), НаправлениеДеятельности (СправочникСсылка.НаправленияДеятельности), СтатьяДвиженияДенежныхСредств (СправочникСсылка.СтатьиДвиженияДенежныхСредств), ОтработаноЧасов (Число 10.2), Сумма (Число 15.2), ИдентификаторСтроки (УникальныйИдентификатор). FillChecking=DontCheck для всех.
```

Если skill meta-edit недоступен или не справился — ручной Edit XML с подготовленным блоком из Step 2 + UUID из Step 3.

- [ ] **Step 5: Валидация XML через cf-validate (если возможно)**

```
Skill: cf-validate
args: проверь корректность изменений в Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml
```

Альтернатива — визуальная проверка через Read 30-50 строк вокруг новой ТЧ.

- [ ] **Step 6: Проверить через MCP что ТЧ появилась**

```
mcp__1c-workerp__get_metadata_structure(metaType: "Documents", name: "ВедомостьНаВыплатуЗарплатыВКассу")
```

⚠️ MCP покажет существующую структуру **из работающей базы**, а не из XML на диске. Поэтому проверка на этом этапе покажет, что ТЧ ещё нет — это нормально. После Task 3 (db-load-xml + db-update) ТЧ появится в базе.

На этом шаге достаточно убедиться что Edit XML прошёл без ошибок (визуально).

- [ ] **Step 7: Commit XML-правки**

```bash
git add Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml
git commit -m "$(cat <<'EOF'
feat(vedomost-vkassu-f2): ТЧ А_РасшифровкаВыплатыЗарплатаПоФизлицам

8 реквизитов: ФизическоеЛицо, Сотрудник, Подразделение (ПодрОрг),
НаправлениеДеятельности, СтатьяДвиженияДенежныхСредств,
ОтработаноЧасов, Сумма, ИдентификаторСтроки.

Назначение: параллельная детализация ВКассу для разнесения
выплаты по подразделениям/направлениям/статьям ДДС из ТЧ
Распределение Ф2 при создании ведомости из РаспределениеФ2.

Spec: docs/superpowers/specs/2026-05-26-vedomost-vkassu-iz-raspredeleniyaf2-design.md

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Доставка XML #1 в работающую базу

**Цель:** Загрузить изменённый XML ВКассу в базу через partial load + dynamic update. Чтобы Task 4-5 могли проводить ссылки на новую ТЧ.

**Files:**
- (нет правок) — только команды доставки

- [ ] **Step 1: Partial load XML**

```
Skill: db-load-xml
args: -Mode Partial -Files "Documents/ВедомостьНаВыплатуЗарплатыВКассу.xml"
```

Ожидаемый вывод: «Загрузка завершена успешно». Авто-обновление `ConfigDumpInfo.xml` для этого файла.

Если skill не доступен — ручная команда:
```powershell
& "C:\Program Files\1cv8\8.3.20.1914\bin\1cv8.exe" CONFIG /S"SQLSERVER\BaseERP" /N"Администратор" /P"24043" /LoadConfigFromFiles "C:\Configuration_downloads\BASERP25" -files "Documents\ВедомостьНаВыплатуЗарплатыВКассу.xml" -updateConfigDumpInfo
```

- [ ] **Step 2: Dynamic update БД**

```
Skill: db-update
args: -Dynamic+
```

Ожидаемый вывод: «БД обновлена в динамическом режиме».

- [ ] **Step 3: Проверить через MCP что ТЧ появилась**

```
mcp__1c-workerp__get_metadata_structure(metaType: "Documents", name: "ВедомостьНаВыплатуЗарплатыВКассу")
```

Expected: в выводе должна быть строка `ТЧ "А_РасшифровкаВыплатыЗарплатаПоФизлицам"` с 8 реквизитами.

Если ТЧ не появилась — перезапустить MCP-сессию (mcp_server в 1С перечитывает метаданные при reconnect).

- [ ] **Step 4: Commit (нет файловых изменений — только метка)**

```bash
git commit --allow-empty -m "$(cat <<'EOF'
chore(vedomost-vkassu-f2): db-load-xml + db-update — ТЧ доставлена в базу

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: BSL — утилитные процедуры `_ВЫК_*`

**Цель:** Добавить 3 утиля в `Documents/РаспределениеФ2/Ext/ObjectModule.bsl`. Сначала утили (без главной процедуры) — потому что Task 5 будет их вызывать. Если разделить — легче тестировать инкрементально.

**Files:**
- Modify: `Documents/РаспределениеФ2/Ext/ObjectModule.bsl` (текущий: 4 строки, останется как есть `ОбработкаПроведения` + добавляется область с утилями)

- [ ] **Step 1: Прочитать текущий ObjectModule.bsl**

```
Read: Documents/РаспределениеФ2/Ext/ObjectModule.bsl
```

Текущее содержимое (4 строки):
```bsl

Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	// Вставить содержимое обработчика.
КонецПроцедуры

```

⚠️ **Этот код нельзя удалить** — он необходим как минимум как стаб. Добавляем НОВУЮ область **после** него.

- [ ] **Step 2: Добавить область с утилитами**

Использовать Edit, добавив после `КонецПроцедуры` (4-я строка):

```bsl

#Область ЕРП_СоздатьВедомостьНаВыплатуВКассу

// Утилита: Сотрудник → ФизическоеЛицо (один запрос на массив сотрудников).
// Возвращает Соответствие.
Функция _ВЫК_ПостроитьКешСотрудникФЛ(МасСотрудников)

    Результат = Новый Соответствие;
    Если МасСотрудников.Количество() = 0 Тогда
        Возврат Результат;
    КонецЕсли;

    Запрос = Новый Запрос;
    Запрос.Текст =
    "ВЫБРАТЬ
    |	С.Ссылка КАК Сотрудник,
    |	С.ФизическоеЛицо КАК ФизическоеЛицо
    |ИЗ Справочник.Сотрудники КАК С
    |ГДЕ С.Ссылка В (&МасСотрудников)";
    Запрос.УстановитьПараметр("МасСотрудников", МасСотрудников);

    Выборка = Запрос.Выполнить().Выбрать();
    Пока Выборка.Следующий() Цикл
        Если ЗначениеЗаполнено(Выборка.ФизическоеЛицо) Тогда
            Результат.Вставить(Выборка.Сотрудник, Выборка.ФизическоеЛицо);
        КонецЕсли;
    КонецЦикла;

    Возврат Результат;

КонецФункции

// Утилита: СтруктураПредприятия → ПодразделенияОрганизаций (через А_Привилегированный).
// Возвращает Соответствие.
Функция _ВЫК_ПостроитьКешПодрОрг(МасСтрПредпр)

    Результат = Новый Соответствие;
    Для Каждого СтрПредпр Из МасСтрПредпр Цикл
        Если НЕ ЗначениеЗаполнено(СтрПредпр) Тогда Продолжить КонецЕсли;
        Если Результат[СтрПредпр] <> Неопределено Тогда Продолжить КонецЕсли;
        Попытка
            ПодрОрг = А_Привилегированный.ОбеспечитьПодразделенияОрганизацийПоПодразделению(СтрПредпр);
        Исключение
            ПодрОрг = Справочники.ПодразделенияОрганизаций.ПустаяСсылка();
        КонецПопытки;
        Результат.Вставить(СтрПредпр, ПодрОрг);
    КонецЦикла;
    Возврат Результат;

КонецФункции

// Утилита: определение кассы для шапки ведомости.
// Порядок поиска: Орг.А_ОсновнаяКасса → первая активная касса организации → НайтиПоНаименованию("Касса ЗП") → ПустаяСсылка.
Функция _ВЫК_ОпределитьКассу(ОрганизацияСсылка)

    // 1. По реквизиту организации (если есть в проекте)
    Попытка
        КассаОсн = ОрганизацияСсылка.А_ОсновнаяКасса;
        Если ЗначениеЗаполнено(КассаОсн) Тогда
            Возврат КассаОсн;
        КонецЕсли;
    Исключение
    КонецПопытки;

    // 2. Первая активная касса организации
    Запрос = Новый Запрос;
    Запрос.Текст =
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |	К.Ссылка КАК Ссылка
    |ИЗ Справочник.Кассы КАК К
    |ГДЕ К.Владелец = &Орг И НЕ К.ПометкаУдаления
    |УПОРЯДОЧИТЬ ПО К.Наименование";
    Запрос.УстановитьПараметр("Орг", ОрганизацияСсылка);
    Рез = Запрос.Выполнить();
    Если НЕ Рез.Пустой() Тогда
        Выб = Рез.Выбрать(); Выб.Следующий();
        Возврат Выб.Ссылка;
    КонецЕсли;

    // 3. По наименованию "Касса ЗП"
    КассаЗП = Справочники.Кассы.НайтиПоНаименованию("Касса ЗП", Истина);
    Если ЗначениеЗаполнено(КассаЗП) Тогда
        Возврат КассаЗП;
    КонецЕсли;

    // 4. Fallback — пусто
    Возврат Справочники.Кассы.ПустаяСсылка();

КонецФункции

#КонецОбласти
```

- [ ] **Step 3: Проверить синтаксис BSL вручную (визуально)**

```
Read: Documents/РаспределениеФ2/Ext/ObjectModule.bsl
```

Убедиться, что:
- Существующий `Процедура ОбработкаПроведения` не повреждён
- Новая область добавлена ПОСЛЕ него
- Все `КонецФункции` сбалансированы
- `#КонецОбласти` закрывает `#Область`

- [ ] **Step 4: Commit утилитных функций**

```bash
git add Documents/РаспределениеФ2/Ext/ObjectModule.bsl
git commit -m "$(cat <<'EOF'
feat(vedomost-vkassu-f2): 3 утилиты _ВЫК_* в РаспределениеФ2

- _ВЫК_ПостроитьКешСотрудникФЛ — Сотр→ФЛ через 1 запрос
- _ВЫК_ПостроитьКешПодрОрг — СтрПредпр→ПодрОрг через А_Привилегированный
- _ВЫК_ОпределитьКассу — 4-уровневый fallback

Главная процедура СоздатьВедомостьНаВыплатуВКассуПоРаспределениюФ2 —
следующим коммитом.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: BSL — главная процедура `СоздатьВедомостьНаВыплатуВКассуПоРаспределениюФ2`

**Цель:** Добавить экспортную процедуру внутрь той же области `#Область ЕРП_СоздатьВедомостьНаВыплатуВКассу` (перед `#КонецОбласти`). Использует утилиты из Task 4.

**Files:**
- Modify: `Documents/РаспределениеФ2/Ext/ObjectModule.bsl` (вставка ПЕРЕД `#КонецОбласти` из Task 4)

- [ ] **Step 1: Edit BSL — вставить главную процедуру**

Использовать Edit с `old_string = "#КонецОбласти"` (из секции добавленной в Task 4 — последний `#КонецОбласти` в файле). Заменить на главную процедуру + `#КонецОбласти`:

```bsl

// Главная экспортная процедура: создаёт/обновляет ВедомостьНаВыплатуЗарплатыВКассу
// на основании текущего документа РаспределениеФ2.
// Паттерн: Find-Or-Create + Cleanup + Заполнение + Запись.Проведение.
// Образец: Documents/А_ОтгрузкаПродукции/Ext/ObjectModule.bsl процедура
// СоздатьДокументОказанияУслугМеждуПодразделениямиПоОтгрузкаПродукции.
Процедура СоздатьВедомостьНаВыплатуВКассуПоРаспределениюФ2() Экспорт

    // === 1. Find existing ведомость по обратной ссылке ===
    ЗапросСущ = Новый Запрос;
    ЗапросСущ.УстановитьПараметр("Основание", Ссылка);
    ЗапросСущ.Текст =
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |	Д.Ссылка КАК Ссылка,
    |	Д.Проведен КАК Проведен,
    |	Д.ПометкаУдаления КАК ПометкаУдаления
    |ИЗ Документ.ВедомостьНаВыплатуЗарплатыВКассу КАК Д
    |ГДЕ Д.А_РаспределениеФ2 = &Основание";
    Результат = ЗапросСущ.Выполнить();

    // === 2. Cleanup существующей: распровести + снять пометку + очистить ТЧ ===
    Если НЕ Результат.Пустой() Тогда
        Выб = Результат.Выбрать(); Выб.Следующий();
        ВыкВКассу = Выб.Ссылка.ПолучитьОбъект();
        Если Выб.ПометкаУдаления Тогда
            ВыкВКассу.УстановитьПометкуУдаления(Ложь);
        КонецЕсли;
        Если Выб.Проведен Тогда
            Попытка
                ВыкВКассу.Записать(РежимЗаписиДокумента.ОтменаПроведения);
            Исключение
                Сообщить("Не удалось распровести существующую ВКассу: " + ОписаниеОшибки());
                Возврат;
            КонецПопытки;
        КонецЕсли;
        ВыкВКассу.Зарплата.Очистить();
        ВыкВКассу.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Очистить();
        ВыкВКассу.Состав.Очистить();
    Иначе
        ВыкВКассу = Документы.ВедомостьНаВыплатуЗарплатыВКассу.СоздатьДокумент();
    КонецЕсли;

    // === 3. Guard: документ Ф2 должен быть проведён ===
    Если НЕ Проведен Тогда
        Сообщить("РаспределениеФ2 не проведено — ВКассу не создаётся.");
        Возврат;
    КонецЕсли;

    // === 4. Шапка ведомости ===
    ПодразделениеЦО = Справочники.ПодразделенияОрганизаций.НайтиПоНаименованию("ЦО", Истина);
    Если НЕ ЗначениеЗаполнено(ПодразделениеЦО) Тогда
        Сообщить("ВНИМАНИЕ: подразделение 'ЦО' не найдено — шапка останется пустой.");
        ПодразделениеЦО = Справочники.ПодразделенияОрганизаций.ПустаяСсылка();
    КонецЕсли;

    КассаЗП = _ВЫК_ОпределитьКассу(Организация);
    Если НЕ ЗначениеЗаполнено(КассаЗП) Тогда
        Сообщить("ВНИМАНИЕ: касса для ВКассу не определена — поле останется пустым.");
    КонецЕсли;

    СпособЧерезКассу = Справочники.СпособыВыплатыЗарплаты.НайтиПоНаименованию("Через кассу", Истина);

    ВыкВКассу.Дата                     = Дата;
    ВыкВКассу.ПериодРегистрации        = НачалоМесяца(КассаДатаС);
    ВыкВКассу.Организация              = Организация;
    ВыкВКассу.Подразделение            = ПодразделениеЦО;
    ВыкВКассу.Касса                    = КассаЗП;
    ВыкВКассу.СпособВыплаты            = СпособЧерезКассу;
    ВыкВКассу.ПорядокЗаполненияНалогов = Перечисления.ПорядокЗаполненияНалогов.БезНалогов;
    ВыкВКассу.А_РаспределениеФ2        = Ссылка;
    ВыкВКассу.Ответственный            = Ответственный;
    ВыкВКассу.Комментарий              = "Сформировано из РаспределениеФ2 №" + Номер
                                       + " от " + Формат(Дата, "ДЛФ=DD")
                                       + " (Направление: " + Строка(Направление) + ")";

    // === 5. Кеши ===
    МасСотр = Новый Массив;
    Для Каждого СтрС Из Сотрудники Цикл
        Если ЗначениеЗаполнено(СтрС.Сотрудник) И МасСотр.Найти(СтрС.Сотрудник) = Неопределено Тогда
            МасСотр.Добавить(СтрС.Сотрудник);
        КонецЕсли;
    КонецЦикла;
    Для Каждого СтрР Из Распределение Цикл
        Если ЗначениеЗаполнено(СтрР.Сотрудник) И МасСотр.Найти(СтрР.Сотрудник) = Неопределено Тогда
            МасСотр.Добавить(СтрР.Сотрудник);
        КонецЕсли;
    КонецЦикла;

    МасПодр = Новый Массив;
    Для Каждого СтрР Из Распределение Цикл
        Если ЗначениеЗаполнено(СтрР.Подразделение) И МасПодр.Найти(СтрР.Подразделение) = Неопределено Тогда
            МасПодр.Добавить(СтрР.Подразделение);
        КонецЕсли;
    КонецЦикла;

    КешФЛ      = _ВЫК_ПостроитьКешСотрудникФЛ(МасСотр);
    КешПодрОрг = _ВЫК_ПостроитьКешПодрОрг(МасПодр);

    // === 6. ТЧ Зарплата ← ТЧ Сотрудники Ф2 ===
    Для Каждого СтрС Из Сотрудники Цикл
        Если НЕ ЗначениеЗаполнено(СтрС.Сотрудник) Тогда Продолжить КонецЕсли;
        Если СтрС.СуммаНачисления = 0 Тогда Продолжить КонецЕсли;

        ФЛ = КешФЛ[СтрС.Сотрудник];
        Если НЕ ЗначениеЗаполнено(ФЛ) Тогда
            Сообщить("Пропуск ТЧ Зарплата: для " + Строка(СтрС.Сотрудник)
                + " не найдено ФизЛицо (ИНН=" + СтрС.ИНН + ").");
            Продолжить;
        КонецЕсли;

        НовСтр = ВыкВКассу.Зарплата.Добавить();
        НовСтр.Сотрудник            = СтрС.Сотрудник;
        НовСтр.ФизическоеЛицо       = ФЛ;
        НовСтр.Подразделение        = ПодразделениеЦО;
        НовСтр.ПериодВзаиморасчетов = НачалоМесяца(КассаДатаС);
        НовСтр.КВыплате             = СтрС.СуммаНачисления;
        НовСтр.ИдентификаторСтроки  = Новый УникальныйИдентификатор();
    КонецЦикла;

    // === 7. ТЧ А_РасшифровкаВыплатыЗарплатаПоФизлицам ← ТЧ Распределение Ф2 ===
    Для Каждого СтрР Из Распределение Цикл
        Если СтрР.СуммаНачисления = 0 Тогда Продолжить КонецЕсли;
        Если НЕ ЗначениеЗаполнено(СтрР.Сотрудник) Тогда Продолжить КонецЕсли;

        ФЛ = КешФЛ[СтрР.Сотрудник];
        ПодрОрг = КешПодрОрг[СтрР.Подразделение];

        // Статья ДДС из реквизита сотрудника (как делает ОбработкаПроведения Ф2 для движений БДДС)
        СтатьяДДС = СтрР.Сотрудник.СтатьяЗарплата;
        Если НЕ ЗначениеЗаполнено(СтатьяДДС) Тогда
            Попытка
                СтатьяДДС = СтрР.Сотрудник.Родитель.СтатьяЗарплата;
            Исключение
                СтатьяДДС = Справочники.СтатьиДвиженияДенежныхСредств.ПустаяСсылка();
            КонецПопытки;
        КонецЕсли;

        НовСтр = ВыкВКассу.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Добавить();
        НовСтр.ФизическоеЛицо                = ФЛ;
        НовСтр.Сотрудник                     = СтрР.Сотрудник;
        НовСтр.Подразделение                 = ПодрОрг;
        НовСтр.НаправлениеДеятельности       = ?(ЗначениеЗаполнено(СтрР.Направление), СтрР.Направление, Направление);
        НовСтр.СтатьяДвиженияДенежныхСредств = СтатьяДДС;
        НовСтр.ОтработаноЧасов               = СтрР.ОтработаноЧасов;
        НовСтр.Сумма                         = СтрР.СуммаНачисления;
        НовСтр.ИдентификаторСтроки           = Новый УникальныйИдентификатор();
    КонецЦикла;

    // Свёртка дубликатов ключа
    ВыкВКассу.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Свернуть(
        "ФизическоеЛицо, Сотрудник, Подразделение, НаправлениеДеятельности, СтатьяДвиженияДенежныхСредств",
        "ОтработаноЧасов, Сумма");

    // === 8. ТЧ Состав ← уникальные ФЛ из Зарплата ===
    НабФЛ = Новый Соответствие;
    Для Каждого СтрЗ Из ВыкВКассу.Зарплата Цикл
        Если ЗначениеЗаполнено(СтрЗ.ФизическоеЛицо) Тогда
            НабФЛ.Вставить(СтрЗ.ФизическоеЛицо, Истина);
        КонецЕсли;
    КонецЦикла;
    Для Каждого КЗ Из НабФЛ Цикл
        НовСтр = ВыкВКассу.Состав.Добавить();
        НовСтр.ФизическоеЛицо      = КЗ.Ключ;
        НовСтр.ИдентификаторСтроки = Новый УникальныйИдентификатор();
    КонецЦикла;

    // === 9. Сумма по документу ===
    ВыкВКассу.СуммаПоДокументу = ВыкВКассу.Зарплата.Итог("КВыплате");

    // === 10. Запись с проведением + fallback на запись без проведения ===
    Попытка
        ВыкВКассу.Записать(РежимЗаписиДокумента.Проведение);
    Исключение
        Сообщить("Ошибка проведения ВКассу: " + ОписаниеОшибки() + " — пробую без проведения.");
        Попытка
            ВыкВКассу.Записать(РежимЗаписиДокумента.Запись);
        Исключение
            Сообщить("Не удалось сохранить ВКассу: " + ОписаниеОшибки());
            Возврат;
        КонецПопытки;
    КонецПопытки;

    // === 11. Обратная ссылка в шапку Ф2 (без рекурсии) ===
    Если А_ВедомостьВКассу <> ВыкВКассу.Ссылка Тогда
        ОбъектДляЗаписи = Ссылка.ПолучитьОбъект();
        ОбъектДляЗаписи.А_ВедомостьВКассу = ВыкВКассу.Ссылка;
        ОбъектДляЗаписи.ОбменДанными.Загрузка = Истина;
        ОбъектДляЗаписи.Записать();
    КонецЕсли;

    Сообщить("ВКассу создана/обновлена: " + Строка(ВыкВКассу.Ссылка)
        + " | Зарплата=" + ВыкВКассу.Зарплата.Количество()
        + " | Расшифровка=" + ВыкВКассу.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Количество()
        + " | Состав=" + ВыкВКассу.Состав.Количество()
        + " | Σ=" + Формат(ВыкВКассу.СуммаПоДокументу, "ЧДЦ=2"));

КонецПроцедуры

#КонецОбласти
```

- [ ] **Step 2: Прочитать ObjectModule.bsl целиком и проверить структуру**

```
Read: Documents/РаспределениеФ2/Ext/ObjectModule.bsl
```

Проверить:
- Существующий `Процедура ОбработкаПроведения` на месте
- Открыта область `#Область ЕРП_СоздатьВедомостьНаВыплатуВКассу`
- 3 утиля из Task 4 + 1 главная процедура
- `#КонецОбласти` закрывает её
- Все `КонецФункции` / `КонецПроцедуры` сбалансированы

- [ ] **Step 3: Commit главной процедуры**

```bash
git add Documents/РаспределениеФ2/Ext/ObjectModule.bsl
git commit -m "$(cat <<'EOF'
feat(vedomost-vkassu-f2): главная процедура СоздатьВедомостьНаВыплатуВКассуПоРаспределениюФ2

Find-Or-Create по А_РаспределениеФ2 + cleanup + 11 шагов:
1. Find existing
2. Cleanup (распровести+очистить 3 ТЧ)
3. Guard (Проведен)
4. Шапка (Подразделение=ЦО, Касса, СпособВыплаты, А_РаспределениеФ2)
5. Кеши Сотр→ФЛ + СтрПредпр→ПодрОрг
6. ТЧ Зарплата ← ТЧ Сотрудники Ф2
7. ТЧ А_Расшифровка ← ТЧ Распределение Ф2 + Свернуть
8. ТЧ Состав ← уникальные ФЛ
9. СуммаПоДокументу = Σ Зарплата.КВыплате
10. Запись.Проведение + fallback Записать
11. Обратная ссылка Ф2.А_ВедомостьВКассу (через ОбменДанными.Загрузка)

Образец-паттерн: А_ОтгрузкаПродукции.СоздатьДокументОказанияУслугМеждуПодразделениямиПоОтгрузкаПродукции.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: BSL — вызов в `ПриЗаписи` (автотриггер)

**Цель:** Добавить процедуру `ПриЗаписи` в РаспределениеФ2, которая автоматически вызывает создание ведомости при каждом проведении/перезаписи. С защитой от рекурсии через `ОбменДанными.Загрузка`.

**Files:**
- Modify: `Documents/РаспределениеФ2/Ext/ObjectModule.bsl` (добавление после `КонецПроцедуры` ОбработкаПроведения, ПЕРЕД областью `#Область ЕРП_СоздатьВедомостьНаВыплатуВКассу`)

- [ ] **Step 1: Edit BSL — добавить ПриЗаписи**

Использовать Edit. `old_string = "КонецПроцедуры\n\n#Область ЕРП_СоздатьВедомостьНаВыплатуВКассу"` (после ОбработкаПроведения, перед областью утилит).

Заменить на:

```bsl
КонецПроцедуры

Процедура ПриЗаписи(Отказ)

    // Защита от рекурсии при импорте через ConvertERP или при записи обратной ссылки
    Если ОбменДанными.Загрузка Тогда
        Возврат;
    КонецЕсли;

    // Триггер: создать/обновить ведомость в кассу
    // Не блокируем Отказ при ошибке создания ведомости — пишем в Сообщить
    Попытка
        СоздатьВедомостьНаВыплатуВКассуПоРаспределениюФ2();
    Исключение
        Сообщить("ПриЗаписи: ошибка создания ВКассу: " + ОписаниеОшибки());
    КонецПопытки;

КонецПроцедуры

#Область ЕРП_СоздатьВедомостьНаВыплатуВКассу
```

- [ ] **Step 2: Visual check структуры**

```
Read: Documents/РаспределениеФ2/Ext/ObjectModule.bsl
```

Ожидаемая структура:
1. `Процедура ОбработкаПроведения(...)` (стаб)
2. `Процедура ПриЗаписи(Отказ)` (новая, с защитой от рекурсии + Try-Except)
3. `#Область ЕРП_СоздатьВедомостьНаВыплатуВКассу`
4. 3 утиля `_ВЫК_*`
5. Главная `Процедура СоздатьВедомостьНаВыплатуВКассуПоРаспределениюФ2() Экспорт`
6. `#КонецОбласти`

- [ ] **Step 3: Commit**

```bash
git add Documents/РаспределениеФ2/Ext/ObjectModule.bsl
git commit -m "$(cat <<'EOF'
feat(vedomost-vkassu-f2): автотриггер ПриЗаписи РаспределениеФ2

Каждое проведение Ф2 → пересоздаёт/обновляет связанную ВКассу.
Защита от рекурсии: ОбменДанными.Загрузка=Истина → возврат.
Try-Except: ошибка создания ВКассу не блокирует запись Ф2.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: Форма Ф2 — кнопка `Создать ведомость в кассу`

**Цель:** Добавить команду формы с id=3 и кнопку, которая записывает + проводит Ф2 и вызывает процедуру.

**Files:**
- Modify: `Documents/РаспределениеФ2/Forms/ФормаДокумента/Ext/Form.xml`
- Modify: `Documents/РаспределениеФ2/Forms/ФормаДокумента/Ext/Form/Module.bsl`

- [ ] **Step 1: Добавить Command в Form.xml**

Edit Form.xml. `old_string` = последний `</Command>` перед `</Commands>` (см. строки 591-593 = `<Action>Распределить</Action>\n\t\t\t<CurrentRowUse>DontUse</CurrentRowUse>\n\t\t</Command>`):

```xml
		</Command>
		<Command name="СоздатьВедомостьВКассу" id="3">
			<Title>
				<v8:item>
					<v8:lang>ru</v8:lang>
					<v8:content>Создать ведомость в кассу</v8:content>
				</v8:item>
			</Title>
			<ToolTip>
				<v8:item>
					<v8:lang>ru</v8:lang>
					<v8:content>Создать или обновить ВедомостьНаВыплатуЗарплатыВКассу на основании этого РаспределениеФ2</v8:content>
				</v8:item>
			</ToolTip>
			<Action>СоздатьВедомостьВКассу</Action>
			<CurrentRowUse>DontUse</CurrentRowUse>
		</Command>
	</Commands>
```

⚠️ `<Action>СоздатьВедомостьВКассу</Action>` ссылается на процедуру в Module.bsl формы — её добавим в Step 4.

- [ ] **Step 2: Добавить кнопку в командную панель формы**

Прочитать Form.xml — найти основную командную панель (обычно `<ChildItems>` с `<Button>` для существующих команд id=1 и id=2). Например, в районе строк 30-167 (родная панель формы).

⚠️ **Эта часть требует визуальной разведки.** Проверить место добавления:

```
Grep pattern: "CommandName>ПолучитьДанныеИзБухгалтерии\|CommandName>Распределить" path: "Documents/РаспределениеФ2/Forms/ФормаДокумента/Ext/Form.xml" output_mode: "content" -n: true -B: 3 -A: 10
```

Найти существующие `<Button>` для команд id=1/id=2. После последней из них добавить:

```xml
		<Button name="ФормаСоздатьВедомостьВКассу">
			<DataPath xsi:type="form:CommandItemRef">
				<Segments>Form.Command.СоздатьВедомостьВКассу</Segments>
			</DataPath>
			<Type>CommandBarButton</Type>
			<CommandName>Form.Command.СоздатьВедомостьВКассу</CommandName>
			<Representation>Auto</Representation>
			<DefaultButton>false</DefaultButton>
			<OnlyInAllActions>Auto</OnlyInAllActions>
			<Shape>NoShape</Shape>
			<ShapeRepresentation>Auto</ShapeRepresentation>
		</Button>
```

⚠️ Если структура XML отличается — использовать Skill `form-edit`:

```
Skill: form-edit
args: добавь в форму Documents/РаспределениеФ2/Forms/ФормаДокумента команду СоздатьВедомостьВКассу (id=3) с кнопкой в основной командной панели, обработчик команды через Action=СоздатьВедомостьВКассу. Заголовок "Создать ведомость в кассу".
```

- [ ] **Step 3: Прочитать текущий Module.bsl формы**

```
Read: Documents/РаспределениеФ2/Forms/ФормаДокумента/Ext/Form/Module.bsl
```

Запомнить размер файла и проверить нет ли уже процедуры `СоздатьВедомостьВКассу`.

- [ ] **Step 4: Добавить обработчик команды в конец Module.bsl**

Использовать Edit или Write. Добавить в конец файла:

```bsl

&НаКлиенте
Процедура СоздатьВедомостьВКассу(Команда)

    // Защита от незаписанного документа
    Если Объект.Ссылка.Пустая() Тогда
        ПоказатьПредупреждение(, "Сначала запишите документ.");
        Возврат;
    КонецЕсли;

    // Записать (если есть изменения) + провести
    Записать();
    Если Модифицированность Тогда
        Возврат;
    КонецЕсли;

    Если НЕ Объект.Проведен Тогда
        ПоказатьПредупреждение(, "Документ должен быть проведён для создания ведомости.");
        Возврат;
    КонецЕсли;

    СоздатьВедомостьВКассуНаСервере();

КонецПроцедуры

&НаСервере
Процедура СоздатьВедомостьВКассуНаСервере()
    ДокОбъект = РеквизитФормыВЗначение("Объект");
    ДокОбъект.СоздатьВедомостьНаВыплатуВКассуПоРаспределениюФ2();
    ЗначениеВРеквизитФормы(ДокОбъект, "Объект");
КонецПроцедуры
```

- [ ] **Step 5: Commit формы**

```bash
git add Documents/РаспределениеФ2/Forms/ФормаДокумента/Ext/Form.xml
git add Documents/РаспределениеФ2/Forms/ФормаДокумента/Ext/Form/Module.bsl
git commit -m "$(cat <<'EOF'
feat(vedomost-vkassu-f2): кнопка 'Создать ведомость в кассу' в форме Ф2

Command id=3 + кнопка в командной панели + обработчик
&НаКлиенте/&НаСервере с проверкой проведённости документа.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: Доставка XML+BSL #2 в работающую базу

**Цель:** Загрузить РаспределениеФ2 (Ext + Forms) в работающую базу.

**Files:**
- (нет правок) — только команды доставки

- [ ] **Step 1: Partial load XML+BSL**

```
Skill: db-load-xml
args: -Mode Partial -Files "Documents/РаспределениеФ2/Ext/ObjectModule.bsl,Documents/РаспределениеФ2/Forms/ФормаДокумента/Ext/Form.xml,Documents/РаспределениеФ2/Forms/ФормаДокумента/Ext/Form/Module.bsl"
```

⚠️ Если skill хочет XML РаспределениеФ2.xml целиком — указать без подпапок. Альтернатива:

```
Skill: db-load-xml
args: -Mode Partial -Files "Documents/РаспределениеФ2.xml,Documents/РаспределениеФ2"
```

- [ ] **Step 2: Dynamic update**

```
Skill: db-update
args: -Dynamic+
```

- [ ] **Step 3: Smoke-проверка — открыть РаспределениеФ2 №000000026 через Designer/Enterprise**

Опционально: открыть Designer, найти РаспределениеФ2 №000000026, убедиться:
- В форме появилась кнопка «Создать ведомость в кассу»
- Реквизит `А_ВедомостьВКассу` пустой (если ещё не нажимали)

Можно пропустить если есть скрипты Step 4.

- [ ] **Step 4: Commit**

```bash
git commit --allow-empty -m "$(cat <<'EOF'
chore(vedomost-vkassu-f2): db-load-xml + db-update — BSL + форма доставлены

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: Python COM smoke-тест

**Цель:** Через COM перепровести Ф2 №000000026 → проверить что создалась ведомость + инвариант сумм.

**Files:**
- Create: `_Rarzrabotki/Python/test/test_create_vedomost_vkassu_iz_f2_smoke.py`

- [ ] **Step 1: Создать smoke-тест**

```python
"""
Smoke: перепровести РаспределениеФ2 №000000026 через COM →
проверить что создалась ВКассу с правильным А_РаспределениеФ2 и Σ=348800.

Запуск: C:\\Python313\\python.exe test_create_vedomost_vkassu_iz_f2_smoke.py
"""
import io
import sys

sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding="utf-8")

import win32com.client

CONN_ERP = 'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"'
EXPECTED_SUM = 348800.00
EXPECTED_SOTR_COUNT = 19
EXPECTED_RASP_COUNT = 2


def main():
    v8 = win32com.client.Dispatch("V83.COMConnector")
    erp = v8.Connect(CONN_ERP)

    # 1. Найти эталон Ф2
    q = erp.NewObject("Запрос")
    q.Текст = """
ВЫБРАТЬ ПЕРВЫЕ 1
    Док.Ссылка КАК Ссылка
ИЗ Документ.РаспределениеФ2 КАК Док
ГДЕ Док.Номер = "000000026" И Док.Проведен
"""
    rs = q.Выполнить()
    if rs.Пустой():
        print("FAIL: РаспределениеФ2 №000000026 не найден")
        sys.exit(1)

    sel = rs.Выбрать(); sel.Следующий()
    f2_ref = sel.Ссылка
    print(f"Ф2: {f2_ref}")

    # 2. Перепровести Ф2 → ПриЗаписи триггерит создание ВКассу
    obj = f2_ref.ПолучитьОбъект()
    try:
        obj.Записать(erp.РежимЗаписиДокумента.Проведение)
        print("OK: РаспределениеФ2 перепроведено → ПриЗаписи отработал")
    except Exception as e:
        info = e.excepinfo[2] if hasattr(e, "excepinfo") and e.excepinfo else str(e)
        print(f"FAIL: перепроведение Ф2: {info}")
        sys.exit(1)

    # 3. Перечитать Ф2 (обратная ссылка должна обновиться)
    f2_re = erp.Документы.РаспределениеФ2.ПолучитьСсылку(f2_ref.УникальныйИдентификатор())
    f2_obj = f2_re.ПолучитьОбъект()
    if not f2_obj.А_ВедомостьВКассу or str(f2_obj.А_ВедомостьВКассу) == "":
        print("FAIL: Ф2.А_ВедомостьВКассу не заполнена после перепроведения")
        sys.exit(1)

    vk_ref = f2_obj.А_ВедомостьВКассу
    print(f"ВКассу: {vk_ref}")

    # 4. Проверить ВКассу
    vk = vk_ref.ПолучитьОбъект()

    # Шапка
    assert str(vk.Подразделение) == "ЦО", f"Подразделение должно быть 'ЦО', а получили {vk.Подразделение}"
    print(f"  Шапка.Подразделение = {vk.Подразделение} ✓")

    assert str(vk.А_РаспределениеФ2) == str(f2_ref), "А_РаспределениеФ2 не совпадает с Ф2"
    print(f"  Шапка.А_РаспределениеФ2 = {vk.А_РаспределениеФ2} ✓")

    # ТЧ Зарплата
    cnt_zp = vk.Зарплата.Количество()
    sum_zp = 0
    for i in range(cnt_zp):
        sum_zp += float(vk.Зарплата.Получить(i).КВыплате)
    print(f"  Зарплата: {cnt_zp} строк, Σ={sum_zp:,.2f}")
    assert cnt_zp == EXPECTED_SOTR_COUNT, f"Ожидалось {EXPECTED_SOTR_COUNT} строк Зарплата, получили {cnt_zp}"
    assert abs(sum_zp - EXPECTED_SUM) < 0.01, f"Σ Зарплата = {sum_zp}, ожидалось {EXPECTED_SUM}"

    # ТЧ Расшифровка
    cnt_rs = vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Количество()
    print(f"  А_Расшифровка: {cnt_rs} строк")
    # Не делаем assert на конкретное число — может быть свёрнуто
    assert cnt_rs > 0, "ТЧ А_Расшифровка пуста"

    # ТЧ Состав
    cnt_sost = vk.Состав.Количество()
    print(f"  Состав: {cnt_sost} строк (уникальные ФЛ)")
    assert cnt_sost == EXPECTED_SOTR_COUNT, f"Состав должен иметь {EXPECTED_SOTR_COUNT} ФЛ"

    # СуммаПоДокументу
    assert abs(float(vk.СуммаПоДокументу) - EXPECTED_SUM) < 0.01, \
        f"СуммаПоДокументу = {vk.СуммаПоДокументу}, ожидалось {EXPECTED_SUM}"
    print(f"  СуммаПоДокументу = {vk.СуммаПоДокументу} ✓")

    # Проведена
    assert vk.Проведен, "ВКассу не проведена"
    print(f"  Проведена ✓")

    print(f"\nPASS: smoke test пройден")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Запустить тест**

```
mcp__python-runner__run_command(command='C:\\Python313\\python.exe "C:\\Configuration_downloads\\BASERP25\\_Rarzrabotki\\Python\\test\\test_create_vedomost_vkassu_iz_f2_smoke.py"')
```

Expected (последние строки):
```
PASS: smoke test пройден
```

При FAIL — диагностировать через `Сообщить()` вывод (он в COM не виден — придётся читать `debug_a_otrazhenie.txt` если процедура его пишет, или добавить логирование).

- [ ] **Step 3: Commit**

```bash
git add _Rarzrabotki/Python/test/test_create_vedomost_vkassu_iz_f2_smoke.py
git commit -m "$(cat <<'EOF'
test(vedomost-vkassu-f2): smoke — перепровести Ф2 №000000026 → ВКассу Σ=348800

Проверки:
- Ф2.А_ВедомостьВКассу заполнена
- ВКассу.Подразделение = ЦО
- ВКассу.А_РаспределениеФ2 = Ф2
- ТЧ Зарплата = 19 строк, Σ КВыплате = 348800
- ТЧ Расшифровка не пуста
- ТЧ Состав = 19 уникальных ФЛ
- СуммаПоДокументу = 348800
- Проведена = Истина

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: Python COM idempotency-тест

**Цель:** Повторный вызов = тот же UUID, те же ТЧ, та же сумма. Не создаются дубли.

**Files:**
- Create: `_Rarzrabotki/Python/test/test_create_vedomost_vkassu_iz_f2_idempotency.py`

- [ ] **Step 1: Создать тест**

```python
"""
Idempotency: дважды перепровести Ф2 → проверить что UUID ВКассу не меняется
и в базе не появляется второй документ.
"""
import io
import sys

sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding="utf-8")

import win32com.client

CONN_ERP = 'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"'


def main():
    v8 = win32com.client.Dispatch("V83.COMConnector")
    erp = v8.Connect(CONN_ERP)

    q = erp.NewObject("Запрос")
    q.Текст = '''ВЫБРАТЬ ПЕРВЫЕ 1 Док.Ссылка КАК Ссылка
ИЗ Документ.РаспределениеФ2 КАК Док
ГДЕ Док.Номер = "000000026" И Док.Проведен'''
    rs = q.Выполнить()
    if rs.Пустой():
        print("FAIL: Ф2 №000000026 не найден")
        sys.exit(1)
    sel = rs.Выбрать(); sel.Следующий()
    f2_ref = sel.Ссылка

    # Первое перепроведение
    obj = f2_ref.ПолучитьОбъект()
    obj.Записать(erp.РежимЗаписиДокумента.Проведение)
    f2_obj = f2_ref.ПолучитьОбъект()
    vk_uid_1 = erp.string(f2_obj.А_ВедомостьВКассу.УникальныйИдентификатор())
    vk_sum_1 = float(f2_obj.А_ВедомостьВКассу.СуммаПоДокументу)
    print(f"Первый прогон: UID = {vk_uid_1}, Σ = {vk_sum_1:,.2f}")

    # Второе перепроведение
    obj2 = f2_ref.ПолучитьОбъект()
    obj2.Записать(erp.РежимЗаписиДокумента.Проведение)
    f2_obj2 = f2_ref.ПолучитьОбъект()
    vk_uid_2 = erp.string(f2_obj2.А_ВедомостьВКассу.УникальныйИдентификатор())
    vk_sum_2 = float(f2_obj2.А_ВедомостьВКассу.СуммаПоДокументу)
    print(f"Второй прогон: UID = {vk_uid_2}, Σ = {vk_sum_2:,.2f}")

    # Проверки
    assert vk_uid_1 == vk_uid_2, f"UUID изменился: {vk_uid_1} → {vk_uid_2}"
    assert abs(vk_sum_1 - vk_sum_2) < 0.01, f"Σ изменилась: {vk_sum_1} → {vk_sum_2}"
    print("UUID и Σ совпали ✓")

    # Проверка отсутствия дублей в базе
    qd = erp.NewObject("Запрос")
    qd.Текст = '''ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК Кол
ИЗ Документ.ВедомостьНаВыплатуЗарплатыВКассу КАК В
ГДЕ В.А_РаспределениеФ2 = &Ф2 И НЕ В.ПометкаУдаления'''
    qd.УстановитьПараметр("Ф2", f2_ref)
    rs2 = qd.Выполнить()
    sel2 = rs2.Выбрать(); sel2.Следующий()
    cnt = int(sel2.Кол)
    assert cnt == 1, f"В базе {cnt} ведомостей по этому Ф2 (должна быть 1)"
    print(f"В базе ровно 1 ВКассу для этого Ф2 ✓")

    print("\nPASS: idempotency test пройден")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Запустить тест**

```
mcp__python-runner__run_command(command='C:\\Python313\\python.exe "C:\\Configuration_downloads\\BASERP25\\_Rarzrabotki\\Python\\test\\test_create_vedomost_vkassu_iz_f2_idempotency.py"')
```

Expected: `PASS: idempotency test пройден`

- [ ] **Step 3: Commit**

```bash
git add _Rarzrabotki/Python/test/test_create_vedomost_vkassu_iz_f2_idempotency.py
git commit -m "$(cat <<'EOF'
test(vedomost-vkassu-f2): idempotency — повторное перепроведение Ф2 не плодит дубли

Проверки:
- UUID ВКассу не меняется
- Σ не меняется
- В базе ровно 1 ведомость по этому Ф2

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: Python COM rashifrovka-тест

**Цель:** Проверить корректность заполнения ТЧ А_Расшифровка из ТЧ Распределение Ф2.

**Files:**
- Create: `_Rarzrabotki/Python/test/test_create_vedomost_vkassu_iz_f2_rashifrovka.py`

- [ ] **Step 1: Создать тест**

```python
"""
Rashifrovka: проверить что ТЧ А_РасшифровкаВыплатыЗарплатаПоФизлицам
заполнена корректно по ТЧ Распределение Ф2 (после Свернуть).
"""
import io
import sys

sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding="utf-8")

import win32com.client

CONN_ERP = 'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"'


def main():
    v8 = win32com.client.Dispatch("V83.COMConnector")
    erp = v8.Connect(CONN_ERP)

    # Получаем эталон Ф2
    q = erp.NewObject("Запрос")
    q.Текст = '''ВЫБРАТЬ ПЕРВЫЕ 1 Док.Ссылка КАК Ссылка
ИЗ Документ.РаспределениеФ2 КАК Док
ГДЕ Док.Номер = "000000026" И Док.Проведен'''
    rs = q.Выполнить()
    sel = rs.Выбрать(); sel.Следующий()
    f2_ref = sel.Ссылка
    f2 = f2_ref.ПолучитьОбъект()

    # Перепровести (если не было)
    f2.Записать(erp.РежимЗаписиДокумента.Проведение)
    f2 = f2_ref.ПолучитьОбъект()

    vk = f2.А_ВедомостьВКассу.ПолучитьОбъект()

    # Σ ОтработаноЧасов в А_Расшифровке должна быть = Σ Распределение.ОтработаноЧасов
    sum_h_f2 = sum(float(f2.Распределение.Получить(i).ОтработаноЧасов)
                   for i in range(f2.Распределение.Количество()))
    sum_h_vk = sum(float(vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Получить(i).ОтработаноЧасов)
                   for i in range(vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Количество()))
    print(f"Σ ОтработаноЧасов: Ф2={sum_h_f2}, ВКассу={sum_h_vk}")
    assert abs(sum_h_f2 - sum_h_vk) < 0.01, f"Несовпадение часов: {sum_h_f2} != {sum_h_vk}"

    # Σ Суммы в А_Расшифровке должна быть = Σ Распределение.СуммаНачисления
    sum_s_f2 = sum(float(f2.Распределение.Получить(i).СуммаНачисления)
                   for i in range(f2.Распределение.Количество()))
    sum_s_vk = sum(float(vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Получить(i).Сумма)
                   for i in range(vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Количество()))
    print(f"Σ Сумма: Ф2={sum_s_f2:,.2f}, ВКассу={sum_s_vk:,.2f}")
    assert abs(sum_s_f2 - sum_s_vk) < 0.01, f"Несовпадение сумм: {sum_s_f2} != {sum_s_vk}"

    # У каждой строки Расшифровки должны быть заполнены ФизЛицо, Подр, Статья
    for i in range(vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Количество()):
        r = vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Получить(i)
        assert str(r.ФизическоеЛицо) != "", f"Строка {i}: ФизическоеЛицо пусто"
        assert str(r.Подразделение) != "", f"Строка {i}: Подразделение пусто"
        # Статья может быть пустой если у сотрудника не заполнен реквизит — это warn, не fail
        if str(r.СтатьяДвиженияДенежныхСредств) == "":
            print(f"  WARN: строка {i} ({r.ФизическоеЛицо}): СтатьяДДС пуста")

    print(f"\nPASS: rashifrovka test пройден ({vk.А_РасшифровкаВыплатыЗарплатаПоФизлицам.Количество()} строк)")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Запустить тест**

```
mcp__python-runner__run_command(command='C:\\Python313\\python.exe "C:\\Configuration_downloads\\BASERP25\\_Rarzrabotki\\Python\\test\\test_create_vedomost_vkassu_iz_f2_rashifrovka.py"')
```

Expected: `PASS: rashifrovka test пройден (N строк)`

- [ ] **Step 3: Commit**

```bash
git add _Rarzrabotki/Python/test/test_create_vedomost_vkassu_iz_f2_rashifrovka.py
git commit -m "$(cat <<'EOF'
test(vedomost-vkassu-f2): rashifrovka — Σ часов и сумм совпадают с Ф2.Распределение

Проверки:
- Σ ОтработаноЧасов А_Расшифровка = Σ Ф2.Распределение
- Σ Сумма А_Расшифровка = Σ Ф2.Распределение.СуммаНачисления
- У каждой строки заполнены ФизЛицо + Подразделение

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 12: Knowledge — документация механизма

**Цель:** Зафиксировать реализованную логику в knowledge базе для будущих ИИ-сессий.

**Files:**
- Create: `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/11_vedomost_iz_raspredeleniyaF2.md`
- Modify: `_Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/KNOWLEDGE_MAP.md` (добавить ссылку в порядок чтения)

- [ ] **Step 1: Создать knowledge файл**

```markdown
# 11. ВедомостьНаВыплатуЗарплатыВКассу из РаспределениеФ2

> Реализовано 2026-05-26. Spec: `docs/superpowers/specs/2026-05-26-vedomost-vkassu-iz-raspredeleniyaf2-design.md`. Plan: `docs/superpowers/plans/2026-05-26-vedomost-vkassu-iz-raspredeleniyaf2-plan.md`.

---

## 1. Что было реализовано

1. Реквизит `Документ.ВедомостьНаВыплатуЗарплатыВКассу.А_РаспределениеФ2` (тип `Документ.РаспределениеФ2`) — связь дочка→родитель.
2. Реквизит `Документ.РаспределениеФ2.А_ВедомостьВКассу` (тип `Документ.ВедомостьНаВыплатуЗарплатыВКассу`) — обратная связь.
3. ТЧ `А_РасшифровкаВыплатыЗарплатаПоФизлицам` (8 реквизитов) в `ВедомостьНаВыплатуЗарплатыВКассу`.
4. Область `#Область ЕРП_СоздатьВедомостьНаВыплатуВКассу` в `Documents/РаспределениеФ2/Ext/ObjectModule.bsl` — 3 утиля + 1 экспортная процедура.
5. `Процедура ПриЗаписи` в РаспределениеФ2 — автотриггер с защитой от рекурсии.
6. Команда `СоздатьВедомостьВКассу` (id=3) в форме РаспределениеФ2 + обработчик.

## 2. Как работает

1. Пользователь проводит `Документ.РаспределениеФ2` (вручную или нажимает кнопку «Создать ведомость в кассу»).
2. `ПриЗаписи` Ф2 (если НЕ ОбменДанными.Загрузка) → вызывает `СоздатьВедомостьНаВыплатуВКассуПоРаспределениюФ2()`.
3. Find-Or-Create по `ВКассу.А_РаспределениеФ2 = Ф2.Ссылка`:
   - Если найдена — распровести + очистить 3 ТЧ
   - Если нет — создать новый документ
4. Шапка ведомости: `Подразделение = "ЦО"`, `Касса = Орг.А_ОсновнаяКасса` (или fallback), `СпособВыплаты = "Через кассу"`, `ПорядокНалогов = БезНалогов`.
5. ТЧ `Зарплата` ← ТЧ `Сотрудники` Ф2 (ФизЛицо × Сотрудник × КВыплате).
6. ТЧ `А_Расшифровка` ← ТЧ `Распределение` Ф2 (ФЛ × ПодрОрг × Направление × СтатьяДДС × Часы × Сумма) + `Свернуть`.
7. ТЧ `Состав` ← уникальные ФЛ.
8. `СуммаПоДокументу = Σ Зарплата.КВыплате`.
9. `Записать(Проведение)` + fallback на `Запись`.
10. Обратная ссылка `Ф2.А_ВедомостьВКассу = ВКассу.Ссылка` (через `ОбменДанными.Загрузка=Истина` → НЕ вызывает повторный ПриЗаписи).

## 3. Эталонный документ (для регрессии)

- **`РаспределениеФ2 №000000026`** от 10.12.2025, Σ = 348 800 UAH (19 ФЛ), 2 строки ТЧ Распределение
- РКО Казны на 348 800 — `№0000053046` от 08.12.2025 (поле «Ведомость:» заполняется вручную пользователем после создания нашей ведомости)

## 4. Python COM тесты

| Файл | Назначение |
|---|---|
| `test_create_vedomost_vkassu_iz_f2_preflight.py` | Проверка эталона перед написанием BSL |
| `test_create_vedomost_vkassu_iz_f2_smoke.py` | Создание + инвариант Σ=348800 |
| `test_create_vedomost_vkassu_iz_f2_idempotency.py` | Повторный вызов = тот же UUID |
| `test_create_vedomost_vkassu_iz_f2_rashifrovka.py` | Σ часов и сумм А_Расшифровка = Σ Ф2.Распределение |

## 5. Out-of-scope

- Автосвязь ВКассу с РКО ERP по UUID Казны (вручную пока)
- ВедомостьНаВыплатуЗарплатыВБанк из А_РаспределениеЗаработнойПлаты
- НДФЛ/ВзносыФОТ в ТЧ ведомости

## 6. Cross-references

- Образец-паттерн: `Documents/А_ОтгрузкаПродукции/Ext/ObjectModule.bsl` (188 строк)
- Spec: `docs/superpowers/specs/2026-05-26-vedomost-vkassu-iz-raspredeleniyaf2-design.md`
- Plan: `docs/superpowers/plans/2026-05-26-vedomost-vkassu-iz-raspredeleniyaf2-plan.md`
- [KNOWLEDGE_MAP.md](KNOWLEDGE_MAP.md)
- [08_doc_raspredelenie_zp.md](08_doc_raspredelenie_zp.md) — структура РаспределениеЗП/Ф2
- [09_vedomost_vyplaty.md](09_vedomost_vyplaty.md) — структура ВКассу + регистр ЗарплатаКВыплате
- [10_payment_flow.md](10_payment_flow.md) — общий поток
```

- [ ] **Step 2: Обновить KNOWLEDGE_MAP.md — добавить ссылку в порядок чтения**

Использовать Edit. `old_string`:
```
| 11 | [10_payment_flow.md](10_payment_flow.md) | **🆕** Полный поток выплаты по подразделениям Казна→ERP→Ведомости (план реализации) |
| 12 | [LESSONS.md](LESSONS.md) | 10+ антипаттернов (cross-base UUID, reserved `Ссылка`, ЗначениеВСтрокуВнутр между разными типами и т.д.) |
```

`new_string`:
```
| 11 | [10_payment_flow.md](10_payment_flow.md) | **🆕** Полный поток выплаты по подразделениям Казна→ERP→Ведомости (план реализации) |
| 12 | [11_vedomost_iz_raspredeleniyaF2.md](11_vedomost_iz_raspredeleniyaF2.md) | **🟢 РЕАЛИЗОВАНО** — ВКассу из РаспределениеФ2 (триггер на Ф2, шапка ЦО, новая ТЧ А_Расшифровка) |
| 13 | [LESSONS.md](LESSONS.md) | 10+ антипаттернов (cross-base UUID, reserved `Ссылка`, ЗначениеВСтрокуВнутр между разными типами и т.д.) |
```

- [ ] **Step 3: Commit knowledge**

```bash
git add _Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/11_vedomost_iz_raspredeleniyaF2.md
git add _Rarzrabotki/notebook/knowledge_А_ОтражениеЗПпоКазне/KNOWLEDGE_MAP.md
git commit -m "$(cat <<'EOF'
docs(knowledge_А_ОтражениеЗПпоКазне): файл 11 — ВКассу из Ф2 (реализовано)

+ описание реализации (3 утиля + 1 экспорт процедура + ПриЗаписи)
+ эталон Ф2 №000000026 / РКО №0000053046
+ список Python COM тестов
+ обновлён KNOWLEDGE_MAP.md (порядок чтения)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 13: Ручная сверка с РКО + финальный отчёт

**Цель:** Пользователь вручную сверяет результат с реальным РКО Казны №0000053046 и подставляет ведомость в поле «Ведомость:» РКО.

**Files:**
- (нет правок)

- [ ] **Step 1: Открыть Enterprise → найти Ф2 №000000026 → проверить кнопку**

Ручная проверка:
1. Открыть `Документ.РаспределениеФ2 №000000026` в Enterprise
2. Убедиться что есть кнопка «Создать ведомость в кассу»
3. Нажать — должно появиться сообщение «ВКассу создана/обновлена: ...»
4. Открыть созданную ведомость (через `Ф2.А_ВедомостьВКассу` или через список ведомостей)
5. Проверить:
   - Подразделение = «ЦО»
   - ТЧ Зарплата = 19 строк
   - ТЧ А_Расшифровка = есть строки (2 после Свернуть)
   - СуммаПоДокументу = 348 800

- [ ] **Step 2: Открыть РКО Казны №0000053046 → подставить ведомость**

Ручная проверка:
1. Открыть `Документ.РасходныйКассовыйОрдер №0000053046` (зеркало Казны в ERP)
2. Поле «Ведомость:» — подставить ссылку на созданную ведомость
3. Записать РКО
4. Убедиться что связка работает (открыть РКО → перейти по ведомости → попасть в ВКассу)

- [ ] **Step 3: Финальный git push (если нужен)**

```bash
git log --oneline -15  # просмотреть всю историю изменений
git status            # убедиться что нет несохранённых
# git push origin main  # если требуется
```

- [ ] **Step 4: Финальный отчёт пользователю**

Сводный отчёт:
- Реализовано: ✅ Find-Or-Create ВКассу из Ф2 + кнопка + автотриггер
- Тесты: ✅ 4 Python COM теста (preflight + smoke + idempotency + rashifrovka) — все PASS
- Эталон: Ф2 №000000026 → ВКассу №X на 348 800 UAH
- Out-of-scope (на следующую итерацию):
  - Автосвязь ВКассу ↔ РКО по UUID
  - ВКассу из А_РаспределениеЗаработнойПлаты (безналичная)
  - НДФЛ/Взносы

---

## Self-Review

### 1. Spec coverage

| Раздел spec | Реализовано в task | Статус |
|---|---|---|
| §1 Контекст + бизнес-сценарий | (документация) | — |
| §2 Архитектура (паттерн А_ОтгрузкаПродукции) | Tasks 4-6 | ✅ |
| §3 Структура новой ТЧ (8 полей) | Tasks 2-3 | ✅ |
| §4 Реквизиты-связи (А_РаспределениеФ2, А_ВедомостьВКассу) | сделано пользователем вручную | ✅ |
| §5 Алгоритм процедуры (11 шагов) | Task 5 | ✅ |
| §6 Триггер: кнопка + ПриЗаписи | Tasks 6-7 | ✅ |
| §7 Связь с РКО (вручную) | Task 13 | ✅ |
| §8 Шапка ведомости (ЦО, Касса, СпособВыплаты) | Task 5 | ✅ |
| §9 Acceptance / smoke + инвариант сумм | Tasks 9-10 | ✅ |
| §10 Python COM тесты (3 файла) | Tasks 9-11 | ✅ |
| §11 Out-of-scope | (документировано) | — |
| §12 Риски и митигации | (учтено в Task 5: fallback Касса, проверка ЦО, обработка ошибок) | ✅ |
| §13 План шагов (1-9) | Tasks 1-13 | ✅ |

### 2. Placeholder scan

- Нет «TBD»/«TODO»/«implement later»
- Все шаги имеют код или конкретные команды
- В Task 2 Step 2/3 уточнено: 13 UUID, не 11

### 3. Type consistency

- Имя процедуры везде `СоздатьВедомостьНаВыплатуВКассуПоРаспределениюФ2` (никаких вариантов типа `СоздатьВКассу...`)
- Реквизит везде `А_РаспределениеФ2` (на ВКассу) и `А_ВедомостьВКассу` (на Ф2)
- ТЧ везде `А_РасшифровкаВыплатыЗарплатаПоФизлицам` (полное имя)
- Утилиты везде `_ВЫК_*` (три штуки)
- Команда формы `СоздатьВедомостьВКассу` (id=3) везде совпадает

### 4. Ambiguity

- Task 2 Step 4 — два варианта: skill meta-edit или ручной Edit. Указано предпочтение skill.
- Task 7 Step 2 — структура XML формы может отличаться → указан skill form-edit как fallback.
- Task 8 Step 1 — формат `-Files` для db-load-xml может требовать корректировки.

---

**Plan complete and saved to `docs/superpowers/plans/2026-05-26-vedomost-vkassu-iz-raspredeleniyaf2-plan.md`.**
