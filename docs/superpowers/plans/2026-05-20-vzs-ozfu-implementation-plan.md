# А_ВзаиморасчетыССотрудниками — наполнение из ОтражениеЗарплатыВФинансовомУчете Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Реализовать заполнение нового регистра накопления `А_ВзаиморасчетыССотрудниками` (Орг + ФизЛицо + Подразделение + СуммаВзаиморасчетов, тип Balance) от документа `ОтражениеЗарплатыВФинансовомУчете`. Источник — 3 ТЧ документа. Σ-инвариант per `(Орг, Подр)` = Σ signed `РегистрНакопления.ПрочиеАктивыПассивы(Статья=ОплатаТруда, Источник=ПустаяСсылка)` per Регистратор (этот документ).

**Architecture:**
- Точка интеграции: одна строка вызова в `Documents/ОтражениеЗарплатыВФинансовомУчете/Ext/ObjectModule.bsl` в процедуре `ОбработкаПроведения` (line 197) после типового `ПроведениеДокументов.ОбработкаПроведенияДокумента(...)`.
- Логика: универсальная процедура `ЗаполнитьИзОтраженияЗП(ДокОбъект)` в ManagerModule регистра — заполняет `ДокОбъект.Движения.А_ВзаиморасчетыССотрудниками` запросом из 3 ТЧ.
- Принцип: единый цикл проведения (`Движения.<регистр>.Записывать = Истина` — flush ВМЕСТЕ со всеми другими регистрами в одной транзакции).

**Tech Stack:**
- BAS ERP 2.5 (v2.13), 1С:Предприятие 8.3.20, BSL
- Python 3 + pywin32 (`V83.COMConnector`) — pre-test/verify
- MCP скилы: `/db-load-xml`, `/db-update`, `mcp__python-runner__run_command`
- БД: BaseERP (SQLSERVER), организация ТОВ ІНДАСТРІАЛБУД (КодПоЕДРПОУ 40645273)

**Pre-test PASSED (2026-05-20):** запрос подтверждён на двух жирных документах:
- январь 2026: Σ signed А_ВзСС = **−6 204 232,53** = Σ ПАП(ОТ), `|Δ|=0,00`, 609 строк, 43 подразделения, 100% с ФизЛицом
- февраль 2026: Σ = **−5 550 627,58** = Σ ПАП(ОТ), `|Δ|=0,00`, 620 строк, 46 подразделений, 100% с ФизЛицом

Pre-test файл: `_Rarzrabotki/Python/test/test_vzs_ozfu_pretest.py`

**Известное:** документ ОЗФУ за декабрь 2025 в pre-test НЕ найден (зарплата за дек 2025 обычно отражается ОЗФУ от январь 2026). Task 0 проверит — если за дек нет документа, verify пройдёт только по январю.

**Запрет:** структуру типового регистра `ВзаиморасчетыССотрудниками` и типового документа `ОтражениеЗарплатыВФинансовомУчете` НЕ менять (memory `feedback_no_typical_register_changes`); только добавление 1 строки вызова в ОбработкаПроведения. ManagerModule регистра — наш кастомный А_*, правки разрешены.

---

## Task 0: Найти кандидаты-документы для дек 2025 + янв 2026

**Files:**
- Modify: `_Rarzrabotki/Python/test/test_vzs_ozfu_pretest.py` (или новый baseline-скрипт)

**Цель:** Получить точный список регистраторов ОЗФУ за дек/янв с UUID, чтобы в Task 5 их перепроводить и сверять.

- [ ] **Step 0.1: Создать поисковый скрипт `_Rarzrabotki/Python/test/find_ozfu_dec_jan.py`**

```python
# -*- coding: utf-8 -*-
"""Поиск регистраторов Документ.ОтражениеЗарплатыВФинансовомУчете
за декабрь 2025 и январь 2026 с движениями ПАП(Статья=ОплатаТруда).
Выводит UUID каждого + ожидаемую Σ signed ПАП(ОТ) per документ."""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime

pythoncom.CoInitialize()
conn = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')

q0 = conn.NewObject("Запрос")
q0.Текст = ('ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Справочник.Организации '
            'ГДЕ КодПоЕДРПОУ = "40645273"')
r = q0.Выполнить().Выбрать(); r.Следующий(); ORG = r.С

qOT = conn.NewObject("Запрос")
qOT.Текст = ('ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ '
             'ПланВидовХарактеристик.СтатьиАктивовПассивов '
             'ГДЕ Наименование = "Оплата труда"')
rOT = qOT.Выполнить().Выбрать(); rOT.Следующий(); ОТ = rOT.С

ПЕРИОДЫ = [
    ("декабрь2025", datetime(2025, 12, 1)),
    ("январь2026",  datetime(2026, 1, 1)),
]

print("# Кандидаты для verify (UUID + Σ signed ПАП(ОТ))\n")
найдено = []
for name, мдата in ПЕРИОДЫ:
    qG = conn.NewObject("Запрос")
    qG.Текст = ("ВЫБРАТЬ НАЧАЛОПЕРИОДА(&М,МЕСЯЦ) КАК НМ, "
                "КОНЕЦПЕРИОДА(КОНЕЦПЕРИОДА(&М,МЕСЯЦ),ДЕНЬ) КАК КД")
    qG.УстановитьПараметр("М", мдата)
    rG = qG.Выполнить().Выбрать(); rG.Следующий(); НМ, КД = rG.НМ, rG.КД

    qR = conn.NewObject("Запрос")
    qR.Текст = """
    ВЫБРАТЬ Т.Регистратор КАК Док,
        Т.Регистратор.Номер КАК Номер,
        Т.Регистратор.Дата КАК Дата,
        Т.Регистратор.ПериодРегистрации КАК ПериодРегистрации,
        СУММА(ВЫБОР КОГДА Т.ВидДвижения=ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
                       ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ) КАК Σ_signed
    ИЗ РегистрНакопления.ПрочиеАктивыПассивы КАК Т
    ГДЕ Т.Организация=&Орг И Т.Период МЕЖДУ &НМ И &КД
        И Т.Источник=ЗНАЧЕНИЕ(Перечисление.ИсточникиУправленческогоБаланса.ПустаяСсылка)
        И Т.Статья=&ОТ
        И ТИПЗНАЧЕНИЯ(Т.Регистратор)=ТИП(Документ.ОтражениеЗарплатыВФинансовомУчете)
    СГРУППИРОВАТЬ ПО Т.Регистратор, Т.Регистратор.Номер, Т.Регистратор.Дата,
        Т.Регистратор.ПериодРегистрации
    УПОРЯДОЧИТЬ ПО Т.Регистратор.Дата
    """
    qR.УстановитьПараметр("Орг", ORG); qR.УстановитьПараметр("НМ", НМ)
    qR.УстановитьПараметр("КД", КД); qR.УстановитьПараметр("ОТ", ОТ)
    tR = qR.Выполнить().Выгрузить()
    print(f"## {name} (период {НМ.strftime('%Y-%m-%d')} … {КД.strftime('%Y-%m-%d')})")
    if tR.Количество() == 0:
        print(f"  ⚠️ Документов ОЗФУ за {name} не найдено\n")
    else:
        for i in range(tR.Количество()):
            e = tR.Получить(i)
            uuid = conn.XMLСтрока(e.Док)
            print(f"  - № {e.Номер} от {e.Дата} (ПериодРег {e.ПериодРегистрации}) "
                  f"Σ_signed={float(e.Σ_signed or 0):,.2f} UUID={uuid}")
            найдено.append((name, str(e.Номер), e.Дата, float(e.Σ_signed or 0), uuid))
        print()

# Сохранить кандидатов в JSON для последующего verify
import json, os
out_path = os.path.join(os.path.dirname(__file__), "ozfu_candidates.json")
with open(out_path, "w", encoding="utf-8") as f:
    json.dump([
        {"period": p, "номер": n, "дата": d.isoformat(),
         "Σ_signed_expected": s, "uuid": u}
        for p, n, d, s, u in найдено
    ], f, ensure_ascii=False, indent=2)
print(f"\nСохранено в {out_path}")
```

- [ ] **Step 0.2: Запустить и зафиксировать выводы**

Run:
```
python "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\find_ozfu_dec_jan.py"
```

Expected: список найденных документов с UUID. Сохраняется в `ozfu_candidates.json`.

**Если за декабрь 2025 пусто:** это нормально (зарплата декабря отражается ОЗФУ января). Verify в Task 5 пройдёт только по январю + дополнительно по февралю (которого pre-test нашёл).

- [ ] **Step 0.3: Commit поисковый скрипт + JSON**

```bash
git add _Rarzrabotki/Python/test/find_ozfu_dec_jan.py _Rarzrabotki/Python/test/ozfu_candidates.json
git commit -m "test(vzs): find ОЗФУ candidates for dec 2025 + jan 2026"
```

---

## Task 1: ManagerModule А_ВзаиморасчетыССотрудниками — добавить процедуру

**Files:**
- Modify: `AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl`

- [ ] **Step 1.1: Прочитать текущее содержимое модуля**

Read: `AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl`

Текущее содержимое — это процедура `ЗаполнитьОрганизацию()` в области `#Область ПервоначальноеЗаполнениеИОбновлениеИнформационнойБазы` под обёрткой `#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда ... #КонецЕсли`. Добавляем новую процедуру до `#КонецОбласти СлужебныеПроцедурыИФункции`.

- [ ] **Step 1.2: Вставить процедуру `ЗаполнитьИзОтраженияЗП` ПЕРЕД строкой `#КонецОбласти` (которая закрывает `СлужебныеПроцедурыИФункции`)**

Точное место вставки — между концом области `ПервоначальноеЗаполнениеИОбновлениеИнформационнойБазы` (`#КонецОбласти` после `КонецПроцедуры` ЗаполнитьОрганизацию) и финальным `#КонецОбласти` (СлужебныеПроцедурыИФункции). Создать новую область `ЗаполнениеДвижений` со следующим содержимым:

```bsl

#Область ЗаполнениеДвижений

// Заполняет движения регистра А_ВзаиморасчетыССотрудниками из документа
// Документ.ОтражениеЗарплатыВФинансовомУчете.
//
// Σ-инвариант (proved через test_vzs_ozfu_pretest.py 2026-05-20, Δ=0,00 ₽):
//   Σ signed (Приход − Расход) per (Орг, Подр) per Регистратор =
//   Σ signed ПАП(Статья=ОплатаТруда, Источник=ПустаяСсылка) per Регистратор
//
// Источники (3 ТЧ документа, зеркало 3 блоков типового ManagerModule.bsl
// ОЗФУ.ТекстЗапросаТаблицаПрочиеАктивыПассивы() для статьи ОплатаТруда,
// но с сохранением ФизическогоЛица в SELECT и GROUP BY):
//   1) НачисленнаяЗарплатаИВзносыПоФизлицам где Налог=Пуст
//      И ВидОперации НЕ В 4 исключения → ВидДвижения=Расход
//   2) НачисленныйНДФЛ                                         → ВидДвижения=Приход
//   3) УдержаннаяЗарплата                                      → ВидДвижения=Приход
//
// Знаки 1:1 ПАП: для пассивной статьи ОТ начисление = Расход (рост обязательства),
// НДФЛ/Удержания = Приход (снижение обязательства перед физлицом, переход в
// бюджет/прочие статьи). Σ signed КО регистра воспроизводит ПАП до копейки
// per (Орг, Подр) и автоматически — per Подразделение в agрегации.
//
// Параметры:
//   ДокОбъект - ДокументОбъект.ОтражениеЗарплатыВФинансовомУчете
//
Процедура ЗаполнитьИзОтраженияЗП(ДокОбъект) Экспорт

	// Явно включаем запись регистра в текущем цикле проведения
	ДокОбъект.Движения.А_ВзаиморасчетыССотрудниками.Записывать = Истина;
	ДокОбъект.Движения.А_ВзаиморасчетыССотрудниками.Очистить();

	// Если документ не проведён — записываем пустой набор (очистка движений)
	Если НЕ ДокОбъект.Проведен Тогда
		Возврат;
	КонецЕсли;

	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Док", ДокОбъект.Ссылка);
	Запрос.УстановитьПараметр("Орг", ДокОбъект.Организация);
	Запрос.Текст =
	"ВЫБРАТЬ
	|	Н.Организация КАК Организация,
	|	Н.ПодразделениеПредприятия КАК Подразделение,
	|	Н.ФизическоеЛицо КАК ФизическоеЛицо,
	|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход) КАК ВидДвижения,
	|	СУММА(Н.Сумма) КАК СуммаВзаиморасчетов
	|ИЗ
	|	Документ.ОтражениеЗарплатыВФинансовомУчете.НачисленнаяЗарплатаИВзносыПоФизлицам КАК Н
	|ГДЕ
	|	Н.Ссылка = &Док
	|	И Н.Налог = ЗНАЧЕНИЕ(Справочник.Налоги.ПустаяСсылка)
	|	И Н.ВидОперации НЕ В (
	|		ЗНАЧЕНИЕ(Перечисление.ВидыОперацийПоЗарплате.ЕжегодныйОтпускОценочныеОбязательстваИРезервы),
	|		ЗНАЧЕНИЕ(Перечисление.ВидыОперацийПоЗарплате.НачисленоСдельноДоход),
	|		ЗНАЧЕНИЕ(Перечисление.ВидыОперацийПоЗарплате.КомпенсацияЕжегодногоОтпускаОценочныеОбязательства),
	|		ЗНАЧЕНИЕ(Перечисление.ВидыОперацийПоЗарплате.ЕжегодныйОтпускОценочныеОбязательства))
	|СГРУППИРОВАТЬ ПО
	|	Н.Организация, Н.ПодразделениеПредприятия, Н.ФизическоеЛицо
	|ИМЕЮЩИЕ СУММА(Н.Сумма) <> 0
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	&Орг,
	|	НДФЛ.ПодразделениеПредприятия,
	|	НДФЛ.ФизическоеЛицо,
	|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход),
	|	СУММА(НДФЛ.Сумма)
	|ИЗ
	|	Документ.ОтражениеЗарплатыВФинансовомУчете.НачисленныйНДФЛ КАК НДФЛ
	|ГДЕ
	|	НДФЛ.Ссылка = &Док
	|СГРУППИРОВАТЬ ПО
	|	НДФЛ.ПодразделениеПредприятия, НДФЛ.ФизическоеЛицо
	|ИМЕЮЩИЕ СУММА(НДФЛ.Сумма) <> 0
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	&Орг,
	|	У.ПодразделениеПредприятия,
	|	У.ФизическоеЛицо,
	|	ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход),
	|	СУММА(У.Сумма)
	|ИЗ
	|	Документ.ОтражениеЗарплатыВФинансовомУчете.УдержаннаяЗарплата КАК У
	|ГДЕ
	|	У.Ссылка = &Док
	|СГРУППИРОВАТЬ ПО
	|	У.ПодразделениеПредприятия, У.ФизическоеЛицо
	|ИМЕЮЩИЕ СУММА(У.Сумма) <> 0";

	Результат = Запрос.Выполнить().Выгрузить();

	Для Каждого СтрТЗ Из Результат Цикл
		Если СтрТЗ.СуммаВзаиморасчетов = 0 Тогда
			Продолжить;
		КонецЕсли;
		Запись = ДокОбъект.Движения.А_ВзаиморасчетыССотрудниками.Добавить();
		Запись.Период              = ДокОбъект.Дата;
		Запись.Организация         = СтрТЗ.Организация;
		Запись.Подразделение       = СтрТЗ.Подразделение;
		Запись.ФизическоеЛицо      = СтрТЗ.ФизическоеЛицо;
		Запись.ВидДвижения         = СтрТЗ.ВидДвижения;
		Запись.СуммаВзаиморасчетов = СтрТЗ.СуммаВзаиморасчетов;
	КонецЦикла;

КонецПроцедуры

#КонецОбласти
```

**ВАЖНО — где именно вставить:** В существующем файле `AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl` есть структура (~61 строка):
```
строка  1: #Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
строка  3: // СтандартныеПодсистемы.УправлениеДоступом
строка  6: Процедура ПриЗаполненииОграниченияДоступа(Ограничение) Экспорт
строка 12: КонецПроцедуры
строка 14: // Конец СтандартныеПодсистемы.УправлениеДоступом
строка 16: #Область СлужебныеПроцедурыИФункции
строка 18: #Область ПервоначальноеЗаполнениеИОбновлениеИнформационнойБазы
строка 20: Процедура ЗаполнитьОрганизацию() Экспорт
строка 55: КонецПроцедуры
строка 57: #КонецОбласти      <- закрытие ПервоначальноеЗаполнение...
строка 59: #КонецОбласти      <- закрытие СлужебныеПроцедурыИФункции
строка 61: #КонецЕсли
```

Вставить новую `#Область ЗаполнениеДвижений ... #КонецОбласти` **МЕЖДУ строкой 57 и 59** (т.е. перед финальным `#КонецОбласти`, который закрывает `СлужебныеПроцедурыИФункции`).

- [ ] **Step 1.3: Применить через `/db-load-xml`**

```
/db-load-xml -Mode Partial -Files "AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl"
```

Expected: "Загружено объектов: 1" (модуль регистра).

**Если ошибка «БД занята Конфигуратором»:** попросить пользователя закрыть Конфигуратор (Designer). НЕ обходить ручным `1cv8.exe`.

- [ ] **Step 1.4: Обновить конфигурацию БД**

```
/db-update -Dynamic+
```

Expected: успешное обновление без ошибок.

- [ ] **Step 1.5: Verify — модуль виден через MCP**

```
mcp__1c-workerp__get_metadata_structure
  metaType=AccumulationRegister metaName=А_ВзаиморасчетыССотрудниками
```

Expected: метаданные регистра доступны. (Прямого способа проверить наличие процедуры через MCP нет — проверим через факт записи в Task 4.)

- [ ] **Step 1.6: Commit**

```bash
git add "AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl"
git commit -m "feat(vzs): ManagerModule.ЗаполнитьИзОтраженияЗП — Σ-консерв. запрос для ОЗФУ"
```

---

## Task 2: ОбработкаПроведения ОЗФУ — добавить 1 строку вызова

**Files:**
- Modify: `Documents/ОтражениеЗарплатыВФинансовомУчете/Ext/ObjectModule.bsl:194-200`

- [ ] **Step 2.1: Прочитать строки 194-200**

Текущее содержимое:
```bsl
Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	
	// Инициализация дополнительных свойств для проведения документа
	ПроведениеДокументов.ОбработкаПроведенияДокумента(ЭтотОбъект, Отказ);
	
	
КонецПроцедуры
```

- [ ] **Step 2.2: Заменить на расширенную версию (Edit tool, old_string → new_string)**

```bsl
Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	
	// Инициализация дополнительных свойств для проведения документа
	ПроведениеДокументов.ОбработкаПроведенияДокумента(ЭтотОбъект, Отказ);
	
	// Кастомное наполнение А_ВзаиморасчетыССотрудниками (расшифровка ОТ по ФизЛицу+Подр)
	// Σ-инвариант per (Орг,Подр) = Σ ПАП(Статья=ОТ, Источник=Пуст) per Регистратор.
	// См. docs/superpowers/plans/2026-05-20-vzs-ozfu-implementation-plan.md
	РегистрыНакопления.А_ВзаиморасчетыССотрудниками.ЗаполнитьИзОтраженияЗП(ЭтотОбъект);
	
КонецПроцедуры
```

- [ ] **Step 2.3: Применить через `/db-load-xml`**

```
/db-load-xml -Mode Partial -Files "Documents/ОтражениеЗарплатыВФинансовомУчете/Ext/ObjectModule.bsl"
```

Expected: "Загружено объектов: 1".

- [ ] **Step 2.4: Обновить конфигурацию БД**

```
/db-update -Dynamic+
```

Expected: успешное обновление без ошибок.

- [ ] **Step 2.5: Commit**

```bash
git add "Documents/ОтражениеЗарплатыВФинансовомУчете/Ext/ObjectModule.bsl"
git commit -m "feat(vzs): ОЗФУ.ОбработкаПроведения вызывает РегистрыНакопления.А_ВзаиморасчетыССотрудниками.ЗаполнитьИзОтраженияЗП"
```

---

## Task 3: Acceptance test — перепроведение + проверка инвариантов

**Files:**
- Create: `_Rarzrabotki/Python/test/test_vzs_ozfu_verify.py`

- [ ] **Step 3.1: Создать verify-тест**

Создать файл `_Rarzrabotki/Python/test/test_vzs_ozfu_verify.py`:

```python
# -*- coding: utf-8 -*-
"""Acceptance тест для процедуры
РегистрыНакопления.А_ВзаиморасчетыССотрудниками.ЗаполнитьИзОтраженияЗП().

Шаги:
  1) Загружает список кандидатов из ozfu_candidates.json (создан Task 0)
  2) Перепроводит каждый ОЗФУ (РежимЗаписиДокумента.Проведение)
  3) Сверка per Регистратор:
     - Σ signed А_ВзСС per Регистратор == Σ signed ПАП(ОТ) per Регистратор
     - 100% строк имеют ФизЛицо ≠ ПустаяСсылка
     - 0 дублей ключа (Орг, Подр, ФизЛицо, ВидДвижения)
  4) Сверка per (Орг, Подр) per Регистратор:
     - Σ signed А_ВзСС per (Орг, Подр) == Σ signed ПАП(ОТ) per (Орг, Подр) — 0 расхождений до 1₽

Эталоны Σ_signed_expected — из ozfu_candidates.json (зафиксированы Task 0).
"""
import sys, io, json, os
from collections import defaultdict, Counter
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime

TOL_RUB = 1.0
pythoncom.CoInitialize()
conn = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')

# Параметры
qOT = conn.NewObject("Запрос")
qOT.Текст = ('ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ '
             'ПланВидовХарактеристик.СтатьиАктивовПассивов '
             'ГДЕ Наименование = "Оплата труда"')
rOT = qOT.Выполнить().Выбрать(); rOT.Следующий(); ОТ = rOT.С
РежПров = conn.PredefinedValue("РежимЗаписиДокумента.Проведение")
ПустоеФЛ = conn.PredefinedValue("Справочник.ФизическиеЛица.ПустаяСсылка")

# Загрузка кандидатов
cand_path = os.path.join(os.path.dirname(__file__), "ozfu_candidates.json")
with open(cand_path, "r", encoding="utf-8") as f:
    КАНДИДАТЫ = json.load(f)

print(f"=== Verify А_ВзаиморасчетыССотрудниками от ОЗФУ ===")
print(f"Кандидатов: {len(КАНДИДАТЫ)}\n")


def найти_документ_по_uuid(uuid_str):
    q = conn.NewObject("Запрос")
    q.Текст = ('ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С '
               'ИЗ Документ.ОтражениеЗарплатыВФинансовомУчете '
               'ГДЕ Ссылка = &Сс')
    Сс = conn.ПолучитьСсылку(conn.NewObject("УникальныйИдентификатор", uuid_str),
                              conn.PredefinedValue("Документ.ОтражениеЗарплатыВФинансовомУчете"))
    # Поскольку ПолучитьСсылку выше не сработает напрямую (двухаргументная форма
    # отсутствует в COM), используем альтернативу через XML
    q2 = conn.NewObject("Запрос")
    q2.Текст = """ВЫБРАТЬ Ссылка КАК С ИЗ Документ.ОтражениеЗарплатыВФинансовомУчете
    ГДЕ XMLСтрока(Ссылка) = &XML"""
    # Запросы по XMLСтрока к Ссылке невозможны — используем перебор по дате/номеру
    return None  # placeholder, см. ниже


def найти_документ_по_номер_дата(номер, дата):
    """Найти документ по номеру и дате (точному совпадению)."""
    q = conn.NewObject("Запрос")
    q.Текст = """ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С
    ИЗ Документ.ОтражениеЗарплатыВФинансовомУчете
    ГДЕ Номер = &Н И Дата = &Д"""
    q.УстановитьПараметр("Н", номер)
    q.УстановитьПараметр("Д", дата)
    r = q.Выполнить().Выбрать()
    return r.С if r.Следующий() else None


def сумма_signed_a_vzs(док):
    """Σ signed (Приход − Расход) по А_ВзаиморасчетыССотрудниками per Регистратор."""
    q = conn.NewObject("Запрос")
    q.Текст = """ВЫБРАТЬ СУММА(ВЫБОР КОГДА В.ВидДвижения=ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
                                  ТОГДА В.СуммаВзаиморасчетов ИНАЧЕ -В.СуммаВзаиморасчетов КОНЕЦ) КАК С
    ИЗ РегистрНакопления.А_ВзаиморасчетыССотрудниками КАК В
    ГДЕ В.Регистратор = &Док"""
    q.УстановитьПараметр("Док", док)
    r = q.Выполнить().Выбрать(); r.Следующий()
    return float(r.С or 0)


def сумма_signed_pap_ot(док):
    """Σ signed ПАП(Статья=ОТ, Источник=Пуст) per Регистратор."""
    q = conn.NewObject("Запрос")
    q.Текст = """ВЫБРАТЬ СУММА(ВЫБОР КОГДА Т.ВидДвижения=ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
                                  ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ) КАК С
    ИЗ РегистрНакопления.ПрочиеАктивыПассивы КАК Т
    ГДЕ Т.Регистратор=&Док И Т.Источник=
        ЗНАЧЕНИЕ(Перечисление.ИсточникиУправленческогоБаланса.ПустаяСсылка)
        И Т.Статья=&ОТ"""
    q.УстановитьПараметр("Док", док); q.УстановитьПараметр("ОТ", ОТ)
    r = q.Выполнить().Выбрать(); r.Следующий()
    return float(r.С or 0)


def сравнение_per_подр(док):
    """Сравнение Σ signed per (Орг, Подр) между А_ВзСС и ПАП(ОТ).
    Возвращает (расхождений_count, max_дельта)."""
    qV = conn.NewObject("Запрос")
    qV.Текст = """ВЫБРАТЬ В.Организация КАК Орг, В.Подразделение КАК Подр,
        СУММА(ВЫБОР КОГДА В.ВидДвижения=ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
                  ТОГДА В.СуммаВзаиморасчетов ИНАЧЕ -В.СуммаВзаиморасчетов КОНЕЦ) КАК С
    ИЗ РегистрНакопления.А_ВзаиморасчетыССотрудниками КАК В
    ГДЕ В.Регистратор=&Док
    СГРУППИРОВАТЬ ПО В.Организация, В.Подразделение"""
    qV.УстановитьПараметр("Док", док)
    tV = qV.Выполнить().Выгрузить()
    vzs = {}
    for i in range(tV.Количество()):
        e = tV.Получить(i)
        vzs[(conn.XMLСтрока(e.Орг), conn.XMLСтрока(e.Подр))] = float(e.С or 0)

    qP = conn.NewObject("Запрос")
    qP.Текст = """ВЫБРАТЬ Т.Организация КАК Орг,
        ЕСТЬNULL(Т.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)) КАК Подр,
        СУММА(ВЫБОР КОГДА Т.ВидДвижения=ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
                  ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ) КАК С
    ИЗ РегистрНакопления.ПрочиеАктивыПассивы КАК Т
    ГДЕ Т.Регистратор=&Док И Т.Источник=
        ЗНАЧЕНИЕ(Перечисление.ИсточникиУправленческогоБаланса.ПустаяСсылка)
        И Т.Статья=&ОТ
    СГРУППИРОВАТЬ ПО Т.Организация, ЕСТЬNULL(Т.Подразделение,
        ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка))"""
    qP.УстановитьПараметр("Док", док); qP.УстановитьПараметр("ОТ", ОТ)
    tP = qP.Выполнить().Выгрузить()
    pap = {}
    for i in range(tP.Количество()):
        e = tP.Получить(i)
        pap[(conn.XMLСтрока(e.Орг), conn.XMLСтрока(e.Подр))] = float(e.С or 0)

    все_ключи = set(vzs.keys()) | set(pap.keys())
    расхождений = 0; max_дельта = 0.0
    for ключ in все_ключи:
        delta = abs(vzs.get(ключ, 0) - pap.get(ключ, 0))
        if delta > max_дельта:
            max_дельта = delta
        if delta > TOL_RUB:
            расхождений += 1
    return расхождений, max_дельта


def стат_строк(док):
    """Кол-во строк, доля с ФизЛицом, дубли ключа."""
    q = conn.NewObject("Запрос")
    q.Текст = """ВЫБРАТЬ В.Организация КАК Орг, В.Подразделение КАК Подр,
        В.ФизическоеЛицо КАК ФЛ, В.ВидДвижения КАК ВД
    ИЗ РегистрНакопления.А_ВзаиморасчетыССотрудниками КАК В
    ГДЕ В.Регистратор=&Док"""
    q.УстановитьПараметр("Док", док)
    t = q.Выполнить().Выгрузить()
    rows = t.Количество()
    с_фл = 0
    keys = Counter()
    for i in range(rows):
        e = t.Получить(i)
        ф = conn.XMLСтрока(e.ФЛ)
        if ф != conn.XMLСтрока(ПустоеФЛ):
            с_фл += 1
        keys[(conn.XMLСтрока(e.Орг), conn.XMLСтрока(e.Подр), ф,
              conn.XMLСтрока(e.ВД))] += 1
    дубли = {k: c for k, c in keys.items() if c > 1}
    return rows, с_фл, дубли


all_pass = True
for k in КАНДИДАТЫ:
    period = k["period"]
    номер = k["номер"]
    дата_iso = k["дата"]
    expected = k["Σ_signed_expected"]
    print(f"--- {period} № {номер} от {дата_iso} (expected Σ={expected:,.2f}) ---")

    дата = datetime.fromisoformat(дата_iso)
    док = найти_документ_по_номер_дата(номер, дата)
    if док is None:
        print(f"  ❌ Документ не найден; пропуск")
        all_pass = False
        continue

    # Перепроведение
    объ = док.ПолучитьОбъект()
    if объ is None:
        print(f"  ❌ Получить объект не удалось"); all_pass = False; continue
    try:
        объ.Записать(РежПров)
        print(f"  ✓ Перепроведено")
    except Exception as e:
        print(f"  ❌ Ошибка перепроведения: {e}"); all_pass = False; continue

    # Σ per Регистратор
    взс = сумма_signed_a_vzs(док)
    пап = сумма_signed_pap_ot(док)
    delta_reg = abs(взс - пап)
    delta_exp = abs(взс - expected)
    s1 = "✅" if delta_reg <= TOL_RUB else "❌"
    s2 = "✅" if delta_exp <= TOL_RUB else "❌"
    print(f"  {s1} Σ А_ВзСС = {взс:,.2f}; Σ ПАП(ОТ) = {пап:,.2f}; |Δ|={delta_reg:.2f}")
    print(f"  {s2} expected {expected:,.2f}; |Δ_exp|={delta_exp:.2f}")
    if delta_reg > TOL_RUB or delta_exp > TOL_RUB:
        all_pass = False

    # Per (Орг,Подр)
    расх, max_d = сравнение_per_подр(док)
    s3 = "✅" if расх == 0 else "❌"
    print(f"  {s3} per (Орг,Подр): {расх} расхождений, max |Δ|={max_d:.4f}")
    if расх > 0:
        all_pass = False

    # Статистика строк
    rows, с_фл, дубли = стат_строк(док)
    доля = (с_фл / rows * 100) if rows > 0 else 0
    s4 = "✅" if доля >= 99 else "❌"
    s5 = "✅" if not дубли else "❌"
    print(f"  {s4} ФизЛицо ≠ Пустая: {с_фл}/{rows} ({доля:.1f}%)")
    print(f"  {s5} Дубли (Орг,Подр,ФЛ,ВидДв): {len(дубли)}")
    if доля < 99 or дубли:
        all_pass = False

    print()

# Идемпотентность — 2-й прогон
print("=== Идемпотентность: 2-й прогон ===")
для_idem = []
for k in КАНДИДАТЫ:
    дата = datetime.fromisoformat(k["дата"])
    док = найти_документ_по_номер_дата(k["номер"], дата)
    if док is None: continue
    взс_до = сумма_signed_a_vzs(док)
    try:
        док.ПолучитьОбъект().Записать(РежПров)
    except: continue
    взс_после = сумма_signed_a_vzs(док)
    delta_idem = abs(взс_до - взс_после)
    s = "✅" if delta_idem <= 0.01 else "❌"
    print(f"  {s} {k['период']} № {k['номер']}: Δ={delta_idem:.4f}")
    if delta_idem > 0.01:
        all_pass = False

print()
print("=" * 60)
print("[OK] ВСЕ PASS" if all_pass else "[FAIL] есть падения")
sys.exit(0 if all_pass else 1)
```

- [ ] **Step 3.2: Запустить verify**

```
python "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_vzs_ozfu_verify.py"
```

Expected output:
```
=== Verify А_ВзаиморасчетыССотрудниками от ОЗФУ ===
Кандидатов: N (2-3, в зависимости от Task 0)

--- январь2026 № <...> от <...> (expected Σ=-6,204,232.53) ---
  ✓ Перепроведено
  ✅ Σ А_ВзСС = -6,204,232.53; Σ ПАП(ОТ) = -6,204,232.53; |Δ|=0.00
  ✅ expected -6,204,232.53; |Δ_exp|=0.00
  ✅ per (Орг,Подр): 0 расхождений, max |Δ|=0.0000
  ✅ ФизЛицо ≠ Пустая: 609/609 (100.0%)
  ✅ Дубли (Орг,Подр,ФЛ,ВидДв): 0

--- декабрь2025 № <...> ... ---
  (либо успешно, либо «Документ не найден» — это нормально, если ОЗФУ за дек 2025 нет)

=== Идемпотентность: 2-й прогон ===
  ✅ январь2026: Δ=0.0000
  ...

[OK] ВСЕ PASS
```

**Если FAIL по Σ per Регистратор:**
- Возможно в реальном проведении были применены другие фильтры — диагностика через `mcp__1c-workerp__execute_query` для конкретного документа: посчитать суммы по 3 ТЧ и сравнить с тем что записалось в А_ВзСС.
- Возможно есть строки со СпособОтраженияЗарплатыВБухучете, влияющие на статью — но в нашей логике мы это не фильтруем. Проверить расхождение поможет понять.

**Если FAIL по идемпотентности:** проверить что `ДокОбъект.Движения.А_ВзаиморасчетыССотрудниками.Очистить()` действительно вызывается перед .Добавить() — это ключ.

- [ ] **Step 3.3: Commit verify-тест**

```bash
git add _Rarzrabotki/Python/test/test_vzs_ozfu_verify.py
git commit -m "test(vzs): verify acceptance — перепроведение ОЗФУ + проверка Σ=ПАП(ОТ)"
```

---

## Task 4: Регрессия А_ОтчетБаланс_Свод

**Цель:** Убедиться, что наша правка ОЗФУ.ОбработкаПроведения не сломала существующий механизм формирования А_ОтчетБаланс_Свод (Свод_ОплатаТруда + остальные 5 Свод_*).

- [ ] **Step 4.1: Найти А_ФинРез_Баланс за январь 2026 и перепровести**

Создать или дополнить `_Rarzrabotki/Python/test/test_vzs_ozfu_regression.py`:

```python
# -*- coding: utf-8 -*-
"""Регрессия: после наших правок ОЗФУ.ОбработкаПроведения баланс
А_ОтчетБаланс_Свод не должен измениться. Эталоны:
  - янв2026 Актив = 288 787 750,11
  - Σ signed Свод_ОплатаТруда per Орг (янв) = −7 196 698,44 (это полная ОТ
    от всех 5 регистраторов, а не только ОЗФУ)
"""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime

TOL = 1.0
pythoncom.CoInitialize()
conn = win32com.client.Dispatch("V83.COMConnector").Connect(
    'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')

q0 = conn.NewObject("Запрос")
q0.Текст = ('ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Справочник.Организации '
            'ГДЕ КодПоЕДРПОУ = "40645273"')
r = q0.Выполнить().Выбрать(); r.Следующий(); ORG = r.С
РежПров = conn.PredefinedValue("РежимЗаписиДокумента.Проведение")

qI = conn.NewObject("Запрос")
qI.Текст = ('ВЫБРАТЬ Ссылка КАК Ссылка ИЗ ПланВидовХарактеристик.СтатьиАктивовПассивов '
            'ГДЕ Наименование В (&Имена)')
имена = conn.NewObject("Массив")
for n in ("Собственные средства", "Доходы текущего периода",
          "Расходы текущего периода"):
    имена.Добавить(n)
qI.УстановитьПараметр("Имена", имена)
tI = qI.Выполнить().Выгрузить()
Искл = conn.NewObject("Массив")
for i in range(tI.Количество()):
    Искл.Добавить(tI.Получить(i).Ссылка)

# Перепровести А_ФинРез_Баланс янв 2026 (баланс свежий)
qB = conn.NewObject("Запрос")
qB.Текст = """ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Документ.А_ФинРез_Баланс
ГДЕ Организация=&Орг И НАЧАЛОПЕРИОДА(Месяц,МЕСЯЦ)=&М И Проведен"""
qB.УстановитьПараметр("Орг", ORG)
qB.УстановитьПараметр("М", datetime(2026, 1, 1))
rB = qB.Выполнить().Выбрать()
if rB.Следующий():
    try:
        rB.С.ПолучитьОбъект().Записать(РежПров)
        print(f"✓ Перепровёл А_ФинРез_Баланс январь 2026: {rB.С}")
    except Exception as e:
        print(f"⚠️ Не удалось перепровести: {e}")
else:
    print("⚠️ А_ФинРез_Баланс за январь 2026 не найден")

# Проверка: Актив янв 2026 = 288 787 750,11
qA = conn.NewObject("Запрос")
qA.Текст = """
ВЫБРАТЬ СУММА(ВЫБОР КОГДА Ст.АктивПассив = ЗНАЧЕНИЕ(Перечисление.ВидыСтатейУправленческогоБаланса.Актив)
                  ТОГДА С.СуммаКонечныйОстаток ИНАЧЕ 0 КОНЕЦ) КАК Актив
ИЗ РегистрСведений.А_ОтчетБаланс_Свод КАК С
    ЛЕВОЕ СОЕДИНЕНИЕ ПланВидовХарактеристик.СтатьиАктивовПассивов КАК Ст
        ПО С.Статья = Ст.Ссылка
ГДЕ С.Организация = &Орг И НЕ С.Статья В ИЕРАРХИИ(&Искл) И С.ДокументДвижения В (
    ВЫБРАТЬ Ссылка ИЗ Документ.А_ФинРез_Баланс
    ГДЕ Организация=&Орг И Проведен И НАЧАЛОПЕРИОДА(Месяц,МЕСЯЦ)=&М)"""
qA.УстановитьПараметр("Орг", ORG)
qA.УстановитьПараметр("М", datetime(2026, 1, 1))
qA.УстановитьПараметр("Искл", Искл)
rA = qA.Выполнить().Выбрать(); rA.Следующий()
актив = float(rA.Актив or 0)
ожидаемый = 288_787_750.11
delta = abs(актив - ожидаемый)
print(f"\nАктив янв2026 = {актив:,.2f}; эталон = {ожидаемый:,.2f}; |Δ|={delta:.2f}")

if delta <= TOL:
    print("[OK] Регрессия А_ОтчетБаланс_Свод не зачеплена")
    sys.exit(0)
else:
    print("[FAIL] Регрессия — баланс отклонился")
    sys.exit(1)
```

- [ ] **Step 4.2: Запустить regression**

```
python "C:\Configuration_downloads\BASERP25\_Rarzrabotki\Python\test\test_vzs_ozfu_regression.py"
```

Expected: `[OK] Регрессия А_ОтчетБаланс_Свод не зачеплена` с Актив = 288,787,750.11 ± 1.

- [ ] **Step 4.3: Commit regression-тест**

```bash
git add _Rarzrabotki/Python/test/test_vzs_ozfu_regression.py
git commit -m "test(vzs): регрессия А_ОтчетБаланс_Свод после правки ОЗФУ"
```

---

## Task 5: Knowledge update

**Files:**
- Create: `_Rarzrabotki/notebook/knowledge_Balanse/balanse_vzs_oplatatruda.md`
- Modify: `_Rarzrabotki/notebook/knowledge_Balanse/KNOWLEDGE_MAP_BALANSE.md` (добавить ссылку)

- [ ] **Step 5.1: Создать knowledge-файл `balanse_vzs_oplatatruda.md`**

```markdown
# А_ВзаиморасчетыССотрудниками — расшифровка статьи "Оплата труда" по ФизЛицу + Подразделению

> **Призначення:** документація механізму заповнення кастомного балансового
> регістра `РегистрНакопления.А_ВзаиморасчетыССотрудниками` (Орг + ФізЛице +
> Підрозділ + СуммаВзаиморасчетов) рухами Σ-консервативної розшифровки ПАП
> по статті "Оплата труда". Працює як **частина стандартного циклу проведення**
> 5 регістраторів (через виклик в `ОбработкаПроведения`).
>
> Тип файлу: ПОСТІЙНИЙ. Оновлювати при додаванні нових регістраторів або зміні
> логіки заповнення.

---

## TL;DR

- **Регістраторы (5):** ОтражениеЗарплатыВФинансовомУчете (DONE 2026-05-20),
  ПриходныйКассовыйОрдер, РасходныйКассовыйОрдер, ПоступлениеБезналичных,
  СписаниеБезналичных (4 — наступні ітерації після ОЗФУ)
- **Точка інтеграції:** один рядок виклику в кінці `ОбработкаПроведения`
  кожного документа: `РегистрыНакопления.А_ВзаиморасчетыССотрудниками.ЗаполнитьИз<Тип>(ЭтотОбъект);`
- **Логіка:** універсальні процедури в `AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl`,
  кожна знає свої поля документа.
- **Σ-інваріант:** Σ signed А_ВзСС per (Орг, Подр) per Регістратор =
  Σ signed `РегистрНакопления.ПрочиеАктивыПассивы(Статья=ОплатаТруда, Источник=ПустаяСсылка)` per Регістратор
- **Прозорість:** працює ВСЕРЕДИНІ стандартного циклу проведення документа
  (Движения.<регістр>.Записывать = Истина → flush ВМЕСТЕ зі всіма іншими
  регістрами в одній транзакції). Жодних event subscriptions.

---

## ОтражениеЗарплатыВФинансовомУчете (DONE 2026-05-20)

**Логіка:** запит з 3 ТЧ документа, signed convention 1:1 ПАП:

| Джерело (ТЧ) | Фільтр | ВидДвижения | Значення |
|---|---|---|---|
| `НачисленнаяЗарплатаИВзносыПоФизлицам` | `Налог = Справочник.Налоги.ПустаяСсылка` І `ВидОперации НЕ В 4 искл` (ЕжегодныйОтпускОценочныеОбязательстваИРезервы, НачисленоСдельноДоход, КомпенсацияЕжегодногоОтпускаОценочныеОбязательства, ЕжегодныйОтпускОценочныеОбязательства) | **Расход** | СУММА(Сумма) |
| `НачисленныйНДФЛ` | (без фільтра) | **Приход** | СУММА(Сумма) |
| `УдержаннаяЗарплата` | (без фільтра) | **Приход** | СУММА(Сумма) |

GROUP BY `(Організація, ПодразделениеПредприятия, ФизическоеЛицо)` — найдетальніший зріз.

**Еталони (ТОВ ІНДАСТРІАЛБУД, ОЗФУ-only signed):**
- січень 2026: Σ = **−6 204 232,53** (609 рядків, 43 підрозділи, 100% з ФізЛице)
- лютий 2026: Σ = **−5 550 627,58** (620 рядків, 46 підрозділів, 100% з ФізЛице)

(Це Σ_ОЗФУ-only; повна Σ ОТ за січень з усіх 5 регістраторів = **−7 196 698,44** — різниця ~−992 466 = решта 4 регістратори.)

**Файл логіки:** `AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl` (процедура `ЗаполнитьИзОтраженияЗП`).

**Точка інтеграції:** `Documents/ОтражениеЗарплатыВФинансовомУчете/Ext/ObjectModule.bsl:198` (рядок виклику після типового `ПроведениеДокументов.ОбработкаПроведенияДокумента(...)`).

**Тести:** `_Rarzrabotki/Python/test/test_vzs_ozfu_pretest.py`, `test_vzs_ozfu_verify.py`, `test_vzs_ozfu_regression.py`.

---

## Roadmap — наступні 4 регістратори

| # | Документ | Джерело | Стан |
|---|---|---|---|
| 1 | ОтражениеЗарплатыВФинансовомУчете | 3 ТЧ (зарплата + НДФЛ + удержання) | ✅ DONE 2026-05-20 |
| 2 | РасходныйКассовыйОрдер | РасшифровкаПлатежа з типом "Сотрудник" | TODO |
| 3 | СписаниеБезналичныхДенежныхСредств | РасшифровкаПлатежа з типом "Сотрудник" | TODO |
| 4 | ПриходныйКассовыйОрдер | РасшифровкаПлатежа з типом "Сотрудник" (ВозвратЗарплаты) | TODO |
| 5 | ПоступлениеБезналичныхДенежныхСредств | РасшифровкаПлатежа з типом "Сотрудник" (рідко) | TODO |

Шаблон імен процедур: `ЗаполнитьИз<КороткеНайменування>(ДокОбъект)`.

## Cross-references

- План реалізації ОЗФУ: `docs/superpowers/plans/2026-05-20-vzs-ozfu-implementation-plan.md`
- Регістр: `AccumulationRegisters/А_ВзаиморасчетыССотрудниками.xml`
- ManagerModule: `AccumulationRegisters/А_ВзаиморасчетыССотрудниками/Ext/ManagerModule.bsl`
- Свод_ОплатаТруда (батьківський механізм у А_ОтчетБаланс_Свод): `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl:1213`
- Patron-канон роботи з ПАП: `_Rarzrabotki/notebook/knowledge_Balanse/balanse_pattern_and_roadmap.md`
```

- [ ] **Step 5.2: Додати посилання в `KNOWLEDGE_MAP_BALANSE.md`**

В таблиці «Файли в цій базі знань» додати рядок:
```markdown
| 7 | [balanse_vzs_oplatatruda.md](balanse_vzs_oplatatruda.md) | А_ВзаиморасчетыССотрудниками — розшифровка ОТ по ФізЛице/Підрозділ через 5 регістраторів (ОЗФУ DONE 2026-05-20, +4 TODO) |
```

- [ ] **Step 5.3: Commit knowledge**

```bash
git add "_Rarzrabotki/notebook/knowledge_Balanse/balanse_vzs_oplatatruda.md" "_Rarzrabotki/notebook/knowledge_Balanse/KNOWLEDGE_MAP_BALANSE.md"
git commit -m "docs(knowledge): А_ВзаиморасчетыССотрудниками + ОЗФУ механизм заполнения"
```

---

## Task 6: Финальная сверка статуса

- [ ] **Step 6.1: Проверить git log**

```bash
git log --oneline -10
```

Expected: ~7 commits (Task 0/1/2/3/4/5/6 + commit `feat(vzs): ManagerModule...` + `feat(vzs): ОЗФУ.ОбработкаПроведения...`).

- [ ] **Step 6.2: git status**

```bash
git status
```

Expected: working tree clean.

- [ ] **Step 6.3: Финальный smoke-test — все 3 теста PASS**

```bash
python _Rarzrabotki/Python/test/test_vzs_ozfu_pretest.py
python _Rarzrabotki/Python/test/test_vzs_ozfu_verify.py
python _Rarzrabotki/Python/test/test_vzs_ozfu_regression.py
```

Все 3 должны вернуть exit code 0.

---

## Self-Review

### Spec coverage

| Требование (со слов user) | Задача в плане |
|---|---|
| Каждый документ — своя процедура | Task 1 (для ОЗФУ; шаблон для 4 других в roadmap) |
| Логика в стандартном цикле проведения | Task 2 (вызов в ОбработкаПроведения, Движения.<регистр>.Записывать=Истина) |
| Источники: 3 ТЧ (НачисленнаяЗПИВзносыПоФизлицам + НДФЛ + Удержания) | Task 1 (запрос) |
| Σ по Подразделению совпадает с ПАП | Task 3 (Step 3.1 функция `сравнение_per_подр`, assert 0 расхождений) |
| Загрузка через скилы | Task 1.3-1.4, Task 2.3-2.4 (`/db-load-xml -Mode Partial` + `/db-update -Dynamic+`) |
| Проверка декабрь 2025 + январь 2026 | Task 0 (поиск кандидатов), Task 3 (verify) |

### Placeholder scan

- В Step 3.1 функция `найти_документ_по_uuid` имеет placeholder реализацию — но фактически используется `найти_документ_по_номер_дата` (по полю Номер+Дата из JSON кандидатов). Это не TBD, это альтернатива (UUID через ПолучитьСсылку с двумя аргументами в Python COM нестабильно).
- Все запросы — полностью развёрнутые, нет TBD.

### Type/name consistency

- `А_ВзаиморасчетыССотрудниками` — везде ✅
- `ЗаполнитьИзОтраженияЗП(ДокОбъект)` — Step 1.2, 2.2 ✅
- `НачисленнаяЗарплатаИВзносыПоФизлицам` (с буквой "Ф" в "Физ") — везде ✅
- Эталоны `−6 204 232,53` (янв) / `−5 550 627,58` (фев) — везде ✅

### Risks / Mitigations

| # | Риск | Mitigation |
|---|---|---|
| R1 | За дек 2025 ОЗФУ не существует | Step 0.2 покажет; verify обрабатывает «Документ не найден» как пропуск, не FAIL |
| R2 | Σ per Регистратор не сходится (например из-за `КоэффициентПересчетаВВалютуУпр`) | Pre-test PASS Δ=0.00 значит для текущих документов коэф=1; диагностика в Task 3 (Step 3.2 «Если FAIL») |
| R3 | Идемпотентность ломается из-за повторного добавления | Шаг `.Очистить()` ПЕРЕД .Добавить() — Task 1 Step 1.2 |
| R4 | Регрессия А_ОтчетБаланс_Свод (баланс изменился) | Task 4 проверяет; если FAIL — откат ObjectModule.bsl, выяснить почему свод-механизм зачеплен |
| R5 | Конфигуратор открыт и блокирует БД | Step 1.3 — попросить закрыть; не обходить ручным 1cv8.exe |
| R6 | Сдельная зарплата в каком-то документе (блок 2 типового запроса) — Σ не сойдётся | Pre-test PASS для янв+фев показал нет сдельной; если в будущем возникнет — добавить блок 2 в запрос |

---

**Plan complete.** 7 задач, ~25 шагов, полный TDD-цикл (pre-test уже PASS → BSL → apply → verify → regression → knowledge). Всё готово к выполнению.
