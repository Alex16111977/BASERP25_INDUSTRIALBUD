# А_ФинРез_Баланс — Етап 1 (MVP) + GATE OD-2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Перепровести `Документ.А_ФинРез_Баланс` так, щоб при проведенні він наповнював `РегистрСведений.А_ОтчетБаланс_Свод` управлінським балансом по **ВСІХ статтях** (без фільтра виключення — v1.2 OD-3), що сходиться зі штатним `Отчёт.УправленческийБаланс` до копійки; субконто розкладається прямо з `ПАП.Аналитика` для ОЗ/НМА.

**Architecture:** Чистий `РегистрНакопления.ПрочиеАктивыПассивы.ОстаткиИОбороты` — єдине джерело сум, **0 JOIN, 0 залежності від `ИдентификаторФинЗаписи`** (OD-8: Етап 1 самодостатній). 4 ресурси 1:1. `Аналитика` (composite) розкладається `ВЫРАЗИТЬ` тільки в SELECT; `СГРУППИРОВАТЬ ПО Б.Аналитика` як-є (інакше втрата Σ). **БЕЗ фільтра по `Статья`** (v1.2 OD-3: штатний звіт включає ВСІ статті й класифікує через `Статья.АктивПассив`; розріз Актив/Пасив — downstream Power BI OD-9). Актив=Пасив = Σ повного набору КонОст ≈ 0 (перевірено 0.00). Завершується блокуючим GATE OD-2, що визначає долю Етапів 2-4 (окремий план).

**Tech Stack:** 1С:Підприємство 8.3.20, BSL, Python 3 + win32com (COM `V83.COMConnector`), skills `/cf-validate` `/db-load-xml` `/db-update`.

**Reference spec:** `docs/superpowers/specs/2026-05-15-balans-svod-canonical-design.md` (Canonical **v1.2** — без фільтра виключення; розріз Актив/Пасив downstream).

**БД:** `Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"` · Організація **ТОВ ІНДАСТРІАЛБУД (ЄДРПОУ 40645273)** · період **лютий 2026**.

**Шляхи:** worktree = `C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b`; main config = `C:\Configuration_downloads\BASERP25` (1С Конфигуратор вантажить ЗВІДСИ — Rule #4).

---

## ⚠️ ПОПРАВКА v1.2 (ПЕРЕВИЗНАЧАЄ код у Tasks 1/2/5 нижче)

Під час виконання Task 1 факт-чек штатного `Отчёт.УправленческийБаланс` (`Template.xml:712`) + БД спростував передумову фільтра. **Канон оновлено до v1.2 (OD-3, OD-5, OD-9).** Обов'язкові поправки до тексту Tasks 1/2/5 нижче (де згадано фільтр/`EXCL`/`ИсключенныеСтатьи`/`НЕ Статья В ИЕРАРХИИ`):

1. **НЕМАЄ фільтра по `Статья`.** Склад регістра = **ВСІ статті** ПАП. Прибрати з усіх запитів і BSL: `EXCL`, `ИсключенныеСтатьи`, блок пошуку 3 статей, `И НЕ Статья В ИЕРАРХИИ(...)`, параметр `&Искл`/`&ИсключенныеСтатьи`, блок `ЗапросИскл`.
2. **Актив=Пасив** перевіряється як `Σ СуммаКонечныйОстаток` повного набору **≈ 0** (подвійний запис ПАП; перевірено факт лютий/ТОВ = 0.00) — НЕ «після фільтра».
3. **Test 3**: звірка регістр↔ПАП по статтях **без фільтра з обох боків** + Σ повного набору КонОст ≈ 0.
4. Розріз Актив/Пасив (`Статья.АктивПассив`) — **downstream Power BI (OD-9)**, у свёд НЕ пишеться, нового поля немає.
5. Імʼя статті у Python брати з `ПРЕДСТАВЛЕНИЕ(Б.Статья)` у запиті (НЕ `conn.Представление` — недоступний через COM); параметри-списки у COM передавати через `conn.NewObject("Массив")`, не Python-list.

Канонічні (вже виправлені) тексти Test 1/Test 3 — у фактичних файлах `_Rarzrabotki/Python/test/test_balans_t1_query.py` / `test_balans_t3_vs_pap.py` (джерело істини коду — файли, не цей план). Решта структури плану (порядок задач, db-load, GATE OD-2, OD-8) — чинна.

---

## File Structure

| Файл | Відповідальність | Дія |
|---|---|---|
| `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl` | `ОбработкаЗаполнения` + `ОбработкаПроведения` + `СформироватьЗапросСверткиБаланса` (одношаровий ПАП, фільтр, розклад під 17 вимірів) | **Перезаписати** (чернетка застаріла — SELECT-ить неіснуючі виміри) |
| `_Rarzrabotki/Python/test/test_balans_t1_query.py` | Test 1 (Rule #-1): верифікація запиту + підбір предиката фільтра | Створити |
| `_Rarzrabotki/Python/test/test_balans_t2_posting.py` | Test 2: проведення, idempotent, 4 ресурси балансуються | Створити |
| `_Rarzrabotki/Python/test/test_balans_t3_vs_pap.py` | Test 3: звірка регістр↔ПАП до копійки + Актив=Пасив після фільтра | Створити |
| `_Rarzrabotki/Python/test/test_balans_gate_od2.py` | GATE OD-2: тест придатності `ИдентификаторФинЗаписи` | Створити |
| `InformationRegisters/А_ОтчетБаланс_Свод.xml`, `Documents/А_ФинРез_Баланс/**`, `Enums/А_ИсточникБаланса/**` | Структура регістра/документа/enum (веде фінансист) | **НЕ редагувати**; лише sync main→worktree (Task 0) |

> Структуру регістра веде фінансист вручну. **AI не викликає `/meta-edit`.** BSL пишеться під фактичну структуру з БД (Task 0 крок 3).

---

### Task 0: Передумови (OD-6) — sync main→worktree, фіксація структури з БД

**Files:**
- Modify (sync): `Documents/А_ФинРез_Баланс/**`, `InformationRegisters/А_ОтчетБаланс_Свод.xml`, `Enums/А_ИсточникБаланса/**` (worktree ← main config)

- [ ] **Step 1: Перевірити стан worktree та наявність об'єктів у main config**

Run:
```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git status --short && echo "---MAIN---" && ls "C:/Configuration_downloads/BASERP25/Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl" "C:/Configuration_downloads/BASERP25/InformationRegisters/А_ОтчетБаланс_Свод.xml" "C:/Configuration_downloads/BASERP25/Enums/А_ИсточникБаланса.xml"
```
Expected: 3 файли в main config існують; worktree чистий або лише docs.

- [ ] **Step 2: Синхронізувати об'єкти з main config у worktree (ідемпотентно)**

Видалити існуючі цілі (синхрон-копії на виділеній гілці — безпечно; git-стан перевірено в Step 1), потім скопіювати з main:
```bash
cd "C:/Configuration_downloads/BASERP25" && W=".claude/worktrees/peaceful-knuth-51295b" && rm -rf "$W/Documents/А_ФинРез_Баланс" "$W/InformationRegisters/А_ОтчетБаланс_Свод" "$W/Enums/А_ИсточникБаланса" && cp -r "Documents/А_ФинРез_Баланс" "$W/Documents/А_ФинРез_Баланс" && cp "Documents/А_ФинРез_Баланс.xml" "$W/Documents/А_ФинРез_Баланс.xml" && cp "InformationRegisters/А_ОтчетБаланс_Свод.xml" "$W/InformationRegisters/А_ОтчетБаланс_Свод.xml" && { [ -d "InformationRegisters/А_ОтчетБаланс_Свод" ] && cp -r "InformationRegisters/А_ОтчетБаланс_Свод" "$W/InformationRegisters/А_ОтчетБаланс_Свод" || true; } && cp "Enums/А_ИсточникБаланса.xml" "$W/Enums/А_ИсточникБаланса.xml" && { [ -d "Enums/А_ИсточникБаланса" ] && cp -r "Enums/А_ИсточникБаланса" "$W/Enums/А_ИсточникБаланса" || true; } && echo "synced"
```
Expected: `synced` (без помилок про неіснуючі каталоги).

- [ ] **Step 3: Зафіксувати фактичну структуру регістра з БД (НЕ з XML)**

Use MCP tool `get_metadata_structure` (metaType=`InformationRegisters`, name=`А_ОтчетБаланс_Свод`).
Expected — рівно **17 вимірів**: `Организация, Подразделение, Статья, Номенклатура, Контрагент, Партнер, Склад, ОбъектыЭксплуатации, Договор, ФизическоеЛицо, ДенежныеСредства, Source, Аналитика1, Аналитика2, Аналитика3, ОбъектРасчетов, НематериальныйАктив`; **4 ресурси**: `СуммаНачальныйОстаток, СуммаПриход, СуммаРасход, СуммаКонечныйОстаток`.
**Якщо структура інша** — STOP, ескалація фінансисту (він міг змінити); BSL у Task 2 будувати під фактичну структуру з цього кроку.

- [ ] **Step 4: Commit sync**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add Documents/А_ФинРез_Баланс Documents/А_ФинРез_Баланс.xml InformationRegisters/А_ОтчетБаланс_Свод InformationRegisters/А_ОтчетБаланс_Свод.xml Enums/А_ИсточникБаланса Enums/А_ИсточникБаланса.xml && git commit -m "chore: sync А_ФинРез_Баланс/А_ОтчетБаланс_Свод/А_ИсточникБаланса worktree ← main"
```
Expected: commit створено (1+ файлів).

---

### Task 1: Test 1 (Rule #-1, RED) — Python COM верифікація запиту + підбір предиката фільтра

Перевіряємо SQL і фільтр виключення **ПЕРЕД** написанням BSL. Цей тест також ВИЗНАЧАЄ як саме шукати 3 виключені статті (за Наименование) — результат піде в BSL Task 2.

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_t1_query.py`

- [ ] **Step 1: Написати Test 1**

```python
# -*- coding: utf-8 -*-
"""Test 1 (Rule #-1): запит А_ФинРез_Баланс Етап 1 + предикат фільтра виключення.
ПАП.ОстаткиИОбороты, фільтр НЕ Статья В ИЕРАРХИИ(3 групи), розклад Аналитика."""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime

CONN = 'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"'
D1 = datetime(2026, 2, 1, 12, 0, 0)
D2 = datetime(2026, 2, 28, 23, 59, 59)
TOL = 0.01
EXCL = ["Собственные средства", "Доходы текущего периода", "Расходы текущего периода"]

pythoncom.CoInitialize()
conn = win32com.client.Dispatch("V83.COMConnector").Connect(CONN)
print("[OK] BaseERP")

q0 = conn.NewObject("Запрос")
q0.Текст = 'ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Справочник.Организации ГДЕ КодПоЕДРПОУ = "40645273"'
s = q0.Выполнить().Выбрать(); s.Следующий(); org = s.С

# --- знайти 3 виключені статті за Наименование + перевірити ієрархію ---
qx = conn.NewObject("Запрос")
qx.Текст = """ВЫБРАТЬ Ссылка КАК С, Наименование КАК Н, ЭтоГруппа КАК Г, Родитель КАК Р
ИЗ ПланВидовХарактеристик.СтатьиАктивовПассивов ГДЕ Наименование В (&Сп)"""
qx.УстановитьПараметр("Сп", EXCL)
tzx = qx.Выполнить().Выгрузить()
excl_refs = []
found = {}
for i in range(tzx.Количество()):
    r = tzx.Получить(i)
    excl_refs.append(r.С); found[str(r.Н)] = bool(r.Г)
print(f"знайдено виключених статей: {tzx.Количество()} {list(found.items())}")
assert tzx.Количество() == 3, f"FAIL: очікувалось 3 статті, знайдено {tzx.Количество()}"
assert found.get("Собственные средства") is True, "FAIL: 'Собственные средства' має бути ГРУПОЮ"

# --- 'Прибыли и убытки' НЕ виключена ---
qp = conn.NewObject("Запрос")
qp.Текст = "ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК К ИЗ ПланВидовХарактеристик.СтатьиАктивовПассивов ГДЕ Наименование = \"Прибыли и убытки\""
sp = qp.Выполнить().Выбрать(); sp.Следующий()
assert int(sp.К) >= 1, "FAIL: 'Прибыли и убытки' не знайдено (має лишитись у балансі)"
print("[OK] 'Прибыли и убытки' існує і НЕ у списку виключення")

# --- основний запит Етапу 1 (1:1 як піде в BSL) ---
q = conn.NewObject("Запрос")
q.Текст = """
ВЫБРАТЬ
    Б.Статья КАК Статья,
    ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.ОбъектыЭксплуатации) КАК ОЭ,
    ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.НематериальныеАктивы) КАК НМА,
    ПРЕДСТАВЛЕНИЕ(Б.Аналитика) КАК А3,
    СУММА(Б.СуммаНачальныйОстаток) КАК sN, СУММА(Б.СуммаПриход) КАК sP,
    СУММА(Б.СуммаРасход) КАК sR, СУММА(Б.СуммаКонечныйОстаток) КАК sK
ИЗ РегистрНакопления.ПрочиеАктивыПассивы.ОстаткиИОбороты(
        &Д1, &Д2, Авто, ,
        Организация = &Орг И НЕ Статья В ИЕРАРХИИ (&Искл)) КАК Б
СГРУППИРОВАТЬ ПО Б.Статья, Б.Аналитика
ИМЕЮЩИЕ НЕ (СУММА(Б.СуммаНачальныйОстаток)=0 И СУММА(Б.СуммаПриход)=0
        И СУММА(Б.СуммаРасход)=0 И СУММА(Б.СуммаКонечныйОстаток)=0)
"""
q.УстановитьПараметр("Д1", D1); q.УстановитьПараметр("Д2", D2)
q.УстановитьПараметр("Орг", org); q.УстановитьПараметр("Искл", excl_refs)
tz = q.Выполнить().Выгрузить()
n = tz.Количество()
print(f"rows={n}")
assert n > 0, "FAIL: 0 рядків"

cnt_oe = cnt_nma = bal_fail = 0
sumK = 0.0
excl_names = set(EXCL)
for i in range(n):
    r = tz.Получить(i)
    sN=float(r.sN or 0); sP=float(r.sP or 0); sR=float(r.sR or 0); sK=float(r.sK or 0)
    if abs(sK-(sN+sP-sR)) > TOL: bal_fail += 1
    sumK += sK
    if r.ОЭ and conn.ЗначениеЗаполнено(r.ОЭ): cnt_oe += 1
    if r.НМА and conn.ЗначениеЗаполнено(r.НМА): cnt_nma += 1
    nm = str(conn.Представление(r.Статья))
    assert nm not in excl_names, f"FAIL: виключена стаття '{nm}' присутня в результаті"

print(f"ОбъектыЭксплуатации непорожніх: {cnt_oe}")
print(f"НематериальныйАктив непорожніх: {cnt_nma}")
print(f"балансова рівність порушена в {bal_fail} рядках")
print(f"Σ КонечныйОстаток (Актив−Пасив) = {sumK:,.2f}")
assert bal_fail == 0, f"FAIL: {bal_fail} рядків не балансуються"
assert cnt_oe > 0, "FAIL: немає субконто ОЗ (ОбъектыЭксплуатации)"
assert abs(sumK) < 1.0, f"FAIL: Актив≠Пасив після фільтра (Σ={sumK:,.2f})"
print("PASS Test 1")
```

- [ ] **Step 2: Запустити Test 1**

Run: `python "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\_Rarzrabotki\Python\test\test_balans_t1_query.py"`
Expected: `знайдено виключених статей: 3`, `[OK] 'Прибыли и убытки' ...`, `ОбъектыЭксплуатации непорожніх: >0`, `балансова рівність порушена в 0 рядках`, `Σ КонечныйОстаток (Актив−Пасив) ≈ 0.00`, `PASS Test 1`.
**Якщо FAIL** «3 статті не знайдено» → підібрати точні Наименование/предопределені імена (вивести всі кореневі статті: `ВЫБРАТЬ Наименование, ЭтоГруппа ИЗ ПланВидовХарактеристик.СтатьиАктивовПассивов ГДЕ Родитель = ЗНАЧЕНИЕ(ПланВидовХарактеристик.СтатьиАктивовПассивов.ПустаяСсылка)`), оновити `EXCL`, повторити. Точний список `EXCL` → у BSL Task 2.

- [ ] **Step 3: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add _Rarzrabotki/Python/test/test_balans_t1_query.py && git commit -m "test: Test 1 (Rule #-1) запит Етап 1 + предикат фільтра виключення (PASS)"
```
Expected: commit створено.

---

### Task 2: Переписати `ObjectModule.bsl` під фактичні 17 вимірів (GREEN)

Чернетку повністю перезаписати: вона SELECT-ить неіснуючі виміри (`ТипНалога`, `СтатьяДвиженияДС` тощо) → `Загрузить()` впаде. Список `EXCL` — точно як у Test 1 (Task 1 Step 2).

**Files:**
- Overwrite: `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl`

- [ ] **Step 1: Перезаписати модуль повністю**

```bsl
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		Организация = А_Привилегированный.А_ПолучитьОрганизациюУпр();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Месяц) Тогда
		Месяц = НачалоМесяца(ТекущаяДатаСеанса());
	КонецЕсли;
КонецПроцедуры

Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	НачМесяца = НачалоМесяца(Месяц);
	КонМесяца = КонецМесяца(Месяц);

	// --- Дублікат-захист ---
	ЗапросДубль = Новый Запрос;
	ЗапросДубль.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1 ФР.Номер КАК Номер, ФР.Дата КАК Дата
	|ИЗ Документ.А_ФинРез_Баланс КАК ФР
	|ГДЕ ФР.Ссылка <> &ТекущийДок И НЕ ФР.ПометкаУдаления И ФР.Проведен
	|	И ФР.Организация = &Организация
	|	И ФР.Месяц МЕЖДУ &НачМесяца И &КонМесяца
	|	И ФР.ВключатьДочерние = &ВключатьДочерние";
	ЗапросДубль.УстановитьПараметр("ТекущийДок",       Ссылка);
	ЗапросДубль.УстановитьПараметр("Организация",      Организация);
	ЗапросДубль.УстановитьПараметр("НачМесяца",        НачМесяца);
	ЗапросДубль.УстановитьПараметр("КонМесяца",        КонМесяца);
	ЗапросДубль.УстановитьПараметр("ВключатьДочерние", ВключатьДочерние);
	РезДубль = ЗапросДубль.Выполнить();
	Если НЕ РезДубль.Пустой() Тогда
		ВыбДубль = РезДубль.Выбрать(); ВыбДубль.Следующий();
		ОбщегоНазначения.СообщитьПользователю(
			СтрШаблон("Не можна провести: за %1 (Організація %2) вже проведено документ %3 від %4.",
				Формат(НачМесяца, "ДФ='ММММ yyyy'"), Организация, ВыбДубль.Номер,
				Формат(ВыбДубль.Дата, "ДФ='dd.MM.yyyy'")));
		Отказ = Истина; Возврат;
	КонецЕсли;

	// --- Виключені статті (OD-3): 3 групи/статті, як перевірено Test 1 ---
	ЗапросИскл = Новый Запрос;
	ЗапросИскл.Текст =
	"ВЫБРАТЬ Ссылка КАК Ссылка ИЗ ПланВидовХарактеристик.СтатьиАктивовПассивов
	|ГДЕ Наименование В (&Имена)";
	ЗапросИскл.УстановитьПараметр("Имена", СтрРазделить(
		"Собственные средства,Доходы текущего периода,Расходы текущего периода", ","));
	ИсключенныеСтатьи = ЗапросИскл.Выполнить().Выгрузить().ВыгрузитьКолонку("Ссылка");

	// --- Свертка ПАП (Етап 1: 0 JOIN) ---
	Движения.А_ОтчетБаланс_Свод.Записывать = Истина;
	Запрос = Новый Запрос;
	Запрос.Текст = СформироватьЗапросСверткиБаланса();
	Запрос.УстановитьПараметр("НачалоПериода",     НачМесяца);
	Запрос.УстановитьПараметр("КонецПериода",      КонецДня(КонМесяца));
	Запрос.УстановитьПараметр("Организация",       Организация);
	Запрос.УстановитьПараметр("ВключатьДочерние",  ВключатьДочерние);
	Запрос.УстановитьПараметр("ИсключенныеСтатьи", ИсключенныеСтатьи);
	Движения.А_ОтчетБаланс_Свод.Загрузить(Запрос.Выполнить().Выгрузить());
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Етап 1 (MVP, OD-8): чистий ПАП, 0 JOIN, 0 залежності від ИдентификаторФинЗаписи.
// Суми завжди з ПАП.ОстаткиИОбороты. Аналитика (composite) → типізовані виміри
// через ВЫРАЗИТЬ ТІЛЬКИ в SELECT; СГРУППИРОВАТЬ ПО Б.Аналитика як-є (інакше втрата Σ).
// Субконто реально лягає для ОЗ (ОбъектыЭксплуатации) і НМА (НематериальныйАктив);
// інше — суми по статті, Аналитика3=ПРЕДСТАВЛЕНИЕ (аудит). Source=ПАП (OD-1 Етап 1).
// Фільтр НЕ Статья В ИЕРАРХИИ(&ИсключенныеСтатьи) (OD-3). Перевірено Test 1.
Функция СформироватьЗапросСверткиБаланса()
	Возврат
	"ВЫБРАТЬ
	|	Б.Организация КАК Организация,
	|	Б.Подразделение КАК Подразделение,
	|	Б.Статья КАК Статья,
	|	ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.Номенклатура)        КАК Номенклатура,
	|	ЕСТЬNULL(ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.Контрагенты),
	|	         ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.Организации)) КАК Контрагент,
	|	ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.Партнеры)             КАК Партнер,
	|	ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.Склады)               КАК Склад,
	|	ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.ОбъектыЭксплуатации)  КАК ОбъектыЭксплуатации,
	|	ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.ФизическиеЛица)       КАК ФизическоеЛицо,
	|	ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.БанковскиеСчетаОрганизаций) КАК ДенежныеСредства,
	|	ВЫРАЗИТЬ(Б.Аналитика КАК Справочник.НематериальныеАктивы) КАК НематериальныйАктив,
	|	ЗНАЧЕНИЕ(Справочник.ДоговорыКонтрагентов.ПустаяСсылка)    КАК Договор,
	|	ЗНАЧЕНИЕ(Справочник.ОбъектыРасчетов.ПустаяСсылка)         КАК ОбъектРасчетов,
	|	ЗНАЧЕНИЕ(Перечисление.А_ИсточникБаланса.ПрочиеАктивыПассивы) КАК Source,
	|	"""" КАК Аналитика1,
	|	"""" КАК Аналитика2,
	|	ПРЕДСТАВЛЕНИЕ(Б.Аналитика) КАК Аналитика3,
	|	СУММА(Б.СуммаНачальныйОстаток) КАК СуммаНачальныйОстаток,
	|	СУММА(Б.СуммаПриход)           КАК СуммаПриход,
	|	СУММА(Б.СуммаРасход)           КАК СуммаРасход,
	|	СУММА(Б.СуммаКонечныйОстаток)  КАК СуммаКонечныйОстаток
	|ИЗ РегистрНакопления.ПрочиеАктивыПассивы.ОстаткиИОбороты(
	|		&НачалоПериода, &КонецПериода, Авто, ,
	|		(ВЫБОР КОГДА &ВключатьДочерние
	|			   ТОГДА Организация В ИЕРАРХИИ (&Организация)
	|			   ИНАЧЕ Организация = &Организация КОНЕЦ)
	|		И НЕ Статья В ИЕРАРХИИ (&ИсключенныеСтатьи)) КАК Б
	|СГРУППИРОВАТЬ ПО Б.Организация, Б.Подразделение, Б.Статья, Б.Аналитика
	|ИМЕЮЩИЕ НЕ (СУММА(Б.СуммаНачальныйОстаток)=0 И СУММА(Б.СуммаПриход)=0
	|		И СУММА(Б.СуммаРасход)=0 И СУММА(Б.СуммаКонечныйОстаток)=0)";
КонецФункции

#КонецОбласти

#КонецЕсли
```

- [ ] **Step 2: Copy back у main config (Rule #4)**

```bash
cp "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b/Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl" "C:/Configuration_downloads/BASERP25/Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl"
```
Expected: без помилок.

- [ ] **Step 3: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl && git commit -m "feat: ObjectModule Етап 1 — одношаровий ПАП, 17 вимірів, фільтр виключення (OD-8)"
```
Expected: commit створено.

---

### Task 3: Завантажити в БД (cf-validate → db-load-xml → db-update)

**Files:** (без змін файлів — операції з БД; БД заблокована Конфигуратором → попросити користувача закрити, **не обходити**)

- [ ] **Step 1: cf-validate worktree**

Run:
```powershell
powershell.exe -NoProfile -File "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\.claude\skills\cf-validate\scripts\cf-validate.ps1" -ConfigPath "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b" -MaxErrors 30
```
Expected: `0 errors` (для `Documents/А_ФинРез_Баланс`). Якщо помилки в ObjectModule — виправити, повторити Task 2 Step 1.

- [ ] **Step 2: db-load-xml Partial (тільки ObjectModule.bsl)**

Run:
```powershell
powershell.exe -NoProfile -File "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\.claude\skills\db-load-xml\scripts\db-load-xml.ps1" -V8Path "C:\Program Files\1cv8\8.3.20.1914\bin" -InfoBaseServer "SQLSERVER" -InfoBaseRef "BaseERP" -UserName "Администратор" -Password "24043" -ConfigDir "C:\Configuration_downloads\BASERP25" -Mode Partial -Files "Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl"
```
Expected: `Load completed successfully`. Якщо «база заблокована» — STOP, попросити користувача закрити Конфигуратор, повторити.

- [ ] **Step 3: db-update Dynamic+**

Run:
```powershell
powershell.exe -NoProfile -File "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\.claude\skills\db-update\scripts\db-update.ps1" -V8Path "C:\Program Files\1cv8\8.3.20.1914\bin" -InfoBaseServer "SQLSERVER" -InfoBaseRef "BaseERP" -UserName "Администратор" -Password "24043" -Dynamic "+"
```
Expected: `Обновление конфигурации успешно завершено`.

---

### Task 4: Test 2 — проведення документа через COM

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_t2_posting.py`

- [ ] **Step 1: Написати Test 2**

```python
# -*- coding: utf-8 -*-
"""Test 2: проведення А_ФинРез_Баланс Етап 1 — 4 ресурси балансуються, idempotent."""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime

CONN = 'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"'
MONTH = datetime(2026, 2, 1, 12, 0, 0)   # TZ-safe
DDATE = datetime(2026, 2, 28, 14, 0, 0)

pythoncom.CoInitialize()
conn = win32com.client.Dispatch("V83.COMConnector").Connect(CONN)

# cleanup попередніх документів
q = conn.NewObject("Запрос")
q.Текст = "ВЫБРАТЬ Ссылка КАК С ИЗ Документ.А_ФинРез_Баланс"
tz = q.Выполнить().Выгрузить()
for i in range(tz.Количество()):
    tz.Получить(i).С.ПолучитьОбъект().Удалить()

q0 = conn.NewObject("Запрос")
q0.Текст = 'ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Справочник.Организации ГДЕ КодПоЕДРПОУ = "40645273"'
s = q0.Выполнить().Выбрать(); s.Следующий(); org = s.С

doc = conn.Документы.А_ФинРез_Баланс.СоздатьДокумент()
doc.Дата = DDATE; doc.Организация = org; doc.Месяц = MONTH; doc.ВключатьДочерние = False
mode = conn.PredefinedValue("РежимЗаписиДокумента.Проведение")
doc.Записать(mode)
assert doc.Проведен, "FAIL: документ не проведено"
ref = doc.Ссылка
print(f"[OK] проведено №{doc.Номер}")

q2 = conn.NewObject("Запрос")
q2.Текст = """
ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК Кол,
    СУММА(СуммаНачальныйОстаток) КАК sN, СУММА(СуммаПриход) КАК sP,
    СУММА(СуммаРасход) КАК sR, СУММА(СуммаКонечныйОстаток) КАК sK
ИЗ РегистрСведений.А_ОтчетБаланс_Свод ГДЕ Регистратор = &Д
"""
q2.УстановитьПараметр("Д", ref)
r = q2.Выполнить().Выбрать(); r.Следующий()
total = int(r.Кол)
print(f"рядків={total}, sN={r.sN:,.2f}, sP={r.sP:,.2f}, sR={r.sR:,.2f}, sK={r.sK:,.2f}")
assert total > 0, "FAIL: 0 рядків у регістрі"
assert abs(float(r.sK) - (float(r.sN)+float(r.sP)-float(r.sR))) < 0.01, "FAIL: 4 ресурси не балансуються"

# idempotency — перепровести, перечитати об'єкт заново
obj2 = ref.ПолучитьОбъект(); obj2.Записать(mode)
r2 = q2.Выполнить().Выбрать(); r2.Следующий()
assert int(r2.Кол) == total, f"FAIL: не idempotent {r2.Кол} != {total}"
print(f"[OK] idempotent: {r2.Кол} рядків")
print("PASS Test 2")
```

- [ ] **Step 2: Запустити Test 2**

Run: `python "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\_Rarzrabotki\Python\test\test_balans_t2_posting.py"`
Expected: `[OK] проведено`, `4 ресурси балансуються`, `[OK] idempotent`, `PASS Test 2`.

- [ ] **Step 3: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add _Rarzrabotki/Python/test/test_balans_t2_posting.py && git commit -m "test: Test 2 проведення Етап 1 (PASS, idempotent)"
```
Expected: commit створено.

---

### Task 5: Test 3 ⭐ — звірка регістр↔ПАП до копійки + Актив=Пасив після фільтра

Критерій успіху Етапу 1 (OD-5). Фільтр виключення — **той самий** з обох боків.

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_t3_vs_pap.py`

- [ ] **Step 1: Написати Test 3**

```python
# -*- coding: utf-8 -*-
"""Test 3 (OD-5): Σ регістра по Статья == ПАП.ОстаткиИОбороты по Статья
з ІДЕНТИЧНИМ фільтром виключення з обох боків, до копійки; Актив=Пасив після фільтра."""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime

CONN = 'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"'
D1 = datetime(2026, 2, 1, 12, 0, 0)
D2 = datetime(2026, 2, 28, 23, 59, 59)
TOL = 0.01
EXCL = ["Собственные средства", "Доходы текущего периода", "Расходы текущего периода"]

pythoncom.CoInitialize()
conn = win32com.client.Dispatch("V83.COMConnector").Connect(CONN)

q0 = conn.NewObject("Запрос")
q0.Текст = 'ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка КАК С ИЗ Справочник.Организации ГДЕ КодПоЕДРПОУ = "40645273"'
s = q0.Выполнить().Выбрать(); s.Следующий(); org = s.С

qx = conn.NewObject("Запрос")
qx.Текст = "ВЫБРАТЬ Ссылка КАК С ИЗ ПланВидовХарактеристик.СтатьиАктивовПассивов ГДЕ Наименование В (&Сп)"
qx.УстановитьПараметр("Сп", EXCL)
excl_refs = qx.Выполнить().Выгрузить().ВыгрузитьКолонку("С")

def key(ref):
    try: return str(conn.XMLСтрока(ref))
    except Exception: return repr(ref)

# 1) регістр (по регістраторах-документах за лютий/ТОВ)
qr = conn.NewObject("Запрос")
qr.Текст = """
ВЫБРАТЬ Статья КАК Статья, СУММА(СуммаКонечныйОстаток) КАК sK
ИЗ РегистрСведений.А_ОтчетБаланс_Свод
ГДЕ Регистратор В (ВЫБРАТЬ Ссылка ИЗ Документ.А_ФинРез_Баланс
                   ГДЕ Организация = &Орг И Дата МЕЖДУ &Д1 И &Д2
                     И НЕ ПометкаУдаления И Проведен)
СГРУППИРОВАТЬ ПО Статья
"""
qr.УстановитьПараметр("Орг", org); qr.УстановитьПараметр("Д1", D1); qr.УстановитьПараметр("Д2", D2)
tz1 = qr.Выполнить().Выгрузить()
reg = {}; reg_total = 0.0
for i in range(tz1.Количество()):
    rr = tz1.Получить(i); v = float(rr.sK or 0); reg[key(rr.Статья)] = v; reg_total += v
print(f"регістр: {len(reg)} статей, Σ КонОст = {reg_total:,.2f}")

# 2) ПАП.ОстаткиИОбороты з ТИМ САМИМ фільтром виключення
qp = conn.NewObject("Запрос")
qp.Текст = """
ВЫБРАТЬ Б.Статья КАК Статья, СУММА(Б.СуммаКонечныйОстаток) КАК sK
ИЗ РегистрНакопления.ПрочиеАктивыПассивы.ОстаткиИОбороты(
        &Д1, &Д2, Авто, ,
        Организация = &Орг И НЕ Статья В ИЕРАРХИИ (&Искл)) КАК Б
СГРУППИРОВАТЬ ПО Б.Статья
ИМЕЮЩИЕ СУММА(Б.СуммаКонечныйОстаток) <> 0
"""
qp.УстановитьПараметр("Орг", org); qp.УстановитьПараметр("Д1", D1)
qp.УстановитьПараметр("Д2", D2); qp.УстановитьПараметр("Искл", excl_refs)
tz2 = qp.Выполнить().Выгрузить()
pap = {}; pap_total = 0.0
for i in range(tz2.Количество()):
    rr = tz2.Получить(i); v = float(rr.sK or 0); pap[key(rr.Статья)] = v; pap_total += v
print(f"ПАП: {len(pap)} статей, Σ КонОст = {pap_total:,.2f}")

# 3) звірка по статтях до копійки
diffs = []
for k in set(reg) | set(pap):
    d = abs(reg.get(k, 0.0) - pap.get(k, 0.0))
    if d > TOL: diffs.append((k, reg.get(k,0.0), pap.get(k,0.0), d))
if diffs:
    print(f"FAIL: {len(diffs)} статей розбіжні")
    for k,a,b,d in sorted(diffs, key=lambda x:-x[3])[:10]:
        print(f"  {k[:46]:<48} рег={a:>16,.2f} ПАП={b:>16,.2f} Δ={d:>13,.2f}")
    sys.exit(1)

# 4) Актив=Пасив ПІСЛЯ фільтра (Σ КонОст ≈ 0)
assert abs(reg_total) < 1.0, f"FAIL: Актив≠Пасив у регістрі після фільтра (Σ={reg_total:,.2f})"
print(f"PASS Test 3 — {len(set(reg)|set(pap))} статей зійшлись до {TOL}; Актив=Пасив (Σ={reg_total:,.2f})")
```

- [ ] **Step 2: Запустити Test 3**

Run: `python "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\_Rarzrabotki\Python\test\test_balans_t3_vs_pap.py"`
Expected: `PASS Test 3 — N статей зійшлись до 0.01; Актив=Пасив (Σ≈0.00)`.

- [ ] **Step 3: Якщо FAIL — діагностика (не правити «наосліп»)**

- Розбіжність по статтях → перевірити: `КонецПериода = КонецДня(КонМесяца)` у BSL == `D2` у тесті; `EXCL` у BSL Task 2 == `EXCL` у тесті (той самий фільтр!); `СГРУППИРОВАТЬ ПО Б.Аналитика` (НЕ по ВЫРАЗИТЬ — інакше втрата Σ).
- Виправити BSL → Task 2 Step 1 → Task 3 (db-load) → Task 5 повторно.

- [ ] **Step 4: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add _Rarzrabotki/Python/test/test_balans_t3_vs_pap.py && git commit -m "test: Test 3 звірка з ПАП до копійки + Актив=Пасив (PASS, критерій Етапу 1)"
```
Expected: commit створено.

---

### Task 6: Закриття Етапу 1 — фінальний прогон + commit (самодостатній deliverable, OD-8)

Етап 1 закривається **незалежно** від результату GATE OD-2.

**Files:**
- Modify: main config (copy back, Rule #4)

- [ ] **Step 1: Copy back усі змінені файли в main config**

```bash
cp "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b/Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl" "C:/Configuration_downloads/BASERP25/Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl"
```
Expected: без помилок.

- [ ] **Step 2: Фінальний прогон 3 тестів поспіль**

Run:
```bash
python "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\_Rarzrabotki\Python\test\test_balans_t1_query.py" && python "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\_Rarzrabotki\Python\test\test_balans_t2_posting.py" && python "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\_Rarzrabotki\Python\test\test_balans_t3_vs_pap.py"
```
Expected: `PASS Test 1`, `PASS Test 2`, `PASS Test 3`.

- [ ] **Step 3: Фінальний commit Етапу 1**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add -A && git commit -m "feat: А_ФинРез_Баланс Етап 1 завершено — баланс по статтях == ПАП до копійки (самодостатній, OD-8)"
```
Expected: commit створено (або «nothing to commit» якщо все вже закомічено — тоді Етап 1 уже закритий).

---

### Task 7: GATE OD-2 (термінальний міст) — придатність `ИдентификаторФинЗаписи`

Блокуючий GATE. Визначає, чи можливі Етапи 2-4. **Без silent-fallback.**

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_gate_od2.py`

- [ ] **Step 1: Написати GATE-тест**

```python
# -*- coding: utf-8 -*-
"""GATE OD-2: чи заповнюється ИдентификаторФинЗаписи у ПАП і чи дає 1:1
з регістрами-джерелами Етапу 2 (РасчетыСКлиентами / РасчетыСПоставщиками)."""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client, pythoncom
from datetime import datetime

CONN = 'Srvr="SQLSERVER";Ref="BaseERP";Usr="Администратор";Pwd="24043"'
D2 = datetime(2026, 2, 28, 23, 59, 59)
pythoncom.CoInitialize()
conn = win32com.client.Dispatch("V83.COMConnector").Connect(CONN)

# 1) чи взагалі заповнене поле в ПАП.Остатки
q = conn.NewObject("Запрос")
q.Текст = """
ВЫБРАТЬ
  КОЛИЧЕСТВО(*) КАК Всего,
  СУММА(ВЫБОР КОГДА Б.ИдентификаторФинЗаписи <> "" ТОГДА 1 ИНАЧЕ 0 КОНЕЦ) КАК Заполнено
ИЗ РегистрНакопления.ПрочиеАктивыПассивы.Остатки(&Д) КАК Б
"""
q.УстановитьПараметр("Д", D2)
r = q.Выполнить().Выбрать(); r.Следующий()
vsego = int(r.Всего); zap = int(r.Заполнено or 0)
print(f"ПАП.Остатки: всього={vsego}, з ИдентификаторФинЗаписи<>''={zap}")

GATE_PASS = vsego > 0 and zap > 0 and (zap / max(vsego, 1)) > 0.5
if GATE_PASS:
    print("GATE OD-2: PASS — ИдентификаторФинЗаписи заповнюється. "
          "Етапи 2-4 РОЗБЛОКОВАНО. Згенерувати окремий план stages 2-4.")
else:
    print("GATE OD-2: FAIL — ИдентификаторФинЗаписи НЕ придатний як ключ JOIN.")
    print("Етапи 2-4 ЗАБЛОКОВАНО (OD-2). НЕ робити silent-fallback.")
    print("ЕСКАЛАЦІЯ ФІНАНСИСТУ — альтернативні ключі (вибір за фінансистом):")
    print("  (а) Регистратор + Контрагент;")
    print("  (б) АналитикаУчетаПоПартнерам / СтатьиРасходов (як штатна ДвиженияАктивовПассивы).")
    print("Після рішення фінансиста — повторити пре-тест для нового ключа, тоді план stages 2-4.")
print("[DECISION RECORDED]")
```

- [ ] **Step 2: Запустити GATE**

Run: `python "C:\Configuration_downloads\BASERP25\.claude\worktrees\peaceful-knuth-51295b\_Rarzrabotki\Python\test\test_balans_gate_od2.py"`
Expected: рядок `GATE OD-2: PASS ...` **або** `GATE OD-2: FAIL ...` + блок ескалації. (Обидва результати валідні — це GATE, не тест на «зелене».)

- [ ] **Step 3: Зафіксувати рішення GATE для користувача/фінансиста**

- **PASS** → повідомити: «GATE OD-2 PASS. Етапи 2-4 розблоковано. Наступний крок — згенерувати окремий план `2026-05-15-balans-svod-stages2-4.md` через writing-plans з канону (ключ = `ИдентификаторФинЗаписи`).»
- **FAIL** → повідомити: «GATE OD-2 FAIL. Етапи 2-4 ЗАБЛОКОВАНО. Ескалація фінансисту: обрати альтернативний ключ (Регистратор+Контрагент / АналитикаУчетаПоПартнерам). Етап 1 — робочий результат незалежно (OD-8).» **НЕ продовжувати до Етапів 2-4.**

- [ ] **Step 4: Commit**

```bash
cd "C:/Configuration_downloads/BASERP25/.claude/worktrees/peaceful-knuth-51295b" && git add _Rarzrabotki/Python/test/test_balans_gate_od2.py && git commit -m "test: GATE OD-2 — придатність ИдентификаторФинЗаписи (рішення зафіксовано)"
```
Expected: commit створено.

---

## Наступний цикл (поза цим планом)

**Етапи 2-4 — окремий план**, генерується через writing-plans з канону §8 **тільки якщо GATE OD-2 (Task 7) = PASS** (або після рішення фінансиста по альтернативному ключу при FAIL). Причина відокремлення: точний ключ JOIN — вихід GATE за дизайном (OD-2); концертні bite-sized задачі JOIN неможливо написати без результату GATE без заборонених плейсхолдерів. Етап 1 (Tasks 0-6) — самодостатній робочий deliverable незалежно від цього (OD-8).

---

## Self-Review

**1. Spec coverage (канон v1.1):**
- §2 структура з БД, без meta-edit → Task 0 Step 3, Task 2 (17 вимірів, AI не міняє структуру) ✅
- §3 підхід B, ПАП єдине джерело, ВЫРАЗИТЬ тільки в SELECT, GROUP BY Б.Аналитика → Task 2 `СформироватьЗапросСверткиБаланса`, Task 5 Step 3 діагностика ✅
- §3 п.5 / OD-8 Етап 1 = 0 JOIN, самодостатній → Task 2 (без JOIN), Task 6 (закриття незалежно від GATE) ✅
- OD-1 Source=ПАП Етап 1 → Task 2 `ЗНАЧЕНИЕ(Перечисление.А_ИсточникБаланса.ПрочиеАктивыПассивы)` ✅
- OD-2 Вимога 1 блокуючий GATE, без silent-fallback, ескалація → Task 7 ✅
- OD-3 фільтр 3 груп через В ИЕРАРХИИ, той самий у BSL і Test 3, Прибыли и убытки лишається → Task 1 (підбір+перевірка), Task 2 (`&ИсключенныеСтатьи`), Task 5 (той самий EXCL) ✅
- OD-4 Етап 1 = статті 1-6 (ПАП-прямий розклад; ОЗ/НМА субконто) → Task 1/2 ✅
- OD-5 Test 3 регістр==ПАП до копійки + Актив=Пасив ПІСЛЯ фільтра, лютий/ТОВ → Task 5 ✅
- OD-6 sync main→worktree, git розрулено явно → Task 0 ✅
- OD-8 Вимога 2 Етап 1 самодостатній → Task 6 Step 3 (commit незалежний від GATE), розділ «Наступний цикл» ✅
- §7 інваріантний цикл Rule #-1 (пре-тест→BSL→cp→cf-validate→db-load→db-update→Test2→Test3→commit) → Tasks 1→2→3→4→5→6 ✅
- Етапи 2-4 — навмисно окремий план (канон §8, скоуп-чек skill) ✅

**2. Placeholder scan:** немає TBD/TODO; весь BSL і Python — повний код; усі команди з очікуваним виводом; `EXCL`/`ИсключенныеСтатьи` визначені конкретним списком (Task 1 його верифікує, Task 2 використовує той самий).

**3. Type/naming consistency:** імена 17 вимірів і 4 ресурсів однакові в Task 2 (BSL SELECT) і Task 4/5 (тести); `СформироватьЗапросСверткиБаланса` — одна назва; параметр `&ИсключенныеСтатьи` (BSL) ↔ `&Искл` (Python-тести) — різні простори імен, узгоджені в межах кожного; фільтр `НЕ Статья В ИЕРАРХИИ` ідентичний у Task 2 і Task 5; org за `КодПоЕДРПОУ="40645273"` скрізь; період `datetime(2026,2,*,12/23,...)` консистентний.

Гепів немає. План готовий.
