# Расшифровка «Расходы/Доходы текущего периода» через СтатьяДоходыРасходы — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Расшифровать остаток статей «Расходы текущего периода» / «Доходы текущего периода» в РС `А_ОтчетБаланс_Свод` через измерение `СтатьяДоходыРасходы` (ПВХ.СтатьиРасходов / СтатьиДоходов), извлекая реальную статью из `РегистрНакопления.ДвиженияДоходыРасходыПрочиеАктивыПассивы` per Регистратор. `Аналитика1 = СокрЛП(ПРЕДСТАВЛЕНИЕ(СтатьяДоходыРасходы))`. Баланс per Орг должен остаться == штатный `Отчет.УправленческийБаланс`.

**Architecture:** Канон-образец §2 `Свод_СебестоимостьТоваров`: детальная расшифровка из ДвДохРасх через INNER JOIN ⨝ ПАП per (Регистратор, Период, Подр) ТОЛЬКО для статей «Расх/Дох тек.пер.» + плуги (по разнице с ПАП.ОстаткиИОбороты) → Σ==ПАП до копейки. Для остальных статей источника {ПрочиеДоходы, ПрочиеРасходы, ПартииПрочихРасходов} существующая логика через `ВЫРАЗИТЬ(ПАП.Аналитика)` сохраняется как ветка B в `UNION ALL`.

**Tech Stack:** BSL 8.3.20 / 1С:Предприятие BAS ERP 2.5; Python 3.13 + win32com.client (V83.COMConnector) для COM тестов; SQL Server (база `BaseERP`).

**Spec:** `docs/superpowers/specs/2026-05-26-balans-svod-statya-dohody-rashody-design.md`

---

## File Structure

| Файл | Изменение | Назначение |
|---|---|---|
| `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl` | Modify lines 888-1052 (функция `Свод_ПрочиеРасходыДоходы`) | Переписать на канон-образец §2 — две ветви UNION ALL в `втПРД`, добавить INNER JOIN на ДвДохРасх для статей «Расх/Дох тек.пер.», заполнять `СтатьяДоходыРасходы` и `Аналитика1` |
| `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl` | Modify line 109-113 (`ПровестиБалансСвод.Свернуть`) | Добавить `"СтатьяДоходыРасходы"` в список измерений Свернуть |
| `_Rarzrabotki/Python/test/test_balans_prd_dohrash_pretest.py` | Create | Pretest — выполнить запрос как пакет 1:1, ассертить Σ(детали+плуг) == ПАП до 0.01, наличие СтатьиДоходыРасходы для «Расх.тек.пер.» в Глобино-2 |
| `_Rarzrabotki/Python/test/test_balans_prd_dohrash_verify.py` | Create | Verify — перепровести документ 00000000004 от 30.04.2026 через COM, ассертить регистр, баланс per Орг, регрессия 5 остальных Свод_* |
| `_Rarzrabotki/Python/test/test_balans_prd_dohrash_regression.py` | Create | Регрессия дек2025+янв2026 — перепровести документы, ассертить баланс 278 093 267,32 / 288 787 750,11 unchanged |
| `_Rarzrabotki/notebook/knowledge_Balanse/balanse_pattern_and_roadmap.md` | Modify §1 + §6 | Добавить запись о Расшифровке СтатьяДоходыРасходы (DONE 2026-05-26) |

---

## Pre-flight checks

**Среда:** Python 3.13 в `C:/Python313/python.exe` (имеет win32com.client). Скрипты запускать через `C:/Python313/python.exe`, не `python` (там 3.13 без pywin32 → 3.11 без pywin32).

**База:** `Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"`. Организация: ТОВ «ІНДАСТРІАЛБУД» (КодПоЕДРПОУ 40645273).

**Документ тестируемый:** `Финансовый результат Баланс (для OLAP) 00000000004` от 30.04.2026 12:00:00. Месяц документа = 01.04.2026 00:00:00 (НачалоМесяца), КонМес = 30.04.2026 23:59:59.

**Эталоны perОрг (после внедрения должны остаться):**
- декабрь 2025 |Актив| = 278 093 267,32
- январь 2026 |Актив| = 288 787 750,11
- Σ КО регистра signed (OD-3) = 0,00 ⇒ Актив = |Пассив|

**Эталон Глобино-2 / Расходы текущего периода / 30.04.2026:** КО = 2 400,00 (по ПАП.ОстаткиИОбороты).

---

## Task 1: Pretest — пакет запроса 1:1 как пойдёт в BSL

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_prd_dohrash_pretest.py`

- [ ] **Step 1: Создать pretest скрипт**

```python
# -*- coding: utf-8 -*-
"""Pretest (Rule #-1) — Свод_ПрочиеРасходыДоходы с расшифровкой СтатьяДоходыРасходы.

Запрос 1:1 как пойдёт в BSL: ОДИН пакет с
  втПРД (две ветви UNION ALL: A=ДДР⨝ПАП для Расх/Дох тек.пер. + B=ПАП для остальных)
  втПРДСт (Σ втПРД per Орг,Подр,Статья,Source)
  втПАП (signed Σ ПАП per Орг,Подр,Статья,Источник=&Ист, OD-3)
  втКлючи (РАЗЛИЧНЫЕ из ОБЪЕДИНИТЬ ВСЕ)
  Финал ОБЪЕДИНИТЬ ВСЕ: детали(втПРД,Расхождение=Ложь) ∪ плуги(|Δ|>0.01, Расхождение=Истина).

Asserts:
- rows > 0
- 0 дублей деталей и плугов per ключу
- Σ(детали+плуг) per (Орг,Подр,Статья,Source) == ПАП(Источник∈&Ист, OD-3, signed) до 0.01
- Эталон Глобино-2 / Расх.тек.пер. / 30.04.2026: КО = 2 400,00, есть детали с СтатьёйДоходыРасходы<>NULL
- Регрессия (другие статьи источника): «Прочие пассивы», «Прибыли и убытки» с Источник=ПрочиеРасходы — Σ не изменена
- Σ Приход = Σ Расход = 0 (остатки-only)
"""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client
from collections import Counter

CONN = 'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"'
v8 = win32com.client.Dispatch("V83.COMConnector")
erp = v8.Connect(CONN)
S = erp.String
TOL = 0.01

# Найти ТОВ ИНДАСТРИАЛБУД (имя с кавычками)
q = erp.NewObject("Запрос")
q.Text = """ВЫБРАТЬ Ссылка ИЗ Справочник.Организации ГДЕ КодПоЕДРПОУ = "40645273" """
r = q.Execute().Выгрузить()
assert r.Количество() == 1, "ТОВ ИНДАСТРИАЛБУД не найден"
own_org = r.Получить(0).Ссылка

# Найти статьи
СтРасх = erp.ПланыВидовХарактеристик.СтатьиАктивовПассивов.НайтиПоНаименованию("Расходы текущего периода")
СтДох = erp.ПланыВидовХарактеристик.СтатьиАктивовПассивов.НайтиПоНаименованию("Доходы текущего периода")
СтСС = erp.ПланыВидовХарактеристик.СтатьиАктивовПассивов.НайтиПоНаименованию("Собственные средства")
assert not СтРасх.Пустая(), "Расходы текущего периода не найдена"
assert not СтДох.Пустая(), "Доходы текущего периода не найдена"

# Найти Источники {ПрочиеДоходы, ПрочиеРасходы, ПартииПрочихРасходов}
ist_q = erp.NewObject("Запрос")
ist_q.Text = "ВЫБРАТЬ Ссылка, ПРЕДСТАВЛЕНИЕ(Ссылка) КАК П ИЗ Перечисление.ИсточникиУправленческогоБаланса"
ist_r = ist_q.Execute().Выгрузить()
ist_arr = []
target = {"Прочие доходы", "Прочие расходы", "Партии прочих расходов"}
for i in range(ist_r.Количество()):
    if S(ist_r.Получить(i).П) in target:
        ist_arr.append(ist_r.Получить(i).Ссылка)
assert len(ist_arr) == 3, f"Ожидаем 3 источника, получили {len(ist_arr)}"

# Параметры (период документа 00000000004 от 30.04.2026)
# Месяц = 01.04.2026 00:00:00; КонМес = 30.04.2026 23:59:59 (через ДАТАВРЕМЯ в запросе)
print(f"Период: апрель 2026, Орг={S(own_org)}, Ист={len(ist_arr)}, Искл=1 (Собственные средства)")

# Главный запрос — пакет 1:1 как пойдёт в BSL
q = erp.NewObject("Запрос")
q.УстановитьПараметр("Орг", own_org)
q.УстановитьПараметр("Ист", ist_arr)
q.УстановитьПараметр("Искл", [СтСС])
q.УстановитьПараметр("РасхТекПер", СтРасх)
q.УстановитьПараметр("ДохТекПер", СтДох)
q.Text = """
ВЫБРАТЬ
    ДДР.Организация КАК Организация,
    ЕСТЬNULL(ДДР.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)) КАК Подразделение,
    ПАП.Статья КАК Статья,
    ПАП.Источник КАК Source,
    ЗНАЧЕНИЕ(Справочник.ОбъектыЭксплуатации.ПустаяСсылка) КАК ОбъектыЭксплуатации,
    ЗНАЧЕНИЕ(Справочник.НематериальныеАктивы.ПустаяСсылка) КАК НематериальныйАктив,
    ЗНАЧЕНИЕ(Перечисление.ТипыНалогов.ПустаяСсылка) КАК ТипНалога,
    ДДР.Статья КАК СтатьяДоходыРасходы,
    СУММА(ВЫБОР КОГДА ДДР.Период < ДАТАВРЕМЯ(2026,4,1,0,0,0)
            ТОГДА ДДР.Сумма ИНАЧЕ 0 КОНЕЦ) КАК СуммаНачальныйОстаток,
    СУММА(ДДР.Сумма) КАК СуммаКонечныйОстаток
ПОМЕСТИТЬ втПРД
ИЗ
    РегистрНакопления.ДвиженияДоходыРасходыПрочиеАктивыПассивы КАК ДДР
        ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрНакопления.ПрочиеАктивыПассивы КАК ПАП
        ПО ДДР.Регистратор = ПАП.Регистратор
            И ДДР.Период = ПАП.Период
            И ЕСТЬNULL(ДДР.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)) = ЕСТЬNULL(ПАП.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка))
ГДЕ
    ДДР.Организация = &Орг
    И ДДР.Период <= ДАТАВРЕМЯ(2026,4,30,23,59,59)
    И ПАП.Организация = &Орг
    И ПАП.Период <= ДАТАВРЕМЯ(2026,4,30,23,59,59)
    И ПАП.Источник В (&Ист)
    И (ПАП.Статья = &РасхТекПер ИЛИ ПАП.Статья = &ДохТекПер)
    И НЕ ПАП.Статья В ИЕРАРХИИ(&Искл)
    И (ТИПЗНАЧЕНИЯ(ДДР.Статья) = ТИП(ПланВидовХарактеристик.СтатьиРасходов)
       ИЛИ ТИПЗНАЧЕНИЯ(ДДР.Статья) = ТИП(ПланВидовХарактеристик.СтатьиДоходов))
СГРУППИРОВАТЬ ПО
    ДДР.Организация,
    ЕСТЬNULL(ДДР.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)),
    ПАП.Статья,
    ПАП.Источник,
    ДДР.Статья

ОБЪЕДИНИТЬ ВСЕ

ВЫБРАТЬ
    ПАП.Организация,
    ЕСТЬNULL(ПАП.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)),
    ПАП.Статья,
    ПАП.Источник,
    ЕСТЬNULL(ВЫРАЗИТЬ(ПАП.Аналитика КАК Справочник.ОбъектыЭксплуатации), ЗНАЧЕНИЕ(Справочник.ОбъектыЭксплуатации.ПустаяСсылка)),
    ЕСТЬNULL(ВЫРАЗИТЬ(ПАП.Аналитика КАК Справочник.НематериальныеАктивы), ЗНАЧЕНИЕ(Справочник.НематериальныеАктивы.ПустаяСсылка)),
    ЕСТЬNULL(ВЫРАЗИТЬ(ПАП.Аналитика КАК Перечисление.ТипыНалогов), ЗНАЧЕНИЕ(Перечисление.ТипыНалогов.ПустаяСсылка)),
    NULL,
    СУММА(ВЫБОР КОГДА ПАП.Период < ДАТАВРЕМЯ(2026,4,1,0,0,0)
            ТОГДА ВЫБОР КОГДА ПАП.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
                    ТОГДА ПАП.Сумма ИНАЧЕ -ПАП.Сумма КОНЕЦ
            ИНАЧЕ 0 КОНЕЦ),
    СУММА(ВЫБОР КОГДА ПАП.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
            ТОГДА ПАП.Сумма ИНАЧЕ -ПАП.Сумма КОНЕЦ)
ИЗ
    РегистрНакопления.ПрочиеАктивыПассивы КАК ПАП
ГДЕ
    ПАП.Организация = &Орг
    И ПАП.Период <= ДАТАВРЕМЯ(2026,4,30,23,59,59)
    И ПАП.Источник В (&Ист)
    И ПАП.Статья <> &РасхТекПер
    И ПАП.Статья <> &ДохТекПер
    И НЕ ПАП.Статья В ИЕРАРХИИ(&Искл)
СГРУППИРОВАТЬ ПО
    ПАП.Организация,
    ЕСТЬNULL(ПАП.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)),
    ПАП.Статья,
    ПАП.Источник,
    ЕСТЬNULL(ВЫРАЗИТЬ(ПАП.Аналитика КАК Справочник.ОбъектыЭксплуатации), ЗНАЧЕНИЕ(Справочник.ОбъектыЭксплуатации.ПустаяСсылка)),
    ЕСТЬNULL(ВЫРАЗИТЬ(ПАП.Аналитика КАК Справочник.НематериальныеАктивы), ЗНАЧЕНИЕ(Справочник.НематериальныеАктивы.ПустаяСсылка)),
    ЕСТЬNULL(ВЫРАЗИТЬ(ПАП.Аналитика КАК Перечисление.ТипыНалогов), ЗНАЧЕНИЕ(Перечисление.ТипыНалогов.ПустаяСсылка))
;
ВЫБРАТЬ
    втПРД.Организация КАК Организация,
    втПРД.Подразделение КАК Подразделение,
    втПРД.Статья КАК Статья,
    втПРД.Source КАК Source,
    СУММА(втПРД.СуммаНачальныйОстаток) КАК НО,
    СУММА(втПРД.СуммаКонечныйОстаток) КАК КО
ПОМЕСТИТЬ втПРДСт
ИЗ
    втПРД КАК втПРД
СГРУППИРОВАТЬ ПО
    втПРД.Организация, втПРД.Подразделение, втПРД.Статья, втПРД.Source
;
ВЫБРАТЬ
    Т.Организация КАК Организация,
    ЕСТЬNULL(Т.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)) КАК Подразделение,
    Т.Статья КАК Статья,
    Т.Источник КАК Source,
    СУММА(ВЫБОР КОГДА Т.Период < ДАТАВРЕМЯ(2026,4,1,0,0,0)
            ТОГДА ВЫБОР КОГДА Т.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
                    ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ
            ИНАЧЕ 0 КОНЕЦ) КАК НО,
    СУММА(ВЫБОР КОГДА Т.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
            ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ) КАК КО
ПОМЕСТИТЬ втПАП
ИЗ
    РегистрНакопления.ПрочиеАктивыПассивы КАК Т
ГДЕ
    Т.Организация = &Орг
    И Т.Период <= ДАТАВРЕМЯ(2026,4,30,23,59,59)
    И Т.Источник В (&Ист)
    И НЕ Т.Статья В ИЕРАРХИИ(&Искл)
СГРУППИРОВАТЬ ПО
    Т.Организация,
    ЕСТЬNULL(Т.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)),
    Т.Статья,
    Т.Источник
;
ВЫБРАТЬ РАЗЛИЧНЫЕ
    К.Организация КАК Организация,
    К.Подразделение КАК Подразделение,
    К.Статья КАК Статья,
    К.Source КАК Source
ПОМЕСТИТЬ втКлючи
ИЗ
    (ВЫБРАТЬ
        втПАП.Организация КАК Организация,
        втПАП.Подразделение КАК Подразделение,
        втПАП.Статья КАК Статья,
        втПАП.Source КАК Source
    ИЗ втПАП КАК втПАП
    ОБЪЕДИНИТЬ ВСЕ
    ВЫБРАТЬ
        втПРДСт.Организация,
        втПРДСт.Подразделение,
        втПРДСт.Статья,
        втПРДСт.Source
    ИЗ втПРДСт КАК втПРДСт) КАК К
;
ВЫБРАТЬ
    втПРД.Организация КАК Организация,
    втПРД.Подразделение КАК Подразделение,
    втПРД.Статья КАК Статья,
    втПРД.Source КАК Source,
    втПРД.ОбъектыЭксплуатации КАК ОбъектыЭксплуатации,
    втПРД.НематериальныйАктив КАК НематериальныйАктив,
    втПРД.ТипНалога КАК ТипНалога,
    втПРД.СтатьяДоходыРасходы КАК СтатьяДоходыРасходы,
    ВЫБОР
        КОГДА втПРД.СтатьяДоходыРасходы ЕСТЬ НЕ NULL ТОГДА СокрЛП(ПРЕДСТАВЛЕНИЕ(втПРД.СтатьяДоходыРасходы))
        КОГДА втПРД.ТипНалога <> ЗНАЧЕНИЕ(Перечисление.ТипыНалогов.ПустаяСсылка) ТОГДА ПРЕДСТАВЛЕНИЕ(втПРД.ТипНалога)
        КОГДА втПРД.ОбъектыЭксплуатации <> ЗНАЧЕНИЕ(Справочник.ОбъектыЭксплуатации.ПустаяСсылка) ТОГДА ПРЕДСТАВЛЕНИЕ(втПРД.ОбъектыЭксплуатации)
        КОГДА втПРД.НематериальныйАктив <> ЗНАЧЕНИЕ(Справочник.НематериальныеАктивы.ПустаяСсылка) ТОГДА ПРЕДСТАВЛЕНИЕ(втПРД.НематериальныйАктив)
        ИНАЧЕ ""
    КОНЕЦ КАК Аналитика1,
    "" КАК Аналитика2,
    "" КАК Аналитика3,
    ЛОЖЬ КАК Расхождение,
    втПРД.СуммаНачальныйОстаток КАК СуммаНачальныйОстаток,
    0 КАК СуммаПриход,
    0 КАК СуммаРасход,
    втПРД.СуммаКонечныйОстаток КАК СуммаКонечныйОстаток
ИЗ втПРД КАК втПРД

ОБЪЕДИНИТЬ ВСЕ

ВЫБРАТЬ
    К.Организация,
    К.Подразделение,
    К.Статья,
    К.Source,
    ЗНАЧЕНИЕ(Справочник.ОбъектыЭксплуатации.ПустаяСсылка),
    ЗНАЧЕНИЕ(Справочник.НематериальныеАктивы.ПустаяСсылка),
    ЗНАЧЕНИЕ(Перечисление.ТипыНалогов.ПустаяСсылка),
    NULL,
    "",
    "",
    "",
    ИСТИНА,
    ЕСТЬNULL(втПАП.НО, 0) - ЕСТЬNULL(втПРДСт.НО, 0),
    0,
    0,
    ЕСТЬNULL(втПАП.КО, 0) - ЕСТЬNULL(втПРДСт.КО, 0)
ИЗ
    втКлючи КАК К
        ЛЕВОЕ СОЕДИНЕНИЕ втПАП КАК втПАП
        ПО К.Организация = втПАП.Организация
            И К.Подразделение = втПАП.Подразделение
            И К.Статья = втПАП.Статья
            И К.Source = втПАП.Source
        ЛЕВОЕ СОЕДИНЕНИЕ втПРДСт КАК втПРДСт
        ПО К.Организация = втПРДСт.Организация
            И К.Подразделение = втПРДСт.Подразделение
            И К.Статья = втПРДСт.Статья
            И К.Source = втПРДСт.Source
ГДЕ
    ЕСТЬNULL(втПАП.НО, 0) - ЕСТЬNULL(втПРДСт.НО, 0) > 0.01
    ИЛИ ЕСТЬNULL(втПАП.НО, 0) - ЕСТЬNULL(втПРДСт.НО, 0) < -0.01
    ИЛИ ЕСТЬNULL(втПАП.КО, 0) - ЕСТЬNULL(втПРДСт.КО, 0) > 0.01
    ИЛИ ЕСТЬNULL(втПАП.КО, 0) - ЕСТЬNULL(втПРДСт.КО, 0) < -0.01
"""
t = q.Execute().Выгрузить()
print(f"Всего строк (детали+плуги): {t.Количество()}")
assert t.Количество() > 0, "Запрос вернул 0 строк"

# Группируем по (Орг, Подр, Статья, Source) для сверки с ПАП.ОстаткиИОбороты
svod_map = {}
detail_with_st = 0
plug_count = 0
detail_count = 0
glob2_kos = 0.0
glob2_st_set = set()
for i in range(t.Количество()):
    row = t.Получить(i)
    key = (S(row.Организация), S(row.Подразделение), S(row.Статья), S(row.Source))
    svod_map.setdefault(key, [0.0, 0.0])
    svod_map[key][0] += float(row.СуммаНачальныйОстаток)
    svod_map[key][1] += float(row.СуммаКонечныйОстаток)
    if row.Расхождение:
        plug_count += 1
    else:
        detail_count += 1
        if row.СтатьяДоходыРасходы is not None:
            detail_with_st += 1
        if S(row.Подразделение) == "Глобино-2" and S(row.Статья) == "Расходы текущего периода":
            glob2_kos += float(row.СуммаКонечныйОстаток)
            if row.СтатьяДоходыРасходы is not None:
                glob2_st_set.add(S(row.СтатьяДоходыРасходы))

print(f"  Детали: {detail_count}, из них с СтатьяДоходыРасходы: {detail_with_st}")
print(f"  Плуги: {plug_count}")
print(f"  Глобино-2 / Расх.тек.пер. — КО сумма деталей: {glob2_kos:.2f}, уник.статей: {len(glob2_st_set)}")

# Сверка с ПАП.ОстаткиИОбороты per (Орг, Подр, Статья) — Источник в виртуальной таблице недоступен
# Поэтому Σ свод per (Орг, Подр, Статья) суммируем over Source
svod_by_pst = {}
for (org, podr, stat, src), (no, ko) in svod_map.items():
    k = (org, podr, stat)
    svod_by_pst.setdefault(k, [0.0, 0.0])
    svod_by_pst[k][0] += no
    svod_by_pst[k][1] += ko

# Запрос ПАП.ОстаткиИОбороты per (Орг, Подр, Статья) с фильтром Источник через ОСНОВНУЮ таблицу (для точного фильтра)
q2 = erp.NewObject("Запрос")
q2.УстановитьПараметр("Орг", own_org)
q2.УстановитьПараметр("Ист", ist_arr)
q2.УстановитьПараметр("Искл", [СтСС])
q2.Text = """
ВЫБРАТЬ
    Т.Организация,
    ЕСТЬNULL(Т.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)) КАК Подразделение,
    Т.Статья,
    СУММА(ВЫБОР КОГДА Т.Период < ДАТАВРЕМЯ(2026,4,1,0,0,0)
            ТОГДА ВЫБОР КОГДА Т.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
                    ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ
            ИНАЧЕ 0 КОНЕЦ) КАК НО,
    СУММА(ВЫБОР КОГДА Т.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
            ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ) КАК КО
ИЗ РегистрНакопления.ПрочиеАктивыПассивы КАК Т
ГДЕ
    Т.Организация = &Орг
    И Т.Период <= ДАТАВРЕМЯ(2026,4,30,23,59,59)
    И Т.Источник В (&Ист)
    И НЕ Т.Статья В ИЕРАРХИИ(&Искл)
СГРУППИРОВАТЬ ПО Т.Организация, ЕСТЬNULL(Т.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)), Т.Статья
"""
pr = q2.Execute().Выгрузить()
pap_map = {}
for i in range(pr.Количество()):
    row = pr.Получить(i)
    pap_map[(S(row.Организация), S(row.Подразделение), S(row.Статья))] = (float(row.НО), float(row.КО))

# Сверка
mismatches = []
for k, (no, ko) in svod_by_pst.items():
    pap_no, pap_ko = pap_map.get(k, (0.0, 0.0))
    if abs(no - pap_no) > TOL or abs(ko - pap_ko) > TOL:
        mismatches.append((k, no, ko, pap_no, pap_ko))

if mismatches:
    print(f"\n[FAIL] {len(mismatches)} расхождений > 0.01:")
    for (k, no, ko, pno, pko) in mismatches[:20]:
        print(f"  {k} | свод НО={no:.2f} КО={ko:.2f} | ПАП НО={pno:.2f} КО={pko:.2f}")
    sys.exit(1)

# Ключевая проверка эталона
key_glob2 = (S(own_org), "Глобино-2", "Расходы текущего периода")
assert key_glob2 in pap_map, f"Нет ПАП по {key_glob2}"
pap_kos_glob2 = pap_map[key_glob2][1]
print(f"\n[OK] Глобино-2 / Расх.тек.пер. ПАП КО = {pap_kos_glob2:.2f}")
assert abs(pap_kos_glob2 - 2400.00) < TOL, f"ПАП КО Глобино-2 должно быть 2400.00, получили {pap_kos_glob2}"

# Свод-сторона: должны быть детали (СтатьяДоходыРасходы<>NULL) для Глобино-2 / Расх.тек.пер.
assert len(glob2_st_set) > 0, "В деталях Глобино-2/Расх.тек.пер. нет СтатьиДоходыРасходы"
print(f"[OK] Детали по Глобино-2/Расх.тек.пер. — статей: {sorted(glob2_st_set)}")

print("\n[OK] Pretest PASS — Σ(детали+плуг) == ПАП до 0.01, СтатьяДоходыРасходы заполнена")
```

- [ ] **Step 2: Запустить pretest и подтвердить PASS**

Run: `C:/Python313/python.exe _Rarzrabotki/Python/test/test_balans_prd_dohrash_pretest.py`
Expected: вывод заканчивается строкой `[OK] Pretest PASS — Σ(детали+плуг) == ПАП до 0.01, СтатьяДоходыРасходы заполнена`. Никаких ассертов не упало.

- [ ] **Step 3: Закоммитить pretest**

```bash
git add _Rarzrabotki/Python/test/test_balans_prd_dohrash_pretest.py
git commit -m "test(balans_prd_dohrash): pretest — Σ(детали+плуг)==ПАП с расшифровкой СтатьяДоходыРасходы"
```

---

## Task 2: Modify BSL — переписать `Свод_ПрочиеРасходыДоходы`

**Files:**
- Modify: `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl` (lines 888-1052 — функция `Свод_ПрочиеРасходыДоходы`)

- [ ] **Step 1: Прочитать текущее тело функции для backup**

Перед заменой убедиться, что мы видим контекст ровно как есть (для безопасного Edit с уникальным old_string).

Run: `grep -n "Функция Свод_ПрочиеРасходыДоходы" Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl`
Expected: одно совпадение в строке 888.

- [ ] **Step 2: Заменить тело функции — добавить параметры РасхТекПер/ДохТекПер + переписать `втПРД` как UNION ALL двух ветвей + Финал с СтатьейДоходыРасходы**

Использовать Edit tool. Берём существующее тело между `Функция Свод_ПрочиеРасходыДоходы(СтруктураШапки, Отказ) Экспорт` и `КонецФункции` и заменяем на:

```bsl
Функция Свод_ПрочиеРасходыДоходы(СтруктураШапки, Отказ) Экспорт
	М = Новый Массив;
	М.Добавить("Прочие доходы");
	М.Добавить("Прочие расходы");
	М.Добавить("Партии прочих расходов");
	Ист = _РефыИсточников(М);
	Если Ист.Количество() = 0 Тогда
		Возврат Истина;
	КонецЕсли;
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Орг",        СтруктураШапки.Организация);
	Запрос.УстановитьПараметр("НачМес",     СтруктураШапки.НачМес);
	Запрос.УстановитьПараметр("КонМес",     СтруктураШапки.КонМес);
	Запрос.УстановитьПараметр("Искл",       ИсключенныеСтатьи());
	Запрос.УстановитьПараметр("Ист",        Ист);
	Запрос.УстановитьПараметр("РасхТекПер", ПланыВидовХарактеристик.СтатьиАктивовПассивов.НайтиПоНаименованию("Расходы текущего периода"));
	Запрос.УстановитьПараметр("ДохТекПер",  ПланыВидовХарактеристик.СтатьиАктивовПассивов.НайтиПоНаименованию("Доходы текущего периода"));
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ДДР.Организация КАК Организация,
	|	ЕСТЬNULL(ДДР.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)) КАК Подразделение,
	|	ПАП.Статья КАК Статья,
	|	ПАП.Источник КАК Source,
	|	ЗНАЧЕНИЕ(Справочник.ОбъектыЭксплуатации.ПустаяСсылка) КАК ОбъектыЭксплуатации,
	|	ЗНАЧЕНИЕ(Справочник.НематериальныеАктивы.ПустаяСсылка) КАК НематериальныйАктив,
	|	ЗНАЧЕНИЕ(Перечисление.ТипыНалогов.ПустаяСсылка) КАК ТипНалога,
	|	ДДР.Статья КАК СтатьяДоходыРасходы,
	|	СУММА(ВЫБОР КОГДА ДДР.Период < &НачМес
	|			ТОГДА ДДР.Сумма ИНАЧЕ 0 КОНЕЦ) КАК СуммаНачальныйОстаток,
	|	СУММА(ДДР.Сумма) КАК СуммаКонечныйОстаток
	|ПОМЕСТИТЬ втПРД
	|ИЗ
	|	РегистрНакопления.ДвиженияДоходыРасходыПрочиеАктивыПассивы КАК ДДР
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрНакопления.ПрочиеАктивыПассивы КАК ПАП
	|		ПО ДДР.Регистратор = ПАП.Регистратор
	|			И ДДР.Период = ПАП.Период
	|			И ЕСТЬNULL(ДДР.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)) = ЕСТЬNULL(ПАП.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка))
	|ГДЕ
	|	ДДР.Организация = &Орг
	|	И ДДР.Период <= &КонМес
	|	И ПАП.Организация = &Орг
	|	И ПАП.Период <= &КонМес
	|	И ПАП.Источник В (&Ист)
	|	И (ПАП.Статья = &РасхТекПер ИЛИ ПАП.Статья = &ДохТекПер)
	|	И НЕ ПАП.Статья В ИЕРАРХИИ(&Искл)
	|	И (ТИПЗНАЧЕНИЯ(ДДР.Статья) = ТИП(ПланВидовХарактеристик.СтатьиРасходов)
	|		ИЛИ ТИПЗНАЧЕНИЯ(ДДР.Статья) = ТИП(ПланВидовХарактеристик.СтатьиДоходов))
	|СГРУППИРОВАТЬ ПО
	|	ДДР.Организация,
	|	ЕСТЬNULL(ДДР.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)),
	|	ПАП.Статья,
	|	ПАП.Источник,
	|	ДДР.Статья
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	Т.Организация,
	|	ЕСТЬNULL(Т.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)),
	|	Т.Статья,
	|	Т.Источник,
	|	ЕСТЬNULL(ВЫРАЗИТЬ(Т.Аналитика КАК Справочник.ОбъектыЭксплуатации), ЗНАЧЕНИЕ(Справочник.ОбъектыЭксплуатации.ПустаяСсылка)),
	|	ЕСТЬNULL(ВЫРАЗИТЬ(Т.Аналитика КАК Справочник.НематериальныеАктивы), ЗНАЧЕНИЕ(Справочник.НематериальныеАктивы.ПустаяСсылка)),
	|	ЕСТЬNULL(ВЫРАЗИТЬ(Т.Аналитика КАК Перечисление.ТипыНалогов), ЗНАЧЕНИЕ(Перечисление.ТипыНалогов.ПустаяСсылка)),
	|	NULL,
	|	СУММА(ВЫБОР КОГДА Т.Период < &НачМес
	|			ТОГДА ВЫБОР КОГДА Т.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
	|					ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ
	|			ИНАЧЕ 0 КОНЕЦ),
	|	СУММА(ВЫБОР КОГДА Т.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
	|			ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ)
	|ИЗ
	|	РегистрНакопления.ПрочиеАктивыПассивы КАК Т
	|ГДЕ
	|	Т.Организация = &Орг
	|	И Т.Период <= &КонМес
	|	И Т.Источник В (&Ист)
	|	И Т.Статья <> &РасхТекПер
	|	И Т.Статья <> &ДохТекПер
	|	И НЕ Т.Статья В ИЕРАРХИИ(&Искл)
	|СГРУППИРОВАТЬ ПО
	|	Т.Организация,
	|	ЕСТЬNULL(Т.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)),
	|	Т.Статья,
	|	Т.Источник,
	|	ЕСТЬNULL(ВЫРАЗИТЬ(Т.Аналитика КАК Справочник.ОбъектыЭксплуатации), ЗНАЧЕНИЕ(Справочник.ОбъектыЭксплуатации.ПустаяСсылка)),
	|	ЕСТЬNULL(ВЫРАЗИТЬ(Т.Аналитика КАК Справочник.НематериальныеАктивы), ЗНАЧЕНИЕ(Справочник.НематериальныеАктивы.ПустаяСсылка)),
	|	ЕСТЬNULL(ВЫРАЗИТЬ(Т.Аналитика КАК Перечисление.ТипыНалогов), ЗНАЧЕНИЕ(Перечисление.ТипыНалогов.ПустаяСсылка))
	|;
	|ВЫБРАТЬ
	|	втПРД.Организация КАК Организация,
	|	втПРД.Подразделение КАК Подразделение,
	|	втПРД.Статья КАК Статья,
	|	втПРД.Source КАК Source,
	|	СУММА(втПРД.СуммаНачальныйОстаток) КАК НО,
	|	СУММА(втПРД.СуммаКонечныйОстаток) КАК КО
	|ПОМЕСТИТЬ втПРДСт
	|ИЗ
	|	втПРД КАК втПРД
	|СГРУППИРОВАТЬ ПО
	|	втПРД.Организация, втПРД.Подразделение, втПРД.Статья, втПРД.Source
	|;
	|ВЫБРАТЬ
	|	Т.Организация КАК Организация,
	|	ЕСТЬNULL(Т.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)) КАК Подразделение,
	|	Т.Статья КАК Статья,
	|	Т.Источник КАК Source,
	|	СУММА(ВЫБОР КОГДА Т.Период < &НачМес
	|			ТОГДА ВЫБОР КОГДА Т.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
	|					ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ
	|			ИНАЧЕ 0 КОНЕЦ) КАК НО,
	|	СУММА(ВЫБОР КОГДА Т.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
	|			ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ) КАК КО
	|ПОМЕСТИТЬ втПАП
	|ИЗ
	|	РегистрНакопления.ПрочиеАктивыПассивы КАК Т
	|ГДЕ
	|	Т.Организация = &Орг
	|	И Т.Период <= &КонМес
	|	И Т.Источник В (&Ист)
	|	И НЕ Т.Статья В ИЕРАРХИИ(&Искл)
	|СГРУППИРОВАТЬ ПО
	|	Т.Организация,
	|	ЕСТЬNULL(Т.Подразделение, ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)),
	|	Т.Статья,
	|	Т.Источник
	|;
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	К.Организация КАК Организация,
	|	К.Подразделение КАК Подразделение,
	|	К.Статья КАК Статья,
	|	К.Source КАК Source
	|ПОМЕСТИТЬ втКлючи
	|ИЗ
	|	(ВЫБРАТЬ
	|		втПАП.Организация КАК Организация,
	|		втПАП.Подразделение КАК Подразделение,
	|		втПАП.Статья КАК Статья,
	|		втПАП.Source КАК Source
	|	ИЗ
	|		втПАП КАК втПАП
	|	ОБЪЕДИНИТЬ ВСЕ
	|	ВЫБРАТЬ
	|		втПРДСт.Организация,
	|		втПРДСт.Подразделение,
	|		втПРДСт.Статья,
	|		втПРДСт.Source
	|	ИЗ
	|		втПРДСт КАК втПРДСт) КАК К
	|;
	|ВЫБРАТЬ
	|	втПРД.Организация КАК Организация,
	|	втПРД.Подразделение КАК Подразделение,
	|	втПРД.Статья КАК Статья,
	|	втПРД.Source КАК Source,
	|	втПРД.ОбъектыЭксплуатации КАК ОбъектыЭксплуатации,
	|	втПРД.НематериальныйАктив КАК НематериальныйАктив,
	|	втПРД.ТипНалога КАК ТипНалога,
	|	втПРД.СтатьяДоходыРасходы КАК СтатьяДоходыРасходы,
	|	ВЫБОР
	|		КОГДА втПРД.СтатьяДоходыРасходы ЕСТЬ НЕ NULL ТОГДА СокрЛП(ПРЕДСТАВЛЕНИЕ(втПРД.СтатьяДоходыРасходы))
	|		КОГДА втПРД.ТипНалога <> ЗНАЧЕНИЕ(Перечисление.ТипыНалогов.ПустаяСсылка) ТОГДА ПРЕДСТАВЛЕНИЕ(втПРД.ТипНалога)
	|		КОГДА втПРД.ОбъектыЭксплуатации <> ЗНАЧЕНИЕ(Справочник.ОбъектыЭксплуатации.ПустаяСсылка) ТОГДА ПРЕДСТАВЛЕНИЕ(втПРД.ОбъектыЭксплуатации)
	|		КОГДА втПРД.НематериальныйАктив <> ЗНАЧЕНИЕ(Справочник.НематериальныеАктивы.ПустаяСсылка) ТОГДА ПРЕДСТАВЛЕНИЕ(втПРД.НематериальныйАктив)
	|		ИНАЧЕ """"
	|	КОНЕЦ КАК Аналитика1,
	|	"""" КАК Аналитика2,
	|	"""" КАК Аналитика3,
	|	ЛОЖЬ КАК Расхождение,
	|	втПРД.СуммаНачальныйОстаток КАК СуммаНачальныйОстаток,
	|	0 КАК СуммаПриход,
	|	0 КАК СуммаРасход,
	|	втПРД.СуммаКонечныйОстаток КАК СуммаКонечныйОстаток
	|ИЗ
	|	втПРД КАК втПРД
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	К.Организация,
	|	К.Подразделение,
	|	К.Статья,
	|	К.Source,
	|	ЗНАЧЕНИЕ(Справочник.ОбъектыЭксплуатации.ПустаяСсылка),
	|	ЗНАЧЕНИЕ(Справочник.НематериальныеАктивы.ПустаяСсылка),
	|	ЗНАЧЕНИЕ(Перечисление.ТипыНалогов.ПустаяСсылка),
	|	NULL,
	|	"""",
	|	"""",
	|	"""",
	|	ИСТИНА,
	|	ЕСТЬNULL(втПАП.НО, 0) - ЕСТЬNULL(втПРДСт.НО, 0),
	|	0,
	|	0,
	|	ЕСТЬNULL(втПАП.КО, 0) - ЕСТЬNULL(втПРДСт.КО, 0)
	|ИЗ
	|	втКлючи КАК К
	|		ЛЕВОЕ СОЕДИНЕНИЕ втПАП КАК втПАП
	|		ПО К.Организация = втПАП.Организация
	|			И К.Подразделение = втПАП.Подразделение
	|			И К.Статья = втПАП.Статья
	|			И К.Source = втПАП.Source
	|		ЛЕВОЕ СОЕДИНЕНИЕ втПРДСт КАК втПРДСт
	|		ПО К.Организация = втПРДСт.Организация
	|			И К.Подразделение = втПРДСт.Подразделение
	|			И К.Статья = втПРДСт.Статья
	|			И К.Source = втПРДСт.Source
	|ГДЕ
	|	ЕСТЬNULL(втПАП.НО, 0) - ЕСТЬNULL(втПРДСт.НО, 0) > 0.01
	|	ИЛИ ЕСТЬNULL(втПАП.НО, 0) - ЕСТЬNULL(втПРДСт.НО, 0) < -0.01
	|	ИЛИ ЕСТЬNULL(втПАП.КО, 0) - ЕСТЬNULL(втПРДСт.КО, 0) > 0.01
	|	ИЛИ ЕСТЬNULL(втПАП.КО, 0) - ЕСТЬNULL(втПРДСт.КО, 0) < -0.01";
	Отразить_Свод(Запрос.Выполнить().Выгрузить());
	Возврат Истина;
КонецФункции
```

- [ ] **Step 3: Проверить отсутствие синтаксических артефактов**

```bash
grep -n "Функция Свод_ПрочиеРасходыДоходы" Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl
grep -c "КонецФункции" Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl
```

Expected: одна функция Свод_ПрочиеРасходыДоходы, общий счёт КонецФункции совпадает с до правки + 0 (тело заменили, число функций не изменилось).

---

## Task 3: Modify BSL — добавить `СтатьяДоходыРасходы` в `ПровестиБалансСвод.Свернуть`

**Files:**
- Modify: `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl` (line 109-113)

- [ ] **Step 1: Заменить строку Свернуть**

Текущая (line 109-114):
```bsl
	мТЗСвод.Свернуть(
		"Организация,Подразделение,Статья,Номенклатура,Контрагент,Партнер,Склад,"
		+ "ОбъектыЭксплуатации,Договор,ФизическоеЛицо,ДенежныеСредства,Source,"
		+ "Расхождение,Аналитика1,Аналитика2,Аналитика3,ОбъектРасчетов,"
		+ "НематериальныйАктив,ТипНалога,ТипПоказателя,ДокументДвижения",
		"СуммаНачальныйОстаток,СуммаПриход,СуммаРасход,СуммаКонечныйОстаток");
```

Должна стать:
```bsl
	мТЗСвод.Свернуть(
		"Организация,Подразделение,Статья,Номенклатура,Контрагент,Партнер,Склад,"
		+ "ОбъектыЭксплуатации,Договор,ФизическоеЛицо,ДенежныеСредства,Source,"
		+ "Расхождение,Аналитика1,Аналитика2,Аналитика3,ОбъектРасчетов,"
		+ "НематериальныйАктив,ТипНалога,ТипПоказателя,СтатьяДоходыРасходы,ДокументДвижения",
		"СуммаНачальныйОстаток,СуммаПриход,СуммаРасход,СуммаКонечныйОстаток");
```

Добавлен `СтатьяДоходыРасходы` между `ТипПоказателя` и `ДокументДвижения`.

- [ ] **Step 2: Закоммитить обе правки BSL**

```bash
git add Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl
git commit -m "feat(balans_svod): расшифровка Расх/Дох тек.пер. через СтатьяДоходыРасходы

Свод_ПрочиеРасходыДоходы переписана на канон-образец §2:
- втПРД с ДВУМЯ ветвями UNION ALL:
  A: ДДР⨝ПАП INNER JOIN per (Регистратор,Период,Подр) для статей Расх/Дох тек.пер.
     → signed Σ из ДвДохРасх, СтатьяДоходыРасходы=ДДР.Статья
  B: ПАП per Источник∈Ист для ВСЕХ остальных статей → как раньше через ВЫРАЗИТЬ
- Финал: детали (Аналитика1=СокрЛП(ПРЕДСТАВЛЕНИЕ(СтатьяДоходыРасходы)) если заполнено,
  иначе fallback ТипНалога/ОбъЭкспл/НМА) + плуги (|втПАП-втПРДСт|>0.01)
- Инвариант: Σ(детали+плуг) per (Орг,Подр,Статья,Source) == ПАП до копейки

ПровестиБалансСвод.Свернуть: добавлено СтатьяДоходыРасходы между
ТипПоказателя и ДокументДвижения.

Spec: docs/superpowers/specs/2026-05-26-balans-svod-statya-dohody-rashody-design.md"
```

---

## Task 4: Применить конфигурацию в БД

**Files:**
- Update database from `Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl`

- [ ] **Step 1: Применить через db-load-xml (Partial)**

```bash
/db-load-xml -Mode Partial -Files "Documents/А_ФинРез_Баланс/Ext/ObjectModule.bsl"
```

Expected: «Загрузка завершена», возвращается без ошибок. Если в Designer открыт документ — попросить пользователя закрыть Designer (НЕ форсить /F и НЕ обходить блокировку).

- [ ] **Step 2: Применить динамическое обновление**

```bash
/db-update -Dynamic+
```

Expected: «Обновление успешно завершено».

---

## Task 5: Verify — перепровести документ 00000000004 и ассертить регистр

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_prd_dohrash_verify.py`

- [ ] **Step 1: Создать verify скрипт**

```python
# -*- coding: utf-8 -*-
"""Verify (Rule #5) — перепровести Финансовый результат Баланс (для OLAP) 00000000004
от 30.04.2026 12:00:00 после внедрения расшифровки СтатьяДоходыРасходы.

Asserts:
- Документ найден (get-or-create по границам апреля 2026)
- Перепроведение через Записать(РежимЗаписиДокумента.Проведение) PASS, без ошибок
- В РС А_ОтчетБаланс_Свод по этому регистратору:
  * Дубль ключа = 0
  * Есть строки Расх.тек.пер./Глобино-2 с СтатьяДоходыРасходы<>NULL
  * Аналитика1 для этих строк непустая
  * Σ КО per (Орг,Подр,Статья,Source) == ПАП.ОстаткиИОбороты(КонМес) до 0.01
  * Σ Приход = Σ Расход = 0 по этому регистратору (остатки-only)
- Перепроведение идемпотентно (Δ=0)
"""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client
from datetime import datetime
from collections import Counter

CONN = 'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"'
v8 = win32com.client.Dispatch("V83.COMConnector")
erp = v8.Connect(CONN)
S = erp.String
TOL = 0.01

# Найти ТОВ ИНДАСТРИАЛБУД
q = erp.NewObject("Запрос")
q.Text = """ВЫБРАТЬ Ссылка ИЗ Справочник.Организации ГДЕ КодПоЕДРПОУ = "40645273" """
r = q.Execute().Выгрузить()
assert r.Количество() == 1
own_org = r.Получить(0).Ссылка

# Get-or-create документ по границам апреля 2026
q = erp.NewObject("Запрос")
q.УстановитьПараметр("Орг", own_org)
q.Text = """
ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка
ИЗ Документ.А_ФинРез_Баланс
ГДЕ Организация = &Орг И НЕ ПометкаУдаления
    И Месяц >= ДАТАВРЕМЯ(2026,4,1,0,0,0) И Месяц <= ДАТАВРЕМЯ(2026,4,30,23,59,59)
"""
r = q.Execute().Выгрузить()
if r.Количество() == 0:
    print("[INFO] Документ не найден, создаём новый")
    doc = erp.Документы.А_ФинРез_Баланс.СоздатьДокумент()
    doc.Дата = datetime(2026, 4, 30, 12, 0, 0)
    doc.Организация = own_org
    doc.Месяц = datetime(2026, 4, 1, 0, 0, 0)
    doc.ВключатьДочерние = False
    doc.Записать()
    doc_ref = doc.Ссылка
else:
    doc_ref = r.Получить(0).Ссылка

doc_obj = doc_ref.ПолучитьОбъект()
print(f"Документ: {S(doc_ref)}, Дата: {doc_obj.Дата}, Месяц: {doc_obj.Месяц}, Проведён: {doc_obj.Проведен}")

# Перепровести
print("Перепроведение...")
try:
    doc_obj.Записать(erp.РежимЗаписиДокумента.Проведение)
    print("[OK] Перепроведение PASS")
except Exception as e:
    print(f"[FAIL] Перепроведение: {e}")
    sys.exit(1)

# Перечитать после Записать()
doc_obj = doc_ref.ПолучитьОбъект()
assert doc_obj.Проведен, "Документ не проведён после Записать"

# Проверка РС А_ОтчетБаланс_Свод по этому регистратору
q = erp.NewObject("Запрос")
q.УстановитьПараметр("Рег", doc_ref)
q.Text = """
ВЫБРАТЬ
    РС.Организация, ПРЕДСТАВЛЕНИЕ(РС.Организация) КАК ОргПред,
    РС.Подразделение, ПРЕДСТАВЛЕНИЕ(РС.Подразделение) КАК ПодрПред,
    РС.Статья, ПРЕДСТАВЛЕНИЕ(РС.Статья) КАК СтатьяПред,
    РС.Source, ПРЕДСТАВЛЕНИЕ(РС.Source) КАК SourcePред,
    РС.СтатьяДоходыРасходы, ПРЕДСТАВЛЕНИЕ(РС.СтатьяДоходыРасходы) КАК СДРПред,
    РС.Аналитика1,
    РС.Расхождение,
    РС.СуммаНачальныйОстаток, РС.СуммаПриход, РС.СуммаРасход, РС.СуммаКонечныйОстаток
ИЗ РегистрСведений.А_ОтчетБаланс_Свод КАК РС
ГДЕ РС.ДокументДвижения = &Рег
"""
r = q.Execute().Выгрузить()
print(f"Всего строк РС после перепроведения: {r.Количество()}")
assert r.Количество() > 0, "После перепроведения нет строк в РС"

# Считаем по статьям Расх/Дох тек.пер.
strok_rashod = 0
strok_rashod_glob2 = 0
sum_kos_glob2 = 0.0
glob2_st_set = set()
glob2_analitika1_set = set()
sum_prihod = 0.0
sum_rashod = 0.0
sum_dub_check = Counter()
for i in range(r.Количество()):
    row = r.Получить(i)
    stat = S(row.СтатьяПред)
    podr = S(row.ПодрПред)
    sum_prihod += float(row.СуммаПриход)
    sum_rashod += float(row.СуммаРасход)
    key = (S(row.Организация), podr, stat, S(row.Source), S(row.СтатьяДоходыРасходы) or "", S(row.Расхождение))
    sum_dub_check[key] += 1
    if stat == "Расходы текущего периода":
        strok_rashod += 1
        if podr == "Глобино-2":
            strok_rashod_glob2 += 1
            sum_kos_glob2 += float(row.СуммаКонечныйОстаток)
            sdr = S(row.СДРПред) if row.СтатьяДоходыРасходы else ""
            if sdr:
                glob2_st_set.add(sdr)
            an1 = S(row.Аналитика1)
            if an1:
                glob2_analitika1_set.add(an1)

print(f"  Строк по Расх.тек.пер.: {strok_rashod}")
print(f"  По Глобино-2: {strok_rashod_glob2}, Σ КО = {sum_kos_glob2:.2f}, статей: {sorted(glob2_st_set)[:5]}...")
print(f"  Σ Приход всего: {sum_prihod:.2f}, Σ Расход всего: {sum_rashod:.2f}")

# Дубли
dubli = {k: v for k, v in sum_dub_check.items() if v > 1}
assert not dubli, f"Дубли по ключу: {dubli}"

# Глобино-2: должна быть хотя бы одна строка с непустой СтатьёйДоходыРасходы
assert strok_rashod_glob2 > 0, "Нет строк Расх.тек.пер./Глобино-2 после перепроведения"
assert len(glob2_st_set) > 0, "В Глобино-2/Расх.тек.пер. нет деталей с СтатьёйДоходыРасходы"
assert len(glob2_analitika1_set) > 0, "Аналитика1 не заполнена для Глобино-2/Расх.тек.пер."

# Σ КО Глобино-2 / Расх.тек.пер. = 2400.00
assert abs(sum_kos_glob2 - 2400.00) < TOL, f"Σ КО Глобино-2/Расх.тек.пер должно быть 2400.00, получили {sum_kos_glob2}"

# Σ Приход = Σ Расход = 0 (остатки-only)
assert abs(sum_prihod) < TOL, f"Σ Приход должно быть 0, получили {sum_prihod}"
assert abs(sum_rashod) < TOL, f"Σ Расход должно быть 0, получили {sum_rashod}"

# Сверка с ПАП per (Орг,Подр,Статья) для статей Расх/Дох тек.пер. — Σ Source должна совпадать
СтРасх = erp.ПланыВидовХарактеристик.СтатьиАктивовПассивов.НайтиПоНаименованию("Расходы текущего периода")
СтДох = erp.ПланыВидовХарактеристик.СтатьиАктивовПассивов.НайтиПоНаименованию("Доходы текущего периода")
СтСС = erp.ПланыВидовХарактеристик.СтатьиАктивовПассивов.НайтиПоНаименованию("Собственные средства")
q = erp.NewObject("Запрос")
q.УстановитьПараметр("Орг", own_org)
q.УстановитьПараметр("СтРасх", СтРасх)
q.УстановитьПараметр("СтДох", СтДох)
q.УстановитьПараметр("Искл", [СтСС])
q.Text = """
ВЫБРАТЬ
    Т.Подразделение, ПРЕДСТАВЛЕНИЕ(Т.Подразделение) КАК ПодрПред,
    Т.Статья, ПРЕДСТАВЛЕНИЕ(Т.Статья) КАК СтатьяПред,
    СУММА(ВЫБОР КОГДА Т.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
            ТОГДА Т.Сумма ИНАЧЕ -Т.Сумма КОНЕЦ) КАК КО
ИЗ РегистрНакопления.ПрочиеАктивыПассивы КАК Т
ГДЕ Т.Организация = &Орг И Т.Период <= ДАТАВРЕМЯ(2026,4,30,23,59,59)
    И (Т.Статья = &СтРасх ИЛИ Т.Статья = &СтДох)
    И НЕ Т.Статья В ИЕРАРХИИ(&Искл)
СГРУППИРОВАТЬ ПО Т.Подразделение, Т.Статья
"""
pap = q.Execute().Выгрузить()
pap_map = {}
for i in range(pap.Количество()):
    row = pap.Получить(i)
    pap_map[(S(row.ПодрПред), S(row.СтатьяПред))] = float(row.КО)

# Σ свод по Расх/Дох тек.пер. per (Подр,Статья) over Source
svod_kos = {}
for i in range(r.Количество()):
    row = r.Получить(i)
    stat = S(row.СтатьяПред)
    if stat not in ("Расходы текущего периода", "Доходы текущего периода"):
        continue
    key = (S(row.ПодрПред), stat)
    svod_kos[key] = svod_kos.get(key, 0.0) + float(row.СуммаКонечныйОстаток)

mismatches = []
for k, kos in svod_kos.items():
    pap_ko = pap_map.get(k, 0.0)
    if abs(kos - pap_ko) > TOL:
        mismatches.append((k, kos, pap_ko))

assert not mismatches, f"Σ свод != ПАП: {mismatches[:5]}"
print(f"[OK] Σ свод по Расх/Дох тек.пер. == ПАП до {TOL} (по {len(svod_kos)} ключей)")

# Идемпотентность — перепровести ещё раз и сравнить
print("Проверка идемпотентности (2-й прогон)...")
doc_obj2 = doc_ref.ПолучитьОбъект()
doc_obj2.Записать(erp.РежимЗаписиДокумента.Проведение)
q = erp.NewObject("Запрос")
q.УстановитьПараметр("Рег", doc_ref)
q.Text = """ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК Кол, СУММА(СуммаКонечныйОстаток) КАК ΣКО
    ИЗ РегистрСведений.А_ОтчетБаланс_Свод ГДЕ ДокументДвижения = &Рег"""
r2 = q.Execute().Выгрузить().Получить(0)
print(f"  После 2-го прогона: строк={r2.Кол}, Σ КО={r2.ΣКО:.2f}")
assert r2.Кол == r.Количество(), f"Количество строк изменилось: {r.Количество()} -> {r2.Кол}"

print("\n[OK] Verify PASS — расшифровка работает, баланс == ПАП, идемпотентно")
```

- [ ] **Step 2: Запустить verify**

Run: `C:/Python313/python.exe _Rarzrabotki/Python/test/test_balans_prd_dohrash_verify.py`
Expected: вывод заканчивается `[OK] Verify PASS — расшифровка работает, баланс == ПАП, идемпотентно`.

- [ ] **Step 3: Закоммитить verify**

```bash
git add _Rarzrabotki/Python/test/test_balans_prd_dohrash_verify.py
git commit -m "test(balans_prd_dohrash): verify — перепроведение 00000000004, ассерты по Глобино-2 и Σ==ПАП"
```

---

## Task 6: Regression — перепровести декабрь 2025 и январь 2026, ассертить балансы

**Files:**
- Create: `_Rarzrabotki/Python/test/test_balans_prd_dohrash_regression.py`

- [ ] **Step 1: Создать regression скрипт**

```python
# -*- coding: utf-8 -*-
"""Regression — перепровести декабрь 2025 и январь 2026 после внедрения расшифровки.

Asserts:
- Документы найдены (get-or-create)
- Перепроведение PASS
- Балансы per Орг unchanged:
  * декабрь 2025: |Актив| = 278 093 267,32
  * январь 2026: |Актив| = 288 787 750,11
- Σ КО регистра signed (OD-3) = 0,00 (Актив = |Пассив|)
"""
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")
import win32com.client
from datetime import datetime

CONN = 'Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"'
v8 = win32com.client.Dispatch("V83.COMConnector")
erp = v8.Connect(CONN)
S = erp.String
TOL = 0.01

# Эталоны (см. spec §3)
ETALONS = {
    "2025-12": ("декабрь 2025", datetime(2025, 12, 1), datetime(2025, 12, 31, 23, 59, 59), 278_093_267.32),
    "2026-01": ("январь 2026", datetime(2026, 1, 1), datetime(2026, 1, 31, 23, 59, 59), 288_787_750.11),
}

# Найти ТОВ ИНДАСТРИАЛБУД
q = erp.NewObject("Запрос")
q.Text = """ВЫБРАТЬ Ссылка ИЗ Справочник.Организации ГДЕ КодПоЕДРПОУ = "40645273" """
r = q.Execute().Выгрузить()
assert r.Количество() == 1
own_org = r.Получить(0).Ссылка

# Найти ИсключенныеСтатьи
СтСС = erp.ПланыВидовХарактеристик.СтатьиАктивовПассивов.НайтиПоНаименованию("Собственные средства")

failures = []

for key, (name, nm, km, expected_aktiv) in ETALONS.items():
    print(f"\n=== {name} ===")
    # Get-or-create
    q = erp.NewObject("Запрос")
    q.УстановитьПараметр("Орг", own_org)
    q.УстановитьПараметр("НМ", nm)
    q.УстановитьПараметр("КМ", km)
    q.Text = """
    ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка
    ИЗ Документ.А_ФинРез_Баланс
    ГДЕ Организация = &Орг И НЕ ПометкаУдаления
        И Месяц >= &НМ И Месяц <= &КМ
    """
    r = q.Execute().Выгрузить()
    if r.Количество() == 0:
        print(f"  [WARN] Документ за {name} не найден — пропуск")
        continue
    doc_ref = r.Получить(0).Ссылка
    print(f"  Документ: {S(doc_ref)}")

    # Перепровести
    try:
        doc_obj = doc_ref.ПолучитьОбъект()
        doc_obj.Записать(erp.РежимЗаписиДокумента.Проведение)
        print(f"  [OK] Перепроведение PASS")
    except Exception as e:
        failures.append((name, f"Перепроведение FAIL: {e}"))
        continue

    # Считаем |Актив| по РС
    q = erp.NewObject("Запрос")
    q.УстановитьПараметр("Орг", own_org)
    q.УстановитьПараметр("НМ", nm)
    q.УстановитьПараметр("КМ", km)
    q.УстановитьПараметр("Искл", [СтСС])
    q.Text = """
    ВЫБРАТЬ
        СУММА(ВЫБОР КОГДА СА.АктивПассив = ЗНАЧЕНИЕ(Перечисление.ВидыСтатейУправленческогоБаланса.Актив)
                ТОГДА РС.СуммаКонечныйОстаток ИНАЧЕ 0 КОНЕЦ) КАК Aktiv,
        СУММА(РС.СуммаКонечныйОстаток) КАК ΣКО
    ИЗ РегистрСведений.А_ОтчетБаланс_Свод КАК РС
        ЛЕВОЕ СОЕДИНЕНИЕ ПланВидовХарактеристик.СтатьиАктивовПассивов КАК СА
        ПО РС.Статья = СА.Ссылка
    ГДЕ РС.Организация = &Орг
        И РС.ДокументДвижения В (
            ВЫБРАТЬ Д.Ссылка ИЗ Документ.А_ФинРез_Баланс КАК Д
            ГДЕ Д.Организация = &Орг И НЕ Д.ПометкаУдаления И Д.Проведен
                И Д.Месяц >= &НМ И Д.Месяц <= &КМ)
        И НЕ РС.Статья В ИЕРАРХИИ(&Искл)
    """
    res = q.Execute().Выгрузить().Получить(0)
    aktiv = float(res.Aktiv)
    sigma_ko = float(res.ΣКО)
    print(f"  |Актив| = {aktiv:.2f}  (эталон {expected_aktiv:.2f})")
    print(f"  Σ КО (OD-3) = {sigma_ko:.2f}  (ожидаем 0.00)")

    if abs(aktiv - expected_aktiv) > 1.0:
        failures.append((name, f"|Актив| {aktiv:.2f} != эталон {expected_aktiv:.2f}"))
    if abs(sigma_ko) > 1.0:
        failures.append((name, f"Σ КО = {sigma_ko:.2f} (Актив!=Пассив)"))

if failures:
    print(f"\n[FAIL] {len(failures)} ошибок:")
    for name, msg in failures:
        print(f"  {name}: {msg}")
    sys.exit(1)

print(f"\n[OK] Regression PASS — балансы декабря 2025 и января 2026 unchanged")
```

- [ ] **Step 2: Запустить regression**

Run: `C:/Python313/python.exe _Rarzrabotki/Python/test/test_balans_prd_dohrash_regression.py`
Expected: `[OK] Regression PASS — балансы декабря 2025 и января 2026 unchanged`. Никаких failures.

- [ ] **Step 3: Закоммитить regression**

```bash
git add _Rarzrabotki/Python/test/test_balans_prd_dohrash_regression.py
git commit -m "test(balans_prd_dohrash): regression — балансы дек2025/янв2026 unchanged"
```

---

## Task 7: Обновить knowledge docs

**Files:**
- Modify: `_Rarzrabotki/notebook/knowledge_Balanse/balanse_pattern_and_roadmap.md`
- Modify: `_Rarzrabotki/notebook/knowledge_Balanse/KNOWLEDGE_MAP_BALANSE.md`

- [ ] **Step 1: Добавить запись в `balanse_pattern_and_roadmap.md` §6 Roadmap**

В таблицу Roadmap §6 после строки #6 (Полный баланс) добавить:

```markdown
| 7 | ✅ **ВЫПОЛНЕНО 2026-05-26** Расшифровка статей «Расходы/Доходы текущего периода» по `СтатьяДоходыРасходы` (ПВХ.СтатьиРасходов / СтатьиДоходов). `Свод_ПрочиеРасходыДоходы` переписана с двумя ветвями UNION ALL в `втПРД`: A=`ДДР⨝ПАП` INNER JOIN per (Регистратор, Период, Подр) для статей Расх/Дох тек.пер. — signed Σ из ДвДохРасх.Сумма; B=ПАП per Источник∈&Ист для остальных статей (как раньше). Аналитика1 = СокрЛП(ПРЕДСТАВЛЕНИЕ(СтатьяДоходыРасходы)) с fallback на ТипНалога/ОбъектыЭксплуатации/НМА. Плуги компенсируют |ΣвтПРД − ПАП| | ✅ Эталон апрель 2026 / ТОВ / Глобино-2 / Расх.тек.пер. КО=**2 400,00** разнесён по реальным ПВХ.СтатьиРасходов. Σ(детали+плуг) per (Орг,Подр,Статья,Source) == ПАП до 0.01. Регрессия дек2025 (278 093 267,32) / янв2026 (288 787 750,11) unchanged. Тесты: `test_balans_prd_dohrash_pretest.py` / `_verify.py` / `_regression.py` (PASS ×3) |
```

- [ ] **Step 2: Обновить KNOWLEDGE_MAP_BALANSE.md** — в раздел «Стан реалізації» (header):

После «Свод_ПрочиеРасходыДоходы DONE 2026-05-18» добавить:

```markdown
**+2026-05-26 расшифровка по СтатьяДоходыРасходы:** в `Свод_ПрочиеРасходыДоходы` добавлена детальная расшифровка для статей «Расходы/Доходы текущего периода» через INNER JOIN `ДвиженияДоходыРасходыПрочиеАктивыПассивы` per (Регистратор, Период, Подразделение). Заполняется измерение `СтатьяДоходыРасходы` (ПВХ.СтатьиРасходов/СтатьиДоходов) + `Аналитика1` = СокрЛП(ПРЕДСТАВЛЕНИЕ(СтатьяДоходыРасходы)) с fallback на существующий приоритет (ТипНалога/ОбъектыЭксплуатации/НМА). Балансы дек2025/янв2026 unchanged (278 093 267,32 / 288 787 750,11). Эталон апрель 2026 / Глобино-2 / Расх.тек.пер. КО=2 400,00 теперь разнесён по реальным СтатьямРасходов в РС.
```

- [ ] **Step 3: Закоммитить knowledge update**

```bash
git add _Rarzrabotki/notebook/knowledge_Balanse/balanse_pattern_and_roadmap.md _Rarzrabotki/notebook/knowledge_Balanse/KNOWLEDGE_MAP_BALANSE.md
git commit -m "docs(knowledge_Balanse): расшифровка Расх/Дох тек.пер. через СтатьяДоходыРасходы (DONE 2026-05-26)"
```

---

## Task 8: Финальная проверка целостности

- [ ] **Step 1: Прогнать pretest + verify + regression последовательно**

```bash
C:/Python313/python.exe _Rarzrabotki/Python/test/test_balans_prd_dohrash_pretest.py
C:/Python313/python.exe _Rarzrabotki/Python/test/test_balans_prd_dohrash_verify.py
C:/Python313/python.exe _Rarzrabotki/Python/test/test_balans_prd_dohrash_regression.py
```

Expected: все 3 PASS, без exit code != 0.

- [ ] **Step 2: Проверить отсутствие сирот в РС**

```python
# inline скрипт
import win32com.client
v8 = win32com.client.Dispatch("V83.COMConnector")
erp = v8.Connect('Srvr="localhost";Ref="BaseERP";Usr="Администратор";Pwd="24043"')
q = erp.NewObject("Запрос")
q.Text = """ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК Кол
ИЗ РегистрСведений.А_ОтчетБаланс_Свод РС
ГДЕ ТИПЗНАЧЕНИЯ(РС.ДокументДвижения) <> ТИП(Документ.А_ФинРез_Баланс)
    ИЛИ РС.ДокументДвижения = НЕОПРЕДЕЛЕНО"""
print("Сирот:", q.Execute().Выгрузить().Получить(0).Кол)
```

Expected: `Сирот: 0`. Если > 0 — запустить `_Rarzrabotki/Python/cleanup_otchetbalans_svod_orphans.py` (см. balanse_pattern_and_roadmap.md §4.1).

- [ ] **Step 3: Финальный summary commit (опциональный)**

Не требуется отдельный коммит — все изменения уже в предыдущих коммитах.

---

## Risks & mitigations

1. **Σ ДвДохРасх per (Регистратор, Период, Подр) ≠ Σ ПАП** для документов закрытия месяца. Плуг компенсирует.
2. **Документы без записей в ДвДохРасх** (например регистрация через корректировку регистра): останутся в плуге с пустой СтатьёйДоходыРасходы. Это OK (как канон §2).
3. **Σ удваивается при многострочных документах в ДвДохРасх**: ИСКЛЮЧЕНО — signed Σ берётся из ДвДохРасх.Сумма, JOIN не реплицирует ПАП-суммы (просто фильтрует выборку через ВНУТРЕННЕЕ СОЕДИНЕНИЕ).
4. **Designer заблокировал БД** во время `/db-load-xml`: попросить пользователя закрыть Designer, не обходить через ручной 1cv8.exe.
5. **ConfigDumpInfo кеш**: после `/db-load-xml -Mode Partial` + `/db-update` — конфигурация применяется автоматически. Если COM-тесты видят старую процедуру — перезапустить COM-сервер или подождать 30 сек.

## Acceptance criteria summary

После всех задач:
- ✅ В Глобино-2 / Расходы текущего периода (на 30.04.2026) в РС А_ОтчетБаланс_Свод есть строки с непустой `СтатьяДоходыРасходы`
- ✅ `Аналитика1` = СокрЛП(ПРЕДСТАВЛЕНИЕ(СтатьяДоходыРасходы))
- ✅ Σ КО per (Орг,Подр,Статья) для Расх/Дох тек.пер. == ПАП.ОстаткиИОбороты(30.04.2026) до 0.01
- ✅ Баланс per Орг == штатный Отчет.УправленческийБаланс
- ✅ Регрессия дек2025 (278 093 267,32) / янв2026 (288 787 750,11) unchanged
- ✅ Идемпотентность перепроведения подтверждена (Δ=0)
- ✅ 5 остальных Свод_* не регрессировали (test_balans_*_verify.py PASS)
