# Дизайн: подотчётне удержание — Субконто1=ФЛ (3721) + ПередачаНач по строковой статье

> Дата: 2026-06-04. Продолжение `2026-06-04-podzvitne-uderzhanie-ozp.md`. Период: декабрь 2025.
> Рабочая папка: `C:\Configuration_downloads\BASERP25` (ветка `claude/bas-doc-skills`). Навыки: Rule #-1, db-load-xml/db-update, verification-before-completion. Перепроведение — только UI.

## 1. Задача (2 части)

1. **Счёт 3721:** в строке удержания подотчётного (Гуняга, Код 00009) проставлять `Субконто1 = ФизическоеЛицо`
   (regl-субконто счёта 3721 «Расчеты с подотчетными лицами»). Сейчас `<...>` (пусто).
2. **А_ПередачаНачисленийМеждуПодразделениями.Удержания:** документ проводит ВСЕ удержания на одну статью
   «Прочие пассивы». Пользователь добавил в ТЧ реквизиты `СтатьяАктивовПассивов` + `АналитикаАктивовПассивов`.
   Нужно проводить по строковой статье/аналитике (Гуняга 400 → «Денежные средства (у подотчетных лиц)»).

## 2. Grounded факты (COM)

- Счёт 3721 Субконто1 = «Работники организаций», тип `Справочник.ФизическиеЛица`.
- `А_ОтражениеЗПпоКазне.УдержанияБухгалтерия` имеет `Субконто1`, `СтатьяАктивовПассивов`, `АналитикаАктивовПассивов`.
- Типовой ОЗФУ ManagerModule (стр.596-597) читает `Удержания.СтатьяАктивовПассивов` + `АналитикаАктивовПассивов`
  из `УдержаннаяЗарплата` → ПАП-движение. ОЗФУ.УдержаннаяЗарплата имеет оба поля + Субконто1.
- `ПередачаНач.Удержания` имеет новые реквизиты `СтатьяАктивовПассивов` + `АналитикаАктивовПассивов`.
- ПАП.Аналитика — составной, включает ФизическиеЛица.
- **Статья «Денежные средства (у подотчетных лиц)» = «Без аналитики»** (карточка статьи + движения Авансового
  отчёта ІБ00-000341: колонка Аналитика пустая) → ПАП-движение БЕЗ аналитики. Аналитику=ФЛ НЕ ставить.

## 3. Часть A — Субконто1=ФЛ (только)

`Documents/А_ОтражениеЗПпоКазне/Ext/ObjectModule.bsl`, `ЗаписатьУдержанияВТЧ_Бух`, подотчётная ветка:
добавить `НоваяСтрока.Субконто1 = СотрудникERP.ФизическоеЛицо;`. **АналитикаАктивовПассивов НЕ трогать**
(статья без аналитики). Субконто1 дотекает в ОЗФУ.УдержаннаяЗарплата через `ЗаполнитьЗначенияСвойств` →
regl-проводка счёта 3721 получает субконто = ФЛ.

## 4. Часть B — ПередачаНач по строковой статье

**B.1** — `А_ОтражениеЗПпоКазне._ПНМП_ЗаполнитьУдержания`: вместо `ПустоеДоп` прокинуть через ДопПоля
`СтатьяАктивовПассивов = Стр.СтатьяАктивовПассивов`, `АналитикаАктивовПассивов = Стр.АналитикаАктивовПассивов`
(зеркалит источник; для подотчётного аналитика пуста). Поля попадают в `ПередачаНач.Удержания`.

**B.2** — `Documents/А_ПередачаНачисленийМеждуПодразделениями/Ext/ObjectModule.bsl`, `ОбработкаПроведения`:
- блок Удержания запроса: вместо `&СтПрочПас` → `ВЫБОР КОГДА У.СтатьяАктивовПассивов = ЗНАЧЕНИЕ(ПланВидовХарактеристик.СтатьиАктивовПассивов.ПустаяСсылка) ТОГДА &СтПрочПас ИНАЧЕ У.СтатьяАктивовПассивов КОНЕЦ КАК Статья`; колонка-аналитика → `У.АналитикаАктивовПассивов`; группировать по статье+аналитике+(Исх,Цел).
- обобщить `_ПАП_ДобавитьДвижение`: параметр `ТипНалога` → `Аналитика` (любое значение); ставит аналитику только если заполнена (для подотчётного пусто → корректно).

## 5. Эффект / регрессия

- Гуняга 400: ОЗФУ счёт «3721, Гуняга»; ОЗФУ + ПередачаНач ПАП «Денежные средства (у подотчетных лиц)» **без аналитики**
  (ПередачаНач: КМД-2 Приход / Строительство Расход).
- **Алименты** (Дубінько/Старіков): статья «Прочие пассивы», без аналитики — без изменений.
- ПередачаНач net-zero по (статья, орг); общий баланс не двигается, меняется только статья для 400.
- `ЗаполнитьИзПередачиНачислений` (А_ВзСС) зеркалит только «Оплата труда» — Удержания не влияют.

## 6. Проверка

RED: ПередачаНач Гуняга сейчас на «Прочие пассивы». GREEN (после UI-перепроведения ОЗП №1 → Заполнить →
пересоздать ОЗФУ + ПередачаНач → провести): ОЗФУ Гуняга счёт «3721, <ФЛ>»; ПередачаНач ПАП «Денежные средства
(у подотчетных лиц)» Σ400 (КМД-2/Строительство), аналитика пуста; алименты на «Прочие пассивы» без изменений;
КонтрольБаланса цел. Каждую правку запроса — сперва Python COM (Rule #-1).
