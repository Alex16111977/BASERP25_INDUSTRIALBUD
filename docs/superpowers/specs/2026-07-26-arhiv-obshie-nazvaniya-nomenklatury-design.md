# Архив «Общие названия номенклатуры» (BuhBud)

**Дата:** 2026-07-26
**База:** `bas_industrialbud` (BAS Бухгалтерія), `Srvr="localhost";Ref="bas_industrialbud";Usr="cfo";Pwd="2442"`
**Дамп конфигурации:** `_Rarzrabotki/BASEBuh/`

## 1. Цель

Снять снимок текущего контура общих названий номенклатуры перед его переработкой:

1. Перенести все элементы `Справочник.ОбщиеНазванияНоменклатуры` в справочник-архив `Справочник.ОбщиеНазванияНоменклатурыСтарая`.
2. Заполнить `Справочник.Номенклатура.ОбщееНазваниеНоменклатурыСтарая` архивным аналогом текущего значения `ОбщееНазваниеНоменклатуры`.

Оригинальный справочник и живой реквизит остаются нетронутыми — архив это копия, а не перенос.

## 2. Установленные факты

Проверено через Python COM на живой базе 2026-07-26.

### Метаданные

| Объект | Состояние |
|---|---|
| `Справочник.ОбщиеНазванияНоменклатурыСтарая` | есть в живой базе |
| `Справочник.Номенклатура.ОбщееНазваниеНоменклатурыСтарая` | есть, тип `СправочникСсылка.ОбщиеНазванияНоменклатурыСтарая` |
| Структура архива | идентична оригиналу: иерархия групп и элементов, ДлинаКода 9, ДлинаНаименования 150, владельцев нет |
| Реквизиты обоих справочников | `ОсновнаяНоменклатура` (СправочникСсылка.Номенклатура), `Единица` и `ЕдиницаРекомендуемая` (СправочникСсылка.КлассификаторЕдиницИзмерения), `НазваниеДляОбъединения` (Строка), `НеПроверятьВЗакупкахпоСС` (Булево) |
| `А_ИдКод` | у `Номенклатура` — есть; у обоих справочников общих названий — нет |
| Использование обоих реквизитов номенклатуры | `ForItem` (`Catalogs/Номенклатура.xml:1371`, `:1463`) — только элементы, у групп полей нет |

### Данные

| Показатель | Значение |
|---|---|
| `ОбщиеНазванияНоменклатуры` — всего | 322 |
| из них групп | 0 |
| помечено на удаление | 0 |
| предопределённых | 0 |
| различных кодов | 322 (коды уникальны) |
| `ОбщиеНазванияНоменклатурыСтарая` — всего | 0 (пуст) |
| `Номенклатура` — всего / групп | 42 552 / 48 |
| `Номенклатура` с заполненным `ОбщееНазваниеНоменклатуры` | **2 640** |
| битых ссылок `Номенклатура → ОбщиеНазванияНоменклатуры` | 0 |

Уточнение по счёту (проверено 2026-07-26 при прогоне вхолостую): реквизит объявлен как `ForItem`, поэтому у 48 групп номенклатуры поле в запросе возвращает `NULL`, а не пустую ссылку. Первоначальная оценка «2 688» получена выражением `СУММА(ВЫБОР КОГДА поле = ПустаяСсылка ТОГДА 0 ИНАЧЕ 1 КОНЕЦ)`, которое засчитывает `NULL` как заполненное значение: 2 640 элементов + 48 групп. Корректный отбор — `ГДЕ поле <> ПустаяСсылка`, он даёт **2 640** и групп не захватывает.

Заполненность реквизитов у 322 элементов: `ОсновнаяНоменклатура` 271, `Единица` 317, `ЕдиницаРекомендуемая` 194, `НазваниеДляОбъединения` 0, `НеПроверятьВЗакупкахпоСС` 1.

### Обмен и подписки

- Планы обмена, содержащие `Справочник.Номенклатура` в составе: 13, во всех авторегистрация = `Запретить`. `ОбщиеНазванияНоменклатуры` входит только в `ОбменУправлениеПредприятиемБухгалтерия20` (авторегистрация `Запретить`). `ОбщиеНазванияНоменклатурыСтарая` не входит ни в один план обмена.
- Реальные узлы есть только у `ПланОбмена.ОбменУправлениеПредприятиемБухгалтерия20` — 1 узел (ЕРП). У `Полный`, `ПоОрганизации`, `АвтономнаяРабота` узлов нет.
- Регистрация к выгрузке выполняется подпиской `ОбменДаннымиОбменЕРПБухгалтерия20ЗарегистрироватьИзменение` (ПередЗаписью) → `ОбменДаннымиСобытия.МеханизмРегистрацииОбъектовПередЗаписью("ОбменУправлениеПредприятиемБухгалтерия20", ...)` (`CommonModules/ОбменДаннымиСобытияЕРПБП/Ext/Module.bsl:10`) → `ОбменДаннымиСобытия.ЗарегистрироватьИзменениеОбъекта`.
- **`ОбменДанными.Загрузка` в этой БСП регистрацию НЕ подавляет, а включает.** `CommonModules/ОбменДаннымиСобытия/Ext/Module.bsl:839-841` прямо документирует: «Проверка значения свойства ОбменДанными.Загрузка отсутствует по причине того, что … логика регистрации изменений на узлах плана обмена … должна выполняться в том числе при установке этого свойства равным Истина». Замер на 5 образцах номенклатуры (в откатываемых транзакциях, 2026-07-26) — результат стабилен 5/5:

  | Способ записи | Регистраций на узле ЕРП |
  |---|---|
  | без `Загрузка` | 0 → 0 |
  | `Загрузка = Истина` | 0 → 1 |
  | `Загрузка = Истина` + `Отправитель = узел` | 0 → 0 |

  Текущий baseline: на узле «BAS ERP, ред. 2.5» зарегистрировано **0** номенклатур.
- У `Справочник.ОбщиеНазванияНоменклатурыСтарая` таблицы изменений не существует вовсе (`Таблица не найдена "…Изменения"`) — справочник вне всех планов обмена, для него вопрос регистрации не стоит.
- Кастомная подписка `А_ДоработкаСправочникиПриЗаписи` (ПриЗаписи, 7 типов источника) → `УстановитьКодUUIDВСправочнике` (`.../Module.bsl:59`): если у справочника есть `А_ИдКод` и он не равен UUID — реквизит проставляется и объект перезаписывается рекурсивно с `ОбменДанными.Загрузка = Истина`. Для `Номенклатура` подписка применима, для обоих справочников общих названий — ранний выход (нет `А_ИдКод`).

### Кто ещё ссылается на `ОбщиеНазванияНоменклатуры`

Полный скан метаданных (бизнес-значимые ссылки, без composite-типов БСП):

- `Справочник.Номенклатура.ОбщееНазваниеНоменклатуры` — в скоупе;
- `Справочник.СтруктураСебестоимости.ТЧ.Комплектующие.ОбщееНазвание`;
- `Документ.РасчетКомплектаций.ТЧ.ТабличнаяЧастьОстатков.ОбщееНазвание`;
- `Документ.РасчетКомплектаций.ТЧ.СписаниеПоНормам.ОбщееНазвание`;
- `Документ.РасчетКомплектаций.ТЧ.СписаниеСверхНормы.ОбщееНазвание`.

Последние четыре продолжают ссылаться на живой справочник и не ломаются — оригинал остаётся на месте. Архивных полей для них не создаётся.

## 3. Решение

Клон с сохранением UUID: каждый архивный элемент получает **ту же** ссылку-UUID, что и оригинал (`Справочники.ОбщиеНазванияНоменклатурыСтарая.ПолучитьСсылку(УИД)` + `Объект.УстановитьСсылкуНового(...)`).

Почему так:

- заполнение `Номенклатура.ОбщееНазваниеНоменклатурыСтарая` становится прямым пересчётом ссылки по UUID живого значения — таблица соответствий не нужна;
- операция идемпотентна: повторный прогон не плодит дубли, а находит уже перенесённый элемент по тому же UUID;
- архив — точное зеркало, сверка выполняется сравнением по ключу-UUID;
- UUID уникальны в пределах таблицы своего типа, коллизий между разными справочниками не бывает.

Поставка — Python COM скрипты (как остальные переносы данных в проекте), без изменений конфигурации:

| Файл | Назначение |
|---|---|
| `_Rarzrabotki/Python/onn_arhiv_common.py` | общий модуль: подключение, UUID-хелперы, батч-запись в транзакциях, упорядочивание по иерархии |
| `_Rarzrabotki/Python/test/test_onn_arhiv_dry_run.py` | шаг 0 — проверка механики в откатываемой транзакции |
| `_Rarzrabotki/Python/perenos_onn_v_arhiv.py` | шаги 1–2 — перенос и заполнение |
| `_Rarzrabotki/Python/proverka_onn_arhiv.py` | шаг 3 — независимая верификация |

## 4. Алгоритм

### Шаг 0 — прогон вхолостую (откатываемая транзакция). ВЫПОЛНЕН 2026-07-26

Внутри `BeginTransaction()` / `RollbackTransaction()` проверено:

1. `УстановитьСсылкуНового` вызывается через COM, объект пишется с заданным UUID, после отката его в базе нет — **подтверждено**;
2. явная установка `Код` не вызывает префиксацию (подписка `УстановитьПрефиксИнформационнойБазыКодуСправочника` висит на `ПриУстановкеНовогоКода` и при ручном присвоении не срабатывает) — код архива совпал с оригиналом, **подтверждено**;
3. запись `Номенклатура` с `ОбменДанными.Загрузка = Истина` **создаёт** регистрацию на узле ЕРП (0 → 1 на 5/5 образцов) — исходное допущение **опровергнуто**; запись без `Загрузка` регистрацию не создаёт (0 → 0 на 5/5).

Следствие для шага 2: номенклатура пишется **без** `ОбменДанными.Загрузка`. Флаг остаётся только на шаге 1, где пишется архивный справочник — он вне планов обмена.

Замер по подписке `А_ДоработкаСправочникиПриЗаписи`: у **0** из 2 640 номенклатур `А_ИдКод` отличается от UUID — вложенной перезаписи не будет.

### Шаг 1 — перенос 322 элементов

Источник — запрос к `Справочник.ОбщиеНазванияНоменклатуры`, упорядоченный так, чтобы родитель обрабатывался раньше потомка (сейчас групп 0, но код universal: обход по уровням иерархии).

Для каждого элемента:

- ссылка архива = `ПолучитьСсылку(УИД оригинала)`; если объект по ней уже существует — пропуск (идемпотентность);
- `ЭтоГруппа` — создаётся `СоздатьГруппу()` либо `СоздатьЭлемент()` соответственно;
- копируются: `Код`, `Наименование`, `ПометкаУдаления`, `Родитель` (ссылка архива по UUID родителя), `ОсновнаяНоменклатура`, `Единица`, `ЕдиницаРекомендуемая`, `НазваниеДляОбъединения`, `НеПроверятьВЗакупкахпоСС`;
- запись с `ОбменДанными.Загрузка = Истина` (обходит `ПроверитьЗаполнение` и БСП-обработчики; регистрации всё равно нет — справочник вне планов обмена);
- батчи по 200 объектов в транзакции, при исключении — откат батча, лог, продолжение со следующего батча; итоговый список ошибок в отчёте.

### Шаг 2 — заполнение `Номенклатура.ОбщееНазваниеНоменклатурыСтарая`

Выборка: `ОбщееНазваниеНоменклатуры` заполнено И архивное значение ещё не соответствует живому (сравнение по UUID на стороне Python — типы разные, в запросе не сопоставить). Оба реквизита объявлены как `ForItem`, поэтому в выборку попадают только элементы, группы номенклатуры не затрагиваются.

Для каждой номенклатуры:

- `ОбщееНазваниеНоменклатурыСтарая = Справочники.ОбщиеНазванияНоменклатурыСтарая.ПолучитьСсылку(УИД от живого значения)`;
- запись **без** `ОбменДанными.Загрузка` — по результату шага 0 именно этот путь не создаёт регистрацию к выгрузке в ЕРП;
- батчи по 200 в транзакции; батч, упавший с исключением, откатывается и повторяется поштучно (каждый элемент в своей транзакции), чтобы одна плохая запись не уносила 199 годных.

Порядок шагов обязателен: шаг 2 выполняется только после успешного шага 1, иначе архивные ссылки будут указывать на несуществующие объекты.

## 5. Побочные эффекты и как они гасятся

| Эффект | Гашение |
|---|---|
| Регистрация 2 640 номенклатур к выгрузке в ЕРП | запись **без** `ОбменДанными.Загрузка` (шаг 0: 0 → 0 на 5/5 образцов). Контроль после прогона: число зарегистрированных номенклатур на узле должно остаться 0, как в baseline |
| Вложенная перезапись подпиской `А_ДоработкаСправочникиПриЗаписи` | не возникает: у 0 из 2 640 номенклатур `А_ИдКод` расходится с UUID (замер шага 0) |
| Одна плохая запись рушит батч из 200 | упавший батч откатывается и повторяется поштучно; в отчёт попадают только реально проблемные элементы |
| **Штатная проверка целостности обнуляет `ОсновнаяНоменклатура` в живом справочнике** (обнаружено при прогоне шага 2) | не гасится и гаситься не должно — см. ниже |

**Про обнуление `ОсновнаяНоменклатура`.** `Catalogs/Номенклатура/Ext/ObjectModule.bsl`: `ПриЗаписи` (строка 100) при `ОбменДанными.Загрузка = Ложь` вызывает `ПроверитьОсновнуюВОбщихНазваниях` (строка 160), которая находит общие названия, где записываемая номенклатура значится основной, но которым она фактически не принадлежит, и обнуляет у них `ОсновнаяНоменклатура`. Массовая запись 2 640 номенклатур на шаге 2 заставила правило отработать: в живом справочнике `ОсновнаяНоменклатура` снята у **10** элементов из 322 (заполнено стало 261 вместо 271). Все 10 проверены поштучно — каждый был нарушением инварианта (например, у «Лампа», «Рамка», «Тепла підлога», «Знаки евакуації» основной значилась «Куточок 45 PP-R D=25 mm сірий», принадлежащая другому общему названию). Это чистка мусора самой конфигурацией, она сработала бы при любом сохранении этих номенклатур пользователем. Доисправленные значения сохранены в архиве — восстановление не требуется и нежелательно: вернётся тот же мусор и снова будет снят при следующей записи.
| Длинные транзакции и блокировки для пользователей | батчи по 200, прогон вне пиковой нагрузки |
| Повторный запуск скрипта | идемпотентен на обоих шагах: пропуск уже перенесённых UUID и уже корректно заполненных реквизитов |

## 6. Верификация (критерии приёмки)

Отдельный скрипт `proverka_onn_arhiv.py`, читающий базу заново после прогона. Все проверки должны дать ноль расхождений:

1. `КОЛИЧЕСТВО(ОбщиеНазванияНоменклатурыСтарая) = 322`, при этом множество UUID архива совпадает с множеством UUID оригинала.
2. Для каждого UUID совпадают: `Код`, `Наименование`, `ПометкаУдаления`, `ЭтоГруппа`, `Родитель` (по UUID), `ОсновнаяНоменклатура`, `Единица`, `ЕдиницаРекомендуемая`, `НазваниеДляОбъединения`, `НеПроверятьВЗакупкахпоСС`. Единственное расхождение, признаваемое ожидаемым: живая `ОсновнаяНоменклатура` пуста, архивная заполнена, и номенклатура из архивного значения привязана к другому общему названию — это след работы `ПроверитьОсновнуюВОбщихНазваниях` (см. раздел 5). Такие случаи выводятся отдельным списком и не считаются провалом; любое другое расхождение — провал.
3. Ноль номенклатур, где `ОбщееНазваниеНоменклатуры` заполнено, а `ОбщееНазваниеНоменклатурыСтарая` пусто (ожидаемое количество заполненных — 2 640).
4. Ноль номенклатур, где оба поля заполнены, но UUID не совпадают.
5. Ноль номенклатур, где `ОбщееНазваниеНоменклатурыСтарая` заполнено, а `ОбщееНазваниеНоменклатуры` пусто.
6. `КОЛИЧЕСТВО(ОбщиеНазванияНоменклатуры) = 322` — оригинал не изменился.
7. На узле `ОбменУправлениеПредприятиемБухгалтерия20` зарегистрировано 0 номенклатур к выгрузке — столько же, сколько было до миграции.

## 7. Вне скоупа

- **Формы архивного справочника не чиним** (решение пользователя от 2026-07-26). `Catalogs/ОбщиеНазванияНоменклатурыСтарая/Forms/ФормаЭлемента` — байт-копия рабочей формы: динамический список фильтрует по `Номенклатура.ОбщееНазваниеНоменклатуры = &Ссылка` (`Ext/Form.xml:197`), где параметр имеет архивный тип — список всегда пуст; кнопки «Подбор номенклатуры», «Удалить привязку», «Сделать основной» и drag-drop пишут в **живой** реквизит `ОбщееНазваниеНоменклатуры` (`Ext/Form/Module.bsl:67`, `:166`) и блокируют `Справочник.ОбщиеНазванияНоменклатуры` (`:246`). Риск: открыв карточку архива, пользователь может перезаписать боевую привязку. Отдельная задача.
- Архивные поля для `СтруктураСебестоимости.ТЧ.Комплектующие` и трёх ТЧ `РасчетКомплектаций` не создаются.
- Перенос контура в ЕРП (`А_ОбщиеНазванияНоменклатуры`) не затрагивается.
