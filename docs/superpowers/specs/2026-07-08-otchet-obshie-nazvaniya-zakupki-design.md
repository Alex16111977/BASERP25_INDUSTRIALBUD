# Дизайн: отчёт «Отчет по общим названиям в СС по закупкам» (BuhBud)

- **Дата:** 2026-07-08. Одобрен пользователем («ДА ДЕЛАЕМ»).
- **Файлы:** `_Rarzrabotki\Загрузка СС\Отчет по общим названиям в СС по закупкам.xml` (+ каталог).
- **Исходное состояние:** клон отчёта «Отчет по общим названиям в СС»; имя пользователь
  сменил на `ОтчетПоОбщимНазваниямВССпоЗакупкам`, но GUID остались от источника
  (cf6cdf86-…) — регенерация обязательна. Синоним пустой/старый — заполнить
  ru «Отчет по общим названиям в СС по закупкам» / uk «Звіт по загальних назвах в СС по закупівлях».

## 1. Цель (бухгалтер)

По каждой карточке «Общего названия» видеть рядом ПЛАН (строки СС: в каких единицах
планировалось) и ФАКТ (приходные накладные: в каких единицах реально закупалось,
суммы С НДС — сопоставимо с СС), с расшифровкой до документов поступления и до самой
структуры себестоимости — чтобы выбрать правильную `ОбщиеНазванияНоменклатуры.Единица`.

## 2. СКД — один набор, пакетный запрос (протестирован 1:1 на живой BuhBud:
план 952 строки / Σ 10 842 173,71; факт 27 955 строк / Σ 302 545 499,52)

- ВТ `АктивныеОН`: РАЗЛИЧНЫЕ ОбщееНазвание из ТЧ Комплектующие структур с
  `Активная И НЕ ПометкаУдаления` (фильтр «только активные» жёстко в запросе;
  факт показывается только по карточкам, используемым в активных СС).
- ОБЪЕДИНИТЬ ВСЕ:
  - **«План СС»**: ТЧ Комплектующие активных структур — ВидСтроки, ОбщееНазвание,
    ЕдиницаОбщая = `К.ОбщееНазвание.Единица`, Номенклатура, Единица (ТЧ), ЕдиницаСС,
    Этап, НоменклатураСС, Документ = `К.Ссылка` (Структура — расшифровка до СС),
    КоличествоПлан, СуммаПлан; факт-поля NULL.
  - **«Факт закупки»**: `Документ.ПоступлениеТоваровУслуг.Товары`, `Ссылка.Проведен`,
    `Номенклатура.ОбщееНазваниеНоменклатуры В (ВТ)` — ОбщееНазвание из привязки,
    Единица = `Т.ЕдиницаИзмерения` (фактическая единица строки поступления!),
    Документ = `Т.Ссылка` (расшифровка до накладной), КоличествоФакт,
    **СуммаФакт = `ВЫБОР КОГДА Т.Ссылка.СуммаВключаетНДС ТОГДА Т.Сумма
    ИНАЧЕ Т.Сумма + Т.СуммаНДС КОНЕЦ`** (НДС-синхронизация с СС-суммами; оба
    случая в базе: 35777 док. без включённого НДС, 12670 с включённым);
    план-поля NULL. Точный текст запроса — в
    `scratchpad\test_zakupki_query.py` контроллера, НЕ менять ни символа.

## 3. Ресурсы и вариант

- Ресурсы: КоличествоПлан, СуммаПлан, КоличествоФакт, СуммаФакт — Σ. Формат сумм ЧЦ=15; ЧДЦ=2.
- Вариант «Основной»: группировки **ОбщееНазвание → Номенклатура → детальные записи**;
  в выбранных полях группировки ОбщееНазвание добавить **ЕдиницаОбщая** (вывод «единица
  общий»); детали: ВидСтроки, Этап, НоменклатураСС, Единица, ЕдиницаСС, Документ,
  4 ресурса. Сортировка деталей: ВидСтроки УБЫВ («План СС» раньше «Факт закупки»).
- Роли полей/отборы доступные: ОбщееНазвание, Номенклатура, Этап, ВидСтроки, Документ.

## 4. Клон-чистка

Регенерировать 4 GUID (ExternalReport uuid, ObjectId, TypeId, ValueId; ClassId
e41aff26-… не трогать); GeneratedType name / MainDataCompositionSchema — на имя
`ОтчетПоОбщимНазваниямВССпоЗакупкам` (уже стоит в Name — проверить согласованность
всех 3 мест); синоним заполнить; ObjectModule пустой (уже); форм нет (уже).

## 5. Проверка

Smoke `_Rarzrabotki\Python\test\test_otchet_zakupki.py`: эталоны прямым запросом
(план/факт: строки и Σ, динамически) → компоновка СКД из .erf → сверка детальных
строк по ВидСтроки и обеих Σ (допуск 0.05). Сборка .erf против BuhBud
(lock → stub-маршрут). Изоляция GUID от отчёта-источника «Отчет по общим названиям в СС».

## 6. Правка v2 (замечание пользователя после первой сборки)

Первая версия тянула ВСЕ закупки карточки по всей фирме. Требование: факт — только
по подразделению структуры. Реализация:
- ВТ `АктивныеОН` расширена до пар `(Подразделение = К.Ссылка.Подразделение, ОбщееНазвание)`;
- обе части UNION получают поле `Подразделение` (план: `К.Ссылка.Подразделение`,
  факт: `Т.Ссылка.Подразделение`);
- факт-фильтр — кортежем: `(Т.Ссылка.Подразделение, Т.Номенклатура.ОбщееНазваниеНоменклатуры)
  В (ВЫБРАТЬ Подразделение, ОбщееНазвание ИЗ АктивныеОН)` — закупки подразделения
  только по карточкам его активных СС;
- группировки варианта: **Подразделение → ОбщееНазвание → Номенклатура → детали**.
Запрос v2 протестирован 1:1 (`test_zakupki_query_v2.py`): план 952 / Σ 10 842 173,71
(не изменился), факт **1 397 / Σ 16 029 751,49**, подразделения факта: МД Вооз ЕМS Мала 2026,
МД МХП ОРІЛЬ, МД ПРООН Черкаси ДСНС (у «МД IRS 2026» проведённых закупок пока нет —
факт по нему пуст, это корректно).

## 6а. Правка v3 (финальная, по референс-отчётам)

Пользователь указал на референсы («ПланФактСС», ERP «А_ПланФактныйПроизводствоПолный»):
оба фильтруют факт ПРОСТО по подразделению структур (все закупки подразделения по любой
привязанной карточке), а не парой «подразделение+карточка» как в v2 — парный фильтр
терял незапланированные закупки. Итоговая реализация v3: ВТ `ПодразделенияСС`
(РАЗЛИЧНЫЕ Подразделение активных структур), факт-фильтр
`Т.Ссылка.Подразделение В (ВЫБРАТЬ ...)` + непустая привязка карточки.
Карточки, встречающиеся только в факте, видны с пустым планом.
Запрос v3 протестирован 1:1 (`test_zakupki_query_v3.py`): план 952 / Σ 10 842 173,71,
факт **1 926 / Σ 18 502 364,91**. Smoke из .erf — точное совпадение.

## 7. Критерии готовности

- [ ] По карточке видно план- и факт-строки с их единицами; ЕдиницаОбщая — в заголовке группы.
- [ ] СуммаФакт с НДС (формула по СуммаВключаетНДС), сопоставима с СС.
- [ ] Расшифровки: план → Структура, факт → документ Поступления.
- [ ] Только активные структуры (жёстко в запросе).
- [ ] GUID отличаются от отчёта-источника; синоним заполнен.
- [ ] Smoke зелёный; .erf в main; закоммичено.
